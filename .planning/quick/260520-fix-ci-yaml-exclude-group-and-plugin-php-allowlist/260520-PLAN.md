---
phase: quick-260520
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/metapixel-qa.yml
  - composer-dependency-analyser.php
autonomous: true
requirements:
  - QUICK-260520-01  # CI workflow: --exclude-group=adapter (drop stale --exclude-testsuite)
  - QUICK-260520-02  # composer-dependency-analyser: allowlist Plugin.php for Lovata DEV_DEPENDENCY_IN_PROD
---

<objective>
Close two CI/QA regressions surfaced by guard tests in tests/Unit/Tooling/.

1. `.github/workflows/metapixel-qa.yml:99` — replace the silent no-op
   `--exclude-testsuite='Metapixel Adapter Tests'` (testsuite removed in Phase 3
   when adapter tests migrated to `#[PHPUnit\Framework\Attributes\Group('adapter')]`)
   with `--exclude-group=adapter`. Drop any residual `Metapixel Adapter Tests`
   string from the file so the stale-suite guard assertion stays green.
2. `composer-dependency-analyser.php` — add `Plugin.php` to the path-scoped
   Lovata allowlist. Plugin.php imports `Lovata\OrdersShopaholic\Models\{CartPosition,Order}`
   in the AdapterRegistry registration block; without an explicit
   `ignoreErrorsOnPackageAndPath` call referencing `Plugin.php`, the analyser
   would (correctly) flag those imports as `DEV_DEPENDENCY_IN_PROD`.

Validation: both `composer test -- --filter=CiWorkflowMatrixTest` and
`composer test -- --filter=ComposerDependencyAnalyserScopeTest` must run all
assertions green.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Swap --exclude-testsuite for --exclude-group=adapter in metapixel-qa.yml Run B</name>
  <files>.github/workflows/metapixel-qa.yml</files>
  <action>
    Edit the "composer qa (minimal — Run B...)" step. Rename the step description from
    `exclude Adapter testsuite` to `exclude adapter group`. Replace the final
    `../../../vendor/bin/pest --configuration phpunit.xml --exclude-testsuite='Metapixel Adapter Tests'`
    line with `--exclude-group=adapter`. Update the inline comment to reference
    Phase 3's #[Group('adapter')] migration WITHOUT containing the literal
    string `Metapixel Adapter Tests` (the stale-string guard assertion regex-fails
    if that substring appears anywhere in the file).
  </action>
  <verify>
    <automated>../../../vendor/bin/pest --filter=CiWorkflowMatrixTest 2>&amp;1 | tail -10</automated>
  </verify>
  <done>
    - All 5 CiWorkflowMatrixTest assertions pass.
    - File contains `--exclude-group=adapter`.
    - File no longer contains the substring `Metapixel Adapter Tests`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Allowlist Plugin.php for Lovata DEV_DEPENDENCY_IN_PROD</name>
  <files>composer-dependency-analyser.php</files>
  <action>
    The ComposerDependencyAnalyserScopeTest regex `/ignoreErrorsOnPackageAndPath[^;]*Plugin\.php/s`
    requires `Plugin.php` to appear inside a SINGLE `ignoreErrorsOnPackageAndPath(...)`
    call statement (semicolon-bounded). Including `__DIR__.'/Plugin.php'` in the
    `$arLovataPaths` array declaration does NOT satisfy the regex because the array
    declaration ends in `;` BEFORE the call.

    Add an explicit per-package `ignoreErrorsOnPackageAndPath($sLovataPackage, __DIR__.'/Plugin.php', [ErrorType::DEV_DEPENDENCY_IN_PROD])`
    call inside the existing package foreach, right after the inner `$arLovataPaths`
    loop. Path-scope the file directly (not the plugin root directory) so the allowlist
    stays exact — `updates/*` and `config/*` continue to be flagged if they ever import Lovata.
  </action>
  <verify>
    <automated>../../../vendor/bin/pest --filter=ComposerDependencyAnalyserScopeTest 2>&amp;1 | tail -15</automated>
  </verify>
  <done>
    - All 8 ComposerDependencyAnalyserScopeTest assertions pass (the previously-failing
      `test_plugin_php_is_covered_by_lovata_allowlist_for_top_level_imports` now passes).
    - `php -l composer-dependency-analyser.php` reports no syntax errors.
    - No bare `ignoreErrorsOnPackage` (without `AndPath`) for any Lovata package.
  </done>
</task>

</tasks>

<verification>
- `../../../vendor/bin/pest --filter=CiWorkflowMatrixTest` — 5 passed, 0 failed.
- `../../../vendor/bin/pest --filter=ComposerDependencyAnalyserScopeTest` — 8 passed, 0 failed.
- `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/metapixel-qa.yml'))"` parses without error.
- `php -l composer-dependency-analyser.php` — no syntax errors.
- `composer deps` not runnable locally (binary lives in plugin's own `vendor/bin` which is uninstalled
  in this checkout); CI workflow installs it via `composer install` per the workflow's per-job step.
</verification>

<success_criteria>
- CI Run B no longer fires the stale `--exclude-testsuite` no-op; adapter tests are excluded
  by `#[Group('adapter')]` filter as intended.
- Plugin.php Lovata imports are explicitly allowlisted; analyser stays exact.
- Both Tooling guard tests stay green end-to-end (no skipped, no warnings).
- One atomic commit on metapixel plugin master touching exactly 2 files.
</success_criteria>

<output>
Write 260520-SUMMARY.md with the exact diffs (or excerpted snippets), the pest
test tails for both filters, and the commit SHA.
</output>
