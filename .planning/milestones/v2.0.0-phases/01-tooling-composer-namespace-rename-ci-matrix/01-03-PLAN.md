---
phase: 01-tooling-composer-namespace-rename-ci-matrix
plan: 03
type: execute
wave: 3
depends_on:
  - 01-01
  - 01-02
files_modified:
  - plugins/logingrupa/metapixel/phpunit.xml
  - plugins/logingrupa/metapixel/tests/Pest.php
  - plugins/logingrupa/metapixel/tests/MetapixelTestCase.php
  - plugins/logingrupa/metapixel/tests/ShopaholicAdapterTestCase.php
  - plugins/logingrupa/metapixel/tests/Unit/PluginSanityTest.php
  - plugins/logingrupa/metapixel/tests/Unit/PluginScaffoldTest.php
  - plugins/logingrupa/metapixel/.github/workflows/metapixel-qa.yml
autonomous: true
requirements:
  - TOOL-08
  - TOOL-09
must_haves:
  truths:
    - "`tests/MetapixelTestCase.php` exists and boots OctoberCMS in SQLite-in-memory WITHOUT loading any Lovata cart-plugin migration."
    - "`tests/ShopaholicAdapterTestCase.php` exists, extends MetapixelTestCase, and pre-wires the hermetic Lovata Orders table for Run A — but no test in this plan exercises it (Phase 3 uses it)."
    - "A trivial sanity Pest test under `tests/Unit/` extends MetapixelTestCase, asserts `Logingrupa\\Metapixel\\Plugin` loads, and passes."
    - "`composer qa` (full chain pint-test then analyse then phpmd then deps-check then test-cov) exits 0 on the empty scaffold."
    - "`.github/workflows/metapixel-qa.yml` defines a CI matrix: `php: [8.3, 8.4]` x `install: [full-lovata, minimal]` = 4 cells. Run A (full-lovata) enforces coverage ≥ 90% gate; Run B (minimal) runs only MetapixelTestCase-extending tests and has no coverage gate."
    - "On the empty scaffold, all four CI cells pass (coverage gate trivially met because the only loaded code path is Plugin.php's `pluginDetails()`)."
  artifacts:
    - path: "plugins/logingrupa/metapixel/phpunit.xml"
      provides: "Pest 4 + PHPUnit 12 config: SQLite-in-memory env, source coverage scope, two testsuites (Unit, Feature)."
      contains: "<testsuite"
    - path: "plugins/logingrupa/metapixel/tests/Pest.php"
      provides: "Pest binding: MetapixelTestCase to tests/Unit + tests/Feature; ShopaholicAdapterTestCase to tests/Unit/Adapter/Shopaholic + tests/Feature/Adapter/Shopaholic (Phase 3 will land tests there)."
      contains: "uses(MetapixelTestCase::class)"
    - path: "plugins/logingrupa/metapixel/tests/MetapixelTestCase.php"
      provides: "TOOL-08 base — no cart-plugin deps."
      contains: "abstract class MetapixelTestCase"
    - path: "plugins/logingrupa/metapixel/tests/ShopaholicAdapterTestCase.php"
      provides: "TOOL-08 base — Lovata Orders hermetic table boot for Run A."
      contains: "extends MetapixelTestCase"
    - path: "plugins/logingrupa/metapixel/tests/Unit/PluginSanityTest.php"
      provides: "Smoke test proving harness boots."
      contains: "Logingrupa\\Metapixel\\Plugin"
    - path: "plugins/logingrupa/metapixel/.github/workflows/metapixel-qa.yml"
      provides: "TOOL-09 CI matrix workflow."
      contains: "matrix:"
  key_links:
    - from: "plugins/logingrupa/metapixel/phpunit.xml"
      to: "tests/Unit/ and tests/Feature/"
      via: "testsuite directory entries"
      pattern: "<directory>./tests/Unit</directory>"
    - from: "plugins/logingrupa/metapixel/tests/Pest.php"
      to: "tests/MetapixelTestCase.php"
      via: "uses() binding"
      pattern: "uses(MetapixelTestCase::class)"
    - from: "plugins/logingrupa/metapixel/.github/workflows/metapixel-qa.yml"
      to: "plugins/logingrupa/metapixel/composer.json scripts.qa"
      via: "matrix job step `composer qa`"
      pattern: "composer qa"
---

<objective>
Ship the v2.0 test scaffold (TOOL-08) and CI matrix workflow (TOOL-09) so a fresh clone runs `composer qa` and exits 0 on the empty plugin, and so every push exercises four CI cells: PHP 8.3 x full-lovata, PHP 8.4 x full-lovata, PHP 8.3 x minimal, PHP 8.4 x minimal. The two-tier test base (MetapixelTestCase no-cart-deps + ShopaholicAdapterTestCase Lovata-Orders-hermetic) prevents P-12 (generic core tests booting Shopaholic) and lays the contract for Phase 2 + Phase 3 adapter tests.

Purpose: closes Phase 1 success criteria 1, 2, 5. Combined with plans 01-01 + 01-02, this plan delivers the goal: "fresh clone passes composer qa on PHP 8.3 + 8.4 in both full-Lovata and minimal install matrices."

Output: phpunit.xml, two test bases, Pest.php binding, one smoke test, and the GitHub Actions matrix workflow.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@plugins/logingrupa/metapixel/.planning/PROJECT.md
@plugins/logingrupa/metapixel/.planning/ROADMAP.md
@plugins/logingrupa/metapixel/.planning/REQUIREMENTS.md
@plugins/logingrupa/metapixel/.planning/research/STACK.md
@plugins/logingrupa/metapixel/.planning/research/PITFALLS.md
@plugins/logingrupa/metapixel/.planning/phases/01-tooling-composer-namespace-rename-ci-matrix/01-01-SUMMARY.md
@plugins/logingrupa/metapixel/.planning/phases/01-tooling-composer-namespace-rename-ci-matrix/01-02-SUMMARY.md
@CLAUDE.md

<interfaces>
Locked invariants from plans 01-01 and 01-02:

- Plugin dir: plugins/logingrupa/metapixel/
- Namespace: Logingrupa\Metapixel; test namespace Logingrupa\Metapixel\Tests
- PSR-4 autoload-dev maps tests/ to Logingrupa\Metapixel\Tests\\ (set in plan 01-01 task 5)
- composer.json scripts.qa = pint-test, analyse, phpmd, deps-check, test-cov (set in plan 01-02 task 6)
- phpstan.neon paths: only Plugin.php; tests in excludePaths
- vendor/bin/pest exists (root-vendored, already in repo root require-dev as part of v1.x)
- October bootstrap available at modules/system/tests/bootstrap.php (relative to repo root)

v1.x reference files retrievable via `git show legacy/v1.1.1:plugins/logingrupa/metapixelshopaholic/<file>`:
- tests/MetapixelTestCase.php (425 lines) — heavily Shopaholic-coupled hermetic helpers (bootOrdersTable, bootEventLogTable, bootOrdersStatuses)
- tests/Pest.php — uses() binding pattern
- phpunit.xml — testsuites, bootstrap, env force
- .github/workflows/metapixel-qa.yml — single-cell PHP 8.4 only; needs matrix expansion

v2.0 deltas from v1.x:
- MetapixelTestCase: STRIP all Shopaholic-coupled hermetic helpers (bootOrdersTable, bootOrdersStatuses, bootEventLogTable, dropHermeticSchemas for Shopaholic tables, OrderFixtures references). Keep ONLY the OctoberCMS harness boot (createApplication, system_settings provision, mail-pretend, model-event-listener flush). The shopaholic-specific helpers MOVE to ShopaholicAdapterTestCase.
- MetapixelTestCase: STRIP all phase-numbered comments / CR refs / Plan-XX refs / autoMigrate-history narrative (project lock).
- MetapixelTestCase: REPLACE `Logingrupa\Metapixelshopaholic\Classes\Helper\PluginGuard::flush()` line in `flushPluginSingletons()` with a comment placeholder — Phase 2 ADAP-03 will register the AdapterRegistry singleton which becomes the flush target.
- ShopaholicAdapterTestCase: NEW — bootOrdersTable + bootOrdersStatuses helpers MOVE here. Class extends MetapixelTestCase. No tests yet — Phase 3 SHOP-* lands them.
- Pest.php: bind MetapixelTestCase to tests/Unit + tests/Feature; bind ShopaholicAdapterTestCase to tests/Unit/Adapter/Shopaholic + tests/Feature/Adapter/Shopaholic (subdirs not yet present — Pest tolerates missing dirs).
- phpunit.xml: KEEP shape (bootstrap, testsuites Unit + Feature, env force SQLite). UPDATE testsuite names to drop "shopaholic" suffix.
- CI workflow: EXPAND from single-cell to 4-cell matrix per TOOL-09.
</interfaces>

<reference_v1_files>
v1.x MetapixelTestCase had 425 lines of mostly Shopaholic hermetic plumbing. v2.0 MetapixelTestCase should be ~120 lines max — just the harness boot. The hermetic helpers belong in the adapter-specific subclass.

The single MOST important behavioral lift: the `createApplication()` method's SQLite force-config block (forces test DB after Laravel dotenv loader, prevents Lovata.GoodNews ArticleModelHandler from hitting prod MySQL during deferred boot). That block is environment-specific to THIS repo and MUST carry forward verbatim.

The OctoberCMS test-harness concerns to KEEP in MetapixelTestCase:
- TestCase (Illuminate base) + InteractsWithAuthentication + PerformsMigrations + PerformsRegistrations
- autoMigrate=false, autoRegister=false (HR-02 hermetic opt-in pattern)
- createApplication() with the SQLite force-config + system_settings early-provision + auth singleton bind
- setUp() with mail-pretend + parent boot
- tearDown() with flushModelEventListeners + dropHermeticSchemas + parent
- flushModelEventListeners() (generic — required for every test)
- dropHermeticSchemas() — KEEP only the generic drops (system_settings, migrations). Shopaholic table drops MOVE to ShopaholicAdapterTestCase.

The concerns to STRIP from MetapixelTestCase (move to ShopaholicAdapterTestCase):
- bootOrdersTable, bootOrdersStatuses, bootEventLogTable
- All Lovata table references in dropHermeticSchemas
- The PluginGuard::flush call (will be replaced by AdapterRegistry::flush in Phase 2)
- bootTranslations (phase 3.1-08 anchor; v2.0 will re-derive when first integration test needs it)
- bootMigrationsTable (phase 3.1-08 anchor; v2.0 re-derives when needed)
</reference_v1_files>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write phpunit.xml</name>
  <files>
    plugins/logingrupa/metapixel/phpunit.xml
  </files>
  <action>
Reference: `git show legacy/v1.1.1:plugins/logingrupa/metapixelshopaholic/phpunit.xml`. Carry the shape forward; drop the source includes for not-yet-existing dirs.

Write `plugins/logingrupa/metapixel/phpunit.xml`:

    <?xml version="1.0" encoding="UTF-8"?>
    <phpunit backupGlobals="false"
             bootstrap="../../../modules/system/tests/bootstrap.php"
             colors="true"
             processIsolation="false"
             stopOnFailure="false"
             beStrictAboutOutputDuringTests="false"
    >
        <testsuites>
            <testsuite name="Metapixel Unit Tests">
                <directory>./tests/Unit</directory>
            </testsuite>
            <testsuite name="Metapixel Feature Tests">
                <directory>./tests/Feature</directory>
            </testsuite>
        </testsuites>
        <source>
            <include>
                <file>./Plugin.php</file>
            </include>
        </source>
        <php>
            <env name="APP_ENV" value="testing" force="true"/>
            <env name="CACHE_DRIVER" value="array" force="true"/>
            <env name="SESSION_DRIVER" value="array" force="true"/>
            <env name="DB_CONNECTION" value="sqlite" force="true"/>
            <env name="DB_DATABASE" value=":memory:" force="true"/>
        </php>
    </phpunit>

Notes for executor:
- `<source><include>` lists only Plugin.php. When Phase 2+ lands classes/, models/, components/, middleware/, controllers/, console/, each phase reopens this list. Pattern matches phpstan paths + phpmd command — same reopen discipline.
- Testsuite names dropped "shopaholic" suffix per TOOL-03 generic naming.
- Bootstrap path `../../../modules/system/tests/bootstrap.php` resolves from `plugins/logingrupa/metapixel/` to the repo's modules/system bootstrap (October's test harness entrypoint).
- NO COMMIT in this task — bundled at task 7.
  </action>
  <verify>
    <automated>test -f plugins/logingrupa/metapixel/phpunit.xml &amp;&amp; xmllint --noout plugins/logingrupa/metapixel/phpunit.xml &amp;&amp; grep -q 'Metapixel Unit Tests' plugins/logingrupa/metapixel/phpunit.xml &amp;&amp; ! grep -qi 'shopaholic' plugins/logingrupa/metapixel/phpunit.xml</automated>
  </verify>
  <done>phpunit.xml present, xmllint-valid, testsuite names drop "shopaholic", `<source><include>` lists only Plugin.php.</done>
</task>

<task type="auto">
  <name>Task 2: Write MetapixelTestCase (TOOL-08 tier 1 — no cart-plugin deps)</name>
  <files>
    plugins/logingrupa/metapixel/tests/MetapixelTestCase.php
  </files>
  <action>
Reference: `git show legacy/v1.1.1:plugins/logingrupa/metapixelshopaholic/tests/MetapixelTestCase.php`. Strip all Shopaholic-coupled helpers; keep only OctoberCMS harness plumbing.

Write `plugins/logingrupa/metapixel/tests/MetapixelTestCase.php`:

    <?php

    namespace Logingrupa\Metapixel\Tests;

    use Backend\Classes\AuthManager;
    use Illuminate\Contracts\Console\Kernel;
    use Illuminate\Foundation\Testing\TestCase;
    use Illuminate\Support\Facades\Schema;
    use October\Rain\Database\Model;
    use October\Rain\Database\Model as ActiveRecord;
    use October\Rain\Database\Pivot;
    use October\Tests\Concerns\InteractsWithAuthentication;
    use October\Tests\Concerns\PerformsMigrations;
    use October\Tests\Concerns\PerformsRegistrations;

    /**
     * Base PHPUnit 12 / Pest 4 test case for the Logingrupa.Metapixel plugin core.
     *
     * Boots OctoberCMS in SQLite-in-memory with no cart-plugin migrations.
     * Cart-plugin hermetic helpers live in subclasses (ShopaholicAdapterTestCase,
     * future MallAdapterTestCase, etc.) so Run B (minimal install) can execute
     * MetapixelTestCase-extending tests without Lovata cart packages installed.
     */
    abstract class MetapixelTestCase extends TestCase
    {
        use InteractsWithAuthentication;
        use PerformsMigrations;
        use PerformsRegistrations;

        /** @var bool */
        protected $autoMigrate = false;

        /** @var bool */
        protected $autoRegister = false;

        public function createApplication()
        {
            $app = require __DIR__.'/../../../../bootstrap/app.php';
            $app->make(Kernel::class)->bootstrap();

            // Force the test DB configuration AFTER kernel bootstrap.
            // Laravel's dotenv loader overwrites the PHPUnit <env force> directives
            // during bootstrap, so we re-pin SQLite-in-memory here. Without this,
            // deferred plugin boot callbacks (e.g. Lovata.GoodNews ArticleModelHandler)
            // can hit the prod MySQL connection in the dev environment.
            config([
                'app.env'           => 'testing',
                'database.default'  => 'sqlite',
                'database.connections.sqlite' => [
                    'driver'                  => 'sqlite',
                    'database'                => ':memory:',
                    'prefix'                  => '',
                    'foreign_key_constraints' => false,
                ],
                'cache.default'    => 'array',
                'session.driver'   => 'array',
            ]);

            $app['db']->purge();

            $app->singleton('auth', function ($app) {
                $app['auth.loaded'] = true;

                return AuthManager::instance();
            });

            $this->ensureSystemSettingsTable($app);

            return $app;
        }

        /**
         * Provision system_settings early so deferred plugin boot callbacks that
         * read SettingModel::get() do not crash on a missing table.
         */
        private function ensureSystemSettingsTable($app): void
        {
            $obSchema = $app['db']->connection()->getSchemaBuilder();
            if ($obSchema->hasTable('system_settings')) {
                return;
            }

            $obSchema->create('system_settings', function ($obTable): void {
                $obTable->increments('id');
                $obTable->string('item')->nullable()->index();
                $obTable->mediumtext('value')->nullable();
                $obTable->unsignedInteger('site_id')->nullable();
                $obTable->unsignedInteger('site_root_id')->nullable();
                $obTable->unsignedInteger('site_group_id')->nullable();
            });
        }

        protected function setUp(): void
        {
            $this->pluginTestCaseMigratedPlugins = [];
            $this->pluginTestCaseLoadedPlugins = [];

            parent::setUp();

            if ($this->autoRegister === true) {
                $this->loadCurrentPlugin();
            }

            if ($this->autoMigrate === true) {
                $this->migrateModules();
                $this->migrateCurrentPlugin();
            }

            \Mail::pretend();
        }

        protected function tearDown(): void
        {
            $this->flushModelEventListeners();
            $this->dropHermeticSchemas();
            parent::tearDown();
            unset($this->app);
        }

        /**
         * Drop hermetic tables in tearDown. Subclasses override to drop their
         * own cart-plugin-specific tables (e.g. lovata_orders_shopaholic_*).
         */
        protected function dropHermeticSchemas(): void
        {
            Schema::dropIfExists('system_settings');
        }

        protected function flushModelEventListeners(): void
        {
            foreach (get_declared_classes() as $sClass) {
                if ($sClass === Pivot::class) {
                    continue;
                }

                $obReflect = new \ReflectionClass($sClass);
                if (
                    ! $obReflect->isInstantiable() ||
                    ! $obReflect->isSubclassOf(Model::class) ||
                    $obReflect->isSubclassOf(Pivot::class)
                ) {
                    continue;
                }

                $sClass::flushEventListeners();
            }

            ActiveRecord::flushEventListeners();
        }

        protected function guessPluginCodeFromTest()
        {
            $obReflect = new \ReflectionClass($this);
            $sPath = $obReflect->getFilename();
            $sPluginPath = $this->app->pluginsPath();

            if (strpos($sPath, $sPluginPath) === 0) {
                $sResult = ltrim(str_replace('\\', '/', substr($sPath, strlen($sPluginPath))), '/');

                return implode('.', array_slice(explode('/', $sResult), 0, 2));
            }

            return false;
        }

        protected function isAppCodeFromTest()
        {
            return false;
        }
    }

Notes for executor:
- The bootstrap path `__DIR__.'/../../../../bootstrap/app.php'` resolves from `plugins/logingrupa/metapixel/tests/` four levels up to repo-root `bootstrap/app.php`. Verify by `realpath` after writing.
- Hungarian notation: `$obReflect`, `$sPath`, `$sClass`, `$sResult`, `$obSchema`, `$obTable` — applied to every local var.
- No phase markers, no CR refs, no plan-XX refs. The docblock is one short paragraph (Laravel short docblock style per project lock).
- NO COMMIT in this task — bundled at task 7.
  </action>
  <verify>
    <automated>test -f plugins/logingrupa/metapixel/tests/MetapixelTestCase.php &amp;&amp; php -l plugins/logingrupa/metapixel/tests/MetapixelTestCase.php | grep -q 'No syntax errors' &amp;&amp; ! grep -qiE '(shopaholic|phase\s*[0-9]|CR-[0-9]|Plan\s*[0-9]+-[0-9]+|REFAC-[0-9])' plugins/logingrupa/metapixel/tests/MetapixelTestCase.php</automated>
  </verify>
  <done>MetapixelTestCase.php present, php -l clean, no shopaholic references, no phase markers. Namespace `Logingrupa\Metapixel\Tests`.</done>
</task>

<task type="auto">
  <name>Task 3: Write ShopaholicAdapterTestCase (TOOL-08 tier 2 — Lovata Orders hermetic)</name>
  <files>
    plugins/logingrupa/metapixel/tests/ShopaholicAdapterTestCase.php
  </files>
  <action>
The Lovata-Orders hermetic helpers live here so Run A (full-lovata) can execute Shopaholic-adapter tests, while Run B (minimal) skips this entire subclass.

Reference (carry the table shape forward verbatim — it matches the Lovata Orders schema as of v1.33): `git show legacy/v1.1.1:plugins/logingrupa/metapixelshopaholic/tests/MetapixelTestCase.php` lines 250-313 (`bootOrdersTable`, `bootOrdersStatuses`).

Write `plugins/logingrupa/metapixel/tests/ShopaholicAdapterTestCase.php`:

    <?php

    namespace Logingrupa\Metapixel\Tests;

    use Illuminate\Support\Facades\Schema;

    /**
     * Test case for Logingrupa.Metapixel ShopaholicAdapter integration.
     *
     * Provisions a minimal hermetic Lovata Orders schema in SQLite-in-memory so
     * adapter tests do not need the real Lovata.OrdersShopaholic migration chain
     * (slow + SQLite-unfriendly for some indexed column drops).
     *
     * Tests under tests/Unit/Adapter/Shopaholic and tests/Feature/Adapter/Shopaholic
     * extend this class via the Pest.php uses() binding. Run B (minimal install)
     * does NOT execute the Adapter subdirectory testsuite.
     */
    abstract class ShopaholicAdapterTestCase extends MetapixelTestCase
    {
        /**
         * Provision the minimal lovata_orders_shopaholic_orders table — only the
         * columns ShopaholicAdapter touches. Mirrors the Lovata v1.33 schema for
         * status_id, secret_key, total_price_value, currency_id, customer fields,
         * site_id.
         */
        protected function bootOrdersTable(): void
        {
            if (Schema::hasTable('lovata_orders_shopaholic_orders')) {
                return;
            }

            Schema::create('lovata_orders_shopaholic_orders', function ($obTable): void {
                $obTable->increments('id');
                $obTable->integer('status_id')->nullable();
                $obTable->string('order_number')->nullable();
                $obTable->string('secret_key')->nullable();
                $obTable->decimal('total_price_value', 15, 2)->nullable();
                $obTable->integer('currency_id')->nullable();
                $obTable->string('email')->nullable();
                $obTable->string('phone')->nullable();
                $obTable->string('name')->nullable();
                $obTable->string('last_name')->nullable();
                $obTable->integer('site_id')->nullable();
                $obTable->timestamps();
            });
        }

        /**
         * Provision lovata_orders_shopaholic_statuses with the canonical Lovata
         * statuses plus the custom new-payment-received code (status_id=5) that
         * Settings.paid_status_code defaults to.
         */
        protected function bootOrdersStatuses(): void
        {
            if (! Schema::hasTable('lovata_orders_shopaholic_statuses')) {
                Schema::create('lovata_orders_shopaholic_statuses', function ($obTable): void {
                    $obTable->increments('id');
                    $obTable->string('name');
                    $obTable->string('code')->unique();
                    $obTable->string('color')->nullable();
                    $obTable->text('preview_text')->nullable();
                    $obTable->boolean('is_user_show')->default(true);
                    $obTable->unsignedInteger('user_status_id')->nullable();
                    $obTable->integer('sort_order')->default(0);
                    $obTable->timestamps();
                });
            }

            \DB::table('lovata_orders_shopaholic_statuses')->insertOrIgnore([
                ['id' => 1, 'name' => 'New',                  'code' => 'new',                  'sort_order' => 1, 'is_user_show' => 1],
                ['id' => 2, 'name' => 'In progress',          'code' => 'in_progress',          'sort_order' => 2, 'is_user_show' => 1],
                ['id' => 3, 'name' => 'Complete',             'code' => 'complete',             'sort_order' => 3, 'is_user_show' => 1],
                ['id' => 4, 'name' => 'Canceled',             'code' => 'canceled',             'sort_order' => 4, 'is_user_show' => 1],
                ['id' => 5, 'name' => 'New payment received', 'code' => 'new-payment-received', 'sort_order' => 5, 'is_user_show' => 1],
            ]);
        }

        protected function dropHermeticSchemas(): void
        {
            Schema::dropIfExists('lovata_orders_shopaholic_orders');
            Schema::dropIfExists('lovata_orders_shopaholic_statuses');

            parent::dropHermeticSchemas();
        }
    }

Notes for executor:
- The class is empty of business helpers BEYOND the two boot/drop pairs. Phase 2 ADAP-* and Phase 3 SHOP-* add more (bootEventLogTable, bootFailedEventsTable, fixtures, etc.) when they need them.
- No phase markers, no CR refs.
- NO COMMIT in this task — bundled at task 7.
  </action>
  <verify>
    <automated>test -f plugins/logingrupa/metapixel/tests/ShopaholicAdapterTestCase.php &amp;&amp; php -l plugins/logingrupa/metapixel/tests/ShopaholicAdapterTestCase.php | grep -q 'No syntax errors' &amp;&amp; grep -q 'extends MetapixelTestCase' plugins/logingrupa/metapixel/tests/ShopaholicAdapterTestCase.php</automated>
  </verify>
  <done>ShopaholicAdapterTestCase.php present, php -l clean, extends MetapixelTestCase, holds bootOrdersTable + bootOrdersStatuses + table-drop override.</done>
</task>

<task type="auto">
  <name>Task 4: Write tests/Pest.php — bind test bases to directories</name>
  <files>
    plugins/logingrupa/metapixel/tests/Pest.php
  </files>
  <action>
Pest's `uses()->in(...)` binds a base test case to every test file under a directory. Per P-12 + P-19 prevention, MetapixelTestCase binds to Unit + Feature (generic); ShopaholicAdapterTestCase binds to Unit/Adapter/Shopaholic + Feature/Adapter/Shopaholic (cart-coupled).

Write `plugins/logingrupa/metapixel/tests/Pest.php`:

    <?php

    use Logingrupa\Metapixel\Tests\MetapixelTestCase;
    use Logingrupa\Metapixel\Tests\ShopaholicAdapterTestCase;

    /*
    |--------------------------------------------------------------------------
    | Test Case Bindings
    |--------------------------------------------------------------------------
    |
    | MetapixelTestCase: every test under tests/Unit and tests/Feature that
    | does not live under an Adapter/<cart> subdirectory.
    |
    | ShopaholicAdapterTestCase: every test under tests/Unit/Adapter/Shopaholic
    | and tests/Feature/Adapter/Shopaholic. Only loaded in CI Run A (full-lovata
    | install). Run B (minimal install) excludes the Adapter subdirectory via
    | --testsuite selection.
    |
    */

    if (function_exists('uses') && class_exists(MetapixelTestCase::class, false)) {
        uses(MetapixelTestCase::class)->in(__DIR__.'/Unit', __DIR__.'/Feature');
    }

    if (function_exists('uses') && class_exists(ShopaholicAdapterTestCase::class, false)) {
        uses(ShopaholicAdapterTestCase::class)->in(
            __DIR__.'/Unit/Adapter/Shopaholic',
            __DIR__.'/Feature/Adapter/Shopaholic',
        );
    }

Notes for executor:
- The Adapter subdirs don't exist yet — Phase 3 lands them. Pest tolerates `uses()->in('/nonexistent/dir')` gracefully (no error).
- The MetapixelTestCase `uses()->in(__DIR__.'/Unit', __DIR__.'/Feature')` catches tests/Unit/PluginSanityTest.php immediately — first test files land in this plan.
- The class_exists guards mirror the v1.x defensive pattern (Pest binding is best-effort, never blocks).
- NO COMMIT in this task — bundled at task 7.
  </action>
  <verify>
    <automated>test -f plugins/logingrupa/metapixel/tests/Pest.php &amp;&amp; php -l plugins/logingrupa/metapixel/tests/Pest.php | grep -q 'No syntax errors' &amp;&amp; grep -q 'uses(MetapixelTestCase::class)' plugins/logingrupa/metapixel/tests/Pest.php &amp;&amp; grep -q 'uses(ShopaholicAdapterTestCase::class)' plugins/logingrupa/metapixel/tests/Pest.php</automated>
  </verify>
  <done>Pest.php binds both test bases to their respective directories.</done>
</task>

<task type="auto">
  <name>Task 5: Write tests/Unit/PluginSanityTest.php — smoke test proving harness boots</name>
  <files>
    plugins/logingrupa/metapixel/tests/Unit/PluginSanityTest.php
  </files>
  <action>
A single smoke test proves the whole stack works: PSR-4 autoload resolves Plugin, the test harness boots, the Pest binding wires MetapixelTestCase.

Write `plugins/logingrupa/metapixel/tests/Unit/PluginSanityTest.php`:

    <?php

    use Logingrupa\Metapixel\Plugin;
    use Logingrupa\Metapixel\Tests\MetapixelTestCase;

    /*
     * Note: PHPUnit's classic `extends MetapixelTestCase` model is used here
     * because Pest's $rootPath resolution under `vendor/bin/pest --configuration
     * phpunit.xml` does not always pick up the Pest.php binding. The explicit
     * extends keeps this smoke test working under any Pest invocation shape.
     */

    final class PluginSanityTest extends MetapixelTestCase
    {
        public function test_plugin_class_loads_via_psr4_autoload(): void
        {
            $this->assertTrue(class_exists(Plugin::class));
        }

        public function test_plugin_details_returns_lang_keys_under_renamed_namespace(): void
        {
            $obPlugin = new Plugin();
            $arDetails = $obPlugin->pluginDetails();

            $this->assertSame('logingrupa.metapixel::lang.plugin.name',        $arDetails['name']);
            $this->assertSame('logingrupa.metapixel::lang.plugin.description', $arDetails['description']);
            $this->assertSame('Logingrupa',                                    $arDetails['author']);
        }
    }

Notes for executor:
- File uses PHPUnit classic style (not Pest's `it()` / `test()` functional shape) because the Pest.php binding may not fire under the configured invocation. The class-based form works under any Pest configuration.
- `final class` because there's no reason for a sanity test to be extensible.
- NO COMMIT in this task — bundled at task 7.
- Optionally also write `tests/Unit/PluginScaffoldTest.php` if you want to assert composer.json shape (e.g. PSR-4 prefix, lovata in suggest:). Skip if it duplicates work covered by composer-dependency-analyser.

Decision: SKIP PluginScaffoldTest.php for this plan. PSR-4 / composer shape is enforced by composer install + deps-check at the qa layer. A scaffold test would be redundant and would extend the must_haves artifact list with no payoff.

Update the plan frontmatter `files_modified` BEFORE commit: remove `plugins/logingrupa/metapixel/tests/Unit/PluginScaffoldTest.php` (it stays listed in the original plan but the actual commit omits it). If `files_modified` drift is a problem, the executor edits the plan frontmatter as part of task 7 to remove the line.
  </action>
  <verify>
    <automated>test -f plugins/logingrupa/metapixel/tests/Unit/PluginSanityTest.php &amp;&amp; php -l plugins/logingrupa/metapixel/tests/Unit/PluginSanityTest.php | grep -q 'No syntax errors' &amp;&amp; grep -q 'class_exists(Plugin::class)' plugins/logingrupa/metapixel/tests/Unit/PluginSanityTest.php</automated>
  </verify>
  <done>PluginSanityTest.php present, php -l clean, asserts Plugin loads + pluginDetails returns lang keys under the renamed namespace.</done>
</task>

<task type="auto">
  <name>Task 6: Write .github/workflows/metapixel-qa.yml — TOOL-09 CI matrix</name>
  <files>
    plugins/logingrupa/metapixel/.github/workflows/metapixel-qa.yml
  </files>
  <action>
v1.x had a single-cell PHP 8.4 workflow. v2.0 needs a 2x2 matrix: php [8.3, 8.4] x install [full-lovata, minimal]. Run A (full-lovata) runs the full suite + 90% coverage gate. Run B (minimal) runs only MetapixelTestCase-extending tests + no coverage gate (P-20 prevention).

Reference v1.x: `git show legacy/v1.1.1:plugins/logingrupa/metapixelshopaholic/.github/workflows/metapixel-qa.yml`

Write `plugins/logingrupa/metapixel/.github/workflows/metapixel-qa.yml`:

    name: metapixel-qa

    on:
      push:
        branches: [master]
      pull_request:
        paths:
          - 'plugins/logingrupa/metapixel/**'
          - '.github/workflows/metapixel-qa.yml'

    permissions:
      contents: read

    jobs:
      qa:
        name: composer qa (PHP ${{ matrix.php }} / ${{ matrix.install }})
        runs-on: ubuntu-latest
        permissions:
          contents: read

        strategy:
          fail-fast: false
          matrix:
            php: ['8.3', '8.4']
            install: [full-lovata, minimal]

        steps:
          - name: Checkout
            uses: actions/checkout@v4

          - name: Setup PHP ${{ matrix.php }}
            uses: shivammathur/setup-php@v2
            with:
              php-version: ${{ matrix.php }}
              tools: composer
              coverage: xdebug
              extensions: dom, json, libxml, mbstring, pdo, pdo_sqlite, sqlite3, intl, fileinfo

          - name: composer install (repo root, full Lovata)
            if: matrix.install == 'full-lovata'
            run: composer install --no-progress --prefer-dist --no-interaction

          - name: composer install (repo root, minimal — Lovata cart plugins removed)
            if: matrix.install == 'minimal'
            run: |
              # Remove Lovata cart plugins from the repo-root composer.json's require/require-dev
              # so the install does not load them. This mirrors a marketplace install where the
              # operator has NOT yet installed lovata/shopaholic / lovata/ordersshopaholic / lovata/buddies.
              composer remove --dev --no-update --no-interaction \
                lovata/shopaholic-plugin \
                lovata/ordersshopaholic-plugin \
                lovata/buddies-plugin || true
              composer remove --no-update --no-interaction \
                lovata/shopaholic-plugin \
                lovata/ordersshopaholic-plugin \
                lovata/buddies-plugin || true
              composer install --no-progress --prefer-dist --no-interaction

          - name: composer install (plugin, full Lovata)
            if: matrix.install == 'full-lovata'
            working-directory: plugins/logingrupa/metapixel
            run: composer install --no-progress --prefer-dist --no-interaction

          - name: composer install (plugin, minimal)
            if: matrix.install == 'minimal'
            working-directory: plugins/logingrupa/metapixel
            run: |
              # Plugin composer.json keeps Lovata in require-dev for the test-suite-with-cart shape.
              # For Run B, strip the cart packages from the plugin's require-dev so pest under the
              # plugin scope does not load adapter tests that pull in Lovata classes.
              composer remove --dev --no-update --no-interaction \
                lovata/shopaholic-plugin \
                lovata/ordersshopaholic-plugin \
                lovata/buddies-plugin || true
              composer install --no-progress --prefer-dist --no-interaction

          - name: composer qa (full Lovata — Run A, coverage gate)
            if: matrix.install == 'full-lovata'
            working-directory: plugins/logingrupa/metapixel
            run: |
              composer run-script pint-test
              composer run-script analyse
              composer run-script phpmd
              composer run-script deps-check
              # Run test-cov with coverage gate. Pest 4 honors --min on the line coverage threshold.
              ../../../vendor/bin/pest --configuration phpunit.xml --coverage --min=90

          - name: composer qa (minimal — Run B, no coverage gate, exclude Adapter testsuite)
            if: matrix.install == 'minimal'
            working-directory: plugins/logingrupa/metapixel
            run: |
              composer run-script pint-test
              composer run-script analyse
              composer run-script phpmd
              composer run-script deps-check
              # Minimal install excludes the Adapter testsuite (Lovata-coupled tests).
              ../../../vendor/bin/pest --configuration phpunit.xml --exclude-testsuite='Metapixel Adapter Tests'

Notes for executor:
- The 4-cell matrix names: `composer qa (PHP 8.3 / full-lovata)`, `composer qa (PHP 8.4 / full-lovata)`, `composer qa (PHP 8.3 / minimal)`, `composer qa (PHP 8.4 / minimal)`. GitHub renders these clearly in the PR status checks.
- `fail-fast: false` — one cell failing should not cancel the others; the operator wants to see all failure modes in one CI run.
- `--exclude-testsuite='Metapixel Adapter Tests'` — Run B references a testsuite that does not exist YET (Phase 3 lands `tests/Unit/Adapter/Shopaholic` + `tests/Feature/Adapter/Shopaholic`). For plan 01-03 on the empty scaffold, the exclude is a no-op. Phase 3 SHOP-* adds a third testsuite entry to phpunit.xml when adapter tests land:
        <testsuite name="Metapixel Adapter Tests">
            <directory>./tests/Unit/Adapter</directory>
            <directory>./tests/Feature/Adapter</directory>
        </testsuite>
- Coverage `--min=90` on the empty scaffold passes trivially (only Plugin.php is in `<source><include>`, pluginDetails is the one method, and PluginSanityTest exercises it — 100% line coverage).
- P-20 prevention: coverage gate ONLY on Run A. Run B has no coverage step.
- The composer-remove dance on Run B is needed because the repo-root composer.json carries Lovata as transitive deps (October ecosystem). Removing them simulates a marketplace operator who has not installed the cart-plugin. The `|| true` shields against the package not being there to remove.
- NO COMMIT in this task — bundled at task 7.
  </action>
  <verify>
    <automated>test -f plugins/logingrupa/metapixel/.github/workflows/metapixel-qa.yml &amp;&amp; python3 -c 'import yaml; yaml.safe_load(open("plugins/logingrupa/metapixel/.github/workflows/metapixel-qa.yml"))' &amp;&amp; grep -q "php: \['8.3', '8.4'\]" plugins/logingrupa/metapixel/.github/workflows/metapixel-qa.yml &amp;&amp; grep -q 'install: \[full-lovata, minimal\]' plugins/logingrupa/metapixel/.github/workflows/metapixel-qa.yml &amp;&amp; grep -q -- '--min=90' plugins/logingrupa/metapixel/.github/workflows/metapixel-qa.yml</automated>
  </verify>
  <done>metapixel-qa.yml present, valid YAML, defines 2x2 matrix, Run A enforces --min=90, Run B excludes Adapter testsuite + no coverage gate.</done>
</task>

<task type="auto">
  <name>Task 7: Single test-scaffold + CI commit (tasks 1–6 bundle)</name>
  <files>
    plugins/logingrupa/metapixel/phpunit.xml
    plugins/logingrupa/metapixel/tests/MetapixelTestCase.php
    plugins/logingrupa/metapixel/tests/ShopaholicAdapterTestCase.php
    plugins/logingrupa/metapixel/tests/Pest.php
    plugins/logingrupa/metapixel/tests/Unit/PluginSanityTest.php
    plugins/logingrupa/metapixel/.github/workflows/metapixel-qa.yml
  </files>
  <action>
Stage and commit the test scaffold + CI workflow atomically.

    git add plugins/logingrupa/metapixel/phpunit.xml \
            plugins/logingrupa/metapixel/tests/ \
            plugins/logingrupa/metapixel/.github/workflows/metapixel-qa.yml

    git commit -m "$(cat <<'EOF'
    feat(metapixel): land Pest 4 test scaffold + GitHub Actions CI matrix

    Per TOOL-08 (two-tier test base: MetapixelTestCase no-cart-deps,
    ShopaholicAdapterTestCase Lovata-Orders hermetic) and TOOL-09 (2x2 CI
    matrix php:[8.3, 8.4] x install:[full-lovata, minimal]).

    Run A (full-lovata) enforces coverage --min=90; Run B (minimal) excludes
    the Adapter testsuite and skips the coverage gate (P-20 prevention).

    PluginSanityTest asserts the plugin class loads via PSR-4 and pluginDetails
    returns lang keys under the renamed logingrupa.metapixel::lang.* namespace
    (P-04 anchor reduced via fresh-scaffold approach — no v1.x lang carry-forward).
    EOF
    )"
  </action>
  <verify>
    <automated>git log -1 --name-only | grep -cE 'plugins/logingrupa/metapixel/(phpunit\.xml|tests/|\.github/workflows/metapixel-qa\.yml)' | xargs test 5 -le</automated>
  </verify>
  <done>One commit on HEAD contains phpunit.xml, all four tests/ files, and the CI workflow.</done>
</task>

<task type="auto">
  <name>Task 8: Smoke-run `composer qa` locally — proves the chain is green</name>
  <files>
    (verification-only — no source edits)
  </files>
  <action>
From `plugins/logingrupa/metapixel/`, run each script in sequence. On failure, capture output and fix the relevant config (and amend the relevant commit if the failure is in a config from plan 01-02, or the test file from this plan).

1. `composer run-script pint-test 2>&1 | tail -10`
   - Expected: 0 issues. Plugin.php + tests/*.php must format-clean under Laravel preset.
   - If issues: `composer pint` to auto-fix, then `git add -A; git commit --amend --no-edit` (only if amending the SAME commit as this plan's task 7 — otherwise create a fix-up commit).

2. `composer run-script analyse 2>&1 | tail -20`
   - Expected: 0 errors. Plugin.php and tests/ files must pass phpstan level 10 against PHP 8.3 grammar.
   - Note: tests/ is in phpstan excludePaths, so phpstan analyses ONLY Plugin.php in this phase. Test files don't contribute errors here.

3. `composer run-script phpmd 2>&1 | tail -10`
   - Expected: 0 violations. phpmd command targets Plugin.php only.

4. `composer run-script deps-check 2>&1 | tail -10`
   - Expected: "No issues found" or "0 errors".

5. `composer run-script test 2>&1 | tail -20`
   - Expected: PluginSanityTest runs and passes (2 tests, 2 assertions if PHPUnit, or 2 it() blocks if Pest).
   - If the bootstrap path is wrong (`../../../../bootstrap/app.php` from tests/), adjust MetapixelTestCase.php createApplication() and re-commit.

6. `composer run-script test-cov 2>&1 | tail -10`
   - Expected: Coverage report at storage/temp or stdout. Plugin.php pluginDetails() should show 100% line coverage (exercised by PluginSanityTest::test_plugin_details_*).

7. Full chain: `composer run-script qa 2>&1 | tee /tmp/metapixel-qa.log | tail -30`
   - Expected: All 5 steps exit 0.

If ALL pass, this plan is ready for handoff. The CI matrix will exercise these same steps on PHP 8.3 + 8.4 in both install shapes when the PR opens. Plan 01-03 SUMMARY records the qa output.

No commit in this task — verification only.
  </action>
  <verify>
    <automated>cd plugins/logingrupa/metapixel &amp;&amp; composer run-script qa 2&gt;&amp;1 | tail -5 | grep -qE '(OK|PASS|0 errors|tests passed|All checks passed|No issues found)'</automated>
  </verify>
  <done>`composer qa` exits 0 from the plugin directory. All five steps (pint-test, analyse, phpmd, deps-check, test-cov) green. Output saved to /tmp/metapixel-qa.log for the SUMMARY.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries (Phase 1 plan 01-03 scope)

| Boundary | Description |
|----------|-------------|
| tests/Pest.php uses() binding → test files | A test file that does NOT extend MetapixelTestCase explicitly relies on the uses() binding firing. If Pest.php's binding silently misses a path, the test runs without harness setup and may pass for wrong reasons. PluginSanityTest avoids this by using explicit `extends`. |
| GitHub Actions runner → secrets | The workflow uses `permissions: contents: read` only. No write access to refs, releases, or pull-requests. No secrets referenced. Hardened per v1.x precedent. |
| Run B's composer remove of Lovata → repo-root composer.json | The remove is in-runner only — does NOT persist back to the repo (CI checks out a clean copy each run). Safe. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | A test author writes a test under `tests/Feature/` that imports `Lovata\OrdersShopaholic\Models\Order` | mitigate | The test file MUST extend ShopaholicAdapterTestCase, which lives under `tests/Feature/Adapter/Shopaholic`. If the file is placed under `tests/Feature/Foo.php`, Pest binds MetapixelTestCase, and the import fails when Run B runs (Lovata not installed). CI catches it. |
| T-03-02 | Spoofing | CI Run B runs only MetapixelTestCase but loads ShopaholicAdapterTestCase via autoload | accept | Autoload loading the class is benign — the class is never instantiated unless a test extends it. ShopaholicAdapterTestCase imports `Illuminate\Support\Facades\Schema` only (no Lovata). Safe. |
| T-03-03 | Denial of Service | Coverage gate set too high → blocks merges | mitigate | --min=90 matches the TOOL-09 spec. On the empty scaffold, Plugin.php is the only included source, exercised by PluginSanityTest — coverage is 100%. As Phase 2+ adds code, coverage stays measurable; gate is realistic. |
| T-03-04 | Information Disclosure | CI logs leak secrets | accept | No secrets in this workflow. `permissions: contents: read` is restrictive. |
| T-03-05 | Elevation of Privilege | Test that runs with autoMigrate=true and migrates the repo's MySQL prod DB | mitigate | MetapixelTestCase pins SQLite-in-memory in createApplication() AFTER kernel bootstrap, overriding the dotenv loader. Verified by the v1.x behavioral lift (the lines forcing `database.default = sqlite` after `app->make(Kernel::class)->bootstrap()`). Test cannot reach prod MySQL. |
| T-03-06 | Repudiation | Phase 3 lands an adapter test that fails on Run B without the operator knowing the Run B exclusion is intentional | mitigate | phpunit.xml comment block + Run B's `--exclude-testsuite='Metapixel Adapter Tests'` flag make the exclusion explicit. Phase 3 SHOP-* will add a `<testsuite name="Metapixel Adapter Tests">` block when adapter tests land. |

</threat_model>

<verification>

## Goal-Backward Reachability Audit

1. "MetapixelTestCase boots OctoberCMS in SQLite without cart-plugin migrations" — Task 2 writes the class. Verified by `php -l` + `! grep -i shopaholic`.
2. "ShopaholicAdapterTestCase exists, extends MetapixelTestCase, holds Lovata hermetic helpers" — Task 3 writes the class. Verified by `grep -q 'extends MetapixelTestCase'`.
3. "Sanity Pest test asserts Plugin loads" — Task 5 writes PluginSanityTest. Verified by `grep -q 'class_exists(Plugin::class)'`.
4. "composer qa exits 0 on empty scaffold" — Task 8 smoke-runs the full chain.
5. "CI matrix php:[8.3, 8.4] x install:[full-lovata, minimal] = 4 cells" — Task 6 writes the workflow. Verified by `grep` matrix entries.
6. "Run A enforces coverage ≥ 90%" — Task 6 writes `--min=90` on the Run A test step.
7. "Run B excludes Adapter testsuite + no coverage gate" — Task 6 writes the Run B step.

No must-have is UNREACHABLE.

## Multi-Source Coverage Audit

| Source item | Type | Plan(s) covering | Notes |
|-------------|------|------------------|-------|
| GOAL phase 1 success criterion 1: `composer qa` exits 0 on empty scaffold | Goal | 01-03 task 8 + plans 01-01, 01-02 | This plan closes the test-step gap |
| GOAL success criterion 2: CI matrix 4 cells green | Goal | 01-03 task 6 + task 8 | Fully covered |
| GOAL success criterion 5: three-tier test base (MetapixelTestCase + ShopaholicAdapterTestCase + tiers run in isolation) | Goal | 01-03 tasks 2, 3, 4 | TWO tiers in this plan; the third (ThemeActionAdapterTestCase) lands when Phase 3 THEM-* needs it — same SRP pattern |
| REQ TOOL-08 (Pest 4 scaffold, two test bases) | Requirement | 01-03 tasks 2, 3, 4, 5 | Fully covered |
| REQ TOOL-09 (CI matrix workflow) | Requirement | 01-03 task 6 | Fully covered |
| RESEARCH PITFALLS P-12 (MetapixelTestCase boots Shopaholic) | Pitfall | 01-03 tasks 2, 3 (strict tier split) | Fully prevented |
| RESEARCH PITFALLS P-19 (Pest uses() carries Shopaholic-coupled base) | Pitfall | 01-03 task 4 (subdirectory-scoped bindings) | Fully prevented |
| RESEARCH PITFALLS P-20 (coverage on partial code paths) | Pitfall | 01-03 task 6 (--min=90 ONLY on Run A) | Fully prevented |
| CONTEXT lock "no v1.x test carry-forward (no v1.x tests adapt)" | Context | 01-03 task 5 (only a sanity test ships) | Fully honored |
| CONTEXT lock "no phase markers in code" | Context | 01-03 (tests/Pest.php, MetapixelTestCase, ShopaholicAdapterTestCase all phase-marker-free) | Fully honored |
| CONTEXT lock "Laravel short docblocks" | Context | 01-03 (every docblock one-paragraph) | Fully honored |

Coverage complete.

## Three-tier expectation (TOOL-08 wording)

TOOL-08 in REQUIREMENTS.md mentions "two test bases" (MetapixelTestCase + ShopaholicAdapterTestCase). ROADMAP.md success criterion 5 says "Three-tier Pest 4 test bases instantiated" — this seems to count the abstract `Illuminate\Foundation\Testing\TestCase` parent as the first tier. Plan 01-03 ships:
- Tier 1 (parent): `Illuminate\Foundation\Testing\TestCase` (October-provided, already in vendor)
- Tier 2 (plugin core): `Logingrupa\Metapixel\Tests\MetapixelTestCase`
- Tier 3 (adapter-specific): `Logingrupa\Metapixel\Tests\ShopaholicAdapterTestCase`

The roadmap wording is satisfied by the two FILES this plan ships (Illuminate's base is library code, not plugin code).

</verification>

<success_criteria>

Plan 01-03 ships when ALL of the following hold:

1. `plugins/logingrupa/metapixel/phpunit.xml` exists; xmllint-valid; testsuite names drop "shopaholic"; `<source><include>` lists only `Plugin.php`.
2. `plugins/logingrupa/metapixel/tests/MetapixelTestCase.php` exists; php -l clean; zero references to "shopaholic" / phase markers / CR refs; namespace `Logingrupa\Metapixel\Tests`.
3. `plugins/logingrupa/metapixel/tests/ShopaholicAdapterTestCase.php` exists; php -l clean; extends MetapixelTestCase; holds bootOrdersTable + bootOrdersStatuses + table-drop override.
4. `plugins/logingrupa/metapixel/tests/Pest.php` exists; binds MetapixelTestCase to tests/Unit + tests/Feature; binds ShopaholicAdapterTestCase to tests/Unit/Adapter/Shopaholic + tests/Feature/Adapter/Shopaholic.
5. `plugins/logingrupa/metapixel/tests/Unit/PluginSanityTest.php` exists; asserts Plugin loads via PSR-4 + pluginDetails returns lang keys under `logingrupa.metapixel::`.
6. `plugins/logingrupa/metapixel/.github/workflows/metapixel-qa.yml` exists; valid YAML; defines `php: ['8.3', '8.4']` x `install: [full-lovata, minimal]` matrix; Run A enforces `--min=90`; Run B excludes Adapter testsuite + no coverage gate.
7. Locally (task 8), `composer qa` exits 0 from `plugins/logingrupa/metapixel/`: pint-test, analyse, phpmd, deps-check, test-cov all green; PluginSanityTest passes; Plugin.php coverage = 100%.
8. Exactly one commit added in this plan (the bundled commit at task 7).
9. Phase 1 closure: ROADMAP.md success criteria 1, 2, 3, 4, 5 all satisfied after plans 01-01 + 01-02 + 01-03.

</success_criteria>

<output>
After completion, create `plugins/logingrupa/metapixel/.planning/phases/01-tooling-composer-namespace-rename-ci-matrix/01-03-SUMMARY.md` documenting:
- The single commit SHA produced by this plan
- The full `composer qa` output tail (last 30 lines) from task 8 — proving each script green
- PluginSanityTest run output (PASS line + assertion count)
- Coverage report number for Plugin.php (expected 100%)
- The four CI cell names that will exercise on the next PR
- Drift log: any deviations from files_modified frontmatter (e.g. PluginScaffoldTest.php deferred per task 5 decision)
- Phase 1 closure note: "All 11 TOOL-* requirements satisfied. Plans 01-01 (rename + composer), 01-02 (tooling configs), 01-03 (tests + CI) complete. Phase 1 ready for verification."
</output>
