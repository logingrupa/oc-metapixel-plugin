---
phase: 01-tooling-composer-namespace-rename-ci-matrix
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - plugins/logingrupa/metapixel/phpstan.neon
  - plugins/logingrupa/metapixel/rector.php
  - plugins/logingrupa/metapixel/pint.json
  - plugins/logingrupa/metapixel/phpmd.xml
  - plugins/logingrupa/metapixel/composer-dependency-analyser.php
  - plugins/logingrupa/metapixel/composer.json
  - composer.json
autonomous: true
requirements:
  - TOOL-04
  - TOOL-05
  - TOOL-06
  - TOOL-07
  - TOOL-10
  - TOOL-11
must_haves:
  truths:
    - "`composer qa` (pint-test then analyse then phpmd then deps-check then test-cov) chain is wired in the plugin's composer.json scripts."
    - "PHPStan analyses at level 10 against PHP 8.3 grammar (phpVersion 80300) and detects PHP 8.4-only syntax as errors."
    - "spaze/phpstan-disallowed-calls bans assert(), @ suppression, array_find, array_any, array_all, array_find_key, and the Deprecated attribute class."
    - "Rector configured with php83: true (NOT php84) plus code-quality / dead-code / early-return / type-declaration sets."
    - "Pint applies the Laravel preset plus nullable_type_declaration_for_default_null_value, ordered_imports alpha, no_unused_imports, single_quote, binary_operator_spaces single_space."
    - "PHPMD config derives from Lovata.Toolbox PHPMD_custom.xml: ShortVariable min=4, LongVariable max=40, CyclomaticComplexity reportLevel=10, ExcessiveClassLength minimum=1000."
    - "shipmonk/composer-dependency-analyser config bans Lovata\\OrdersShopaholic and Lovata\\Shopaholic imports outside classes/adapter/shopaholic/."
    - "Every step of `composer qa` (except test-cov) exits 0 on the empty scaffold; test-cov is wired but deferred until plan 01-03 lands the test bases."
  artifacts:
    - path: "plugins/logingrupa/metapixel/phpstan.neon"
      provides: "TOOL-04 phpstan config"
      contains: "phpVersion: 80300"
    - path: "plugins/logingrupa/metapixel/rector.php"
      provides: "TOOL-05 rector config"
      contains: "php83: true"
    - path: "plugins/logingrupa/metapixel/pint.json"
      provides: "TOOL-06 pint config"
      contains: "nullable_type_declaration_for_default_null_value"
    - path: "plugins/logingrupa/metapixel/phpmd.xml"
      provides: "TOOL-07 phpmd ruleset"
      contains: 'name="Metapixel"'
    - path: "plugins/logingrupa/metapixel/composer-dependency-analyser.php"
      provides: "TOOL-11 shipmonk config"
      contains: "ignoreErrorsOnPackageAndPath"
    - path: "plugins/logingrupa/metapixel/composer.json"
      provides: "TOOL-10 qa script chain"
      contains: '"qa":'
  key_links:
    - from: "plugins/logingrupa/metapixel/composer.json scripts.qa"
      to: "pint-test, analyse, phpmd, deps-check, test-cov"
      via: "composer script chain"
      pattern: '@pint-test'
    - from: "plugins/logingrupa/metapixel/phpstan.neon"
      to: "spaze/phpstan-disallowed-calls extension"
      via: "includes vendor/spaze/phpstan-disallowed-calls/extension.neon"
      pattern: "spaze/phpstan-disallowed-calls"
    - from: "plugins/logingrupa/metapixel/composer-dependency-analyser.php"
      to: "classes/adapter/shopaholic"
      via: "ignoreErrorsOnPackageAndPath allowlist"
      pattern: "classes/adapter/shopaholic"
---

<objective>
Ship the v2.0 quality-toolchain configs (phpstan, rector, pint, phpmd, composer-dependency-analyser) for the renamed plugin and wire the full `composer qa` chain. Configs lock PHP 8.3 baseline, ban PHP 8.4-only syntax, and ban Lovata cart imports outside the adapter directory. A fresh clone runs `composer qa` and exits 0 on the empty scaffold (test step exit 0 once plan 01-03 lands tests; phpstan / phpmd / pint / deps-check are green now).

Purpose: prevent P-03 (hidden Lovata imports) and P-06 (PHP 8.4-only syntax slips) at the static-analysis layer before any business code lands in Phase 2.
Output: five tooling configs plus expanded composer.json scripts block, all committed atomically.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@plugins/logingrupa/metapixel/.planning/PROJECT.md
@plugins/logingrupa/metapixel/.planning/ROADMAP.md
@plugins/logingrupa/metapixel/.planning/REQUIREMENTS.md
@plugins/logingrupa/metapixel/.planning/research/STACK.md
@plugins/logingrupa/metapixel/.planning/research/PITFALLS.md
@plugins/logingrupa/metapixel/.planning/phases/01-tooling-composer-namespace-rename-ci-matrix/01-01-SUMMARY.md
@CLAUDE.md
@plugins/lovata/toolbox/PHPMD_custom.xml

<interfaces>
Locked invariants from plan 01-01:

- Plugin dir: plugins/logingrupa/metapixel/
- Namespace: Logingrupa\Metapixel
- Composer name: logingrupa/oc-metapixel-plugin
- PHP constraint: ^8.3 || ^8.4
- Plugin.php: empty boot/register
- vendor/ lives at repo root (../../../vendor/)

v1.x reference files retrievable via `git show legacy/v1.1.1:plugins/logingrupa/metapixelshopaholic/<file>`:
- phpstan.neon (level 10, larastan, disallowed-calls, universalObjectCrates)
- phpmd.xml (codesize + naming rules)
- pint.json (Laravel preset + ordering)
- rector.php (withPaths + withPhpSets + withPreparedSets)

v2.0 deltas from v1.x:
- phpstan: add phpVersion 80300, expand disallowedFunctionCalls with PHP 8.4 array funcs, add disallowedClasses for Deprecated, drop phase-numbered comment block
- rector: withPhpSets(php83: true) NOT php84, drop console/ from paths
- pint: ADD nullable_type_declaration_for_default_null_value
- phpmd: rename ruleset Metapixel, drop project-specific exceptions (mId, ExcessiveClassComplexity 55)
- composer: rebuild qa script chain per TOOL-10
- composer-dependency-analyser: NEW config (not in v1.x)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write phpstan.neon (TOOL-04)</name>
  <files>
    plugins/logingrupa/metapixel/phpstan.neon
  </files>
  <action>
Reference for shape: `git show legacy/v1.1.1:plugins/logingrupa/metapixelshopaholic/phpstan.neon`. Apply deltas listed in `<interfaces>`.

Write `plugins/logingrupa/metapixel/phpstan.neon` with this exact content:

    includes:
        - ../../../vendor/larastan/larastan/extension.neon
        - ../../../vendor/spaze/phpstan-disallowed-calls/extension.neon

    parameters:
        level: 10
        phpVersion: 80300

        paths:
            - Plugin.php

        excludePaths:
            - tests (?)
            - updates (?)
            - lang (?)
            - .github (?)

        tmpDir: ../../../storage/temp/phpstan/metapixel

        reportUnmatchedIgnoredErrors: true
        treatPhpDocTypesAsCertain: true
        checkUninitializedProperties: true

        universalObjectCratesClasses:
            - Lovata\Toolbox\Classes\Item\ElementItem
            - Lovata\Toolbox\Classes\Collection\ElementCollection

        disallowedFunctionCalls:
            -
                function: 'assert()'
                message: 'use throw — assert() is a silent no-op when zend.assertions=0 (production default)'
            -
                function: '@'
                message: 'no @ suppression — handle errors explicitly'
            -
                function: 'array_find()'
                message: 'PHP 8.4-only — use array_filter + early return or foreach'
            -
                function: 'array_find_key()'
                message: 'PHP 8.4-only — use array_keys + array_filter or foreach'
            -
                function: 'array_any()'
                message: 'PHP 8.4-only — use array_filter or foreach with early return'
            -
                function: 'array_all()'
                message: 'PHP 8.4-only — use array_filter or foreach'

        disallowedClasses:
            -
                class: 'Deprecated'
                message: 'PHP 8.4-only attribute — use @deprecated docblock instead'

Notes for executor:
- DO NOT include phpstan-baseline.neon — on the empty scaffold there is nothing to baseline. Baseline lands when Phase 2 first generates analysis output.
- `paths` lists only `Plugin.php`. Each phase reopens this list as it lands its directories.
- DO NOT add any phase-numbered comment blocks (project lock: no phase markers in code or configs).
- NO COMMIT in this task — bundled with tasks 2–6 into one tooling-configs commit at task 7.
  </action>
  <verify>
    <automated>test -f plugins/logingrupa/metapixel/phpstan.neon &amp;&amp; grep -q 'phpVersion: 80300' plugins/logingrupa/metapixel/phpstan.neon &amp;&amp; grep -q 'array_find' plugins/logingrupa/metapixel/phpstan.neon &amp;&amp; grep -q 'spaze/phpstan-disallowed-calls' plugins/logingrupa/metapixel/phpstan.neon</automated>
  </verify>
  <done>phpstan.neon present with phpVersion 80300, level 10, larastan + disallowed-calls extensions wired, PHP 8.4 array-function bans + Deprecated attribute ban in disallowed lists.</done>
</task>

<task type="auto">
  <name>Task 2: Write rector.php (TOOL-05)</name>
  <files>
    plugins/logingrupa/metapixel/rector.php
  </files>
  <action>
Reference for shape: `git show legacy/v1.1.1:plugins/logingrupa/metapixelshopaholic/rector.php`. v1.x uses `withPhpSets(php84: true)`; v2.0 caps at PHP 8.3 — named arg becomes `php83: true`. Drop `console/` from paths (no console commands until Phase 4 HOST-03 lands `metapixel:refresh-psl`).

Write `plugins/logingrupa/metapixel/rector.php` with:

    <?php

    use Rector\Config\RectorConfig;

    return RectorConfig::configure()
        ->withPaths(array_filter([
            __DIR__.'/Plugin.php',
            __DIR__.'/classes',
            __DIR__.'/models',
            __DIR__.'/components',
            __DIR__.'/middleware',
            __DIR__.'/controllers',
        ], static fn (string $sPath): bool => file_exists($sPath)))
        ->withPhpSets(php83: true)
        ->withPreparedSets(
            deadCode: true,
            codeQuality: true,
            typeDeclarations: true,
            earlyReturn: true,
        )
        ->withSkip([
            __DIR__.'/lang',
            __DIR__.'/updates',
            __DIR__.'/tests',
            __DIR__.'/.github',
        ]);

Notes for executor:
- DROP `declare(strict_types=1);` — project lock: optional per-file; ecosystem norm is no enforcement.
- `array_filter` + `file_exists` short-circuits non-existent paths; only `Plugin.php` exists today, so the other entries are no-ops.
- NO COMMIT in this task — bundled at task 7.
  </action>
  <verify>
    <automated>test -f plugins/logingrupa/metapixel/rector.php &amp;&amp; grep -q 'php83: true' plugins/logingrupa/metapixel/rector.php &amp;&amp; ! grep -q 'php84' plugins/logingrupa/metapixel/rector.php &amp;&amp; grep -q 'deadCode: true' plugins/logingrupa/metapixel/rector.php</automated>
  </verify>
  <done>rector.php present with withPhpSets(php83: true), prepared sets all four enabled, paths file_exists-filtered.</done>
</task>

<task type="auto">
  <name>Task 3: Write pint.json (TOOL-06)</name>
  <files>
    plugins/logingrupa/metapixel/pint.json
  </files>
  <action>
Reference for shape: `git show legacy/v1.1.1:plugins/logingrupa/metapixelshopaholic/pint.json`. v2.0 delta is adding `nullable_type_declaration_for_default_null_value: true` to prevent the PHP 8.4 implicit-nullable deprecation slipping past Pint.

Write `plugins/logingrupa/metapixel/pint.json` with this exact JSON:

    {
        "preset": "laravel",
        "rules": {
            "nullable_type_declaration_for_default_null_value": true,
            "ordered_imports": {
                "sort_algorithm": "alpha"
            },
            "no_unused_imports": true,
            "single_quote": true,
            "binary_operator_spaces": {
                "default": "single_space"
            }
        },
        "exclude": [
            "updates",
            "lang",
            "tests"
        ]
    }

Notes for executor:
- Drop "partials" from `exclude` (v2.0 plugin tree has no partials/ dir).
- NO COMMIT in this task — bundled at task 7.
  </action>
  <verify>
    <automated>test -f plugins/logingrupa/metapixel/pint.json &amp;&amp; grep -q 'nullable_type_declaration_for_default_null_value' plugins/logingrupa/metapixel/pint.json &amp;&amp; python3 -c 'import json; json.load(open("plugins/logingrupa/metapixel/pint.json"))'</automated>
  </verify>
  <done>pint.json present with Laravel preset, nullable rule, ordering, single_quote, single_space, valid JSON.</done>
</task>

<task type="auto">
  <name>Task 4: Write phpmd.xml (TOOL-07)</name>
  <files>
    plugins/logingrupa/metapixel/phpmd.xml
  </files>
  <action>
Reference 1: `git show legacy/v1.1.1:plugins/logingrupa/metapixelshopaholic/phpmd.xml`. Reference 2: `plugins/lovata/toolbox/PHPMD_custom.xml`. v2.0 deltas:
- ruleset name "MetapixelShopaholic" → "Metapixel"
- DROP phase-numbered comments around ExcessiveClassComplexity (project lock)
- DROP mId exception under ShortVariable (no carry-forward; if needed later, the owning phase re-adds it)
- Keep all TOOL-07 thresholds: ShortVariable min=4, LongVariable max=40, CyclomaticComplexity reportLevel=10, ExcessiveClassLength minimum=1000

Write `plugins/logingrupa/metapixel/phpmd.xml`:

    <?xml version="1.0"?>
    <ruleset name="Metapixel" xmlns="http://pmd.sf.net/ruleset/1.0.0"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xsi:schemaLocation="http://pmd.sf.net/ruleset/1.0.0 http://pmd.sf.net/ruleset_xml_schema.xsd"
             xsi:noNamespaceSchemaLocation="http://pmd.sf.net/ruleset_xml_schema.xsd">
        <description>Lovata.Toolbox-derived PHPMD ruleset for Logingrupa.Metapixel.</description>

        <rule ref="rulesets/codesize.xml/CyclomaticComplexity">
            <properties>
                <property name="reportLevel" value="10" />
                <property name="showClassesComplexity" value="true" />
                <property name="showMethodsComplexity" value="true" />
            </properties>
        </rule>
        <rule ref="rulesets/codesize.xml/NPathComplexity">
            <properties>
                <property name="minimum" value="200" />
            </properties>
        </rule>
        <rule ref="rulesets/codesize.xml/ExcessiveMethodLength">
            <properties>
                <property name="minimum" value="100" />
                <property name="ignore-whitespace" value="true" />
            </properties>
        </rule>
        <rule ref="rulesets/codesize.xml/ExcessiveClassLength">
            <properties>
                <property name="minimum" value="1000" />
                <property name="ignore-whitespace" value="true" />
            </properties>
        </rule>
        <rule ref="rulesets/codesize.xml/ExcessiveParameterList">
            <properties>
                <property name="minimum" value="8" />
            </properties>
        </rule>
        <rule ref="rulesets/codesize.xml/ExcessivePublicCount">
            <properties>
                <property name="minimum" value="45" />
            </properties>
        </rule>
        <rule ref="rulesets/codesize.xml/TooManyFields">
            <properties>
                <property name="maxfields" value="20" />
            </properties>
        </rule>
        <rule ref="rulesets/codesize.xml/TooManyMethods">
            <properties>
                <property name="maxmethods" value="25" />
                <property name="ignorepattern" value="(^(set|get))i" />
            </properties>
        </rule>
        <rule ref="rulesets/codesize.xml/TooManyPublicMethods">
            <properties>
                <property name="maxmethods" value="10" />
                <property name="ignorepattern" value="(^(set|get))i" />
            </properties>
        </rule>
        <rule ref="rulesets/codesize.xml/ExcessiveClassComplexity">
            <properties>
                <property name="maximum" value="50" />
            </properties>
        </rule>

        <rule ref="rulesets/naming.xml/ShortVariable">
            <properties>
                <property name="minimum" value="4" />
            </properties>
        </rule>
        <rule ref="rulesets/naming.xml/LongVariable">
            <properties>
                <property name="maximum" value="40" />
            </properties>
        </rule>
        <rule ref="rulesets/naming.xml/ShortMethodName">
            <properties>
                <property name="minimum" value="3" />
                <property name="exceptions" value="up" />
            </properties>
        </rule>
        <rule ref="rulesets/naming.xml/ConstructorWithNameAsEnclosingClass" />
        <rule ref="rulesets/naming.xml/ConstantNamingConventions" />
        <rule ref="rulesets/naming.xml/BooleanGetMethodName">
            <properties>
                <property name="checkParameterizedMethods" value="false" />
            </properties>
        </rule>
    </ruleset>

Notes for executor:
- ExcessiveClassComplexity threshold is 50 (canonical Toolbox value). v1.x raised it to 55 for a specific carry-over; v2.0 starts strict.
- `ShortMethodName exceptions="up"` retained — October migrations require `up()` as canonical method name.
- NO COMMIT in this task — bundled at task 7.
  </action>
  <verify>
    <automated>test -f plugins/logingrupa/metapixel/phpmd.xml &amp;&amp; grep -q 'name="Metapixel"' plugins/logingrupa/metapixel/phpmd.xml &amp;&amp; xmllint --noout plugins/logingrupa/metapixel/phpmd.xml</automated>
  </verify>
  <done>phpmd.xml present with ruleset name "Metapixel", all TOOL-07 thresholds, valid XML.</done>
</task>

<task type="auto">
  <name>Task 5: Write composer-dependency-analyser.php (TOOL-11)</name>
  <files>
    plugins/logingrupa/metapixel/composer-dependency-analyser.php
  </files>
  <action>
Write the shipmonk dependency-analyser config (see https://github.com/shipmonk-rnd/composer-dependency-analyser#configuration). The lib expects a file named `composer-dependency-analyser.php` (or default `composer-dependency-analyser.dist.php`) returning a `Configuration` object.

Write `plugins/logingrupa/metapixel/composer-dependency-analyser.php`:

    <?php

    use ShipMonk\ComposerDependencyAnalyser\Config\Configuration;
    use ShipMonk\ComposerDependencyAnalyser\Config\ErrorType;

    $obConfig = new Configuration();

    // Production-code scan paths. Filtered through file_exists so the empty
    // scaffold does not fail on missing subdirectories.
    foreach (['/Plugin.php', '/classes', '/models', '/components', '/middleware', '/controllers'] as $sPath) {
        if (file_exists(__DIR__ . $sPath)) {
            $obConfig->addPathToScan(__DIR__ . $sPath, isDev: false);
        }
    }

    if (file_exists(__DIR__ . '/tests')) {
        $obConfig->addPathToScan(__DIR__ . '/tests', isDev: true);
    }

    foreach (['/lang', '/updates', '/.planning', '/.github'] as $sPath) {
        if (file_exists(__DIR__ . $sPath)) {
            $obConfig->addPathToExclude(__DIR__ . $sPath);
        }
    }

    // Lovata cart plugins live in composer "suggest" (production) and "require-dev"
    // (test suite). The analyser would flag them as dev-deps used in prod if any
    // src file outside the adapter directory imports them.
    $obConfig->ignoreErrorsOnPackage('lovata/shopaholic-plugin',         [ErrorType::DEV_DEPENDENCY_IN_PROD]);
    $obConfig->ignoreErrorsOnPackage('lovata/ordersshopaholic-plugin',   [ErrorType::DEV_DEPENDENCY_IN_PROD]);
    $obConfig->ignoreErrorsOnPackage('lovata/buddies-plugin',            [ErrorType::DEV_DEPENDENCY_IN_PROD]);

    // The adapter directory IS allowed to import Lovata cart classes.
    // Phase 3 lands classes/adapter/shopaholic/; this pre-wires the allowlist.
    $obConfig->ignoreErrorsOnPackageAndPath(
        'lovata/shopaholic-plugin',
        __DIR__ . '/classes/adapter/shopaholic',
        [ErrorType::DEV_DEPENDENCY_IN_PROD],
    );
    $obConfig->ignoreErrorsOnPackageAndPath(
        'lovata/ordersshopaholic-plugin',
        __DIR__ . '/classes/adapter/shopaholic',
        [ErrorType::DEV_DEPENDENCY_IN_PROD],
    );

    // Dev tooling — referenced via composer scripts, not imported.
    foreach ([
        'shipmonk/composer-dependency-analyser',
        'spaze/phpstan-disallowed-calls',
        'larastan/larastan',
        'phpmd/phpmd',
        'laravel/pint',
        'rector/rector',
        'mockery/mockery',
        'pestphp/pest',
        'pestphp/pest-plugin-drift',
        'phpunit/phpunit',
    ] as $sDevPackage) {
        $obConfig->ignoreErrorsOnPackage($sDevPackage, [ErrorType::UNUSED_DEPENDENCY]);
    }

    return $obConfig;

Notes for executor:
- The file_exists guards keep the config friendly to the empty scaffold; phpstan covers the case where a class is imported from a non-allowlisted dir.
- The Phase 3 adapter directory allowlist is pre-wired so phase 3 needs zero config edits.
- NO COMMIT in this task — bundled at task 7.
  </action>
  <verify>
    <automated>test -f plugins/logingrupa/metapixel/composer-dependency-analyser.php &amp;&amp; php -l plugins/logingrupa/metapixel/composer-dependency-analyser.php | grep -q 'No syntax errors'</automated>
  </verify>
  <done>composer-dependency-analyser.php present, php -l clean, adapter directory allowlist defined, dev tooling unused-dep ignores set.</done>
</task>

<task type="auto">
  <name>Task 6: Extend plugin composer.json — require-dev shipmonk, full TOOL-10 qa chain</name>
  <files>
    plugins/logingrupa/metapixel/composer.json
  </files>
  <action>
Plan 01-01 wrote a minimal `scripts` block with only `test` and `test-cov`. Now wire the full TOOL-10 chain and add the shipmonk dev dep.

1. Read `plugins/logingrupa/metapixel/composer.json`. Confirm current `scripts` shape.
2. Use the Edit tool to:

   A. Add to `require-dev` (alphabetic insertion, before `spaze/phpstan-disallowed-calls`):
        "shipmonk/composer-dependency-analyser": "^1.8"

   B. Replace the entire `scripts` block with:

        "scripts": {
            "test":       "../../../vendor/bin/pest --configuration phpunit.xml",
            "test-cov":   "../../../vendor/bin/pest --configuration phpunit.xml --coverage",
            "analyse":    "../../../vendor/bin/phpstan analyse --configuration=phpstan.neon",
            "baseline":   "../../../vendor/bin/phpstan analyse --configuration=phpstan.neon --generate-baseline=phpstan-baseline.neon",
            "phpmd":      "../../../vendor/bin/phpmd Plugin.php text phpmd.xml",
            "pint":       "../../../vendor/bin/pint . --config=pint.json",
            "pint-test":  "../../../vendor/bin/pint . --config=pint.json --test",
            "rector":     "../../../vendor/bin/rector process --config=rector.php",
            "rector-dry": "../../../vendor/bin/rector process --config=rector.php --dry-run",
            "deps-check": "../../../vendor/bin/composer-dependency-analyser --config=composer-dependency-analyser.php",
            "qa": ["@pint-test", "@analyse", "@phpmd", "@deps-check", "@test-cov"]
        }

3. Verify JSON validity:
       python3 -c 'import json; json.load(open("plugins/logingrupa/metapixel/composer.json"))'

Notes for executor:
- `phpmd` script lists only `Plugin.php` for now. Each subsequent phase appends its newly landed dirs (classes,models,components,middleware,controllers,updates) when it lands them.
- `qa` chain order is fast-fail: pint-test (formatting) → analyse (types) → phpmd (complexity) → deps-check (composer hygiene) → test-cov (slowest).
- NO COMMIT in this task — bundled at task 7.
  </action>
  <verify>
    <automated>python3 -c 'import json; c = json.load(open("plugins/logingrupa/metapixel/composer.json")); assert "shipmonk/composer-dependency-analyser" in c["require-dev"], "shipmonk missing"; assert c["scripts"]["qa"] == ["@pint-test", "@analyse", "@phpmd", "@deps-check", "@test-cov"], "qa chain wrong"; print("OK")'</automated>
  </verify>
  <done>composer.json has shipmonk in require-dev, full qa script chain in TOOL-10 order, valid JSON.</done>
</task>

<task type="auto">
  <name>Task 7: Single tooling-configs commit (tasks 1–6 bundle)</name>
  <files>
    plugins/logingrupa/metapixel/phpstan.neon
    plugins/logingrupa/metapixel/rector.php
    plugins/logingrupa/metapixel/pint.json
    plugins/logingrupa/metapixel/phpmd.xml
    plugins/logingrupa/metapixel/composer-dependency-analyser.php
    plugins/logingrupa/metapixel/composer.json
  </files>
  <action>
Stage and commit all six files in a single atomic commit so the qa chain lands together.

    git add plugins/logingrupa/metapixel/phpstan.neon \
            plugins/logingrupa/metapixel/rector.php \
            plugins/logingrupa/metapixel/pint.json \
            plugins/logingrupa/metapixel/phpmd.xml \
            plugins/logingrupa/metapixel/composer-dependency-analyser.php \
            plugins/logingrupa/metapixel/composer.json

    git commit -m "$(cat <<'EOF'
    feat(metapixel): wire v2.0 quality toolchain — phpstan, rector, pint, phpmd, deps-check

    Per TOOL-04 (phpstan level 10 + phpVersion 80300 + disallowed PHP 8.4 funcs),
    TOOL-05 (rector php83 set), TOOL-06 (pint Laravel preset + nullable rule),
    TOOL-07 (phpmd from Lovata.Toolbox custom), TOOL-10 (composer qa chain
    pint-test then analyse then phpmd then deps-check then test-cov), TOOL-11
    (shipmonk dependency-analyser with classes/adapter/shopaholic/ allowlist).

    Configs reject PHP 8.4-only syntax at static analysis (P-06 prevention) and
    pre-wire the Lovata cart import allowlist before any adapter lands (P-03
    prevention).
    EOF
    )"
  </action>
  <verify>
    <automated>git log -1 --name-only | grep -c 'plugins/logingrupa/metapixel/\(phpstan\.neon\|rector\.php\|pint\.json\|phpmd\.xml\|composer-dependency-analyser\.php\|composer\.json\)' | xargs test 6 -le</automated>
  </verify>
  <done>One commit on HEAD containing exactly six files: phpstan.neon, rector.php, pint.json, phpmd.xml, composer-dependency-analyser.php, composer.json.</done>
</task>

<task type="auto">
  <name>Task 8: Add shipmonk/composer-dependency-analyser to REPO-ROOT composer.json require-dev</name>
  <files>
    composer.json
    composer.lock
  </files>
  <action>
The plugin invokes `../../../vendor/bin/composer-dependency-analyser`. That binary exists only if the package is in the REPO-ROOT composer.json's require-dev. This mirrors the spaze/phpstan-disallowed-calls add from commit 8442742.

1. Read repo-root `composer.json`. Identify the `require-dev` block.
2. Add (alphabetically positioned):
        "shipmonk/composer-dependency-analyser": "^1.8"
3. Run:
        composer update shipmonk/composer-dependency-analyser --no-progress --no-interaction
4. Confirm binary present:
        test -x vendor/bin/composer-dependency-analyser
5. Commit (composer.lock will also stage):
        git add composer.json composer.lock
        git commit -m "$(cat <<'EOF'
    chore(deps): add shipmonk/composer-dependency-analyser ^1.8 to repo require-dev

    Vendored for plugins/logingrupa/metapixel composer qa chain (TOOL-11).
    EOF
    )"
  </action>
  <verify>
    <automated>test -x vendor/bin/composer-dependency-analyser &amp;&amp; grep -q 'shipmonk/composer-dependency-analyser' composer.json</automated>
  </verify>
  <done>Repo-root composer.json + composer.lock include shipmonk; vendor/bin/composer-dependency-analyser is executable.</done>
</task>

<task type="auto">
  <name>Task 9: Smoke-run the qa chain on the empty scaffold — proves the wiring</name>
  <files>
    (no source edits — verification-only task; if a config errors on the empty scaffold, fix the relevant config file and amend the relevant commit)
  </files>
  <action>
Run each composer script individually so a failure pinpoints the offending config. From `plugins/logingrupa/metapixel/`:

1. `composer run-script pint-test 2>&1 | tail -10`
   - Plugin.php is the only source file. Pint should report 0 issues. If it surfaces an issue, run `composer pint` to auto-fix and re-commit Plugin.php under the existing plan 01-01 task 5 commit (or as a fix-up commit if the original is already pushed).

2. `composer run-script analyse 2>&1 | tail -20`
   - phpstan analyses Plugin.php only. The class extends `System\Classes\PluginBase` (provided by `october/system`). With `phpVersion: 80300` + larastan, it should report 0 errors. If errors surface, investigate before continuing — do NOT generate a baseline at this stage (the scaffold is empty by design; baselining premature).

3. `composer run-script phpmd 2>&1 | tail -10`
   - phpmd runs against Plugin.php only. Class has 1 short method (`pluginDetails`), empty `register`, empty `boot`. Zero violations expected.

4. `composer run-script deps-check 2>&1 | tail -10`
   - Plugin.php imports `System\Classes\PluginBase`. The `system` namespace is provided by `october/all` / `october/system` (already in require:). Expected: "No issues found" or "0 errors".

5. `composer run-script test 2>&1 | tail -5`
   - phpunit.xml does NOT yet exist (lands in plan 01-03). This script WILL fail with "configuration file not found". That is expected for plan 01-02 — record it in the SUMMARY as "test step deferred to plan 01-03".

6. The full `composer qa` chain WILL fail at the test step. Plan 01-02's exit gate is: steps 1–4 pass; step 5 (test/test-cov) is acknowledged as deferred. Do NOT add a `phpunit.xml` stub here — plan 01-03 owns it.

No commit in this task — pure verification.
  </action>
  <verify>
    <automated>cd plugins/logingrupa/metapixel &amp;&amp; ../../../vendor/bin/phpstan analyse --configuration=phpstan.neon 2&gt;&amp;1 | tail -3 | grep -qE '(No errors|\[OK\])' &amp;&amp; ../../../vendor/bin/pint . --config=pint.json --test 2&gt;&amp;1 | tail -3 | grep -qiE '(no\\ issues|nothing\\ to\\ change|0\\ files)' &amp;&amp; ../../../vendor/bin/phpmd Plugin.php text phpmd.xml; test $? -le 1 &amp;&amp; ../../../vendor/bin/composer-dependency-analyser --config=composer-dependency-analyser.php 2&gt;&amp;1 | tail -3 | grep -qiE '(no\\ issues|0\\ errors)'</automated>
  </verify>
  <done>pint-test, analyse, phpmd, deps-check all exit 0 on the empty scaffold. The test step is acknowledged as deferred to plan 01-03 (plan 01-02 SUMMARY records this).</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries (Phase 1 plan 01-02 scope)

| Boundary | Description |
|----------|-------------|
| composer.json (plugin) → ../../../vendor/bin/* | Plugin scripts invoke binaries from repo-root vendor. If the repo-root composer.json is missing a dep, the script fails with "command not found". Task 8 closes this for shipmonk. |
| phpstan disallowed-calls config → developer's IDE auto-fix | A developer's IDE that auto-inserts array_find() will see the phpstan error; the config is the only enforcement layer. No network access required. |
| Lovata cart imports outside adapter dir → static analysis | Two layers: phpstan disallowedClasses (when added in future phase) + shipmonk dependency-analyser. Both required because phpstan analyses imports declaratively but shipmonk analyses composer dependency graph. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | A future developer disables a phpstan disallowed-call to fix a noisy error | mitigate | Each entry has a `message:` explaining WHY the call is banned. PR reviewer sees the entry was deleted. Code review catches it. |
| T-02-02 | Spoofing | shipmonk allowlist mis-scopes (e.g. typo `classes/adapter/shopaholic` vs `Classes/Adapter/Shopaholic`) | mitigate | Phase 3 lands actual adapter code. Run `composer deps-check` after the first adapter import; if it fails despite being in the correct dir, fix the allowlist path. Plan 03 SUMMARY records the verification. |
| T-02-03 | Denial of Service | qa chain too slow → developer skips it | accept | Fast-fail ordering (pint-test first, test-cov last). Empty scaffold runs the full chain in <2 seconds. Pre-commit hook (out of plan scope) can add `composer pint-test && composer analyse` to enforce. |
| T-02-04 | Information Disclosure | composer-dependency-analyser config references internal path patterns | accept | Config lives in plugin repo (will be public on marketplace). No secrets — just directory paths. No mitigation needed. |
| T-02-05 | Elevation of Privilege | shipmonk runs PHP config file with file_exists side effect | accept | The config file is plugin-owned, php -l clean (task 5 verify). Standard composer-dep-analyser pattern. |

</threat_model>

<verification>

## Goal-Backward Reachability Audit

1. "composer qa chain wired" — Task 6 writes the scripts block. Verified by `python3 -c 'json.load(...)'` assert.
2. "phpstan analyses at PHP 8.3 grammar level 10" — Task 1 writes `phpVersion: 80300`. Verified by `grep -q` in task 1 + actual phpstan run in task 9.
3. "spaze/phpstan-disallowed-calls bans 8.4 array funcs" — Task 1 writes the disallowedFunctionCalls block. Verified by grep in task 1.
4. "rector php83 set" — Task 2 writes `php83: true`. Verified by grep in task 2.
5. "pint nullable rule" — Task 3 writes the rule. Verified by grep in task 3.
6. "phpmd thresholds match TOOL-07" — Task 4 writes the ruleset. Verified by xmllint in task 4.
7. "shipmonk bans Lovata cart imports outside adapter dir" — Task 5 writes the allowlist. Verified by php -l in task 5 + actual deps-check run in task 9.
8. "qa exits 0 on empty scaffold (except deferred test step)" — Task 9 smoke-runs the chain.

No must-have is UNREACHABLE.

## Multi-Source Coverage Audit

| Source item | Type | Plan(s) covering | Notes |
|-------------|------|------------------|-------|
| GOAL phase 1 success criterion 1: `composer qa` exits 0 on empty scaffold | Goal | 01-02 tasks 6, 9 + 01-03 (provides test step) | Joint coverage with plan 01-03 |
| GOAL success criterion 3: 8.4-only syntax rejected | Goal | 01-02 (phpstan + rector + pint enforce) | Fully covered |
| GOAL success criterion 4: shipmonk would flag hidden Lovata imports | Goal | 01-02 task 5 + task 9 verification | Fully covered |
| REQ TOOL-04 (phpstan config) | Requirement | 01-02 task 1 | Fully covered |
| REQ TOOL-05 (rector config) | Requirement | 01-02 task 2 | Fully covered |
| REQ TOOL-06 (pint config) | Requirement | 01-02 task 3 | Fully covered |
| REQ TOOL-07 (phpmd config) | Requirement | 01-02 task 4 | Fully covered |
| REQ TOOL-10 (composer qa script) | Requirement | 01-02 task 6 | Fully covered |
| REQ TOOL-11 (shipmonk dependency-analyser) | Requirement | 01-02 tasks 5, 7, 8 | Fully covered |
| RESEARCH STACK.md §PHP 8.4-only features to AVOID table | Research | 01-02 task 1 (disallowedFunctionCalls + disallowedClasses) | Fully covered |
| PITFALL P-03 (hidden Lovata imports) | Pitfall | 01-02 task 5 | Static-analysis layer prevented |
| PITFALL P-06 (PHP 8.4-only syntax slips) | Pitfall | 01-02 tasks 1, 2, 3 (phpstan + rector + pint triple-layer) | Fully covered |
| PITFALL P-19 (Pest uses() carries Shopaholic-coupled base) | Pitfall | Plan 01-03 (test infra) | Out of scope for 01-02 |
| PITFALL P-20 (coverage on partial code paths) | Pitfall | Plan 01-03 (CI matrix) | Out of scope for 01-02 |
| CONTEXT lock "no phase/CR markers in code" | Context | 01-02 (all configs free of phase-numbered comments) | Fully honored |
| CONTEXT lock "no v1.x carry-forward, fresh and simple" | Context | 01-02 task 4 (drops mId exception, drops ExcessiveClassComplexity=55 carry-over) | Fully honored |

Coverage complete for this plan's scope.

</verification>

<success_criteria>

Plan 01-02 ships when ALL of the following hold:

1. `plugins/logingrupa/metapixel/phpstan.neon` exists with `phpVersion: 80300`, level 10, larastan + disallowed-calls includes, all five PHP 8.4-only function bans + Deprecated attribute ban.
2. `plugins/logingrupa/metapixel/rector.php` exists with `withPhpSets(php83: true)`; no `php84` references.
3. `plugins/logingrupa/metapixel/pint.json` exists with `nullable_type_declaration_for_default_null_value: true`; JSON valid.
4. `plugins/logingrupa/metapixel/phpmd.xml` exists with `name="Metapixel"`; xmllint-valid.
5. `plugins/logingrupa/metapixel/composer-dependency-analyser.php` exists; `php -l` clean; allowlists `classes/adapter/shopaholic/` for Lovata cart packages.
6. `plugins/logingrupa/metapixel/composer.json` has `scripts.qa = ["@pint-test", "@analyse", "@phpmd", "@deps-check", "@test-cov"]` and `require-dev` includes `shipmonk/composer-dependency-analyser`.
7. Repo-root `composer.json` has `shipmonk/composer-dependency-analyser` in `require-dev`; `composer.lock` updated; `vendor/bin/composer-dependency-analyser` exists and is executable.
8. From `plugins/logingrupa/metapixel/`, each of `composer pint-test`, `composer analyse`, `composer phpmd`, `composer deps-check` exits 0 on the empty scaffold.
9. `composer test` is acknowledged as deferred (plan 01-03 lands phpunit.xml + tests).
10. Exactly two commits added in this plan: (a) the bundled tooling-configs commit (task 7), (b) the repo-root require-dev commit (task 8).

</success_criteria>

<output>
After completion, create `plugins/logingrupa/metapixel/.planning/phases/01-tooling-composer-namespace-rename-ci-matrix/01-02-SUMMARY.md` documenting:
- The two commit SHAs produced by this plan
- Output of `composer pint-test`, `composer analyse`, `composer phpmd`, `composer deps-check` (tail 5 lines each) proving 0 errors on the empty scaffold
- Confirmation that `composer test` is deferred to plan 01-03 (no phpunit.xml yet)
- Handoff note for plan 01-03: "phpstan paths list contains only Plugin.php; phpmd command lists only Plugin.php. When tests/ lands in plan 01-03, neither config needs editing — `tests` is in phpstan excludePaths and is owned by phpunit.xml. The reopen discipline applies when Phase 2 lands classes/."
- Drift log: any deviations from the plan's `files_modified` frontmatter
</output>
