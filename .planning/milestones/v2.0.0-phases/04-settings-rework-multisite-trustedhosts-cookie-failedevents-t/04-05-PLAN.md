---
phase: 04-settings-rework-multisite-trustedhosts-cookie-failedevents-t
plan: 05
type: execute
wave: 4
depends_on: ["04-01", "04-02", "04-03", "04-04"]
files_modified:
  - plugins/logingrupa/metapixel/lang/en/lang.php
  - plugins/logingrupa/metapixel/lang/lv/lang.php
  - plugins/logingrupa/metapixel/models/settings/fields.yaml
  - plugins/logingrupa/metapixel/tests/Feature/Lang/LangKeyCoverageTest.php
autonomous: true
requirements: [LANG-01]
must_haves:
  truths:
    - "LANG-01/D-17: lang/en/lang.php + lang/lv/lang.php both return nested arrays with identical key shape (Pest coverage gate enforces)"
    - "LANG-01/D-18: LV translations are native Latvian (Latvian-fluent author wording — NOT machine-translated stubs)"
    - "LANG-01/D-19: ≈ 60 keys per language covering all 4 tab labels + 12 field labels + 12 field commentAbove + 10 failed_events column labels + 3 filter labels + 3 confirm modal labels + 3 button labels + backend menu (parent + child) + ≥ 6 validation/error messages + ≥ 3 exception messages"
    - "fields.yaml label paths migrated from settings.fields.* to field.* per RESEARCH Pattern 11 final shape (lines 1295-1366)"
    - "All UI strings in Phase 4 surfaces (Settings, FailedEvents, middleware error logs) resolve through Lang::get; no raw English in YAML/Twig"
    - "NO ru/lang.php shipped (D-17 lock — operator self-services Russian)"
  artifacts:
    - path: "plugins/logingrupa/metapixel/lang/en/lang.php"
      provides: "EN translations — RainLab.Translate-compatible nested array"
      contains: "trusted_hosts_label"
    - path: "plugins/logingrupa/metapixel/lang/lv/lang.php"
      provides: "LV translations — same key shape as EN, native Latvian"
      contains: "trusted_hosts_label"
    - path: "plugins/logingrupa/metapixel/tests/Feature/Lang/LangKeyCoverageTest.php"
      provides: "Pest assertion that LV key shape matches EN key shape"
      contains: "toEqualCanonicalizing"
  key_links:
    - from: "plugins/logingrupa/metapixel/models/settings/fields.yaml"
      to: "plugins/logingrupa/metapixel/lang/en/lang.php"
      via: "Every label / commentAbove path resolves to a non-empty string under field.* / tab.* / settings.* groups"
      pattern: "logingrupa.metapixel::lang\\."
    - from: "plugins/logingrupa/metapixel/lang/en/lang.php"
      to: "plugins/logingrupa/metapixel/lang/lv/lang.php"
      via: "Flatten-and-compare canonical key set match (Pest test)"
      pattern: "toEqualCanonicalizing"
---

<objective>
Implement LANG-01 — Populate `lang/en/lang.php` + `lang/lv/lang.php` with ~60 keys per language covering all Phase 4 UI surfaces (Settings tabs + fields, FailedEvents columns + filters + buttons + confirms, backend menu, exceptions). Migrate `models/settings/fields.yaml` label paths from `settings.fields.*` to the unified `field.*` shape per RESEARCH Pattern 11 final structure. Add a Pest coverage gate that walks the EN nested array and asserts LV exposes the same key shape.

Purpose: Marketplace-ready i18n. Removes raw-English leak risk from YAML/Twig. RainLab.Translate-compatible structure allows operator backend locale switching.

Output: Updated `lang/en/lang.php` (full expansion from current 14-key stub to ~60 keys), updated `lang/lv/lang.php` (native Latvian mirror), updated `models/settings/fields.yaml` (label/commentAbove path renames), 1 Pest test file enforcing parity.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@plugins/logingrupa/metapixel/.planning/PROJECT.md
@plugins/logingrupa/metapixel/.planning/ROADMAP.md
@plugins/logingrupa/metapixel/.planning/STATE.md
@plugins/logingrupa/metapixel/.planning/phases/04-settings-rework-multisite-trustedhosts-cookie-failedevents-t/04-CONTEXT.md
@plugins/logingrupa/metapixel/.planning/phases/04-settings-rework-multisite-trustedhosts-cookie-failedevents-t/04-RESEARCH.md
@plugins/logingrupa/metapixel/.planning/phases/04-settings-rework-multisite-trustedhosts-cookie-failedevents-t/04-PATTERNS.md
@plugins/logingrupa/metapixel/CLAUDE.md
@/home/forge/nailscosmetics.lv/CLAUDE.md
@plugins/logingrupa/metapixel/lang/en/lang.php
@plugins/logingrupa/metapixel/lang/lv/lang.php
@plugins/logingrupa/metapixel/models/settings/fields.yaml

<interfaces>
<!-- Final lang structure per RESEARCH Pattern 11 lines 1294-1366 -->

return [
    'plugin' => [...],
    'settings' => [
        'label', 'description', 'category',
        // settings.fields.* keys MIGRATED into top-level field.* group
    ],
    'tab' => [
        'pixel_and_capi', 'hosts_and_cookies', 'theme_tracking', 'advanced',
    ],
    'field' => [
        // pixel_id, capi_access_token, test_event_code, trusted_hosts,
        // ensure_fbp_fbc, theme_custom_event_names, paid_status_code,
        // default_currency_code — each with _label + _comment
        // ≥ 16 entries (8 fields × 2 keys)
    ],
    'menu' => [
        'label', 'failed_events', 'failed_events_description',
    ],
    'failed_events' => [
        'list_title', 'no_records', 'search_prompt',
        'column_id', 'column_event_id', 'column_event_name', 'column_adapter_type',
        'column_http_status', 'column_attempts', 'column_graph_error',
        'column_dedup_pct', 'column_emq', 'column_dedup_checked_at', 'column_created_at',
        'filter_event_name', 'filter_adapter_type', 'filter_date_range',
        'button_replay', 'button_check_dedup', 'button_delete',
        'confirm_replay', 'confirm_delete',
        'flash_replay_success', 'flash_replay_error', 'flash_dedup_success',
    ],
    'exception' => [
        'missing_pixel_config', 'missing_capi_token', 'order_has_no_currency',
        'invalid_trusted_hosts',  // D-14 strict-halt model exception message
    ],
];

Pest coverage gate (RESEARCH Pattern 11 lines 1375-1397 — flatten + canonical compare):
expect($arLvKeys)->toEqualCanonicalizing($arEnKeys);
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 0: Wave 0 — author failing LangKeyCoverageTest</name>
  <files>
    plugins/logingrupa/metapixel/tests/Feature/Lang/LangKeyCoverageTest.php
  </files>
  <read_first>
    plugins/logingrupa/metapixel/lang/en/lang.php (current 14-key stub),
    plugins/logingrupa/metapixel/lang/lv/lang.php (current subset),
    plugins/logingrupa/metapixel/tests/Unit/PluginSanityTest.php (in-plugin Pest unit pattern — top-level test pattern),
    plugins/logingrupa/metapixel/.planning/phases/04-settings-rework-multisite-trustedhosts-cookie-failedevents-t/04-RESEARCH.md (Pattern 11 lines 1373-1397 — flatten + canonical compare snippet),
    plugins/logingrupa/metapixel/.planning/phases/04-settings-rework-multisite-trustedhosts-cookie-failedevents-t/04-PATTERNS.md (Tests sibling table line 930)
  </read_first>
  <action>
    Author `tests/Feature/Lang/LangKeyCoverageTest.php` (lowercase folder + PascalCase basename).

    `final class LangKeyCoverageTest extends MetapixelTestCase`.

    setUp: `parent::setUp();` — no DB-mutating work needed; lang loading is filesystem-only.

    Cases (≥ 8):
    - `test_en_lang_file_exists_and_returns_array`: `$arEn = require base_path('plugins/logingrupa/metapixel/lang/en/lang.php');` → assert `is_array($arEn)`.
    - `test_lv_lang_file_exists_and_returns_array`: same for `/lv/`.
    - `test_no_ru_lang_file_shipped`: assert `! file_exists(base_path('plugins/logingrupa/metapixel/lang/ru/lang.php'))` (D-17 lock — RU dropped).
    - `test_en_lang_has_at_least_50_keys`: flatten the nested array and assert key count ≥ 50.
    - `test_lv_key_shape_matches_en`: flatten both and assert `toEqualCanonicalizing` (Pest matcher). Implementation per RESEARCH Pattern 11 lines 1380-1396 (recursive closure flattening dot-notation keys; canonical-sort match).
    - `test_required_phase_4_keys_exist_in_en`: assert specific keys present (use Lang::get-style dot-notation read): `field.trusted_hosts_label`, `field.trusted_hosts_comment`, `field.ensure_fbp_fbc_label`, `field.ensure_fbp_fbc_comment`, `tab.pixel_and_capi`, `tab.hosts_and_cookies`, `tab.theme_tracking`, `tab.advanced`, `failed_events.list_title`, `failed_events.column_dedup_pct`, `failed_events.button_replay`, `failed_events.button_check_dedup`, `failed_events.confirm_replay`, `menu.failed_events`, `exception.missing_pixel_config`, `exception.invalid_trusted_hosts`.
    - `test_lv_strings_are_not_blank`: every leaf string in LV is non-empty (catches missed translations).
    - `test_lv_strings_are_not_machine_translation_artifacts`: assert no LV string starts with `[TODO]` or `[TRANSLATE]` (sanity check that operator authored — D-18 native-Latvian lock).

    Hungarian notation throughout (`$arEn`, `$arLv`, `$arEnKeys`, `$fnFlatten`, etc).
  </action>
  <acceptance_criteria>
    - File `tests/Feature/Lang/LangKeyCoverageTest.php` exists.
    - File contains literal `toEqualCanonicalizing` (Pest matcher for key-shape parity).
    - File contains literal `field.trusted_hosts_label` AND `tab.hosts_and_cookies` AND `failed_events.column_dedup_pct` (required-key assertions).
    - File contains literal `'lang/ru/lang.php'` reverse check (D-17 RU absent lock).
    - `grep -c 'function test_' tests/Feature/Lang/LangKeyCoverageTest.php` returns ≥ 8.
    - Tests currently fail (red) — `composer test -- --filter=LangKeyCoverageTest` exits NON-ZERO.
  </acceptance_criteria>
  <verify>
    <automated>test -f plugins/logingrupa/metapixel/tests/Feature/Lang/LangKeyCoverageTest.php &amp;&amp; cd plugins/logingrupa/metapixel &amp;&amp; [ "$(grep -c 'function test_' tests/Feature/Lang/LangKeyCoverageTest.php)" -ge 8 ]</automated>
  </verify>
  <done>Wave 0 RED test in place; 8+ cases; LANG-01 coverage gate codified.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1: lang/en/lang.php expansion to ~60 keys + lang/lv/lang.php native Latvian mirror</name>
  <files>
    plugins/logingrupa/metapixel/lang/en/lang.php,
    plugins/logingrupa/metapixel/lang/lv/lang.php
  </files>
  <behavior>
    - `lang/en/lang.php` returns a nested array with 7 top-level groups: `plugin`, `settings`, `tab`, `field`, `menu`, `failed_events`, `exception` — totalling ~60 leaf entries per RESEARCH Pattern 11 lines 1294-1366.
    - `lang/lv/lang.php` returns an array with the SAME nested shape (same group names, same leaf keys) — values are native Latvian translations.
    - No leaf string is empty.
    - No machine-translation placeholder strings (`[TODO]`, `[TRANSLATE]`, etc).
    - Pest `LangKeyCoverageTest` exits 0 — flatten + canonical compare passes.
  </behavior>
  <read_first>
    plugins/logingrupa/metapixel/lang/en/lang.php (current 14-key stub — preserve plugin.* + settings.* top-level; expand into new groups),
    plugins/logingrupa/metapixel/lang/lv/lang.php (current LV subset — preserve native wording where present, expand to match EN shape),
    plugins/logingrupa/metapixel/.planning/phases/04-settings-rework-multisite-trustedhosts-cookie-failedevents-t/04-RESEARCH.md (Pattern 11 lines 1294-1366 — full EN shape verbatim; D-18 native LV requirement; D-19 coverage list)
  </read_first>
  <action>
    1) Rewrite `plugins/logingrupa/metapixel/lang/en/lang.php` per RESEARCH Pattern 11 final shape (lines 1294-1366) verbatim:

    Structure (top-level groups + their leaves):

    `plugin` (2 leaves):
    - `name` = 'Meta Pixel + Conversions API'
    - `description` = 'Server-deduplicated Meta Pixel and Conversions API tracking via adapter pattern.'

    `settings` (3 leaves):
    - `label` = 'Meta Pixel + CAPI'
    - `description` = 'Configure the Pixel ID, CAPI access token, and Test Events code for Meta tracking.'
    - `category` = 'Marketing'

    `tab` (4 leaves per D-15):
    - `pixel_and_capi`, `hosts_and_cookies`, `theme_tracking`, `advanced` — English UI labels.

    `field` (16+ leaves — every Phase 4 field gets `_label` + `_comment` pair):
    - `pixel_id_label`, `pixel_id_comment`
    - `capi_access_token_label`, `capi_access_token_comment`
    - `test_event_code_label`, `test_event_code_comment`
    - `trusted_hosts_label` = 'Trusted Hosts'
    - `trusted_hosts_comment` per RESEARCH Pattern 11 line 1319: 'One host per line. The plugin sets _fbp / _fbc cookies only on these hosts. Sub-TLDs supported via the bundled Public Suffix List.'
    - `ensure_fbp_fbc_label` = 'Set _fbp / _fbc cookies server-side'
    - `ensure_fbp_fbc_comment` per RESEARCH Pattern 11 line 1321: 'Turn OFF if your theme already writes these cookies, or for GDPR consent banner integration where cookies must wait for opt-in.'
    - `theme_custom_event_names_label`, `theme_custom_event_names_comment`
    - `paid_status_code_label`, `paid_status_code_comment`
    - `default_currency_code_label`, `default_currency_code_comment`

    `menu` (3 leaves):
    - `label` = 'Meta Pixel'
    - `failed_events` = 'Failed events'
    - `failed_events_description` = 'Replay or check dedup status of Meta CAPI events that failed retry.'

    `failed_events` (≈ 23 leaves):
    - `list_title`, `no_records`, `search_prompt`
    - `column_id`, `column_event_id`, `column_event_name`, `column_adapter_type`, `column_http_status`, `column_attempts`, `column_graph_error`, `column_dedup_pct`, `column_emq`, `column_dedup_checked_at`, `column_created_at` (10 column labels per columns.yaml from plan 04-04)
    - `filter_event_name`, `filter_adapter_type`, `filter_date_range` (3 filter labels)
    - `button_replay`, `button_check_dedup`, `button_delete` (3 button labels per toolbar partial)
    - `confirm_replay`, `confirm_delete` (2 modal-confirm strings)
    - `flash_replay_success`, `flash_replay_error`, `flash_dedup_success` (3 flash strings — controller messages can interpolate `:event_id` and `:error` placeholders per Laravel Lang convention)

    `exception` (4 leaves):
    - `missing_pixel_config`, `missing_capi_token`, `order_has_no_currency` (Phase 2 carry-forward)
    - `invalid_trusted_hosts` — D-14 strict-halt model exception message: 'metapixel: invalid trusted_hosts — unknown TLD or invalid charset on: :rejected'

    Total: ≈ 55-60 leaves.

    2) Rewrite `plugins/logingrupa/metapixel/lang/lv/lang.php` — same exact nested structure as EN, with native Latvian translations (D-18 lock). Sample translations:
    - `tab.pixel_and_capi` = 'Pixel & CAPI'  (technical terms retained per Latvian software-localization convention)
    - `tab.hosts_and_cookies` = 'Resursdatori un sīkfaili'
    - `tab.theme_tracking` = 'Tēmas izsekošana'
    - `tab.advanced` = 'Papildu iestatījumi'
    - `field.trusted_hosts_label` = 'Uzticamie resursdatori'
    - `field.trusted_hosts_comment` = 'Viens resursdators rindā. Spraudnis iestata _fbp / _fbc sīkfailus tikai šajos resursdatoros. Apakšdomēni tiek atbalstīti ar iebūvēto Public Suffix List.'
    - `field.ensure_fbp_fbc_label` = 'Iestatīt _fbp / _fbc sīkfailus servera pusē'
    - `field.ensure_fbp_fbc_comment` = 'Izslēdziet, ja jūsu tēma jau raksta šos sīkfailus, vai GDPR piekrišanas integrācijai, kur sīkfailiem jāgaida atļauja.'
    - `failed_events.list_title` = 'Neveiksmīgie CAPI notikumi'
    - `failed_events.button_replay` = 'Pārsūtīt'
    - `failed_events.button_check_dedup` = 'Pārbaudīt dedup'
    - `failed_events.button_delete` = 'Dzēst'
    - `exception.invalid_trusted_hosts` = 'metapixel: nederīgi uzticamie resursdatori — nezināms TLD vai nederīgs simbols: :rejected'

    All leaves MUST be non-empty Latvian strings (native). Preserve technical terms (`Pixel`, `CAPI`, `_fbp`, `_fbc`, `event_id`, `EMQ`) unchanged — Latvian software localization convention keeps acronyms in source language. NO `[TODO]` placeholders, NO machine-translation artifacts.

    Both files: leave a class-level `<?php` opener; no `declare(strict_types=1)` (plugin CLAUDE.md "NO declare(strict_types=1) enforcement").

    Apply Pint after edit (Pint will format the PHP array syntax to Laravel preset).
  </action>
  <acceptance_criteria>
    - `lang/en/lang.php` `return` array flattens to ≥ 50 leaves.
    - `lang/lv/lang.php` `return` array flattens to ≥ 50 leaves.
    - Both files contain top-level keys `plugin`, `settings`, `tab`, `field`, `menu`, `failed_events`, `exception`.
    - Both files contain literal `'trusted_hosts_label'`, `'ensure_fbp_fbc_label'`, `'column_dedup_pct'`, `'button_replay'`, `'confirm_replay'`, `'invalid_trusted_hosts'`.
    - NO file at `lang/ru/lang.php` exists (D-17 lock).
    - `lang/lv/lang.php` contains at least 3 non-ASCII characters (`ē`, `ā`, `ī`, `š`, `ū`, etc.) — proxy for native Latvian content.
    - `composer test -- --filter=LangKeyCoverageTest` exits 0 — all 8+ cases pass (canonical key-shape parity proven).
    - `vendor/bin/pint --test --format agent plugins/logingrupa/metapixel/lang/en/lang.php plugins/logingrupa/metapixel/lang/lv/lang.php` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd plugins/logingrupa/metapixel &amp;&amp; composer test -- --filter=LangKeyCoverageTest</automated>
  </verify>
  <done>EN + LV lang files populated end-to-end; canonical key parity verified; native Latvian strings throughout.</done>
</task>

<task type="auto">
  <name>Task 2: fields.yaml label paths migration to field.* + final composer qa</name>
  <files>
    plugins/logingrupa/metapixel/models/settings/fields.yaml
  </files>
  <read_first>
    plugins/logingrupa/metapixel/models/settings/fields.yaml (current state after plan 04-02 4-tab restructure — label paths still under settings.fields.*),
    plugins/logingrupa/metapixel/lang/en/lang.php (now expanded with `field.*` group keys from Task 1),
    plugins/logingrupa/metapixel/.planning/phases/04-settings-rework-multisite-trustedhosts-cookie-failedevents-t/04-PATTERNS.md (fields.yaml section lines 119-150 — D-17 path-rename annotation referenced),
    plugins/logingrupa/metapixel/.planning/phases/04-settings-rework-multisite-trustedhosts-cookie-failedevents-t/04-RESEARCH.md (Pattern 6 lines 663-705 — fields.yaml tab+label shape)
  </read_first>
  <action>
    Edit `plugins/logingrupa/metapixel/models/settings/fields.yaml`:

    For each existing field block, rename the lang-key paths from `settings.fields.*` → `field.*` to match the lang file structure from Task 1. Concrete renames:

    - `label: logingrupa.metapixel::lang.settings.fields.pixel_id_label` → `label: logingrupa.metapixel::lang.field.pixel_id_label`
    - `commentAbove: logingrupa.metapixel::lang.settings.fields.pixel_id_comment` → `commentAbove: logingrupa.metapixel::lang.field.pixel_id_comment`
    - Same pattern for `capi_access_token_*`, `test_event_code_*`, `paid_status_code_*`, `default_currency_code_*`, `theme_custom_event_names_*`, `trusted_hosts_*`, `ensure_fbp_fbc_*`.

    Per RESEARCH Pattern 11 / D-17 — the `settings.*` group keeps `label`, `description`, `category` only (registerSettings entry references). The `settings.fields.*` sub-group is RETIRED in favour of the new top-level `field.*` group.

    Remove the YAML comment `# Label keys remain settings.fields.* until plan 04-05 LANG-01 ships the unified field.* shape.` that plan 04-02 Task 4 left as a marker — that work is now done.

    Smoke check after edit:
    - `grep -c 'settings.fields' plugins/logingrupa/metapixel/models/settings/fields.yaml` returns 0 (full migration).
    - `grep -c 'logingrupa.metapixel::lang.field' plugins/logingrupa/metapixel/models/settings/fields.yaml` returns ≥ 16 (8 fields × 2 keys = label + commentAbove).
    - All Phase 4 fields preserved (no accidental field deletion).

    Then run the full QA chain to confirm Phase 4 close-out:
    - `composer qa` — full pint + phpstan + phpmd + pest --coverage chain.
    - Smoke: `php artisan list metapixel` from host root lists both `metapixel:purge-event-log` AND `metapixel:refresh-psl`.

    Apply Pint (YAML files are not Pint-formatted but PHP files updated transitively might need formatting).
  </action>
  <acceptance_criteria>
    - `models/settings/fields.yaml` contains literal `logingrupa.metapixel::lang.field.pixel_id_label` (rename done).
    - `grep -c 'settings.fields' plugins/logingrupa/metapixel/models/settings/fields.yaml` returns 0.
    - `grep -c 'logingrupa.metapixel::lang.field' plugins/logingrupa/metapixel/models/settings/fields.yaml` returns ≥ 16.
    - All 8 field blocks (`pixel_id`, `capi_access_token`, `test_event_code`, `paid_status_code`, `default_currency_code`, `theme_custom_event_names`, `trusted_hosts`, `ensure_fbp_fbc_server_side`) still present with `tab:` and `type:` attributes intact.
    - `composer test -- --filter=LangKeyCoverageTest` STILL exits 0 (no regression from fields.yaml edit).
    - `composer test -- --filter='SettingsLookupForSiteTest|HostIndexResolverTest|EnsureFbpFbcCookiesTest|FailedEventsListTest|FailedEventsReplayTest|FailedEventsCheckDedupTest|TrustedHostsValidationTest|RefreshPslTest|AddMultisitePixelIdAndTokenTest|AddDedupColumnsToFailedEventsTest|MultisiteEventLogRoutingTest|SettingsMultisiteTraitTest'` exits 0 (regression across plans 04-01..04-04).
    - `composer qa` exits 0 — full chain green at Phase 4 close.
  </acceptance_criteria>
  <verify>
    <automated>cd plugins/logingrupa/metapixel &amp;&amp; composer qa</automated>
  </verify>
  <done>fields.yaml label paths migrated to field.* shape; full Phase 4 regression suite green; composer qa exits 0 at Phase 4 close-out.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator browser → backend Settings UI | All UI strings rendered server-side from `Lang::get` resolving the lang/{en,lv}/lang.php arrays. No raw English leak. |
| `Lang::get('logingrupa.metapixel::lang.exception.invalid_trusted_hosts')` → admin Flash::error UI | Translated strings interpolate `:rejected` placeholder; placeholders use Laravel's safe substitution. |
| `Lang::get` ← any YAML config / Twig / controller flash | Read-only lang file load; arrays are require'd PHP literals — no user input boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-LANG-01 | Information Disclosure | Raw English string leak in UI when LV locale active | mitigate | LangKeyCoverageTest enforces canonical key-shape parity (Task 0 — `toEqualCanonicalizing`); CI gate blocks merge if LV has fewer keys than EN. |
| T-04-LANG-02 | Tampering / XSS | Lang strings injected with HTML markup | accept | All lang strings are plain prose; consumers (`<?= e(trans(...)) ?>` in toolbar partials per plan 04-04, Twig auto-escape in fields.yaml) escape output. No raw HTML lang values. |
| T-04-LANG-03 | Repudiation | Operator sees `:rejected` placeholder unsubstituted in Flash::error | mitigate | `Flash::error('metapixel: rejected trusted_hosts: ' . implode(...))` in plan 04-02 Task 4 inlines the value — does NOT use the lang `exception.invalid_trusted_hosts` placeholder path at runtime. The lang key is for future README + documentation surface use; operator runtime sees the inline-PHP-built string. |
</threat_model>

<verification>
- Task 0 — `composer test -- --filter=LangKeyCoverageTest` exits NON-ZERO (red — EN file not yet expanded).
- Task 1 — `composer test -- --filter=LangKeyCoverageTest` exits 0; 8+ cases pass; canonical EN/LV parity proven.
- Task 2 — `composer qa` exits 0 across full plan 04-01..04-05 regression matrix; fields.yaml label paths fully migrated.
- Phase 4 close gate: `composer qa` exits 0; coverage ≥ 90%; 19/19 REQ-IDs implemented.
</verification>

<success_criteria>
- LANG-01 / D-17: lang/en/lang.php + lang/lv/lang.php both ~60 keys, identical nested shape, RainLab.Translate-compatible.
- LANG-01 / D-18: LV is native Latvian (non-ASCII char count + no TODO placeholders verified).
- LANG-01 / D-19: ≥ 60-key coverage per language including all tab labels, field labels + comments, FailedEvents columns + filters + buttons + confirms + flash, menu labels, exception messages.
- fields.yaml label paths fully migrated from `settings.fields.*` to `field.*` (RESEARCH Pattern 11 final shape).
- D-17 RU-absent lock — no `lang/ru/lang.php` ships.
- Pest coverage gate enforces canonical key-shape parity.
- `composer qa` exits 0 at Phase 4 close; coverage ≥ 90%.
</success_criteria>

<output>
Create `.planning/phases/04-settings-rework-multisite-trustedhosts-cookie-failedevents-t/04-05-SUMMARY.md` when done — capture: EN key count, LV key count, fields.yaml rename diff summary, regression confirmation across plans 04-01..04-04, native Latvian sanity (non-ASCII char count + non-empty leaves), Phase 4 close-out marker.
</output>
