---
phase: 04-settings-rework-multisite-trustedhosts-cookie-failedevents-t
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - plugins/logingrupa/metapixel/composer.json
  - plugins/logingrupa/metapixel/classes/helper/HostIndexResolver.php
  - plugins/logingrupa/metapixel/console/RefreshPsl.php
  - plugins/logingrupa/metapixel/resources/data/public_suffix_list.dat
  - plugins/logingrupa/metapixel/models/Settings.php
  - plugins/logingrupa/metapixel/models/settings/fields.yaml
  - plugins/logingrupa/metapixel/Plugin.php
  - plugins/logingrupa/metapixel/tests/Unit/Helper/HostIndexResolverTest.php
  - plugins/logingrupa/metapixel/tests/Feature/Settings/TrustedHostsValidationTest.php
  - plugins/logingrupa/metapixel/tests/Feature/Console/RefreshPslTest.php
  - plugins/logingrupa/metapixel/tests/fixtures/data/test_psl.dat
autonomous: false  # has checkpoint:human-verify gate for jeremykendall/php-domain-parser legitimacy
requirements: [HOST-01, HOST-02, HOST-03, HOST-04, HOST-05, HOST-06]
must_haves:
  truths:
    - "HOST-01/D-13/D-14: trusted_hosts textarea exists in Settings; strict beforeSave validation rejects unknown-TLD lines via PSL"
    - "HOST-02/D-12: HostIndexResolver wraps jeremykendall/php-domain-parser ^6.4 and returns int subdomain-index (1 apex, 2 www, etc.) or null for unresolvable hosts"
    - "HOST-03/D-09/D-11: PSL data ships at resources/data/public_suffix_list.dat; metapixel:refresh-psl artisan command updates from upstream; cache wiped at storage/app/metapixel/psl/"
    - "HOST-04: HostIndexResolver bound as App singleton in Plugin::register; resolve() returns null for unknown TLD — middleware-callable contract"
    - "HOST-05: Multi-TLD matrix — apex example.test, www.example.test, example.co.uk, www.example.co.uk, shop.example.com.br, IDN xn--bcher-kva.example — all resolve to correct subdomain-index"
    - "HOST-06: Untrusted host fail-safe — resolve(unknown.tld) returns null; partition rejects in beforeSave"
    - "D-10: Stale-PSL operator nudge — HostIndexResolver checks filemtime(PSL) once per request; if older than 180 days, emits Log::warning('PSL snapshot is N days old — run php artisan metapixel:refresh-psl') exactly once via request-scoped one-shot flag. Cookies still write (D-10 operator-feedback rationale: silently breaking tracking on stale snapshot is worse than a log line; PSL is additive-only so old entries remain valid)."
    - "D-15: Settings fields.yaml ships 4-tab layout — tab.pixel_and_capi, tab.hosts_and_cookies, tab.theme_tracking, tab.advanced. Task 4 assigns each field to one of these tabs; v1.x tab name 'Compliance' is dropped (vague for marketplace audience). Mapping matches the README section structure."
  artifacts:
    - path: "plugins/logingrupa/metapixel/classes/helper/HostIndexResolver.php"
      provides: "PSL-aware subdomain-index resolver"
      contains: "public function resolve(string $sHost): ?int"
    - path: "plugins/logingrupa/metapixel/console/RefreshPsl.php"
      provides: "metapixel:refresh-psl artisan command"
      contains: "metapixel:refresh-psl"
    - path: "plugins/logingrupa/metapixel/resources/data/public_suffix_list.dat"
      provides: "Bundled Mozilla PSL snapshot (D-09)"
      min_lines: 1000
    - path: "plugins/logingrupa/metapixel/composer.json"
      provides: "jeremykendall/php-domain-parser ^6.4 in require: (NOT require-dev)"
      contains: "jeremykendall/php-domain-parser"
  key_links:
    - from: "plugins/logingrupa/metapixel/Plugin.php::register"
      to: "App::singleton(HostIndexResolver::class, ...)"
      via: "Service-container singleton binding with base_path('plugins/logingrupa/metapixel/resources/data/public_suffix_list.dat')"
      pattern: "HostIndexResolver::class"
    - from: "plugins/logingrupa/metapixel/models/Settings.php::beforeSave"
      to: "App::make(HostIndexResolver::class)->resolve(\\$sHost)"
      via: "partitionHosts private helper rejects null-resolving lines"
      pattern: "HostIndexResolver::class"
---

<objective>
Implement HOST-01..06 — Operator-supplied `trusted_hosts` allowlist + PSL-aware `HostIndexResolver` + bundled PSL data file + `metapixel:refresh-psl` artisan command. Adds strict `Settings::beforeSave` validation that rejects unknown-TLD lines (D-14). Closes P-15 marketplace launch blocker (host-spoofing safety) at the resolver + Settings layer; the middleware that consumes the resolver lands in plan 04-03.

Purpose: Replaces v1.x hardcoded `HOST_INDEX_MAP` with a marketplace-ready operator-configurable + PSL-derived computation that works on any multi-TLD (`.co.uk`, `.com.br`, IDN). Untrusted-host = null-result = fail-safe NO-OP downstream.

Output: New `composer.json` dependency + `HostIndexResolver` class (with D-10 stale-PSL operator nudge) + `RefreshPsl` command + bundled PSL data file + updated `Settings::beforeSave` + extended `fields.yaml` (4-tab layout per D-15) + 3 Pest test files + 1 hermetic PSL fixture.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@plugins/logingrupa/metapixel/.planning/PROJECT.md
@plugins/logingrupa/metapixel/.planning/ROADMAP.md
@plugins/logingrupa/metapixel/.planning/STATE.md
@plugins/logingrupa/metapixel/.planning/phases/04-settings-rework-multisite-trustedhosts-cookie-failedevents-t/04-CONTEXT.md
@plugins/logingrupa/metapixel/.planning/phases/04-settings-rework-multisite-trustedhosts-cookie-failedevents-t/04-RESEARCH.md
@plugins/logingrupa/metapixel/.planning/phases/04-settings-rework-multisite-trustedhosts-cookie-failedevents-t/04-PATTERNS.md
@plugins/logingrupa/metapixel/CLAUDE.md
@/home/forge/nailscosmetics.lv/CLAUDE.md
@plugins/logingrupa/metapixel/composer.json
@plugins/logingrupa/metapixel/classes/helper/PluginGuard.php
@plugins/logingrupa/metapixel/console/PurgeEventLog.php
@plugins/logingrupa/metapixel/models/Settings.php
@plugins/logingrupa/metapixel/models/settings/fields.yaml
@plugins/logingrupa/metapixel/Plugin.php

<interfaces>
<!-- Public contract for HostIndexResolver — consumed by plan 04-03 middleware and Settings::beforeSave. -->

Logingrupa\Metapixel\Classes\Helper\HostIndexResolver (NEW):
```
public function __construct(private readonly string $sPslPath)
public function resolve(string $sHost): ?int   // 1 = apex, 2 = www, 3 = a.b.example.com, null = unresolvable/unknown TLD
```
Bound as service-container singleton in Plugin::register:
```
$this->app->singleton(HostIndexResolver::class, function () {
    return new HostIndexResolver(
        base_path('plugins/logingrupa/metapixel/resources/data/public_suffix_list.dat')
    );
});
```

Stale-PSL operator nudge (D-10) — private members:
```
private bool $bStaleWarningEmitted = false;
private const STALE_THRESHOLD_SECONDS = 15552000; // 180 days × 86400
```
On first resolve() call per resolver instance (request-scoped because the resolver is a singleton bound per request), `checkPslAge()` runs once: reads `filemtime($this->sPslPath)`, computes age in seconds against `time()`, and if `$iAgeSeconds > self::STALE_THRESHOLD_SECONDS` emits `Log::warning('PSL snapshot is ' . $iAgeDays . ' days old — run php artisan metapixel:refresh-psl')`. The `$bStaleWarningEmitted = true` latch prevents duplicate warnings within a single request.

Logingrupa\Metapixel\Console\RefreshPsl (NEW):
```
protected $signature = 'metapixel:refresh-psl'
public function handle(): int   // self::SUCCESS / self::FAILURE
```
URL constant `UPSTREAM_URL = 'https://publicsuffix.org/list/public_suffix_list.dat'` — pinned per SSRF mitigation.
Sentinel constant `SENTINEL = '// ===BEGIN ICANN DOMAINS==='`.

Pdp API surface (jeremykendall/php-domain-parser ^6.4):
```
Pdp\Rules::fromPath(string $sPath): Rules
Pdp\Domain::fromIDNA2008(string $sHost): Domain   // ext-intl required
$obRules->resolve(Domain $obDomain): ResolvedDomainName
$obResolved->subDomain()->labels(): array
$obResolved->suffix()->isPublicSuffix(): bool
$obResolved->secondLevelDomain()->value(): ?string
```
Throws: Pdp\UnableToResolveDomain, Pdp\SyntaxError (catch as \Throwable per Pitfall 5).
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 0: Legitimacy checkpoint — jeremykendall/php-domain-parser ^6.4</name>
  <what-built>Composer dependency add: `jeremykendall/php-domain-parser:^6.4`. NOT YET run — pending operator verification per 04-RESEARCH §"Package Legitimacy Audit" (slopcheck CLI unavailable in environment; package marked [ASSUMED]).</what-built>
  <how-to-verify>
    Operator opens https://packagist.org/packages/jeremykendall/php-domain-parser in a browser and confirms each of:
    1. Package name = `jeremykendall/php-domain-parser` (exact).
    2. Latest stable version ≥ `6.4.0` (released 2025-04-26).
    3. License = `MIT`.
    4. Source repo = `https://github.com/jeremykendall/php-domain-parser`.
    5. Total installs ≥ 13M (proxy for marketplace trust).
    6. Required PHP extension: `ext-intl` (confirm host PHP 8.4 has it: `php -m | grep intl` returns `intl`).

    If ANY of the six fails OR the package shows abandonment / hostile takeover signals: type `rejected` and STOP — planner must research an alternative.
  </how-to-verify>
  <resume-signal>Type `approved` to proceed with `composer require`. Type `rejected` and describe issue to halt the plan.</resume-signal>
</task>

<task type="auto">
  <name>Task 1: composer require jeremykendall/php-domain-parser ^6.4 + Wave 0 test scaffolds + hermetic PSL fixture</name>
  <files>
    plugins/logingrupa/metapixel/composer.json,
    plugins/logingrupa/metapixel/tests/Unit/Helper/HostIndexResolverTest.php,
    plugins/logingrupa/metapixel/tests/Feature/Settings/TrustedHostsValidationTest.php,
    plugins/logingrupa/metapixel/tests/Feature/Console/RefreshPslTest.php,
    plugins/logingrupa/metapixel/tests/fixtures/data/test_psl.dat
  </files>
  <read_first>
    plugins/logingrupa/metapixel/composer.json (current require/require-dev shape — verify Guzzle present; verify no PSL dep currently),
    plugins/logingrupa/metapixel/tests/Unit/Helper/SiteResolverTest.php (in-plugin Unit helper test pattern — analog from 04-PATTERNS.md line 924),
    plugins/logingrupa/metapixel/tests/Feature/Console/PurgeEventLogTest.php (verbatim setUp/tearDown — 04-PATTERNS.md lines 932-961),
    plugins/logingrupa/metapixel/.planning/phases/04-settings-rework-multisite-trustedhosts-cookie-failedevents-t/04-RESEARCH.md (Pattern 4 — HostIndexResolver body lines 514-580; Pattern 5 — RefreshPsl body lines 596-647; Pitfall 7 — `require:` not `require-dev:` lines 1529-1536; Wave 0 Gaps lines 1825-1834),
    plugins/logingrupa/metapixel/.planning/phases/04-settings-rework-multisite-trustedhosts-cookie-failedevents-t/04-PATTERNS.md (HostIndexResolver section lines 317-394; RefreshPsl section lines 409-446; Tests sibling table line 924)
  </read_first>
  <action>
    1) Edit `composer.json`: append `"jeremykendall/php-domain-parser": "^6.4"` to the `require:` block (NOT `require-dev:` — Pitfall 7 lock). DO NOT run `composer update` from the plugin dir per H-4 plugin marketplace contract — the project root operator runs `composer update logingrupa/oc-metapixel-plugin --with-dependencies` to refresh the host lockfile. For this plan's test runs in this repo, run a one-time `composer install --working-dir=/home/forge/nailscosmetics.lv` from the host root to land the dep in the host's `vendor/` (or rely on the host already carrying it transitively — verify with `composer show jeremykendall/php-domain-parser` from host root).

    2) Author 3 Pest test files under lowercase `tests/` (PascalCase class basenames):

    `tests/Unit/Helper/HostIndexResolverTest.php` (HOST-02 + HOST-04 + HOST-05 + HOST-06 + D-10):
    - `final class HostIndexResolverTest extends MetapixelTestCase` — but DO NOT require the framework SQLite scaffold; this is a pure unit test. The setUp() can skip parent::setUp() heavy work or use `MetapixelTestCase::setUp()` and tolerate the extra cost.
    - Constructor takes hermetic PSL fixture path `tests/fixtures/data/test_psl.dat` (small subset shipped this task).
    - `#[DataProvider('provideKnownHosts')]` data-driven cases (PHPUnit 12 attribute per Phase 2 STATE.md lock):
      - `example.test` → 1 (apex on .test PSL) — if `.test` isn't in PSL, switch to `example.com`.
      - `www.example.test` → 2 (or `www.example.com` → 2).
      - `example.co.uk` → 1.
      - `www.example.co.uk` → 2.
      - `shop.example.com.br` → 2.
      - `a.b.example.com` → 3.
      - `xn--bcher-kva.example` → IDN — assert NOT null when `.example` is a known PSL suffix; if `.example` not in PSL, switch fixture to a known IDN-eligible suffix.
    - `#[DataProvider('provideUnresolvableHosts')]`:
      - `''` → null.
      - `'127.0.0.1'` → null (IP — Pitfall 5).
      - `'localhost'` → null.
      - `'invalid_with_underscore.example'` → null.
      - `'host-with-unknown-tld.fakeylock'` → null.
    - `test_resolve_memoizes_repeated_lookups`: instrument by spying on Pdp\Rules — alternatively, simply assert two successive `resolve('example.co.uk')` calls produce identical results without throwing (cheap smoke).
    - `test_stale_psl_emits_log_warning_once_when_filemtime_older_than_180_days` (NEW — D-10 acceptance):
      - **Setup:** copy `tests/fixtures/data/test_psl.dat` to a tmp path under `sys_get_temp_dir()` (use `tempnam()` for portability across CI/local), then set its mtime to 200 days in the past via `touch($sTmpPath, Carbon::now()->subDays(200)->getTimestamp())`. Use `\Carbon\Carbon` (October-aliased `Carbon`) rather than `touch -t` shell — portable across Linux/macOS CI runners.
      - **Log capture:** call `Log::shouldReceive('warning')->once()->with(Mockery::pattern('/^PSL snapshot is \d+ days old/'))->andReturnNull();` BEFORE instantiating the resolver. (`Mockery` is already wired through `MetapixelTestCase`; if not, fall back to `Log::spy()` + `Log::shouldHaveReceived('warning')->once();` Pest pattern.)
      - **Act:** `$obResolver = new HostIndexResolver($sTmpPath);` then call `$obResolver->resolve('example.co.uk')` twice in succession.
      - **Assert:** Mockery exits 0 — the `->once()` constraint fails the test if `Log::warning` was emitted twice or never. This proves the request-scoped one-shot latch (`$bStaleWarningEmitted = true` after first warn).
      - **Teardown:** `unlink($sTmpPath)`.
      - Companion test `test_fresh_psl_emits_no_warning_when_filemtime_within_180_days`: same setup but mtime set to 30 days ago; assert `Log::shouldNotReceive('warning')`.

    `tests/Feature/Settings/TrustedHostsValidationTest.php` (HOST-01):
    - `test_save_with_only_valid_hosts_persists_normalized_lowercase_string`: set `trusted_hosts` to `"  Example.Co.UK\nWWW.example.co.uk  \n"` → save → assert stored attribute equals `"example.co.uk\nwww.example.co.uk"` (trimmed + lowercased + de-duped).
    - `test_save_rejects_unknown_tld_line`: set `trusted_hosts` to `"example.co.uk\nbogus.fakeylock\n"` → expect `\October\Rain\Database\ModelException` thrown OR save aborts with `Flash::error` containing literal `bogus.fakeylock`. (D-14 says halt save; planner picks ModelException throw — confirms with RESEARCH §"TrustedHosts field UX" Pattern 6 line 723-727.)
    - `test_save_rejects_charset_violations`: input `"example.com/path"` → rejected (slash not in `/^[a-z0-9.-]+$/`).
    - `test_save_with_empty_input_is_noop`: empty string → save succeeds, attribute stays empty.
    - `test_save_preserves_existing_theme_custom_event_names_pipeline`: confirm the existing `splitEventNameInput` + `partitionEventNames` pipeline is not broken by the new `trusted_hosts` partition pair (regression guard).

    `tests/Feature/Console/RefreshPslTest.php` (HOST-03):
    - `test_refresh_psl_writes_atomic_rename_on_valid_upstream`: bind a fake GuzzleHttp\Client (or use Guzzle Mock+HandlerStack with Middleware::history per Phase 2 H-7 pattern) returning a body containing the sentinel `// ===BEGIN ICANN DOMAINS===` plus 100+ bytes of PSL-like content. Run `Artisan::call('metapixel:refresh-psl')` → assert exit code SUCCESS + bundle file mtime moved + cache dir at `storage/app/metapixel/psl/` cleaned.
    - `test_refresh_psl_rejects_response_missing_sentinel`: fake client returns body without sentinel → exit code FAILURE + bundle file UNCHANGED (atomic).
    - `test_refresh_psl_rejects_empty_response`: empty body → FAILURE + bundle unchanged.
    - `test_refresh_psl_handles_connect_exception`: handler throws ConnectException → FAILURE + bundle unchanged + no half-written tmp file lingering (`tmp` cleaned up).

    3) Author hermetic PSL fixture at `tests/fixtures/data/test_psl.dat`:
    - Verbatim subset of the real PSL with enough entries to resolve the HostIndexResolver test matrix: include `// ===BEGIN ICANN DOMAINS===` sentinel, list entries `com`, `co.uk`, `com.br`, `test`, `example`, and trailing `// ===END ICANN DOMAINS===`.
    - Keep < 8 KB so test boot is fast.

    Use Hungarian notation throughout. Every test file's docblock declares "Wave 0 RED — fails until plan 04-02 production code ships."
  </action>
  <acceptance_criteria>
    - `composer.json` contains literal `"jeremykendall/php-domain-parser": "^6.4"` under `require:` (NOT `require-dev:`).
    - File `tests/Unit/Helper/HostIndexResolverTest.php` exists; references `#[DataProvider(` attribute; has provideKnownHosts + provideUnresolvableHosts + at least one memoization test + the D-10 stale-PSL `test_stale_psl_emits_log_warning_once_when_filemtime_older_than_180_days` + fresh-PSL companion case.
    - File `tests/Feature/Settings/TrustedHostsValidationTest.php` exists with ≥ 5 `test_*` methods.
    - File `tests/Feature/Console/RefreshPslTest.php` exists with ≥ 4 `test_*` methods.
    - File `tests/fixtures/data/test_psl.dat` exists and contains literal `// ===BEGIN ICANN DOMAINS===` + literal `co.uk` + literal `com.br`.
    - `grep -c 'function test_' plugins/logingrupa/metapixel/tests/Unit/Helper/HostIndexResolverTest.php plugins/logingrupa/metapixel/tests/Feature/Settings/TrustedHostsValidationTest.php plugins/logingrupa/metapixel/tests/Feature/Console/RefreshPslTest.php` sum ≥ 18 (was 16; added 2 D-10 cases).
    - `grep -q 'test_stale_psl_emits_log_warning_once_when_filemtime_older_than_180_days' plugins/logingrupa/metapixel/tests/Unit/Helper/HostIndexResolverTest.php` exits 0.
    - `grep -q 'PSL snapshot is' plugins/logingrupa/metapixel/tests/Unit/Helper/HostIndexResolverTest.php` exits 0 (matches the log message substring asserted via `Mockery::pattern`).
    - Tests currently fail (red) — `composer test -- --filter='HostIndexResolverTest|TrustedHostsValidationTest|RefreshPslTest'` exits NON-ZERO.
  </acceptance_criteria>
  <verify>
    <automated>cd plugins/logingrupa/metapixel &amp;&amp; grep -q '"jeremykendall/php-domain-parser"' composer.json &amp;&amp; test -f tests/Unit/Helper/HostIndexResolverTest.php &amp;&amp; test -f tests/Feature/Settings/TrustedHostsValidationTest.php &amp;&amp; test -f tests/Feature/Console/RefreshPslTest.php &amp;&amp; test -f tests/fixtures/data/test_psl.dat &amp;&amp; grep -q 'BEGIN ICANN DOMAINS' tests/fixtures/data/test_psl.dat &amp;&amp; grep -q 'test_stale_psl_emits_log_warning_once_when_filemtime_older_than_180_days' tests/Unit/Helper/HostIndexResolverTest.php &amp;&amp; grep -q 'PSL snapshot is' tests/Unit/Helper/HostIndexResolverTest.php</automated>
  </verify>
  <done>composer.json declares PSL dep in require:; Wave 0 RED tests authored including D-10 stale-PSL coverage; hermetic fixture in place.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: HostIndexResolver class + D-10 stale-PSL operator nudge + Plugin::register singleton binding</name>
  <files>
    plugins/logingrupa/metapixel/classes/helper/HostIndexResolver.php,
    plugins/logingrupa/metapixel/Plugin.php
  </files>
  <behavior>
    - `HostIndexResolver::resolve('example.co.uk')` returns `1` (apex on multi-TLD).
    - `HostIndexResolver::resolve('www.example.co.uk')` returns `2`.
    - `HostIndexResolver::resolve('shop.example.com.br')` returns `2`.
    - `HostIndexResolver::resolve('a.b.example.com')` returns `3`.
    - `HostIndexResolver::resolve('xn--bcher-kva.example')` returns a non-null int (IDN decoded via Domain::fromIDNA2008).
    - `HostIndexResolver::resolve('')` returns `null`.
    - `HostIndexResolver::resolve('127.0.0.1')` returns `null` (caught SyntaxError per Pitfall 5).
    - `HostIndexResolver::resolve('host.unknownsuffix')` returns `null` (suffix is not in PSL).
    - Two successive calls with the same host produce identical results (memoization; private `$arMemo`).
    - **D-10:** On first `resolve()` call per resolver instance, if `filemtime(sPslPath)` is older than 180 days (15_552_000 seconds), emit exactly one `Log::warning('PSL snapshot is N days old — run php artisan metapixel:refresh-psl')`. Subsequent `resolve()` calls in the same request emit **zero** additional warnings (request-scoped `$bStaleWarningEmitted` one-shot latch). Resolver continues to return correct subdomain-index values — the warning is operator feedback, NOT a failure mode (D-10 rationale: PSL is additive-only; an old snapshot still correctly resolves every pre-existing host).
    - `Plugin::register` binds `App::singleton(HostIndexResolver::class, fn() => new HostIndexResolver(base_path('plugins/logingrupa/metapixel/resources/data/public_suffix_list.dat')))`.
  </behavior>
  <read_first>
    plugins/logingrupa/metapixel/classes/helper/PluginGuard.php (in-plugin `final class` + Log facade + memo pattern — analog),
    plugins/logingrupa/metapixel/.planning/phases/04-settings-rework-multisite-trustedhosts-cookie-failedevents-t/04-RESEARCH.md (Pattern 4 lines 514-580 — full HostIndexResolver body; Pitfall 4 — memoization lines 1475-1484; Example 3 lines 1623-1641 — Plugin::register binding),
    plugins/logingrupa/metapixel/.planning/phases/04-settings-rework-multisite-trustedhosts-cookie-failedevents-t/04-PATTERNS.md (HostIndexResolver section lines 317-405),
    plugins/logingrupa/metapixel/.planning/phases/04-settings-rework-multisite-trustedhosts-cookie-failedevents-t/04-CONTEXT.md (D-10 stale-PSL decision text — "log warning + continue", 180-day threshold, request-scoped one-shot),
    plugins/logingrupa/metapixel/Plugin.php (current register/boot — append, do not rewrite)
  </read_first>
  <action>
    1) Create `plugins/logingrupa/metapixel/classes/helper/HostIndexResolver.php` (lowercase folder, PascalCase basename per Phase 2 STATE.md folder-convention lock):

    - Namespace `Logingrupa\Metapixel\Classes\Helper`.
    - `final class HostIndexResolver` (no extension — instance singleton).
    - Imports: `Illuminate\Support\Facades\Log`, `Pdp\Domain`, `Pdp\Rules`, `Pdp\UnableToResolveDomain`, `Throwable`.
    - Class constant: `private const STALE_THRESHOLD_SECONDS = 15552000; // 180 days × 86_400 — D-10 operator-feedback threshold`.
    - Properties:
      - `private ?Rules $obRules = null` (lazy-loaded PSL parser).
      - `/** @var array<string, ?int> request-scoped memo */ private array $arMemo = []`.
      - `private bool $bStaleWarningEmitted = false` (D-10 one-shot latch — set on first stale-PSL warn; checked on every subsequent `resolve()` to prevent log spam within a single request).
    - Constructor: `public function __construct(private readonly string $sPslPath) {}`.
    - Public method `resolve(string $sHost): ?int`:
      - Step A (D-10 stale-PSL operator nudge): BEFORE the existing lowercase/trim/memo logic, call private `$this->checkPslAge()`. The method runs only on first call per instance because the `$bStaleWarningEmitted` latch (and a `$bPslAgeChecked` companion latch if needed for the negative case) ensures `filemtime()` runs exactly once.
      - Step B onwards: existing RESEARCH Pattern 4 body verbatim — lowercase + trim, memo lookup, lazy `getRules()`, `Domain::fromIDNA2008` + `Rules::resolve` inside try/catch (catches `UnableToResolveDomain | Throwable`), suffix `isPublicSuffix()` + `secondLevelDomain()->value() !== null` guards, returns `count(subDomain()->labels()) + 1` OR memoized null.
    - Private method `checkPslAge(): void` (D-10 implementation):
      - Early-return if `$this->bStaleWarningEmitted === true` (latch already tripped — never log twice).
      - Use a SECOND latch `$bPslAgeChecked` initialised `false` in property declarations OR rely on `$bStaleWarningEmitted` covering both branches; pick one and pin: the cleaner choice is `if ($this->bStaleWarningEmitted) { return; }` PLUS check `is_file($this->sPslPath)` — if the file is missing entirely, return silently (the Pdp `Rules::fromPath` call later will fail and be caught by the existing Throwable handler).
      - Compute `$iAgeSeconds = time() - filemtime($this->sPslPath);` (cast to int; `filemtime` can return false on missing file — guard above prevents that path).
      - If `$iAgeSeconds > self::STALE_THRESHOLD_SECONDS`:
        - `$iAgeDays = (int) floor($iAgeSeconds / 86400);`
        - `Log::warning('PSL snapshot is ' . $iAgeDays . ' days old — run php artisan metapixel:refresh-psl');`
        - `$this->bStaleWarningEmitted = true;`
      - Set the `$bPslAgeChecked` latch (or rely on the warning latch — picker decision: a `$bStaleWarningEmitted` latch SUFFICES because if the PSL is fresh, the cost of repeatedly checking `filemtime()` is one syscall per `resolve()` call, which is negligible compared to PDP parsing; if stale, the latch prevents re-warns). For Tiger-Style fail-fast simplicity, use only `$bStaleWarningEmitted` and accept the harmless repeated `filemtime()` syscall on fresh snapshots — this is a tradeoff favoring code simplicity over micro-optimization (plugin CLAUDE.md "Simple > clever").
      - `// reason: D-10 operator feedback — stale snapshot is not a failure; tracking still works because PSL is additive-only.`
    - Private method `getRules(): Rules` lazy-initialises `$this->obRules ??= Rules::fromPath($this->sPslPath)`.
    - Class-level docblock: one-line summary "PSL-aware subdomain-index resolver. Returns 1 for apex, 2 for www, null for unresolvable/unknown TLD. Emits one Log::warning per request when bundled PSL is older than 180 days (D-10)." (Plugin CLAUDE.md "Laravel short docblocks".)
    - No `assert()`. Every `catch` documents reason: `// silent: Pdp throws on syntactically invalid hosts (IPs, empty) — middleware NO-OPs is the safe path`.
    - Hungarian locals: `$obRules`, `$obDomain`, `$obResolved`, `$obSuffix`, `$obException`, `$sHost`, `$iSubdomainLabels`, `$iAgeSeconds`, `$iAgeDays`, `$bWarningEmitted` (mirror of `$this->bStaleWarningEmitted` if a local copy is taken for clarity).

    2) Edit `plugins/logingrupa/metapixel/Plugin.php`:
    - Add import `use Logingrupa\Metapixel\Classes\Helper\HostIndexResolver;` near existing imports.
    - In `register()`, AFTER the existing `$this->app->singleton(AdapterRegistry::class)` and `$this->app->singleton(ThemeEventCollector::class)` lines, add the HostIndexResolver singleton binding:
      - `$this->app->singleton(HostIndexResolver::class, fn () => new HostIndexResolver(base_path('plugins/logingrupa/metapixel/resources/data/public_suffix_list.dat')));`
    - The RefreshPsl command registration is added in Task 3 (next task). This task only binds the resolver singleton.
    - DO NOT modify `boot()` — middleware registration is plan 04-03.

    Apply Pint after edits.
  </action>
  <acceptance_criteria>
    - File `plugins/logingrupa/metapixel/classes/helper/HostIndexResolver.php` exists.
    - File contains literal `final class HostIndexResolver`.
    - File contains literal `Rules::fromPath` and `Domain::fromIDNA2008`.
    - File contains literal `private array $arMemo` (request-scoped memo).
    - File contains literal `?int` return type on `resolve` (no nullable suppression).
    - File has zero `@phpstan-ignore` occurrences.
    - **D-10 grep gates (all three required):**
      - `grep -q 'filemtime' plugins/logingrupa/metapixel/classes/helper/HostIndexResolver.php` exits 0 (proves filemtime call exists).
      - `grep -q '15552000' plugins/logingrupa/metapixel/classes/helper/HostIndexResolver.php` exits 0 (proves the pinned 180-day threshold constant `15552000` seconds is present — chosen over `Carbon::now()->subDays(180)` per planner decision: constant is cheaper than Carbon instantiation per request and matches `STALE_THRESHOLD_SECONDS` convention).
      - `grep -q 'PSL snapshot is' plugins/logingrupa/metapixel/classes/helper/HostIndexResolver.php` exits 0 (proves literal log message substring matching the test assertion).
    - File contains literal `private bool $bStaleWarningEmitted` (D-10 one-shot latch).
    - File contains literal `Log::warning` (D-10 emit).
    - `Plugin.php` contains literal `HostIndexResolver::class` (singleton binding).
    - `composer test -- --filter=HostIndexResolverTest` exits 0 — all DataProvider rows pass (≥ 7 known + 5 unresolvable + 1 memoization + 2 D-10 stale/fresh cases).
    - `vendor/bin/pint --test --format agent plugins/logingrupa/metapixel/classes/helper/HostIndexResolver.php plugins/logingrupa/metapixel/Plugin.php` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd plugins/logingrupa/metapixel &amp;&amp; composer test -- --filter=HostIndexResolverTest</automated>
  </verify>
  <done>HostIndexResolver lands as service-container singleton with D-10 stale-PSL operator nudge; matrix test green including 2 new D-10 cases; Plugin.php register() extended.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: RefreshPsl artisan command + bundled PSL data file + Plugin::register console wiring</name>
  <files>
    plugins/logingrupa/metapixel/console/RefreshPsl.php,
    plugins/logingrupa/metapixel/resources/data/public_suffix_list.dat,
    plugins/logingrupa/metapixel/Plugin.php
  </files>
  <behavior>
    - `Artisan::call('metapixel:refresh-psl')` with mocked Guzzle returning valid sentinel body → exit code SUCCESS, bundle file overwritten via atomic rename, `storage/app/metapixel/psl/` directory cleaned.
    - Same call with body missing sentinel → exit code FAILURE; bundle file unchanged.
    - Connect exception during fetch → exit code FAILURE; bundle file unchanged; no `.tmp` lingering.
    - Empty body → FAILURE.
    - Command is registered in `Plugin::register()` via `$this->registerConsoleCommand('metapixel:refresh-psl', RefreshPsl::class)`.
  </behavior>
  <read_first>
    plugins/logingrupa/metapixel/console/PurgeEventLog.php (in-plugin console pattern — verbatim shape from 04-PATTERNS.md lines 413-435),
    plugins/logingrupa/metapixel/.planning/phases/04-settings-rework-multisite-trustedhosts-cookie-failedevents-t/04-RESEARCH.md (Pattern 5 lines 596-647 — full RefreshPsl body),
    plugins/logingrupa/metapixel/.planning/phases/04-settings-rework-multisite-trustedhosts-cookie-failedevents-t/04-PATTERNS.md (RefreshPsl section lines 409-446 + registration line 443)
  </read_first>
  <action>
    1) Ship the bundled PSL data file at `plugins/logingrupa/metapixel/resources/data/public_suffix_list.dat`:
    - Fetch upstream snapshot from `https://publicsuffix.org/list/public_suffix_list.dat` (Mozilla CDN, MPL 2.0 license per D-09). If network access is restricted in the execution environment, attempt the fetch and on failure leave a STUB file with the literal first line `// ===BEGIN ICANN DOMAINS===` + a single safe entry (`com`) + literal `// ===END ICANN DOMAINS===` and a comment header `// metapixel: PSL stub — run 'php artisan metapixel:refresh-psl' to populate the real file`. Mark this deviation in the SUMMARY.
    - File size: real PSL is ~280 KB. Stub is ~200 bytes.

    2) Create `plugins/logingrupa/metapixel/console/RefreshPsl.php` per Pattern 5 verbatim (lines 596-647):
    - Namespace `Logingrupa\Metapixel\Console`. PascalCase basename matches FQN.
    - `final class RefreshPsl extends Illuminate\Console\Command`.
    - Imports: `GuzzleHttp\Client`, `Illuminate\Console\Command`, `Illuminate\Support\Facades\File`, `Throwable`.
    - `protected $signature = 'metapixel:refresh-psl'`.
    - `protected $description = 'Refresh the bundled Public Suffix List from publicsuffix.org.'`.
    - Constants: `UPSTREAM_URL = 'https://publicsuffix.org/list/public_suffix_list.dat'` + `SENTINEL = '// ===BEGIN ICANN DOMAINS==='`.
    - `handle(): int` body verbatim from Pattern 5: build target path via `base_path('plugins/logingrupa/metapixel/resources/data/public_suffix_list.dat')`; tmp path = bundle + `.tmp`; instantiate Guzzle `new Client(['timeout' => 10])`; GET upstream wrapped in try/catch → on Throwable error + return `self::FAILURE`; check body non-empty + `str_contains($sBody, self::SENTINEL)` → on miss error + FAILURE; `file_put_contents($sTmpPath, $sBody)` check → false return FAILURE; `rename($sTmpPath, $sBundlePath)` check → on fail unlink tmp + FAILURE; finally `$sCacheDir = storage_path('app/metapixel/psl')` + `File::cleanDirectory` IF `is_dir($sCacheDir)`; emit `$this->info(...)` with byte count; return `self::SUCCESS`.
    - Constructor injection of `?Client $obClient = null` for test injectability (mirror MetaClient pattern). The instance Client created in handle() falls back to `$this->obClient ?? new Client(...)`.
    - Hungarian locals: `$sBundlePath`, `$sTmpPath`, `$obClient`, `$sBody`, `$obException`, `$sCacheDir`.
    - Every `catch` documents reason inline.

    3) Edit `Plugin.php::register()` (already extended in Task 2):
    - Add import `use Logingrupa\Metapixel\Console\RefreshPsl;`.
    - After existing `$this->registerConsoleCommand('metapixel:purge-event-log', PurgeEventLog::class);`, append:
      - `$this->registerConsoleCommand('metapixel:refresh-psl', RefreshPsl::class);`

    Apply Pint.
  </action>
  <acceptance_criteria>
    - File `resources/data/public_suffix_list.dat` exists; first 50 bytes contain literal `// ===BEGIN ICANN DOMAINS===` OR file size > 100 KB (real PSL snapshot).
    - File `console/RefreshPsl.php` exists; contains literal `protected $signature = 'metapixel:refresh-psl'`.
    - File contains literal `UPSTREAM_URL = 'https://publicsuffix.org/list/public_suffix_list.dat'` (pinned URL — SSRF mitigation).
    - File contains literal `SENTINEL = '// ===BEGIN ICANN DOMAINS==='`.
    - File contains literal `File::cleanDirectory` (cache wipe per D-11).
    - `Plugin.php` contains literal `registerConsoleCommand('metapixel:refresh-psl'` (registration).
    - `composer test -- --filter=RefreshPslTest` exits 0 — all 4 cases pass.
    - `vendor/bin/pint --test --format agent plugins/logingrupa/metapixel/console/RefreshPsl.php plugins/logingrupa/metapixel/Plugin.php` exits 0.
    - `php artisan list metapixel` (host root) lists `metapixel:refresh-psl` command (smoke).
  </acceptance_criteria>
  <verify>
    <automated>cd plugins/logingrupa/metapixel &amp;&amp; composer test -- --filter=RefreshPslTest</automated>
  </verify>
  <done>PSL ships bundled; refresh command registered + tested; all HOST-03 cases green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Settings::beforeSave trusted_hosts partition + fields.yaml 4-tab layout</name>
  <files>
    plugins/logingrupa/metapixel/models/Settings.php,
    plugins/logingrupa/metapixel/models/settings/fields.yaml
  </files>
  <behavior>
    - Saving Settings with `trusted_hosts = "example.co.uk\nwww.example.co.uk\n"` persists normalized lowercase newline-joined string `"example.co.uk\nwww.example.co.uk"`.
    - Saving with `trusted_hosts` containing an unknown-TLD line (e.g. `"bogus.fakeylock"`) throws `October\Rain\Database\ModelException` (D-14 strict halt) with a Flash::error message listing the rejected hosts.
    - Saving with `trusted_hosts` containing charset violation (e.g. `"example.com/path"`) is also rejected.
    - Empty `trusted_hosts` input → save succeeds, value persists as empty string.
    - The existing `theme_custom_event_names` partition pipeline is unaffected (regression-tested).
    - `fields.yaml` declares 4 tabs (`tab.pixel_and_capi` / `tab.hosts_and_cookies` / `tab.theme_tracking` / `tab.advanced`) with each existing field assigned a `tab:` per D-15; adds `trusted_hosts` textarea + `ensure_fbp_fbc_server_side` switch.
  </behavior>
  <read_first>
    plugins/logingrupa/metapixel/models/Settings.php (existing splitEventNameInput + partitionEventNames pattern — preserve verbatim),
    plugins/logingrupa/metapixel/models/settings/fields.yaml (current 6-field flat layout — re-tab in place),
    plugins/logingrupa/metapixel/.planning/phases/04-settings-rework-multisite-trustedhosts-cookie-failedevents-t/04-RESEARCH.md (Pattern 6 lines 663-757 — partitionHosts body + fields.yaml diff),
    plugins/logingrupa/metapixel/.planning/phases/04-settings-rework-multisite-trustedhosts-cookie-failedevents-t/04-PATTERNS.md (Settings.php section lines 36-117 — change list)
  </read_first>
  <action>
    1) Edit `models/Settings.php` (already has lookupForSite body from plan 04-01):
    - Add import `use Illuminate\Support\Facades\App;`.
    - Add import `use Logingrupa\Metapixel\Classes\Helper\HostIndexResolver;`.
    - Add import `use October\Rain\Database\ModelException;`.
    - Extend `beforeSave()`: AFTER the existing `theme_custom_event_names` partition block, add a parallel block for `trusted_hosts`:
      - `$arLines = $this->splitHostInput($this->getAttribute('trusted_hosts'));`
      - Early-return if null (same shape as splitEventNameInput).
      - `[$arClean, $arRejected] = $this->partitionHosts($arLines);`
      - If `$arRejected !== []`: `Flash::error('metapixel: rejected trusted_hosts (unknown TLD or invalid charset): ' . implode(', ', $arRejected));` then `throw new ModelException($this, 'metapixel: invalid trusted_hosts');` (per RESEARCH Pattern 6 line 723-727 — D-14 strict halt).
      - Else: `$this->setAttribute('trusted_hosts', implode("\n", $arClean));`.
    - Add private method `splitHostInput(mixed $mValue): ?array` — STRUCTURALLY IDENTICAL to existing `splitEventNameInput` (mirror the docblock + body verbatim; same `preg_split('/\R/', $mValue)` logic). Return `list<mixed>|null`.
    - Add private method `partitionHosts(array $arLines): array` returning `array{0: list<string>, 1: list<string>}`:
      - Resolve `$obResolver = App::make(HostIndexResolver::class);` once outside the loop.
      - Loop: trim + lowercase each line; skip empties; check `preg_match('/^[a-z0-9.-]+$/', $sHost) === 1` — if fail → `$arRejected[] = $sHost; continue;`; check `$obResolver->resolve($sHost) === null` → reject; else push to `$arClean`.
      - Return `[$arClean, $arRejected]`.
    - Tiger-Style fail-fast: `ModelException` throw IS the boundary; no second `catch` rethrow needed.

    2) Edit `models/settings/fields.yaml` per RESEARCH Pattern 6 lines 669-705:
    - Wrap the existing 6 fields in a `tabs:` block: top-level becomes `tabs:\n  fields:\n    <existing fields with tab: assignments>`.
    - Tab assignments per D-15:
      - `pixel_id`, `capi_access_token`, `test_event_code`, `paid_status_code`, `default_currency_code` → `tab: logingrupa.metapixel::lang.tab.pixel_and_capi`.
      - `theme_custom_event_names` → `tab: logingrupa.metapixel::lang.tab.theme_tracking`.
      - (Advanced tab gets no field this plan — reserved for future ops fields per CONTEXT D-15.)
    - Rename label keys from `settings.fields.*` to the new shape introduced in plan 04-05 lang files. For THIS plan, leave the existing `settings.fields.*` keys in place; plan 04-05 LANG-01 ships the actual `field.*` shape and updates this fields.yaml + lang files atomically. Annotate via YAML comment: `# Label keys remain settings.fields.* until plan 04-05 LANG-01 ships the unified field.* shape.`
    - ADD new fields:
      - `trusted_hosts`: `tab: logingrupa.metapixel::lang.tab.hosts_and_cookies`, `label: logingrupa.metapixel::lang.settings.fields.trusted_hosts_label`, `commentAbove: logingrupa.metapixel::lang.settings.fields.trusted_hosts_comment`, `type: textarea`, `size: small`, `span: full`.
      - `ensure_fbp_fbc_server_side`: `tab: logingrupa.metapixel::lang.tab.hosts_and_cookies`, `label: logingrupa.metapixel::lang.settings.fields.ensure_fbp_fbc_label`, `commentAbove: logingrupa.metapixel::lang.settings.fields.ensure_fbp_fbc_comment`, `type: switch`, `default: true`.
    - The lang keys `settings.fields.trusted_hosts_*`, `settings.fields.ensure_fbp_fbc_*`, `tab.*` MUST exist in `lang/en/lang.php` + `lang/lv/lang.php` as English/Latvian placeholders BEFORE plan 04-05 ships final i18n. Add minimal stubs (English strings only) in `lang/en/lang.php` for the 4 new keys (tab labels + 2 field labels + 2 field comments + 2 tab labels) — plan 04-05 expands the surface to the full ~60-key shape.

    Apply Pint.
  </action>
  <acceptance_criteria>
    - `models/Settings.php` contains literal `private function partitionHosts(`.
    - `models/Settings.php` contains literal `private function splitHostInput(`.
    - `models/Settings.php` contains literal `App::make(HostIndexResolver::class)`.
    - `models/Settings.php` contains literal `throw new ModelException` (strict halt per D-14).
    - `models/settings/fields.yaml` contains literal `tab: logingrupa.metapixel::lang.tab.hosts_and_cookies` (HOST-01 tab assignment).
    - `models/settings/fields.yaml` contains a field block keyed `trusted_hosts:` with `type: textarea`.
    - `models/settings/fields.yaml` contains a field block keyed `ensure_fbp_fbc_server_side:` with `type: switch` AND `default: true`.
    - `lang/en/lang.php` includes string for `tab.hosts_and_cookies` AND `settings.fields.trusted_hosts_label`.
    - `composer test -- --filter=TrustedHostsValidationTest` exits 0 — all 5 cases pass.
    - `composer test -- --filter=SettingsLookupForSiteTest` STILL exits 0 (regression — plan 04-01 not broken).
    - `composer qa` exits 0 — full chain green; coverage ≥ 90%.
  </acceptance_criteria>
  <verify>
    <automated>cd plugins/logingrupa/metapixel &amp;&amp; composer test -- --filter='TrustedHostsValidationTest|SettingsLookupForSiteTest|HostIndexResolverTest|RefreshPslTest'</automated>
  </verify>
  <done>trusted_hosts strict validation live; 4-tab fields.yaml layout in place; minimal lang stubs cover new keys; full composer qa green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator backend → Settings.trusted_hosts | Operator types one host per line; beforeSave parses against PSL + charset regex. Untrusted hosts get rejected at the save boundary, not at the cookie-write boundary. |
| `artisan metapixel:refresh-psl` → `https://publicsuffix.org` | One-way HTTPS GET to a constant URL (no operator override). Atomic rename prevents partial overwrite. |
| `HostIndexResolver::resolve` ← any caller | Pure function; deterministic per request lifecycle via memo; never throws. Null result = "untrusted/unknown" — caller's responsibility to NO-OP. |
| `HostIndexResolver::checkPslAge` → `Log::warning` (D-10) | Side-effect — log line emitted once per request when PSL is older than 180 days. Operator-facing nudge; not a request failure. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-HOST-01 (CR-02) | Tampering | `HostIndexResolver::resolve` naive parser exploit | mitigate | Library `jeremykendall/php-domain-parser ^6.4` (PSL-aware) replaces naive `explode('.')`; HOST-05 multi-TLD matrix test enforces. |
| T-04-HOST-02 (P-18) | Denial of Service | PSL cache write to read-only Forge release dir | mitigate | Cache path locked to `storage_path('app/metapixel/psl/')` (Forge-writable shared path); `File::cleanDirectory` wipes via shared symlink. Pattern 5 / D-09 lock. |
| T-04-HOST-03 | Tampering / Spoofing | Untrusted host bypass via Host header spoof | mitigate | `Settings::partitionHosts` rejects unknown-TLD lines at save time; `HostIndexResolver::resolve` returns null on unknown suffix; downstream middleware (plan 04-03) treats null as NO-OP. |
| T-04-HOST-04 (D-10) | Repudiation | Operator runs stale PSL for months, new ccTLD additions silently absent — tracking gradually drifts | mitigate | `HostIndexResolver::checkPslAge` emits `Log::warning` once per request when filemtime > 180 days; operator sees the line in Laravel logs and knows to run `metapixel:refresh-psl`. No silent failure mode; tracking continues to work for all pre-existing hosts because PSL is additive-only. |
| T-04-PSL-01 | Tampering / SSRF | `metapixel:refresh-psl` fetches arbitrary URL | mitigate | URL pinned to constant `UPSTREAM_URL = 'https://publicsuffix.org/list/public_suffix_list.dat'`; not operator-configurable. Sentinel + atomic rename prevent partial overwrite. |
| T-04-PSL-02 | Repudiation | Operator save persists invalid trusted_hosts → tracking silently breaks at request time | mitigate | D-14 strict halt via `ModelException` throw forces operator to clean input at boundary they understand. Flash::error lists rejected hosts. |
| T-04-SC (carry) | Tampering | Composer install of `jeremykendall/php-domain-parser` package legitimacy | mitigate | `checkpoint:human-verify` (Task 0) blocks composer require until operator confirms packagist metadata. Package marked `[ASSUMED]` per RESEARCH §"Package Legitimacy Audit". |
</threat_model>

<verification>
- After Task 1 — `composer.json` has PSL dep; Wave 0 tests author phase complete; D-10 stale-PSL cases authored; `grep -q '"jeremykendall/php-domain-parser"' composer.json` exits 0; `grep -q 'test_stale_psl_emits_log_warning_once_when_filemtime_older_than_180_days' tests/Unit/Helper/HostIndexResolverTest.php` exits 0.
- After Task 2 — `composer test -- --filter=HostIndexResolverTest` exits 0; multi-TLD matrix green; memoization smoke passes; D-10 stale-PSL test green (Log::warning emitted exactly once); fresh-PSL companion green (Log::warning NOT emitted); grep gates for `filemtime`, `15552000`, `PSL snapshot is`, `private bool $bStaleWarningEmitted` all green.
- After Task 3 — `composer test -- --filter=RefreshPslTest` exits 0; PSL artisan command registered + tested; bundled file shipped.
- After Task 4 — `composer test -- --filter='TrustedHostsValidationTest|SettingsLookupForSiteTest|HostIndexResolverTest|RefreshPslTest'` exits 0; `composer qa` exits 0.
</verification>

<success_criteria>
- HOST-01: `trusted_hosts` textarea field exists in `fields.yaml` under Hosts & Cookies tab; `Settings::beforeSave` partitions valid/invalid lines via PSL, halts save on invalid input.
- HOST-02: `HostIndexResolver` final class wraps `jeremykendall/php-domain-parser ^6.4`; bound as App singleton in `Plugin::register`; `resolve(string): ?int` contract verified.
- HOST-03: PSL data file at `resources/data/public_suffix_list.dat`; `metapixel:refresh-psl` artisan command registered; cache cleared at `storage/app/metapixel/psl/` on refresh; sentinel + atomic rename safety.
- HOST-04: HostIndexResolver returns `null` for unresolvable host — middleware-facing contract (consumed by plan 04-03).
- HOST-05: 7 known-host data-provider cases pass: `example.test/com` apex, `www.*`, `example.co.uk` apex, `www.example.co.uk`, `shop.example.com.br`, `a.b.example.com`, IDN `xn--bcher-kva.example`.
- HOST-06: 5 unresolvable-host data-provider cases pass (empty, IP, localhost, underscore, unknown TLD) — all return null.
- **D-10:** Stale-PSL operator nudge — `HostIndexResolver` emits exactly one `Log::warning('PSL snapshot is N days old ...')` per request when bundled PSL is older than 180 days; resolver continues to return correct subdomain-index values; fresh PSL (< 180 days) emits zero warnings.
- `composer qa` exits 0; PSL parser ^6.4 confirmed via composer show; package legitimacy human-verified.
</success_criteria>

<output>
Create `.planning/phases/04-settings-rework-multisite-trustedhosts-cookie-failedevents-t/04-02-SUMMARY.md` when done — capture: PSL data file source (real upstream fetch vs stub), `composer require` host-root run command actually used, multi-TLD matrix cases passed, memoization smoke result, D-10 stale/fresh PSL test outcomes + Log::warning latch correctness, package legitimacy checkpoint approval timestamp, Hungarian/Tiger-Style/Pint compliance.
</output>
</content>
</invoke>