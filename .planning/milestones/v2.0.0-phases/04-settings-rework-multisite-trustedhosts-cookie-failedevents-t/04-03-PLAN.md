---
phase: 04-settings-rework-multisite-trustedhosts-cookie-failedevents-t
plan: 03
type: execute
wave: 2
depends_on: ["04-02"]
files_modified:
  - plugins/logingrupa/metapixel/middleware/EnsureFbpFbcCookies.php
  - plugins/logingrupa/metapixel/Plugin.php
  - plugins/logingrupa/metapixel/tests/Feature/Middleware/EnsureFbpFbcCookiesTest.php
autonomous: true
requirements: [COOK-01, COOK-02, COOK-03]
must_haves:
  truths:
    - "COOK-01/D-16: middleware honors Settings::get('ensure_fbp_fbc_server_side', true) kill switch — false → middleware short-circuits and writes no cookies"
    - "COOK-02/CR-03: fbclid input validated against [A-Za-z0-9_-] charset AND ≤255 chars before _fbc cookie write; invalid → skip _fbc (no throw, no exception)"
    - "COOK-03: README documents Cache-Control: private as operator responsibility; class-level PHPDoc references README section (doc-only — no automated test)"
    - "D-20: fresh derivation — NOT a v1.x port. v1.x locked DECISIONS (90-day TTL, fb.{N}.{ts}.{rand} format, CSPRNG random_bytes(8), 9-key cookie attrs) reused verbatim against new HostIndexResolver injection."
    - "Untrusted host (Settings.trusted_hosts not containing Request::getHost()) → middleware NO-OPs (no cookies set, no exception) — closes P-15 marketplace launch blocker at HTTP layer."
    - "Boundary fail-safe (Pitfall 8): Settings::get inside try/catch returning false on Throwable so middleware does NOT 500 the initial migration HTTP request when system_settings table is missing."
  artifacts:
    - path: "plugins/logingrupa/metapixel/middleware/EnsureFbpFbcCookies.php"
      provides: "Server-side _fbp + _fbc cookie writer with kill switch + CR-03 fbclid validation + PSL-aware subdomain index"
      contains: "class EnsureFbpFbcCookies"
    - path: "plugins/logingrupa/metapixel/Plugin.php"
      provides: "Middleware registration via Plugin::boot pushMiddleware"
      contains: "pushMiddleware"
  key_links:
    - from: "plugins/logingrupa/metapixel/middleware/EnsureFbpFbcCookies.php"
      to: "App::make(HostIndexResolver::class)"
      via: "Constructor injection (readonly promoted property)"
      pattern: "HostIndexResolver"
    - from: "plugins/logingrupa/metapixel/Plugin.php::boot"
      to: "Illuminate\\Contracts\\Http\\Kernel::class"
      via: "pushMiddleware(EnsureFbpFbcCookies::class)"
      pattern: "pushMiddleware"
---

<objective>
Implement COOK-01..03 — Fresh derivation of `EnsureFbpFbcCookies` HTTP middleware per D-20. Honors operator kill switch (COOK-01), validates fbclid per CR-03 charset + 255-char length cap (COOK-02), documents `Cache-Control: private` operator responsibility in class-level docblock (COOK-03). Consumes the `HostIndexResolver` service from plan 04-02; treats untrusted-host result (null) as NO-OP — closes P-15 marketplace launch blocker at the HTTP cookie-write layer.

Purpose: Server-side `_fbp` + `_fbc` cookie writer that supplies Meta CAPI deduplication anchors. Without this, browser pixel + server CAPI cannot match on `(fbp, fbc, event_id)` triple → Meta dedup fails → double-counted events.

Output: New `middleware/` directory with `EnsureFbpFbcCookies.php`; `Plugin.php::boot()` extended to register middleware via `pushMiddleware`; 1 feature test file with multi-case matrix.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@plugins/logingrupa/metapixel/.planning/PROJECT.md
@plugins/logingrupa/metapixel/.planning/ROADMAP.md
@plugins/logingrupa/metapixel/.planning/STATE.md
@plugins/logingrupa/metapixel/.planning/phases/04-settings-rework-multisite-trustedhosts-cookie-failedevents-t/04-CONTEXT.md
@plugins/logingrupa/metapixel/.planning/phases/04-settings-rework-multisite-trustedhosts-cookie-failedevents-t/04-RESEARCH.md
@plugins/logingrupa/metapixel/.planning/phases/04-settings-rework-multisite-trustedhosts-cookie-failedevents-t/04-PATTERNS.md
@plugins/logingrupa/metapixel/CLAUDE.md
@/home/forge/nailscosmetics.lv/CLAUDE.md
@plugins/logingrupa/metapixel/Plugin.php
@plugins/logingrupa/metapixel/classes/helper/HostIndexResolver.php

<interfaces>
<!-- Public contract — middleware class consumes HostIndexResolver (plan 04-02) and Settings (plan 04-01 + 04-02). -->

Logingrupa\Metapixel\Middleware\EnsureFbpFbcCookies (NEW):
```
public function __construct(private readonly HostIndexResolver $obResolver)
public function handle(Illuminate\Http\Request $obRequest, Closure $fnNext): Symfony\Component\HttpFoundation\Response
```
Class-level constants (D-20 — v1.x decision lock):
```
private const COOKIE_TTL_SECONDS = 60 * 60 * 24 * 90;   // 90 days
private const COOKIE_FBP = '_fbp';
private const COOKIE_FBC = '_fbc';
private const FBCLID_MAX_LENGTH = 255;
private const FBCLID_ALLOWED_PATTERN = '/^[A-Za-z0-9_-]+$/';
```

Cookie format locks (v1.x carry-forward — RESEARCH Pattern 7):
- `_fbp` value = `sprintf('fb.%d.%d.%s', $iIndex, $iMs, bin2hex(random_bytes(8)))`
- `_fbc` value = `sprintf('fb.%d.%d.%s', $iIndex, $iMs, $sFbclid)`
- creation_time_ms = `(int) (microtime(true) * 1000)`
- Symfony Cookie attrs: path `/`, domain `null`, secure mirrors `Request::secure()`, httpOnly false, raw false, sameSite `lax`

Illuminate\Contracts\Http\Kernel (Laravel 12 / October 4):
```
$this->app[Kernel::class]->pushMiddleware(EnsureFbpFbcCookies::class): void
```
Registered in Plugin::boot AFTER existing adapter wiring (RESEARCH Example 4 lines 1643-1656).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 0: Wave 0 — author failing middleware feature test matrix</name>
  <files>
    plugins/logingrupa/metapixel/tests/Feature/Middleware/EnsureFbpFbcCookiesTest.php
  </files>
  <read_first>
    plugins/logingrupa/metapixel/tests/Feature/Console/PurgeEventLogTest.php (setUp/tearDown shape — closest feature test analog in-plugin),
    plugins/logingrupa/metapixel/tests/MetapixelTestCase.php (Pest base + system_settings recreation),
    plugins/logingrupa/metapixel/.planning/phases/04-settings-rework-multisite-trustedhosts-cookie-failedevents-t/04-RESEARCH.md (Pattern 7 lines 759-914 — full middleware body; Pitfall 3 + 8 — boundary fail-safe; Validation Architecture lines 1801-1808),
    plugins/logingrupa/metapixel/.planning/phases/04-settings-rework-multisite-trustedhosts-cookie-failedevents-t/04-PATTERNS.md (Middleware section lines 450-531; Cookie locks table lines 1032-1046; Boundary fail-safe pattern lines 1026-1031)
  </read_first>
  <action>
    Author `plugins/logingrupa/metapixel/tests/Feature/Middleware/EnsureFbpFbcCookiesTest.php` (lowercase folder, PascalCase basename; `final class EnsureFbpFbcCookiesTest extends MetapixelTestCase`).

    Test setUp:
    - `parent::setUp()` + `Settings::clearInternalCache()` (Pitfall 1 lock).
    - Bind a real `HostIndexResolver` instance pointed at the hermetic `tests/fixtures/data/test_psl.dat` (from plan 04-02 Task 1) via `$this->app->instance(HostIndexResolver::class, new HostIndexResolver(__DIR__ . '/../../fixtures/data/test_psl.dat'))` so the test does NOT load the real ~280 KB PSL.
    - Seed Settings rows: `Settings::set(['trusted_hosts' => "example.com\nshop.example.co.uk", 'ensure_fbp_fbc_server_side' => true])`.

    Build a helper `dispatchRequest(string $sHost, ?string $sFbclidQuery = null, array $arExistingCookies = []): Symfony\Component\HttpFoundation\Response`:
    - Builds an `Illuminate\Http\Request` with `HTTP_HOST` = $sHost, optional `fbclid` query, optional incoming cookies, secure=true.
    - Resolves the middleware via `$this->app->make(EnsureFbpFbcCookies::class)`.
    - Invokes `->handle($obRequest, fn() => new Response('ok'))` and returns the result.

    Test cases (≥ 14 — explicit list):

    Kill switch (COOK-01):
    - `test_kill_switch_off_skips_cookie_write`: set `ensure_fbp_fbc_server_side = false`; dispatch on trusted host → response has zero `Set-Cookie` headers for `_fbp` / `_fbc`.
    - `test_kill_switch_default_true_writes_cookies_when_trusted`: default `true`; dispatch on `example.com` → `_fbp` cookie set in response.
    - `test_kill_switch_on_with_string_value_1_writes_cookies`: set `ensure_fbp_fbc_server_side = '1'` (legacy string-coerced) → cookies written (RESEARCH Pattern 7 shouldSkip logic).

    fbclid validation (COOK-02):
    - `test_valid_fbclid_writes_fbc_cookie`: trusted host + `?fbclid=IwAR1abc_XYZ-123` → response carries `_fbc` cookie matching `/^fb\.\d+\.\d+\.IwAR1abc_XYZ-123$/`.
    - `test_invalid_fbclid_charset_skips_fbc`: trusted host + `?fbclid=ab<script>` → response has NO `_fbc` cookie; no exception.
    - `test_oversize_fbclid_skips_fbc`: 256-char fbclid → no `_fbc` cookie; no exception.
    - `test_exactly_255_char_fbclid_is_accepted`: 255-char alnum/_/- fbclid → `_fbc` written.
    - `test_existing_fbc_cookie_not_overwritten`: request carries pre-existing `_fbc` cookie + valid `fbclid` query → response does NOT set a new `_fbc` (idempotent).
    - `test_no_fbclid_query_skips_fbc_but_still_writes_fbp`: trusted host, no fbclid query → `_fbp` set, `_fbc` absent.

    Host trust:
    - `test_untrusted_host_writes_no_cookies`: host `attacker.example.org` not in `trusted_hosts` → response has zero `_fbp` / `_fbc` headers; no exception (closes P-15 / CR-02).
    - `test_psl_unresolvable_host_writes_no_cookies`: host `wat.fakeylock` (not in PSL) — even if added to `trusted_hosts` → `HostIndexResolver` returns null → no cookies (defence in depth).
    - `test_existing_fbp_cookie_not_overwritten`: request carries `_fbp` → no new `_fbp` Set-Cookie.

    Boundary fail-safe (Pitfall 8):
    - `test_settings_get_throwing_does_not_500`: drop `system_settings` table (or mock `Settings::get` to throw `\Throwable`); dispatch → response is the inner pipeline's response (no 500); a `Log::warning` is emitted (assert via Log facade swap).

    Cookie format (D-20 v1.x lock):
    - `test_fbp_cookie_format_matches_fb_index_ms_random`: trusted host `shop.example.co.uk` (subdomain-index 2 per PSL) → `_fbp` value matches `/^fb\.2\.\d{13}\.[0-9a-f]{16}$/` (subdomain-index 2 + 13-digit ms + 16-hex random bytes).
    - `test_fbc_cookie_format_matches_fb_index_ms_fbclid`: trusted apex `example.com` (subdomain-index 1) + `?fbclid=FBCLID_TOKEN_123` → `_fbc` value matches `/^fb\.1\.\d{13}\.FBCLID_TOKEN_123$/`.

    Hungarian notation throughout. Class docblock: "Wave 0 RED — fails until plan 04-03 middleware production code ships."

    Cookie inspection helper: read response cookies via `$obResponse->headers->getCookies()` (Symfony) — returns array of `Cookie` instances; filter by name.
  </action>
  <acceptance_criteria>
    - File `tests/Feature/Middleware/EnsureFbpFbcCookiesTest.php` exists.
    - `grep -c 'function test_' tests/Feature/Middleware/EnsureFbpFbcCookiesTest.php` ≥ 14.
    - File contains literal `EnsureFbpFbcCookies::class` (will resolve once production code ships).
    - File contains literal `_fbp` AND literal `_fbc` cookie-name strings.
    - File contains literal `IwAR1abc_XYZ-123` valid fbclid + literal `ab<script>` invalid fbclid (charset case).
    - File contains literal regex `fb\.1\.` OR `fb\.2\.` (cookie format assertion).
    - File extends `MetapixelTestCase`.
    - Tests currently fail (red) — `composer test -- --filter=EnsureFbpFbcCookiesTest` exits NON-ZERO.
  </acceptance_criteria>
  <verify>
    <automated>test -f plugins/logingrupa/metapixel/tests/Feature/Middleware/EnsureFbpFbcCookiesTest.php &amp;&amp; cd plugins/logingrupa/metapixel &amp;&amp; [ "$(grep -c 'function test_' tests/Feature/Middleware/EnsureFbpFbcCookiesTest.php)" -ge 14 ]</automated>
  </verify>
  <done>14+ test cases author phase complete; tests red; Pitfall 8 boundary fail-safe case included.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1: EnsureFbpFbcCookies middleware — fresh derivation per D-20</name>
  <files>
    plugins/logingrupa/metapixel/middleware/EnsureFbpFbcCookies.php
  </files>
  <behavior>
    - Kill switch OFF (`ensure_fbp_fbc_server_side = false`) → middleware returns the inner pipeline response with zero cookie writes.
    - Backend path (Request::is('backend*')) → middleware short-circuits.
    - App::bound('metapixel.disabled') AND App::make('metapixel.disabled') truthy → short-circuits.
    - Settings::get throws Throwable → log warning + return false in shouldSkip → middleware proceeds with default behavior (boundary fail-safe per Pitfall 8).
    - Untrusted host (not in trusted_hosts list) → return response untouched (CR-02).
    - HostIndexResolver returns null → return response untouched.
    - Trusted host, missing _fbp cookie → set `_fbp` cookie with format `fb.{index}.{ms}.{16-hex-random}`, TTL 90 days, path /, secure mirrors request, sameSite lax.
    - Trusted host, missing _fbc cookie, valid fbclid query string ([A-Za-z0-9_-]{1,255}) → set `_fbc` cookie with format `fb.{index}.{ms}.{fbclid}`.
    - fbclid > 255 chars OR fails charset regex → skip _fbc; no throw.
    - Pre-existing _fbp or _fbc cookie → not overwritten.
  </behavior>
  <read_first>
    plugins/logingrupa/metapixel/.planning/phases/04-settings-rework-multisite-trustedhosts-cookie-failedevents-t/04-RESEARCH.md (Pattern 7 lines 759-914 — full middleware body verbatim; Pitfalls 3 + 8 lines 1467-1545 — boundary fail-safe + backend path short-circuit),
    plugins/logingrupa/metapixel/.planning/phases/04-settings-rework-multisite-trustedhosts-cookie-failedevents-t/04-PATTERNS.md (Middleware section lines 450-531; Cookie creation locks lines 1032-1046),
    plugins/logingrupa/metapixel/classes/helper/HostIndexResolver.php (constructor signature and resolve(string): ?int contract from plan 04-02),
    plugins/logingrupa/metapixel/CLAUDE.md (Hungarian, Tiger-Style fail-fast, no `assert()`, no `// CR-XX` markers in code),
    legacy/v1.1.1 branch reference for DECISIONS only (DO NOT port code per D-20)
  </read_first>
  <action>
    Create `plugins/logingrupa/metapixel/middleware/EnsureFbpFbcCookies.php`.

    Per D-20 + RESEARCH Pattern 7 verbatim:

    1) Namespace `Logingrupa\Metapixel\Middleware`. (Note: NOT lowercase under classes/ — `middleware/` is a top-level plugin folder per RESEARCH §"Recommended Project Structure" lines 352-355; Phase 2 STATE.md lowercase-folder lock applies — `middleware/` is already lowercase.)

    2) Class `EnsureFbpFbcCookies` — NOT `final` (October may need extensibility; but plugin CLAUDE.md prefers `final` unless extension is documented). Make it `class EnsureFbpFbcCookies` (non-final) per the RESEARCH Pattern 7 sketch, to allow operator subclass for custom cookie attributes — pattern proven by Phase 2 MetaClient `final` drop (STATE.md "MetaClient `final` keyword dropped"). Class-level docblock includes COOK-03 documentation reference:
       - `Operator MUST serve routes hitting this middleware with Cache-Control: private to prevent shared-cache cookie leakage. See README "Cookie middleware" section.`

    3) Imports: `Closure`, `Illuminate\Http\Request`, `Illuminate\Support\Facades\App`, `Illuminate\Support\Facades\Log`, `Logingrupa\Metapixel\Classes\Helper\HostIndexResolver`, `Logingrupa\Metapixel\Models\Settings`, `Symfony\Component\HttpFoundation\Cookie`, `Symfony\Component\HttpFoundation\Response`, `Throwable`.

    4) Class constants — verbatim per RESEARCH Pattern 7 lines 799-804:
       - `private const COOKIE_TTL_SECONDS = 60 * 60 * 24 * 90;`
       - `private const COOKIE_FBP = '_fbp';`
       - `private const COOKIE_FBC = '_fbc';`
       - `private const FBCLID_MAX_LENGTH = 255;`
       - `private const FBCLID_ALLOWED_PATTERN = '/^[A-Za-z0-9_-]+$/';`

    5) Constructor with readonly promoted HostIndexResolver:
       - `public function __construct(private readonly HostIndexResolver $obResolver) {}`

    6) `public function handle(Request $obRequest, Closure $fnNext): Response` — body per Pattern 7 lines 807-834 verbatim. Order: call `$fnNext` first (inner pipeline runs), check shouldSkip, derive `$sHost`/`$arTrustedHosts`/`$iIndex`, compute `$iCreationMs` + `$bSecure` + `$iExpire`, call `maybeSetFbp` + `maybeSetFbc`, return response.

    7) `private function shouldSkip(Request $obRequest): bool` body per Pattern 7 lines 836-858:
       - Backend path: `$mBackendUri = config('cms.backendUri', 'backend'); $sBackendUri = is_scalar($mBackendUri) ? (string) $mBackendUri : ''; if ($sBackendUri !== '' && $obRequest->is(ltrim($sBackendUri, '/') . '*')) { return true; }`.
       - PluginGuard defence: `if (App::bound('metapixel.disabled') && App::make('metapixel.disabled')) { return true; }`.
       - Kill switch lookup inside try/catch (Pitfall 8): `try { $mToggle = Settings::get('ensure_fbp_fbc_server_side', true); return ! ($mToggle === true || $mToggle === 1 || $mToggle === '1'); } catch (Throwable $obException) { Log::warning('metapixel: kill-switch lookup threw — middleware defaults to enabled', ['exception' => get_class($obException)]); return false; }`.

    8) `private function readTrustedHosts(): array` — `/** @return list<string> */` returning the parsed Settings.trusted_hosts as a lowercase-trimmed non-empty list per Pattern 7 lines 860-879. Read via `Settings::get('trusted_hosts', '')` (this is OK — the D-02 phpstan ban is specifically on `pixel_id` + `capi_access_token`, not other Settings keys; verify against the plan 04-01 phpstan rule).

    9) `private function maybeSetFbp(Request, Response, int $iIndex, int $iMs, int $iExpire, bool $bSecure): void` per Pattern 7 lines 882-892. Skip if `_fbp` already on request. Random segment = `bin2hex(random_bytes(8))` (CSPRNG — 16 hex chars). Cookie via `Cookie::create('_fbp', $sFbp, $iExpire, '/', null, $bSecure, false, false, 'lax')` — 9 positional args: name, value, expires, path, domain, secure, httpOnly, raw, sameSite.

    10) `private function maybeSetFbc(Request, Response, int $iIndex, int $iMs, int $iExpire, bool $bSecure): void` per Pattern 7 lines 894-913:
        - Read fbclid via `$mFbclid = $obRequest->query('fbclid', ''); $sFbclid = is_scalar($mFbclid) ? (string) $mFbclid : '';` (mixed-cast lock).
        - Skip if empty OR strlen > FBCLID_MAX_LENGTH.
        - Skip if `preg_match(self::FBCLID_ALLOWED_PATTERN, $sFbclid) !== 1`.
        - Skip if request already has `_fbc`.
        - Cookie create + setCookie identical shape to `_fbp`.

    11) Tiger-Style: every catch documents reason; no `assert()`; no `// CR-02:` / `// CR-03:` markers (plugin CLAUDE.md "No comment pollution" rule). The CR locks live in the PR description + this PLAN.md, not in source.

    Apply Pint.
  </action>
  <acceptance_criteria>
    - File `plugins/logingrupa/metapixel/middleware/EnsureFbpFbcCookies.php` exists.
    - File contains literal `class EnsureFbpFbcCookies`.
    - File contains literal `private readonly HostIndexResolver $obResolver` (constructor injection).
    - File contains literal `60 * 60 * 24 * 90` (90-day TTL — D-20 v1.x lock).
    - File contains literal `bin2hex(random_bytes(8))` (CSPRNG 16-hex).
    - File contains literal `/^[A-Za-z0-9_-]+$/` (CR-03 fbclid charset).
    - File contains literal `FBCLID_MAX_LENGTH = 255` (CR-03 length cap).
    - File contains literal `Cache-Control: private` in docblock (COOK-03 reference).
    - File contains literal `try {` AND literal `catch (Throwable $obException)` in `shouldSkip` (Pitfall 8 boundary fail-safe).
    - `grep -c '// CR-0' plugins/logingrupa/metapixel/middleware/EnsureFbpFbcCookies.php` returns 0 (no CR-XX markers per plugin CLAUDE.md).
    - `grep -c 'assert(' plugins/logingrupa/metapixel/middleware/EnsureFbpFbcCookies.php` returns 0.
    - `grep -c '@phpstan-ignore' plugins/logingrupa/metapixel/middleware/EnsureFbpFbcCookies.php` returns 0.
    - `vendor/bin/pint --test --format agent plugins/logingrupa/metapixel/middleware/EnsureFbpFbcCookies.php` exits 0.
    - `composer test -- --filter=EnsureFbpFbcCookiesTest` exits 0 — all 14+ cases pass.
  </acceptance_criteria>
  <verify>
    <automated>cd plugins/logingrupa/metapixel &amp;&amp; composer test -- --filter=EnsureFbpFbcCookiesTest</automated>
  </verify>
  <done>Middleware production code lands; CR-02 + CR-03 + COOK-01..03 verified; 14+ test cases green; zero workflow markers in source.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Plugin::boot pushMiddleware registration + composer qa full chain</name>
  <files>
    plugins/logingrupa/metapixel/Plugin.php
  </files>
  <behavior>
    - `Plugin::boot()` registers `EnsureFbpFbcCookies` via `pushMiddleware` after existing adapter wiring.
    - `composer qa` chain exits 0 (pint-test → phpstan analyse level 10 → phpmd → pest --coverage --min=90).
    - Smoke: `php artisan route:list` (host root) shows middleware on the global stack OR the middleware test suite asserts handle() is actually invoked when bound.
  </behavior>
  <read_first>
    plugins/logingrupa/metapixel/Plugin.php (current boot — append, do not rewrite; existing adapter registration is preserved),
    plugins/logingrupa/metapixel/.planning/phases/04-settings-rework-multisite-trustedhosts-cookie-failedevents-t/04-RESEARCH.md (Example 4 lines 1645-1656 — verbatim pushMiddleware snippet; Pitfall 3 — middleware ordering),
    plugins/logingrupa/metapixel/.planning/phases/04-settings-rework-multisite-trustedhosts-cookie-failedevents-t/04-PATTERNS.md (Plugin.php section lines 769-825 — append shape)
  </read_first>
  <action>
    Edit `plugins/logingrupa/metapixel/Plugin.php`:

    1) Add imports:
       - `use Illuminate\Contracts\Http\Kernel;`
       - `use Logingrupa\Metapixel\Middleware\EnsureFbpFbcCookies;`

    2) In `boot()` method, AFTER the existing `Event::subscribe(ThemeAjaxHandler::class);` line, append a single line:
       - `$this->app[Kernel::class]->pushMiddleware(EnsureFbpFbcCookies::class);`

    This appends the middleware to the global HTTP middleware stack so it fires on every web request. The middleware itself short-circuits for backend paths via its `shouldSkip` check (Pitfall 3 / RESEARCH Pattern 7 line 841).

    3) DO NOT add conditional registration based on `trusted_hosts` empty state — per RESEARCH Open Question 4 recommendation, always push; the middleware self-defends via `readTrustedHosts` empty-list NO-OP.

    Apply Pint.

    Then run the full QA chain to confirm Wave 2 stability:
    - `composer qa` (pint-test → phpstan level 10 → phpmd → pest --coverage --min=90).
  </action>
  <acceptance_criteria>
    - `Plugin.php` contains literal `pushMiddleware(EnsureFbpFbcCookies::class)`.
    - `Plugin.php` contains import `use Illuminate\Contracts\Http\Kernel;`.
    - `Plugin.php` contains import `use Logingrupa\Metapixel\Middleware\EnsureFbpFbcCookies;`.
    - `composer test -- --filter=EnsureFbpFbcCookiesTest` STILL exits 0 (regression on plan 04-03 Task 1).
    - `composer test -- --filter='SettingsLookupForSiteTest|HostIndexResolverTest|RefreshPslTest|TrustedHostsValidationTest'` exits 0 (regression on plans 04-01 + 04-02).
    - `composer qa` exits 0 (full chain green; coverage ≥ 90% after Wave 2 additions).
    - `vendor/bin/pint --test --format agent plugins/logingrupa/metapixel/Plugin.php` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd plugins/logingrupa/metapixel &amp;&amp; composer qa</automated>
  </verify>
  <done>Middleware registered in Plugin::boot; full composer qa chain green at Wave 2 close; COOK-01..03 complete; plans 04-01..04-03 all regression-clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Laravel HTTP Kernel → middleware stack | `EnsureFbpFbcCookies` runs on every web request (after session middleware, before response render). All input (Host header, fbclid query, existing cookies) is untrusted. |
| Middleware → Settings.trusted_hosts | Read-only Settings lookup with try/catch boundary fail-safe (Pitfall 8). |
| Middleware → HostIndexResolver | App-container resolved; resolver returns null for any unparseable / unknown-TLD host — middleware treats null as NO-OP. |
| Middleware → Response cookies | Symfony `Cookie::create` with 9 attrs locked (path /, domain null, secure mirrors request, httpOnly false, sameSite lax). No raw `header('Set-Cookie: …')` — Symfony handles encoding. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-COOK-01 (CR-03) | Tampering / XSS | fbclid query injection | mitigate | `preg_match('/^[A-Za-z0-9_-]+$/', $sFbclid) === 1` AND `strlen <= 255` BEFORE cookie value composition. Invalid → skip (`return;`). 3 test cases (`test_invalid_fbclid_charset_skips_fbc`, `test_oversize_fbclid_skips_fbc`, `test_exactly_255_char_fbclid_is_accepted`). |
| T-04-COOK-02 | Information Disclosure | Cookie domain attribute misderived → cookie set on wrong scope | mitigate | Cookie domain attribute hardcoded to `null` (implicit current-host scope per RFC 6265 §5.1.3). No subdomain widening. PSL-derived `index` is the cookie VALUE encoding, NOT the Cookie Domain attribute. |
| T-04-COOK-03 (P-15) | Tampering / Spoofing | Host-header spoofing → cookies set on attacker-controlled host | mitigate | Untrusted host (not in `Settings::trusted_hosts`) → middleware returns response untouched. `HostIndexResolver` null result → same NO-OP. 2 test cases (`test_untrusted_host_writes_no_cookies`, `test_psl_unresolvable_host_writes_no_cookies`). |
| T-04-COOK-04 (Pitfall 8) | Denial of Service | Initial migration HTTP request 500s because `system_settings` table doesn't exist | mitigate | `shouldSkip` try/catch on Settings::get → Log::warning + return false (middleware proceeds with default behavior). Verified by `test_settings_get_throwing_does_not_500`. |
| T-04-COOK-05 (CR-03 length) | Resource exhaustion | Multi-KB fbclid payload | mitigate | 255-char hard cap via strlen check BEFORE preg_match (cheap O(1) length test runs first). |
| T-04-COOK-06 | Tampering | Cookie poisoning via shared-cache `Cache-Control: public` response | accept (documented) | COOK-03: README documents operator responsibility for `Cache-Control: private`; middleware does not auto-set headers (would conflict with operator's CMS-side response shaping). Class-level docblock references README. |
</threat_model>

<verification>
- Task 0 — `tests/Feature/Middleware/EnsureFbpFbcCookiesTest.php` exists with ≥ 14 test cases; tests red (production class not yet authored).
- Task 1 — `composer test -- --filter=EnsureFbpFbcCookiesTest` exits 0; cookie format regex assertions match; boundary fail-safe case verified.
- Task 2 — `composer qa` exits 0 end-to-end (full chain at Wave 2 close); regression suite green across plans 04-01 + 04-02 + 04-03.
</verification>

<success_criteria>
- COOK-01: Kill switch `Settings::get('ensure_fbp_fbc_server_side', true)` honored — boolean true OR int 1 OR string `'1'` all keep middleware active; false → short-circuit.
- COOK-02: CR-03 fbclid validation enforced — `[A-Za-z0-9_-]` charset AND ≤ 255 chars; invalid → skip `_fbc` (no throw).
- COOK-03: Class docblock contains `Cache-Control: private` README reference. README population is deferred to Phase 5 (DOCS-01); the docblock reference is in place this plan.
- D-20: Fresh code lands; legacy v1.x middleware NOT ported; v1.x decisions (90-day TTL, `fb.{N}.{ts}.{rand}` format, CSPRNG random, Symfony Cookie attrs) reused verbatim against new HostIndexResolver injection.
- 14+ feature test cases pass; `composer qa` exits 0.
</success_criteria>

<output>
Create `.planning/phases/04-settings-rework-multisite-trustedhosts-cookie-failedevents-t/04-03-SUMMARY.md` when done — capture: middleware file shape (line count + LOC), test case counts (cookie format / kill switch / fbclid / host / boundary), pushMiddleware Plugin.php diff summary, regression confirmation across plans 04-01 + 04-02, Hungarian/Tiger-Style/Pint compliance.
</output>
