---
phase: 03-shopaholicadapter-themeactionadapter-parallel-wave
plan: 03
type: execute
wave: 1
depends_on: [03-02]
files_modified:
  - classes/adapter/shopaholic/ShopaholicCartPositionAdapter.php
  - classes/adapter/shopaholic/ShopaholicCartPositionValueResolver.php
  - classes/event/adapter/shopaholic/CartPositionWatcher.php
  - Plugin.php
  - tests/ShopaholicAdapterTestCase.php
  - tests/Contract/Adapter/Shopaholic/ShopaholicCartPositionAdapterContractTest.php
  - tests/Unit/Adapter/Shopaholic/ShopaholicCartPositionValueResolverTest.php
  - tests/Unit/Event/Adapter/Shopaholic/CartPositionWatcherTest.php
autonomous: true
requirements: []
must_haves:
  truths:
    - "ShopaholicCartPositionAdapter satisfies the EventSubjectAdapter contract — 10 invariant contract test passes."
    - "ShopaholicCartPositionAdapter::getSubjectType returns the opaque alias 'shopaholic.cart_position' (NOT class FQN — P-05 prevention)."
    - "ShopaholicCartPositionValueResolver accesses Offer via the CartPosition->item MorphTo (NOT a non-existent offer_id column — Pitfall 1)."
    - "CartPositionWatcher binds eloquent.created (always dispatch) + eloquent.updated (dispatch only when EventLog row absent — qty-bump dedup via UNIQUE race-fence)."
    - "Plugin::boot registers the CartPosition adapter + subscribes CartPositionWatcher alongside the Order adapter, gated by the SAME `$this->isShopaholicEnabled()` helper introduced in 03-02 (resolved via `App::make(PluginManager::class)` for test-swappability)."
  artifacts:
    - path: "classes/adapter/shopaholic/ShopaholicCartPositionAdapter.php"
      provides: "EventSubjectAdapter for Lovata\\OrdersShopaholic\\Models\\CartPosition"
      contains: "'shopaholic.cart_position'"
    - path: "classes/adapter/shopaholic/ShopaholicCartPositionValueResolver.php"
      provides: "ValueResolver — MorphTo-aware Offer access for SKU content_ids"
      contains: "->item"
    - path: "classes/event/adapter/shopaholic/CartPositionWatcher.php"
      provides: "Watcher with eloquent.created + dedup-aware eloquent.updated dispatch"
      contains: "eloquent.created"
  key_links:
    - from: "classes/event/adapter/shopaholic/CartPositionWatcher.php"
      to: "classes/queue/SendCapiEvent.php"
      via: "SendCapiEvent::dispatch('AddToCart', $arPayload, $obCartPosition, ShopaholicCartPositionAdapter::class)"
      pattern: "'AddToCart'"
    - from: "classes/adapter/shopaholic/ShopaholicCartPositionValueResolver.php"
      to: "Lovata\\Shopaholic\\Models\\Offer"
      via: "$obCartPosition->item MorphTo with null-guard"
      pattern: "->item"
---

<objective>
Ship the second Shopaholic adapter for `Lovata\OrdersShopaholic\Models\CartPosition` (alias `'shopaholic.cart_position'`, event `AddToCart`) alongside `ShopaholicCartPositionValueResolver` and `CartPositionWatcher`. Honors RESEARCH.md Pitfall 1 — CartPosition has NO `offer_id` column; Offer is accessed via the `$obCartPosition->item` MorphTo with null-guard. Watcher dispatches on `eloquent.created` always; on `eloquent.updated` only when no EventLog row exists for the tuple (qty-bump dedup anchor).

Purpose: Carries SHOP-01..04 surface across to the second subject kind. UNIQUE race-fence on `(subject_type='shopaholic.cart_position', subject_id, AddToCart, channel, site_id)` is the dedup anchor — qty-bumps do not re-fire because subject_id stays constant. Demonstrates the adapter pattern scales beyond Order without modifying any Phase 2 backbone code.
Output: Two new adapter classes, one Watcher, registration wired into Plugin::boot, hermetic test fixtures, three test files (contract + ValueResolver unit + Watcher unit).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-shopaholicadapter-themeactionadapter-parallel-wave/03-CONTEXT.md
@.planning/phases/03-shopaholicadapter-themeactionadapter-parallel-wave/03-RESEARCH.md
@classes/adapter/EventSubjectAdapter.php
@classes/adapter/ValueResolver.php
@classes/adapter/shopaholic/ShopaholicOrderAdapter.php
@classes/adapter/shopaholic/ShopaholicOrderValueResolver.php
@classes/event/adapter/shopaholic/OrderStatusWatcher.php
@classes/queue/SendCapiEvent.php
@classes/helper/EventLogWriter.php
@classes/meta/PayloadBuilder.php
@models/EventLog.php
@Plugin.php
@tests/ShopaholicAdapterTestCase.php
@tests/Contract/Adapter/Shopaholic/ShopaholicOrderAdapterContractTest.php
@CLAUDE.md
@../../CLAUDE.md

<interfaces>
<!-- Lovata models — VERIFIED column shapes -->

CartPosition (plugins/lovata/ordersshopaholic/models/CartPosition.php — lines 18-90):
- Columns: id, cart_id (int), item_id (int), item_type (string), property (array), quantity (int), timestamps, deleted_at.
- NO `offer_id` column. Offer access is via MorphTo: `$obCartPosition->item` returns Offer (or any morphable type per Lovata convention; first-party scope is Offer only).
- `cart` belongsTo (Cart model — Cart has site_id column).
- Quantity column is `quantity` (verified line 23 in PHPDoc).
- NO direct `price` column — price comes from `$obCartPosition->item->price` (Offer attribute).
- NO direct `site_id` column — site_id flows through `$obCartPosition->cart->site_id`.

Offer (Lovata\Shopaholic\Models\Offer — schema not re-read here; rely on Phase 3 test fixtures):
- $obOffer->id (int), $obOffer->product (BelongsTo Product), $obOffer->price_value (float), $obOffer->product->id (int).
- $obOffer->product->offer() relation count gating single- vs multi-offer SKU format (anchor: FacebookCatalog exporter line 356).

EventLogWriter::record (post 03-01 D-07 trailing arg):
EventLogWriter::record($sEventId, $sEventName, $sChannel, $obSubject, $sSecretKey, $iEventTime, $iSiteId, $arPayload): bool

EventLog scope (models/EventLog.php — for pre-dispatch dedup pre-check in Watcher):
- `EventLog::query()->where('subject_type', 'shopaholic.cart_position')->where('subject_id', $iId)->where('event_name', 'AddToCart')->where('channel', 'capi')->where('site_id', $iSiteId)->exists()` is the dedup gate. (UNIQUE race-fence is the source of truth — the pre-check is just an early-return optimization; the race-fence handles concurrent inserts.)

ShopaholicOrderAdapter pattern (post 03-02 — copy adapter shape for CartPosition):
- Class const SUBJECT_TYPE + SUPPORTED_EVENTS.
- 7 contract methods. getUserData populates only the keys derivable from the subject; missing keys = null.

ShopaholicCartPositionAdapter user_data note:
- CartPosition has NO email / name / phone / last_name fields — those belong to the customer/Buddies user, not the cart row. For an anonymous cart event, ALL 13 user_data keys are null in this adapter (UserDataHasher hashes nothing → all-null user_data is valid per CAPI spec; Meta still accepts events when only client_ip + client_user_agent populate on the request boundary, but Phase 3 first-party scope leaves those for Theme/EventPixel to populate via Phase 4 cookie middleware).
- 'external_id' for an anonymous cart subject = sha256 of the cart's session-derived token. Phase 3 simplest path: return null (cart row has no stable secret_key); UserDataHasher honors null → omits the field hash.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend ShopaholicAdapterTestCase with bootCartPositionTable + bootProductsOffers helpers</name>
  <files>tests/ShopaholicAdapterTestCase.php</files>
  <read_first>
    - tests/ShopaholicAdapterTestCase.php (existing bootOrdersTable + bootOrdersStatuses; dropHermeticSchemas)
    - plugins/lovata/ordersshopaholic/models/CartPosition.php (column list — id, cart_id, item_id, item_type, property, quantity, timestamps, deleted_at)
    - plugins/lovata/shopaholic/models/Offer.php (read first 60 lines if exists — for hermetic offer table columns; otherwise extrapolate the minimal columns: id, product_id, price_value)
    - plugins/lovata/shopaholic/models/Product.php (first 60 lines — minimal columns: id, name)
  </read_first>
  <action>
Add NEW protected methods to `tests/ShopaholicAdapterTestCase.php` (do NOT remove or modify existing methods):

- `bootCartPositionTable(): void` — `if (Schema::hasTable('lovata_orders_shopaholic_cart_positions')) return;` Schema::create with columns: id (increments), cart_id (int nullable), item_id (int nullable), item_type (string nullable), property (text nullable), quantity (int nullable), timestamps, softDeletes (nullable deleted_at).
- `bootCartTable(): void` — minimal `lovata_orders_shopaholic_carts` table: id, user_id nullable, site_id nullable int, timestamps. Needed because CartPosition has `cart_id` FK to Cart, and Cart carries site_id.
- `bootOffersAndProductsTables(): void` — minimal `lovata_shopaholic_offers` (id, product_id int nullable, name nullable, price_value decimal nullable, active bool default true, timestamps) + `lovata_shopaholic_products` (id, name nullable, slug nullable unique, active bool default true, timestamps). Idempotent guards (`if (Schema::hasTable(...)) return;`).

Update `dropHermeticSchemas(): void` to also `Schema::dropIfExists` the 4 new tables. Call `parent::dropHermeticSchemas();` last.

DO NOT seed offer rows here — concrete tests create rows in their own setUp / test body so each test is hermetic.

LOC budget: post-edit file ≤ 130 LOC.
  </action>
  <verify>
    <automated>cd /home/forge/nailscosmetics.lv/plugins/logingrupa/metapixel && grep -q "bootCartPositionTable" tests/ShopaholicAdapterTestCase.php && grep -q "bootOffersAndProductsTables" tests/ShopaholicAdapterTestCase.php && grep -q "lovata_orders_shopaholic_cart_positions" tests/ShopaholicAdapterTestCase.php && wc -l tests/ShopaholicAdapterTestCase.php | awk '{exit ($1<=130)?0:1}' && /home/forge/nailscosmetics.lv/vendor/bin/pest --compact tests/Feature/Adapter/BackboneIntegrationTest.php 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/ShopaholicAdapterTestCase.php` declares the three new protected methods (`bootCartPositionTable`, `bootCartTable`, `bootOffersAndProductsTables`).
    - `dropHermeticSchemas` drops the new tables — `grep -c "dropIfExists" tests/ShopaholicAdapterTestCase.php` returns ≥ 6 (2 existing + 4 new).
    - Phase 2 BackboneIntegrationTest still passes (no regression from helper additions).
    - LOC ≤ 130.
  </acceptance_criteria>
  <done>ShopaholicAdapterTestCase now provisions hermetic CartPosition + Cart + Offer + Product tables for CartPosition adapter tests.</done>
</task>

<task type="auto">
  <name>Task 2: Author ShopaholicCartPositionAdapter + ShopaholicCartPositionValueResolver</name>
  <files>classes/adapter/shopaholic/ShopaholicCartPositionAdapter.php, classes/adapter/shopaholic/ShopaholicCartPositionValueResolver.php</files>
  <read_first>
    - classes/adapter/EventSubjectAdapter.php (7-method contract)
    - classes/adapter/ValueResolver.php (5-method contract)
    - classes/adapter/shopaholic/ShopaholicOrderAdapter.php (post 03-02 — pattern reference for adapter shape)
    - classes/adapter/shopaholic/ShopaholicOrderValueResolver.php (post 03-02 — pattern reference for resolver shape + SKU helper)
    - plugins/lovata/ordersshopaholic/models/CartPosition.php (lines 18-90 — MorphTo `$item` access)
    - .planning/phases/03-shopaholicadapter-themeactionadapter-parallel-wave/03-RESEARCH.md §"Pitfall 1" (CartPosition MorphTo + null-guard)
  </read_first>
  <action>
Create `classes/adapter/shopaholic/ShopaholicCartPositionAdapter.php`. Namespace `Logingrupa\Metapixel\Classes\Adapter\Shopaholic`. `final class ShopaholicCartPositionAdapter implements EventSubjectAdapter`. Imports `Logingrupa\Metapixel\Classes\Adapter\EventSubjectAdapter`, `Logingrupa\Metapixel\Classes\Adapter\ValueResolver`, `Lovata\OrdersShopaholic\Models\CartPosition`. Class const `SUBJECT_TYPE = 'shopaholic.cart_position'` (D-19). Class const `SUPPORTED_EVENTS = ['AddToCart' => ['capi', 'pixel']]`.

Implement 7 methods:
- `getSubjectType(object $obSubject): string { return self::SUBJECT_TYPE; }`
- `getSubjectId(object $obSubject): int { return (int) $obSubject->getAttribute('id'); }`
- `getSiteId(object $obSubject): ?int` — read from the parent Cart row: `$obCart = $obSubject->cart; if ($obCart === null) return null; $mSiteId = $obCart->getAttribute('site_id'); return is_int($mSiteId) ? $mSiteId : (is_numeric($mSiteId) ? (int) $mSiteId : null);` Per `EventSubjectAdapter` contract this still reads "from the subject" — Cart is a 1-hop relation traversal, not request context. PHPStan disallowed-calls bans Site/SiteManager/Request inside the adapter dir; the Cart navigation is permitted.
- `getSecretKey(object $obSubject): ?string { return null; }` — CartPosition has no stable secret; UserDataHasher honors null + omits external_id hash.
- `getValueResolver(object $obSubject): ValueResolver { return new ShopaholicCartPositionValueResolver; }`
- `getUserData(object $obSubject): array` — return ALL 13 Meta CAPI keys with null values. (Cart row has no PII; theme-side cookies populate fbp/fbc/client_ip/user_agent in the EventPixel render path; Phase 4 cookie middleware sets them on the request boundary.) PHPDoc one-liner notes the all-null shape is by design for anonymous cart subjects.
- `getSupportedEvents(): array { return self::SUPPORTED_EVENTS; }`

Total file LOC ≤ 80 (D-27).

Create `classes/adapter/shopaholic/ShopaholicCartPositionValueResolver.php`. Namespace `Logingrupa\Metapixel\Classes\Adapter\Shopaholic`. `final class ShopaholicCartPositionValueResolver implements ValueResolver`. Imports `Logingrupa\Metapixel\Classes\Adapter\ValueResolver`. Class-level PHPDoc one-liner: "ValueResolver for Lovata\\OrdersShopaholic\\Models\\CartPosition. Accesses Offer via the MorphTo \\$item relation with null-guard (Pitfall 1)."

Implement 5 methods (all operate on a single CartPosition representing ONE line item — not a full cart):
- `resolveContentIds(object $obSubject): array` — `$obItem = $obSubject->item; if ($obItem === null) return [];` (null-guard MorphTo per Pitfall 1). Then build the SKU id: `$bMultiOffer = $obItem->product !== null && method_exists($obItem->product, 'offer') ? $obItem->product->offer()->count() > 1 : false; $iProductId = (int) ($obItem->product->id ?? 0); $sId = $bMultiOffer ? sprintf('SKU-%d-%d', $iProductId, (int) $obItem->id) : sprintf('SKU-%d', $iProductId);` Return `[$sId]`.
- `resolveValue(object $obSubject): float` — `$obItem = $obSubject->item; $iQuantity = (int) $obSubject->getAttribute('quantity'); $mPrice = $obItem !== null ? $obItem->getAttribute('price_value') : 0; return $iQuantity * (is_numeric($mPrice) ? (float) $mPrice : 0.0);`
- `resolveCurrency(object $obSubject): string` — fallback chain: Settings.default_currency_code first (cart row has no currency relation typically), else throw — actually simplest: read `Settings::get('default_currency_code', 'EUR')` with runtime string guard; if non-empty, return it; else throw `OrderHasNoCurrencyException` ("CartPosition has no currency context; configure Settings.default_currency_code"). Reuse the same exception class from 03-02.
- `resolveContents(object $obSubject): array` — single-item list: `if ($obSubject->item === null) return []; return [[ 'id' => $this->resolveContentIds($obSubject)[0] ?? '', 'quantity' => (int) $obSubject->getAttribute('quantity'), 'item_price' => is_numeric($obSubject->item->getAttribute('price_value')) ? (float) $obSubject->item->getAttribute('price_value') : 0.0 ]];`
- `resolveNumItems(object $obSubject): int { return (int) $obSubject->getAttribute('quantity'); }`

Total file LOC ≤ 100.

Hungarian notation throughout. Final classes. No `@phpstan-ignore`. PHPDoc one-line summaries only.
  </action>
  <verify>
    <automated>cd /home/forge/nailscosmetics.lv/plugins/logingrupa/metapixel && grep -q "'shopaholic.cart_position'" classes/adapter/shopaholic/ShopaholicCartPositionAdapter.php && grep -q "'AddToCart' => \\['capi', 'pixel'\\]" classes/adapter/shopaholic/ShopaholicCartPositionAdapter.php && grep -q "\\\$obSubject->item" classes/adapter/shopaholic/ShopaholicCartPositionValueResolver.php && grep -q "\\\$obItem === null" classes/adapter/shopaholic/ShopaholicCartPositionValueResolver.php && wc -l classes/adapter/shopaholic/ShopaholicCartPositionAdapter.php | awk '{exit ($1<=80)?0:1}' && wc -l classes/adapter/shopaholic/ShopaholicCartPositionValueResolver.php | awk '{exit ($1<=100)?0:1}' && /home/forge/nailscosmetics.lv/vendor/bin/phpstan analyse --configuration=phpstan.neon --no-progress 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - File `classes/adapter/shopaholic/ShopaholicCartPositionAdapter.php` contains literal `'shopaholic.cart_position'` AND `'AddToCart' => ['capi', 'pixel']`.
    - File `classes/adapter/shopaholic/ShopaholicCartPositionValueResolver.php` accesses `$obSubject->item` AND contains a null-guard branch (`$obItem === null` or equivalent — verified by grep).
    - File does NOT reference a `offer_id` column attribute (`! grep -E "->getAttribute\\('offer_id'\\)|->offer_id" classes/adapter/shopaholic/ShopaholicCartPositionValueResolver.php`).
    - LOC: adapter ≤ 80, resolver ≤ 100.
    - PHPStan level 10 exits 0.
    - Adapter does NOT reference Site / SiteManager / Request: `! grep -E '(Site|SiteManager|Request)::|request\\(' classes/adapter/shopaholic/ShopaholicCartPositionAdapter.php`.
  </acceptance_criteria>
  <done>CartPosition adapter + resolver shipped; MorphTo `$item` null-guard respected; SKU format mirrors Order resolver; phpstan green.</done>
</task>

<task type="auto">
  <name>Task 3: Author CartPositionWatcher with created + updated-with-dedup-precheck dispatch</name>
  <files>classes/event/adapter/shopaholic/CartPositionWatcher.php</files>
  <read_first>
    - classes/event/adapter/shopaholic/OrderStatusWatcher.php (post 03-02 — pattern reference for plain Event::subscribe class shape)
    - classes/adapter/shopaholic/ShopaholicCartPositionAdapter.php (post Task 2)
    - classes/adapter/shopaholic/ShopaholicCartPositionValueResolver.php (post Task 2)
    - models/EventLog.php (scopeForSubject — uses subject_type + subject_id pair)
    - plugins/lovata/ordersshopaholic/models/CartPosition.php (verified columns)
  </read_first>
  <action>
Create `classes/event/adapter/shopaholic/CartPositionWatcher.php`. Namespace `Logingrupa\Metapixel\Classes\Event\Adapter\Shopaholic`. `final class CartPositionWatcher`. Imports:
- `Illuminate\Events\Dispatcher`
- `Illuminate\Support\Facades\DB`
- `Illuminate\Support\Facades\Log`
- `Logingrupa\Metapixel\Classes\Adapter\Shopaholic\ShopaholicCartPositionAdapter`
- `Logingrupa\Metapixel\Classes\Adapter\Shopaholic\ShopaholicCartPositionValueResolver`
- `Logingrupa\Metapixel\Classes\Meta\PayloadBuilder`
- `Logingrupa\Metapixel\Classes\Meta\UserDataHasher`
- `Logingrupa\Metapixel\Classes\Queue\SendCapiEvent`
- `Lovata\OrdersShopaholic\Models\CartPosition`
- `Ramsey\Uuid\Uuid`
- `Throwable`

Class-level PHPDoc one-liner: "Watches CartPosition eloquent.created|updated; fires AddToCart on creation always, on update only when EventLog has no prior (cart_position, AddToCart, capi, site_id) row — qty-bump dedup via UNIQUE race-fence."

Method `subscribe(Dispatcher $obDispatcher): void`:
- `$obDispatcher->listen('eloquent.created: ' . CartPosition::class, [$this, 'handleCreated']);`
- `$obDispatcher->listen('eloquent.updated: ' . CartPosition::class, [$this, 'handleUpdated']);`

Method `handleCreated(CartPosition $obCartPosition): void`:
- Single line of try-wrap; on success calls `$this->dispatchAddToCart($obCartPosition);` Catch logs + returns (Tiger-Style; never rethrow). Log context: `meta_pixel.cart_position_id`, `meta_pixel.event` = 'created'.

Method `handleUpdated(CartPosition $obCartPosition): void`:
- Try-wrap. Inside try:
  1. Build adapter to derive subject_id + site_id: `$obAdapter = new ShopaholicCartPositionAdapter; $iSubjectId = $obAdapter->getSubjectId($obCartPosition); $iSiteId = $obAdapter->getSiteId($obCartPosition);` (Adapter call — NOT request/SiteManager.)
  2. Dedup pre-check via DB::table (fast — avoids building payload when an EventLog row already exists):
     ```
     $bAlreadyLogged = DB::table('logingrupa_metapixel_event_log')
         ->where('subject_type', 'shopaholic.cart_position')
         ->where('subject_id', $iSubjectId)
         ->where('event_name', 'AddToCart')
         ->where('channel', 'capi')
         ->where('site_id', $iSiteId)
         ->exists();
     if ($bAlreadyLogged) { return; }
     ```
  3. Else call `$this->dispatchAddToCart($obCartPosition);`. The UNIQUE race-fence inside EventLogWriter::record remains the source of truth — the pre-check is an early-return optimization that avoids unnecessary PayloadBuilder + queue dispatch when a row already exists. (Per RESEARCH.md "Discretion: CartPositionWatcher trigger semantics".)
- Catch: log + return.

Private method `dispatchAddToCart(CartPosition $obCartPosition): void`:
- Null-guard MorphTo item: `if ($obCartPosition->item === null) { Log::info('metapixel: CartPositionWatcher — item MorphTo null, skipping', ['meta_pixel.cart_position_id' => $obCartPosition->id]); return; }`
- Build payload: `$obAdapter = new ShopaholicCartPositionAdapter; $obResolver = new ShopaholicCartPositionValueResolver; $obBuilder = new PayloadBuilder(new UserDataHasher); $sEventId = Uuid::uuid4()->toString(); $iEventTime = time(); $arPayload = $obBuilder->buildEventPayload('AddToCart', $obAdapter, $obCartPosition, $obResolver, $sEventId, $iEventTime, []);`
- Dispatch: `SendCapiEvent::dispatch('AddToCart', $arPayload, $obCartPosition, ShopaholicCartPositionAdapter::class);`

Total LOC ≤ 70 (D-27).

DO NOT extend any Lovata.Toolbox `ModelHandler` (RESEARCH.md "Anti-Patterns to Avoid" — plain Event::subscribe class is the locked pattern).
  </action>
  <verify>
    <automated>cd /home/forge/nailscosmetics.lv/plugins/logingrupa/metapixel && grep -q "eloquent.created: ' . CartPosition::class" classes/event/adapter/shopaholic/CartPositionWatcher.php && grep -q "eloquent.updated: ' . CartPosition::class" classes/event/adapter/shopaholic/CartPositionWatcher.php && grep -q "exists()" classes/event/adapter/shopaholic/CartPositionWatcher.php && grep -q "'AddToCart'" classes/event/adapter/shopaholic/CartPositionWatcher.php && wc -l classes/event/adapter/shopaholic/CartPositionWatcher.php | awk '{exit ($1<=80)?0:1}' && /home/forge/nailscosmetics.lv/vendor/bin/phpstan analyse --configuration=phpstan.neon --no-progress 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - File `classes/event/adapter/shopaholic/CartPositionWatcher.php` binds BOTH `eloquent.created: ' . CartPosition::class` AND `eloquent.updated: ' . CartPosition::class`.
    - File `handleUpdated` body contains `->where('event_name', 'AddToCart')` + `->exists()` (dedup pre-check via DB::table).
    - File dispatches `SendCapiEvent::dispatch('AddToCart', $arPayload, $obCartPosition, ShopaholicCartPositionAdapter::class);`.
    - File catches do NOT rethrow: `! grep -E 'throw \$obException' classes/event/adapter/shopaholic/CartPositionWatcher.php`.
    - File does NOT reference Site / SiteManager / Request: `! grep -E '(Site|SiteManager|Request)::|request\\(' classes/event/adapter/shopaholic/CartPositionWatcher.php`.
    - File does NOT extend ModelHandler: `! grep "ModelHandler" classes/event/adapter/shopaholic/CartPositionWatcher.php`.
    - LOC ≤ 80.
    - PHPStan level 10 exits 0.
  </acceptance_criteria>
  <done>CartPositionWatcher binds both eloquent events; created always dispatches; updated dispatches only when EventLog row absent; null-guarded MorphTo item access; Tiger-Style log + return on exceptions; race-fence remains the canonical dedup.</done>
</task>

<task type="auto">
  <name>Task 4: Wire CartPosition adapter + watcher into the Plugin::boot Lovata-gated block</name>
  <files>Plugin.php</files>
  <read_first>
    - Plugin.php (post 03-02 — already has the `if ($this->isShopaholicEnabled()) { ... }` block registering the OrderAdapter + OrderStatusWatcher; the helper resolves PluginManager via App::make for test-swappability)
    - classes/adapter/shopaholic/ShopaholicCartPositionAdapter.php (post Task 2)
    - classes/event/adapter/shopaholic/CartPositionWatcher.php (post Task 3)
  </read_first>
  <action>
Modify `Plugin.php` to extend the existing Phase 3-02 Lovata-gate block:
- Add imports: `use Logingrupa\Metapixel\Classes\Adapter\Shopaholic\ShopaholicCartPositionAdapter;`, `use Logingrupa\Metapixel\Classes\Event\Adapter\Shopaholic\CartPositionWatcher;`, `use Lovata\OrdersShopaholic\Models\CartPosition;`.
- Inside the existing `if ($this->isShopaholicEnabled()) { ... }` block in Plugin::boot, after the `Event::subscribe(OrderStatusWatcher::class);` call, add:
  ```
  App::make(AdapterRegistry::class)->register(
      CartPosition::class,
      ShopaholicCartPositionAdapter::class,
  );
  Event::subscribe(CartPositionWatcher::class);
  ```
DO NOT add a second if-block — keep both adapter registrations under the SAME PluginManager gate.
  </action>
  <verify>
    <automated>cd /home/forge/nailscosmetics.lv/plugins/logingrupa/metapixel && grep -c "isShopaholicEnabled" Plugin.php | awk '{exit ($1>=2)?0:1}' && grep -q "CartPosition::class," Plugin.php && grep -q "Event::subscribe(CartPositionWatcher::class)" Plugin.php && /home/forge/nailscosmetics.lv/vendor/bin/phpstan analyse --configuration=phpstan.neon --no-progress 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - File `Plugin.php` contains EXACTLY ONE `$this->isShopaholicEnabled()` conditional block in Plugin::boot guarding both Order + CartPosition adapter registrations + both Watcher subscriptions (no duplicate gate).
    - File registers both `ShopaholicOrderAdapter::class` AND `ShopaholicCartPositionAdapter::class` inside the same block.
    - File subscribes both `OrderStatusWatcher::class` AND `CartPositionWatcher::class`.
    - PHPStan exits 0.
  </acceptance_criteria>
  <done>Plugin::boot now registers BOTH Shopaholic adapters + subscribes BOTH watchers, gated by the single PluginManager check.</done>
</task>

<task type="auto">
  <name>Task 5: Author 3 test files — CartPositionContractTest + ValueResolverTest + WatcherTest</name>
  <files>tests/Contract/Adapter/Shopaholic/ShopaholicCartPositionAdapterContractTest.php, tests/Unit/Adapter/Shopaholic/ShopaholicCartPositionValueResolverTest.php, tests/Unit/Event/Adapter/Shopaholic/CartPositionWatcherTest.php</files>
  <read_first>
    - classes/testing/EventSubjectAdapterContractTestCase.php (10 invariants base)
    - tests/Contract/Adapter/Shopaholic/ShopaholicOrderAdapterContractTest.php (post 03-02 — pattern reference for setUp + makeAdapter + makeSubject)
    - tests/Unit/Event/Adapter/Shopaholic/OrderStatusWatcherTest.php (post 03-02 — Bus::fake pattern, dispatch assertions)
    - tests/ShopaholicAdapterTestCase.php (post Task 1 — new bootCartPositionTable + bootOffersAndProductsTables helpers)
    - classes/adapter/shopaholic/ShopaholicCartPositionAdapter.php (post Task 2)
    - classes/event/adapter/shopaholic/CartPositionWatcher.php (post Task 3)
  </read_first>
  <action>
Create `tests/Contract/Adapter/Shopaholic/ShopaholicCartPositionAdapterContractTest.php`. Class attribute `#[Group('adapter')]`. `final class ShopaholicCartPositionAdapterContractTest extends EventSubjectAdapterContractTestCase`. setUp boots hermetic cart_position + cart + offers + products tables (mirror Plan 03-02 contract test setUp pattern — inline copy or trait-extracted helper). Implement:
- `makeAdapter(): EventSubjectAdapter { return new ShopaholicCartPositionAdapter; }`
- `makeSubject(): object` — insert hermetic rows: Product id=1, Offer id=1 product_id=1 price_value=12.50, Cart id=1 site_id=1, CartPosition id=1 cart_id=1 item_id=1 item_type=Lovata\\Shopaholic\\Models\\Offer::class quantity=3. Return `CartPosition::find(1)` (loads with item MorphTo).

The 10 invariants inherited from the base now run for the CartPosition adapter. Special attention on invariant 02 (subject_id > 0 — CartPosition.id=1 satisfies) and invariant 03 (site_id deterministic — reads Cart.site_id, both calls return 1).

Create `tests/Unit/Adapter/Shopaholic/ShopaholicCartPositionValueResolverTest.php`. `#[Group('adapter')]`. Extends ShopaholicAdapterTestCase (auto-bound via Pest.php for `tests/Unit/Adapter/Shopaholic/`). setUp boots cart_position + cart + offers + products. Tests:
- `test_resolve_content_ids_returns_sku_single_offer_form_when_one_offer_per_product` — Product id=42 with one Offer id=7; CartPosition->item->product->offer()->count() == 1; assert resolveContentIds returns `['SKU-42']`.
- `test_resolve_content_ids_returns_sku_multi_offer_form_when_multiple_offers_per_product` — Product id=42 with two offers id=7,8; CartPosition uses item_id=7; assert resolveContentIds returns `['SKU-42-7']`.
- `test_resolve_content_ids_returns_empty_when_item_morphto_is_null` — CartPosition with item_id pointing to a deleted Offer (no row in offers table); assert resolveContentIds returns `[]` (null-guard per Pitfall 1).
- `test_resolve_value_multiplies_quantity_by_offer_price_value` — CartPosition quantity=4, offer price_value=12.50; resolveValue returns 50.0.
- `test_resolve_num_items_returns_quantity` — quantity=7; resolveNumItems returns 7.
- `test_resolve_currency_falls_back_to_settings_default_when_set` — `Settings::set(['default_currency_code' => 'NOK']);` Settings::clearInternalCache(); resolveCurrency returns 'NOK'.
- `test_resolve_currency_throws_when_no_settings_default` — Settings has no default_currency_code; assert OrderHasNoCurrencyException thrown.

Create `tests/Unit/Event/Adapter/Shopaholic/CartPositionWatcherTest.php`. `#[Group('adapter')]`. Extends ShopaholicAdapterTestCase. setUp boots all hermetic tables + binds AdapterRegistry + registers CartPosition adapter + `Bus::fake();`. Tests:
- `test_handle_created_dispatches_add_to_cart` — Create CartPosition; call `(new CartPositionWatcher)->handleCreated($obPos);`; assert `Bus::assertDispatched(SendCapiEvent::class, fn($obJob) => $obJob->sEventName === 'AddToCart' && $obJob->sAdapterClass === ShopaholicCartPositionAdapter::class);`.
- `test_handle_updated_dispatches_when_event_log_row_absent` — Empty EventLog; call handleUpdated; assert dispatched.
- `test_handle_updated_skips_when_event_log_row_present` — Insert an EventLog row matching (shopaholic.cart_position, $iId, AddToCart, capi, 1); call handleUpdated; assert `Bus::assertNotDispatched(SendCapiEvent::class)`.
- `test_handle_created_skips_when_item_morphto_null` — CartPosition with item_id pointing to non-existent Offer; call handleCreated; assert no dispatch; `Log::shouldReceive('info')` captured.
- `test_handle_created_catches_exception_logs_warning_no_rethrow` — Force exception by overriding the resolver (`App::bind` swap, OR pass a CartPosition with no cart relation so getSiteId fails); assert no throw; Log::warning observed.

All test files use the `#[Group('adapter')]` class attribute via `use PHPUnit\Framework\Attributes\Group;`.
  </action>
  <verify>
    <automated>cd /home/forge/nailscosmetics.lv/plugins/logingrupa/metapixel && /home/forge/nailscosmetics.lv/vendor/bin/pest --compact tests/Contract/Adapter/Shopaholic/ShopaholicCartPositionAdapterContractTest.php tests/Unit/Adapter/Shopaholic/ShopaholicCartPositionValueResolverTest.php tests/Unit/Event/Adapter/Shopaholic/CartPositionWatcherTest.php 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - All 3 test files exist and carry `#[Group('adapter')]` class attribute.
    - CartPosition contract test runs all 10 inherited invariants green for ShopaholicCartPositionAdapter.
    - ValueResolver test covers both SKU branches (single + multi offer) + MorphTo null-guard + quantity-times-price + currency fallback + exception path.
    - Watcher test covers (a) handleCreated dispatch, (b) handleUpdated dispatch when log absent, (c) handleUpdated skip when log present (dedup pre-check), (d) skip when item MorphTo null, (e) Tiger-Style catch.
    - `pest --compact` exit 0.
  </acceptance_criteria>
  <done>3 new tests green; SHOP-01..04 surface confirmed across both Order + CartPosition.</done>
</task>

<task type="auto">
  <name>Task 6: composer qa green + atomic commit</name>
  <files>(QA gate)</files>
  <read_first>
    - composer.json
    - All files modified in Tasks 1-5
  </read_first>
  <action>
Run `vendor/bin/pint --dirty --format agent`, then `composer qa`, then `composer deps`. Fix any violations without `@phpstan-ignore`. Atomic commit `feat(03-03): ShopaholicCartPositionAdapter + ValueResolver + CartPositionWatcher`.
  </action>
  <verify>
    <automated>cd /home/forge/nailscosmetics.lv/plugins/logingrupa/metapixel && composer qa 2>&1 | tail -20 && composer deps 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `composer qa` exits 0 (full chain green; coverage ≥ 90%).
    - `composer deps` exits 0.
    - `! grep -rn "@phpstan-ignore" classes/ models/ Plugin.php`.
    - Single atomic commit on master.
  </acceptance_criteria>
  <done>Plan 03-03 closed; SHOP-01..04 surface complete for both Order + CartPosition; Plan 03-04 integration test can proceed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Anonymous browser → Cart::onAdd → CartPosition row | Browser-controlled cart action creates a CartPosition; payload is server-built. |
| CartPosition → Cart → site_id | Cart row carries site_id (Lovata.OrdersShopaholic-controlled). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03-01 | Tampering | subject_type for CartPosition | mitigate | Opaque alias `'shopaholic.cart_position'` from class const; contract test invariant 01 asserts no backslashes (P-05). |
| T-03-03-02 | Tampering | site_id at dispatch | mitigate | Reads from `$obCartPosition->cart->site_id` only — Cart navigation via Eloquent relation. PHPStan disallowed-calls bans Site/SiteManager/Request inside adapter dir. |
| T-03-03-03 | DoS | qty-bump fires duplicate AddToCart | mitigate | (a) DB::table dedup pre-check in handleUpdated; (b) UNIQUE race-fence on `(subject_type, subject_id, AddToCart, channel, site_id)` is the canonical dedup. Test in Task 5 asserts skip. |
| T-03-03-04 | DoS | Cascade-break on Cart save when item is null/Offer deleted | mitigate | dispatchAddToCart null-guards MorphTo `$item`; Log::info + return. Cart::save not affected. |
| T-03-03-05 | Tampering | CartPosition with non-Offer item_type (e.g. gift card) | accept | Phase 3 first-party scope is Offer only. Null-guard skips dispatch gracefully (A4 documented limitation). Third party writes own adapter per D-13. |
| T-03-03-SC | Tampering | composer install | accept | Zero new packages (Phase 3 RESEARCH §"Package Legitimacy Audit"). |
</threat_model>

<verification>
- `composer qa` + `composer deps` exit 0.
- `pest tests/Contract/Adapter/Shopaholic/ --compact` runs both Order + CartPosition contract tests (20 invariants = 2 × 10) green.
- `pest tests/Unit/Adapter/Shopaholic/ --compact` runs both ValueResolver tests green.
- `pest tests/Unit/Event/Adapter/Shopaholic/ --compact` runs both Watcher tests green.
- Static-source defence on the CartPosition adapter + watcher: `grep -rE '(Site|SiteManager|Request)::|request\\(' classes/adapter/shopaholic/ShopaholicCartPosition* classes/event/adapter/shopaholic/CartPositionWatcher.php` returns NO matches.
- Plugin.php still has exactly ONE `$this->isShopaholicEnabled()` conditional block in Plugin::boot guarding both Order + CartPosition adapter registrations + both Watcher subscriptions (`grep -c "isShopaholicEnabled" Plugin.php` returns >= 2 — one helper definition + at least one call site).
</verification>

<success_criteria>
- ShopaholicCartPositionAdapter satisfies the 10-invariant contract; alias `'shopaholic.cart_position'`.
- ShopaholicCartPositionValueResolver handles MorphTo `$item` null-guard correctly (Pitfall 1); SKU format mirrors Order resolver byte-for-byte.
- CartPositionWatcher binds eloquent.created + eloquent.updated; created always dispatches; updated dispatches only when EventLog has no prior row; Tiger-Style log + return on exceptions; ≤ 70 LOC.
- Plugin::boot registers BOTH Shopaholic adapters under the same `$this->isShopaholicEnabled()` conditional block guarding both Watcher subscriptions.
- All 3 new test files green; tagged `#[Group('adapter')]`.
- composer qa + composer deps green; phpstan level 10 zero errors; coverage ≥ 90%.
</success_criteria>

<output>
Create `.planning/phases/03-shopaholicadapter-themeactionadapter-parallel-wave/03-03-SUMMARY.md` when done.
</output>
