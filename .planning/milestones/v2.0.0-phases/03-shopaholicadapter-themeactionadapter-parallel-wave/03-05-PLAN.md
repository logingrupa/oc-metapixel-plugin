---
phase: 03-shopaholicadapter-themeactionadapter-parallel-wave
plan: 05
type: execute
wave: 1
depends_on: [03-04]
files_modified:
  - classes/adapter/theme/ThemeActionEvent.php
  - classes/adapter/theme/ThemeActionAdapter.php
  - classes/adapter/theme/ThemeActionValueResolver.php
  - phpstan.neon
  - tests/Contract/Adapter/Theme/ThemeActionAdapterContractTest.php
  - tests/Unit/Adapter/Theme/ThemeActionEventTest.php
  - tests/Unit/Adapter/Theme/ThemeActionAdapterTest.php
autonomous: true
requirements: [THEM-01, THEM-02]
must_haves:
  truths:
    - "ThemeActionEvent is a value object carrying sActionKey, iSyntheticId, sEventName, arPayload — iSyntheticId is a positive int derived from crc32 of action_key via sprintf('%u', ...) cast (Pitfall 6 workaround for 32-bit signed wrap)."
    - "ThemeActionEvent::fromArray validates `action_key` is a non-empty string. NO length cap is enforced (operators may use long descriptive keys like `pdp:product-{slug}-{id}-{site}`); validation kept minimal per Tiger-Style 'validate at boundary, not over-validate'."
    - "ThemeActionAdapter satisfies the EventSubjectAdapter contract — 10 invariant contract test passes."
    - "ThemeActionAdapter::getSiteId reads arPayload['site_id'] first; if missing or non-int, falls back to Site::getCurrent()?->getId() — D-15 ONE documented P-01 exception, encoded in class PHPDoc."
    - "PHPStan disallowed-calls config EXCLUDES classes/adapter/theme/ from the SiteManager/Site/Request ban — D-16 sole documented exception."
    - "classes/adapter/shopaholic/ + classes/event/adapter/shopaholic/ + classes/queue/ + classes/event/ remain under the disallow ban — defence-in-depth on the Shopaholic path."
  artifacts:
    - path: "classes/adapter/theme/ThemeActionEvent.php"
      provides: "Value object for theme-fired events"
      contains: "sActionKey"
    - path: "classes/adapter/theme/ThemeActionAdapter.php"
      provides: "EventSubjectAdapter for ThemeActionEvent"
      contains: "Site::getCurrent"
    - path: "phpstan.neon"
      provides: "disallowed-calls config with theme dir excluded"
      contains: "classes/adapter/shopaholic/*"
  key_links:
    - from: "classes/adapter/theme/ThemeActionAdapter.php"
      to: "classes/adapter/theme/ThemeActionEvent.php"
      via: "$obSubject is a ThemeActionEvent instance"
      pattern: "ThemeActionEvent"
---

<objective>
Ship `ThemeActionEvent` (value object — THEM-01) + `ThemeActionAdapter` (THEM-02) + a companion `ThemeActionValueResolver`. Encode D-15 + D-16 — `ThemeActionAdapter::getSiteId` is the ONE documented P-01 exception that may read from `Site::getCurrent()` as a fallback. PHPStan `disallowIn` deny-list narrows from `classes/adapter/*` glob → explicit per-subdir entries so the theme adapter dir is excluded.

Purpose: Closes THEM-01 + THEM-02. Subsequent plans 03-06 (ThemeEventCollector + Twig API), 03-07 (Larajax handler + P-09 defence), and 03-08 (EventPixel + PixelHead components) all consume this adapter pair. The phpstan deny-list narrowing this plan ships is consumed by all of them.
Output: Three new classes under `classes/adapter/theme/`, two unit tests, one contract test, an updated phpstan.neon config splitting the disallow scopes per-subdir.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-shopaholicadapter-themeactionadapter-parallel-wave/03-CONTEXT.md
@.planning/phases/03-shopaholicadapter-themeactionadapter-parallel-wave/03-RESEARCH.md
@classes/adapter/EventSubjectAdapter.php
@classes/adapter/ValueResolver.php
@classes/adapter/AdapterRegistry.php
@classes/adapter/shopaholic/ShopaholicOrderAdapter.php
@classes/testing/EventSubjectAdapterContractTestCase.php
@classes/meta/PayloadBuilder.php
@models/EventLog.php
@phpstan.neon
@tests/MetapixelTestCase.php
@tests/Contract/Adapter/Shopaholic/ShopaholicOrderAdapterContractTest.php
@CLAUDE.md
@../../CLAUDE.md

<interfaces>
<!-- D-15 + D-16 — theme dir is the SOLE P-01 exception -->

PHPStan deny-list change (D-16):
- BEFORE (Phase 2 lock):
  ```
  disallowIn:
    - 'classes/queue/*'
    - 'classes/event/*'
    - 'classes/adapter/*'   ← wildcard glob covers theme
  ```
- AFTER (Phase 3 D-16):
  ```
  disallowIn:
    - 'classes/queue/*'
    - 'classes/event/*'
    - 'classes/adapter/shopaholic/*'
    # NOTE: classes/adapter/theme/* is EXCLUDED (D-16 sole exception — ThemeActionAdapter
    #       reads Site::getCurrent() as fallback per D-15)
  ```
- Plan 03-02 Task 1 added `classes/event/adapter/shopaholic/*` to disallowIn. Plan 03-05 NARROWS the third entry from `classes/adapter/*` → `classes/adapter/shopaholic/*` (theme excluded). The `event/adapter/shopaholic/*` entry stays from 03-02.

Pitfall 6 + iSyntheticId construction:
- `crc32($sActionKey)` returns int — may be negative on 32-bit (rare on PHP 8.3 64-bit; safe to be defensive).
- Use `(int) sprintf('%u', crc32($sActionKey))` → forces unsigned interpretation; positive int in all cases.
- Cap to PHP_INT_MAX if needed (sprintf('%u') stays within unsigned 32-bit range = 0..4_294_967_295; fits in PHP_INT_MAX on 64-bit).
- Result satisfies contract invariant 02 (subject_id > 0) provided $sActionKey is non-empty.

D-15 ThemeActionAdapter::getSiteId:
1. `$mPayloadSiteId = $obSubject->arPayload['site_id'] ?? null;`
2. `if (is_int($mPayloadSiteId) && $mPayloadSiteId > 0) return $mPayloadSiteId;`
3. `if (is_numeric($mPayloadSiteId)) return (int) $mPayloadSiteId;`
4. Fallback: `$obSite = \October\Rain\Support\Facades\Site::getCurrent(); return $obSite !== null ? $obSite->getId() : null;`
5. Pitfall 6: `Site::getCurrent()` returns null in CLI / queue context → return null gracefully. Theme events fire in-request by definition.

ThemeActionEvent value object (THEM-01):
- Public readonly promoted properties: `string $sActionKey`, `int $iSyntheticId`, `string $sEventName`, `array $arPayload` (`@var array<string, mixed>` PHPDoc).
- Static named constructor `fromArray(array $arData): self` validating shape: requires `name` (string, in allowlist — Plan 03-07 enforces allowlist; THEM-01 just requires non-empty) + `action_key` (string, non-empty); arPayload is the remainder.
- Constructor throws `\InvalidArgumentException` on missing required keys.
- iSyntheticId built via `(int) sprintf('%u', crc32($sActionKey))`.

ThemeActionValueResolver (THEM-01 carrier — value + currency + content_ids come from arPayload):
- `resolveContentIds(object $obSubject): array` — reads `$obSubject->arPayload['content_ids'] ?? []`; runtime guard ensures list<string>.
- `resolveValue(object $obSubject): float` — reads `$obSubject->arPayload['value'] ?? 0.0`; numeric cast.
- `resolveCurrency(object $obSubject): string` — reads `$obSubject->arPayload['currency'] ?? 'EUR'`; runtime string guard with default 'EUR'.
- `resolveContents(object $obSubject): array` — reads `$obSubject->arPayload['contents'] ?? []`; list<array{id,quantity,item_price}>.
- `resolveNumItems(object $obSubject): int` — reads `$obSubject->arPayload['num_items'] ?? count(resolveContents())`; int cast.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Narrow phpstan.neon disallowed-calls deny-list to exclude classes/adapter/theme/ (D-16)</name>
  <files>phpstan.neon</files>
  <read_first>
    - phpstan.neon (current shape — post 03-02 Task 1; the four `disallowIn` lists each have 3 or 4 entries depending on how 03-02 wrote them)
  </read_first>
  <action>
Modify `phpstan.neon`. For each of the four `disallowIn` lists (one under `disallowedFunctionCalls.request()` + three under `disallowedMethodCalls` for SiteManager/Site/Request):

Replace the `'classes/adapter/*'` glob entry with TWO explicit entries:
- `'classes/adapter/shopaholic/*'`
- (the 03-02-added `'classes/event/adapter/shopaholic/*'` entry stays as-is)

Do NOT add `'classes/adapter/theme/*'` to ANY disallowIn list — that is the documented D-16 exception.

Add a YAML comment one-liner above each modified `disallowIn` list: `# D-16: classes/adapter/theme/* excluded — ThemeActionAdapter D-15 site fallback exception`.

After the edit, every `disallowIn` list contains: `classes/queue/*`, `classes/event/*`, `classes/adapter/shopaholic/*`, `classes/event/adapter/shopaholic/*`. NOTHING else.
  </action>
  <verify>
    <automated>cd /home/forge/nailscosmetics.lv/plugins/logingrupa/metapixel && ! grep -E "'classes/adapter/\*'" phpstan.neon && grep -c "'classes/adapter/shopaholic/\*'" phpstan.neon | awk '{exit ($1>=4)?0:1}' && ! grep -E "'classes/adapter/theme/\*'" phpstan.neon && /home/forge/nailscosmetics.lv/vendor/bin/phpstan analyse --configuration=phpstan.neon --no-progress 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `phpstan.neon` no longer contains `'classes/adapter/*'` (the wide glob); replaced by the narrower `'classes/adapter/shopaholic/*'` per-subdir entries.
    - `phpstan.neon` does NOT contain `'classes/adapter/theme/*'` (D-16 exception encoded by absence).
    - Each `disallowIn` list contains the four entries: `classes/queue/*`, `classes/event/*`, `classes/adapter/shopaholic/*`, `classes/event/adapter/shopaholic/*`.
    - `phpstan analyse` exits 0 on current tree (no regression — no adapter/theme files exist yet).
  </acceptance_criteria>
  <done>D-16 deny-list narrowing encoded; the theme adapter dir is the sole excluded path.</done>
</task>

<task type="auto">
  <name>Task 2: Author ThemeActionEvent value object + ThemeActionValueResolver</name>
  <files>classes/adapter/theme/ThemeActionEvent.php, classes/adapter/theme/ThemeActionValueResolver.php</files>
  <read_first>
    - classes/adapter/ValueResolver.php (5-method contract)
    - classes/adapter/shopaholic/ShopaholicOrderValueResolver.php (post 03-02 — pattern reference for runtime-guard mixed narrowing)
    - .planning/phases/03-shopaholicadapter-themeactionadapter-parallel-wave/03-RESEARCH.md §"Pitfall 6" (crc32 sprintf %u cast) + §"Example 4" (ThemeEventCollector — not used in this task; informs Plan 03-06)
    - CLAUDE.md §"No @phpstan-ignore" (D-28 — extract runtime guards)
  </read_first>
  <action>
Create `classes/adapter/theme/ThemeActionEvent.php`. Namespace `Logingrupa\Metapixel\Classes\Adapter\Theme`. `final class ThemeActionEvent`. Class-level PHPDoc one-liner: "Value object carrying a theme-fired event — actionKey + crc32-derived synthetic id + event name + raw payload. Subject of ThemeActionAdapter."

Public readonly promoted properties via constructor:
```
public function __construct(
    public readonly string $sActionKey,
    public readonly int $iSyntheticId,
    public readonly string $sEventName,
    public readonly array $arPayload,
) {}
```
PHPDoc `@param array<string, mixed> $arPayload`.

Static named constructor:
```
/**
 * @param  array<string, mixed>  $arData
 * @throws \InvalidArgumentException
 */
public static function fromArray(array $arData): self
{
    $mName = $arData['name'] ?? null;
    if (! is_string($mName) || $mName === '') {
        throw new \InvalidArgumentException('ThemeActionEvent.name is required (non-empty string)');
    }
    $mActionKey = $arData['action_key'] ?? null;
    // no length cap — see CONTEXT.md decisions / Plan must_haves
    if (! is_string($mActionKey) || $mActionKey === '') {
        throw new \InvalidArgumentException('ThemeActionEvent.action_key is required (non-empty string)');
    }
    $iSyntheticId = (int) sprintf('%u', crc32($mActionKey));
    return new self($mActionKey, $iSyntheticId, $mName, $arData);
}
```

Total file LOC ≤ 60.

Create `classes/adapter/theme/ThemeActionValueResolver.php`. Namespace `Logingrupa\Metapixel\Classes\Adapter\Theme`. `final class ThemeActionValueResolver implements ValueResolver`. Imports `Logingrupa\Metapixel\Classes\Adapter\ValueResolver`. Class-level PHPDoc one-liner: "ValueResolver for ThemeActionEvent — reads everything from arPayload with runtime-guarded fallbacks."

Implement 5 methods, all reading from `$obSubject` (which is a ThemeActionEvent instance — type-hint stays `object` per contract; runtime asserts via `$obSubject instanceof ThemeActionEvent` early-return). Pattern: at each method's top, if `$obSubject` is NOT a ThemeActionEvent, return safe default ([] for arrays, 0.0 for float, 'EUR' for currency, 0 for int). Then:
- `resolveContentIds`: `$mIds = $obSubject->arPayload['content_ids'] ?? []; if (! is_array($mIds)) return []; $arResult = []; foreach ($mIds as $mId) { if (is_string($mId)) $arResult[] = $mId; } return $arResult;`
- `resolveValue`: `$mValue = $obSubject->arPayload['value'] ?? 0.0; return is_numeric($mValue) ? (float) $mValue : 0.0;`
- `resolveCurrency`: `$mCur = $obSubject->arPayload['currency'] ?? 'EUR'; return is_string($mCur) && $mCur !== '' ? $mCur : 'EUR';`
- `resolveContents`: `$mContents = $obSubject->arPayload['contents'] ?? []; if (! is_array($mContents)) return []; $arResult = []; foreach ($mContents as $arItem) { if (is_array($arItem) && isset($arItem['id'], $arItem['quantity'], $arItem['item_price'])) { $arResult[] = [ 'id' => (string) $arItem['id'], 'quantity' => (int) $arItem['quantity'], 'item_price' => (float) $arItem['item_price'] ]; } } return $arResult;`
- `resolveNumItems`: `$mNum = $obSubject->arPayload['num_items'] ?? null; if (is_numeric($mNum)) return (int) $mNum; return count($this->resolveContents($obSubject));`

Total file LOC ≤ 100. Final class. No `@phpstan-ignore`. Hungarian notation.
  </action>
  <verify>
    <automated>cd /home/forge/nailscosmetics.lv/plugins/logingrupa/metapixel && grep -q "readonly string \\\$sActionKey" classes/adapter/theme/ThemeActionEvent.php && grep -q "readonly int \\\$iSyntheticId" classes/adapter/theme/ThemeActionEvent.php && grep -q "sprintf('%u', crc32" classes/adapter/theme/ThemeActionEvent.php && grep -q "implements ValueResolver" classes/adapter/theme/ThemeActionValueResolver.php && wc -l classes/adapter/theme/ThemeActionEvent.php | awk '{exit ($1<=60)?0:1}' && /home/forge/nailscosmetics.lv/vendor/bin/phpstan analyse --configuration=phpstan.neon --no-progress 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - File `classes/adapter/theme/ThemeActionEvent.php` declares constructor-promoted `readonly` props.
    - File contains `sprintf('%u', crc32(...))` literal (Pitfall 6 cast).
    - File contains literal `no length cap` (W-NEW-5 lock — deliberate omission of any character-count assertion on action_key; `grep -q "no length cap" classes/adapter/theme/ThemeActionEvent.php`).
    - File has `public static function fromArray(array $arData): self` that throws `\InvalidArgumentException` on missing keys.
    - File `classes/adapter/theme/ThemeActionValueResolver.php` implements `ValueResolver` interface.
    - LOC: event ≤ 60, resolver ≤ 100.
    - `phpstan analyse` exits 0.
  </acceptance_criteria>
  <done>Value object + companion resolver shipped; phpstan green; iSyntheticId always positive int.</done>
</task>

<task type="auto">
  <name>Task 3: Author ThemeActionAdapter with D-15 site_id fallback</name>
  <files>classes/adapter/theme/ThemeActionAdapter.php</files>
  <read_first>
    - classes/adapter/EventSubjectAdapter.php (7-method contract)
    - classes/adapter/theme/ThemeActionEvent.php (post Task 2)
    - classes/adapter/theme/ThemeActionValueResolver.php (post Task 2)
    - classes/adapter/shopaholic/ShopaholicOrderAdapter.php (post 03-02 — pattern reference; differs only in getSiteId)
    - .planning/phases/03-shopaholicadapter-themeactionadapter-parallel-wave/03-RESEARCH.md §"Pitfall 6" (Site::getCurrent null in CLI)
    - phpstan.neon (post Task 1 — confirms theme dir excluded)
  </read_first>
  <action>
Create `classes/adapter/theme/ThemeActionAdapter.php`. Namespace `Logingrupa\Metapixel\Classes\Adapter\Theme`. `final class ThemeActionAdapter implements EventSubjectAdapter`. Imports:
- `Logingrupa\Metapixel\Classes\Adapter\EventSubjectAdapter`
- `Logingrupa\Metapixel\Classes\Adapter\ValueResolver`
- `October\Rain\Support\Facades\Site`

Class-level PHPDoc (3-line — this is the ONE place multi-line PHPDoc is acceptable per CLAUDE.md style note — this is the documented D-15 + D-16 exception):
```
/**
 * EventSubjectAdapter for ThemeActionEvent.
 *
 * D-15 / D-16 — this adapter is the ONE documented P-01 exception. getSiteId
 * reads arPayload['site_id'] first; if missing or non-int, falls back to
 * Site::getCurrent()?->getId(). Theme events fire in-request by definition;
 * forcing operator boilerplate `site_id: this.site.id` on every Twig push
 * yields no real safety gain. phpstan.neon excludes classes/adapter/theme/*
 * from the SiteManager/Site/Request disallow list (D-16).
 *
 * Alias 'theme.action'. Supports any Meta-standard event name through Twig +
 * Larajax push paths; channel set declared at getSupportedEvents call.
 */
```

Class const `SUBJECT_TYPE = 'theme.action'` (D-19 alias convention; not a Shopaholic alias).
Class const `SUPPORTED_EVENTS` — the 18 Meta-standard event names from D-11, each mapped to `['capi', 'pixel']`:
```
private const SUPPORTED_EVENTS = [
    'PageView' => ['capi', 'pixel'],
    'ViewContent' => ['capi', 'pixel'],
    'AddToCart' => ['capi', 'pixel'],
    'AddToWishlist' => ['capi', 'pixel'],
    'InitiateCheckout' => ['capi', 'pixel'],
    'Purchase' => ['capi', 'pixel'],
    'Lead' => ['capi', 'pixel'],
    'CompleteRegistration' => ['capi', 'pixel'],
    'Search' => ['capi', 'pixel'],
    'Subscribe' => ['capi', 'pixel'],
    'Contact' => ['capi', 'pixel'],
    'FindLocation' => ['capi', 'pixel'],
    'Donate' => ['capi', 'pixel'],
    'CustomizeProduct' => ['capi', 'pixel'],
    'SubmitApplication' => ['capi', 'pixel'],
    'AddPaymentInfo' => ['capi', 'pixel'],
    'StartTrial' => ['capi', 'pixel'],
    'Schedule' => ['capi', 'pixel'],
];
```

Implement 7 methods:
- `getSubjectType(object $obSubject): string { return self::SUBJECT_TYPE; }`
- `getSubjectId(object $obSubject): int` — runtime guard: `if ($obSubject instanceof ThemeActionEvent) { return $obSubject->iSyntheticId; } return 0;` (0 will be rejected by EventLogWriter — defensive default).
- `getSiteId(object $obSubject): ?int` — D-15 implementation:
  ```
  if ($obSubject instanceof ThemeActionEvent) {
      $mPayloadSiteId = $obSubject->arPayload['site_id'] ?? null;
      if (is_int($mPayloadSiteId) && $mPayloadSiteId > 0) {
          return $mPayloadSiteId;
      }
      if (is_numeric($mPayloadSiteId)) {
          $iSiteId = (int) $mPayloadSiteId;
          return $iSiteId > 0 ? $iSiteId : null;
      }
  }
  $obSite = Site::getCurrent();
  return $obSite !== null ? (int) $obSite->getId() : null;
  ```
  Pitfall 6: `Site::getCurrent()` returns null in CLI/queue → null is fine (nullable contract).
- `getSecretKey(object $obSubject): ?string` — `if ($obSubject instanceof ThemeActionEvent) { $mKey = $obSubject->arPayload['secret_key'] ?? null; return is_string($mKey) ? $mKey : null; } return null;`
- `getValueResolver(object $obSubject): ValueResolver { return new ThemeActionValueResolver; }`
- `getUserData(object $obSubject): array` — Build the 13-key array; populate from arPayload runtime-guarded. Theme-side typical population:
  - 'em' => arPayload.em ?? null (string runtime guard)
  - 'ph' => arPayload.ph ?? null
  - 'fn' / 'ln' / 'ct' / 'st' / 'zp' / 'country' / 'external_id' => similarly
  - 'fbp' => arPayload.fbp ?? null  (passthrough — UserDataHasher does NOT hash this)
  - 'fbc' => arPayload.fbc ?? null
  - 'client_ip_address' => arPayload.client_ip_address ?? null
  - 'client_user_agent' => arPayload.client_user_agent ?? null
  - ALL 13 keys explicitly => null when absent (contract invariant 07).
- `getSupportedEvents(): array { return self::SUPPORTED_EVENTS; }`

Total file LOC ≤ 130 (slightly larger than 80 because of the 18-event const — acceptable per "reviewer smell test" since the const is data, not logic).

PHPStan disallowed-calls — confirm this class CAN import `October\Rain\Support\Facades\Site` because the deny-list excludes `classes/adapter/theme/*` (post Task 1).
  </action>
  <verify>
    <automated>cd /home/forge/nailscosmetics.lv/plugins/logingrupa/metapixel && grep -q "'theme.action'" classes/adapter/theme/ThemeActionAdapter.php && grep -q "Site::getCurrent" classes/adapter/theme/ThemeActionAdapter.php && grep -q "ThemeActionEvent" classes/adapter/theme/ThemeActionAdapter.php && grep -c "'capi', 'pixel'" classes/adapter/theme/ThemeActionAdapter.php | awk '{exit ($1>=18)?0:1}' && wc -l classes/adapter/theme/ThemeActionAdapter.php | awk '{exit ($1<=130)?0:1}' && /home/forge/nailscosmetics.lv/vendor/bin/phpstan analyse --configuration=phpstan.neon --no-progress 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - File `classes/adapter/theme/ThemeActionAdapter.php` contains literal `'theme.action'` for SUBJECT_TYPE.
    - File contains `Site::getCurrent()` call (allowed because phpstan excludes theme dir).
    - File contains all 18 Meta-standard event names in `SUPPORTED_EVENTS` const (`grep -c "'capi', 'pixel'"` ≥ 18).
    - LOC ≤ 130.
    - PHPStan level 10 exits 0 with NO `request()` / `Request::` / `SiteManager::` / `Site::` errors from this file (because deny-list excludes the path).
    - File has multi-line class-level PHPDoc documenting D-15 + D-16 exception (note: this is the ONE acceptable place for multi-paragraph PHPDoc per CLAUDE.md — the documented exception requires the explanation in-source).
  </acceptance_criteria>
  <done>ThemeActionAdapter shipped with D-15 site_id fallback + D-16 phpstan exception; 7 contract methods satisfied.</done>
</task>

<task type="auto">
  <name>Task 4: Author 3 test files — ThemeActionEventTest + ThemeActionAdapterTest + ThemeActionAdapterContractTest</name>
  <files>tests/Unit/Adapter/Theme/ThemeActionEventTest.php, tests/Unit/Adapter/Theme/ThemeActionAdapterTest.php, tests/Contract/Adapter/Theme/ThemeActionAdapterContractTest.php</files>
  <read_first>
    - classes/testing/EventSubjectAdapterContractTestCase.php (10 invariants base)
    - tests/Contract/Adapter/Shopaholic/ShopaholicOrderAdapterContractTest.php (post 03-02 — pattern reference for contract test)
    - classes/adapter/theme/ThemeActionEvent.php (post Task 2)
    - classes/adapter/theme/ThemeActionAdapter.php (post Task 3)
    - classes/adapter/theme/ThemeActionValueResolver.php (post Task 2)
  </read_first>
  <action>
Create `tests/Unit/Adapter/Theme/ThemeActionEventTest.php`. `#[Group('adapter')]` class attribute (`use PHPUnit\Framework\Attributes\Group;`). Extends MetapixelTestCase. NO `$autoMigrate` needed (value object — no DB). Tests:
- `test_from_array_builds_value_object_with_synthetic_id_positive_int`: pass `['name' => 'ViewContent', 'action_key' => 'product-view:42']`; assert `$obEvent->sEventName === 'ViewContent'`, `$obEvent->sActionKey === 'product-view:42'`, `$obEvent->iSyntheticId > 0`, `is_int($obEvent->iSyntheticId)`.
- `test_from_array_throws_on_missing_name`: pass `['action_key' => 'x']`; assert InvalidArgumentException with message containing 'name is required'.
- `test_from_array_throws_on_missing_action_key`: pass `['name' => 'ViewContent']`; assert exception.
- `test_from_array_throws_on_empty_name`: pass `['name' => '', 'action_key' => 'x']`; assert exception.
- `test_synthetic_id_deterministic_for_same_action_key`: build twice with same action_key 'cart-add:7'; assert `$obA->iSyntheticId === $obB->iSyntheticId`.
- `test_synthetic_id_different_for_different_action_keys`: 'cart-add:7' vs 'cart-add:8'; assert iSyntheticIds differ.
- `test_payload_round_trip_includes_all_input_keys`: pass `['name' => 'X', 'action_key' => 'y', 'value' => 12.5, 'currency' => 'EUR']`; assert `$obEvent->arPayload['value'] === 12.5`.

Create `tests/Unit/Adapter/Theme/ThemeActionAdapterTest.php`. `#[Group('adapter')]`. Extends MetapixelTestCase. Tests:
- `test_get_subject_type_returns_theme_action_alias`: `(new ThemeActionAdapter)->getSubjectType(/*any*/)` returns 'theme.action'.
- `test_get_subject_id_returns_synthetic_id_when_subject_is_theme_action_event`: build event, call getSubjectId, assert matches iSyntheticId.
- `test_get_subject_id_returns_zero_for_non_event_subject`: pass a stdClass; assert returns 0 (defensive default).
- `test_get_site_id_reads_payload_site_id_first_when_int`: build event with `arPayload = ['site_id' => 7]`; assert getSiteId returns 7.
- `test_get_site_id_reads_payload_site_id_when_numeric_string`: arPayload site_id = '7'; assert returns 7.
- `test_get_site_id_falls_back_to_site_get_current_when_payload_missing`: arPayload has no site_id key; mock `Site::getCurrent()` via Mockery `overload` or `alias` to return a stub with `getId() === 3`; assert returns 3. (If overload is fiddly, this test may use a partial mock + facade swap: `Site::shouldReceive('getCurrent')->andReturn($obFakeSite);` — October's Site facade extends `Illuminate\Support\Facades\Facade`, so `shouldReceive` works.)
- `test_get_site_id_returns_null_in_cli_when_site_get_current_returns_null`: mock `Site::getCurrent()` to return null; arPayload missing site_id; assert getSiteId returns null (Pitfall 6).
- `test_get_supported_events_contains_all_18_meta_standard_names`: `assertCount(18, (new ThemeActionAdapter)->getSupportedEvents());` AND `assertArrayHasKey('Purchase', ...);` AND `assertArrayHasKey('PageView', ...);`.

Create `tests/Contract/Adapter/Theme/ThemeActionAdapterContractTest.php`. `#[Group('adapter')]`. `final class ThemeActionAdapterContractTest extends EventSubjectAdapterContractTestCase`. Tests inherit all 10 invariants. Implement:
- `protected function makeAdapter(): EventSubjectAdapter { return new ThemeActionAdapter; }`
- `protected function makeSubject(): object { return ThemeActionEvent::fromArray(['name' => 'ViewContent', 'action_key' => 'product-view:42', 'site_id' => 1, 'value' => 12.50, 'currency' => 'EUR', 'content_ids' => ['SKU-42']]); }`

Note that invariant 04 (`getSiteId returns ?int — no exception, no Request side effect`) is the one most likely to surface a regression for the theme adapter: the fallback to `Site::getCurrent()` MUST not throw + must satisfy the nullable-int contract. The contract test runs in MetapixelTestCase context where Site::getCurrent() may return null — that satisfies the contract (Pitfall 6).

Invariant 09 (registry round-trip) registers ThemeActionAdapter for `ThemeActionEvent::class` — this is the production registration pattern from Plan 03-08's eventual Plugin::boot wire-up (which Plan 03-05 stops short of).

All test classes carry the PHP attribute `#[\PHPUnit\Framework\Attributes\Group('adapter')]`.
  </action>
  <verify>
    <automated>cd /home/forge/nailscosmetics.lv/plugins/logingrupa/metapixel && /home/forge/nailscosmetics.lv/vendor/bin/pest --compact tests/Unit/Adapter/Theme/ThemeActionEventTest.php tests/Unit/Adapter/Theme/ThemeActionAdapterTest.php tests/Contract/Adapter/Theme/ThemeActionAdapterContractTest.php 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - All 3 test files declare `#[Group('adapter')]` (`grep -l "Group('adapter')"` lists all 3).
    - ThemeActionEventTest covers fromArray happy + 3 failure cases + determinism + payload round-trip.
    - ThemeActionAdapterTest covers (a) alias, (b) subjectId for ThemeActionEvent + non-event, (c) site_id payload-first, (d) site_id numeric-string cast, (e) site_id fallback to Site::getCurrent, (f) site_id null in CLI, (g) all 18 supported events present.
    - ThemeActionAdapterContractTest runs all 10 inherited invariants green for ThemeActionAdapter.
    - `pest --compact` exit 0.
  </acceptance_criteria>
  <done>3 test files green; THEM-01 + THEM-02 closed at the contract level.</done>
</task>

<task type="auto">
  <name>Task 5: composer qa green + atomic commit</name>
  <files>(QA gate)</files>
  <read_first>
    - composer.json
    - All files modified Tasks 1-4
  </read_first>
  <action>
Run `vendor/bin/pint --dirty --format agent`, `composer qa`, `composer deps`. Atomic commit `feat(03-05): ThemeActionEvent + ThemeActionAdapter (THEM-01..02) + D-16 phpstan exclusion`.
  </action>
  <verify>
    <automated>cd /home/forge/nailscosmetics.lv/plugins/logingrupa/metapixel && composer qa 2>&1 | tail -15 && composer deps 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `composer qa` exits 0 (full chain).
    - `composer deps` exits 0.
    - `! grep -rn "@phpstan-ignore" classes/`.
    - Single atomic commit on master.
  </acceptance_criteria>
  <done>THEM-01 + THEM-02 closed; ThemeActionAdapter ready for ThemeEventCollector + Twig wiring in Plan 03-06.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator-supplied Twig `pushEvent` arg → ThemeActionEvent::fromArray | Untrusted array shape; fromArray validates name + action_key. |
| ThemeActionAdapter::getSiteId fallback → Site::getCurrent() | D-15 documented exception — request-bound theme events. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05-01 | Tampering | ThemeActionEvent constructed from operator arData | mitigate | `fromArray` validates `name` + `action_key` as non-empty strings; throws InvalidArgumentException on miss. Plan 03-07 layers on top with allowlist + rate-limit + JS-escape. |
| T-03-05-02 | Spoofing | site_id from operator-supplied payload | accept | D-15 documents exception; theme events fire in-request; phpstan deny-list excludes only this dir; runtime guard restricts to positive ints. |
| T-03-05-03 | Tampering | iSyntheticId collision (crc32 has 4B-key space) | accept | crc32 collision rate for typical action_key cardinality is negligible; UNIQUE race-fence on (subject_type, subject_id, event_name, channel, site_id) absorbs the rare collision (one of the two would lose the race). |
| T-03-05-04 | Information Disclosure | Site::getCurrent() in non-request context | accept | Returns null gracefully (Pitfall 6); contract invariant 04 asserts nullable. |
| T-03-05-SC | Tampering | composer | accept | Zero new packages. |
</threat_model>

<verification>
- `composer qa` + `composer deps` exit 0.
- `pest tests/Unit/Adapter/Theme/ tests/Contract/Adapter/Theme/ --compact` runs all 3 test files green.
- ThemeActionAdapterContractTest passes all 10 inherited invariants.
- phpstan.neon `disallowIn` lists no longer contain `classes/adapter/*` glob; explicit narrowing to `classes/adapter/shopaholic/*` only.
- `classes/adapter/theme/ThemeActionAdapter.php` calls `Site::getCurrent()` without phpstan complaint (deny-list excludes path).
- Static-source defence on shopaholic dir stays intact: `grep -rE 'Site::getCurrent|SiteManager::|Request::' classes/adapter/shopaholic/ classes/event/adapter/shopaholic/` returns NO matches.
</verification>

<success_criteria>
- THEM-01 closed: ThemeActionEvent value object built; iSyntheticId always positive int via sprintf('%u', crc32(...)) cast.
- THEM-02 closed: ThemeActionAdapter satisfies the 10-invariant contract; getSiteId reads payload first, falls back to Site::getCurrent gracefully.
- D-16 phpstan exclusion encoded: `classes/adapter/theme/*` is the SOLE path NOT in the disallow deny-list.
- composer qa + composer deps green; phpstan level 10 zero errors; coverage stays ≥ 90%.
</success_criteria>

<output>
Create `.planning/phases/03-shopaholicadapter-themeactionadapter-parallel-wave/03-05-SUMMARY.md` when done.
</output>
