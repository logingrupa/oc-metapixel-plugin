---
phase: 03-shopaholicadapter-themeactionadapter-parallel-wave
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - updates/AddPayloadToMetapixelEventLogTable.php
  - updates/version.yaml
  - models/EventLog.php
  - classes/helper/EventLogWriter.php
  - classes/queue/SendCapiEvent.php
  - console/PurgeEventLog.php
  - Plugin.php
  - phpunit.xml
  - tests/Feature/Migrations/AddPayloadColumnTest.php
  - tests/Feature/Console/PurgeEventLogTest.php
  - tests/Unit/Helper/EventLogWriterPayloadColumnTest.php
autonomous: true
requirements: []
must_haves:
  truths:
    - "EventLog rows carry the frozen Meta event payload at the moment of dispatch (longText NULL column)."
    - "EventLogWriter::record accepts a trailing array $arPayload and persists it to the new column."
    - "metapixel:purge-event-log artisan command exists and removes EventLog rows whose created_at is older than 7 days."
    - "Plugin::registerSchedule wires the purge command to a daily run via the host's schedule:run cron."
    - "Phase 2 SendCapiEvent::handle passes $this->arPayload to EventLogWriter::record without breakage; all Phase 2 tests stay green."
  artifacts:
    - path: "updates/AddPayloadToMetapixelEventLogTable.php"
      provides: "Additive migration adding payload longText NULL"
      contains: "class AddPayloadToMetapixelEventLogTable"
    - path: "console/PurgeEventLog.php"
      provides: "Daily TTL purge command"
      contains: "metapixel:purge-event-log"
    - path: "classes/helper/EventLogWriter.php"
      provides: "Race-fence writer with payload column write"
      contains: "array \\$arPayload"
    - path: "Plugin.php"
      provides: "registerSchedule + registerConsoleCommand wiring"
      contains: "registerSchedule"
  key_links:
    - from: "classes/queue/SendCapiEvent.php"
      to: "classes/helper/EventLogWriter.php"
      via: "EventLogWriter::record(...,\\$this->arPayload)"
      pattern: "EventLogWriter::record\\("
    - from: "Plugin.php"
      to: "console/PurgeEventLog.php"
      via: "registerConsoleCommand + registerSchedule"
      pattern: "PurgeEventLog::class"
---

<objective>
Lay the Phase 3 foundation that every subsequent plan consumes: the EventLog `payload` longText column (D-06), the EventLogWriter trailing `array $arPayload` parameter (D-07), and the daily 7-day TTL purge command + scheduler wire-up (D-08). Every Phase 2 caller of `EventLogWriter::record` is updated in lockstep so the regression surface stays at zero.

Purpose: Subsequent plans 03-02 through 03-08 dispatch real events; they all rely on the frozen-payload audit row being written at dispatch + the 7-day TTL purge keeping the table bounded. Without this foundation, EventPixel cannot render from a server-confirmed row (D-09 read path), the test fixtures pile up unbounded, and the Plugin.php boot path lacks the registerSchedule + registerConsoleCommand surface that 03-07 + 03-08 extend.
Output: Migration + writer signature update + console command + Plugin.php scheduling wire-up + 3 new test files covering the migration / writer / purge behaviour. composer qa stays green end-to-end.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-shopaholicadapter-themeactionadapter-parallel-wave/03-CONTEXT.md
@.planning/phases/03-shopaholicadapter-themeactionadapter-parallel-wave/03-RESEARCH.md
@updates/CreateMetapixelEventLogTable.php
@classes/helper/EventLogWriter.php
@classes/queue/SendCapiEvent.php
@models/EventLog.php
@models/FailedEvent.php
@Plugin.php
@phpunit.xml
@CLAUDE.md
@../../CLAUDE.md

<interfaces>
<!-- Phase 2 anchors the executor must mirror -->

Existing race-fence writer signature (classes/helper/EventLogWriter.php:23-31):
```
EventLogWriter::record(
    string $sEventId,
    string $sEventName,
    string $sChannel,
    object $obSubject,
    ?string $sSecretKey,
    int $iEventTime,
    ?int $iSiteId,
): bool
```

Phase 3 D-07 new signature (trailing param):
```
EventLogWriter::record(
    string $sEventId,
    string $sEventName,
    string $sChannel,
    object $obSubject,
    ?string $sSecretKey,
    int $iEventTime,
    ?int $iSiteId,
    array $arPayload,
): bool
```

Existing Phase 2 call sites (must be updated in lockstep):
- classes/queue/SendCapiEvent.php:105-113 — `EventLogWriter::record($this->readEventId(), $this->sEventName, 'capi', $this->obSubject, $obAdapter->getSecretKey($this->obSubject), $this->readEventTime(), $iSiteId)` — append `$this->arPayload`.

October-side reference (live `registerSchedule` example):
plugins/renatio/formbuilder/Plugin.php — `$schedule->command('model:prune', [...])->daily();`

October idiom recap (plugin CLAUDE.md "Model property convention"):
- EventLog will store `payload` as JSON-encoded longText. Add `protected $jsonable = ['payload']` to EventLog model. DO NOT add `'array'` cast. EventLog stays without Validation trait (no user-input boundary; helpers validate).
- New file basenames PascalCase (H-5 lock): `AddPayloadToMetapixelEventLogTable.php`, `PurgeEventLog.php`.
- Lowercase folders + PascalCase namespace (D-25).
- Hungarian notation for local vars (`$sCutoff`, `$iDeleted`, `$obTable`). Eloquent schema props (`$table`, `$fillable`, `$jsonable`, `$casts`) keep Laravel-standard names per CLAUDE.md "Model property convention".

EventLog Phase 2 schema (updates/CreateMetapixelEventLogTable.php):
- Columns: id, event_id (36), event_name (64), channel (16), subject_type (255), subject_id (unsigned int), secret_key (64 NULL), site_id (unsigned int NULL), event_time (unsigned bigint), fired_at, created_at, updated_at.
- UNIQUE on (subject_type, subject_id, event_name, channel, site_id) — race-fence anchor; do NOT touch.
- 3 indexes — do NOT touch.

Phase 2 callsites count check (anchored by ripgrep):
- `EventLogWriter::record(` should appear at exactly ONE production call site (SendCapiEvent::handle line ~105). The other matches are in tests under `tests/Feature/EventLogWriterRaceFenceTest.php` + similar — they MUST also be updated to pass an empty `[]` trailing arg (or a real payload where they assert payload persistence).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add EventLog payload column via additive migration + register $jsonable on the model</name>
  <files>updates/AddPayloadToMetapixelEventLogTable.php, updates/version.yaml, models/EventLog.php</files>
  <read_first>
    - updates/CreateMetapixelEventLogTable.php (Phase 2 base — DO NOT amend; see column list + UNIQUE index name)
    - models/EventLog.php (current state — adds `$jsonable = ['payload']` next to `$fillable` + `$casts`)
    - updates/version.yaml (October version log)
    - models/FailedEvent.php (pattern reference for `$jsonable` usage on longText)
    - CLAUDE.md plugin-local §"Model property convention" (jsonable vs `'array'` cast lock)
  </read_first>
  <action>
Create NEW migration class `Logingrupa\Metapixel\Updates\AddPayloadToMetapixelEventLogTable` at `updates/AddPayloadToMetapixelEventLogTable.php` extending `October\Rain\Database\Updates\Migration`. PascalCase basename matches FQN (H-5 lock from Phase 2 plan 02-03a). Table constant `TABLE = 'logingrupa_metapixel_event_log'`. `up()` calls `Schema::table(self::TABLE, fn(Blueprint $obTable) => $obTable->longText('payload')->nullable()->after('event_time'));` guarded by `Schema::hasColumn(self::TABLE, 'payload')` returning early if the column already exists (re-run safe per D-06 "Idempotent"). `down()` is symmetric: guarded `dropColumn('payload')`. Do NOT touch the UNIQUE index or any other column.

Append the migration filename to `updates/version.yaml` under a new version key `1.0.1` (or next sequential key after the last Phase 2 entry — read the file first to pick the correct key) with comment-style description per October convention.

Update `models/EventLog.php`:
- Add `'payload'` to the `$fillable` array (alphabetically or appended after `'fired_at'`).
- Add `protected $jsonable = ['payload'];` declared right after `$fillable` (mirror `FailedEvent::$jsonable = ['payload']` placement). DO NOT cast to `'array'` in `$casts`.
- Do NOT add `Validation` trait (internal append-only model per plugin CLAUDE.md — no user-input boundary).
- Do NOT change class final-ness; keep class non-final (Phase 2 lock — Eloquent + October scaffolding rely on inheritance).
  </action>
  <verify>
    <automated>cd /home/forge/nailscosmetics.lv/plugins/logingrupa/metapixel && grep -q "class AddPayloadToMetapixelEventLogTable" updates/AddPayloadToMetapixelEventLogTable.php && grep -q "longText('payload')" updates/AddPayloadToMetapixelEventLogTable.php && grep -q "jsonable = \['payload'\]" models/EventLog.php && grep -q "AddPayloadToMetapixelEventLogTable.php" updates/version.yaml && /home/forge/nailscosmetics.lv/vendor/bin/phpstan analyse --configuration=phpstan.neon --no-progress 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - File `updates/AddPayloadToMetapixelEventLogTable.php` exists and `grep -q "longText('payload')"` matches.
    - File `models/EventLog.php` contains literal substring `jsonable = ['payload']`.
    - File `updates/version.yaml` references `AddPayloadToMetapixelEventLogTable.php`.
    - File `models/EventLog.php` does NOT contain literal substring `'array'` inside the `$casts` array for the payload key (assert via `! grep -q "'payload' *=> *'array'" models/EventLog.php`).
    - `phpstan analyse` exits 0 on the new files (level 10, phpVersion 80300).
    - `up()` body contains `Schema::hasColumn(self::TABLE, 'payload')` early-return guard for idempotency.
  </acceptance_criteria>
  <done>Additive migration creates a `payload` longText NULL column on `logingrupa_metapixel_event_log` after `event_time`; running the migration twice is a no-op; EventLog Eloquent reads decode payload as array via `$jsonable`; phpstan green.</done>
</task>

<task type="auto">
  <name>Task 2: Add trailing array $arPayload to EventLogWriter::record + update SendCapiEvent + all Phase 2 callsites in lockstep</name>
  <files>classes/helper/EventLogWriter.php, classes/queue/SendCapiEvent.php, tests/Feature/EventLogWriterRaceFenceTest.php, tests/Feature/Adapter/EventLogWriterRaceFenceTest.php, tests/Unit/Helper/EventLogWriterPayloadColumnTest.php</files>
  <read_first>
    - classes/helper/EventLogWriter.php (entire body — Phase 2 signature anchors)
    - classes/queue/SendCapiEvent.php (lines 105-115 — the only production caller)
    - tests/Feature/Adapter/EventLogWriterRaceFenceTest.php (all current EventLogWriter::record callsites in tests; pattern for test fixtures)
    - tests/Feature/Adapter/BackboneIntegrationTest.php (Phase 2 integration template — references EventLogWriter indirectly via SendCapiEvent::handle)
    - models/EventLog.php (post Task 1 — now has $jsonable=['payload'])
  </read_first>
  <action>
Modify `classes/helper/EventLogWriter.php`:
- Append new parameter `array $arPayload` to the end of the `record(...)` method signature (after `?int $iSiteId`). PHPDoc adds `@param array<string, mixed> $arPayload`.
- Inside the `DB::table('logingrupa_metapixel_event_log')->insertOrIgnore([...])` array literal, add a new key `'payload' => $arPayload !== [] ? json_encode($arPayload, JSON_UNESCAPED_SLASHES|JSON_UNESCAPED_UNICODE) : null,` placed after `'event_time' => $iEventTime,` and before `'fired_at' => $sNow,` (positional readability — matches column order from Task 1).
- Local `json_encode` failure → cast to empty string (`?: null`). Do NOT throw — the writer is fail-safe (Phase 2 lock).
- Do NOT change any other behaviour: subject_type alias resolution via adapter, positive subject_id guard, Log::warning paths, outer try/catch return-false fail-safe — all stay verbatim.

Modify `classes/queue/SendCapiEvent.php`:
- Append `$this->arPayload` as the 8th argument to the `EventLogWriter::record(...)` call at lines 105-113. Keep argument list multi-line.

Update Phase 2 test fixtures — every `EventLogWriter::record(...)` call site in `tests/**/*.php` (find via `grep -rln "EventLogWriter::record(" tests/`) gets a trailing `[]` (empty array) added to its argument list. Where a test asserts payload persistence, pass a realistic single-event envelope shape `['data' => [['event_name' => 'Purchase']]]`.

Create NEW unit test `tests/Unit/Helper/EventLogWriterPayloadColumnTest.php` extending `Logingrupa\Metapixel\Tests\MetapixelTestCase`. Test class declares `protected $autoMigrate = true;` so Phase 2 + Phase 3 migrations run. The test uses a fake/dummy subject with an in-test AdapterRegistry binding (mirror the pattern in `tests/Feature/Adapter/EventLogWriterRaceFenceTest.php`'s setUp). Three cases:
  - `test_record_persists_payload_json_encoded_when_non_empty` — call `EventLogWriter::record(..., ['data'=>[['event_name'=>'Purchase','event_id'=>'x']]])`, assert DB row `payload` column is the literal JSON string `{"data":[{"event_name":"Purchase","event_id":"x"}]}` (use `DB::table(...)->where('event_id','x')->value('payload')`).
  - `test_record_persists_null_payload_when_empty_array` — call with `[]`, assert DB row `payload` is SQL NULL (use `assertNull(DB::table(...)->value('payload'))`).
  - `test_record_returns_true_when_payload_persisted` — record returns `true` (race-fence won) regardless of payload contents.
  </action>
  <verify>
    <automated>cd /home/forge/nailscosmetics.lv/plugins/logingrupa/metapixel && grep -E "array \\\$arPayload" classes/helper/EventLogWriter.php && grep -E "'payload' => " classes/helper/EventLogWriter.php && grep -E "\\\$this->arPayload" classes/queue/SendCapiEvent.php && /home/forge/nailscosmetics.lv/vendor/bin/pest --compact tests/Unit/Helper/EventLogWriterPayloadColumnTest.php 2>&1 | tail -5 && /home/forge/nailscosmetics.lv/vendor/bin/pest --compact tests/Feature/Adapter/EventLogWriterRaceFenceTest.php tests/Feature/Adapter/BackboneIntegrationTest.php 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - File `classes/helper/EventLogWriter.php` signature contains the exact substring `array $arPayload,` (8th parameter, trailing).
    - File `classes/helper/EventLogWriter.php` body contains `'payload' => $arPayload !== []` (writer encodes only non-empty payload).
    - File `classes/queue/SendCapiEvent.php` `EventLogWriter::record(` call site contains `$this->arPayload` as the 8th argument.
    - `pest tests/Unit/Helper/EventLogWriterPayloadColumnTest.php --compact` exits 0 (3 cases pass).
    - `pest tests/Feature/Adapter/EventLogWriterRaceFenceTest.php tests/Feature/Adapter/BackboneIntegrationTest.php --compact` exits 0 (Phase 2 fixtures regreen with updated signature).
    - PHPStan level 10 reports zero new errors on the touched files.
    - No Phase 2 test file is left with the 7-arg invocation pattern: `! grep -rn "EventLogWriter::record(" tests/ | grep -v "arPayload" | grep -v "\[\]" | head -1` returns no orphans.
  </acceptance_criteria>
  <done>EventLogWriter persists the frozen payload (or NULL when empty); SendCapiEvent::handle pipes its `$arPayload` through; all Phase 2 callsites pass the 8th arg; new payload-column unit test asserts persistence + NULL semantics; Phase 2 race-fence + backbone integration tests stay green.</done>
</task>

<task type="auto">
  <name>Task 3: Add PurgeEventLog console command + Plugin::registerSchedule daily wire-up + phpunit.xml console coverage + scheduled-job tests</name>
  <files>console/PurgeEventLog.php, Plugin.php, phpunit.xml, tests/Feature/Console/PurgeEventLogTest.php, tests/Feature/Migrations/AddPayloadColumnTest.php</files>
  <read_first>
    - Plugin.php (current register() body — already binds AdapterRegistry singleton; will gain registerConsoleCommand + registerSchedule + register the new singleton later in 03-06)
    - phpunit.xml (current `<source><include>` shape; needs new `<directory>./console</directory>` entry)
    - tests/Feature/Adapter/BackboneIntegrationTest.php (test setUp pattern — `protected $autoMigrate = true;`, MetapixelTestCase extension, Carbon::setTestNow usage if any)
    - updates/AddPayloadToMetapixelEventLogTable.php (post Task 1 — migration ordering check)
    - classes/queue/SendCapiEvent.php (post Task 2 — shape of the writer call now informing test expectations)
    - .planning/phases/03-shopaholicadapter-themeactionadapter-parallel-wave/03-RESEARCH.md §"Example 5" (PurgeEventLog reference shape — ≤60 LOC)
  </read_first>
  <action>
Create NEW console command `Logingrupa\Metapixel\Console\PurgeEventLog` at `console/PurgeEventLog.php`. Extends `Illuminate\Console\Command`. `final` class. `$signature = 'metapixel:purge-event-log'`. `$description = 'Delete EventLog rows older than 7 days (Phase 3 TTL purge)'`. `handle(): int`:
- Compute `$sCutoff = (string) Carbon::now()->subDays(7);` (use `Illuminate\Support\Carbon`).
- `$iDeleted = DB::table('logingrupa_metapixel_event_log')->where('created_at', '<', $sCutoff)->delete();`
- `Log::info('metapixel: purge-event-log', ['meta_pixel.rows_deleted' => $iDeleted, 'meta_pixel.cutoff' => $sCutoff]);`
- `$this->info(sprintf('Purged %d EventLog rows older than %s', $iDeleted, $sCutoff));`
- `return self::SUCCESS;`
- Body ≤ 30 LOC excluding PHPDoc + use statements.
- Use FQN facades via `Illuminate\Support\Facades\DB` + `Illuminate\Support\Facades\Log` (L-4 imports pattern from Phase 2).

Modify `Plugin.php`:
- Add `use Illuminate\Console\Scheduling\Schedule;`
- Add `use Logingrupa\Metapixel\Console\PurgeEventLog;`
- Extend `register(): void` to register the console command via `$this->registerConsoleCommand('metapixel:purge-event-log', PurgeEventLog::class);` AFTER the existing `$this->app->singleton(AdapterRegistry::class);` line.
- Add NEW method `public function registerSchedule(Schedule $obSchedule): void { $obSchedule->command('metapixel:purge-event-log')->daily(); }` placed right after the existing `boot()` method. Type hint is explicit per D-27.
- Extend `PluginSanityTest` (or create one if not present at `tests/Unit/PluginSanityTest.php` — check first) so the new `registerSchedule` method is exercised (coverage gate carrier pattern from Phase 2 plan 02-03b). If a PluginSanityTest already exists, add a test method `test_register_schedule_wires_purge_command_daily` that instantiates Plugin via the H-8 setUp pattern, calls registerSchedule with a fresh `Schedule` instance from the container, and asserts the schedule's events array contains a command with `command:metapixel:purge-event-log` substring + frequency `0 0 * * *`.

Modify `phpunit.xml`:
- Inside `<source><include>` add `<directory>./console</directory>` immediately after `<directory>./models</directory>`.
- Do NOT touch the `<exclude>` block.
- Do NOT add a `<testsuite>` entry — `tests/Feature/Console/` is picked up by the existing `<directory>./tests/Feature</directory>` testsuite.

Create NEW feature test `tests/Feature/Console/PurgeEventLogTest.php` extending `Logingrupa\Metapixel\Tests\MetapixelTestCase`. `protected $autoMigrate = true;`. Two test methods:
- `test_purge_deletes_rows_older_than_seven_days_keeps_newer`: Use `Illuminate\Support\Carbon::setTestNow(Carbon::parse('2026-05-20 12:00:00'));` then directly DB::table-insert two EventLog rows: one with `created_at = '2026-05-12 11:59:59'` (>7 days old, should be deleted) and one with `created_at = '2026-05-13 12:00:01'` (<7 days, should survive). Run `Artisan::call('metapixel:purge-event-log')` (use `Illuminate\Support\Facades\Artisan`). Assert `DB::table('logingrupa_metapixel_event_log')->count() === 1` AND the surviving row matches the new one. Carbon::setTestNow(null) in tearDown.
- `test_purge_returns_zero_when_no_rows_exist`: Empty table → `Artisan::call('metapixel:purge-event-log')` returns 0 (Command::SUCCESS); table count stays 0.

Create NEW feature test `tests/Feature/Migrations/AddPayloadColumnTest.php` extending `Logingrupa\Metapixel\Tests\MetapixelTestCase`. `protected $autoMigrate = true;`. Three test methods:
- `test_payload_column_exists_after_migration`: Assert `Schema::hasColumn('logingrupa_metapixel_event_log', 'payload')` is true.
- `test_payload_column_is_nullable_longtext`: Assert via `DB::select("PRAGMA table_info(logingrupa_metapixel_event_log)")` that the `payload` row's `type` matches `/text/i` and `notnull = 0`. (SQLite-specific introspection is acceptable; production MySQL is asserted via the migration body inspection in Task 1.)
- `test_migration_up_is_idempotent_when_column_already_present`: Manually call the migration's `up()` a second time → no Schema exception thrown. Use `(new AddPayloadToMetapixelEventLogTable)->up()` directly.
  </action>
  <verify>
    <automated>cd /home/forge/nailscosmetics.lv/plugins/logingrupa/metapixel && grep -q "metapixel:purge-event-log" console/PurgeEventLog.php && grep -q "registerSchedule" Plugin.php && grep -q "<directory>./console</directory>" phpunit.xml && /home/forge/nailscosmetics.lv/vendor/bin/pest --compact tests/Feature/Console/PurgeEventLogTest.php tests/Feature/Migrations/AddPayloadColumnTest.php 2>&1 | tail -5 && php /home/forge/nailscosmetics.lv/artisan list 2>&1 | grep -q "metapixel:purge-event-log"</automated>
  </verify>
  <acceptance_criteria>
    - File `console/PurgeEventLog.php` exists; class is `final`; contains literal substring `metapixel:purge-event-log` in `$signature`.
    - File `Plugin.php` contains the exact method signature `public function registerSchedule(Schedule $obSchedule): void` and body invokes `$obSchedule->command('metapixel:purge-event-log')->daily();`.
    - File `Plugin.php` `register()` body contains `$this->registerConsoleCommand('metapixel:purge-event-log', PurgeEventLog::class);`.
    - File `phpunit.xml` `<source><include>` block contains `<directory>./console</directory>`.
    - `pest tests/Feature/Console/PurgeEventLogTest.php tests/Feature/Migrations/AddPayloadColumnTest.php --compact` exits 0 (5 cases pass — 2 console + 3 migration).
    - `php /home/forge/nailscosmetics.lv/artisan list | grep metapixel:purge-event-log` finds the command (host-vendor binary; smoke proof the registration wired through).
    - `console/PurgeEventLog.php` total file LOC ≤ 60 (`wc -l console/PurgeEventLog.php` returns ≤ 60).
  </acceptance_criteria>
  <done>The artisan command `metapixel:purge-event-log` is registered + wired daily via `Plugin::registerSchedule`; running the command deletes EventLog rows older than 7 days deterministically (Carbon::setTestNow assertion); migration test confirms column existence + nullable + idempotent up().</done>
</task>

<task type="auto">
  <name>Task 4: composer qa green — pint format, full phpstan + pest run, commit</name>
  <files>(QA gate — no production code changes; may apply Pint reformatting in this task only)</files>
  <read_first>
    - composer.json (composer qa script chain)
    - All files modified in Tasks 1-3
  </read_first>
  <action>
Run `vendor/bin/pint --dirty --format agent` from the plugin dir to auto-format any Pint-style violations introduced by Tasks 1-3. Then run the full `composer qa` chain (pint-test → phpstan analyse → phpmd → pest --coverage --min=90). Fix any reported violations. Do NOT use `@phpstan-ignore` to silence findings — extract a runtime-guard helper per Phase 2 lock (D-28). On green, atomic-commit all Plan 03-01 files with message `feat(03-01): EventLog payload column + writer sig change + PurgeEventLog daily TTL`.
  </action>
  <verify>
    <automated>cd /home/forge/nailscosmetics.lv/plugins/logingrupa/metapixel && composer qa 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `composer qa` exits 0 end-to-end on the full-Lovata CI cell (pint-test green, phpstan level 10 green, phpmd green, pest coverage ≥90%).
    - No `@phpstan-ignore` comment introduced anywhere (`! grep -rn "@phpstan-ignore" classes/ models/ Plugin.php console/`).
    - Pest run reports zero failures + Phase 2 + Phase 3 test counts both green.
    - Single atomic commit `feat(03-01): EventLog payload column + writer sig change + PurgeEventLog daily TTL` on master with `git status` clean after.
  </acceptance_criteria>
  <done>composer qa exits 0; Phase 3 foundation merged; subsequent plans 03-02..03-08 can rely on the EventLog payload column + the EventLogWriter D-07 signature.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → artisan command | `metapixel:purge-event-log` is invoked by `schedule:run` (CLI) — no untrusted input. |
| Eloquent → SQLite/MySQL | EventLog table writes go through parameterized DB::table inserts. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01-01 | Tampering | EventLog.payload column | accept | Payload is server-built (PayloadBuilder); no untrusted input path to this column in Phase 3 (Theme path lands Plan 03-07 + writes through SendCapiEvent which already runs PayloadBuilder). |
| T-03-01-02 | Denial of Service | EventLog unbounded growth | mitigate | 7-day TTL purge daily (this plan) — table bounded by `weekly_event_rate * 7`. |
| T-03-01-03 | Information Disclosure | EventLog.payload contains hashed user_data | accept | user_data is sha256-hashed by UserDataHasher (Phase 2); no PII in cleartext. |
| T-03-01-SC | Tampering | composer install / vendor | accept | Phase 3 ships ZERO new packages (RESEARCH.md §"Package Legitimacy Audit" — N/A). No supply chain surface. |
</threat_model>

<verification>
- `composer qa` exits 0 (full chain).
- Migration is idempotent: running `php artisan october:migrate` twice never throws (proven by the Task 3 idempotency test + `Schema::hasColumn` guard).
- `php artisan metapixel:purge-event-log` returns 0 and deletes only rows where `created_at < now() - 7 days` (proven by Carbon::setTestNow test fixture in Task 3).
- All Phase 2 callers of `EventLogWriter::record` updated in lockstep — Phase 2 tests stay green (`pest tests/Feature/Adapter/EventLogWriterRaceFenceTest.php tests/Feature/Adapter/BackboneIntegrationTest.php --compact` exits 0).
</verification>

<success_criteria>
- The Phase 3 EventLog `payload` longText NULL column exists and stores the frozen Meta payload at the moment of dispatch.
- `EventLogWriter::record` accepts trailing `array $arPayload` and persists it (D-07).
- `metapixel:purge-event-log` is registered + scheduled daily and removes EventLog rows older than 7 days (D-08).
- `composer qa` exits 0 on the full-Lovata CI cell with zero phpstan errors, zero pint diffs, ≥90% coverage.
- No `@phpstan-ignore` comments anywhere in the plugin tree.
- No Phase 2 caller of `EventLogWriter::record` is left at the old 7-arg signature (grep gate in Task 2 acceptance).
</success_criteria>

<output>
Create `.planning/phases/03-shopaholicadapter-themeactionadapter-parallel-wave/03-01-SUMMARY.md` when done.
</output>
