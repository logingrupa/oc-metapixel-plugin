---
phase: 03-shopaholicadapter-themeactionadapter-parallel-wave
plan: 10
type: execute
wave: 9
depends_on: [03-07]
gap_closure: true
gap_refs:
  - "VERIFICATION.md::Gap 3 — Settings::beforeSave stores PHP array — textarea coerces to 'Array'"
  - "VERIFICATION.md::Gap 4 — six settings.fields lang keys missing from lang/en/lang.php"
requirements: [THEM-05, THEM-07]
files_modified:
  - models/Settings.php
  - lang/en/lang.php
  - tests/Unit/Models/SettingsBeforeSaveTest.php
autonomous: true
must_haves:
  truths:
    - "Settings::beforeSave writes theme_custom_event_names to the model as a newline-joined STRING (implode(\"\\n\", $arClean)), NOT a PHP array; the October textarea widget can display, edit, and round-trip the stored value without coercing to the literal string 'Array' on the next save."
    - "Settings::getThemeCustomEventNames() reads the stored string back and splits on newlines (explode + trim + regex filter) — returns list<string> matching the pre-fix contract. Idempotent re-save preserves the operator's exact entries."
    - "lang/en/lang.php declares all six previously-missing keys under the `settings.fields` namespace: paid_status_code_label, paid_status_code_comment, default_currency_code_label, default_currency_code_comment, theme_custom_event_names_label, theme_custom_event_names_comment. Strings are clear human-readable English (NOT placeholder lorem)."
    - "The Settings backend page renders human-readable labels for the three new fields (paid_status_code, default_currency_code, theme_custom_event_names) instead of raw dotted lang key strings. Operators can configure the plugin via the admin UI."
    - "A new test case proves the round-trip: persist a list of three event names via Settings::set, save the model, read back via Settings::getThemeCustomEventNames — assert exact list equality. A second test case proves the six lang keys resolve to non-key strings via Lang::get()."
  artifacts:
    - path: "models/Settings.php"
      provides: "beforeSave persists theme_custom_event_names as newline-joined string; getThemeCustomEventNames parses string back to list<string>"
      contains: "implode(\"\\n\""
    - path: "lang/en/lang.php"
      provides: "Six new settings.fields keys with English placeholder values"
      contains: "paid_status_code_label"
    - path: "tests/Unit/Models/SettingsBeforeSaveTest.php"
      provides: "Round-trip regression test (Gap 3) + lang key resolution test (Gap 4)"
      contains: "test_theme_custom_event_names_round_trips_through_textarea"
  key_links:
    - from: "models/Settings.php"
      to: "Eloquent attribute theme_custom_event_names"
      via: "setAttribute(implode(\"\\n\", $arClean))"
      pattern: "implode"
    - from: "models/Settings.php"
      to: "static getThemeCustomEventNames() return"
      via: "explode + array_map(trim) + regex filter"
      pattern: "explode"
    - from: "lang/en/lang.php"
      to: "models/settings/fields.yaml field labels + commentAbove keys"
      via: "logingrupa.metapixel::lang.settings.fields.{paid_status_code,default_currency_code,theme_custom_event_names}_{label,comment}"
      pattern: "paid_status_code_label"
---

<objective>
Close VERIFICATION.md Gap 3 + Gap 4: patch `Settings::beforeSave` to persist `theme_custom_event_names` as a newline-joined STRING (instead of a PHP array that October's textarea coerces to the literal "Array" on re-save), update `Settings::getThemeCustomEventNames` to parse the string back into `list<string>`, and add the six missing `settings.fields` lang keys to `lang/en/lang.php` so the Settings backend page renders human-readable labels instead of raw dotted key strings.

Purpose: Two production blockers prevent the plugin from being configurable in practice. THEM-05 + THEM-07 are blocked because (a) operators cannot persist their custom event names — every save wipes the list — and (b) the Settings UI shows raw `logingrupa.metapixel::lang.settings.fields.paid_status_code_label`-style strings instead of "Paid status code". Plan 03-10 narrows on the two fixes without scope creep — no replan of 03-01..03-08, no new features, no operator-facing copy beyond the necessary English placeholder strings.
Output: One implode/explode swap in `Settings::beforeSave` + `Settings::getThemeCustomEventNames`, six new lang keys in `lang/en/lang.php`, two new test cases (round-trip + lang resolution), composer qa green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-shopaholicadapter-themeactionadapter-parallel-wave/03-CONTEXT.md
@.planning/phases/03-shopaholicadapter-themeactionadapter-parallel-wave/03-RESEARCH.md
@.planning/phases/03-shopaholicadapter-themeactionadapter-parallel-wave/03-VERIFICATION.md
@.planning/phases/03-shopaholicadapter-themeactionadapter-parallel-wave/03-07-SUMMARY.md
@models/Settings.php
@models/settings/fields.yaml
@lang/en/lang.php
@tests/Unit/Models/SettingsBeforeSaveTest.php
@tests/MetapixelTestCase.php
@CLAUDE.md
@../../CLAUDE.md

<interfaces>
<!-- The exact current state + the exact target state -->

Current Settings::beforeSave (models/Settings.php lines 52-70) — BROKEN per Gap 3:
- `setAttribute('theme_custom_event_names', $arClean)` stores a PHP `list<string>`. October's textarea widget then calls `strval($mValue)` on render → emits the literal "Array" string → operator saves → all entries wiped.

Current Settings::getThemeCustomEventNames (models/Settings.php lines 122-136) — works for the array shape:
- `$mList = self::get('theme_custom_event_names', []);` + `is_array($mList)` check + foreach filter → returns `list<string>`.
- POST-fix: input shape becomes a STRING. The getter must explode on newlines (or any \R), trim each entry, regex-filter via the same `/^[A-Za-z0-9_]{1,50}$/`. Defensive: tolerate both string AND array shapes during the data-migration window (in case a pre-fix stored array survives in the JSON value column — split() returns []) — but the canonical persisted shape after this plan is string.

Target Settings::beforeSave body (replace the existing setAttribute call):
```
$this->setAttribute('theme_custom_event_names', implode("\n", $arClean));
```

Target Settings::getThemeCustomEventNames body:
```
public static function getThemeCustomEventNames(): array
{
    $mList = self::get('theme_custom_event_names', '');

    // Tolerate legacy array shape (pre-Gap-3 fix) for one-shot data migration.
    if (is_array($mList)) {
        $arCandidates = $mList;
    } elseif (is_string($mList)) {
        $arParts = preg_split('/\R/', $mList);
        $arCandidates = $arParts === false ? [] : $arParts;
    } else {
        return [];
    }

    $arResult = [];
    foreach ($arCandidates as $mItem) {
        $sTrim = is_string($mItem) ? trim($mItem) : '';
        if ($sTrim !== '' && preg_match('/^[A-Za-z0-9_]{1,50}$/', $sTrim) === 1) {
            $arResult[] = $sTrim;
        }
    }

    return $arResult;
}
```

Current lang/en/lang.php structure (lines 1-21):
- `settings.fields` namespace has 6 keys: pixel_id_label, pixel_id_comment, capi_access_token_label, capi_access_token_comment, test_event_code_label, test_event_code_comment.
- MISSING: paid_status_code_label, paid_status_code_comment, default_currency_code_label, default_currency_code_comment, theme_custom_event_names_label, theme_custom_event_names_comment.

Current models/settings/fields.yaml (lines 17-36) — verified the three field entries already reference the missing keys correctly. The fix is purely on the lang side.

Test base for SettingsBeforeSaveTest — Plan 03-07 SUMMARY documents alias-mock pattern on Flash facade. The new test cases reuse the same pattern. MetapixelTestCase auto-applies via Pest binding.

October Lang resolution contract:
- `Lang::get('logingrupa.metapixel::lang.settings.fields.paid_status_code_label')` returns the resolved string when the key exists.
- When the key is MISSING, Lang::get returns the key itself ("logingrupa.metapixel::lang.settings.fields.paid_status_code_label" as-is) — this is October's documented fallback behavior. The new test asserts `Lang::get(...) !== '<the key string>'` for each of the six new keys.

JSON encoding note (ExpandoModel storage path B — locked by Plan 03-07 SUMMARY):
- SettingModel::beforeSave writes through standard Eloquent attribute access; ExpandoModel's `expandoBeforeSaveDone` priority -1 listener packs `$this->attributes` into the `value` JSON column. A string attribute survives the JSON encode/decode round-trip cleanly. An array attribute also survives (json_encode encodes it as a JSON array) — but the October textarea widget reads the attribute via `getAttribute` BEFORE the JSON pack/unpack and renders it directly into the HTML textarea via `strval` → "Array". Storing the string upstream of the textarea widget bypasses the coercion.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Swap Settings::beforeSave to persist newline-joined string + update getThemeCustomEventNames to parse string back</name>
  <files>models/Settings.php</files>
  <read_first>
    - models/Settings.php (current state — lines 52-70 store PHP array; lines 122-136 read array)
    - .planning/phases/03-shopaholicadapter-themeactionadapter-parallel-wave/03-VERIFICATION.md (Gap 3 — full description: "October textarea coerces array to literal string 'Array'; operator saves -> all custom event names are lost")
    - .planning/phases/03-shopaholicadapter-themeactionadapter-parallel-wave/03-07-SUMMARY.md (Pattern 16: ExpandoModel storage path B via setAttribute / getAttribute — the SAVE-boundary contract this fix preserves)
    - models/settings/fields.yaml (theme_custom_event_names is type:textarea — confirms textarea is the rendering widget)
  </read_first>
  <action>
Modify `models/Settings.php`:

1. Update `beforeSave(): void` body. Replace the existing `setAttribute('theme_custom_event_names', $arClean)` call (around line 65) with the newline-joined string form:

   ```
   $this->setAttribute('theme_custom_event_names', implode("\n", $arClean));
   ```

   Keep everything else in beforeSave verbatim — the `splitEventNameInput` + `partitionEventNames` helpers stay (they correctly normalize both string AND array input shapes, which is the right defensive surface for textarea coercion edge cases).

2. Update `getThemeCustomEventNames(): array` body. Replace the entire method body with the string-aware parser that ALSO tolerates legacy array shapes (one-shot defensive surface for pre-fix rows that survived in the JSON column):

   ```
   /**
    * Sanitized list of operator-supplied custom event names. Always returns
    * list<string> with no malformed entries (beforeSave persists a
    * newline-joined string; this getter explodes + trims + regex-filters).
    * Tolerates legacy array shape (pre-Gap-3 fix) for one-shot data migration.
    *
    * @return list<string>
    */
   public static function getThemeCustomEventNames(): array
   {
       $mList = self::get('theme_custom_event_names', '');

       if (is_array($mList)) {
           $arCandidates = $mList;
       } elseif (is_string($mList)) {
           $arParts = preg_split('/\R/', $mList);
           $arCandidates = $arParts === false ? [] : $arParts;
       } else {
           return [];
       }

       $arResult = [];
       foreach ($arCandidates as $mItem) {
           $sTrim = is_string($mItem) ? trim($mItem) : '';
           if ($sTrim !== '' && preg_match('/^[A-Za-z0-9_]{1,50}$/', $sTrim) === 1) {
               $arResult[] = $sTrim;
           }
       }

       return $arResult;
   }
   ```

3. DO NOT change the class-level `@method static list<string> getThemeCustomEventNames()` docblock — the return type signature stays `list<string>` and that's already declared. DO NOT change `splitEventNameInput` or `partitionEventNames` — they correctly accept both string and array. DO NOT touch `lookupForSite`, `beforeSave` signature, or the existing comment about ExpandoModel storage path B.

4. PHPStan level 10 surface: the change from `setAttribute('field', list<string>)` to `setAttribute('field', string)` should not affect PHPStan since `setAttribute` is `mixed`-typed. The getter's added `is_string($mList)` branch is the canonical D-28-compliant runtime guard.

5. LOC budget: file currently 138 LOC; expected new total ≤ 145 LOC.
  </action>
  <verify>
    <automated>cd /home/forge/nailscosmetics.lv/plugins/logingrupa/metapixel && grep -q "implode(\"\\\\n\", \$arClean)" models/Settings.php && grep -q "preg_split('/.R/'" models/Settings.php && ! grep -q "setAttribute('theme_custom_event_names', \$arClean)" models/Settings.php && wc -l models/Settings.php | awk '{exit ($1<=150)?0:1}' && PATH=/home/forge/nailscosmetics.lv/vendor/bin:$PATH phpstan analyse --configuration=phpstan.neon --no-progress models/Settings.php 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - File `models/Settings.php` `beforeSave` body contains `implode("\n", $arClean)` (literal — note the escaped \n inside double quotes).
    - File `getThemeCustomEventNames` body contains `preg_split('/\R/'` (newline-regex split for textarea round-trip).
    - File `getThemeCustomEventNames` body STILL contains `/^[A-Za-z0-9_]{1,50}$/` (the validation regex is preserved — read-side defense is unchanged).
    - File `getThemeCustomEventNames` body contains both `is_array($mList)` AND `is_string($mList)` branches (legacy array shape tolerated; string is the canonical post-fix shape).
    - File NO LONGER contains the literal substring `setAttribute('theme_custom_event_names', $arClean)` — the array-store line is removed.
    - File total LOC ≤ 150.
    - `phpstan analyse models/Settings.php` exits 0 with no new errors (level 10, phpVersion 80300).
    - No `@phpstan-ignore` introduced (`! grep -q "@phpstan-ignore" models/Settings.php`).
  </acceptance_criteria>
  <done>Settings::beforeSave persists a newline-joined string for theme_custom_event_names; getThemeCustomEventNames parses the string back to list<string>; textarea round-trip is intact; legacy array shape is tolerated for one-shot migration.</done>
</task>

<task type="auto">
  <name>Task 2: Add six missing settings.fields lang keys to lang/en/lang.php</name>
  <files>lang/en/lang.php</files>
  <read_first>
    - lang/en/lang.php (current state — 22 lines; settings.fields has 6 keys; need to ADD 6 more)
    - models/settings/fields.yaml (lines 17-36 — verify the exact key references the fields use: `logingrupa.metapixel::lang.settings.fields.{paid_status_code,default_currency_code,theme_custom_event_names}_{label,comment}`)
    - .planning/phases/03-shopaholicadapter-themeactionadapter-parallel-wave/03-VERIFICATION.md (Gap 4 — full list of missing keys + scope: "English placeholder strings sufficient")
  </read_first>
  <action>
Modify `lang/en/lang.php`. Inside the existing `settings.fields` sub-array (lines 12-19, RIGHT AFTER `'test_event_code_comment'`), append the six new keys. Maintain the existing key-alignment (single-quote-string keys, `=>`-aligned values, trailing commas):

```
'paid_status_code_label'              => 'Paid status code',
'paid_status_code_comment'            => 'The Shopaholic order status that triggers a Purchase event (one value). Acquire status codes from Shopaholic > Settings > Order statuses. Default: new-payment-received.',
'default_currency_code_label'         => 'Default currency code',
'default_currency_code_comment'       => 'ISO 4217 currency code used as the fallback when an Order or CartPosition has no currency relation or currency_code field. Required by Meta CAPI for every event.',
'theme_custom_event_names_label'      => 'Custom theme event names',
'theme_custom_event_names_comment'    => 'Operator-supplied event names allowed by the theme AJAX handler. One per line. Alphanumeric + underscore, 1-50 chars. Standard Meta events (PageView, ViewContent, AddToCart, Purchase, Lead, ...) do not need to be listed here.',
```

The total new line count is 6 (one per key). DO NOT add a trailing newline beyond what already exists. DO NOT touch the `plugin` or `settings.label/description/category` blocks. DO NOT touch any other lang file (lv, no) — Gap 4 scope is en-only ("Optionally add matching keys to lang/lv/lang.php and lang/no/lang.php if those locales exist" — those locales do NOT exist in this plugin per repo grep; Phase 4 LANG-01 owns the broader translation rollout per CONTEXT.md "Deferred Ideas").

LOC budget: file currently 22 lines; expected new total ≤ 28 lines.

Format rule: match the existing keys' indent (8 spaces) and `=>` alignment style. October's `Lang::get` resolves the key regardless of indent, but PSR-12 + Pint enforce the visual style. If Pint reformats the file in Task 4, that is acceptable — the contract is the keys exist and resolve to non-key strings.
  </action>
  <verify>
    <automated>cd /home/forge/nailscosmetics.lv/plugins/logingrupa/metapixel && grep -q "'paid_status_code_label'" lang/en/lang.php && grep -q "'paid_status_code_comment'" lang/en/lang.php && grep -q "'default_currency_code_label'" lang/en/lang.php && grep -q "'default_currency_code_comment'" lang/en/lang.php && grep -q "'theme_custom_event_names_label'" lang/en/lang.php && grep -q "'theme_custom_event_names_comment'" lang/en/lang.php && wc -l lang/en/lang.php | awk '{exit ($1<=32)?0:1}' && php -l lang/en/lang.php 2>&1 | tail -2</automated>
  </verify>
  <acceptance_criteria>
    - File `lang/en/lang.php` contains all six new key strings (paid_status_code_label, paid_status_code_comment, default_currency_code_label, default_currency_code_comment, theme_custom_event_names_label, theme_custom_event_names_comment).
    - Each value is a non-empty English string (NOT the same as the key — no `'paid_status_code_label' => 'paid_status_code_label'` placeholder shortcuts).
    - `php -l lang/en/lang.php` exits 0 (no syntax error introduced).
    - File total LOC ≤ 32.
    - The existing six lang keys (pixel_id_*, capi_access_token_*, test_event_code_*) are UNCHANGED — `grep -c "_label" lang/en/lang.php` returns 6 (3 existing + 3 new) and `grep -c "_comment" lang/en/lang.php` returns 6 (3 existing + 3 new), total 12 keys under settings.fields.
    - No other lang file touched (lv/no do not exist per scope rules; Phase 4 LANG-01 owns rollout).
  </acceptance_criteria>
  <done>Six missing settings.fields lang keys present in lang/en/lang.php; values are clear human-readable English strings; PHP file syntactically valid.</done>
</task>

<task type="auto">
  <name>Task 3: Add round-trip regression test (Gap 3) + lang key resolution test (Gap 4)</name>
  <files>tests/Unit/Models/SettingsBeforeSaveTest.php</files>
  <read_first>
    - tests/Unit/Models/SettingsBeforeSaveTest.php (current state — Plan 03-07 SUMMARY documents 5 cases using alias-mock on Flash facade; extend with 2 NEW cases)
    - .planning/phases/03-shopaholicadapter-themeactionadapter-parallel-wave/03-07-SUMMARY.md (Pattern 16 + Mockery alias hygiene + the test file's existing structure)
    - models/Settings.php (POST Task 1 — new implode/explode shape)
    - lang/en/lang.php (POST Task 2 — six new keys present)
    - tests/MetapixelTestCase.php (base — auto-applies via Pest binding for `tests/Unit/Models/*Test.php`)
  </read_first>
  <action>
Append TWO new test methods (or Pest closure tests — match the existing file's style) to `tests/Unit/Models/SettingsBeforeSaveTest.php`:

**Test 1 — Round-trip regression (Gap 3 anchor):**

```
test('theme_custom_event_names round trips through textarea without coercing to Array', function () {
    // Pre-Gap-3 fix: setAttribute(array) → textarea renders 'Array' → re-save wipes the list.
    // Post-fix: setAttribute(implode("\n", array)) → textarea renders "Name1\nName2\nName3" → re-save preserves.

    $obSettings = new Settings;

    // Operator types in three event names (textarea raw input is a single string with \n separators).
    $obSettings->setAttribute('theme_custom_event_names', "FirstEvent\nSecondEvent\nThirdEvent");

    // beforeSave runs at save() time; invoke directly for hermetic test (no DB write needed).
    $obSettings->beforeSave();

    // Post-beforeSave: the attribute MUST be a string (NOT an array). Assert via getAttribute.
    $mStored = $obSettings->getAttribute('theme_custom_event_names');
    expect($mStored)->toBeString(); // <-- the critical Gap-3 fix: string NOT array

    // Round-trip: the stored string parses back to the original three event names.
    // Use Settings::get + Settings::getThemeCustomEventNames via a fake/in-memory mock — the simplest
    // hermetic surface is to write the stored value into a temporary key + read it back via the getter.
    // (If the test base already binds Settings::set to in-memory storage, use that; otherwise the
    // assertion on getAttribute above is the Gap-3 regression anchor — the string round-trip is then
    // verifiable via direct preg_split on $mStored.)
    expect(preg_split('/\R/', $mStored))->toBe(['FirstEvent', 'SecondEvent', 'ThirdEvent']);

    // Negative assertion (the Gap-3 regression guard): the stored value MUST NOT be the PHP-array shape
    // OR the literal string 'Array' that the textarea widget would emit for an array attribute.
    expect($mStored)->not->toBe('Array');
    expect(is_array($mStored))->toBeFalse();
});
```

**Test 2 — Lang key resolution (Gap 4 anchor):**

```
test('settings.fields lang keys resolve to human-readable strings (not raw key fallback)', function () {
    // October's Lang::get returns the key as-is when the key is missing. Asserting that the resolved
    // string DIFFERS from the key proves the key resolves successfully.

    $arKeys = [
        'logingrupa.metapixel::lang.settings.fields.paid_status_code_label',
        'logingrupa.metapixel::lang.settings.fields.paid_status_code_comment',
        'logingrupa.metapixel::lang.settings.fields.default_currency_code_label',
        'logingrupa.metapixel::lang.settings.fields.default_currency_code_comment',
        'logingrupa.metapixel::lang.settings.fields.theme_custom_event_names_label',
        'logingrupa.metapixel::lang.settings.fields.theme_custom_event_names_comment',
    ];

    foreach ($arKeys as $sKey) {
        $sValue = trans($sKey); // (or Lang::get($sKey) — match the test base convention)
        expect($sValue)->not->toBe($sKey);   // <-- key DID resolve (didn't fall back to itself)
        expect(strlen($sValue))->toBeGreaterThan(0); // <-- value is non-empty
        // Pre-Gap-4 fix: $sValue === $sKey (October fallback for missing key) — this would fail.
        // Post-fix: $sValue is the English string from lang/en/lang.php.
    }
});
```

Match the existing file's style:
- If the existing 5 cases use Pest closure tests (`test('...', function () { ... });`), append the new cases in the same closure-style.
- If the existing 5 cases are class methods (`public function test_xxx(): void`), append as class methods.
- Check via `grep -E "^(test|public function test)" tests/Unit/Models/SettingsBeforeSaveTest.php` and follow the dominant pattern.

Imports needed at the file top (add to existing imports):
- `use Logingrupa\Metapixel\Models\Settings;` (likely already present)
- `use Illuminate\Support\Facades\Lang;` OR `trans()` helper directly (preferred — terser, same behaviour as Lang::get).

The two new tests do NOT need Mockery alias for Flash (Gap 3 fix path does NOT call Flash::warning for the round-trip happy path — no malformed entries means no warning; Gap 4 just resolves lang keys, no Flash interaction).

Class-level `#[\PHPUnit\Framework\Attributes\Group('adapter')]` — check if the existing file declares this; if YES, the new tests inherit. If NO, do not add (the SettingsBeforeSaveTest may be in the minimal-install cell per Plan 03-07 — preserving its current group tagging).
  </action>
  <verify>
    <automated>cd /home/forge/nailscosmetics.lv/plugins/logingrupa/metapixel && grep -q "round trips through textarea" tests/Unit/Models/SettingsBeforeSaveTest.php && grep -q "settings.fields lang keys resolve" tests/Unit/Models/SettingsBeforeSaveTest.php && grep -q "paid_status_code_label" tests/Unit/Models/SettingsBeforeSaveTest.php && PATH=/home/forge/nailscosmetics.lv/vendor/bin:$PATH pest --compact tests/Unit/Models/SettingsBeforeSaveTest.php 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/Unit/Models/SettingsBeforeSaveTest.php` contains TWO new test entries: one with name/identifier referencing "round trips" (Gap 3) and one referencing "lang keys resolve" (Gap 4).
    - The round-trip test asserts: (a) `$mStored` is a string (`->toBeString()` or `is_string` equivalent); (b) the string parses back via `preg_split('/\R/', ...)` to the original three event names; (c) negative assertion that the stored value is NOT a PHP array AND NOT the literal "Array".
    - The lang test iterates all six new keys and asserts each `trans($sKey) !== $sKey` (key resolved, did not fall back to itself).
    - All 5 existing test cases still pass (Plan 03-07 SUMMARY documents them: valid-keep, malformed-drop, oversize-drop, empty-silent, Flash::warning-with-dropped-list).
    - `pest --compact tests/Unit/Models/SettingsBeforeSaveTest.php` exits 0 — total 7 cases pass (5 existing + 2 new).
    - No new business-logic mocks (no Mockery beyond what the existing 5 cases use; the new 2 cases are pure-Eloquent + pure-Lang assertions).
    - File's existing Mockery `tearDown` (or `afterEach`) hygiene is preserved — new tests do not leak Mockery state.
  </acceptance_criteria>
  <done>Two regression tests prove (a) theme_custom_event_names persists as string and round-trips correctly through the textarea storage path; (b) all six new lang keys resolve to non-key human-readable strings. Both tests live in the existing SettingsBeforeSaveTest file; no new test file.</done>
</task>

<task type="auto">
  <name>Task 4: composer qa green + atomic commit</name>
  <files>(QA gate — verifies Tasks 1-3 land cleanly)</files>
  <read_first>
    - composer.json (qa script chain)
    - All files modified in Tasks 1-3
  </read_first>
  <action>
Run `PATH=/home/forge/nailscosmetics.lv/vendor/bin:$PATH vendor/bin/pint --dirty --format agent` from the plugin dir to auto-format the touched files (Pint may reformat lang/en/lang.php alignment — that's expected and harmless). Then run the full `PATH=/home/forge/nailscosmetics.lv/vendor/bin:$PATH composer qa` chain (pint-test → phpstan level 10 → phpmd → pest --coverage --min=90). Fix any reported violations without `@phpstan-ignore` (D-28 lock) and without `assert()` (D-28 lock).

If phpmd reports a CyclomaticComplexity false-positive on `getThemeCustomEventNames` (same pattern as Plan 03-07 saw on `beforeSave`), apply the same fix: extract a small `partitionCandidates(array $arCandidates): array` helper that holds the foreach + regex filter, leaving `getThemeCustomEventNames` as a thin dispatcher between the array/string branch and the helper. The fix is mechanical, not behavioral. Same precedent as Plan 03-07 Deviation #3 (phpmd 2.15 multi-path cross-attribution).

On green, ONE atomic commit covering all three files:

`fix(03-10): persist theme_custom_event_names as newline-string + add settings lang keys (Gap 3 + Gap 4)`

Verify minimal-install regression cell stays green:
`PATH=/home/forge/nailscosmetics.lv/vendor/bin:$PATH pest --exclude-group=adapter --compact 2>&1 | tail -5` — count must match the pre-plan count (no orphaned tagged tests leaking; SettingsBeforeSaveTest stays in the minimal cell if it was previously, or in the adapter cell if it was previously — preserve).
  </action>
  <verify>
    <automated>cd /home/forge/nailscosmetics.lv/plugins/logingrupa/metapixel && PATH=/home/forge/nailscosmetics.lv/vendor/bin:$PATH composer qa 2>&1 | tail -20 && PATH=/home/forge/nailscosmetics.lv/vendor/bin:$PATH pest --exclude-group=adapter --compact 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `composer qa` exits 0 (pint-test + phpstan level 10 + phpmd + pest --coverage --min=90).
    - `! grep -rn "@phpstan-ignore" classes/ models/ Plugin.php tests/Unit/Models/` returns no matches.
    - `! grep -rn "assert(" models/ tests/Unit/Models/` returns no matches (production + new test code).
    - `pest --exclude-group=adapter` exit code 0 (minimal-install regression guard).
    - Single atomic commit with `git status` clean after.
  </acceptance_criteria>
  <done>THEM-05 + THEM-07 unblocked; Settings UI is operator-configurable; theme_custom_event_names persists across save cycles; composer qa + minimal-install cell both green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin operator → Settings textarea | Operator types arbitrary text into the theme_custom_event_names textarea; Settings::beforeSave sanitizes at the SAVE boundary (regex filter, malformed entries dropped, Flash::warning surfaces drops). |
| Settings storage → Settings::get reads | The persisted value lives in the `value` JSON column on logingrupa_metapixel_settings; Settings::getThemeCustomEventNames re-filters at the READ boundary (defense-in-depth, tolerates legacy array shape during one-shot migration window). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-10-01 | Information Disclosure | Settings textarea round-trip | mitigate | Persist as newline-joined string so the textarea widget renders the stored value back to the operator on re-edit (instead of the literal "Array" coercion); Pattern 17 two-zone defence preserved (SAVE-boundary regex filter + READ-boundary re-filter). |
| T-03-10-02 | Tampering | Legacy array-shaped rows surviving from pre-fix | mitigate | Settings::getThemeCustomEventNames tolerates both string AND array shapes; first read after fix returns list<string>; first save after fix persists the string shape going forward (one-shot data migration without an explicit migration file). |
| T-03-10-03 | Spoofing | Settings UI label rendering | accept | Six new lang keys carry plain English strings; no operator-supplied input goes through Lang::get; XSS surface is zero (lang strings are operator-visible, not operator-controlled). |
| T-03-10-SC | Tampering | composer install / vendor | accept | Plan ships ZERO new external packages. No supply chain surface. |
</threat_model>

<verification>
- `composer qa` exits 0 end-to-end (pint + phpstan + phpmd + pest --coverage --min=90).
- `pest tests/Unit/Models/SettingsBeforeSaveTest.php --compact` exits 0 — 7 cases pass (5 existing + 2 new).
- `pest --exclude-group=adapter --compact` exit code 0 — minimal-install regression count unchanged.
- `grep -q "implode(\"\\\\n\", \$arClean)" models/Settings.php` matches — beforeSave persists newline-joined string.
- `grep -q "preg_split('/.R/'" models/Settings.php` matches — getThemeCustomEventNames parses string back via newline regex.
- `grep -q "'paid_status_code_label'" lang/en/lang.php` matches; same for the other five new keys.
- Static defense: `! grep -q "setAttribute('theme_custom_event_names', \$arClean)" models/Settings.php` — the buggy array-store line is removed.
</verification>

<success_criteria>
- VERIFICATION Gap 3 closed: Settings::beforeSave persists theme_custom_event_names as a newline-joined string; the October textarea widget round-trips the value through save/re-edit/save without coercing to "Array".
- VERIFICATION Gap 4 closed: lang/en/lang.php declares all six previously-missing settings.fields keys; the Settings backend page renders human-readable English labels instead of raw dotted key strings.
- Two regression tests anchor the bug-was-fixed evidence: round-trip (Gap 3) + lang key resolution (Gap 4). Both live in the existing SettingsBeforeSaveTest file.
- composer qa exits 0; phpstan level 10 clean; no `@phpstan-ignore`; no `assert()`; minimal-install regression cell unchanged.
</success_criteria>

<output>
Create `.planning/phases/03-shopaholicadapter-themeactionadapter-parallel-wave/03-10-SUMMARY.md` when done.
</output>
