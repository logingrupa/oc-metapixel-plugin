---
phase: 05-documentation-marketplace-launch
plan: 22
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - tests/Feature/Docs/ReadmeStructureTest.php
  - .planning/launch/launch-02-PLAN.md
autonomous: true
gap_closure: true
requirements: [DOCS-01]

must_haves:
  truths:
    - "Every `october:up` occurrence in README.md (Install block, Settings-panel fallback note, quick-start step 4, Troubleshoot table) is replaced with `october:migrate` — the command that actually applies plugin migrations on October 4.3."
    - "ReadmeStructureTest asserts `php artisan october:migrate` (not the deprecated command) so the doc-gate stays green and pins the corrected instruction."
    - "README quick-start step 6 AND Theme walkthrough step 1 show the `[pixelHead]` INI component declaration in the layout config section alongside the `{% component 'pixelHead' %}` Twig tag — the Twig tag alone is a silent no-op."
    - "README Install section documents a pre-release install fallback (explicit `:dev-master` version constraint) that resolves on a fresh October 4.x root today, while keeping the primary `oc-metapixel-plugin -W` command that will resolve once the stable v2.0.0 tag exists."
    - "launch-02-PLAN.md carries a checklist item to re-run the README verbatim install command on a fresh October install immediately after the v2.0.0 tag push, and to drop the README pre-release fallback note once the stable command resolves."
  artifacts:
    - README.md
    - tests/Feature/Docs/ReadmeStructureTest.php
    - .planning/launch/launch-02-PLAN.md
  key_links:
    - from: "README install command (`oc-metapixel-plugin -W`)"
      to: "launch-02 verbatim-re-verify checklist item"
      via: "cross-reference note in README Install + launch-02 Step F"
      pattern: "(post-tag doc re-verification)"
---

<objective>
Close the one failed UAT gap (test 7 — timed clean-room README dry-run, SC1/DOCS-01) by fixing the three README-verbatim defects proven by the live clean-room run of 2026-07-03 in /home/forge/metapixel-test7 (October v4.3.1, SQLite, no cart plugin):

1. `php artisan october:up` is a deprecated no-op on October 4.3 — it prints a deprecation notice and applies zero plugin migrations; `october:migrate` applies them cleanly.
2. `{% component 'pixelHead' %}` mounted without the `[pixelHead]` INI component declaration silently renders nothing (HTTP 200, zero `fbq()`, no log signature).
3. The bare `composer require logingrupa/oc-metapixel-plugin -W` presumes an installable stable version, but the public remote has zero tags (v1.1.1 + v2.0.0-rc.1 are local-only) and a fresh October root defaults to `minimum-stability=stable`, so Composer refuses with "Could not find a version ... matching your minimum-stability (stable)".

The end-to-end pipeline itself is verified working once the workarounds are applied (Settings save clean, fbq init + PageView with server UUID eventID, CAPI twin logged, failed_events empty = Meta accepted). This plan corrects the docs so a buyer reaches that same working state following the README verbatim.

Purpose: DOCS-01 launch acceptance gate — a buyer following only the README completes install → Settings → first CAPI event without hitting a dead-ended command.
Output: corrected README.md, updated doc-gate test, launch-02 verbatim-re-verify checklist item.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-documentation-marketplace-launch/05-UAT.md
@README.md
@tests/Feature/Docs/ReadmeStructureTest.php
@.planning/launch/launch-02-PLAN.md

Test invocation for every `<verify>` below (plugin dir has no local vendor/; the qa toolchain lives in the host October app vendor per the 05-21 CI topology decision):

    cd /home/forge/nailscosmetics.lv/plugins/logingrupa/metapixel
    ../../../vendor/bin/pest --configuration phpunit.xml --filter=ReadmeStructure

Baseline: 8 tests / 21 assertions green before this plan.
</context>

<tasks>

<!-- planner-discipline-allow: october:up -->

<task type="auto">
  <name>Task 1: Replace the deprecated october:up command across README + pin the fix in the doc-gate test</name>
  <files>README.md, tests/Feature/Docs/ReadmeStructureTest.php</files>
  <action>
    October 4.3 deprecated the migrate-on-install command shown in the README to a no-op — it prints a deprecation notice and applies zero plugin migrations. Replace every occurrence of the deprecated `php artisan october:up` in README.md with `php artisan october:migrate` (proven to apply all 5 plugin migrations cleanly in the clean-room run). There are four occurrences to fix:
      - Install block "Then require the package and run the migrations" fenced command (~line 52).
      - Settings-panel fallback note "If Settings → Marketing → Meta Pixel + CAPI is not visible after install ..." (~line 57).
      - Quick-start "first event in 10 minutes" step 4 "Run the migrations" (~line 68).
      - Troubleshoot table "Event log writes fail" row fix cell (~line 179).

    Then update `tests/Feature/Docs/ReadmeStructureTest.php` so the doc-gate pins the corrected command instead of the deprecated one:
      - Rename `test_readme_install_block_shows_october_up` to `test_readme_install_block_shows_october_migrate`.
      - Change its `assertStringContainsString` needle from the deprecated command to `php artisan october:migrate`.
      - Update the failure message and the method docblock to reference `october:migrate` and the October 4.3 deprecation (drop the "Pitfall 5 october:up" wording).

    Do NOT introduce a negative assertion for the old command string inside the test file body — a test asserting the absence of a literal would embed that literal in the shipped test source; the positive `october:migrate` assertion plus this plan's grep gate are sufficient.

    Keep the commit atomic — this one concern only.
  </action>
  <verify>
    <automated>cd /home/forge/nailscosmetics.lv/plugins/logingrupa/metapixel && ../../../vendor/bin/pest --configuration phpunit.xml --filter=ReadmeStructure 2>&1 | tail -3 && echo "residual-deprecated-count:" && { grep -v '^#' README.md | grep -c 'october:up' || true; }</automated>
  </verify>
  <done>ReadmeStructure filter all green (8 tests), the renamed test asserts `october:migrate`, and residual-deprecated-count is 0 — no `october:up` remains in README body.</done>
</task>

<task type="auto">
  <name>Task 2: Show the required [pixelHead] INI declaration alongside the Twig mount</name>
  <files>README.md</files>
  <action>
    A theme layout in October has an INI config section, an optional PHP section, and the Twig markup section, separated by `==`. Mounting `{% component 'pixelHead' %}` in markup without declaring the component in the INI config section makes October render the tag as an empty string — HTTP 200, zero `fbq()`, no log signature (proven via the /pixeltest2 control page). The README currently shows only the Twig tag in two places; both must show the `[pixelHead]` INI declaration as well.

    Fix quick-start step 6 (~line 70). Replace the single-line "Mount the head Pixel by adding the `pixelHead` component to your layout: `{% component 'pixelHead' %}`" with an instruction that shows BOTH pieces: declare `[pixelHead]` in the layout's INI/config section AND place `{% component 'pixelHead' %}` in the layout markup. Note explicitly that the Twig tag alone renders nothing without the INI declaration.

    Fix Theme walkthrough step 1 (~lines 128-133). Replace the lone `{% component 'pixelHead' %}` twig fence with a fence that shows the full layout shape so the INI declaration and the markup tag are both visible, for example:

        ##
        description = "Default layout"

        [pixelHead]
        ==
        ...
        {% component 'pixelHead' %}

    Keep the surrounding prose about the base Pixel boot + `PageView` rendering in `<head>`. Add one sentence stating the `[pixelHead]` INI declaration is required — the Twig tag alone is a silent no-op.

    Keep the commit atomic — this one concern only.
  </action>
  <verify>
    <automated>cd /home/forge/nailscosmetics.lv/plugins/logingrupa/metapixel && echo "ini-decl-count:" && grep -c '\[pixelHead\]' README.md && ../../../vendor/bin/pest --configuration phpunit.xml --filter=ReadmeStructure 2>&1 | tail -3</automated>
  </verify>
  <done>`[pixelHead]` INI declaration appears at least twice in README (quick-start + Theme walkthrough), both alongside the existing Twig tag, and the ReadmeStructure filter stays green (8 tests).</done>
</task>

<task type="auto">
  <name>Task 3: Document the pre-release install fallback + cross-reference launch-02 for post-tag re-verification</name>
  <files>README.md, .planning/launch/launch-02-PLAN.md</files>
  <action>
    The bare `composer require logingrupa/oc-metapixel-plugin -W` fails on a fresh October 4.x root today: the public remote has zero tags, so Composer sees only `dev-master`, and a fresh October root composer.json defaults to `minimum-stability=stable` — Composer refuses with "Could not find a version ... matching your minimum-stability (stable)". Pushing the stable v2.0.0 tag is operator-gated (launch-02, LAUNCH SCHEDULED signal) and out of scope here — this task fixes the docs only.

    In README.md, keep the existing primary `composer require logingrupa/oc-metapixel-plugin -W` command intact (it resolves once the v2.0.0 tag lands, and the doc-gate test asserts the `oc-metapixel-plugin -W` substring). Immediately after the Install-block require command (~line 51) and after quick-start step 3 (~line 67), add a short "Pre-release install" note stating that until the stable v2.0.0 tag is published, a fresh install must pin the dev branch explicitly with `composer require logingrupa/oc-metapixel-plugin:dev-master -W` — the explicit `:dev-master` constraint opts that one package past the fresh-root `minimum-stability=stable` filter without loosening the whole project. State plainly that once v2.0.0 is tagged the plain `-W` command above works and the pre-release note can be ignored. Do NOT remove or weaken the `project:set` prerequisite or the `-W` explanation already present.

    In `.planning/launch/launch-02-PLAN.md`, add a checklist item so the README verbatim command is re-verified right after the tag push. Extend Task 3 Step F (the unauth-install smoke) with an explicit sub-step: after `git push origin v2.0.0`, run the README's exact primary command — `composer require logingrupa/oc-metapixel-plugin -W` — inside a fresh `composer create-project october/october "^4.0"` root (the same clean-room shape as test 7 / metapixel-test7) and confirm it resolves the stable `v2.0.0` tag with no `minimum-stability` error. Add a paired instruction: once that verbatim command resolves, delete the README "Pre-release install" note (it is only correct while the remote is tagless). Add a matching bullet to the plan's `<verification>` list. Reference this gap: closes UAT test 7 defect (1) — README install command unresolvable on a tagless remote.

    Keep the commit atomic — this one concern (install-command resolvability + its post-tag re-verification) only.
  </action>
  <verify>
    <automated>cd /home/forge/nailscosmetics.lv/plugins/logingrupa/metapixel && echo "prerelease-note:" && grep -c 'dev-master -W' README.md && echo "launch02-reverify:" && grep -ci 'oc-metapixel-plugin -W' .planning/launch/launch-02-PLAN.md && ../../../vendor/bin/pest --configuration phpunit.xml --filter=ReadmeStructure 2>&1 | tail -3</automated>
  </verify>
  <done>README documents the `:dev-master -W` pre-release fallback while retaining the primary `oc-metapixel-plugin -W` command; launch-02-PLAN.md Step F re-verifies the verbatim command after the tag push and mandates removing the README note once it resolves; ReadmeStructure filter stays green (8 tests).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| README install instructions → buyer's shell | Buyer copies commands verbatim onto a fresh October root; a wrong command dead-ends or silently no-ops the install |
| composer VCS require → public GitHub remote | Composer fetches `logingrupa/oc-metapixel-plugin` over HTTPS from the pinned GitHub URL |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-22-01 | Tampering | README `:dev-master` pre-release install note | low | mitigate | Note pins the exact package name + GitHub VCS URL already fixed in the Install block; existing "install the exact package name ... do not install a similarly named package" warning (README ~line 59) remains in force |
| T-05-22-02 | Denial of Service | buyer install dead-ends on tagless remote (defect 1) | medium | mitigate | Pre-release `:dev-master -W` fallback keeps a fresh install unblocked until the stable v2.0.0 tag lands; launch-02 re-verifies the verbatim stable command post-tag |
| T-05-22-03 | Tampering | composer VCS install fetched over HTTPS | low | accept | Composer rejects `http://` VCS by default; URL pinned to GitHub HTTPS (inherited from launch-02 T-05-04) |
</threat_model>

<verification>
- `../../../vendor/bin/pest --configuration phpunit.xml --filter=ReadmeStructure` green (8 tests) after every task.
- No `october:up` remains in README body; the doc-gate test pins `october:migrate`.
- `[pixelHead]` INI declaration present in quick-start + Theme walkthrough, alongside the Twig tag.
- README documents the `:dev-master -W` pre-release fallback; primary `oc-metapixel-plugin -W` command retained.
- launch-02-PLAN.md Step F re-verifies the verbatim install command after the tag push and mandates dropping the README pre-release note once it resolves.
</verification>

<success_criteria>
- A buyer following the README verbatim on a fresh October 4.x install runs commands that actually apply migrations (`october:migrate`), actually render the Pixel (`[pixelHead]` INI + Twig tag), and actually resolve the package (`:dev-master -W` pre-release fallback until the stable tag exists) — the three clean-room dead-ends from UAT test 7 are removed.
- Doc-gate `ReadmeStructureTest` stays green and now enforces the corrected `october:migrate` instruction.
- The verbatim stable install command is scheduled for re-verification in launch-02 immediately after the v2.0.0 tag push.
</success_criteria>

<output>
Create `.planning/phases/05-documentation-marketplace-launch/05-22-SUMMARY.md` when done — note each defect closed, the four `october:up` → `october:migrate` replacements, the two `[pixelHead]` INI declarations added, the pre-release fallback note, and the launch-02 cross-reference. After this plan, re-run UAT test 7 acceptance once the v2.0.0 tag is pushed (launch-02) to close the SC1/DOCS-01 stopwatch gate verbatim.
</output>
