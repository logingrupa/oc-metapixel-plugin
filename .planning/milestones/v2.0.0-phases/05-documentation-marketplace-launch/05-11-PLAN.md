---
phase: 5
plan: 11
plan_id: 05-11
type: execute
wave: 2
depends_on: [05-00]
files_modified:
  - Plugin.php
  - classes/queue/SendCapiEvent.php
  - classes/testing/EventSubjectAdapterContractTestCase.php
  - classes/adapter/shopaholic/ShopaholicCartPositionAdapter.php
  - classes/adapter/shopaholic/ShopaholicSettingsOptions.php
  - lang/en/lang.php
  - lang/lv/lang.php
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - tests/Feature/Docs/NoV1xReferencesTest.php
autonomous: true
threat_refs: []
requirements: [MKT-04]

# Wave assignment rationale (W-14 fix): plan 05-11 is placed in Wave 2 alongside plan 05-02
# (theme strip). The two plans touch disjoint file sets — 05-02 modifies
# themes/logingrupa-naisstore/*; 05-11 modifies plugin source docblocks + .planning/* + a
# new test file. Running 05-11 early (option a per W-14) keeps the post-smoke commit history
# minimal so plan 05-14 Pitfall 6 (wrong-SHA tag) is easier to avoid. The alternative —
# deferring to post-smoke Wave 7 — was rejected because it offered no parallelism benefit and
# pushed planning-doc rewrites closer to the irreversible tag operation.

must_haves:
  truths:
    - "All 13 in-code `Phase [0-9]` / RESEARCH-pitfall N / Plan N docblock decorators are stripped from 5 PHP files (Plugin.php, SendCapiEvent.php, EventSubjectAdapterContractTestCase.php, ShopaholicCartPositionAdapter.php, ShopaholicSettingsOptions.php). Behavior unchanged — strip is pure docblock/comment text per Tiger-Style."
    - "ROADMAP.md MKT-03 + MKT-04 wording rewritten per D-23 — no `v1.1.1` references, no `legacy/v1.1.1 branch preserved` phrasing"
    - "REQUIREMENTS.md MKT-03 + MKT-04 wording rewritten per D-23 — same"
    - "REQUIREMENTS.md `Out of Scope` table row 'Backward-compat migration from v1.x' rewritten to remove `legacy/v1.1.1` ref"
    - "lang/en/lang.php and lang/lv/lang.php verified clean (zero `v1\\.|legacy/v1|Phase [0-9]` hits — no edit expected, but explicit verification + the D-23 gate test enforces going forward)"
    - "D-23 gate test `tests/Feature/Docs/NoV1xReferencesTest.php` exists and GREEN across the public-shipped surface (Plugin.php, classes/, lang/)"
  artifacts:
    - path: "tests/Feature/Docs/NoV1xReferencesTest.php"
      provides: "D-23 gate — prevents future `Phase N` / `v1.x` / `legacy/v1` decorators from landing in public source"
    - path: "Plugin.php (stripped)"
      provides: "Strip site line 148 `Phase 3 D-08` and RESEARCH pitfall ref per PATTERNS lines 427-450"
    - path: "classes/queue/SendCapiEvent.php (stripped)"
      provides: "Strip sites line 45 + 250 `Phase 4 admin UI` per PATTERNS lines 459-467"
    - path: "classes/testing/EventSubjectAdapterContractTestCase.php (stripped)"
      provides: "Strip 4 sites at lines 14, 15, 34, 36 per PATTERNS lines 475-481"
    - path: "classes/adapter/shopaholic/ShopaholicCartPositionAdapter.php (stripped)"
      provides: "Strip line 70 `Phase 4 cookie middleware` per PATTERNS lines 487-491"
    - path: "classes/adapter/shopaholic/ShopaholicSettingsOptions.php (stripped)"
      provides: "Strip line 38 `Extend at Phase 4` per PATTERNS lines 496-500"
    - path: ".planning/ROADMAP.md (MKT-04 rewritten)"
      provides: "Public-surface phrasing per D-23"
    - path: ".planning/REQUIREMENTS.md (MKT-04 + MKT-03 + Out-of-Scope row rewritten)"
      provides: "Public-surface phrasing per D-23"
  key_links:
    - from: "NoV1xReferencesTest.php"
      to: "Plugin.php, classes/, lang/"
      via: "Recursive grep against in-plugin public surface"
      pattern: "Phase\\s*[0-9]|v1\\.|legacy/v1"
---

<objective>
Strip every public-surface reference to v1.x semantics from the plugin source + planning docs (D-23). Five PHP files have 13 docblock decorators (`(Phase N D-XX)`, `RESEARCH pitfall N`, etc.) that must come out. Two planning docs (`ROADMAP.md`, `REQUIREMENTS.md`) have MKT-03 + MKT-04 wording referencing `v1.1.1` / `legacy/v1.1.1 branch preserved` — rewritten per D-23 to "v2.0.0 annotated tag from master". A new D-23 gate test enforces zero `Phase [0-9]` / `v1\.` / `legacy/v1` hits going forward.

Purpose: Public-repo hygiene. After plan 05-13 flips the repo public, a reader finds no trace of v1.x. Behavior of stripped PHP files MUST remain unchanged — strip is pure docblock/comment text.

Output: 5 PHP files edited (docblock only), 2 planning docs rewritten, 1 new test file. lang/{en,lv}/lang.php verified clean. Single commit per Tiger-Style "small commits, one concern".
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-documentation-marketplace-launch/05-CONTEXT.md
@.planning/phases/05-documentation-marketplace-launch/05-RESEARCH.md
@.planning/phases/05-documentation-marketplace-launch/05-PATTERNS.md
@plugins/logingrupa/metapixel/Plugin.php
@plugins/logingrupa/metapixel/classes/queue/SendCapiEvent.php
@plugins/logingrupa/metapixel/classes/testing/EventSubjectAdapterContractTestCase.php
@plugins/logingrupa/metapixel/classes/adapter/shopaholic/ShopaholicCartPositionAdapter.php
@plugins/logingrupa/metapixel/classes/adapter/shopaholic/ShopaholicSettingsOptions.php
@plugins/logingrupa/metapixel/lang/en/lang.php
@plugins/logingrupa/metapixel/lang/lv/lang.php
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public repo → buyer | Public-shipped surface must not advertise v1.x internal history |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-11-01 | Information Disclosure | docblock decorators leak internal phase/CR/plan structure to public readers | mitigate | Strip + add NoV1xReferencesTest gate to prevent regression |
| T-05-11-02 | Tampering | strip accidentally removes load-bearing code | mitigate | Strip is pure docblock/comment text; composer qa runs full test suite after strip; behavior MUST stay GREEN |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Strip Phase N decorators from 5 PHP files (docblock-only edits)</name>
  <read_first>
    - .planning/phases/05-documentation-marketplace-launch/05-PATTERNS.md lines 420-501 (per-file strip table — exact line numbers + before/after wording)
    - plugins/logingrupa/metapixel/Plugin.php (line 148 + surrounding context)
    - plugins/logingrupa/metapixel/classes/queue/SendCapiEvent.php (line 45 class-level docblock + line 248-252 writeFailedEvent docblock)
    - plugins/logingrupa/metapixel/classes/testing/EventSubjectAdapterContractTestCase.php (lines 13-40)
    - plugins/logingrupa/metapixel/classes/adapter/shopaholic/ShopaholicCartPositionAdapter.php (line 70)
    - plugins/logingrupa/metapixel/classes/adapter/shopaholic/ShopaholicSettingsOptions.php (line 38)
  </read_first>
  <action>
    Apply the PATTERNS-locked strip table verbatim:

    `Plugin.php` line 148 area: change docblock for `registerSchedule` — remove the `(Phase 3 D-08)` parenthetical from the "Wire the daily TTL purge of EventLog rows older than 7 days" line; also remove the `RESEARCH pitfall 7` ref or rephrase as plain prose. Final wording per PATTERNS lines 442-450.

    `classes/queue/SendCapiEvent.php` two sites:
      - Line 45 class-level docblock: change `(enables Phase 4 admin UI re-resolution)` to `(enables admin UI re-resolution)`.
      - Line 248-252 writeFailedEvent docblock: change `Phase 4 admin UI can re-resolve` to `admin UI can re-resolve`.

    `classes/testing/EventSubjectAdapterContractTestCase.php` four sites in class-level docblock:
      - Line 14: `adapters (FakeAdapter in Phase 2, ShopaholicOrderAdapter + ThemeActionAdapter` → `adapters (FakeAdapter, ShopaholicOrderAdapter + ThemeActionAdapter`
      - Line 15: `in Phase 3) extend this base` → `extend this base`
      - Line 34: `the Phase 2 marketplace contract.` → `the marketplace contract.`
      - Line 36: `Extending MetapixelTestCase is a Phase 2 YAGNI choice — Phase 2 has exactly` → `Extending MetapixelTestCase is a YAGNI choice — the suite has exactly`; also rewrite the sentence ending `Revisit at v2.1` to `Revisit when first third-party adapter ships`.

    `classes/adapter/shopaholic/ShopaholicCartPositionAdapter.php` line 70: change `EventPixel render path; Phase 4 cookie middleware sets them at the` → `EventPixel render path; the cookie middleware sets them at the`.

    `classes/adapter/shopaholic/ShopaholicSettingsOptions.php` line 38: change `(NOK, EUR, USD, GBP). Extend at Phase 4 if operator demand surfaces.` → `(NOK, EUR, USD, GBP). Extend when operator demand surfaces.`.

    Tiger-Style invariant: code/behavior unchanged. Every existing test stays GREEN.
  </action>
  <verify>
    <automated>cd /home/forge/nailscosmetics.lv/plugins/logingrupa/metapixel && grep -rnE 'Phase\s+[0-9]' Plugin.php classes/ 2>/dev/null | wc -l | awk '$1 == 0 {print "ok"; exit 0} {print "fail: "$1" Phase N decorators remaining"; exit 1}' && vendor/bin/pest --filter='PluginSanity|SendCapiEventBranchCoverage|ShopaholicCartPosition' 2>&1 | tail -10</automated>
  </verify>
  <done>
    `grep -rnE 'Phase\s+[0-9]' Plugin.php classes/` returns 0 hits across the 5 stripped files. Existing test suite still GREEN.
  </done>
</task>

<task type="auto">
  <name>Task 2: Rewrite ROADMAP.md + REQUIREMENTS.md MKT-* wording per D-23 (W-9: explicit two-pass check)</name>
  <read_first>
    - .planning/ROADMAP.md (Phase 5 entry + Success Criterion 5 + MKT-04 wording)
    - .planning/REQUIREMENTS.md (lines 111-112 MKT-03 + MKT-04 + line 150 Out of Scope row + lines 11/14/27/43/45/47/55/65/76/85/91/97/101/107 Phase-N category headers)
    - .planning/phases/05-documentation-marketplace-launch/05-PATTERNS.md lines 615-625 (locked rewrite table)
  </read_first>
  <action>
    In `.planning/REQUIREMENTS.md`:
      - Line 111 MKT-03: change `CHANGELOG.md documenting v2.0.0 changes vs v1.x legacy branch.` to `CHANGELOG.md documenting the v2.0.0 initial public release.`
      - Line 112 MKT-04: change `Plugin git tag v2.0.0 annotated. Pushed to remote. v1.1.1 + legacy/v1.1.1 branch preserved (operator may stay on legacy indefinitely).` to `Plugin git tag v2.0.0 annotated. Pushed to remote.`
      - Line 150 Out-of-Scope row "Backward-compat migration from v1.x" → keep the row label but change the Reason column from `Operators stay on legacy/v1.1.1 branch indefinitely. Fresh installs only.` to `Fresh installs only.`
      - Lines 11/14/27/43/45/47/55/65/76/85/91/97/101/107 — category headers like `### Tooling (Phase 1 — composer.json + namespace rename + CI)`. Strip the `(Phase N — …)` parenthetical from each public header.

    In `.planning/ROADMAP.md`:
      - Phase 5 Success Criterion 5 currently says `legacy/v1.1.1 branch preserved on origin (operator may stay on legacy indefinitely — no BC shim, no upgrade migration in v2.0).` — change to `No BC shim; no upgrade migration in v2.0.`
      - Phase 5 Success Criterion 4 currently says `documenting v2.0.0 changes vs legacy/v1.1.1 branch.` — change to `documenting the v2.0.0 initial public release.`
      - Any other `legacy/v1.1.1` mentions in the Phase 5 entry — replace with phrasing that does not name v1.x.

    Do NOT touch `STATE.md` (plan 05-13 handles operator-infra refs separately). Do NOT touch milestones/v1.1.1-ROADMAP.md (D-24 archive).
  </action>
  <verify>
    <automated>cd /home/forge/nailscosmetics.lv && bash -c 'a=$(grep -c "v1\.1\.1" plugins/logingrupa/metapixel/.planning/REQUIREMENTS.md); b=$(grep -c "v1\.1\.1" plugins/logingrupa/metapixel/.planning/ROADMAP.md); c=$(grep -c "legacy/v1\.1\.1 branch preserved" plugins/logingrupa/metapixel/.planning/REQUIREMENTS.md plugins/logingrupa/metapixel/.planning/ROADMAP.md 2>/dev/null | awk -F: "{s+=\$2} END {print s+0}"); d=$(grep -cF "documenting v2.0.0 changes vs v1.x legacy branch" plugins/logingrupa/metapixel/.planning/REQUIREMENTS.md); [ "$a" = "0" ] && [ "$b" = "0" ] && [ "$c" = "0" ] && [ "$d" = "0" ] && echo "ok-all-clean" || echo "fail: REQ-v1.1.1=$a ROAD-v1.1.1=$b legacy-branch-preserved=$c MKT-03-old=$d"'</automated>
  </verify>
  <done>
    `.planning/REQUIREMENTS.md` contains zero `v1.1.1` substring AND `.planning/ROADMAP.md` contains zero `v1.1.1` substring AND the literal phrase `legacy/v1.1.1 branch preserved` appears in neither file AND the literal phrase `documenting v2.0.0 changes vs v1.x legacy branch` no longer appears in REQUIREMENTS.md MKT-03. Internal Shipped Milestones row referencing legacy/v1.1.1 (ROADMAP.md line 311 archive entry) may remain — it's the local archive pointer per D-24, not a public-shipping requirement string. Operator confirms whether to leave or sweep that single archive ref during plan 05-13 grep pass.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add tests/Feature/Docs/NoV1xReferencesTest.php D-23 gate + verify lang/ clean (W-8: bare grep)</name>
  <read_first>
    - plugins/logingrupa/metapixel/tests/Feature/Lang/LangKeyCoverageTest.php (test_no_ru_lang_file_shipped pattern — literal-anchor + assertFalse style)
    - plugins/logingrupa/metapixel/lang/en/lang.php (verify zero `v1\.|legacy/v1|Phase [0-9]` hits)
    - plugins/logingrupa/metapixel/lang/lv/lang.php (same)
    - .planning/phases/05-documentation-marketplace-launch/05-PATTERNS.md lines 402-417 (NoV1xReferencesTest pattern + assertion shape)
  </read_first>
  <action>
    Verify cleanly that `lang/en/lang.php` and `lang/lv/lang.php` contain zero `v1\.|legacy/v1|Phase [0-9]` hits. If grep finds any (none expected per RESEARCH), edit out in this task.

    Create `tests/Feature/Docs/NoV1xReferencesTest.php`. Header `<?php` + `use Logingrupa\Metapixel\Tests\MetapixelTestCase;`. `final class NoV1xReferencesTest extends MetapixelTestCase`. Tests:

      - `test_plugin_php_has_no_phase_n_decorators` — load Plugin.php, assertDoesNotMatchRegularExpression `/Phase\s+[0-9]/`
      - `test_classes_dir_has_no_phase_n_decorators` — recursive read of all `*.php` files under `classes/` (recursive directory iterator); for each file assert no `Phase\s+[0-9]` match
      - `test_lang_en_has_no_v1x_references` — load lang/en/lang.php; flatten leaf string values; assert no value contains `v1.` or `legacy/v1` substring
      - `test_lang_lv_has_no_v1x_references` — symmetric
      - `test_plugin_php_has_no_legacy_v1_references` — assertDoesNotMatchRegularExpression `/legacy\/v1/` against Plugin.php content

    All tests follow the hermetic file-load pattern from `LangKeyCoverageTest`.

    Commit message draft: `docs(05-11): strip v1.x decorators from source + planning + ship NoV1xReferencesTest gate (D-23)`.
  </action>
  <verify>
    <automated>cd /home/forge/nailscosmetics.lv/plugins/logingrupa/metapixel && vendor/bin/pest --filter='NoV1xReferencesTest' 2>&1 | tail -15 && grep -rnE 'Phase\s+[0-9]' Plugin.php classes/ 2>/dev/null | wc -l | awk '$1 == 0 {print "ok-zero-decorators"; exit 0} {print "fail: "$1; exit 1}'</automated>
  </verify>
  <done>
    `tests/Feature/Docs/NoV1xReferencesTest.php` exists; all 5 tests GREEN. lang/{en,lv}/lang.php verified clean. Any future PR adding a `Phase N` / `v1.x` / `legacy/v1` reference to public source files fails this gate. `composer qa` full chain exit 0.
  </done>
</task>

</tasks>

<verification>
- 5 PHP files docblock-stripped — behavior unchanged
- ROADMAP.md + REQUIREMENTS.md MKT-* wording rewritten per D-23 (verified by explicit AND-chain grep — Task 2 verify)
- `tests/Feature/Docs/NoV1xReferencesTest.php` exists and GREEN
- `composer qa` exit 0 — no behavior regression
</verification>

<success_criteria>
- 13 in-code `Phase N` decorators removed
- MKT-04 wording on public planning surface cleaned
- D-23 gate test enforces zero future v1.x decorators in public source
- Existing 430+ test suite remains GREEN
</success_criteria>

<output>
Create `.planning/phases/05-documentation-marketplace-launch/05-11-SUMMARY.md` when done — note count of stripped decorators, rewritten planning-doc lines, NoV1xReferencesTest status (GREEN), commit SHA.
</output>
</content>
</invoke>