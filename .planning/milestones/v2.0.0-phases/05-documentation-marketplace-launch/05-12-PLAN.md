---
phase: 5
plan: 12
plan_id: 05-12
type: execute
wave: 8
depends_on: [05-00, 05-08, 05-09, 05-10, 05-11]
files_modified:
  - CHANGELOG.md
  - plugin.yaml
  - composer.json
autonomous: true
threat_refs: [T-05-03]
requirements: [MKT-02, MKT-03]

must_haves:
  truths:
    - "CHANGELOG.md exists at plugin root with Keep-a-Changelog 1.1.0 format — single `## [2.0.0] - YYYY-MM-DD` section, `### Added` subsection enumerating deliverables, zero v1.x diff text (D-22)"
    - "plugin.yaml is verified MKT-02-ready — no edit expected unless verification fails. Fields: name + description = lang keys; author = 'Logingrupa'; icon = 'icon-bullseye' (D-20); homepage = 'https://github.com/logingrupa/oc-metapixel-plugin'"
    - "composer.json gets a `keywords` array (planner Discretion subset of `meta-pixel`, `conversions-api`, `capi`, `october-cms`, `october-plugin`, `shopaholic`, `tracking`, `analytics`)"
    - "composer.json `license` field re-evaluation deferred to plan 05-14 (operator decision)"
    - "5 PNG screenshots from plan 05-08 are verified present at plugin-relative path `docs/screenshots/0[1-5]-*.png` (resolved from plugin root — see B-4 callout) AND verified to show ONLY D-18 dummy values via visual review (operator confirms in summary)"
    - "Wave 0 tests AssetsExistTest + PluginYamlSanityTest flip RED → GREEN after this plan"
    - "Screenshot paths follow D-19: `plugins/logingrupa/metapixel/docs/screenshots/{01-settings,02-failed-events,03-replay,04-check-dedup,05-twig-api}.png`. README references via plugin-relative `docs/screenshots/`."
  artifacts:
    - path: "CHANGELOG.md"
      provides: "Fresh v2.0.0 initial release entry (D-22) — Keep-a-Changelog 1.1.0 format"
      contains: "## [2.0.0]"
      min_lines: 25
    - path: "plugin.yaml (verified)"
      provides: "MKT-02 sanity — no edit expected"
    - path: "composer.json (keywords added)"
      provides: "Marketplace discoverability"
      contains: "\"keywords\""
  key_links:
    - from: "CHANGELOG.md"
      to: "tests/Feature/Docs/AssetsExistTest.php"
      via: "AssetsExistTest flips to GREEN once CHANGELOG.md + 5 PNGs exist"
      pattern: "(test target)"
    - from: "plugin.yaml"
      to: "tests/Feature/Plugin/PluginYamlSanityTest.php"
      via: "Yaml::parseFile assertions"
      pattern: "(test target — should already be GREEN; this plan re-verifies)"
---

<objective>
Author `CHANGELOG.md` as a fresh v2.0.0 initial release entry per Keep-a-Changelog 1.1.0 (D-22 lock — ZERO v1.x diff text). Verify `plugin.yaml` is MKT-02-ready (no edit expected per RESEARCH). Add `keywords` array to `composer.json`. Visual-review the 5 PNG screenshots from plan 05-08 to confirm dummy-row strategy held. Flips Wave 0 `AssetsExistTest` from RED to GREEN.

**Screenshot path callout (B-4):** Per plan 05-08 specification, PNGs live at `plugins/logingrupa/metapixel/docs/screenshots/`. From plugin root (`cd plugins/logingrupa/metapixel`) the relative path is `docs/screenshots/`. Wave 0 `AssetsExistTest` resolves via `dirname(__DIR__, 3).'/docs/screenshots/...'` from `tests/Feature/Docs/`, which lands at plugin root. Operator references PNGs relative to plugin root throughout this plan (e.g. `docs/screenshots/01-settings.png`, NOT `/docs/screenshots/01-settings.png`).

Purpose: MKT-02 + MKT-03 deliverables. CHANGELOG + manifest + screenshots all ready for the public flip in plan 05-14.

Output: 1 new CHANGELOG.md, 1 modified composer.json, 0-1 edits to plugin.yaml. Wave 0 AssetsExistTest GREEN. Wave 0 PluginYamlSanityTest stays GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-documentation-marketplace-launch/05-CONTEXT.md
@.planning/phases/05-documentation-marketplace-launch/05-RESEARCH.md
@.planning/phases/05-documentation-marketplace-launch/05-PATTERNS.md
@.planning/phases/05-documentation-marketplace-launch/05-08-SUMMARY.md
@plugins/logingrupa/metapixel/plugin.yaml
@plugins/logingrupa/metapixel/composer.json
@plugins/logingrupa/metapixel/lang/en/lang.php
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| screenshot binary → public repo | PNG content visible to all post-flip viewers |
| plugin.yaml/composer.json → public | Manifest values readable in repo without auth |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03 | Information Disclosure | screenshot leaks real Pixel ID or token (Pitfall 3) | mitigate | Plan 05-12 Task 3 visual-review checklist of every PNG — operator confirms only `000000000000000` and `REDACTED_FOR_DEMO_DO_NOT_USE` visible; D-18 dummy-row already applied by plan 05-08; this plan re-verifies before commit |
| T-05-12-01 | Tampering | composer.json keywords typosquat surface | accept | All keywords are descriptive nouns; no install-name shadowing |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Author CHANGELOG.md per Keep-a-Changelog 1.1.0 + D-22</name>
  <read_first>
    - .planning/phases/05-documentation-marketplace-launch/05-RESEARCH.md lines 741-774 (Example 3 — verbatim CHANGELOG template with full Added subsection)
    - .planning/phases/05-documentation-marketplace-launch/05-PATTERNS.md lines 145-184 (CHANGELOG.md pattern + content)
    - tests/Feature/Docs/AssetsExistTest.php (Wave 0 assertions on CHANGELOG — section header regex, Added subsection present, no v1.x diff)
  </read_first>
  <action>
    Create `plugins/logingrupa/metapixel/CHANGELOG.md` at plugin root. Structure:

    ```
    # Changelog

    All notable changes to this project will be documented in this file.

    The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/),
    and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

    ## [2.0.0] - YYYY-MM-DD

    Initial public release.

    ### Added

    - <bullet for each deliverable, per RESEARCH Example 3 lines 755-771>

    [2.0.0]: https://github.com/logingrupa/oc-metapixel-plugin/releases/tag/v2.0.0
    ```

    Replace `YYYY-MM-DD` with actual date this plan executes (operator runs `date +%Y-%m-%d` at commit time). Format locked to ISO 8601 date.

    Body bullets (verbatim from RESEARCH lines 755-771):
      - Generic adapter pattern (EventSubjectAdapter + ValueResolver + AdapterRegistry)
      - First-party ShopaholicOrderAdapter with `SKU-{product_id}[-{offer_id}]` content_ids
      - First-party ThemeActionAdapter (Twig API + Larajax handler)
      - Three extension hooks (before_dispatch halt-able, after_dispatch observe, dead_letter alert)
      - Multisite-native Settings with $propagatable = []
      - TrustedHosts allowlist + PSL-aware multi-TLD subdomain index
      - EnsureFbpFbcCookies middleware with kill switch + fbclid validation
      - FailedEvents backend UI (list view, Replay, CheckDedup)
      - EventLog UNIQUE race-fence (peer-wins idempotency)
      - Graph API pinned to v23.0
      - English + Latvian translations
      - PHP 8.3 + 8.4 dual support
      - composer qa chain + CI matrix
      - EventSubjectAdapterContractTestCase 10-invariant marketplace contract
      - docs/CUSTOM-ADAPTERS.md authoring guide with AcmeCart register snippet + OFFLINE Mall inline example

    Hard constraints (Wave 0 enforces):
      - NO `v1.1.1` or `legacy/v1` text anywhere
      - Section header MUST match regex `^## \[2\.0\.0\] - \d{4}-\d{2}-\d{2}$`
      - `### Added` subsection MUST be present
  </action>
  <verify>
    <automated>cd /home/forge/nailscosmetics.lv/plugins/logingrupa/metapixel && test -f CHANGELOG.md && grep -qE '^## \[2\.0\.0\] - [0-9]{4}-[0-9]{2}-[0-9]{2}$' CHANGELOG.md && grep -q '^### Added' CHANGELOG.md && ! grep -qE 'v1\.1\.1|legacy/v1' CHANGELOG.md && echo ok</automated>
  </verify>
  <done>
    `CHANGELOG.md` exists at plugin root; matches Keep-a-Changelog header format; `### Added` subsection present with ≥14 bullets; no v1.x diff text.
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify plugin.yaml MKT-02 + add composer.json keywords array</name>
  <read_first>
    - plugins/logingrupa/metapixel/plugin.yaml (verify current state — name + description lang keys, author 'Logingrupa', icon 'icon-bullseye', homepage GitHub URL)
    - plugins/logingrupa/metapixel/composer.json (current state — license, description, require, suggest, extra)
    - tests/Feature/Plugin/PluginYamlSanityTest.php (Wave 0 assertions)
    - .planning/phases/05-documentation-marketplace-launch/05-PATTERNS.md lines 530-549 (composer.json keywords array — Discretion candidate list)
  </read_first>
  <action>
    Verify `plugin.yaml`. If `vendor/bin/pest --filter='PluginYamlSanityTest'` is already GREEN, no edit required. If any test fails, fix the offending YAML key.

    Edit `composer.json` to add a `keywords` array: `["october-cms", "october-plugin", "meta-pixel", "conversions-api", "capi", "shopaholic", "tracking", "analytics"]`. Insert after the existing `"description"` key. Preserve JSON formatting style.

    Do NOT change the `license` field here — D-25 + Open Question 2 defers to plan 05-14. Leave as `"proprietary"`.

    Verify `composer validate` exits 0 after the edit.
  </action>
  <verify>
    <automated>cd /home/forge/nailscosmetics.lv/plugins/logingrupa/metapixel && vendor/bin/pest --filter='PluginYamlSanityTest' 2>&1 | tail -5 && php -r '$j=json_decode(file_get_contents("composer.json"),true); echo isset($j["keywords"]) && count($j["keywords"])>=4 ? "ok-keywords-".count($j["keywords"]) : "fail-keywords"; echo PHP_EOL;'</automated>
  </verify>
  <done>
    PluginYamlSanityTest 6/6 GREEN. composer.json contains a `keywords` array with ≥4 entries. composer.json remains valid JSON.
  </done>
</task>

<task type="auto">
  <name>Task 3: Visual-review the 5 PNG screenshots from plan 05-08 (plugin-relative path) + run Wave 0 AssetsExistTest</name>
  <read_first>
    - .planning/phases/05-documentation-marketplace-launch/05-08-SUMMARY.md (which screenshots were captured + dummy-row transitions per step + Backend-UI-vs-SQL mechanism noted for Step PRE)
    - plugins/logingrupa/metapixel/docs/screenshots/01-settings.png (verify file exists + non-zero size + PNG magic byte)
    - plugins/logingrupa/metapixel/docs/screenshots/02-failed-events-list.png
    - plugins/logingrupa/metapixel/docs/screenshots/03-replay-flow.png
    - plugins/logingrupa/metapixel/docs/screenshots/04-check-dedup.png
    - plugins/logingrupa/metapixel/docs/screenshots/05-twig-api.png
    - tests/Feature/Docs/AssetsExistTest.php (Wave 0 assertions — uses `dirname(__DIR__, 3).'/docs/screenshots/...'` resolving to plugin root)
    - .planning/phases/05-documentation-marketplace-launch/05-RESEARCH.md lines 478-490 (Pitfall 3 — screenshot Pixel ID leak)
  </read_first>
  <action>
    Verify each screenshot file at plugin-relative path `docs/screenshots/0X-*.png` (resolved from the plugin root `plugins/logingrupa/metapixel/`): exists, is a valid PNG (`file plugins/logingrupa/metapixel/docs/screenshots/01-settings.png` reports "PNG image data"), is non-trivial size (> 5 KB).

    Operator visual-review checklist (executor LLM cannot inspect image content; operator confirms in summary):
      - `01-settings.png` shows Pixel ID = `000000000000000` AND access_token = `REDACTED_FOR_DEMO_DO_NOT_USE` (or visibly masked)
      - `02-failed-events-list.png` shows the FailedEvents list with at least one row; no real Pixel ID or token surfaces
      - `03-replay-flow.png` shows the Replay success flash; no leak
      - `04-check-dedup.png` shows the CheckDedup JSON response; no real token value
      - `05-twig-api.png` shows the theme Twig API source / browser devtools; no real Pixel ID

    If any screenshot leaks a real value, return to plan 05-08 Step PRE (set Settings to dummy via Backend UI) and re-capture before this plan commits.

    Run Wave 0 AssetsExistTest after CHANGELOG.md (Task 1) and screenshot review.
  </action>
  <verify>
    <automated>cd /home/forge/nailscosmetics.lv/plugins/logingrupa/metapixel && bash -c 'count=$(ls docs/screenshots/0[1-5]-*.png 2>/dev/null | wc -l); test "$count" = "5" && for f in docs/screenshots/0[1-5]-*.png; do file "$f" | grep -q "PNG image data" || { echo "fail-magic-$f"; exit 1; }; done && echo ok-5-pngs' && vendor/bin/pest --filter='AssetsExistTest' 2>&1 | tail -15</automated>
  </verify>
  <done>
    5 PNG files present at plugin-relative `docs/screenshots/0[1-5]-*.png`; each passes `file ... | grep PNG image data`; AssetsExistTest 5/5 GREEN. Operator-confirmed visual review documented in plan summary.
  </done>
</task>

<task type="auto">
  <name>Task 4: Commit CHANGELOG.md + composer.json keywords + screenshots as a single feature commit (W-10 strict && chain)</name>
  <read_first>
    - CHANGELOG.md (just authored — Task 1)
    - composer.json (just modified — Task 2)
    - docs/screenshots/0[1-5]-*.png (verified — Task 3)
  </read_first>
  <action>
    From the plugin root: `git add CHANGELOG.md composer.json docs/screenshots/0[1-5]-*.png`. If plugin.yaml needed an edit in Task 2, also stage it.

    Commit with message:
    ```
    docs(05-12): ship CHANGELOG.md + 5 marketplace screenshots + composer keywords (MKT-02 + MKT-03)
    ```

    Run `composer qa` once after commit to confirm full test suite is GREEN at this milestone. If it fails, investigate but do NOT amend — open a fix-plan per Tiger-Style.
  </action>
  <verify>
    <automated>cd /home/forge/nailscosmetics.lv/plugins/logingrupa/metapixel && git log --oneline -1 | grep -qE 'docs\(05-12\)' && [ "$(git status --porcelain CHANGELOG.md composer.json docs/screenshots/ 2>/dev/null | wc -l)" = "0" ] && echo ok-committed</automated>
  </verify>
  <done>
    Single commit covers CHANGELOG + screenshots + composer keywords. Working tree clean for all 05-12 paths. composer qa exit 0 confirmed.
  </done>
</task>

</tasks>

<verification>
- CHANGELOG.md present, matches Keep-a-Changelog 1.1.0 format
- plugin.yaml verified MKT-02-ready (PluginYamlSanityTest GREEN)
- composer.json gains keywords array (≥4 entries)
- 5 PNG screenshots present at plugin-relative `docs/screenshots/0[1-5]-*.png`
- Wave 0 AssetsExistTest GREEN
- Single feature commit; working tree clean (Task 4 strict-AND verify)
</verification>

<success_criteria>
- MKT-02 + MKT-03 deliverables shipped
- Wave 0 RED→GREEN for AssetsExistTest + PluginYamlSanityTest
- composer qa exit 0 at plan close
</success_criteria>

<output>
Create `.planning/phases/05-documentation-marketplace-launch/05-12-SUMMARY.md` when done — note CHANGELOG date stamped, composer.json keywords subset chosen, screenshot visual-review confirmation (operator-signed), commit SHA, Wave 0 test status (GREEN).
</output>
</content>
