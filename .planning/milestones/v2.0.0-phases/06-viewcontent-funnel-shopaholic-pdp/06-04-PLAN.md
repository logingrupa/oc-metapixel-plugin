---
phase: 06-viewcontent-funnel-shopaholic-pdp
plan: 04
type: execute
wave: 3
depends_on:
  - 06-01
  - 06-03
files_modified:
  - classes/adapter/shopaholic/ShopaholicProductAdapter.php
  - classes/adapter/shopaholic/ShopaholicProductValueResolver.php
  - phpstan.neon
  - tests/Feature/Adapter/Shopaholic/ShopaholicProductValueResolverTest.php
  - tests/Contract/Adapter/Shopaholic/ShopaholicProductAdapterContractTest.php
autonomous: true
requirements:
  - VIEW-02
  - VIEW-03
  - VIEW-08
must_haves:
  truths:
    - "ShopaholicProductAdapter implements SupportsHybridAjax (extends EventSubjectAdapter). Alias 'shopaholic.product'. getSiteId reads $obProduct->site_list pivot when single-site; falls back to Site::getSiteIdFromContext() for multi-site (D-15 documented exception, third occurrence after ThemeActionAdapter + ShopaholicCartPositionAdapter)."
    - "loadSubject(int, array) re-enforces Product::active()->find guards (active scope excludes inactive AND SoftDelete-trashed) plus site-relation match against $obProduct->site_list — T-6-05 mitigation prevents cross-site subject_id spoofing through hybrid AJAX."
    - "ShopaholicProductValueResolver implements ValueResolver. Default offer per D-10 = $obProduct->offer->where('active', true)->sortBy('sort_order')->first(). content_ids = ['SKU-{pid}-{oid}'] when offer count > 1, ['SKU-{pid}'] when count <= 1 or default offer null. value = raw price_value off default offer. currency chain = CurrencyHelper::instance()->getActiveCurrencyCode() → Settings::get('default_currency_code', '') → throw OrderHasNoCurrencyException."
    - "phpstan.neon allowIn extended on all FOUR deny-list rules (Site::*, SiteManager::*, Request::*, request()) to include ShopaholicProductAdapter.php — composer qa stays green after Task 1 + Task 3 land."
    - "ShopaholicProductValueResolverTest covers 7 GREEN behaviors: 4 SKU shapes via DataProvider, value reads, currency chain (active + fallback + throw), contents shape, numItems constant."
    - "ShopaholicProductAdapterContractTest extends EventSubjectAdapterContractTestCase and proves all 10 Phase 2 invariants hold for the Product adapter (mirrors ShopaholicOrderAdapterContractTest + ShopaholicCartPositionAdapterContractTest pattern); supplies makeAdapter() + makeSubject() + hermetic Product/Offer/Price + product-site-relation schema bootstrap."
    - "Composer-dependency-analyser stays clean — ShopaholicProductAdapter.php is the only new file importing Lovata\\Shopaholic\\Models\\Product, living under classes/adapter/shopaholic/* which is already in the Lovata-import allowlist (composer-dependency-analyser.php lines 31-34)."
  artifacts:
    - path: "classes/adapter/shopaholic/ShopaholicProductAdapter.php"
      provides: "EventSubjectAdapter + SupportsHybridAjax for Lovata\\Shopaholic\\Models\\Product; alias 'shopaholic.product'"
      contains: "final class ShopaholicProductAdapter implements SupportsHybridAjax"
    - path: "classes/adapter/shopaholic/ShopaholicProductValueResolver.php"
      provides: "ValueResolver per D-5 + D-10 — SKU format, default-offer price, currency chain"
      contains: "final class ShopaholicProductValueResolver implements ValueResolver"
    - path: "phpstan.neon"
      provides: "Deny-list allowIn entries extended for new adapter file"
      contains: "ShopaholicProductAdapter.php"
    - path: "tests/Feature/Adapter/Shopaholic/ShopaholicProductValueResolverTest.php"
      provides: "Resolver behavior test — SKU shapes, price source, currency chain, contents, numItems"
      contains: "provideOfferShapes"
    - path: "tests/Contract/Adapter/Shopaholic/ShopaholicProductAdapterContractTest.php"
      provides: "Phase 2 contract proof — 10 EventSubjectAdapterContractTestCase invariants pass for ShopaholicProductAdapter"
      contains: "extends EventSubjectAdapterContractTestCase"
  key_links:
    - from: "classes/adapter/shopaholic/ShopaholicProductAdapter.php"
      to: "classes/adapter/SupportsHybridAjax.php"
      via: "implements SupportsHybridAjax"
      pattern: "implements SupportsHybridAjax"
    - from: "classes/adapter/shopaholic/ShopaholicProductAdapter.php"
      to: "classes/adapter/shopaholic/ShopaholicProductValueResolver.php"
      via: "getValueResolver() returns new ShopaholicProductValueResolver"
      pattern: "new ShopaholicProductValueResolver"
    - from: "phpstan.neon"
      to: "classes/adapter/shopaholic/ShopaholicProductAdapter.php"
      via: "allowIn entries on 4 deny-list rules"
      pattern: "ShopaholicProductAdapter.php"
    - from: "tests/Contract/Adapter/Shopaholic/ShopaholicProductAdapterContractTest.php"
      to: "classes/testing/EventSubjectAdapterContractTestCase.php"
      via: "extends EventSubjectAdapterContractTestCase"
      pattern: "extends EventSubjectAdapterContractTestCase"
---

<objective>
Ship `ShopaholicProductAdapter` + `ShopaholicProductValueResolver` adapter pair for `Lovata\Shopaholic\Models\Product`, with the phpstan deny-list allowlist extension that lets the D-15 site-context fallback pass static analysis, AND prove the new adapter satisfies the Phase 2 contract via a dedicated contract test (mirroring ShopaholicOrderAdapterContractTest + ShopaholicCartPositionAdapterContractTest — every adapter in the plugin ships with its contract proof).

Purpose: Phase 6 ViewContent funnel needs a vendor adapter for the Product subject. This plan ships the adapter pair following the `ShopaholicCartPositionAdapter` + `ShopaholicCartPositionValueResolver` shape verbatim (PATTERNS.md §2.1 + §2.2), with adapter-specific deltas: subject_type alias `'shopaholic.product'`, `getSiteId` reading `$obProduct->site_list` array (Lovata Shopaholic ships Product without a `site_id` column — verified per RESEARCH §10), `loadSubject` re-enforcing `Product::active()->find` + site-match (T-6-05 mitigation — prevents cross-site subject_id spoofing via hybrid AJAX). The contract test (Task 4) extends Phase 2's abstract `EventSubjectAdapterContractTestCase` and supplies only `makeAdapter()` + `makeSubject()` — all 10 invariants are inherited automatically.

Output:
- `classes/adapter/shopaholic/ShopaholicProductAdapter.php` — implements `SupportsHybridAjax` (subinterface from plan 06-03), alias constant `'shopaholic.product'`, supported events `['ViewContent' => ['capi', 'pixel']]`, getSiteId D-15 fallback.
- `classes/adapter/shopaholic/ShopaholicProductValueResolver.php` — implements `ValueResolver`, default-offer via `$obProduct->offer->where('active', true)->sortBy('sort_order')->first()` (D-10), SKU per D-5.
- `phpstan.neon` allowIn extended on 4 deny-list rules to include the new adapter file.
- `tests/Feature/Adapter/Shopaholic/ShopaholicProductValueResolverTest.php` upgraded from RED stubs to GREEN tests covering resolver behavior; the `ShopaholicProductAdapter` integration is covered by `ProductPageWatcherTest` in plan 06-05.
- `tests/Contract/Adapter/Shopaholic/ShopaholicProductAdapterContractTest.php` (NEW) — extends `EventSubjectAdapterContractTestCase`, supplies `makeAdapter()` + `makeSubject()` + hermetic schema bootstrap; the 10 inherited invariants run automatically.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-viewcontent-funnel-shopaholic-pdp/06-CONTEXT.md
@.planning/phases/06-viewcontent-funnel-shopaholic-pdp/06-RESEARCH.md
@.planning/phases/06-viewcontent-funnel-shopaholic-pdp/06-PATTERNS.md
@.planning/phases/06-viewcontent-funnel-shopaholic-pdp/06-03-SUMMARY.md

@classes/adapter/shopaholic/ShopaholicCartPositionAdapter.php
@classes/adapter/shopaholic/ShopaholicCartPositionValueResolver.php
@classes/adapter/shopaholic/ShopaholicOrderAdapter.php
@classes/adapter/ValueResolver.php
@classes/exception/OrderHasNoCurrencyException.php
@classes/testing/EventSubjectAdapterContractTestCase.php
@tests/Contract/Adapter/Shopaholic/ShopaholicOrderAdapterContractTest.php
@tests/Contract/Adapter/Shopaholic/ShopaholicCartPositionAdapterContractTest.php
@phpstan.neon
</context>

<interface_context>
**`ShopaholicProductAdapter` signatures:**

- `private const SUBJECT_TYPE = 'shopaholic.product';`
- `private const SUPPORTED_EVENTS = ['ViewContent' => ['capi', 'pixel']];`
- `getSubjectType(object): string` returns `self::SUBJECT_TYPE` always.
- `getSubjectId(object): int` reads `$obProduct->getAttribute('id')`, casts numeric → int, 0 otherwise.
- `getSiteId(object): ?int` — Path A: when `$obProduct->site_list` array count === 1, return that single int. Path B: fall back to `\October\Rain\Support\Facades\Site::getSiteIdFromContext()` (D-15 documented exception per RESEARCH §10). Returns null when both yield non-positive.
- `getSecretKey(object): ?string` returns null (no per-product secret).
- `getValueResolver(object): ValueResolver` returns `new ShopaholicProductValueResolver`.
- `getUserData(object): array<string, ?string>` — all 13 Meta CAPI keys nulled (cookie middleware populates via CapturesRequestUserData on the watcher side).
- `getSupportedEvents(): array<string, list<string>>` returns `self::SUPPORTED_EVENTS`.
- `loadSubject(int $iSubjectId, array $arContext): ?object` — `Product::active()->find($iSubjectId)`; null on miss; then site-match check against `Site::getSiteIdFromContext()` (return null when current site not in `$obProduct->site_list`).

**`ShopaholicProductValueResolver` signatures (per ValueResolver interface):**

- `resolveContentIds(object): list<string>`:
  - `productOf($obSubject)` → null returns `[]`.
  - `defaultOffer($obProduct)` (= first active by `sort_order` asc per D-10) → null returns `['SKU-{pid}']` (bare).
  - When `$obProduct->offer->count() > 1`: returns `['SKU-{pid}-{oid}']`.
  - When count === 1 or 0 (but default-offer non-null): returns `['SKU-{pid}']`.
- `resolveValue(object): float` — returns `$obDefaultOffer->price_value` (raw column) cast to float; 0.0 when no default offer.
- `resolveCurrency(object): string` — chain: `CurrencyHelper::instance()->getActiveCurrencyCode()` → `Settings::get('default_currency_code', '')` (with runtime `is_string($mValue) ? $mValue : ''` guard per project's PHPStan-level-10 mixed-cast pattern) → throw `OrderHasNoCurrencyException('Product has no currency context; configure Settings.default_currency_code')`. Reuse existing exception class (D-discretion).
- `resolveContents(object): list<array{id, quantity, item_price}>` — one item, quantity 1, content-id from `resolveContentIds[0]`, item_price from `resolveValue`.
- `resolveNumItems(object): int` — constant 1.

**phpstan.neon allowIn additions (4 rules):**

Add `- 'classes/adapter/shopaholic/ShopaholicProductAdapter.php'` line to the existing `allowIn:` block in each of the 4 deny-list rules, alphabetical after the existing ShopaholicCartPositionAdapter entry.

**`ShopaholicProductAdapterContractTest` shape:**

- Class: `final class ShopaholicProductAdapterContractTest extends EventSubjectAdapterContractTestCase` with class-level `#[\PHPUnit\Framework\Attributes\Group('adapter')]`.
- Namespace: `Logingrupa\Metapixel\Tests\Contract\Adapter\Shopaholic`.
- Extends Phase 2's abstract base — all 10 invariants ship for free via inheritance:
  - test_invariant_01_subject_type_is_opaque_alias_format
  - test_invariant_02_subject_id_is_positive_int
  - test_invariant_03_site_id_deterministic_across_set_site_context
  - test_invariant_04_get_site_id_reads_no_request_or_site_manager
  - test_invariant_05_get_secret_key_returns_string_or_null_never_throws
  - test_invariant_06_get_value_resolver_returns_value_resolver_instance
  - test_invariant_07_get_user_data_returns_documented_meta_capi_keys
  - test_invariant_08_get_supported_events_returns_correct_shape
  - test_invariant_09_registry_round_trip_returns_same_adapter
  - test_invariant_10_payload_builder_produces_valid_envelope_shape
- Required hooks: `makeAdapter(): EventSubjectAdapter` returns `new ShopaholicProductAdapter` and `makeSubject(): object` returns a hermetically-seeded `Product` row with a `product-site-relation` pivot row anchoring it to site_id=1.
- Required schema bootstrap (setUp): create `lovata_shopaholic_products`, `lovata_shopaholic_offers`, `lovata_shopaholic_prices`, and the multisite pivot `lovata_shopaholic_product_site_relation` (column shape: `product_id`, `site_id`). tearDown drops them.
</interface_context>

<tasks>

<task type="auto">
  <name>Task 1: Create ShopaholicProductAdapter.php implementing SupportsHybridAjax with D-15 site fallback + loadSubject guards</name>
  <files>classes/adapter/shopaholic/ShopaholicProductAdapter.php</files>
  <read_first>
    - classes/adapter/shopaholic/ShopaholicCartPositionAdapter.php (full file — class shape, constants, getSiteId fallback pattern verbatim at lines 44-55, getUserData all-null shape at lines 75-83).
    - classes/adapter/shopaholic/ShopaholicOrderAdapter.php (full file — alternative subject_type alias example).
    - classes/adapter/SupportsHybridAjax.php (NEW interface from plan 06-03 — required signature for loadSubject).
    - classes/adapter/EventSubjectAdapter.php (parent interface — 7 methods to implement).
    - classes/adapter/ValueResolver.php (return type of getValueResolver).
    - plugins/lovata/shopaholic/models/Product.php lines 1-200 — verify `active()` query scope existence + SoftDelete trait mount (line 13 import, line 137 use SoftDelete confirmed via grep during planning).
    - plugins/lovata/toolbox/traits/models/MultisiteHelperTrait.php — `$site_list` array accessor at line 11 PHPDoc + `getSiteListAttribute` body at line 46 (returns `array<int>`).
    - .planning/phases/06-viewcontent-funnel-shopaholic-pdp/06-RESEARCH.md §10 (getSiteId resolution chain + PHPStan allowlist requirement) + Pitfall 3 (loadSubject MUST replicate ProductPage::getElementObject guards) + Pitfall 6 (SoftDelete RESOLVED — Product IS soft-delete aware).
    - .planning/phases/06-viewcontent-funnel-shopaholic-pdp/06-PATTERNS.md §ShopaholicProductAdapter delta block (DELTA list verbatim).
  </read_first>
  <action>
Create `classes/adapter/shopaholic/ShopaholicProductAdapter.php`. Namespace `Logingrupa\Metapixel\Classes\Adapter\Shopaholic`.

Imports (use statements, alphabetical):
- `Logingrupa\Metapixel\Classes\Adapter\SupportsHybridAjax`
- `Logingrupa\Metapixel\Classes\Adapter\ValueResolver`
- `Lovata\Shopaholic\Models\Product`
- `October\Rain\Support\Facades\Site`

Do NOT also import `Logingrupa\Metapixel\Classes\Adapter\EventSubjectAdapter` — `SupportsHybridAjax` already extends it; the `implements` clause uses the subinterface only.

Class header — `final class ShopaholicProductAdapter implements SupportsHybridAjax`. Class-level PHPDoc (mirror CartPositionAdapter lines 11-24). Mention: alias source, site_id chain (single-site list → Site::getSiteIdFromContext fallback as third documented P-01 D-15 exception), loadSubject re-enforces Product::active()->find guards + site-match (T-6-05 mitigation — prevents cross-site subject_id spoofing through hybrid AJAX), Product mounts SoftDelete trait (RESEARCH Pitfall 6 confirmed) and Product::active() scope excludes both inactive AND trashed.

Constants:
- `private const SUBJECT_TYPE = 'shopaholic.product';`
- `private const SUPPORTED_EVENTS = ['ViewContent' => ['capi', 'pixel']];` with `@var array<string, list<string>>` PHPDoc.

Method bodies (described — no fenced code in plan):

- `getSubjectType(object $obSubject): string` — return `self::SUBJECT_TYPE` directly; argument intentionally ignored per alias-opacity contract (RESEARCH A3 verified across shipping adapters).

- `getSubjectId(object $obSubject): int` — `$this->productOf($obSubject)?->getAttribute('id')` → `is_numeric` guard → `(int)` cast; 0 on null/non-numeric.

- `getSiteId(object $obSubject): ?int`:
  - `$obProduct = $this->productOf($obSubject);` — null returns null.
  - `$mSiteList = $obProduct->site_list ?? null;` — `is_array && count === 1` → `(int) $mSiteList[0]` when positive.
  - Path B fallback: `$mContext = Site::getSiteIdFromContext();` — `is_int && > 0` → `(int) $mContext`. Otherwise null.

- `getSecretKey(object $obSubject): ?string` — return null.

- `getValueResolver(object $obSubject): ValueResolver` — `return new ShopaholicProductValueResolver;`.

- `getUserData(object $obSubject): array` — all 13 Meta CAPI keys nulled (copy CartPositionAdapter lines 75-83 verbatim with `@return array<string, ?string>` PHPDoc).

- `getSupportedEvents(): array` — return `self::SUPPORTED_EVENTS` (`@return array<string, list<string>>`).

- `loadSubject(int $iSubjectId, array $arContext): ?object` per RESEARCH §10 critical-finding sketch:
  - When `$iSubjectId <= 0` return null.
  - `$obProduct = Product::active()->find($iSubjectId);` — null returns null.
  - Site-match check: `$mContext = Site::getSiteIdFromContext();` + `$mSiteList = $obProduct->site_list ?? null;` — when `is_int($mContext) && is_array($mSiteList) && ! in_array($mContext, $mSiteList, true)` return null.
  - Return `$obProduct` otherwise.
  - The `$arContext` (carrying `offer_id` from the hybrid AJAX path) is NOT used for hydration here — the offer is resolved by the ValueResolver at payload-build time from the product's offer relation. The context is reserved for adapters that need to disambiguate sub-entities at load time.

- Private helper `productOf(object $obSubject): ?Product` — `return $obSubject instanceof Product ? $obSubject : null;`.

Tiger-Style: no try/catch in adapter; ThemeAjaxHandler::dispatchViaAdapter (plan 06-06) wraps the outer Throwable catch.

Hungarian discipline: `$obSubject`, `$obProduct`, `$iSubjectId`, `$arContext`, `$mSiteList`, `$mContext`.

DO NOT register the adapter in `Plugin::boot()` yet — that lands in plan 06-05.

Note on phpstan run: this file currently fails phpstan because deny-list bans `Site::*` in `classes/adapter/shopaholic/*`. Task 3 adds it to the allowlist. Until Task 3 runs, `composer qa` will be RED on this file — that is expected.
  </action>
  <verify>
    <automated>php -l classes/adapter/shopaholic/ShopaholicProductAdapter.php</automated>
  </verify>
  <acceptance_criteria>
    - Source: `classes/adapter/shopaholic/ShopaholicProductAdapter.php` exists; `grep -q 'final class ShopaholicProductAdapter implements SupportsHybridAjax' classes/adapter/shopaholic/ShopaholicProductAdapter.php` matches.
    - Source: `grep -q "private const SUBJECT_TYPE = 'shopaholic.product'" classes/adapter/shopaholic/ShopaholicProductAdapter.php` matches.
    - Source: `grep -q "'ViewContent' => \['capi', 'pixel'\]" classes/adapter/shopaholic/ShopaholicProductAdapter.php` matches.
    - Source: `grep -q 'Product::active' classes/adapter/shopaholic/ShopaholicProductAdapter.php` matches (loadSubject uses active scope).
    - Source: `grep -q 'getSiteIdFromContext' classes/adapter/shopaholic/ShopaholicProductAdapter.php` matches (D-15 fallback present).
    - Behavior: `php -l classes/adapter/shopaholic/ShopaholicProductAdapter.php` exits 0 (syntax check).
    - Behavior: phpstan will fail on this file UNTIL Task 3 lands the allowlist; that is the expected interim state.
  </acceptance_criteria>
  <done>ShopaholicProductAdapter.php on disk; implements SupportsHybridAjax; D-15 fallback + loadSubject guards present; phpstan flags the expected Site::* deny-list hit (will be resolved in Task 3).</done>
</task>

<task type="auto">
  <name>Task 2: Create ShopaholicProductValueResolver.php with default-offer + SKU + currency chain</name>
  <files>classes/adapter/shopaholic/ShopaholicProductValueResolver.php</files>
  <read_first>
    - classes/adapter/shopaholic/ShopaholicCartPositionValueResolver.php (full file — analog; verbatim helpers `intAttr`/`floatAttr` at lines 122-134; `buildContentId` at lines 97-108).
    - classes/adapter/shopaholic/ShopaholicOrderValueResolver.php (analog for resolveContents shape).
    - classes/adapter/ValueResolver.php (interface — 5 methods to implement).
    - classes/exception/OrderHasNoCurrencyException.php (existing exception to throw on empty currency chain — D-discretion reuse).
    - plugins/lovata/shopaholic/classes/helper/CurrencyHelper.php lines 84-92 — `getActiveCurrencyCode(): ?string` contract.
    - .planning/phases/06-viewcontent-funnel-shopaholic-pdp/06-RESEARCH.md §4 (currency + price chain) + Example 1 (drop-in resolver template at L863-942).
    - .planning/phases/06-viewcontent-funnel-shopaholic-pdp/06-PATTERNS.md §ShopaholicProductValueResolver delta.
  </read_first>
  <action>
Create `classes/adapter/shopaholic/ShopaholicProductValueResolver.php`. Namespace `Logingrupa\Metapixel\Classes\Adapter\Shopaholic`.

Imports:
- `Logingrupa\Metapixel\Classes\Adapter\ValueResolver`
- `Logingrupa\Metapixel\Classes\Exception\OrderHasNoCurrencyException`
- `Logingrupa\Metapixel\Models\Settings`
- `Lovata\Shopaholic\Classes\Helper\CurrencyHelper`
- `Lovata\Shopaholic\Models\Offer`
- `Lovata\Shopaholic\Models\Product`
- `October\Rain\Database\Model`

`final class ShopaholicProductValueResolver implements ValueResolver`. Class-level PHPDoc one-liner per `<interface_context>` summary.

Method shapes:

- `resolveContentIds(object $obSubject): list<string>` — null-product returns `[]`; `defaultOffer($obProduct)` null returns `['SKU-{pid}']` bare; multi-offer (count > 1) returns `['SKU-{pid}-{oid}']`; single offer returns `['SKU-{pid}']`. Use `sprintf('SKU-%d', $iId)` / `sprintf('SKU-%d-%d', $iPid, $iOid)`.

- `resolveValue(object $obSubject): float` — `defaultOffer` null returns 0.0; else `floatAttr($obDefault, 'price_value')`.

- `resolveCurrency(object $obSubject): string`:
  - `$sCode = CurrencyHelper::instance()->getActiveCurrencyCode();` — `is_string && !== ''` → return.
  - `$mDefault = Settings::get('default_currency_code', '');` — `is_string && !== ''` → return.
  - Else throw `OrderHasNoCurrencyException('Product has no currency context; configure Settings.default_currency_code')`. D-discretion reuses existing exception class (no caller branches on type, no need to mint ProductHasNoCurrencyException).

- `resolveContents(object $obSubject): list<array{id: string, quantity: int, item_price: float}>`:
  - `$arIds = $this->resolveContentIds($obSubject);` — empty returns `[]`.
  - Returns `[['id' => $arIds[0], 'quantity' => 1, 'item_price' => $this->resolveValue($obSubject)]]`.

- `resolveNumItems(object $obSubject): int` — return 1.

Private helpers (copy verbatim from CartPositionValueResolver lines 122-134):
- `productOf(object $obSubject): ?Product` — `$obSubject instanceof Product ? $obSubject : null`.
- `defaultOffer(?Product $obProduct): ?Offer` per D-10: null product returns null; null/empty offer collection returns null; return `$obCollection->where('active', true)->sortBy('sort_order')->first()`.
- `intAttr(Model $obModel, string $sAttr): int` — verbatim.
- `floatAttr(Model $obModel, string $sAttr): float` — verbatim.

PHPStan level-10 mixed-cast pattern: `Settings::get` returns mixed; the `is_string($mValue) ? $mValue : ''` runtime guard is mandatory (project ban on `@phpstan-ignore`).

Tiger-Style boundary: `resolveCurrency` throws (fail-fast at the value-resolution boundary). The watcher's outer try/catch catches the throw → logs + skips emission.

Composer dep boundary: file at `classes/adapter/shopaholic/` IS allowlisted for Lovata imports (composer-dependency-analyser.php lines 31-34).
  </action>
  <verify>
    <automated>./vendor/bin/phpstan analyse classes/adapter/shopaholic/ShopaholicProductValueResolver.php --level=10 --no-progress</automated>
  </verify>
  <acceptance_criteria>
    - Source: `classes/adapter/shopaholic/ShopaholicProductValueResolver.php` exists; `grep -q 'final class ShopaholicProductValueResolver implements ValueResolver' classes/adapter/shopaholic/ShopaholicProductValueResolver.php` matches.
    - Source: `grep -q "where('active', true)->sortBy('sort_order')" classes/adapter/shopaholic/ShopaholicProductValueResolver.php` matches (D-10 implementation).
    - Source: `grep -q 'CurrencyHelper::instance' classes/adapter/shopaholic/ShopaholicProductValueResolver.php` matches.
    - Source: `grep -q 'OrderHasNoCurrencyException' classes/adapter/shopaholic/ShopaholicProductValueResolver.php` matches (currency throw path).
    - Static: `./vendor/bin/phpstan analyse classes/adapter/shopaholic/ShopaholicProductValueResolver.php --level=10 --no-progress` exits 0 — resolver does NOT touch Site/SiteManager/Request, no allowlist needed.
    - Behavior: `composer deps` (composer-dependency-analyser) does NOT flag this file.
  </acceptance_criteria>
  <done>ShopaholicProductValueResolver.php on disk; default-offer logic per D-10; currency chain with throw at boundary; phpstan L10 clean.</done>
</task>

<task type="auto">
  <name>Task 3: Extend phpstan.neon allowIn for ShopaholicProductAdapter + GREEN ShopaholicProductValueResolverTest</name>
  <files>
    phpstan.neon,
    tests/Feature/Adapter/Shopaholic/ShopaholicProductValueResolverTest.php
  </files>
  <read_first>
    - phpstan.neon (current state — 4 deny-list rules at the disallowedFunctionCalls + disallowedMethodCalls blocks; each has `allowIn:` listing `ShopaholicCartPositionAdapter.php`).
    - tests/Feature/Adapter/Shopaholic/ShopaholicProductValueResolverTest.php (RED stubs from plan 06-01).
    - classes/adapter/shopaholic/ShopaholicCartPositionValueResolver.php (analog the resolver was built from).
    - .planning/phases/06-viewcontent-funnel-shopaholic-pdp/06-PATTERNS.md §Shared Patterns / PHPStan disallowed-calls allowlist extension.
  </read_first>
  <action>
Modify `phpstan.neon`. In each of the FOUR `allowIn:` blocks listed below, append a new line `- 'classes/adapter/shopaholic/ShopaholicProductAdapter.php'` immediately after the existing `- 'classes/adapter/shopaholic/ShopaholicCartPositionAdapter.php'` line:

1. `disallowedFunctionCalls` block for `request()`.
2. `disallowedMethodCalls` block for `System\Classes\SiteManager::*`.
3. `disallowedMethodCalls` block for `October\Rain\Support\Facades\Site::*`.
4. `disallowedMethodCalls` block for `Illuminate\Http\Request::*`.

YAML indentation MUST match existing convention. Each addition is a single line.

Then transform `tests/Feature/Adapter/Shopaholic/ShopaholicProductValueResolverTest.php` from RED stubs (plan 06-01) to GREEN tests.

Class declaration: namespace `Logingrupa\Metapixel\Tests\Feature\Adapter\Shopaholic`. `final class ShopaholicProductValueResolverTest extends MetapixelTestCase`. Class-level `#[Group('adapter')]` (already from plan 06-01).

Imports (use statements):
- Logingrupa\Metapixel\Classes\Adapter\Shopaholic\ShopaholicProductValueResolver
- Logingrupa\Metapixel\Classes\Exception\OrderHasNoCurrencyException
- Logingrupa\Metapixel\Models\Settings
- Logingrupa\Metapixel\Tests\MetapixelTestCase
- Lovata\Shopaholic\Models\Offer
- Lovata\Shopaholic\Models\Product
- PHPUnit\Framework\Attributes\DataProvider
- PHPUnit\Framework\Attributes\Group

setUp ritual: `parent::setUp(); Settings::clearInternalCache(); Settings::set(['default_currency_code' => 'EUR']);` (known baseline).

Fixture helpers (private methods) build Product + Offer fixtures without booting full Lovata schema:
- `private function makeProduct(int $iProductId, array $arOffers): Product` — uses `(new Product)->forceFill(['id' => $iProductId, 'name' => 'Test Product'])`; then `$obProduct->setRelation('offer', new \October\Rain\Database\Collection($arOffers))`.
- `private function makeOffer(int $iOfferId, float $fPrice, int $iSortOrder, bool $bActive): Offer` — `(new Offer)->forceFill([...])`.

Seven test methods (turn the 7 RED stub methods from plan 06-01 GREEN):

1. `test_resolveContentIds_matches_sku_format_for_offer_shapes` decorated with `#[DataProvider('provideOfferShapes')]`. Provider returns 4 named cases:
   - 'zero offers' => `[42, [], ['SKU-42']]`
   - 'single offer' => `[42, [[100, 9.99, 0, true]], ['SKU-42']]`
   - 'multi-offer first-active by sort_order' => `[42, [[101, 5.00, 1, true], [100, 9.99, 0, true]], ['SKU-42-100']]`
   - 'multi-offer skipping inactive first' => `[42, [[101, 5.00, 0, false], [100, 9.99, 1, true]], ['SKU-42-100']]`
   Per case: build product → call `resolveContentIds` → assertSame against expected list.
2. `test_resolveValue_reads_default_offer_price_value_raw` — product with single offer price_value=12.50 → asserts 12.50. Empty offer product → asserts 0.0.
3. `test_resolveCurrency_returns_active_currency_code_when_available` — bind a stub `CurrencyHelper` instance into the container OR use `App::singleton('Lovata\\Shopaholic\\Classes\\Helper\\CurrencyHelper', fn() => $obStub)` returning a fake with `getActiveCurrencyCode() === 'EUR'`. Assert resolver returns 'EUR'. Prefer container-binding over Mockery overload to avoid `RunInSeparateProcess` overhead — CurrencyHelper uses the Singleton trait so `App::singleton(...)` overrides cleanly.
4. `test_resolveCurrency_falls_back_to_settings_default_currency_code` — stub CurrencyHelper to return null; Settings has `default_currency_code => 'NOK'`. Assert resolver returns 'NOK'.
5. `test_resolveCurrency_throws_when_both_sources_empty` — `Settings::set(['default_currency_code' => ''])`. Stub CurrencyHelper to return null. `$this->expectException(OrderHasNoCurrencyException::class);` + call resolveCurrency.
6. `test_resolveContents_returns_one_item_with_quantity_one` — single-offer product price_value=7.77 → asserts `resolveContents` equals `[['id' => 'SKU-42', 'quantity' => 1, 'item_price' => 7.77]]`.
7. `test_resolveNumItems_returns_one_constant` — assertSame(1, ...) regardless of offer count.

Drop the `fail('GREEN in plan 06-04 — Task 3')` placeholders from each method.

Note on CurrencyHelper container binding: read `plugins/lovata/shopaholic/classes/helper/CurrencyHelper.php` to confirm it uses Lovata's `Singleton` trait (which exposes `::instance()` reading from the container or a static `$instance`). If Singleton uses a private static `$instance` field that bypasses the container, container-binding alone won't work — fall back to `$obReflection = new \ReflectionProperty(CurrencyHelper::class, 'instance'); $obReflection->setAccessible(true); $obReflection->setValue(null, $obStub);` then reset in tearDown. Document the choice in a setUp comment.
  </action>
  <verify>
    <automated>./vendor/bin/phpstan analyse classes/adapter/shopaholic/ShopaholicProductAdapter.php classes/adapter/shopaholic/ShopaholicProductValueResolver.php --level=10 --no-progress &amp;&amp; ./vendor/bin/pest tests/Feature/Adapter/Shopaholic/ShopaholicProductValueResolverTest.php --no-coverage</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c "ShopaholicProductAdapter.php" phpstan.neon` returns 4 (one entry per allowIn block).
    - Source: `tests/Feature/Adapter/Shopaholic/ShopaholicProductValueResolverTest.php` has 7 `public function test_*` methods (`grep -c 'public function test_'` == 7).
    - Source: data provider `provideOfferShapes` returns 4 named cases (`grep -c "=>" inside provideOfferShapes body` ≥ 4 — verify by reading the static method).
    - Static: `./vendor/bin/phpstan analyse classes/adapter/shopaholic/ShopaholicProductAdapter.php classes/adapter/shopaholic/ShopaholicProductValueResolver.php --level=10 --no-progress` exits 0.
    - Behavior: `./vendor/bin/pest tests/Feature/Adapter/Shopaholic/ShopaholicProductValueResolverTest.php` exits 0 with 10 passing tests (data provider expands test 1 into 4 cases + 6 standalone tests = 10).
    - Static: `composer deps` exits 0 (no new dep-boundary violations introduced).
  </acceptance_criteria>
  <done>phpstan allowlist extended for ShopaholicProductAdapter; resolver test 10 cases GREEN; composer qa green on the file set.</done>
</task>

<task type="auto">
  <name>Task 4: Create ShopaholicProductAdapterContractTest (Phase 2 10-invariant proof for ShopaholicProductAdapter)</name>
  <files>tests/Contract/Adapter/Shopaholic/ShopaholicProductAdapterContractTest.php</files>
  <read_first>
    - tests/Contract/Adapter/Shopaholic/ShopaholicOrderAdapterContractTest.php (full file — primary analog; class shape, setUp/tearDown ritual, `bootOrdersTable` schema-bootstrap pattern, `makeAdapter()` + `makeSubject()` overrides).
    - tests/Contract/Adapter/Shopaholic/ShopaholicCartPositionAdapterContractTest.php (full file — secondary analog; shows multi-table schema bootstrap via `bootOffersAndProductsTables` + `bootCartTable` + `bootPricesTable`).
    - classes/testing/EventSubjectAdapterContractTestCase.php (full file — abstract base; verify the 10 invariant method names + the `makeAdapter()` / `makeSubject()` abstract signatures + any `setSiteContext` helper used inside invariant 03).
    - classes/adapter/shopaholic/ShopaholicProductAdapter.php (post Task 1 state — the SUT).
    - plugins/lovata/shopaholic/models/Product.php (schema — fillable + active scope columns to seed: id, name, slug, active, sort_order; verify the product-site-relation pivot table name `lovata_shopaholic_product_site_relation` and pivot columns `product_id`, `site_id`).
    - plugins/lovata/toolbox/traits/models/MultisiteHelperTrait.php (confirms `$site_list` accessor reads the product-site-relation pivot).
  </read_first>
  <action>
Create `tests/Contract/Adapter/Shopaholic/ShopaholicProductAdapterContractTest.php`. Namespace `Logingrupa\Metapixel\Tests\Contract\Adapter\Shopaholic`. Class-level docblock mirrors ShopaholicOrderAdapterContractTest lines 14-19: explain this is the Phase 2 contract proof — ShopaholicProductAdapter satisfies all 10 invariants of EventSubjectAdapterContractTestCase, and the hermetic Product + Offer + Price + product-site-relation tables are provisioned inline (the abstract base extends MetapixelTestCase, not ShopaholicAdapterTestCase, so the cart-plugin schema must be created here).

Class header — `#[\PHPUnit\Framework\Attributes\Group('adapter')]` then `final class ShopaholicProductAdapterContractTest extends EventSubjectAdapterContractTestCase`.

Imports (alphabetical):
- `Illuminate\Support\Facades\DB`
- `Illuminate\Support\Facades\Schema`
- `Logingrupa\Metapixel\Classes\Adapter\EventSubjectAdapter`
- `Logingrupa\Metapixel\Classes\Adapter\Shopaholic\ShopaholicProductAdapter`
- `Logingrupa\Metapixel\Classes\Testing\EventSubjectAdapterContractTestCase`
- `Logingrupa\Metapixel\Models\Settings`
- `Lovata\Shopaholic\Models\Product`
- `PHPUnit\Framework\Attributes\Group`

setUp() ritual (mirror ShopaholicOrderAdapterContractTest setUp lines 27-35):
- `parent::setUp();`
- `$this->bootProductsTable();`
- `$this->bootOffersTable();`
- `$this->bootPricesTable();`
- `$this->bootProductSiteRelationTable();`
- `Settings::clearInternalCache();`
- `Settings::set(['default_currency_code' => 'EUR']);`

tearDown() drops the four tables in reverse-create order, then `parent::tearDown();`.

makeAdapter() returns `new ShopaholicProductAdapter`.

makeSubject() inserts a hermetic Product row (id=1, name='Test Product', slug='test-product', active=1, sort_order=0) plus a corresponding product-site-relation pivot row (product_id=1, site_id=1) so `$obProduct->site_list` resolves to `[1]` and invariant 03 (`site_id_deterministic_across_set_site_context`) reads a single deterministic site. Then `$obProduct = Product::find(1);` with a `fail('Failed to load Product fixture')` fallback when null. Return `$obProduct`.

Schema bootstrap helpers (private, follow ShopaholicOrderAdapterContractTest pattern verbatim):

- `bootProductsTable(): void` — guard via `Schema::hasTable(...)` early return. Columns: increments('id'), string('name')->nullable, string('slug')->nullable, boolean('active')->default(true), integer('sort_order')->default(0), timestamps, softDeletes (Pitfall 6 — Product mounts SoftDelete).
- `bootOffersTable(): void` — guard. Columns: increments('id'), integer('product_id')->nullable, string('name')->nullable, decimal('price_value', 18, 4)->nullable, boolean('active')->default(true), integer('sort_order')->default(0), timestamps.
- `bootPricesTable(): void` — guard. Columns: increments('id'), integer('item_id')->nullable, string('item_type')->nullable, integer('price_type_id')->nullable, decimal('price_value', 18, 4)->nullable.
- `bootProductSiteRelationTable(): void` — guard. Table name `lovata_shopaholic_product_site_relation`. Columns: integer('product_id'), integer('site_id'). No primary key needed for the contract test (Lovata real schema uses composite PK; SQLite in-memory accepts plain integer columns).

The 10 invariant tests inherit from EventSubjectAdapterContractTestCase — DO NOT redefine them in this subclass. The class body only declares: class-level Group attribute, setUp, tearDown, makeAdapter, makeSubject, and the four `boot*` helpers.

Hungarian discipline: `$obAdapter`, `$obProduct`, `$obTable`. Use `function ($obTable): void` Schema closure signature (no curly Schema::create body without explicit return type).

PHPStan level-10 compliance: tests directory excluded from phpstan level-10 paths per phpstan.neon (verify before commit). If included, add `@return` PHPDoc on every private helper + `function ($obTable): void` type hints.

Tiger-Style boundary: makeSubject's null-Product fail() is the only catch — schema misconfigurations explode loud.
  </action>
  <verify>
    <automated>./vendor/bin/pest tests/Contract/Adapter/Shopaholic/ShopaholicProductAdapterContractTest.php --no-coverage</automated>
  </verify>
  <acceptance_criteria>
    - Source: `tests/Contract/Adapter/Shopaholic/ShopaholicProductAdapterContractTest.php` exists; `grep -q 'final class ShopaholicProductAdapterContractTest extends EventSubjectAdapterContractTestCase' tests/Contract/Adapter/Shopaholic/ShopaholicProductAdapterContractTest.php` matches.
    - Source: `grep -q "#\[Group('adapter')\]" tests/Contract/Adapter/Shopaholic/ShopaholicProductAdapterContractTest.php` matches.
    - Source: `grep -q 'protected function makeAdapter' tests/Contract/Adapter/Shopaholic/ShopaholicProductAdapterContractTest.php` matches.
    - Source: `grep -q 'protected function makeSubject' tests/Contract/Adapter/Shopaholic/ShopaholicProductAdapterContractTest.php` matches.
    - Source: `grep -q 'lovata_shopaholic_product_site_relation' tests/Contract/Adapter/Shopaholic/ShopaholicProductAdapterContractTest.php` matches (product-site pivot bootstrap present).
    - Behavior: `./vendor/bin/pest tests/Contract/Adapter/Shopaholic/ShopaholicProductAdapterContractTest.php` exits 0 with 10 passing tests (inherited from EventSubjectAdapterContractTestCase).
    - Behavior: `./vendor/bin/pest --exclude-group=adapter` does NOT execute this file (minimal-install isolation).
    - Behavior: existing Phase 2/3 contract tests (`ShopaholicOrderAdapterContractTest`, `ShopaholicCartPositionAdapterContractTest`, `ThemeActionAdapterContractTest`, `FakeAdapterContractTest`) continue to pass — no shared-state regression.
  </acceptance_criteria>
  <done>ShopaholicProductAdapterContractTest committed; extends EventSubjectAdapterContractTestCase; 10 inherited invariant tests GREEN against ShopaholicProductAdapter; every Shopaholic adapter in the plugin now ships with its Phase 2 contract proof.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Hybrid AJAX `subject_id` integer → ShopaholicProductAdapter::loadSubject | Attacker-supplied PK. MUST be re-validated against active scope + site relation before being treated as a real subject. |
| `$obProduct->site_list` array → getSiteId Path A | Subject-derived data trusted; comes from Lovata's `MultisiteHelperTrait` reading `lovata_shopaholic_product_site_relation` pivot table — operator-controlled, not attacker-controlled. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-6-02 | Information Disclosure | ShopaholicProductAdapter::getSiteId reading from `Request::*` / `SiteManager::*` (PHPStan disallowed-calls scope) | mitigate | D-15 pattern — read `$obProduct->site_list` pivot first; only fall back to `Site::getSiteIdFromContext()` when multi-site. Add adapter file to phpstan.neon allowIn lists in Task 3. The fallback is in the disallow scope by directory, but legitimate per D-15 + RESEARCH §10. Contract test invariant 04 (`get_site_id_reads_no_request_or_site_manager`) is the runtime gate proving the adapter respects D-15 — Task 4 ships that proof. |
| T-6-05 | Spoofing / Information Disclosure | Offer-switch JS posts a forged `subject_id` to fire ViewContent for a cross-site or inactive product | mitigate | `loadSubject` re-enforces `Product::active()->find($iSubjectId)` (active scope + SoftDelete excluded by Product's `active` scope) AND site-relation match against `Site::getSiteIdFromContext()`. Adapter test 5 in plan 06-06 (ThemeAjaxHandlerSubjectTypeTest item 5 — loadSubject returns null → 404) is the integration gate. |
| T-6-W4-T | Tampering | `Settings::get('default_currency_code')` returning non-string injected value | mitigate | Runtime `is_string($mValue) ? $mValue : ''` guard. PHPStan level-10 mixed-cast pattern is mandatory; the project bans `@phpstan-ignore`. Same idiom used in `PluginGuard::isDisabled` + `Settings::lookupForSite`. |
| T-6-W4-C | Tampering | Future regression breaks one of the 10 Phase 2 invariants on the Product adapter (e.g. someone alters getSubjectType to read site context) | mitigate | Task 4 ships ShopaholicProductAdapterContractTest — running `./vendor/bin/pest --group=adapter` runs the inherited 10 invariants. Any regression fails CI before merge. Same gate that protects ShopaholicOrderAdapter + ShopaholicCartPositionAdapter today. |
</threat_model>

<verification>
- `./vendor/bin/phpstan analyse classes/adapter/shopaholic/ShopaholicProductAdapter.php classes/adapter/shopaholic/ShopaholicProductValueResolver.php --level=10 --no-progress` exits 0 after Task 3.
- `./vendor/bin/pest tests/Feature/Adapter/Shopaholic/ShopaholicProductValueResolverTest.php --no-coverage` exits 0 with 10 passing tests.
- `./vendor/bin/pest tests/Contract/Adapter/Shopaholic/ShopaholicProductAdapterContractTest.php --no-coverage` exits 0 with 10 passing tests.
- `composer deps` (composer-dependency-analyser) exits 0.
- `grep -c "ShopaholicProductAdapter.php" phpstan.neon` returns 4.
- Existing `tests/Feature/Adapter/Shopaholic/PurchaseFlowIntegrationTest.php` still passing (no Order/CartPosition regression).
- Existing `tests/Contract/Adapter/Shopaholic/ShopaholicOrderAdapterContractTest.php` + `ShopaholicCartPositionAdapterContractTest.php` still passing (no shared-state regression).
</verification>

<success_criteria>
- ShopaholicProductAdapter + ShopaholicProductValueResolver shipped under classes/adapter/shopaholic/.
- Adapter implements SupportsHybridAjax; subject_type alias 'shopaholic.product'; D-15 site_id fallback verified.
- loadSubject re-enforces Product::active()->find + site-match guards (T-6-05 mitigation).
- Resolver enforces D-5 SKU format + D-10 default-offer logic.
- phpstan.neon allowIn extended on 4 deny-list rules.
- 10 ShopaholicProductValueResolverTest cases GREEN.
- 10 ShopaholicProductAdapterContractTest invariants GREEN (Phase 2 contract proof — every Shopaholic adapter in the plugin now ships with its 10-invariant contract test).
- composer qa coverage stays ≥ 90 % aggregate.
</success_criteria>

<output>
Create `.planning/phases/06-viewcontent-funnel-shopaholic-pdp/06-04-SUMMARY.md` listing: 2 production files + 1 config file + 1 resolver test + 1 contract test updated/created; REQ closed (VIEW-02, VIEW-03, VIEW-08); T-6-02 + T-6-05 + T-6-W4-C mitigation status; phpstan delta + resolver test count + contract test count (10 inherited invariants); unblock list (Plan 05 + Plan 06).
</output>
