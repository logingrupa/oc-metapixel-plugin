---
phase: 06-viewcontent-funnel-shopaholic-pdp
plan: 05
type: execute
wave: 4
depends_on:
  - 06-02
  - 06-03
  - 06-04
autonomous: true
files_modified:
  - classes/event/adapter/shopaholic/ProductPageWatcher.php
  - Plugin.php
  - tests/Feature/Adapter/Shopaholic/ProductPageWatcherTest.php
  - tests/Feature/Plugin/ShopaholicConditionalRegistrationTest.php
requirements:
  - VIEW-04
  - VIEW-10
must_haves:
  truths:
    - "ProductPageWatcher subscribes 'shopaholic.product.open' via Dispatcher; handler receives Product, generates UUIDv4 event_id, builds payload via PayloadBuilder + UserDataHasher, injects request user_data via CapturesRequestUserData trait, pushes to ThemeEventCollector (with product_id key for ProductPixel default.htm consumption), dispatches SendCapiEvent('ViewContent', $arPayload, $obProduct, ShopaholicProductAdapter::class)."
    - "ProductPageWatcher exposes a public dispatchForOfferSwitch(int $iProductId, int $iOfferId): string method that returns the new server-generated event_id and emits a fresh CAPI dispatch with content_ids containing SKU-{pid}-{oid_new} — wired by ThemeAjaxHandler::dispatchViaAdapter (plan 06-06) AND independently exercisable from ProductPageWatcherTest item 11 without booting October's full request lifecycle."
    - "Plugin::boot inside the existing isShopaholicEnabled() branch registers ShopaholicProductAdapter (alias map gains 'shopaholic.product') AND subscribes ProductPageWatcher (Event::subscribe). One-guard pattern (RESEARCH §10): OrdersShopaholic transitively requires Shopaholic."
    - "Watcher's handle() wraps the entire payload-build-and-dispatch block in try/catch Throwable → Log::warning + return (T-6-03 mitigation — page render MUST NOT 500 on pixel failure). dispatchForOfferSwitch reuses the same boundary catch and additionally re-throws via fail-safe return-value contract documented in PHPDoc."
    - "action_key shape 'viewcontent:{product_id}:{event_id}' for PDP-render path; 'viewcontent:{product_id}:{offer_id}:{event_id}' for offer-switch path (per CONTEXT.md Claude's Discretion + brief D-3 anchor extension) — per-request-unique via event_id suffix, race-fence-safe across multiple PDP renders + switches in the same site_id."
    - "Plugin.php composer-dependency-analyser allowlist still passes (Plugin.php was already in the allowlist for Lovata\\OrdersShopaholic — extension to also import Lovata\\Shopaholic\\Models\\Product works because the per-Plugin.php carve-out at composer-dependency-analyser.php lines 51-55 covers ALL Lovata packages on that one path)."
    - "ProductPageWatcherTest 11 brief-matrix items ALL GREEN (no skipped tests — SC6 in ROADMAP requires all 11 GREEN). ShopaholicConditionalRegistrationTest covers VIEW-10 boot-gate: when PluginManager::exists('Lovata.OrdersShopaholic') returns false, ProductPageWatcher is NOT subscribed and ShopaholicProductAdapter is NOT registered."
  artifacts:
    - path: "classes/event/adapter/shopaholic/ProductPageWatcher.php"
      provides: "Subscriber for shopaholic.product.open + public dispatchForOfferSwitch entry point; dispatches ViewContent CAPI + pushes to ThemeEventCollector"
      contains: "final class ProductPageWatcher"
    - path: "Plugin.php"
      provides: "Boot-time AdapterRegistry::register + Event::subscribe(ProductPageWatcher) inside the isShopaholicEnabled branch"
      contains: "ProductPageWatcher"
  key_links:
    - from: "classes/event/adapter/shopaholic/ProductPageWatcher.php"
      to: "classes/adapter/theme/ThemeEventCollector.php"
      via: "App::make(ThemeEventCollector::class)->push([...])"
      pattern: "ThemeEventCollector"
    - from: "classes/event/adapter/shopaholic/ProductPageWatcher.php"
      to: "classes/queue/SendCapiEvent.php"
      via: "SendCapiEvent::dispatch('ViewContent', ...)"
      pattern: "SendCapiEvent::dispatch"
    - from: "Plugin.php"
      to: "classes/event/adapter/shopaholic/ProductPageWatcher.php"
      via: "Event::subscribe(ProductPageWatcher::class) inside isShopaholicEnabled() boot branch"
      pattern: "Event::subscribe(ProductPageWatcher"
---

<objective>
Ship `ProductPageWatcher` (subscribes `shopaholic.product.open`, dispatches ViewContent CAPI + pushes to `ThemeEventCollector`) and wire it into `Plugin::boot` under the existing `isShopaholicEnabled()` gate alongside the `ShopaholicProductAdapter` registration.

Purpose: this plan is the keystone of Phase 6. The watcher receives a `Lovata\Shopaholic\Models\Product` from Lovata's `Event::fire('shopaholic.product.open', [$obElement])` at `plugins/lovata/shopaholic/components/ProductPage.php:71` and converts it into a CAPI dispatch + a browser-side fbq script block (via collector push, flushed by the deferred-flush listener from plan 06-02). It mirrors `CartPositionWatcher` patterns verbatim, with Product-specific deltas. The watcher additionally exposes a `dispatchForOfferSwitch(int, int): string` public entry point so the offer-switch AJAX path (plan 06-06) reuses the SAME payload-build chain — keeping a single owner of the ViewContent contract and giving ProductPageWatcherTest item 11 a real GREEN assertion path without needing the full HTTP request lifecycle.

Output:
- `classes/event/adapter/shopaholic/ProductPageWatcher.php` (NEW) — subscriber class with `subscribe(Dispatcher)` + `handle(Product)` + `dispatchForOfferSwitch(int, int): string` methods; mixes in `CapturesRequestUserData` trait; Tiger-Style boundary catch.
- `Plugin.php` boot block extended: `$obRegistry->register(Product::class, ShopaholicProductAdapter::class)` + `Event::subscribe(ProductPageWatcher::class)` added immediately after the existing `CartPositionWatcher` subscribe line.
- `tests/Feature/Adapter/Shopaholic/ProductPageWatcherTest.php` upgraded from 11 RED stubs to 11 GREEN tests (brief matrix items 1-11, all passing — item 11 GREEN via the watcher's `dispatchForOfferSwitch` entry point).
- `tests/Feature/Plugin/ShopaholicConditionalRegistrationTest.php` extended with assertions for the new adapter + watcher registration (VIEW-10 gate).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-viewcontent-funnel-shopaholic-pdp/06-CONTEXT.md
@.planning/phases/06-viewcontent-funnel-shopaholic-pdp/06-RESEARCH.md
@.planning/phases/06-viewcontent-funnel-shopaholic-pdp/06-PATTERNS.md
@.planning/phases/06-viewcontent-funnel-shopaholic-pdp/06-04-SUMMARY.md

@classes/event/adapter/shopaholic/CartPositionWatcher.php
@classes/event/adapter/shopaholic/OrderStatusWatcher.php
@classes/event/CapturesRequestUserData.php
@classes/adapter/theme/ThemeEventCollector.php
@classes/queue/SendCapiEvent.php
@classes/meta/PayloadBuilder.php
@classes/meta/UserDataHasher.php
@classes/helper/PluginGuard.php
@Plugin.php
@tests/Feature/Plugin/ShopaholicConditionalRegistrationTest.php
</context>

<interface_context>
**`ProductPageWatcher` shape** (per RESEARCH §6 Example 3 + PATTERNS.md §ProductPageWatcher delta):

```
namespace Logingrupa\Metapixel\Classes\Event\Adapter\Shopaholic;

final class ProductPageWatcher
{
    use CapturesRequestUserData;

    public function subscribe(Dispatcher $obDispatcher): void
    {
        $obDispatcher->listen('shopaholic.product.open', [$this, 'handle']);
    }

    public function handle(Product $obProduct): void
    {
        try {
            if (PluginGuard::isDisabled()) {
                return;
            }
            // adapter + resolver + builder; UUIDv4 event_id; payload build;
            // injectRequestUserData; collector push (carries product_id);
            // SendCapiEvent::dispatch — action_key shape viewcontent:{pid}:{eid}.
        } catch (Throwable $obException) {
            // Tiger-Style: log + return; page render MUST NOT 500.
            Log::warning('metapixel: ProductPageWatcher emission failed', [...]);
        }
    }

    /**
     * Offer-switch entry point — re-fires ViewContent with a NEW UUIDv4 event_id
     * and content_ids containing SKU-{pid}-{oid_new}. Returns the server-generated
     * event_id so the AJAX caller (ThemeAjaxHandler::dispatchViaAdapter) can echo
     * it back in the JSON response (used by the browser fbq's 4th-arg eventID).
     *
     * action_key shape: viewcontent:{pid}:{oid_new}:{event_id} — server appends
     * the generated event_id to the wire-format action_key (viewcontent:{pid}:{oid})
     * BEFORE EventLog insert. Race-fence remains safe via the event_id suffix.
     *
     * Tiger-Style: on Throwable inside the build/dispatch block, log + rethrow.
     * The AJAX boundary (dispatchViaAdapter) re-catches and converts to a 500
     * JsonResponse; page render is not involved here.
     */
    public function dispatchForOfferSwitch(int $iProductId, int $iOfferId): string
    {
        // PluginGuard::isDisabled() → throw \RuntimeException (AJAX 422 boundary)
        // load Product via adapter->loadSubject(); null → throw \RuntimeException
        // build payload (override resolveContentIds output to ['SKU-{pid}-{oid}'])
        // collector push (carries product_id + offer_id)
        // SendCapiEvent::dispatch
        // return $sEventId
    }
}
```

**Collector push shape — PDP-render path** (per RESEARCH Example 3 lines 1023-1033):
- `name => 'ViewContent'`
- `action_key => "viewcontent:{$obProduct->id}:{$sEventId}"`
- `event_id => $sEventId`
- `content_ids => $obResolver->resolveContentIds($obProduct)`
- `content_name => is_string($obProduct->name) ? $obProduct->name : ''`
- `content_type => 'product'`
- `value => $obResolver->resolveValue($obProduct)`
- `currency => $obResolver->resolveCurrency($obProduct)`
- `product_id => $obProduct->id`

**Collector push shape — offer-switch path** (per CONTEXT.md Claude's Discretion + brief D-3 anchor extension):
- `name => 'ViewContent'`
- `action_key => "viewcontent:{$iProductId}:{$iOfferId}:{$sEventId}"` (server-appended event_id)
- `event_id => $sEventId`
- `content_ids => ['SKU-' . $iProductId . '-' . $iOfferId]` (forced multi-offer SKU since offer_id is explicit)
- other keys same as PDP-render

**Plugin.php delta** (inside `isShopaholicEnabled()` block at lines 75-81):
- `$obRegistry->register(Product::class, ShopaholicProductAdapter::class);` added after the existing CartPosition registration.
- `Event::subscribe(ProductPageWatcher::class);` added after the existing `Event::subscribe(CartPositionWatcher::class);`.
- New imports: `Logingrupa\Metapixel\Classes\Adapter\Shopaholic\ShopaholicProductAdapter` + `Logingrupa\Metapixel\Classes\Event\Adapter\Shopaholic\ProductPageWatcher` + `Lovata\Shopaholic\Models\Product`.

**One-guard pattern** (RESEARCH §10 recommendation a): keep `isShopaholicEnabled()` checking `Lovata.OrdersShopaholic` (existing) — covers Phase 6 too because OrdersShopaholic transitively requires Shopaholic. Do not split into two guards in v2.0.
</interface_context>

<tasks>

<task type="auto">
  <name>Task 1: Create ProductPageWatcher.php — handle() PDP path + dispatchForOfferSwitch() entry point</name>
  <files>classes/event/adapter/shopaholic/ProductPageWatcher.php</files>
  <read_first>
    - classes/event/adapter/shopaholic/CartPositionWatcher.php (full file — analog; subscribe pattern at lines 28-32, dispatchAddToCart at lines 62-87, logFailure at lines 90-97).
    - classes/event/adapter/shopaholic/OrderStatusWatcher.php (full file — alternative analog; Tiger-Style log+return boundary).
    - classes/event/CapturesRequestUserData.php (trait — `injectRequestUserData(array): array` signature).
    - classes/adapter/theme/ThemeEventCollector.php (singleton `push(array): void` contract).
    - classes/queue/SendCapiEvent.php lines 1-80 (constructor signature — 4 args; verify `string $sAdapterClass` is 4th).
    - classes/meta/PayloadBuilder.php (full file — `buildEventPayload(string, EventSubjectAdapter, object, ValueResolver, string, int, array): array` 7-arg signature).
    - classes/meta/UserDataHasher.php (constructor — parameterless? confirm by reading; PayloadBuilder accepts it).
    - classes/helper/PluginGuard.php (`isDisabled(): bool` signature).
    - .planning/phases/06-viewcontent-funnel-shopaholic-pdp/06-RESEARCH.md §6 + Example 3 (lines 979-1045 — full implementation reference).
    - .planning/phases/06-viewcontent-funnel-shopaholic-pdp/06-PATTERNS.md §ProductPageWatcher delta.
  </read_first>
  <action>
Create `classes/event/adapter/shopaholic/ProductPageWatcher.php`. Namespace `Logingrupa\Metapixel\Classes\Event\Adapter\Shopaholic`.

Imports (alphabetical):
- `Illuminate\Events\Dispatcher`
- `Illuminate\Support\Facades\App`
- `Illuminate\Support\Facades\Log`
- `Logingrupa\Metapixel\Classes\Adapter\Shopaholic\ShopaholicProductAdapter`
- `Logingrupa\Metapixel\Classes\Adapter\Shopaholic\ShopaholicProductValueResolver`
- `Logingrupa\Metapixel\Classes\Adapter\Theme\ThemeEventCollector`
- `Logingrupa\Metapixel\Classes\Event\CapturesRequestUserData`
- `Logingrupa\Metapixel\Classes\Helper\PluginGuard`
- `Logingrupa\Metapixel\Classes\Meta\PayloadBuilder`
- `Logingrupa\Metapixel\Classes\Meta\UserDataHasher`
- `Logingrupa\Metapixel\Classes\Queue\SendCapiEvent`
- `Lovata\Shopaholic\Models\Product`
- `Ramsey\Uuid\Uuid`
- `Throwable`

`final class ProductPageWatcher`. Class-level PHPDoc one-liner: `Subscribes Lovata Shopaholic shopaholic.product.open event (fired inside ProductPage::getElementObject after active+site guards pass per RESEARCH §3). Builds ViewContent payload via PayloadBuilder + UserDataHasher; injects request user_data; pushes the event onto ThemeEventCollector so PixelHead's deferred-flush listener emits the browser fbq script with matching event_id; dispatches SendCapiEvent to mirror to CAPI. Tiger-Style boundary: page render MUST NOT 500 on pixel failure. Exposes dispatchForOfferSwitch(int, int): string entry point for the AJAX offer-switch path (ThemeAjaxHandler::dispatchViaAdapter delegates here so the ViewContent payload contract has a single owner).`

`use CapturesRequestUserData;` inside class body.

Methods:

- `public function subscribe(Dispatcher $obDispatcher): void` — body: `$obDispatcher->listen('shopaholic.product.open', [$this, 'handle']);` (single listen line; no other event subscriptions).

- `public function handle(Product $obProduct): void`:
  - Outer `try { ... } catch (Throwable $obException) { Log::warning('metapixel: ProductPageWatcher emission failed', ['meta_pixel.product_id' => $obProduct->id ?? null, 'meta_pixel.exception' => get_class($obException), 'meta_pixel.message' => $obException->getMessage()]); }`. Reason comment on the catch: `// Tiger-Style fail-safe (T-6-03 mitigation): page render MUST NOT 500 on pixel failure — log and skip.`
  - Inside try:
    1. `if (PluginGuard::isDisabled()) { return; }` (first line).
    2. `$obAdapter = new ShopaholicProductAdapter;` + `$obResolver = new ShopaholicProductValueResolver;` + `$obBuilder = new PayloadBuilder(new UserDataHasher);`.
    3. `$sEventId = Uuid::uuid4()->toString();` + `$iEventTime = time();`.
    4. `$arPayload = $obBuilder->buildEventPayload('ViewContent', $obAdapter, $obProduct, $obResolver, $sEventId, $iEventTime, []);`.
    5. `$arPayload = $this->injectRequestUserData($arPayload);` (trait method).
    6. Collector push — `App::make(ThemeEventCollector::class)->push([...])` with the 9-key PDP-render shape from `<interface_context>`. Use `(int) $obProduct->id` for `product_id` key + `is_string($obProduct->name) ? $obProduct->name : ''` for `content_name`. `action_key` = `"viewcontent:{$obProduct->id}:{$sEventId}"`.
    7. `SendCapiEvent::dispatch('ViewContent', $arPayload, $obProduct, ShopaholicProductAdapter::class);` — 4 args per the constructor lock in STATE.md (Phase 2 SendCapiEvent shape).
  - Method body returns void.

- `public function dispatchForOfferSwitch(int $iProductId, int $iOfferId): string`:
  - PHPDoc: see `<interface_context>` block (action_key shape note + server-appended event_id contract).
  - Body:
    1. Validate inputs: `if ($iProductId <= 0 || $iOfferId <= 0) { throw new \InvalidArgumentException('ProductPageWatcher::dispatchForOfferSwitch requires positive iProductId and iOfferId'); }`.
    2. `if (PluginGuard::isDisabled()) { throw new \RuntimeException('metapixel disabled — offer-switch ViewContent suppressed'); }`. Caller (`ThemeAjaxHandler::dispatchViaAdapter`) is expected to translate this into the 422 boundary OR rate-limit short-circuit; ProductPageWatcher does NOT silently swallow here because the AJAX boundary needs to surface the disabled-state to the JS soft-gate (else the JS keeps posting).
    3. `$obAdapter = new ShopaholicProductAdapter;` + `$obResolver = new ShopaholicProductValueResolver;` + `$obBuilder = new PayloadBuilder(new UserDataHasher);`.
    4. `$obProduct = $obAdapter->loadSubject($iProductId, ['offer_id' => $iOfferId]);` — null → throw `\RuntimeException('ProductPageWatcher::dispatchForOfferSwitch: product not found or inactive')`. The AJAX boundary translates this to JsonResponse 404 (the dispatchViaAdapter path already catches and re-routes; the throw shape stays internal).
    5. `$sEventId = Uuid::uuid4()->toString();` + `$iEventTime = time();`.
    6. Build payload via `$obBuilder->buildEventPayload('ViewContent', $obAdapter, $obProduct, $obResolver, $sEventId, $iEventTime, []);`. Then OVERRIDE `$arPayload['data'][0]['custom_data']['content_ids']` to `['SKU-' . $iProductId . '-' . $iOfferId]` (forced multi-offer SKU per CONTEXT.md Claude's Discretion — the offer_id is explicit in this branch so always emit the dashed SKU regardless of resolver's first-active-offer default).
    7. `$arPayload = $this->injectRequestUserData($arPayload);`.
    8. Collector push with `action_key => "viewcontent:{$iProductId}:{$iOfferId}:{$sEventId}"` and `product_id => $iProductId` + `offer_id => $iOfferId` keys (the latter informational for downstream observers).
    9. `SendCapiEvent::dispatch('ViewContent', $arPayload, $obProduct, ShopaholicProductAdapter::class);`.
    10. `return $sEventId;`.

Composer dep boundary: `classes/event/adapter/shopaholic/` IS in the Lovata-import allowlist per `composer-dependency-analyser.php` line 33. The `use Lovata\Shopaholic\Models\Product` line is allowed.

PHPStan deny-list note: `classes/event/adapter/shopaholic/*` IS in the disallowIn scope for Site/SiteManager/Request/request(). ProductPageWatcher MUST NOT call any of those — verified by reading the action plan: the file calls App + Log + Uuid + the adapter (which carries its own allowlist). No new allowlist entry needed for the watcher.

Hungarian discipline: `$obProduct`, `$obAdapter`, `$obResolver`, `$obBuilder`, `$arPayload`, `$sEventId`, `$iEventTime`, `$iProductId`, `$iOfferId`, `$obException`.

PHP 8.3+8.4 compat: no 8.4-only syntax.

Do NOT register the watcher in Plugin.php yet — Task 2 owns that.
  </action>
  <verify>
    <automated>./vendor/bin/phpstan analyse classes/event/adapter/shopaholic/ProductPageWatcher.php --level=10 --no-progress</automated>
  </verify>
  <acceptance_criteria>
    - Source: `classes/event/adapter/shopaholic/ProductPageWatcher.php` exists; `grep -q 'final class ProductPageWatcher' classes/event/adapter/shopaholic/ProductPageWatcher.php` matches.
    - Source: `grep -q 'use CapturesRequestUserData;' classes/event/adapter/shopaholic/ProductPageWatcher.php` matches (trait mixed in).
    - Source: `grep -q "shopaholic.product.open" classes/event/adapter/shopaholic/ProductPageWatcher.php` matches (event name string).
    - Source: `grep -q "SendCapiEvent::dispatch.*ViewContent" classes/event/adapter/shopaholic/ProductPageWatcher.php` matches.
    - Source: `grep -q "ThemeEventCollector::class" classes/event/adapter/shopaholic/ProductPageWatcher.php` matches.
    - Source: `grep -q 'PluginGuard::isDisabled' classes/event/adapter/shopaholic/ProductPageWatcher.php` matches (guard line).
    - Source: `grep -q 'catch (Throwable' classes/event/adapter/shopaholic/ProductPageWatcher.php` matches (Tiger-Style boundary).
    - Source: `grep -q 'public function dispatchForOfferSwitch' classes/event/adapter/shopaholic/ProductPageWatcher.php` matches (offer-switch entry point exposed).
    - Static: `./vendor/bin/phpstan analyse classes/event/adapter/shopaholic/ProductPageWatcher.php --level=10 --no-progress` exits 0.
    - Static: `composer deps` exits 0 (no Lovata-import boundary violation).
  </acceptance_criteria>
  <done>ProductPageWatcher.php on disk; subscribes shopaholic.product.open; dispatches CAPI + pushes collector; exposes dispatchForOfferSwitch entry; Tiger-Style catch in place; phpstan + composer deps clean.</done>
</task>

<task type="auto">
  <name>Task 2: Wire Plugin.php boot — register adapter + subscribe watcher inside isShopaholicEnabled gate</name>
  <files>Plugin.php</files>
  <read_first>
    - Plugin.php (state after plan 06-02 — second cms.page.beforeRenderPage listener already registered; `register()` has new singleton).
    - .planning/phases/06-viewcontent-funnel-shopaholic-pdp/06-RESEARCH.md §10 (boot wiring sketch).
  </read_first>
  <action>
Modify `Plugin.php`:

(a) Add imports (alphabetical insertion in existing `use` block):
- `use Logingrupa\Metapixel\Classes\Adapter\Shopaholic\ShopaholicProductAdapter;` (after existing ShopaholicOrderAdapter import).
- `use Logingrupa\Metapixel\Classes\Event\Adapter\Shopaholic\ProductPageWatcher;` (after existing OrderStatusWatcher import).
- `use Lovata\Shopaholic\Models\Product;` (after existing Lovata\OrdersShopaholic\Models\Order import).

(b) Inside `boot()`'s `isShopaholicEnabled()` branch (lines 75-81):
- Add `$obRegistry->register(Product::class, ShopaholicProductAdapter::class);` immediately after the existing CartPosition registration line.
- Add `Event::subscribe(ProductPageWatcher::class);` immediately after the existing `Event::subscribe(CartPositionWatcher::class);` line.
- Add the inline comment `// One-guard pattern (RESEARCH §10): OrdersShopaholic transitively requires Shopaholic.` directly above the `$obRegistry->register(Product::class, ...)` line OR at the top of the `isShopaholicEnabled()` block (executor's call — keep it tight to the code path it justifies).

Final block shape (text-described, no fenced code):
```
if ($this->isShopaholicEnabled()) {
    // One-guard pattern (RESEARCH §10): OrdersShopaholic transitively requires Shopaholic.
    $obRegistry = App::make(AdapterRegistry::class);
    $obRegistry->register(Order::class, ShopaholicOrderAdapter::class);
    $obRegistry->register(CartPosition::class, ShopaholicCartPositionAdapter::class);
    $obRegistry->register(Product::class, ShopaholicProductAdapter::class);   // <— NEW
    Event::subscribe(OrderStatusWatcher::class);
    Event::subscribe(CartPositionWatcher::class);
    Event::subscribe(ProductPageWatcher::class);                              // <— NEW
}
```

Do NOT split the `isShopaholicEnabled()` gate — keep the single one-guard pattern checking `Lovata.OrdersShopaholic` per RESEARCH §10 recommendation a (OrdersShopaholic transitively requires Shopaholic, so the gate covers Product too).

Composer-dependency-analyser note: `Plugin.php` is on the per-path allowlist for all Lovata packages per `composer-dependency-analyser.php` lines 51-55. Adding `use Lovata\Shopaholic\Models\Product;` to Plugin.php imports stays clean.

PHPStan note: `Plugin.php` is NOT in the disallowIn deny-list scope, so the new imports + Event::subscribe calls do not trip static analysis.
  </action>
  <verify>
    <automated>./vendor/bin/phpstan analyse Plugin.php --level=10 --no-progress &amp;&amp; ./vendor/bin/pest tests/Feature/Plugin/ShopaholicConditionalRegistrationTest.php tests/Feature/Plugin/PluginSanityTest.php --no-coverage</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -q "use Lovata\\\\Shopaholic\\\\Models\\\\Product;" Plugin.php` matches.
    - Source: `grep -q 'ShopaholicProductAdapter::class' Plugin.php` matches.
    - Source: `grep -q "Event::subscribe(ProductPageWatcher::class);" Plugin.php` matches.
    - Source: `grep -q 'One-guard pattern' Plugin.php` matches (the inline comment justifying the single PluginManager check).
    - Source: `grep -c 'Event::subscribe' Plugin.php` returns 4 (OrderStatusWatcher + CartPositionWatcher + ThemeAjaxHandler + ProductPageWatcher).
    - Source: `grep -c '\$obRegistry->register' Plugin.php` returns 3 (Order + CartPosition + Product).
    - Static: `./vendor/bin/phpstan analyse Plugin.php --level=10 --no-progress` exits 0.
    - Static: `composer deps` exits 0.
    - Behavior: `./vendor/bin/pest tests/Feature/Plugin/PluginSanityTest.php` continues to pass (no Plugin.php regression on existing registrations).
    - Behavior: `App::make(AdapterRegistry::class)->resolveByAlias('shopaholic.product')` returns `ShopaholicProductAdapter::class` after Plugin.boot fires (verified indirectly by Task 3 + by plan 06-06 ThemeAjaxHandlerSubjectTypeTest).
  </acceptance_criteria>
  <done>Plugin.php imports + register call + subscribe call added inside isShopaholicEnabled() block; one-guard pattern comment inline; phpstan + composer deps clean; existing Plugin sanity tests still green.</done>
</task>

<task type="auto">
  <name>Task 3: GREEN ProductPageWatcherTest 11-item matrix (all passing) + extend ShopaholicConditionalRegistrationTest VIEW-10</name>
  <files>
    tests/Feature/Adapter/Shopaholic/ProductPageWatcherTest.php,
    tests/Feature/Plugin/ShopaholicConditionalRegistrationTest.php
  </files>
  <read_first>
    - tests/Feature/Adapter/Shopaholic/ProductPageWatcherTest.php (RED stubs from plan 06-01 — 11 stub method names locked).
    - tests/Feature/Adapter/Shopaholic/PurchaseFlowIntegrationTest.php (analog — full Shopaholic integration; Bus::fake harness; Product + Offer fixture builders).
    - tests/Feature/Components/PixelHeadBasePixelTest.php (analog — setUp/tearDown ritual, ThemeEventCollector singleton swap, Settings + PluginGuard reset).
    - tests/Feature/Plugin/ShopaholicConditionalRegistrationTest.php (analog/extension target — current test class covers Order + CartPosition; this task ADDS Product assertions).
    - classes/event/adapter/shopaholic/ProductPageWatcher.php (state after Task 1 — includes the new dispatchForOfferSwitch entry point).
    - Plugin.php (state after Task 2).
    - .planning/briefs/2026-05-27-viewcontent-funnel-shopaholic.md "## Test matrix" (the 11 assertion lines verbatim — item 11 owned by ProductPageWatcherTest per the brief).
    - .planning/ROADMAP.md SC6 ("11 ProductPageWatcher assertions ... all GREEN") — drives the no-skip mandate for this task.
  </read_first>
  <action>
Transform `tests/Feature/Adapter/Shopaholic/ProductPageWatcherTest.php` from RED stubs to 11 GREEN tests — ALL PASSING, no `markTestSkipped`, per ROADMAP SC6. Class-level `#[Group('adapter')]` already present from plan 06-01.

setUp ritual:
```
parent::setUp();
(new \Logingrupa\Metapixel\Updates\CreateMetapixelEventLogTable)->up();
$this->app->singleton(\Logingrupa\Metapixel\Classes\Adapter\Theme\ThemeEventCollector::class);
\Logingrupa\Metapixel\Classes\Helper\PluginGuard::reset();
\Logingrupa\Metapixel\Models\Settings::clearInternalCache();
\Logingrupa\Metapixel\Models\Settings::set(['pixel_id' => 'TEST-PIXEL-1', 'capi_access_token' => 'TEST-TOKEN-1', 'default_currency_code' => 'EUR']);
Bus::fake();
```

tearDown ritual:
```
$this->app->forgetInstance(\Logingrupa\Metapixel\Classes\Adapter\Theme\ThemeEventCollector::class);
\Logingrupa\Metapixel\Classes\Helper\PluginGuard::reset();
(new \Logingrupa\Metapixel\Updates\CreateMetapixelEventLogTable)->down();
parent::tearDown();
```

Fixture helpers (private methods) — same `makeProduct` + `makeOffer` pattern as plan 06-04 Task 3 (forceFill + setRelation manual relations to avoid booting Lovata Shopaholic schema).

GREEN bodies for each of 11 stub methods:

1. `test_viewcontent_dispatches_capi_and_pushes_collector_on_shopaholic_product_open`:
   - Build product with single offer.
   - Instantiate watcher: `$obWatcher = new ProductPageWatcher;`. Call `$obWatcher->handle($obProduct);` directly (no Event::dispatch needed for unit test; the subscribe wiring is covered by ShopaholicConditionalRegistrationTest).
   - Assert: `Bus::assertDispatched(SendCapiEvent::class, fn($obJob) => $obJob->sEventName === 'ViewContent');` AND `App::make(ThemeEventCollector::class)->count() === 1`.

2. `test_does_not_fire_when_plugin_guard_disabled`:
   - Setup Settings with empty pixel_id (or call `PluginGuard::reset()` + simulate disabled via Settings clear).
   - Call `$obWatcher->handle($obProduct);`.
   - Assert `Bus::assertNotDispatched(SendCapiEvent::class)` + collector count zero.

3. `test_does_not_subscribe_when_lovata_shopaholic_absent`:
   - This test belongs in `ShopaholicConditionalRegistrationTest` (boot-time concern, not watcher-instance). Inside the watcher test, replace with `test_is_subscribed_to_shopaholic_product_open_event_handler` — assert via reflection that `(new ProductPageWatcher)->subscribe(...)` registers `'shopaholic.product.open'` listener.
   - The original "missing plugin" assertion lands in ShopaholicConditionalRegistrationTest below.

4. `test_zero_offer_product_resolves_bare_sku_pid`:
   - Product with empty offer collection.
   - Call handle, assert the collector push carries `'content_ids' => ['SKU-{pid}']` (bare).

5. `test_multi_offer_product_resolves_sku_pid_oid_first_active_by_sort_order`:
   - Product with 2 offers (one inactive sort_order 0, one active sort_order 1) — D-10 default-offer = first active by sort_order.
   - Assert collector push content_ids contains `SKU-{pid}-{oid_of_first_active}`.

6. `test_single_offer_product_resolves_bare_sku_pid`:
   - Product with single offer.
   - Assert collector push content_ids = `['SKU-{pid}']`.

7. `test_capi_payload_event_id_matches_collector_pushed_event_id`:
   - Call handle.
   - `$arPushedEvents = App::make(ThemeEventCollector::class)->flush();`
   - `Bus::assertDispatched(SendCapiEvent::class, fn($obJob) => $obJob->arPayload['data'][0]['event_id'] === $arPushedEvents[0]['event_id']);`
   - Tests the dedup contract: server-generated UUIDv4 used for BOTH CAPI payload and the browser fbq's 4th arg.

8. `test_user_data_populated_from_server_and_cookies`:
   - Set `$_SERVER['HTTP_USER_AGENT'] = 'Test/1.0';`, `$_SERVER['REMOTE_ADDR'] = '203.0.113.1';`, `$_COOKIE['_fbp'] = 'fb.1.123.456';`, `$_COOKIE['_fbc'] = 'fb.1.789.abc';`.
   - Call handle.
   - Assert CAPI payload's `data[0]['user_data']` contains all 4 values (client_user_agent + client_ip_address + fbp + fbc).
   - Reset superglobals in tearDown.

9. `test_test_event_code_appears_in_capi_payload_and_collector_event`:
   - `Settings::set([..., 'test_event_code' => 'TEST123'])`.
   - Call handle.
   - Assert CAPI payload top-level has `test_event_code => 'TEST123'` (per Phase 2 SendCapiEvent shape lock — STATE.md).
   - For browser side: in this plan we only verify the collector push includes `event_id`. The actual fbq 4th-arg + test_event_code wiring is in plan 06-02 (PixelHead deferred flush).

10. `test_event_log_race_fence_does_not_block_per_pageload_duplicates`:
    - Call handle twice on the same product in a single test (simulates two PDP renders).
    - Assert: 2 distinct SendCapiEvent dispatches (Bus::assertDispatchedTimes 2), 2 distinct collector pushes.
    - `action_key` per push MUST be unique — `viewcontent:{pid}:{event_id1}` vs `viewcontent:{pid}:{event_id2}` — the event_id UUIDv4 suffix makes the EventLog UNIQUE race-fence happy because (subject_type, subject_id, event_name, channel, site_id) collision-free with different action_keys is NOT the race-fence anchor (the action_key is informational; the UNIQUE is on the 5-column tuple). Per-pageload duplicates ARE expected to fire (D-1: per-pageload AND per-offer-switch). The race-fence inside `EventLogWriter::record` runs on `(subject_type='shopaholic.product', subject_id, event_name='ViewContent', channel='capi', site_id)` — and ALSO collides on per-pageload duplicates. **Document this:** per the brief item 10 "EventLog UNIQUE race-fence does NOT block per-pageload duplicates" implies the watcher fires; the dedup that DOES happen is queue-side. The test asserts: handle() called twice → 2 dispatches; what `EventLogWriter::record` does inside `SendCapiEvent::handle` is queue-side (Bus::fake never runs handle, so the race-fence does not engage in this test). The assertion is "the watcher fires twice", not "the second fire reaches Meta".

11. `test_offer_switch_ajax_re_fires_viewcontent_with_new_event_id_and_offer_sku` (GREEN — REQUIRED per ROADMAP SC6, NOT skipped):
    - Approach: exercise the watcher's `dispatchForOfferSwitch(int, int): string` entry point directly. This invokes the SAME payload-build + collector-push + SendCapiEvent::dispatch chain that `ThemeAjaxHandler::dispatchViaAdapter` will route to in plan 06-06 — without booting October's full request lifecycle.
    - Setup:
      - Build product with 2+ active offers; persist (or stub via the same `makeProduct`/`makeOffer` helpers).
      - Settings::set(['pixel_id' => 'TEST-PIXEL-1', 'capi_access_token' => 'TEST-TOKEN-1', 'default_currency_code' => 'EUR']) (already in setUp).
      - Bus::fake() (already in setUp). Event::fake() additionally if the test wants to verify metapixel.event.after_dispatch fires (optional).
      - Capture the watcher's PDP-render dispatch first so the offer-switch dispatch is the SECOND fire: call `$obWatcher->handle($obProduct);` then capture `$arFirstPushed = App::make(ThemeEventCollector::class)->peek();` (uses the peek method added in plan 06-06 Task 1 — if plan 06-06 has not landed yet, fall back to flush()-then-restore-by-re-pushing). Save first event_id from `$arFirstPushed[0]['event_id']`.
      - Pick the SECOND offer (NOT the default-first-active one): `$iNewOfferId = $obProduct->offer->slice(1, 1)->first()->id;`.
    - Act: `$sNewEventId = $obWatcher->dispatchForOfferSwitch((int) $obProduct->id, $iNewOfferId);`.
    - Assert (each is a hard assertion, not soft):
      - `$sNewEventId !== $arFirstPushed[0]['event_id']` — fresh UUIDv4, NOT reusing the PDP-render event_id.
      - `\Ramsey\Uuid\Uuid::isValid($sNewEventId)` is true (return value is a valid UUIDv4 string).
      - `Bus::assertDispatchedTimes(SendCapiEvent::class, 2);` — 1 PDP-render + 1 offer-switch dispatch.
      - `Bus::assertDispatched(SendCapiEvent::class, fn($obJob) => $obJob->arPayload['data'][0]['event_id'] === $sNewEventId && $obJob->arPayload['data'][0]['custom_data']['content_ids'] === ['SKU-' . $obProduct->id . '-' . $iNewOfferId]);` — second dispatch carries the new event_id AND content_ids ending in the new offer SKU.
      - Collector now has 2 pushes; `App::make(ThemeEventCollector::class)->peek()[1]['action_key']` equals `'viewcontent:' . $obProduct->id . ':' . $iNewOfferId . ':' . $sNewEventId` — canonical wire format `viewcontent:{pid}:{oid}:{eid}` with server-appended event_id.
    - Tiger-Style coverage check (optional but recommended): a sibling test (item 11.b inline within the same test method or a tiny private assertion helper) exercises the disabled-state path — `Settings::set(['pixel_id' => ''])`, then expect `$obWatcher->dispatchForOfferSwitch(...)` throws `\RuntimeException` (matches the throw contract documented in dispatchForOfferSwitch's PHPDoc).
    - Cross-reference note (in a `// ` comment above the test body, not as a fail message): `// Item 11 is owned by ProductPageWatcherTest per the brief test matrix (Section "Test matrix"). The HTTP entry-point coverage of the same code path is in ThemeAjaxHandlerSubjectTypeTest::test_valid_alias_routes_through_registered_adapter_and_dispatches_send_capi_event (plan 06-06).`

Then extend `tests/Feature/Plugin/ShopaholicConditionalRegistrationTest.php` with 2 new test methods covering VIEW-10:

A. `test_product_page_watcher_subscribed_when_lovata_orders_shopaholic_present`:
   - Existing test pattern uses a stubbed PluginManager bound into the container. Reuse setUp.
   - After Plugin.boot fires (which the existing test base already triggers), assert `Event::hasListeners('shopaholic.product.open')` returns true.
   - Assert `App::make(AdapterRegistry::class)->resolveByAlias('shopaholic.product')` returns `ShopaholicProductAdapter::class`.

B. `test_product_page_watcher_NOT_subscribed_when_lovata_orders_shopaholic_absent`:
   - Bind a fake PluginManager returning false for `exists('Lovata.OrdersShopaholic')`. Re-run Plugin.boot in test.
   - Assert `Event::hasListeners('shopaholic.product.open')` returns false.
   - Assert `App::make(AdapterRegistry::class)->resolveByAlias('shopaholic.product')` throws `UnknownSubjectTypeException` (the adapter was never registered).

These two test methods join the existing ShopaholicConditionalRegistrationTest body; no class rename needed.

Drop the `fail('GREEN in plan 06-05 — Task 3')` placeholders from each method.

Composer dep boundary: tests/* IS in the require-dev scope — Lovata imports allowed in any test file per the composer-dependency-analyser tests-as-dev scope (line 16-18 in composer-dependency-analyser.php).

Tiger-Style note: test 2 verifies the disabled-state guard. Test 8 verifies the CapturesRequestUserData trait integration. Both are existing patterns from CartPositionWatcher and PixelHeadBasePixelTest — copy verbatim and adapt.
  </action>
  <verify>
    <automated>./vendor/bin/pest tests/Feature/Adapter/Shopaholic/ProductPageWatcherTest.php tests/Feature/Plugin/ShopaholicConditionalRegistrationTest.php --no-coverage</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c 'public function test_' tests/Feature/Adapter/Shopaholic/ProductPageWatcherTest.php` returns 11.
    - Source: `grep -c 'markTestSkipped' tests/Feature/Adapter/Shopaholic/ProductPageWatcherTest.php` returns 0 — ROADMAP SC6 requires ALL 11 GREEN, no skips.
    - Source: `grep -c 'public function test_' tests/Feature/Plugin/ShopaholicConditionalRegistrationTest.php` returns at least the original count + 2.
    - Behavior: `./vendor/bin/pest tests/Feature/Adapter/Shopaholic/ProductPageWatcherTest.php` exits 0 with 11 PASSING tests (Tests: 11, Failures: 0, Skipped: 0).
    - Behavior: `./vendor/bin/pest tests/Feature/Plugin/ShopaholicConditionalRegistrationTest.php` exits 0 — all tests pass, including 2 new VIEW-10 assertions.
    - Behavior: `./vendor/bin/pest --exclude-group=adapter` does NOT execute these two test files (minimal-install CI cell).
    - Static: `./vendor/bin/phpstan analyse tests/Feature/Adapter/Shopaholic/ProductPageWatcherTest.php --level=10 --no-progress` — if tests are excluded from phpstan paths (per phpstan.neon excludePaths), this command is informational; otherwise exits 0.
    - Behavior: `composer qa` passes with aggregate coverage ≥ 90 % (this plan's new production code has coverage from the GREEN tests).
  </acceptance_criteria>
  <done>11 ProductPageWatcherTest stubs ALL GREEN (Tests: 11, Failures: 0, Skipped: 0 — ROADMAP SC6 satisfied); 2 new ShopaholicConditionalRegistrationTest assertions GREEN for VIEW-10 boot gate; composer qa coverage ≥ 90 %.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Lovata-emitted `shopaholic.product.open` event payload → ProductPageWatcher::handle | Trusted — Lovata's `ProductPage::getElementObject` validates `Product::active()` + `hasRelationWithSite` before firing (RESEARCH §3 invariants 1-5). |
| ProductPageWatcher → SendCapiEvent::dispatch | The dispatched job runs in the queue worker context. Subject (`Product`) serializes via `SerializesModels` — verified backbone-integration test pattern (Phase 2 M-5 lock). |
| ThemeAjaxHandler::dispatchViaAdapter (plan 06-06) → ProductPageWatcher::dispatchForOfferSwitch | Untrusted (POST data crosses); the AJAX boundary validates subject_type alias + subject_id positivity + loadSubject !== null BEFORE calling dispatchForOfferSwitch. dispatchForOfferSwitch re-throws InvalidArgumentException on negative ints (defense-in-depth) and RuntimeException on disabled-state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-6-03 | Denial of Service | ProductPageWatcher catch swallowing real Throwables and silently dropping events → operators see no telemetry, no log | mitigate | The catch logs `Log::warning('metapixel: ProductPageWatcher emission failed', ['meta_pixel.product_id', 'meta_pixel.exception' => get_class(...), 'meta_pixel.message' => ->getMessage()])` with FULL exception context. NEVER throws (page render MUST NOT 500). The catch is intentional and documented — the failure mode is "ViewContent does not fire for this PDP render", not "the page 500s". Operator telemetry surfaces via `Log::warning` to whatever log driver Laravel is configured for (file / syslog / log-stack). T-6-03 mitigation is the explicit Log::warning + context array (vs. a silent `catch {}` that the threat model warned against). |
| T-6-W5-T | Tampering | Third-party listener subscribing to `shopaholic.product.open` BEFORE ProductPageWatcher and short-circuiting the dispatch chain | accept | RESEARCH §1 invariant 5: `Event::fire('shopaholic.product.open', [$obElement])` is `$halt = false` (3rd arg omitted). Listener return values are ignored; halt semantics DO NOT apply. A third-party listener cannot prevent ProductPageWatcher from firing. Documented invariant. |
| T-6-W5-S | Spoofing | Third-party plugin re-firing `shopaholic.product.open` from a non-PDP context (e.g. cart bonus-box) → spurious ViewContent | accept | The watcher contract is: `shopaholic.product.open` semantically MEANS "PDP rendered". If a third party fires it elsewhere, they own the false positive — operator-side concern. Soft-gate via `window.__metapixelProduct` (plan 06-06) covers the browser-side spam from cart-modal selectors. Plugin core does not gate the event-source. |
</threat_model>

<verification>
- `./vendor/bin/pest tests/Feature/Adapter/Shopaholic/ProductPageWatcherTest.php --no-coverage` — 11 PASSING (no skips per ROADMAP SC6).
- `./vendor/bin/pest tests/Feature/Plugin/ShopaholicConditionalRegistrationTest.php --no-coverage` — all tests passing (existing + 2 new VIEW-10 assertions).
- `./vendor/bin/phpstan analyse classes/event/adapter/shopaholic/ProductPageWatcher.php Plugin.php --level=10 --no-progress` — exits 0.
- `composer deps` (composer-dependency-analyser) — exits 0.
- `composer qa` — exits 0 with aggregate coverage ≥ 90 %.
- Manual: at this point, when Lovata's `Event::fire('shopaholic.product.open', [$obProduct])` triggers in the live request, the full chain fires (collector pushed + CAPI dispatched).
</verification>

<success_criteria>
- ProductPageWatcher.php shipped under classes/event/adapter/shopaholic/.
- Watcher subscribes shopaholic.product.open; dispatches ViewContent CAPI + pushes ThemeEventCollector with product_id key.
- Watcher exposes dispatchForOfferSwitch(int, int): string entry point reused by plan 06-06's AJAX boundary.
- Tiger-Style boundary catch logs + skips on Throwable in handle() (T-6-03 mitigation).
- Plugin.php boot registers adapter + subscribes watcher inside isShopaholicEnabled() gate with inline one-guard comment.
- 11 ProductPageWatcherTest brief-matrix items ALL GREEN (ROADMAP SC6 satisfied — no skips).
- VIEW-10 gate covered by ShopaholicConditionalRegistrationTest (2 new assertions).
- composer qa green; coverage ≥ 90 %.
</success_criteria>

<output>
Create `.planning/phases/06-viewcontent-funnel-shopaholic-pdp/06-05-SUMMARY.md`: 1 production file + 1 Plugin.php delta + 2 test files updated; REQ closed (VIEW-04, VIEW-10); T-6-03 mitigation status; PluginManager one-guard pattern lock; collector push 9-key shape excerpt; offer-switch entry-point contract; unblock list (Plan 06 — ProductPixel + ThemeAjaxHandler hybrid path now has the collector + adapter + dispatchForOfferSwitch dependencies live).
</output>
</output>
