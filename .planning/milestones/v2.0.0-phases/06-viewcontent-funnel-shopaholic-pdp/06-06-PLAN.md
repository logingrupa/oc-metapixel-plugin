---
phase: 06-viewcontent-funnel-shopaholic-pdp
plan: 06
type: execute
wave: 5
depends_on:
  - 06-02
  - 06-03
  - 06-04
  - 06-05
files_modified:
  - components/ProductPixel.php
  - components/productpixel/default.htm
  - classes/adapter/theme/ThemeAjaxHandler.php
  - Plugin.php
  - tests/Feature/Components/ProductPixelTest.php
  - tests/Feature/Adapter/Theme/ThemeAjaxHandlerSubjectTypeTest.php
autonomous: true
requirements:
  - VIEW-05
  - VIEW-06
  - VIEW-09
must_haves:
  truths:
    - "ProductPixel component (alias [productPixel]) renders the offer-switch JS via $this->page['productPixelOfferSwitchJs'] and the window.__metapixelProduct global via $this->page['productPixelProductGlobalJs'] — both null when PluginGuard::isDisabled() OR when ThemeEventCollector has no product_id push."
    - "The inline JS is idempotent (window.__metapixelProductPixelInit flag), soft-gated by window.__metapixelProduct.id (T-6-06 mitigation — cart-modal [name=offer_id] from cart-position-list-present-box.htm cannot trigger ViewContent), delegated change-listener on document for [name=offer_id], posts via jax.ajax('Metapixel::onFireEvent', { data: {name, subject_type:'shopaholic.product', subject_id, offer_id, action_key} })."
    - "Wire-format action_key sent from JS is 'viewcontent:{pid}:{oid}' (no event_id suffix — UUIDv4 is unavailable at JS send time because it is server-generated). ThemeAjaxHandler::dispatchViaAdapter appends the server-generated event_id to the wire-format action_key BEFORE EventLog insert, producing canonical 'viewcontent:{pid}:{oid}:{eid}' per CONTEXT.md Claude's Discretion (D-3 anchor extension)."
    - "ThemeAjaxHandler::onBeforeRun detects the optional subject_type field BEFORE the existing ThemeActionEvent path. When present: resolves via AdapterRegistry::resolveByAlias (catches UnknownSubjectTypeException → 422 unknown subject_type), validates instanceof SupportsHybridAjax (→ 422 if not), validates subject_id > 0 (→ 422 invalid), calls loadSubject (→ 404 if null), delegates to ProductPageWatcher::dispatchForOfferSwitch (plan 06-05) for the ViewContent payload + event_id generation, appends event_id to wire-format action_key. When absent: existing ThemeActionEvent path unchanged."
    - "Plugin.php registerComponents() now returns 3 entries (EventPixel + PixelHead + ProductPixel)."
    - "ProductPixelTest 4 GREEN tests cover the script render shape + disabled state + collector-empty case. ThemeAjaxHandlerSubjectTypeTest 5 GREEN tests cover unknown-alias-422, valid-routes-adapter, no-hybrid-support-422, invalid-subject-id-422, loadSubject-null-404."
  artifacts:
    - path: "components/ProductPixel.php"
      provides: "PDP-level component; offer-switch JS + product_id global emitter; PluginGuard-gated"
      contains: "final class ProductPixel"
    - path: "components/productpixel/default.htm"
      provides: "Twig partial — conditionally renders productPixelProductGlobalJs + productPixelOfferSwitchJs"
      contains: "productPixelOfferSwitchJs"
    - path: "classes/adapter/theme/ThemeAjaxHandler.php"
      provides: "Extended onBeforeRun — hybrid subject_type routing via AdapterRegistry::resolveByAlias + SupportsHybridAjax loadSubject; appends event_id to wire-format action_key before EventLog insert"
      contains: "resolveByAlias"
    - path: "Plugin.php"
      provides: "registerComponents() returns the new ProductPixel alias"
      contains: "'productPixel'"
  key_links:
    - from: "components/ProductPixel.php"
      to: "classes/helper/PluginGuard.php"
      via: "if (PluginGuard::isDisabled()) return;"
      pattern: "PluginGuard::isDisabled"
    - from: "components/ProductPixel.php"
      to: "classes/adapter/theme/ThemeEventCollector.php"
      via: "App::make(ThemeEventCollector::class) peek for product_id"
      pattern: "ThemeEventCollector"
    - from: "classes/adapter/theme/ThemeAjaxHandler.php"
      to: "classes/adapter/AdapterRegistry.php"
      via: "App::make(AdapterRegistry::class)->resolveByAlias($sSubjectType)"
      pattern: "resolveByAlias"
    - from: "classes/adapter/theme/ThemeAjaxHandler.php"
      to: "classes/adapter/SupportsHybridAjax.php"
      via: "instanceof SupportsHybridAjax check + ->loadSubject($iId, $arContext) call"
      pattern: "SupportsHybridAjax"
---

<objective>
Ship `ProductPixel` component (browser-side fbq + offer-switch JS injector) and extend `ThemeAjaxHandler::onBeforeRun` with the hybrid `subject_type`-routing branch that powers the offer-switch AJAX call.

Purpose: Phase 6 needs zero theme code from operators. The component renders inline JS + window-global on the PDP, and the AJAX handler routes offer-switch POSTs through the registered adapter using the alias allowlist from plan 06-03. Together they close the offer-switch loop (DOM change → AJAX → adapter dispatch → fbq script injection on response).

Output:
- `components/ProductPixel.php` (NEW) — ComponentBase subclass, alias `[productPixel]`, vendor-neutral name per D-8.
- `components/productpixel/default.htm` (NEW) — Twig partial rendering the two Twig page vars conditionally.
- `classes/adapter/theme/ThemeAjaxHandler.php` (MODIFIED) — `onBeforeRun` detects optional `subject_type` field; delegates to new private `dispatchViaAdapter` method.
- `Plugin.php` (MODIFIED) — `registerComponents()` returns `[EventPixel => 'eventPixel', PixelHead => 'pixelHead', ProductPixel => 'productPixel']`.
- `tests/Feature/Components/ProductPixelTest.php` (RED → GREEN, 4 tests).
- `tests/Feature/Adapter/Theme/ThemeAjaxHandlerSubjectTypeTest.php` (RED → GREEN, 5 tests).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-viewcontent-funnel-shopaholic-pdp/06-CONTEXT.md
@.planning/phases/06-viewcontent-funnel-shopaholic-pdp/06-RESEARCH.md
@.planning/phases/06-viewcontent-funnel-shopaholic-pdp/06-PATTERNS.md
@.planning/phases/06-viewcontent-funnel-shopaholic-pdp/06-05-SUMMARY.md

@components/EventPixel.php
@components/PixelHead.php
@components/pixelhead/default.htm
@classes/adapter/theme/ThemeAjaxHandler.php
@classes/adapter/theme/ThemeActionAdapter.php
@classes/adapter/theme/ThemeActionEvent.php
@classes/adapter/theme/ThemeActionValueResolver.php
@classes/adapter/theme/ThemeEventCollector.php
@classes/adapter/AdapterRegistry.php
@classes/adapter/SupportsHybridAjax.php
@classes/exception/UnknownSubjectTypeException.php
@tests/Feature/Adapter/Theme/ThemeAjaxHandlerAllowlistTest.php
</context>

<interface_context>
**`ProductPixel` component shape:**

- Alias `productPixel`.
- `componentDetails` returns `['name' => 'ProductPixel', 'description' => 'PDP-level Meta Pixel ViewContent + offer-switch trigger. Place inside the layout/page hosting Shopaholic [ProductPage].']`.
- `defineProperties` returns `[]` (zero properties — operator-zero-config).
- `onRun()` peeks (does NOT flush) `ThemeEventCollector` for an event whose `product_id` key is set. When present + `PluginGuard::isDisabled()` is false: emits `$this->page['productPixelProductGlobalJs']` = `<script>window.__metapixelProduct={id:N};</script>` (JS-safe encoded). Emits `$this->page['productPixelOfferSwitchJs']` = the inline JS string (RESEARCH §7 + Example 2).
- When disabled OR collector has no product_id push: both Twig vars remain null.
- "Peek without flush" — read but do not consume. The collector flush is owned by PixelHead's deferred-flush listener (plan 06-02). ProductPixel must NOT call `flush()` else the events disappear from the buffer before PixelHead drains them. Implementation: extend `ThemeEventCollector` with a non-mutating `peekProductId(): ?int` helper, OR iterate via `count() + push history` — NO, simpler: add a `peek(): list<array<string, mixed>>` method that returns the array without resetting. **Decision: add `ThemeEventCollector::peek(): array` non-mutating method in Task 2 (alongside the existing `flush()`).**

**JS contract** (per RESEARCH §7 Example 2 + Pitfall 8 soft-gate):
- Idempotency: `if (window.__metapixelProductPixelInit) return; window.__metapixelProductPixelInit = true;`
- Soft-gate: `if (!window.__metapixelProduct || !window.__metapixelProduct.id) return;` (BEFORE the `el.name !== 'offer_id'` check). This bypasses cart-modal `[name="offer_id"]` outside PDP (T-6-06 mitigation).
- Delegated `change` listener on `document` matching `[name="offer_id"]`.
- Posts via `jax.ajax('Metapixel::onFireEvent', { data: { name: 'ViewContent', subject_type: 'shopaholic.product', subject_id: iProductId, offer_id: iOfferId, action_key: 'viewcontent:' + iProductId + ':' + iOfferId } })`.
- **Wire-format action_key shape note:** the JS sends `'viewcontent:' + iProductId + ':' + iOfferId` — no event_id suffix — because the canonical `event_id` is a server-generated UUIDv4 (the dedup-contract anchor; never reverse the direction per CONTEXT.md locked decision). The server (`ThemeAjaxHandler::dispatchViaAdapter`) appends the freshly-minted event_id to the received action_key BEFORE writing the EventLog row, producing the final canonical shape `viewcontent:{pid}:{oid}:{eid}` per CONTEXT.md Claude's Discretion. The JS half of the action_key is informational/observability; the server-appended event_id is the per-request uniqueness anchor.
- Success callback: parses `oResp.script` if present and appends to body (RESEARCH Example 2 lines 968-973).

**`ThemeAjaxHandler::onBeforeRun` hybrid branch** (per RESEARCH §9):
After the existing allowlist + rate-limit checks, BEFORE the existing `ThemeActionEvent::fromArray($arData)` instantiation:

- `$mSubjectType = $arData['subject_type'] ?? null;`
- When `is_string && !== ''`: return `$this->dispatchViaAdapter($arData, $sEventId, $iEventTime);` (NEW private method).
- When absent: existing path unchanged.

`dispatchViaAdapter` body (high-level):
- Try `$sAdapterClass = App::make(AdapterRegistry::class)->resolveByAlias((string) $arData['subject_type']);` — catch `UnknownSubjectTypeException` → return `JsonResponse(['error' => 'unknown subject_type'], 422)`.
- `$obAdapter = App::make($sAdapterClass);`.
- Check `instanceof SupportsHybridAjax` — else return 422 `'subject_type does not support hybrid AJAX'`.
- Read `$arData['subject_id']`: `is_numeric` → `(int)` cast. If `<= 0` return 422 `'invalid subject_id'`.
- Build context: `$arContext = is_array($arData['context'] ?? null) ? $arData['context'] : [];` then for each key in `['offer_id']` if `isset($arData[$sKey])` add to `$arContext[$sKey] = $arData[$sKey]`.
- `$obSubject = $obAdapter->loadSubject($iSubjectId, $arContext);` — null → return 404 `'subject not found'`.
- For the `shopaholic.product` alias specifically, delegate to `App::make(ProductPageWatcher::class)->dispatchForOfferSwitch($iSubjectId, (int) $arData['offer_id'])` (plan 06-05 entry point) — returns the server-generated `$sEventId`. This keeps the ViewContent payload contract owned by ProductPageWatcher and avoids duplicating the payload-build logic. For other registered hybrid aliases, fall back to the generic `(new PayloadBuilder(new UserDataHasher))->buildEventPayload(...)` + `SendCapiEvent::dispatch(...)` path.
- **Append event_id to action_key BEFORE EventLog write:** read `$sWireActionKey = (string) ($arData['action_key'] ?? '');` from the POST. Compute `$sFinalActionKey = $sWireActionKey === '' ? '' : $sWireActionKey . ':' . $sEventId;`. Pass `$sFinalActionKey` as the `action_key` value in the collector push and in any direct `EventLogWriter::record(...)` call. For the `shopaholic.product` alias, the watcher's `dispatchForOfferSwitch` already builds its own canonical `viewcontent:{pid}:{oid}:{eid}` action_key internally — in that branch, the handler skips the wire-format append step (the watcher owns it). The append step applies to OTHER hybrid aliases (e.g. future `mall.product`) where the AJAX boundary is the action_key authority.
- Build script `<script>fbq("track", "ViewContent", {}, {eventID: "..."});</script>` using `JSON_HEX_*` mask (same as existing handler line 102).
- Return `JsonResponse(['event_id' => $sEventId, 'script' => $sScript])`.

The OUTER try/catch around `onBeforeRun` (existing lines 109-116) already catches `Throwable` → 500 with masked message. UnknownSubjectTypeException is caught INSIDE dispatchViaAdapter so it returns a typed 422 rather than the generic 500.
</interface_context>

<tasks>

<task type="auto">
  <name>Task 1: Create ProductPixel component + default.htm Twig partial + add ThemeEventCollector::peek</name>
  <files>
    components/ProductPixel.php,
    components/productpixel/default.htm,
    classes/adapter/theme/ThemeEventCollector.php
  </files>
  <read_first>
    - components/EventPixel.php (analog header — `final class` + componentDetails shape; JS const at line 24).
    - components/PixelHead.php (analog — PluginGuard gate pattern at lines 74-76; JSON_HEX_* const at line 41; emit shape at lines 200-202).
    - components/pixelhead/default.htm (analog Twig partial).
    - classes/adapter/theme/ThemeEventCollector.php (current state — current methods push/pushEvent/flush/count; this task adds peek).
    - classes/helper/PluginGuard.php (`isDisabled(): bool` static signature).
    - .planning/phases/06-viewcontent-funnel-shopaholic-pdp/06-RESEARCH.md §7 (ProductPixel shape) + §8 (idempotency + soft-gate) + Example 2 (JS template at L946-977) + Pitfall 8 (soft-gate via window.__metapixelProduct).
    - .planning/phases/06-viewcontent-funnel-shopaholic-pdp/06-PATTERNS.md §components/ProductPixel.php + §components/productpixel/default.htm.
  </read_first>
  <action>
**Action_key contract note (read before implementing the JS):** the JS-side `action_key` value posted to the server is `'viewcontent:' + iProductId + ':' + iOfferId` — TWO colon-separated segments after the literal prefix, NO `event_id` suffix. The reason this departs from the canonical four-segment shape `viewcontent:{pid}:{oid}:{eid}` documented in CONTEXT.md Claude's Discretion is that the `event_id` is a server-generated UUIDv4 (the dedup-contract anchor; per the locked decision the direction is server → frontend only and never reversed). At JS send-time the UUID does not yet exist. The server (`ThemeAjaxHandler::onBeforeRun` → `dispatchViaAdapter` in Task 2) generates the UUIDv4 and APPENDS it to the received action_key BEFORE the EventLog insert, producing the final canonical four-segment `viewcontent:{pid}:{oid}:{eid}` shape that satisfies CONTEXT.md's discretion contract. Do not change the JS to call an extra round-trip to fetch the event_id first — that would double the request count and serves no dedup purpose.

**(a) Extend `ThemeEventCollector.php`** — add a non-mutating `peek` method:
- Add `public function peek(): array { return $this->arEvents; }` (with `@return list<array<string, mixed>>` PHPDoc, mirroring `flush`'s return type). Note: returns the live array — caller MUST treat as read-only. Document in PHPDoc: `Returns the current accumulated events WITHOUT consuming or resetting. Distinguishes "I want to look" (ProductPixel onRun reads product_id for window.__metapixelProduct global) from "I want to consume" (PixelHead deferred-flush drains). Multiple readers safe; no need for defensive copy because callers iterate read-only.`

**(b) Create `components/ProductPixel.php`** — namespace `Logingrupa\Metapixel\Components`. Imports:
- `Cms\Classes\ComponentBase`
- `Illuminate\Support\Facades\App`
- `Logingrupa\Metapixel\Classes\Adapter\Theme\ThemeEventCollector`
- `Logingrupa\Metapixel\Classes\Helper\PluginGuard`

`final class ProductPixel extends ComponentBase`. Class-level PHPDoc: `PDP-level Meta Pixel browser layer for Phase 6 ViewContent funnel. Renders two inline <script> blocks via Twig page vars: (1) productPixelProductGlobalJs — window.__metapixelProduct = {id: N} server-injected global, sourced from ThemeEventCollector's most recent product_id push by ProductPageWatcher; (2) productPixelOfferSwitchJs — delegated change-listener on document for [name="offer_id"] that posts to Metapixel::onFireEvent (hybrid alias subject_type='shopaholic.product') and injects the returned script. Disabled-state contract: PluginGuard::isDisabled() OR no product_id in collector → both Twig vars null, default.htm renders nothing.`

Private const `JS = JSON_HEX_TAG | JSON_HEX_QUOT | JSON_HEX_AMP | JSON_HEX_APOS;` (same mask as PixelHead).

Methods:

- `componentDetails(): array` returns `['name' => 'ProductPixel', 'description' => 'PDP-level Meta Pixel ViewContent + offer-switch trigger. Place inside the layout/page hosting Shopaholic [ProductPage].']` (`@return array{name: string, description: string}` PHPDoc).

- `defineProperties(): array` returns `[]` (`@return array<string, array<string, mixed>>` PHPDoc).

- `onRun(): void`:
  - `$this->page['productPixelProductGlobalJs'] = null;`
  - `$this->page['productPixelOfferSwitchJs'] = null;`
  - `if (PluginGuard::isDisabled()) { return; }`
  - `/** @var ThemeEventCollector $obCollector */ $obCollector = App::make(ThemeEventCollector::class);`
  - Iterate `$obCollector->peek()` looking for first event whose `'product_id'` key is `is_numeric` and positive. Save into `$iProductId`. If none, set `$this->page['productPixelOfferSwitchJs'] = $this->buildOfferSwitchJs();` (the JS is rendered EVEN WITHOUT a product context — Pitfall 8 soft-gate inside the JS itself handles the "no product" case at runtime). Actually wait — see the next clause.
  - **Refinement**: per Pitfall 8 + D-9, the JS IS soft-gated by `window.__metapixelProduct`. If `__metapixelProduct` is null, the JS no-ops. So always rendering the JS is safe + simpler. Two cases:
    1. Product context present: render BOTH globals.
    2. Product context absent (PDP not rendered, ProductPixel placed on a layout shared with non-PDP pages): render JS (it self-no-ops) + leave product-global null.
  - Better: ALWAYS render the JS string when not disabled; render the product-global ONLY when collector has product_id. This way the offer-switch JS attaches once-per-document but no-ops without the soft-gate match.
  - Final shape:
    - After PluginGuard check, set `$this->page['productPixelOfferSwitchJs'] = $this->buildOfferSwitchJs();` (always, when not disabled).
    - Iterate `$obCollector->peek()` for `product_id`. When found: `$this->page['productPixelProductGlobalJs'] = sprintf('<script>window.__metapixelProduct={id:%d};</script>', $iProductId);`. (Direct sprintf is safe because the value is cast `(int)` server-side — no XSS surface; document this in inline comment.)
  - Method returns void.

- `private function buildOfferSwitchJs(): string`:
  - Returns the heredoc/nowdoc-quoted JS string per RESEARCH Example 2 verbatim (idempotency flag + soft-gate + delegated change listener + jax.ajax + script-injection success callback). Wrap in `<script>...</script>` tags. Use `<<<JS ... JS;` heredoc syntax — single-quoted nowdoc `<<<'JS'` is BETTER because the JS body contains no PHP interpolation and nowdoc prevents accidental `$` interpolation. **Use `<<<'JS'` nowdoc.**

The JS body (verbatim from RESEARCH Example 2 lines 946-977 — note the two-segment action_key per the contract note at the top of this action):
```
<script>
(function () {
    if (window.__metapixelProductPixelInit) return;
    window.__metapixelProductPixelInit = true;
    document.addEventListener('change', function (ev) {
        if (!window.__metapixelProduct || !window.__metapixelProduct.id) return;
        var el = ev.target;
        if (!el || el.name !== 'offer_id') return;
        var iProductId = parseInt(window.__metapixelProduct.id, 10);
        var iOfferId   = parseInt(el.value || '0', 10);
        if (!iProductId || !iOfferId) return;
        jax.ajax('Metapixel::onFireEvent', { data: {
            name: 'ViewContent',
            subject_type: 'shopaholic.product',
            subject_id: iProductId,
            offer_id: iOfferId,
            action_key: 'viewcontent:' + iProductId + ':' + iOfferId
        }}).done(function (oResp) {
            if (oResp && oResp.script) {
                var oTmp = document.createElement('div');
                oTmp.innerHTML = oResp.script;
                if (oTmp.firstChild) document.body.appendChild(oTmp.firstChild);
            }
        });
    }, false);
})();
</script>
```

Note on `jax.ajax(...).done()` — RESEARCH A5 flags this as ASSUMED. Spot-check: read one existing theme partial using Larajax (e.g. `themes/logingrupa-naisstore/partials/...`) to confirm `.done()` callback chain works in this Larajax version. If the project uses an alternative success-callback shape (e.g. Larajax-style `success` option in the second arg), adjust the JS accordingly. Document the choice in inline comment.

**(c) Create `components/productpixel/default.htm`** — Twig partial. Body (verbatim from RESEARCH §7 + PATTERNS.md §components/productpixel/default.htm):
```
{% if productPixelProductGlobalJs %}
{{ productPixelProductGlobalJs|raw }}
{% endif %}
{% if productPixelOfferSwitchJs %}
{{ productPixelOfferSwitchJs|raw }}
{% endif %}
```

Two conditional blocks, both `|raw` (server-side JSON-escaped content). Filename MUST be `default.htm` lowercase under `components/productpixel/` directory.

Composer dep boundary: `components/` is NOT in the Lovata-import allowlist — ProductPixel MUST NOT import any Lovata\* class. The component reads `product_id` from the collector (an integer pushed by the watcher) — no Lovata model touched here.

PHP 8.3+8.4: no 8.4-only syntax. `sprintf('<script>window.__metapixelProduct={id:%d};</script>', $iProductId)` is universally compatible.
  </action>
  <verify>
    <automated>./vendor/bin/phpstan analyse components/ProductPixel.php classes/adapter/theme/ThemeEventCollector.php --level=10 --no-progress</automated>
  </verify>
  <acceptance_criteria>
    - Source: `components/ProductPixel.php` exists; `grep -q 'final class ProductPixel extends ComponentBase' components/ProductPixel.php` matches.
    - Source: `components/productpixel/default.htm` exists; `grep -q 'productPixelOfferSwitchJs|raw' components/productpixel/default.htm` matches.
    - Source: `grep -q 'public function peek' classes/adapter/theme/ThemeEventCollector.php` matches.
    - Source: `grep -q 'window.__metapixelProductPixelInit' components/ProductPixel.php` matches (idempotency flag in JS body).
    - Source: `grep -q "!window.__metapixelProduct" components/ProductPixel.php` matches (soft-gate in JS body — Pitfall 8 / T-6-06).
    - Source: `grep -q "subject_type: 'shopaholic.product'" components/ProductPixel.php` matches (alias literal in JS).
    - Source: `grep -q "action_key: 'viewcontent:' + iProductId + ':' + iOfferId" components/ProductPixel.php` matches (two-segment wire-format action_key — server appends event_id).
    - Source: `grep -q 'window.__metapixelProduct={id:%d}' components/ProductPixel.php` matches (sprintf shape for global).
    - Static: `./vendor/bin/phpstan analyse components/ProductPixel.php classes/adapter/theme/ThemeEventCollector.php --level=10 --no-progress` exits 0.
    - Behavior: existing `tests/Feature/Adapter/Theme/ThemeMarkupTagsTwigTest.php` (or similar) still passes — ThemeEventCollector peek does not break existing flush behavior.
  </acceptance_criteria>
  <done>ProductPixel.php + default.htm shipped; ThemeEventCollector gains peek(); JS body verbatim from RESEARCH Example 2 (two-segment wire-format action_key — server appends event_id per CONTEXT.md discretion); phpstan clean.</done>
</task>

<task type="auto">
  <name>Task 2: Extend ThemeAjaxHandler::onBeforeRun with hybrid subject_type branch + register ProductPixel in Plugin.php</name>
  <files>
    classes/adapter/theme/ThemeAjaxHandler.php,
    Plugin.php
  </files>
  <read_first>
    - classes/adapter/theme/ThemeAjaxHandler.php (full file — current onBeforeRun at lines 63-117; allowlist + rate-limit + ThemeActionEvent path).
    - classes/adapter/theme/ThemeActionAdapter.php (analog for value-resolver pattern).
    - classes/adapter/AdapterRegistry.php (state after plan 06-03 — resolveByAlias method).
    - classes/adapter/SupportsHybridAjax.php (interface from plan 06-03).
    - classes/exception/UnknownSubjectTypeException.php (exception from plan 06-03).
    - classes/event/adapter/shopaholic/ProductPageWatcher.php (state after plan 06-05 — exposes dispatchForOfferSwitch(int, int): string entry point).
    - classes/meta/PayloadBuilder.php (signature confirm).
    - Plugin.php (state after plan 06-05 — registerComponents has 2 entries: EventPixel + PixelHead).
    - .planning/phases/06-viewcontent-funnel-shopaholic-pdp/06-RESEARCH.md §9 (dispatchViaAdapter sketch + 5-row status code matrix).
    - .planning/phases/06-viewcontent-funnel-shopaholic-pdp/06-PATTERNS.md §ThemeAjaxHandler delta + status-code matrix.
  </read_first>
  <action>
**(a) Modify `classes/adapter/theme/ThemeAjaxHandler.php`:**

Add imports (alphabetical insertion):
- `use Logingrupa\Metapixel\Classes\Adapter\AdapterRegistry;`
- `use Logingrupa\Metapixel\Classes\Adapter\EventSubjectAdapter;`
- `use Logingrupa\Metapixel\Classes\Adapter\SupportsHybridAjax;`
- `use Logingrupa\Metapixel\Classes\Adapter\Shopaholic\ShopaholicProductAdapter;`
- `use Logingrupa\Metapixel\Classes\Event\Adapter\Shopaholic\ProductPageWatcher;`
- `use Logingrupa\Metapixel\Classes\Exception\UnknownSubjectTypeException;`

Inside `onBeforeRun` body — AFTER the rate-limit check (line 79) and BEFORE the `try { $obEvent = ThemeActionEvent::fromArray($arData); }` block (line 80), insert the hybrid detection branch:

```
$mSubjectType = $arData['subject_type'] ?? null;
if (is_string($mSubjectType) && $mSubjectType !== '') {
    return $this->dispatchViaAdapter($arData, $mSubjectType);
}
```

(`$sEventId` + `$iEventTime` generation MOVES inside each branch since the existing path generates them at line 89 — keep that scope.)

Add new private method `dispatchViaAdapter(array $arData, string $sSubjectType): JsonResponse`:

Body — described, not code-fenced:
1. Try-catch around the alias resolve: `$sAdapterClass = App::make(AdapterRegistry::class)->resolveByAlias($sSubjectType);` — catch `UnknownSubjectTypeException $obException` → return `new JsonResponse(['error' => 'unknown subject_type'], 422)`.
2. `$obAdapter = App::make($sAdapterClass);` (the alias-index already verified subclass-of-EventSubjectAdapter at register-time).
3. `if (! $obAdapter instanceof SupportsHybridAjax) { return new JsonResponse(['error' => 'subject_type does not support hybrid AJAX'], 422); }`
4. Validate subject_id: `$mSubjectId = $arData['subject_id'] ?? 0; $iSubjectId = is_numeric($mSubjectId) ? (int) $mSubjectId : 0;` — if `<= 0` return `new JsonResponse(['error' => 'invalid subject_id'], 422)`.
5. Build context: `$arContext = is_array($arData['context'] ?? null) ? $arData['context'] : [];` then for each extra key in the array `['offer_id']`, `if (isset($arData[$sExtra])) { $arContext[$sExtra] = $arData[$sExtra]; }`.
6. `$obSubject = $obAdapter->loadSubject($iSubjectId, $arContext);` — null returns `new JsonResponse(['error' => 'subject not found'], 404)`.
7. Validate name: `$mName = $arData['name'] ?? '';` — if `! is_string($mName) || $mName === '' || ! $this->isAllowedEventName($mName)` return `new JsonResponse(['error' => 'event_name not allowed'], 422)`. (Defense-in-depth — the outer onBeforeRun already validated, but the hybrid branch enforces independently in case future refactors split the validation.)
8. **Shopaholic-product branch (delegated event_id generation + canonical action_key shape):** when `$sAdapterClass === ShopaholicProductAdapter::class`, validate `$mOfferId = $arData['offer_id'] ?? 0; $iOfferId = is_numeric($mOfferId) ? (int) $mOfferId : 0;`. If `<= 0`, return 422 `'invalid offer_id'`. Then call `$sEventId = App::make(ProductPageWatcher::class)->dispatchForOfferSwitch($iSubjectId, $iOfferId);` — the watcher owns event_id generation, payload build, collector push (with its canonical four-segment `viewcontent:{pid}:{oid}:{eid}` action_key), and SendCapiEvent::dispatch. `$iEventTime = time();` recorded only for the response shape (the watcher's payload uses its own time()). Skip the wire-format-append step (the watcher already produced the canonical action_key).
9. **Generic hybrid branch (for future aliases that do NOT delegate to a dedicated watcher):** `$sEventId = Uuid::uuid4()->toString();` + `$iEventTime = time();`. Build payload via `(new PayloadBuilder(new UserDataHasher))->buildEventPayload($mName, $obAdapter, $obSubject, $obAdapter->getValueResolver($obSubject), $sEventId, $iEventTime, []);`. Then: read `$sWireActionKey = is_string($arData['action_key'] ?? null) ? $arData['action_key'] : '';`. Compute `$sFinalActionKey = $sWireActionKey === '' ? '' : $sWireActionKey . ':' . $sEventId;` — this is the WARN-1 server-side append step that converts the JS wire-format action_key into the canonical four-segment shape per CONTEXT.md Claude's Discretion. Inject `$sFinalActionKey` into the collector push (if the generic branch pushes — currently it does not, but document the contract for future aliases). Then `SendCapiEvent::dispatch($mName, $arPayload, $obSubject, $sAdapterClass);`.
10. Build script string: `$sScript = sprintf('<script>fbq("track", %s, {}, {eventID: %s});</script>', (string) json_encode($mName, JSON_HEX_TAG | JSON_HEX_QUOT | JSON_HEX_AMP | JSON_HEX_APOS), (string) json_encode($sEventId, ...));` — same mask as existing line 102.
11. Return `new JsonResponse(['event_id' => $sEventId, 'script' => $sScript]);`

PHPDoc on `dispatchViaAdapter`:
> `Hybrid AJAX path — routes a subject_type alias through AdapterRegistry to the registered adapter's loadSubject + getValueResolver. The alias is the ONLY untrusted string accepted from JS; it is bounded to the register-time alias index, so no FQN injection is possible. Adapters MUST implement SupportsHybridAjax to opt in (T-6-04 mitigation). Adapters MUST re-enforce subject's domain guards inside loadSubject (active/site/SoftDelete) — T-6-05 mitigation. For the shopaholic.product alias the handler delegates to ProductPageWatcher::dispatchForOfferSwitch which owns the canonical viewcontent:{pid}:{oid}:{eid} action_key shape. For generic hybrid aliases the handler appends the server-generated event_id to the wire-format action_key BEFORE dispatch (per CONTEXT.md Claude's Discretion: JS sends viewcontent:{pid}:{oid}, server stores viewcontent:{pid}:{oid}:{eid}).`

Outer try/catch on `onBeforeRun` (lines 109-116) STAYS — it catches any Throwable not handled by `dispatchViaAdapter`'s typed catches. Note: `ProductPageWatcher::dispatchForOfferSwitch` throws `\InvalidArgumentException` (on negative ints) and `\RuntimeException` (on disabled-state or null loadSubject) — both surface to the outer handler's 500 path with masked message. Disabled-state is acceptable as 500 because the JS soft-gate makes it unreachable in practice; null loadSubject was already converted to 404 in step 6 above (handler-side loadSubject runs BEFORE the dispatchForOfferSwitch call).

Composer dep boundary: `classes/adapter/theme/` is NOT in the Lovata allowlist — ThemeAjaxHandler MUST NOT import Lovata classes. The new code only references plugin-internal classes (Logingrupa\Metapixel\Classes\Adapter\*, Event\Adapter\Shopaholic\ProductPageWatcher, Exception\*) — no Lovata import added.

PHPStan deny-list note: `classes/adapter/theme/*` IS in the deny-list scope (per phpstan.neon line 73 onward). The existing handler legitimately uses `Request::input` inside `onBeforeRun` — verify the deny-list `disallowIn` SKIPS `classes/adapter/theme/*` (D-16 second documented exception per phpstan.neon comment at line 71). Verified by reading existing code that calls `Request::input` cleanly. Adding `App::make(AdapterRegistry::class)` and `App::make(ProductPageWatcher::class)` does not trip the deny-list (Site/SiteManager/Request only). No new allowlist needed.

**(b) Modify `Plugin.php` `registerComponents()`:**
- Add import: `use Logingrupa\Metapixel\Components\ProductPixel;` (after existing PixelHead import).
- Inside the returned array, add `ProductPixel::class => 'productPixel',` after the PixelHead entry. Resulting array has 3 entries.

Composer-dependency-analyser: Plugin.php is per-file allowlisted for all Lovata packages (line 51-55). Adding `use Logingrupa\Metapixel\Components\ProductPixel;` does not affect Lovata import boundary.
  </action>
  <verify>
    <automated>./vendor/bin/phpstan analyse classes/adapter/theme/ThemeAjaxHandler.php Plugin.php --level=10 --no-progress</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -q 'dispatchViaAdapter' classes/adapter/theme/ThemeAjaxHandler.php` matches (new private method).
    - Source: `grep -q 'resolveByAlias' classes/adapter/theme/ThemeAjaxHandler.php` matches.
    - Source: `grep -q 'instanceof SupportsHybridAjax' classes/adapter/theme/ThemeAjaxHandler.php` matches.
    - Source: `grep -q 'UnknownSubjectTypeException' classes/adapter/theme/ThemeAjaxHandler.php` matches.
    - Source: `grep -q 'dispatchForOfferSwitch' classes/adapter/theme/ThemeAjaxHandler.php` matches (delegation to plan 06-05 watcher entry point).
    - Source: `grep -q "'unknown subject_type'" classes/adapter/theme/ThemeAjaxHandler.php` matches (422 response body).
    - Source: `grep -q "'subject not found'" classes/adapter/theme/ThemeAjaxHandler.php` matches (404 response body).
    - Source: `grep -q "ProductPixel::class => 'productPixel'" Plugin.php` matches.
    - Source: `grep -c 'class =>' Plugin.php | head -1 | awk '$1 >= 3 {exit 0} {exit 1}'` — registerComponents returns ≥ 3 entries.
    - Static: `./vendor/bin/phpstan analyse classes/adapter/theme/ThemeAjaxHandler.php Plugin.php --level=10 --no-progress` exits 0.
    - Behavior: existing `tests/Feature/Adapter/Theme/ThemeAjaxHandlerAllowlistTest.php` continues to pass (non-subject_type path unchanged).
    - Behavior: existing `tests/Feature/Adapter/Theme/ThemeAjaxHandlerRateLimitTest.php` continues to pass.
  </acceptance_criteria>
  <done>ThemeAjaxHandler::onBeforeRun gains hybrid subject_type branch + dispatchViaAdapter private method (delegates to ProductPageWatcher::dispatchForOfferSwitch for shopaholic.product alias, appends event_id to wire-format action_key for generic hybrid aliases); ProductPixel registered as productPixel component alias; existing AJAX handler tests still green.</done>
</task>

<task type="auto">
  <name>Task 3: GREEN ProductPixelTest + ThemeAjaxHandlerSubjectTypeTest</name>
  <files>
    tests/Feature/Components/ProductPixelTest.php,
    tests/Feature/Adapter/Theme/ThemeAjaxHandlerSubjectTypeTest.php
  </files>
  <read_first>
    - tests/Feature/Components/ProductPixelTest.php (RED stubs from plan 06-01 — 4 method names locked).
    - tests/Feature/Adapter/Theme/ThemeAjaxHandlerSubjectTypeTest.php (RED stubs from plan 06-01 — 5 method names locked).
    - tests/Feature/Components/PixelHeadBasePixelTest.php (analog — runComponent reflection helper, ThemeEventCollector singleton swap, PluginGuard reset).
    - tests/Feature/Adapter/Theme/ThemeAjaxHandlerAllowlistTest.php (analog — Mockery + Request::shouldReceive + JsonResponse assertion).
    - components/ProductPixel.php (state after Task 1).
    - classes/adapter/theme/ThemeAjaxHandler.php (state after Task 2).
  </read_first>
  <action>
**(a) GREEN `tests/Feature/Components/ProductPixelTest.php`:**

Class-level `#[Group('adapter')]` (already from plan 06-01). Imports + setUp/tearDown ritual (analog from PixelHeadBasePixelTest):
- `parent::setUp(); $this->app->singleton(ThemeEventCollector::class); PluginGuard::reset(); Settings::clearInternalCache(); Settings::set(['pixel_id' => 'PX', 'capi_access_token' => 'TOK']);`
- tearDown reverse.

`runComponent` reflection helper — copy-paste from PixelHeadBasePixelTest (the helper that injects a fake `Page` ArrayAccess and calls `onRun()`).

GREEN bodies for the 4 stubs:

1. `test_offer_switch_js_rendered_with_idempotency_flag_and_soft_gate`:
   - Plugin guard NOT disabled (Settings has pixel_id).
   - Run component.
   - Assert `$arPage['productPixelOfferSwitchJs']` is a non-empty string.
   - Substring asserts: contains `'window.__metapixelProductPixelInit'` (idempotency flag), contains `'!window.__metapixelProduct'` (soft-gate), contains `"subject_type: 'shopaholic.product'"`, contains `"document.addEventListener('change'"`, contains `"jax.ajax('Metapixel::onFireEvent'"`, contains `"action_key: 'viewcontent:' + iProductId + ':' + iOfferId"` (two-segment wire-format action_key — server appends event_id).

2. `test_window_metapixel_product_global_emitted_when_collector_carries_product_id`:
   - Push event onto `App::make(ThemeEventCollector::class)` with `name: 'ViewContent', product_id: 42`.
   - Run component.
   - Assert `$arPage['productPixelProductGlobalJs']` contains `<script>window.__metapixelProduct={id:42};</script>`.

3. `test_plugin_guard_disabled_yields_null_twig_vars`:
   - `Settings::set(['pixel_id' => '']);` (disabled state).
   - PluginGuard::reset() so memo recomputes.
   - Run component.
   - Assert `$arPage['productPixelOfferSwitchJs']` IS null AND `$arPage['productPixelProductGlobalJs']` IS null.

4. `test_collector_empty_yields_null_product_global`:
   - Plugin guard NOT disabled.
   - Collector empty (no push).
   - Run component.
   - Assert `$arPage['productPixelProductGlobalJs']` IS null.
   - Assert `$arPage['productPixelOfferSwitchJs']` is NOT null (JS still attaches; it self-no-ops via Pitfall 8 soft-gate).

Drop the `fail('GREEN in plan 06-06')` placeholders.

**(b) GREEN `tests/Feature/Adapter/Theme/ThemeAjaxHandlerSubjectTypeTest.php`:**

setUp (analog from `ThemeAjaxHandlerAllowlistTest::setUp` lines 29-46):
- `parent::setUp(); $this->app->singleton(AdapterRegistry::class); App::make(AdapterRegistry::class)->register(ThemeActionEvent::class, ThemeActionAdapter::class);` (existing pattern).
- Additionally register the new ShopaholicProductAdapter for Product: `App::make(AdapterRegistry::class)->register(Product::class, ShopaholicProductAdapter::class);`. Plus a stub adapter that does NOT implement SupportsHybridAjax for the negative test.
- `Settings::clearInternalCache(); Settings::set(['pixel_id' => 'PIXEL-1', 'capi_access_token' => 'TOKEN-1']);`
- `$this->app->forgetInstance(RateLimiter::class); Session::shouldReceive('getId')->andReturn('test-session'); Request::shouldReceive('ip')->andReturn('127.0.0.1');`
- `Bus::fake();`

GREEN bodies for the 5 stubs:

1. `test_unknown_subject_type_alias_returns_422`:
   - `Request::shouldReceive('input')->with('data', [])->andReturn(['name' => 'ViewContent', 'subject_type' => 'mall.product', 'subject_id' => 1, 'action_key' => 'foo']);`
   - Call `(new ThemeAjaxHandler)->onBeforeRun(Mockery::mock(Controller::class), 'Metapixel::onFireEvent');`
   - Assert response is `JsonResponse` with status 422 + body `{error: 'unknown subject_type'}`.

2. `test_valid_alias_routes_through_registered_adapter_and_dispatches_send_capi_event`:
   - Build a Product fixture via the makeProduct helper from plan 06-04 (real DB or forceFill). Persist or stub `Product::active()->find($iId)` to return it.
   - Bind a fake ProductPageWatcher into the container whose `dispatchForOfferSwitch($iPid, $iOid): string` returns a deterministic UUID for assertion stability: `$this->app->instance(ProductPageWatcher::class, $obFakeWatcher);`.
   - `Request::shouldReceive('input')->with('data', [])->andReturn(['name' => 'ViewContent', 'subject_type' => 'shopaholic.product', 'subject_id' => $obProduct->id, 'offer_id' => 100, 'action_key' => 'viewcontent:' . $obProduct->id . ':100']);` (two-segment wire-format action_key — the handler's delegate path skips the append step because ProductPageWatcher owns the canonical action_key for this alias).
   - Mock `Site::getSiteIdFromContext()` to return 1 so the site-match check inside loadSubject passes.
   - Call handler.
   - Assert response 200 JSON with `event_id` (UUIDv4 string — equals the deterministic UUID returned by the fake watcher) + `script` (contains `fbq("track", "ViewContent", {}, {eventID: "..."})`).
   - Assert the fake watcher's `dispatchForOfferSwitch` was called once with `($obProduct->id, 100)`.

3. `test_adapter_lacking_supports_hybrid_ajax_returns_422`:
   - Register a stub adapter for an alias `'fake.bare'` that ONLY implements EventSubjectAdapter (NOT SupportsHybridAjax).
   - Request input with `subject_type: 'fake.bare'`.
   - Call handler.
   - Assert 422 + body `{error: 'subject_type does not support hybrid AJAX'}`.

4. `test_non_positive_subject_id_returns_422`:
   - Request input with `subject_type: 'shopaholic.product', subject_id: 0`.
   - Call handler.
   - Assert 422 + body `{error: 'invalid subject_id'}`.

5. `test_loadSubject_returning_null_returns_404`:
   - Request input with `subject_type: 'shopaholic.product', subject_id: 99999` (a PK that does not exist).
   - Mock `Site::getSiteIdFromContext()` to return 1.
   - Call handler.
   - Assert 404 + body `{error: 'subject not found'}`.

Drop the `fail('GREEN in plan 06-06')` placeholders.

Notes on Product fixture for test 2 + 5:
- The full Lovata Product table is not booted in MetapixelTestCase. Options:
  - (a) Use Mockery to mock `Product::active()->find($iId)` chain.
  - (b) Boot the Product migration via direct migration call (similar to `(new CreateMetapixelEventLogTable)->up()` pattern). Lovata's Product migration may have hard dependencies on Category etc — likely too heavy.
  - (c) Bind a fake `ShopaholicProductAdapter` into the container whose `loadSubject` returns a stub `Product` model for the test, bypassing `Product::active()->find` entirely.
- **Decision: option (c) — bind a test-double adapter via `$this->app->instance(ShopaholicProductAdapter::class, $obFakeAdapter);` in tests 2 + 5.** This isolates the integration boundary at the AdapterRegistry::resolveByAlias contract, which is exactly what this test class verifies (NOT the adapter's loadSubject internals — those are covered by ShopaholicProductValueResolverTest + a future contract test in plan 06-07 or v2.1).

Mockery cleanup: tearDown calls `Mockery::close()` (standard pattern).

Composer dep boundary: `tests/*` is allowed all imports per the require-dev scope (composer-dependency-analyser.php line 16-18).
  </action>
  <verify>
    <automated>./vendor/bin/pest tests/Feature/Components/ProductPixelTest.php tests/Feature/Adapter/Theme/ThemeAjaxHandlerSubjectTypeTest.php --no-coverage</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c 'public function test_' tests/Feature/Components/ProductPixelTest.php` returns 4.
    - Source: `grep -c 'public function test_' tests/Feature/Adapter/Theme/ThemeAjaxHandlerSubjectTypeTest.php` returns 5.
    - Behavior: `./vendor/bin/pest tests/Feature/Components/ProductPixelTest.php` exits 0 with 4 passing tests.
    - Behavior: `./vendor/bin/pest tests/Feature/Adapter/Theme/ThemeAjaxHandlerSubjectTypeTest.php` exits 0 with 5 passing tests.
    - Behavior: `./vendor/bin/pest tests/Feature/Adapter/Theme/ThemeAjaxHandlerAllowlistTest.php` continues to pass (existing path unchanged).
    - Behavior: `composer qa` exits 0 with aggregate coverage ≥ 90 %.
  </acceptance_criteria>
  <done>4 ProductPixelTest cases GREEN; 5 ThemeAjaxHandlerSubjectTypeTest cases GREEN; existing AJAX handler tests still green; composer qa clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser POST `subject_type` string → ThemeAjaxHandler::onBeforeRun | Untrusted; allowlist-gated via AdapterRegistry::resolveByAlias (plan 06-03) — only registered aliases pass. |
| Browser POST `subject_id` integer → ShopaholicProductAdapter::loadSubject | Untrusted; re-validated against `Product::active()->find` + site-match (plan 06-04 T-6-05 mitigation). |
| Browser POST `action_key` string (two-segment wire format) → ThemeAjaxHandler::dispatchViaAdapter | Untrusted; the handler treats it as informational/observability only (the server-generated event_id is the per-request uniqueness anchor). For the shopaholic.product alias the watcher's canonical four-segment action_key overrides the wire-format entirely; for generic aliases the handler appends the server-generated event_id (server-side concatenation; client cannot influence the event_id segment). |
| ProductPixel JS `window.__metapixelProduct` global → cart-modal `[name="offer_id"]` selector | Soft-gate (T-6-06) — JS no-ops when global absent; cart-bonus-box on non-PDP pages cannot trigger ViewContent. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-6-04 | Tampering / Elevation | ThemeAjaxHandler hybrid path accepting FQN string for `subject_type` → arbitrary-class-load RCE | mitigate | `dispatchViaAdapter` calls `AdapterRegistry::resolveByAlias` which lookups against the register-time alias index. UnknownSubjectTypeException → 422. NO `new $sFromUntrusted` path. Additional defense: `instanceof SupportsHybridAjax` check before loadSubject — adapters MUST explicitly opt in. Test 1 + Test 3 are the gates. |
| T-6-05 | Spoofing / Information Disclosure | Offer-switch JS posts forged `subject_id` (cross-site CSRF) | mitigate | October CSRF token via Larajax X-XSRF-TOKEN header (existing infrastructure). Existing rate-limit middleware in ThemeAjaxHandler (RATE_LIMIT_MAX=30/60s). `loadSubject($iSubjectId, $arContext)` in ShopaholicProductAdapter re-enforces `Product::active()->find` + site-match. Cross-site or inactive `subject_id` → null → 404. Test 5 is the gate. |
| T-6-06 | Tampering | Operator-rogue `[name="offer_id"]` outside PDP (e.g. cart-position-list-present-box.htm:20 bonus-box selector) → spurious ViewContent fires for cart line items | mitigate | Soft-gate via server-injected `window.__metapixelProduct` global — JS only fires when global is set (ProductPixel rendered = PDP). Cart-modal bonus-box select on a non-PDP page → no fire. JS check: `if (!window.__metapixelProduct || !window.__metapixelProduct.id) return;` BEFORE the `el.name !== 'offer_id'` check. ProductPixelTest test 1 + test 4 cover the JS render shape; T-6-06 behavior is a JS-runtime guard verified via substring assertion on the rendered JS string. |
| T-6-W6-T | Tampering | JSON-encoded `subject_type` from the JS containing `</script>` payload | mitigate | Inline JS uses LITERAL strings (`'shopaholic.product'`), no server-side JSON serialization of attacker-supplied content. Returned `script` payload from dispatchViaAdapter uses `JSON_HEX_*` mask on event_name + event_id (both server-derived). XSS surface is zero on the response path. |
| T-6-W6-D | Denial of Service | Storm-flood 1000 offer-switches/sec from a single client | accept | Existing RateLimiter in ThemeAjaxHandler (30 req / 60s / IP+session) throws 429. No new mitigation needed; verified by existing ThemeAjaxHandlerRateLimitTest. |
</threat_model>

<verification>
- `./vendor/bin/pest tests/Feature/Components/ProductPixelTest.php` — 4 passing.
- `./vendor/bin/pest tests/Feature/Adapter/Theme/ThemeAjaxHandlerSubjectTypeTest.php` — 5 passing.
- `./vendor/bin/pest tests/Feature/Adapter/Theme/ThemeAjaxHandlerAllowlistTest.php` — still passing (no regression on existing path).
- `./vendor/bin/phpstan analyse components/ProductPixel.php classes/adapter/theme/ThemeAjaxHandler.php Plugin.php classes/adapter/theme/ThemeEventCollector.php --level=10 --no-progress` — exits 0.
- `composer deps` — exits 0.
- `composer qa` — exits 0; coverage ≥ 90 %.
</verification>

<success_criteria>
- ProductPixel component + default.htm shipped under components/.
- ThemeEventCollector gains peek() non-mutating method.
- ThemeAjaxHandler::onBeforeRun gains hybrid subject_type branch (T-6-04 mitigation).
- Wire-format action_key (JS-sent two-segment shape) is canonicalized to four segments via watcher delegation (shopaholic.product) OR server-side append (generic hybrid aliases) — per CONTEXT.md Claude's Discretion.
- ProductPixel JS soft-gated via window.__metapixelProduct (T-6-06 mitigation).
- Plugin.registerComponents returns 3 entries.
- 4 ProductPixelTest cases GREEN.
- 5 ThemeAjaxHandlerSubjectTypeTest cases GREEN.
- composer qa green; coverage ≥ 90 %.
</success_criteria>

<output>
Create `.planning/phases/06-viewcontent-funnel-shopaholic-pdp/06-06-SUMMARY.md`: 4 production files (2 new, 2 modified) + 2 test files updated + Plugin.php delta; REQ closed (VIEW-05, VIEW-06, VIEW-09); T-6-04 + T-6-05 + T-6-06 mitigation status; status-code matrix for hybrid AJAX path; action_key canonicalization contract (JS sends two-segment, server stores four-segment); unblock list (Plan 07 docs).
</output>
</output>
