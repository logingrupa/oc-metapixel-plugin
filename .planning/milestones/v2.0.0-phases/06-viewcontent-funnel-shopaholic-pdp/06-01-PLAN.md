---
phase: 06-viewcontent-funnel-shopaholic-pdp
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/phases/06-viewcontent-funnel-shopaholic-pdp/06-VALIDATION.md
  - tests/Feature/Components/PixelHeadDeferredFlushTest.php
  - tests/Feature/Adapter/Shopaholic/ProductPageWatcherTest.php
  - tests/Feature/Adapter/Shopaholic/ShopaholicProductValueResolverTest.php
  - tests/Feature/Adapter/Theme/ThemeAjaxHandlerSubjectTypeTest.php
  - tests/Feature/Components/ProductPixelTest.php
  - tests/Contract/Adapter/Shopaholic/ShopaholicProductAdapterContractTest.php
autonomous: true
requirements:
  - VIEW-01
  - VIEW-02
  - VIEW-03
  - VIEW-04
  - VIEW-05
  - VIEW-06
  - VIEW-07
  - VIEW-08
  - VIEW-09
  - VIEW-10
  - VIEW-11
must_haves:
  truths:
    - "REQUIREMENTS.md lists VIEW-01..11 with phase 6 mapping; total v2 requirement count updated."
    - "06-VALIDATION.md Per-Task Verification Map populated with concrete task IDs and nyquist_compliant: true."
    - "6 RED test files exist in tests/Feature/* and tests/Contract/* with #[Group('adapter')] class attributes; running them fails (missing production classes)."
    - "Each RED test file declares at least the brief-matrix test method stubs (PHPUnit attribute-discoverable)."
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "VIEW-01..11 phase 6 requirement entries + traceability rows"
      contains: "VIEW-01"
    - path: ".planning/phases/06-viewcontent-funnel-shopaholic-pdp/06-VALIDATION.md"
      provides: "Per-Task Verification Map populated; nyquist_compliant: true"
      contains: "nyquist_compliant: true"
    - path: "tests/Feature/Components/PixelHeadDeferredFlushTest.php"
      provides: "4 stub tests for PixelHead deferred-flush matrix (RED)"
      contains: "#[Group('adapter')]"
    - path: "tests/Feature/Adapter/Shopaholic/ProductPageWatcherTest.php"
      provides: "11 stub tests for ProductPageWatcher matrix (RED)"
      contains: "shopaholic.product.open"
    - path: "tests/Feature/Adapter/Shopaholic/ShopaholicProductValueResolverTest.php"
      provides: "Stub tests for ShopaholicProductValueResolver SKU/value/currency (RED)"
      contains: "provideOfferShapes"
    - path: "tests/Feature/Adapter/Theme/ThemeAjaxHandlerSubjectTypeTest.php"
      provides: "5 stub tests for hybrid subject_type AJAX path (RED)"
      contains: "shopaholic.product"
    - path: "tests/Feature/Components/ProductPixelTest.php"
      provides: "Stub tests for ProductPixel JS render + soft-gate (RED)"
      contains: "window.__metapixelProduct"
    - path: "tests/Contract/Adapter/Shopaholic/ShopaholicProductAdapterContractTest.php"
      provides: "RED stub extending EventSubjectAdapterContractTestCase; turns GREEN in plan 06-04 Task 4"
      contains: "extends EventSubjectAdapterContractTestCase"
  key_links:
    - from: ".planning/REQUIREMENTS.md"
      to: ".planning/phases/06-viewcontent-funnel-shopaholic-pdp/06-VALIDATION.md"
      via: "Phase 6 VIEW-01..11 IDs referenced by Per-Task Verification Map"
      pattern: "VIEW-[0-9]+"
    - from: "tests/Feature/Components/PixelHeadDeferredFlushTest.php"
      to: "components/PixelHead.php"
      via: "deferred-flush listener invocation (will exist after Plan 02)"
      pattern: "PixelHead::flushDeferred"
    - from: "tests/Contract/Adapter/Shopaholic/ShopaholicProductAdapterContractTest.php"
      to: "classes/adapter/shopaholic/ShopaholicProductAdapter.php"
      via: "makeAdapter() returns new ShopaholicProductAdapter (class lands in 06-04 Task 1)"
      pattern: "ShopaholicProductAdapter"
---

<objective>
Author Phase 6 RED test stubs and populate REQUIREMENTS.md + VALIDATION.md so subsequent waves have a Nyquist-compliant feedback loop and traceable requirement coverage.

Purpose: Phase 6 has zero requirements in REQUIREMENTS.md and the Nyquist-validation map in 06-VALIDATION.md is unpopulated. Wave 1 closes both gaps and lands 6 RED test files (matching the brief's 11+4 matrix plus 4 supplemental files for resolver, ProductPixel, hybrid-AJAX coverage, and the Phase 2 contract proof for ShopaholicProductAdapter) so every downstream task in waves 2–5 has a failing automated test to drive against.

Output:
- `.planning/REQUIREMENTS.md` gains a `### ViewContent funnel (VIEW-XX)` section with 11 rows + 11 new traceability table rows under Phase 6.
- `.planning/phases/06-viewcontent-funnel-shopaholic-pdp/06-VALIDATION.md` Per-Task Verification Map filled in with concrete task IDs (resolved from this plan set); `nyquist_compliant: true` flipped in frontmatter.
- 6 RED test files exist (5 under `tests/Feature/*` + 1 under `tests/Contract/Adapter/Shopaholic/*`), each carrying `#[Group('adapter')]` class-level attribute, each containing the stub methods named in the brief matrix (assertions left as `$this->markTestIncomplete()` or `$this->fail('GREEN in plan NN')` so the suite is RED but informative).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-viewcontent-funnel-shopaholic-pdp/06-CONTEXT.md
@.planning/phases/06-viewcontent-funnel-shopaholic-pdp/06-RESEARCH.md
@.planning/phases/06-viewcontent-funnel-shopaholic-pdp/06-PATTERNS.md
@.planning/phases/06-viewcontent-funnel-shopaholic-pdp/06-VALIDATION.md
@.planning/briefs/2026-05-27-viewcontent-funnel-shopaholic.md

# Existing analogs the executor copies test scaffolding from:
@tests/Feature/Components/PixelHeadBasePixelTest.php
@tests/Feature/Adapter/Theme/ThemeAjaxHandlerAllowlistTest.php
@tests/Feature/Adapter/Shopaholic/PurchaseFlowIntegrationTest.php
@tests/Contract/Adapter/Shopaholic/ShopaholicOrderAdapterContractTest.php
</context>

<interface_context>
PHPUnit 12 + Pest 4 attribute conventions used across the suite:
- `#[\PHPUnit\Framework\Attributes\Group('adapter')]` class-level attribute (minimal-install CI excludes via `--exclude-group=adapter`).
- `#[\PHPUnit\Framework\Attributes\DataProvider('provideXxx')]` method-level attribute (annotation-based dataProviders no longer discovered).
- Test methods named `test_<snake_case_behavior>` extend `Logingrupa\Metapixel\Tests\MetapixelTestCase` and call `parent::setUp()` / `parent::tearDown()`.

Phase 6 task ID scheme used to fill VALIDATION.md Per-Task Verification Map:
- `6-01-01` … `6-01-03` (this plan's three tasks)
- `6-02-01` … `6-02-03` (Plan 02 — PixelHead deferred flush)
- `6-03-01` … `6-03-02` (Plan 03 — AdapterRegistry::resolveByAlias + SupportsHybridAjax)
- `6-04-01` … `6-04-04` (Plan 04 — ShopaholicProductAdapter + ValueResolver + ContractTest)
- `6-05-01` … `6-05-02` (Plan 05 — ProductPageWatcher + Plugin.php boot wiring)
- `6-06-01` … `6-06-02` (Plan 06 — ProductPixel + ThemeAjaxHandler hybrid path)
- `6-07-01` … `6-07-02` (Plan 07 — README + CHANGELOG + PHPDoc lifecycle docblock)
</interface_context>

<tasks>

<task type="auto">
  <name>Task 1: Add VIEW-01..11 requirement rows + traceability to REQUIREMENTS.md</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (full file — read once; locate ### sections + Traceability table + Coverage Summary table + Per-Phase Requirement Counts).
    - .planning/phases/06-viewcontent-funnel-shopaholic-pdp/06-RESEARCH.md §Phase Requirements (the 11 VIEW-XX rows the planner endorses).
    - .planning/phases/06-viewcontent-funnel-shopaholic-pdp/06-CONTEXT.md `<domain>` 7-item Phase Boundary list (the source-of-truth for VIEW-XX wording).
  </read_first>
  <action>
Insert a new `### ViewContent funnel (VIEW-XX)` section after the existing `### Marketplace launch` section and BEFORE `## Future Requirements (v2.1+, deferred)`. Use the same `- [ ] **VIEW-NN**: …` bullet shape used by sibling sections. Eleven rows verbatim per researcher recommendation (D-1..D-10 + research §5/§9 informed):

| ID | Wording (one-liner) |
|---|---|
| VIEW-01 | `PixelHead` flushes `ThemeEventCollector` at `cms.page.beforeRenderPage` (NOT `onRun`). Base PageView stays in `onRun`. `action_key` shape `base:pageview:{site_id}:{event_id}` unchanged. |
| VIEW-02 | `Logingrupa\Metapixel\Classes\Adapter\Shopaholic\ShopaholicProductAdapter` implements `EventSubjectAdapter` + new `SupportsHybridAjax` subinterface for `Lovata\Shopaholic\Models\Product`. `getSubjectType()` returns alias `'shopaholic.product'`. `getSiteId()` reads `$obProduct->site_list` single-element when count==1, falls back to `Site::getSiteIdFromContext()` (D-15 exception) — never `Request::*` / `SiteManager::*`. Proven against the Phase 2 contract via `ShopaholicProductAdapterContractTest` (10 invariants inherited from `EventSubjectAdapterContractTestCase`). |
| VIEW-03 | `Logingrupa\Metapixel\Classes\Adapter\Shopaholic\ShopaholicProductValueResolver` resolves `value` (default-offer `price_value`), `currency` (`CurrencyHelper::instance()->getActiveCurrencyCode()` → `Settings::get('default_currency_code', '')` fallback → throw), `content_ids` (`['SKU-{pid}-{oid}']` multi-offer, `['SKU-{pid}']` single/empty offer per D-5 + D-10). |
| VIEW-04 | `Logingrupa\Metapixel\Classes\Event\Adapter\Shopaholic\ProductPageWatcher` subscribes `shopaholic.product.open`. Uses `CapturesRequestUserData`. Generates UUIDv4 `event_id`. Action key `viewcontent:{product_id}:{event_id}`. Pushes to `ThemeEventCollector` + dispatches `SendCapiEvent('ViewContent', $arPayload, $obProduct, ShopaholicProductAdapter::class)`. `Throwable` boundary catch logs + returns; page render MUST NOT 500. |
| VIEW-05 | `Logingrupa\Metapixel\Components\ProductPixel` (`[productPixel]` alias) renders `window.__metapixelProduct={id:N}` global + inline offer-switch JS via `$this->page` Twig vars. `PluginGuard::isDisabled()` → both Twig vars null. Vendor-neutral name. |
| VIEW-06 | Offer-switch JS (delegated `change` on `document` matching `[name="offer_id"]`) is idempotent (single attach via `window.__metapixelProductPixelInit` flag), soft-gated by `window.__metapixelProduct` (Pitfall 8 — cart-modal bonus-box ignored), posts to `Metapixel::onFireEvent` via `jax.ajax(...)` with `subject_type:'shopaholic.product'`. |
| VIEW-07 | `AdapterRegistry::resolveByAlias(string $sAlias): string` (returns adapter FQN) added; internal `<alias, adapter-class>` index populated at `register()` time; unknown alias throws `Logingrupa\Metapixel\Classes\Exception\UnknownSubjectTypeException`. |
| VIEW-08 | New marker subinterface `Logingrupa\Metapixel\Classes\Adapter\SupportsHybridAjax extends EventSubjectAdapter` defines `loadSubject(int $iSubjectId, array $arContext): ?object`. `ShopaholicProductAdapter::loadSubject` re-enforces `Product::active()->find($iId)` + site-relation match — returns null on miss. Soft-deleted excluded (Product has `SoftDelete` trait, RESEARCH Pitfall 6 confirmed). |
| VIEW-09 | `ThemeAjaxHandler::onBeforeRun` detects optional `subject_type` field. Resolves via `AdapterRegistry::resolveByAlias` (catches `UnknownSubjectTypeException` → 422 `unknown subject_type`). Validates `$obAdapter instanceof SupportsHybridAjax` (else 422), `subject_id > 0` (else 422), `loadSubject(...) !== null` (else 404). Builds payload via resolved adapter + its `ValueResolver`. Returns `{event_id, script}` shape unchanged. |
| VIEW-10 | `Plugin::boot()` registers `ProductPageWatcher` + `AdapterRegistry::register(Product::class, ShopaholicProductAdapter::class)` ONLY when `PluginManager::exists('Lovata.OrdersShopaholic')` — per the one-guard pattern (RESEARCH §10 recommendation a), `OrdersShopaholic` transitively requires `Lovata.Shopaholic`, so this single check covers both the `shopaholic.product.open` event source (`Lovata.Shopaholic`) and the existing OrdersShopaholic-gated watchers. Component alias `productPixel` registered unconditionally. Plugin.php `cms.page.beforeRenderPage` gains a SECOND listener invoking `PixelHead::flushDeferredFromController($obController)`. |
| VIEW-11 | All Phase 6 tests carry class-level `#[\PHPUnit\Framework\Attributes\Group('adapter')]`. `composer qa` exits 0 with full-Lovata coverage ≥ 90 %. Minimal-install cell (`pest --exclude-group=adapter`) drops all Phase 6 tests cleanly. |

Then update the **## Coverage Summary** table: add row `| 6 | ViewContent funnel (VIEW-01..11) | 11 |` after the `| 5 | Marketplace (MKT-01..05) | 5 |` row, and bump the `**Total v2.0** | **61 requirements** |` line to `**72 requirements**`.

Then update **## Traceability** header counter from `**Coverage:** 61/61` to `**Coverage:** 72/72`. Append 11 new rows to the traceability table (pattern `| VIEW-01 | Phase 6 | Pending |` … through `VIEW-11`).

Then update **### Per-Phase Requirement Counts** table: add row `| Phase 6 | 11 | VIEW-01..11 |`. Bump the Total row to `| **Total** | **72** |`.

Hungarian-style discipline still applies to PROSE: refer to symbols verbatim ($obProduct, $arPayload, $iSubjectId) but do not invent new prose conventions. Per D-01..D-10 of CONTEXT.md these are locked decisions — wording above implements D-2, D-3, D-5, D-6, D-7, D-8, D-9, D-10 verbatim.
  </action>
  <verify>
    <automated>grep -c '^- \[ \] \*\*VIEW-' .planning/REQUIREMENTS.md | grep -q '^11$' &amp;&amp; grep -q '| VIEW-11 | Phase 6 |' .planning/REQUIREMENTS.md &amp;&amp; grep -q '**Total v2.0** | **72 requirements**' .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c '^- \[ \] \*\*VIEW-' .planning/REQUIREMENTS.md` outputs exactly `11`.
    - Source: traceability table contains literal lines for every ID `VIEW-01` through `VIEW-11` with `Phase 6` column.
    - Source: Coverage Summary total updated to `**72 requirements**` (no stale `61` reference outside historical prose).
    - Source: Per-Phase Requirement Counts table includes `| Phase 6 | 11 | VIEW-01..11 |`.
    - Behavior: no removal of any existing requirement row (`grep -c '^- \[' .planning/REQUIREMENTS.md` is at least 61 higher than baseline minus existing minus 11 — diff must add only, not subtract).
  </acceptance_criteria>
  <done>REQUIREMENTS.md has 11 new VIEW-XX rows + traceability + Coverage Summary + Per-Phase Counts updated; baseline 61/61 lifted to 72/72.</done>
</task>

<task type="auto">
  <name>Task 2: Populate 06-VALIDATION.md Per-Task Verification Map + flip nyquist_compliant</name>
  <files>.planning/phases/06-viewcontent-funnel-shopaholic-pdp/06-VALIDATION.md</files>
  <read_first>
    - .planning/phases/06-viewcontent-funnel-shopaholic-pdp/06-VALIDATION.md (current draft; identify the placeholder rows + nyquist_compliant frontmatter flag).
    - The seven Phase 6 PLAN.md files (`06-NN-PLAN.md` 01..07) — read each plan's `<tasks>` block to derive concrete `Task ID` columns `6-NN-MM`.
  </read_first>
  <action>
Replace the placeholder rows in the `## Per-Task Verification Map` table with one row per task across all seven Phase 6 plans. Use the task IDs scheme from `<interface_context>` (`6-01-01` through `6-07-02`). Columns to populate per existing schema:

- `Task ID` — `6-NN-MM`
- `Plan` — `01`..`07`
- `Wave` — `1`..`5` (this plan = 1; PixelHead = 2; AdapterRegistry = 2 (sibling to PixelHead, both depend only on Plan 01); Adapter+Resolver+ContractTest = 3; Watcher+Component parallel = 4; Docs = 5)
- `Requirement` — VIEW-01..11 (this plan's tasks reference VIEW-11 + meta VIEW-01..10 by virtue of writing the RED stubs)
- `Threat Ref` — T-6-01..T-6-06 per orchestrator threat brief (each plan's `<threat_model>` block declares which threat that plan owns; map here)
- `Secure Behavior` — one-line summary copied from the matching task's `<done>` field
- `Test Type` — `unit` / `feature` / `contract` / `static` / `doc`
- `Automated Command` — the exact `./vendor/bin/pest …` or `composer qa` invocation from the task's `<verify><automated>` field
- `File Exists` — `❌ W0` for files this plan creates (test stubs); `✅ created in 6-NN-MM` for files later plans create; `✅ exists` for files already on disk
- `Status` — `⬜ pending` for every row (executor flips to ✅/❌ during runs)

Also update the Wave 0 Requirements list to include the new contract-test stub:
- [ ] `tests/Contract/Adapter/Shopaholic/ShopaholicProductAdapterContractTest.php` — RED stub (extends EventSubjectAdapterContractTestCase; 10 inherited invariants run RED until ShopaholicProductAdapter lands in Plan 06-04)

Total Wave 0 Requirements after this update: 6 RED test stubs (5 Feature + 1 Contract) + "No new framework install required" line.

Also update the Manual-Only Verifications section to reference concrete VIEW-XX IDs (replace `VIEW-XX` placeholders with VIEW-01/VIEW-06/VIEW-11 as appropriate).

Flip frontmatter `nyquist_compliant: false` → `nyquist_compliant: true`. Bump `wave_0_complete: false` → `wave_0_complete: true` once Task 3 below ships.

Sign the Validation Sign-Off block with `**Approval:** ready` after the table is populated.
  </action>
  <verify>
    <automated>grep -q '^nyquist_compliant: true$' .planning/phases/06-viewcontent-funnel-shopaholic-pdp/06-VALIDATION.md &amp;&amp; grep -c '| 6-0[1-7]-' .planning/phases/06-viewcontent-funnel-shopaholic-pdp/06-VALIDATION.md | awk '$1 >= 12 {exit 0} {exit 1}'</automated>
  </verify>
  <acceptance_criteria>
    - Source: frontmatter `nyquist_compliant: true` present.
    - Source: Per-Task Verification Map has ≥ 13 rows (this plan's 3 + Plan 02's 2-3 + Plan 03's 2 + Plan 04's 4 + Plan 05's 2 + Plan 06's 2 + Plan 07's 2 ≈ 17 — the gate accepts ≥ 12 minimum).
    - Source: every row populated has a non-empty `Automated Command` column (no `TBD` strings).
    - Source: every row's `Threat Ref` is one of `T-6-01` … `T-6-06` or `n/a` for doc-only tasks.
    - Source: Manual-Only Verifications table has no `VIEW-XX` placeholder remaining (each row references a real VIEW-NN id).
    - Source: Wave 0 Requirements list contains 6 RED test stub bullets (5 Feature + 1 Contract).
  </acceptance_criteria>
  <done>VALIDATION.md per-task map fully populated with 06-NN-MM IDs spanning waves 1–5; nyquist_compliant + wave_0_complete = true; Manual verifications reference concrete VIEW-NN IDs; Wave 0 Requirements list includes the new contract-test stub.</done>
</task>

<task type="auto">
  <name>Task 3: Author 6 RED test stub files with class-level Group attribute and brief-matrix method stubs</name>
  <files>
    tests/Feature/Components/PixelHeadDeferredFlushTest.php,
    tests/Feature/Adapter/Shopaholic/ProductPageWatcherTest.php,
    tests/Feature/Adapter/Shopaholic/ShopaholicProductValueResolverTest.php,
    tests/Feature/Adapter/Theme/ThemeAjaxHandlerSubjectTypeTest.php,
    tests/Feature/Components/ProductPixelTest.php,
    tests/Contract/Adapter/Shopaholic/ShopaholicProductAdapterContractTest.php
  </files>
  <read_first>
    - tests/Feature/Components/PixelHeadBasePixelTest.php (analog — class-level `#[Group('adapter')]`, setUp/tearDown pattern, runComponent reflection helper, `Bus::fake()` + `Settings::clearInternalCache()` + `PluginGuard::reset()` ritual).
    - tests/Feature/Adapter/Theme/ThemeAjaxHandlerAllowlistTest.php (analog — Mockery + `Request::shouldReceive('input')` + JsonResponse status-code assertion shape).
    - tests/Feature/Adapter/Shopaholic/PurchaseFlowIntegrationTest.php (analog — full Shopaholic integration; copy Bus::fake harness + Product fixture pattern).
    - tests/Contract/Adapter/Shopaholic/ShopaholicOrderAdapterContractTest.php (analog — contract-test class shape extending EventSubjectAdapterContractTestCase; setUp/tearDown + makeAdapter/makeSubject hook signatures).
    - .planning/phases/06-viewcontent-funnel-shopaholic-pdp/06-PATTERNS.md §Pattern Assignments — Add (test files section) — DELTA list per file.
    - .planning/briefs/2026-05-27-viewcontent-funnel-shopaholic.md "## Test matrix" — verbatim assertion list (ProductPageWatcherTest 1-11, PixelHeadDeferredFlushTest 1-4).
  </read_first>
  <action>
Create SIX test files. Each MUST: (a) declare PSR-4 namespace matching the directory; (b) carry class-level `#[\PHPUnit\Framework\Attributes\Group('adapter')]`; (c) extend `Logingrupa\Metapixel\Tests\MetapixelTestCase` (or `EventSubjectAdapterContractTestCase` for the contract stub); (d) declare each stub test method with body `$this->fail('GREEN in plan 06-NN — Task NN');` referencing the plan that turns it green; (e) be RED (assertion fails, not skipped — so the Nyquist gate registers an actual failure count).

File 1 — `tests/Feature/Components/PixelHeadDeferredFlushTest.php` (namespace `Logingrupa\Metapixel\Tests\Feature\Components`). Four stub methods covering brief matrix items 1–4 — turns GREEN in plan 06-02:
- `test_emit_collected_events_flushes_on_cms_page_beforeRenderPage_not_onRun`
- `test_collector_push_between_onRun_and_beforeRenderPage_is_flushed`
- `test_base_pageview_action_key_shape_unchanged`
- `test_test_event_code_flows_to_fbq_script_block`

File 2 — `tests/Feature/Adapter/Shopaholic/ProductPageWatcherTest.php` (namespace `Logingrupa\Metapixel\Tests\Feature\Adapter\Shopaholic`). Eleven stub methods covering brief matrix items 1–11 — turns GREEN in plan 06-05:
- `test_viewcontent_dispatches_capi_and_pushes_collector_on_shopaholic_product_open`
- `test_does_not_fire_when_plugin_guard_disabled`
- `test_does_not_subscribe_when_lovata_shopaholic_absent`
- `test_zero_offer_product_resolves_bare_sku_pid`
- `test_multi_offer_product_resolves_sku_pid_oid_first_active_by_sort_order`
- `test_single_offer_product_resolves_bare_sku_pid`
- `test_capi_payload_event_id_matches_collector_pushed_event_id`
- `test_user_data_populated_from_server_and_cookies`
- `test_test_event_code_appears_in_capi_payload_and_collector_event`
- `test_event_log_race_fence_does_not_block_per_pageload_duplicates`
- `test_offer_switch_ajax_re_fires_viewcontent_with_new_event_id_and_offer_sku` (turns GREEN in plan 06-05 — exercises the watcher's payload-build path with an offer-switch shaped invocation; full HTTP-entry coverage cross-references ThemeAjaxHandlerSubjectTypeTest item 2)

File 3 — `tests/Feature/Adapter/Shopaholic/ShopaholicProductValueResolverTest.php` (namespace `Logingrupa\Metapixel\Tests\Feature\Adapter\Shopaholic`). Stub methods — turns GREEN in plan 06-04:
- `test_resolveContentIds_matches_sku_format_for_offer_shapes` decorated with `#[\PHPUnit\Framework\Attributes\DataProvider('provideOfferShapes')]` + a `public static function provideOfferShapes(): array` returning the 4 fixture rows from PATTERNS.md (zero/single/multi-first-active/multi-not-first).
- `test_resolveValue_reads_default_offer_price_value_raw`
- `test_resolveCurrency_returns_active_currency_code_when_available`
- `test_resolveCurrency_falls_back_to_settings_default_currency_code`
- `test_resolveCurrency_throws_when_both_sources_empty`
- `test_resolveContents_returns_one_item_with_quantity_one`
- `test_resolveNumItems_returns_one_constant`

File 4 — `tests/Feature/Adapter/Theme/ThemeAjaxHandlerSubjectTypeTest.php` (namespace `Logingrupa\Metapixel\Tests\Feature\Adapter\Theme`). Five stub methods — turns GREEN in plan 06-06:
- `test_unknown_subject_type_alias_returns_422`
- `test_valid_alias_routes_through_registered_adapter_and_dispatches_send_capi_event`
- `test_adapter_lacking_supports_hybrid_ajax_returns_422`
- `test_non_positive_subject_id_returns_422`
- `test_loadSubject_returning_null_returns_404`

File 5 — `tests/Feature/Components/ProductPixelTest.php` (namespace `Logingrupa\Metapixel\Tests\Feature\Components`). Stub methods — turns GREEN in plan 06-06:
- `test_offer_switch_js_rendered_with_idempotency_flag_and_soft_gate`
- `test_window_metapixel_product_global_emitted_when_collector_carries_product_id`
- `test_plugin_guard_disabled_yields_null_twig_vars`
- `test_collector_empty_yields_null_product_global`

File 6 — `tests/Contract/Adapter/Shopaholic/ShopaholicProductAdapterContractTest.php` (namespace `Logingrupa\Metapixel\Tests\Contract\Adapter\Shopaholic`). Turns GREEN in plan 06-04 Task 4. Structural difference from files 1–5: this file extends `EventSubjectAdapterContractTestCase` (not `MetapixelTestCase`) because the 10 invariant tests are inherited — DO NOT declare them in this subclass. Stub body must satisfy the abstract base:
- `protected function makeAdapter(): \Logingrupa\Metapixel\Classes\Adapter\EventSubjectAdapter` — body: `$this->fail('GREEN in plan 06-04 — Task 4 — ShopaholicProductAdapter not yet shipped');`
- `protected function makeSubject(): object` — body: `$this->fail('GREEN in plan 06-04 — Task 4 — Product fixture + product-site-relation pivot not yet built');`
- Class-level `#[Group('adapter')]` attribute.
- Class-level docblock one-liner: `RED STUB — turns GREEN in plan 06-04 Task 4. Will inherit 10 invariants from EventSubjectAdapterContractTestCase.`
- Use statements: `use Logingrupa\Metapixel\Classes\Adapter\EventSubjectAdapter;` + `use Logingrupa\Metapixel\Classes\Testing\EventSubjectAdapterContractTestCase;` + `use PHPUnit\Framework\Attributes\Group;`.
- DO NOT import `Logingrupa\Metapixel\Classes\Adapter\Shopaholic\ShopaholicProductAdapter` (does not exist until plan 06-04 Task 1) — reference it inside the `fail()` message string only.

Each file's `<?php` block follows the analog header verbatim: declare `use` for `MetapixelTestCase`, `Group`, and any `DataProvider` only when used. Do NOT import production classes that do not yet exist (`ShopaholicProductAdapter`, `ProductPixel`, `UnknownSubjectTypeException`, `SupportsHybridAjax`) — reference them via fully-qualified strings inside the stub body's `fail()` message so the file is loadable today but informative when red.

Tiger-Style boundary: the stub bodies do `$this->fail(...)` — no silent `markTestIncomplete`. RED gate must register failures.
  </action>
  <verify>
    <automated>./vendor/bin/pest --filter='Logingrupa\\Metapixel\\Tests\\(Feature\\(Components\\PixelHeadDeferredFlushTest|Adapter\\Shopaholic\\ProductPageWatcherTest|Adapter\\Shopaholic\\ShopaholicProductValueResolverTest|Adapter\\Theme\\ThemeAjaxHandlerSubjectTypeTest|Components\\ProductPixelTest)|Contract\\Adapter\\Shopaholic\\ShopaholicProductAdapterContractTest)' 2>&amp;1 | grep -E 'Failed|Tests:'</automated>
  </verify>
  <acceptance_criteria>
    - Source: all 6 files exist at the listed paths.
    - Source: `grep -l "#\[Group('adapter')\]" tests/Feature/Components/PixelHeadDeferredFlushTest.php tests/Feature/Adapter/Shopaholic/ProductPageWatcherTest.php tests/Feature/Adapter/Shopaholic/ShopaholicProductValueResolverTest.php tests/Feature/Adapter/Theme/ThemeAjaxHandlerSubjectTypeTest.php tests/Feature/Components/ProductPixelTest.php tests/Contract/Adapter/Shopaholic/ShopaholicProductAdapterContractTest.php` returns 6 lines.
    - Source: `grep -q 'extends EventSubjectAdapterContractTestCase' tests/Contract/Adapter/Shopaholic/ShopaholicProductAdapterContractTest.php` matches.
    - Behavior: running `./vendor/bin/pest tests/Feature/Adapter/Shopaholic/ProductPageWatcherTest.php` exits non-zero (RED — 11 failures) AND outputs "Tests: 11" (not "Tests: 0").
    - Behavior: running `./vendor/bin/pest tests/Feature/Components/PixelHeadDeferredFlushTest.php` exits non-zero (RED — 4 failures).
    - Behavior: running `./vendor/bin/pest tests/Contract/Adapter/Shopaholic/ShopaholicProductAdapterContractTest.php` exits non-zero (RED — 10 inherited invariant failures, all triggered by the makeAdapter/makeSubject `fail()` stubs).
    - Behavior: PHPUnit loads every stub class without `Error: Class not found` (production-class FQNs MUST live in fail() message strings, NOT in `use` statements that resolve at autoload time).
    - Static: `./vendor/bin/pest --exclude-group=adapter` does NOT execute any of the 6 new files (minimal-install CI cell isolation).
  </acceptance_criteria>
  <done>6 RED test stub files committed; each declares brief-matrix methods (Feature stubs) OR makeAdapter/makeSubject hooks (Contract stub); Group('adapter') class attribute present on each; running them fails informatively with `GREEN in plan 06-NN — Task NN` failure messages.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Planning artifact → Execute-phase | Wave 1 planning text becomes the executable spec for waves 2-5. Garbage VIEW-XX wording propagates everywhere. |
| RED-stub fail message → Executor agent | Each stub's `fail('GREEN in plan 06-NN — Task NN')` IS the contract telling a downstream executor what file's expectations they must meet. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-6-01 | Tampering | PixelHead deferred flush listener (this plan creates the RED stub) | mitigate (in plan 02) | Stub asserts listener fires on `cms.page.beforeRenderPage` and short-circuits on 404 redirect path (no `renderPageContents` → no flush). |
| T-6-W1-D | Tampering | RED stub `fail()` message wording | mitigate | Use exact format `GREEN in plan 06-NN — Task NN` so executors cannot fabricate a different plan link; verified by grep gate in Task 3 acceptance criteria (`grep -c "GREEN in plan 06-" tests/Feature/...` returns 32 — sum of all stubs). |
| T-6-W1-R | Repudiation | REQUIREMENTS.md edit (no signed authorship) | accept | Project convention: git commit author is the authorship anchor. No additional sign-off needed. |
</threat_model>

<verification>
- `grep -c '^- \[ \] \*\*VIEW-' .planning/REQUIREMENTS.md` == 11
- `grep -q 'nyquist_compliant: true' .planning/phases/06-viewcontent-funnel-shopaholic-pdp/06-VALIDATION.md`
- `./vendor/bin/pest --group=adapter --filter=ProductPageWatcherTest` exits non-zero with `Tests: 11, Failures: 11` (RED informative)
- `./vendor/bin/pest --group=adapter --filter=ShopaholicProductAdapterContractTest` exits non-zero with 10 inherited invariant failures.
- Phase 6 directory contains 06-CONTEXT.md, 06-RESEARCH.md, 06-PATTERNS.md, 06-VALIDATION.md (now populated), 06-DISCUSSION-LOG.md (existing) — no extra files.
</verification>

<success_criteria>
- 11 VIEW-XX requirements in REQUIREMENTS.md; total bumped 61 → 72.
- 06-VALIDATION.md Per-Task Verification Map filled in with concrete `6-NN-MM` task IDs; nyquist_compliant: true.
- 6 RED test files committed (5 to `tests/Feature/*` + 1 to `tests/Contract/Adapter/Shopaholic/`) with class-level `#[Group('adapter')]` attribute.
- Each RED test exits non-zero with informative `GREEN in plan 06-NN` failure message — no skipped tests.
- `composer qa` may STILL fail at this point (production classes missing); that is expected — RED Wave 0 establishes the Nyquist baseline.
</success_criteria>

<output>
Create `.planning/phases/06-viewcontent-funnel-shopaholic-pdp/06-01-SUMMARY.md` listing: VIEW-XX rows added, baseline → updated total counts, 6 RED test file paths + per-file stub-method count (5 Feature + 1 Contract), REQ-IDs covered, next-plan unblock list (Plan 02 unblocks immediately).
</output>
