---
phase: 06-viewcontent-funnel-shopaholic-pdp
plan: 03
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - classes/adapter/AdapterRegistry.php
  - classes/adapter/SupportsHybridAjax.php
  - classes/exception/UnknownSubjectTypeException.php
  - tests/Unit/Adapter/AdapterRegistryResolveByAliasTest.php
autonomous: true
requirements:
  - VIEW-07
  - VIEW-08
must_haves:
  truths:
    - "AdapterRegistry::resolveByAlias(string $sAlias): string returns the registered adapter class FQN OR throws UnknownSubjectTypeException."
    - "AdapterRegistry::register builds the alias index at register-time by App::make-ing the adapter and calling getSubjectType(new \\stdClass) — verified safe per RESEARCH A3 (all shipping adapters return a constant alias and ignore the argument)."
    - "SupportsHybridAjax marker subinterface extends EventSubjectAdapter and declares loadSubject(int, array): ?object — opt-in contract for hybrid AJAX path."
    - "UnknownSubjectTypeException extends MetaPixelException; ships under classes/exception/ matching sibling layout."
    - "EventSubjectAdapter (base interface) is UNCHANGED — Phase 2 contract test 10 invariants stay intact; no BC break."
    - "AdapterRegistryResolveByAliasTest covers: known alias → FQN, unknown alias → throws, alias index persists across register() calls, register-time instantiation throws InvalidArgumentException when adapter constructor needs unbound deps (documented limitation)."
  artifacts:
    - path: "classes/adapter/AdapterRegistry.php"
      provides: "Existing registry + new resolveByAlias method + register-time alias index"
      contains: "resolveByAlias"
    - path: "classes/adapter/SupportsHybridAjax.php"
      provides: "Marker subinterface declaring loadSubject(int, array): ?object contract"
      contains: "interface SupportsHybridAjax extends EventSubjectAdapter"
    - path: "classes/exception/UnknownSubjectTypeException.php"
      provides: "Thrown by AdapterRegistry::resolveByAlias on miss; caught at ThemeAjaxHandler boundary"
      contains: "final class UnknownSubjectTypeException extends MetaPixelException"
    - path: "tests/Unit/Adapter/AdapterRegistryResolveByAliasTest.php"
      provides: "Unit test coverage for resolveByAlias happy path + unknown-alias path + alias-index integrity"
      contains: "#[Group('adapter')]"
  key_links:
    - from: "classes/adapter/AdapterRegistry.php"
      to: "classes/exception/UnknownSubjectTypeException.php"
      via: "throw new UnknownSubjectTypeException(...) in resolveByAlias on miss"
      pattern: "UnknownSubjectTypeException"
    - from: "classes/adapter/SupportsHybridAjax.php"
      to: "classes/adapter/EventSubjectAdapter.php"
      via: "interface SupportsHybridAjax extends EventSubjectAdapter"
      pattern: "extends EventSubjectAdapter"
---

<objective>
Extend `AdapterRegistry` with O(1) alias resolution and add a marker subinterface plus typed exception that the hybrid AJAX path (plan 06-06) consumes.

Purpose: the offer-switch JS posts a `subject_type` alias string (e.g. `'shopaholic.product'`). The server MUST resolve that alias to a concrete adapter FQN through an allowlist that cannot be bypassed by FQN injection. Building the alias index at `register()` time gives O(1) lookup and ensures the allowlist is byte-for-byte the registered set. The `SupportsHybridAjax` subinterface preserves Phase 2's `EventSubjectAdapterContractTestCase` 10 invariants by NOT mutating the base interface — adapters that need PK-based hydration opt in.

Output:
- `classes/adapter/AdapterRegistry.php` gains `private array $arAliasMap = []` field, alias-index population inside `register()`, and `public function resolveByAlias(string $sAlias): string`.
- `classes/adapter/SupportsHybridAjax.php` (NEW) declares `loadSubject(int $iSubjectId, array $arContext): ?object` on a subinterface extending `EventSubjectAdapter`.
- `classes/exception/UnknownSubjectTypeException.php` (NEW) extends `MetaPixelException`.
- `tests/Unit/Adapter/AdapterRegistryResolveByAliasTest.php` (NEW) — coverage on the new surface; class-level `#[Group('adapter')]`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-viewcontent-funnel-shopaholic-pdp/06-CONTEXT.md
@.planning/phases/06-viewcontent-funnel-shopaholic-pdp/06-RESEARCH.md
@.planning/phases/06-viewcontent-funnel-shopaholic-pdp/06-PATTERNS.md
@.planning/phases/06-viewcontent-funnel-shopaholic-pdp/06-01-SUMMARY.md

@classes/adapter/AdapterRegistry.php
@classes/adapter/EventSubjectAdapter.php
@classes/exception/MetaPixelException.php
@classes/exception/MissingCapiTokenException.php
@classes/adapter/shopaholic/ShopaholicCartPositionAdapter.php
@classes/adapter/shopaholic/ShopaholicOrderAdapter.php
@classes/adapter/theme/ThemeActionAdapter.php
</context>

<interface_context>
**Final `AdapterRegistry` shape (additions):**

```
final class AdapterRegistry
{
    /** @var array<string, class-string<EventSubjectAdapter>> */
    private array $arAdapterMap = [];

    /** @var array<string, class-string<EventSubjectAdapter>> */
    private array $arAliasMap = [];

    public function register(string $sSubjectClass, string $sAdapterClass): void
    {
        // existing is_subclass_of guard preserved …
        $this->arAdapterMap[$sSubjectClass] = $sAdapterClass;

        // alias-index build at register-time (RESEARCH §5 option a; A3 verified
        // — all shipping adapters return constant alias from getSubjectType).
        /** @var EventSubjectAdapter $obAdapter */
        $obAdapter = App::make($sAdapterClass);
        $sAlias = $obAdapter->getSubjectType(new \stdClass);
        $this->arAliasMap[$sAlias] = $sAdapterClass;
    }

    /**
     * Resolve adapter class FQN by opaque subject_type alias.
     *
     * @throws UnknownSubjectTypeException
     * @return class-string<EventSubjectAdapter>
     */
    public function resolveByAlias(string $sAlias): string
    {
        if (! isset($this->arAliasMap[$sAlias])) {
            throw new UnknownSubjectTypeException(
                "No adapter registered for subject_type alias '{$sAlias}'",
            );
        }
        return $this->arAliasMap[$sAlias];
    }
}
```

**`SupportsHybridAjax` interface shape (NEW):**

```
namespace Logingrupa\Metapixel\Classes\Adapter;

interface SupportsHybridAjax extends EventSubjectAdapter
{
    /**
     * Hydrate the subject from PK + arbitrary context. Return null when
     * subject is missing, inactive, soft-deleted, or fails site-match.
     * Adapters MUST re-enforce subject's domain guards inside this method
     * — bypass-via-AJAX is exploitable otherwise.
     *
     * @param array<string, mixed> $arContext
     */
    public function loadSubject(int $iSubjectId, array $arContext): ?object;
}
```

**`UnknownSubjectTypeException` shape:**

```
namespace Logingrupa\Metapixel\Classes\Exception;

final class UnknownSubjectTypeException extends MetaPixelException {}
```

Inherits the 4-arg constructor + `getContext()` from `MetaPixelException`.

**Test class:** `Logingrupa\Metapixel\Tests\Unit\Adapter\AdapterRegistryResolveByAliasTest` extending `MetapixelTestCase`. Path `tests/Unit/Adapter/AdapterRegistryResolveByAliasTest.php`.
</interface_context>

<tasks>

<task type="auto">
  <name>Task 1: Add SupportsHybridAjax interface + UnknownSubjectTypeException + extend AdapterRegistry</name>
  <files>
    classes/adapter/SupportsHybridAjax.php,
    classes/exception/UnknownSubjectTypeException.php,
    classes/adapter/AdapterRegistry.php
  </files>
  <read_first>
    - classes/adapter/EventSubjectAdapter.php (parent interface — 7 method signatures the subinterface must accept).
    - classes/exception/MissingCapiTokenException.php (sibling exception — 3-line shape, extends MetaPixelException with PHPDoc one-liner).
    - classes/exception/MetaPixelException.php (parent — 4-arg constructor signature).
    - classes/adapter/AdapterRegistry.php (current state — `register()` at line 41, `arAdapterMap` field at line 32; the file the executor edits).
    - classes/adapter/shopaholic/ShopaholicCartPositionAdapter.php (verifies `getSubjectType($obSubject)` body ignores argument — A3 assumption proof).
    - classes/adapter/shopaholic/ShopaholicOrderAdapter.php (same A3 verification).
    - classes/adapter/theme/ThemeActionAdapter.php (same A3 verification).
  </read_first>
  <action>
NOTE: contrary to CONTEXT.md code_context recommendation, loadSubject() is placed in a new SupportsHybridAjax subinterface rather than the base EventSubjectAdapter to preserve the 10 invariants in Phase 2 EventSubjectAdapterContractTestCase. The code_context section is reference material, not a locked decision (per RESEARCH §11 open Q 2 recommendation).

Create `classes/adapter/SupportsHybridAjax.php`. Namespace `Logingrupa\Metapixel\Classes\Adapter`. Single interface declaration `interface SupportsHybridAjax extends EventSubjectAdapter` with one method per `<interface_context>`. PHPDoc text verbatim from `<interface_context>`. PHP 8.3 compat — no readonly properties, no asymmetric visibility.

Create `classes/exception/UnknownSubjectTypeException.php`. Namespace `Logingrupa\Metapixel\Classes\Exception`. `final class UnknownSubjectTypeException extends MetaPixelException {}` — empty body, inherits constructor. PHPDoc one-liner per PATTERNS.md §classes/exception delta: `Thrown by AdapterRegistry::resolveByAlias when the supplied subject_type string does not match any registered adapter's alias. Caught at ThemeAjaxHandler::onBeforeRun → returns JsonResponse 422.`

Modify `classes/adapter/AdapterRegistry.php`:
- Add `use Logingrupa\Metapixel\Classes\Exception\UnknownSubjectTypeException;` to the import block.
- Add new private property `private array $arAliasMap = [];` with PHPDoc `/** @var array<string, class-string<EventSubjectAdapter>> */` AFTER the existing `$arAdapterMap` declaration.
- Inside `register()`, AFTER `$this->arAdapterMap[$sSubjectClass] = $sAdapterClass;` (line 48), add:
  - `/** @var EventSubjectAdapter $obAdapter */`
  - `$obAdapter = App::make($sAdapterClass);`
  - `$sAlias = $obAdapter->getSubjectType(new \stdClass);`
  - `$this->arAliasMap[$sAlias] = $sAdapterClass;`
  - Inline comment: `// Build the alias index at register-time. Per A3 (RESEARCH §5), every shipping adapter's getSubjectType() returns a constant string and ignores its argument — passing new \stdClass is safe. Third-party adapters that conditional-dispatch on $obSubject inside getSubjectType violate the alias-opacity contract documented on EventSubjectAdapter::getSubjectType.`
- Add new method per `<interface_context>` after `resolveByClass()`:
  - `public function resolveByAlias(string $sAlias): string`
  - PHPDoc with `@throws UnknownSubjectTypeException` + `@return class-string<EventSubjectAdapter>`.
  - Body: `if (! isset($this->arAliasMap[$sAlias])) { throw new UnknownSubjectTypeException("No adapter registered for subject_type alias '{$sAlias}'"); } return $this->arAliasMap[$sAlias];`

Tiger-Style note: `register()` constructor failure for the adapter (e.g. `BindingResolutionException`) cascades to `Plugin.boot()`. Per RESEARCH Pitfall 11, all shipping adapters have parameterless constructors. Document this convention by appending to the existing class PHPDoc at line 29: `Adapters MUST have parameterless constructors so the register-time alias-index population (App::make($sAdapterClass)) cannot fail.`

PHPStan deny-list note: `classes/adapter/AdapterRegistry.php` is in `classes/adapter/` but NOT in `classes/adapter/shopaholic/*` nor any deny-listed subpath — the deny-list (`disallowIn`) scopes are `classes/queue/*`, `classes/event/*`, `classes/adapter/shopaholic/*`, `classes/event/adapter/shopaholic/*`. AdapterRegistry's root-level path is NOT deny-listed → no allowlist needed.

Composer-dependency-analyser note: `classes/adapter/` root is NOT in the Lovata allowlist (only `classes/adapter/shopaholic/` is). The registry imports no Lovata classes — the `App::make` resolution string is symbolic. Verify by `grep -l 'Lovata\\\\' classes/adapter/AdapterRegistry.php` returns nothing.

PHP 8.3+8.4 compat: `new \stdClass` is valid in both versions — no syntactic concern.
  </action>
  <verify>
    <automated>./vendor/bin/phpstan analyse classes/adapter/AdapterRegistry.php classes/adapter/SupportsHybridAjax.php classes/exception/UnknownSubjectTypeException.php --level=10 --no-progress</automated>
  </verify>
  <acceptance_criteria>
    - Source: `classes/adapter/SupportsHybridAjax.php` exists; `grep -q 'interface SupportsHybridAjax extends EventSubjectAdapter' classes/adapter/SupportsHybridAjax.php` matches.
    - Source: `classes/exception/UnknownSubjectTypeException.php` exists; `grep -q 'final class UnknownSubjectTypeException extends MetaPixelException' classes/exception/UnknownSubjectTypeException.php` matches.
    - Source: `grep -q 'public function resolveByAlias' classes/adapter/AdapterRegistry.php` matches.
    - Source: `grep -q 'private array \$arAliasMap' classes/adapter/AdapterRegistry.php` matches.
    - Source: `grep -q 'new \\\\stdClass' classes/adapter/AdapterRegistry.php` matches (alias-index registration).
    - Static: `./vendor/bin/phpstan analyse classes/adapter/AdapterRegistry.php classes/adapter/SupportsHybridAjax.php classes/exception/UnknownSubjectTypeException.php --level=10 --no-progress` exits 0.
    - Behavior: existing Phase 2 contract tests (`tests/Contract/Adapter/*ContractTest.php` if any) continue to pass — base `EventSubjectAdapter` interface unchanged.
    - Behavior: existing `Plugin.boot` adapter registrations (Order, CartPosition, ThemeActionEvent) continue to succeed AT BOOT despite the new alias-index instantiation step — verified by `./vendor/bin/pest tests/Feature/Plugin/PluginSanityTest.php` exit 0.
  </acceptance_criteria>
  <done>Three artifacts in place: SupportsHybridAjax interface, UnknownSubjectTypeException, extended AdapterRegistry; phpstan L10 clean; existing Plugin sanity tests still pass.</done>
</task>

<task type="auto">
  <name>Task 2: Author AdapterRegistryResolveByAliasTest covering happy/unknown/idempotency paths</name>
  <files>tests/Unit/Adapter/AdapterRegistryResolveByAliasTest.php</files>
  <read_first>
    - tests/Feature/Adapter/Theme/ThemeAjaxHandlerAllowlistTest.php (setUp pattern using `$this->app->singleton(AdapterRegistry::class)` + `AdapterRegistry::register(...)`).
    - tests/Unit/Adapter/ (directory — read one sibling test if present for unit-test conventions; if empty, mirror tests/Feature/Plugin/PluginSanityTest.php).
    - classes/adapter/AdapterRegistry.php (state after Task 1).
    - .planning/phases/06-viewcontent-funnel-shopaholic-pdp/06-RESEARCH.md §5 (alias-index design) + Pitfall 11 (constructor-dependency failure mode).
  </read_first>
  <action>
Create `tests/Unit/Adapter/AdapterRegistryResolveByAliasTest.php`. Namespace `Logingrupa\Metapixel\Tests\Unit\Adapter`. Class `final class AdapterRegistryResolveByAliasTest extends MetapixelTestCase`. Class-level `#[\PHPUnit\Framework\Attributes\Group('adapter')]`. Use statements:
- `use Logingrupa\Metapixel\Classes\Adapter\AdapterRegistry;`
- `use Logingrupa\Metapixel\Classes\Adapter\EventSubjectAdapter;`
- `use Logingrupa\Metapixel\Classes\Adapter\SupportsHybridAjax;`
- `use Logingrupa\Metapixel\Classes\Adapter\ValueResolver;`
- `use Logingrupa\Metapixel\Classes\Exception\UnknownSubjectTypeException;`
- `use Logingrupa\Metapixel\Tests\MetapixelTestCase;`
- `use PHPUnit\Framework\Attributes\Group;`

`setUp()`: `parent::setUp(); $this->app->singleton(AdapterRegistry::class, fn() => new AdapterRegistry);` (fresh instance per test — test-swap idiom per AdapterRegistry class PHPDoc lines 15-16).

Define two private FAKE adapter classes inline at the top of the file using PHP 8.3-compatible anonymous-class fixtures (NOT in the public namespace — keep test-local):
- `FakeFooSubjectAdapter implements EventSubjectAdapter` returning alias `'fake.foo'` from `getSubjectType()` and stubbed values for the other 6 methods.
- `FakeBarHybridAdapter implements SupportsHybridAjax` returning alias `'fake.bar'` and a non-null `loadSubject(int $iId, array $arContext): ?object` body returning `new \stdClass`.

Make these `final class` declarations in the same file under the same `namespace Logingrupa\Metapixel\Tests\Unit\Adapter\Doubles` (sub-namespace) — they are unit-test fixtures, MUST NOT live in production paths or pollute `Logingrupa\Metapixel\Classes\*`.

Test methods (each named `test_*`):
1. `test_resolveByAlias_returns_registered_adapter_class_for_known_alias`:
   - Register FakeFooSubjectAdapter for `\stdClass::class`.
   - Assert `AdapterRegistry::resolveByAlias('fake.foo')` returns `FakeFooSubjectAdapter::class`.
2. `test_resolveByAlias_throws_UnknownSubjectTypeException_for_unknown_alias`:
   - Empty registry.
   - `$this->expectException(UnknownSubjectTypeException::class);`
   - `$this->expectExceptionMessage("No adapter registered for subject_type alias 'mall.product'");`
   - Call `AdapterRegistry::resolveByAlias('mall.product');`.
3. `test_alias_index_persists_across_multiple_register_calls`:
   - Register FakeFooSubjectAdapter for `\stdClass::class` AND FakeBarHybridAdapter for `\ArrayObject::class`.
   - Assert resolveByAlias('fake.foo') and resolveByAlias('fake.bar') BOTH return correct FQNs.
4. `test_alias_index_overwrites_when_same_alias_re_registered`:
   - Register FakeFooSubjectAdapter twice (idempotent contract per AdapterRegistry PHPDoc line 36).
   - Assert resolveByAlias('fake.foo') still returns FakeFooSubjectAdapter::class.
5. `test_SupportsHybridAjax_adapter_resolves_through_alias_index_too`:
   - Register FakeBarHybridAdapter.
   - Assert resolveByAlias('fake.bar') returns FakeBarHybridAdapter::class.
   - Assert `App::make(FakeBarHybridAdapter::class) instanceof SupportsHybridAjax` (subinterface check works).
6. `test_register_throws_when_adapter_does_not_implement_EventSubjectAdapter`:
   - `$this->expectException(\InvalidArgumentException::class);`
   - Call `register(\stdClass::class, \stdClass::class)` — `\stdClass` does NOT implement EventSubjectAdapter.

Drop the RED stubs that may have referenced these tests in plan 06-01 (this is a NEW test file, no RED stub from plan 06-01 — plan 06-01 RED stubs covered 5 files, this is a 6th, supplemental).

PHPMD ShortVariable min=4 compliance: use `$iId`/`$obContext` (4+ chars), never `$id`. Hungarian discipline in all variables.

Coverage gate: Aim for 100 % line coverage on `classes/adapter/AdapterRegistry.php` after this test runs alongside the existing Phase 2 registry tests. The aggregate ≥ 90 % gate stays green if existing coverage was solid.
  </action>
  <verify>
    <automated>./vendor/bin/pest tests/Unit/Adapter/AdapterRegistryResolveByAliasTest.php --no-coverage</automated>
  </verify>
  <acceptance_criteria>
    - Source: `tests/Unit/Adapter/AdapterRegistryResolveByAliasTest.php` exists; `grep -q "#\[Group('adapter')\]" tests/Unit/Adapter/AdapterRegistryResolveByAliasTest.php` matches.
    - Source: 6 test methods named `test_*` declared (grep `'public function test_'` count == 6).
    - Behavior: `./vendor/bin/pest tests/Unit/Adapter/AdapterRegistryResolveByAliasTest.php` exits 0 with 6 passing tests.
    - Behavior: `./vendor/bin/pest --group=adapter --filter=AdapterRegistry` includes this file's test count in totals.
    - Behavior: `./vendor/bin/pest --exclude-group=adapter` does NOT execute the file.
    - Static: `./vendor/bin/phpstan analyse tests/Unit/Adapter/AdapterRegistryResolveByAliasTest.php --level=10 --no-progress` exits 0 (or 0 once phpstan paths include `tests/Unit/Adapter` — verify the current phpstan.neon paths cover unit tests; if not, this file is excluded from static scan per excludePaths `tests (?)`).
  </acceptance_criteria>
  <done>6 tests passing for resolveByAlias coverage; alias-index correctness, throw-on-miss, idempotency, subinterface compatibility, register guard all covered.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| JS POST `subject_type` string → AdapterRegistry::resolveByAlias | Alias-only allowlist; FQN strings impossible to inject as no `new $sFromUntrusted(...)` path exists in the registry. |
| Third-party adapter class FQN passed to `register()` | Constructor invoked at boot via `App::make`; trust deferred to operator who installed the third-party plugin (extensibility contract per plugin CLAUDE.md). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-6-04 | Tampering / Elevation | ThemeAjaxHandler hybrid path accepting FQN string for subject_type → arbitrary-class-load RCE | mitigate | `resolveByAlias` lookups against `$arAliasMap` (built at register-time from a known finite set). Throw `UnknownSubjectTypeException` on miss. NO `new $sFqn(...)` path in registry. Test 2 above is the gate. |
| T-6-W3-T | Tampering | Third-party adapter registered with malicious `getSubjectType` returning `'shopaholic.product'` (alias collision) | mitigate | Last `register()` call wins (idempotent contract per registry PHPDoc). If a third-party plugin loads after the core Shopaholic registration, its adapter would shadow. Operator-side concern; documented in extensibility contract. Allowlist principle: only operator-installed plugins can register. Defense-in-depth: ThemeAjaxHandler hybrid path still verifies `instanceof SupportsHybridAjax` and `loadSubject` re-enforces site/active guards on the adapter's own logic. |
| T-6-W3-R | Repudiation | Boot-time `App::make($sAdapterClass)` failing on third-party adapter with unbound deps | accept | RESEARCH Pitfall 11 — operator's plugin order issue. Documented in AdapterRegistry PHPDoc convention `Adapters MUST have parameterless constructors`. |
</threat_model>

<verification>
- `./vendor/bin/phpstan analyse classes/adapter/AdapterRegistry.php classes/adapter/SupportsHybridAjax.php classes/exception/UnknownSubjectTypeException.php --level=10 --no-progress` — exits 0.
- `./vendor/bin/pest tests/Unit/Adapter/AdapterRegistryResolveByAliasTest.php` — 6 passing.
- `./vendor/bin/pest tests/Feature/Plugin/PluginSanityTest.php` — passing (no regression on boot-time adapter registration).
- `grep -q 'new \\\\stdClass' classes/adapter/AdapterRegistry.php` matches.
- `composer qa` on touched files — green.
</verification>

<success_criteria>
- `AdapterRegistry::resolveByAlias(string): string` available; alias index built at register-time.
- `SupportsHybridAjax` subinterface declared; ready for `ShopaholicProductAdapter` (plan 06-04) to implement.
- `UnknownSubjectTypeException` placed under `classes/exception/`; throwable from registry.
- EventSubjectAdapter base interface UNCHANGED (Phase 2 contract test 10 invariants stay intact).
- Phase 2 Plugin sanity tests still green (boot-time adapter registrations succeed despite new alias-index step).
- 6 unit tests covering resolveByAlias green; coverage on AdapterRegistry.php at 100 %.
</success_criteria>

<output>
Create `.planning/phases/06-viewcontent-funnel-shopaholic-pdp/06-03-SUMMARY.md` documenting: new files + new method signatures + Phase 2 invariant preservation note + T-6-04 mitigation status + unblock list (plans 04, 05, 06 depend on this).
</output>
</output>
</output>
