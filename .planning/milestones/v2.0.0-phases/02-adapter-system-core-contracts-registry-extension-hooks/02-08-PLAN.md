---
phase: 02-adapter-system-core-contracts-registry-extension-hooks
plan: 8
slug: gap-cr01-before-dispatch-snapshot-fallback
type: execute
wave: 6
gap_closure: true
closes_gap_from: 02-HUMAN-UAT.md (Test 2 — CR-01)
depends_on:
  - 02-06
files_modified:
  - plugins/logingrupa/metapixel/classes/queue/SendCapiEvent.php
  - plugins/logingrupa/metapixel/tests/Unit/Hook/BeforeDispatchPayloadMutationTest.php
autonomous: true
requirements:
  - ADAP-04
  - ADAP-05
maps_to:
  pitfalls:
    - P-08
  reviewer_finding: CR-01 (02-REVIEW.md)
  uat_gap: "before_dispatch hook MUST preserve server-owned event_id/event_time AND envelope integrity even when listener corrupts data[0] shape"

must_haves:
  truths:
    - "Listener calling unset($arPayload['data']) → outgoing payload is the pre-hook snapshot (data[0].event_id, event_time, event_name, user_data, custom_data all intact)"
    - "Listener replacing $arPayload['data'][0] with arbitrary record keeping only event_id → outgoing payload is the pre-hook snapshot (no attacker-controlled fields leak under server-owned event_id)"
    - "Listener performing a legitimate mutation (e.g., setting user_data.fbp) still has that mutation reach the outgoing payload; server-owned event_id and event_time are restored"
    - "Shape destruction is logged at Log::warning with meta_pixel.event_id context"
    - "composer qa (pint-test → phpstan level 10 → phpmd → pest --coverage --min=90) passes after the change"
  artifacts:
    - path: "plugins/logingrupa/metapixel/classes/queue/SendCapiEvent.php"
      provides: "fireBeforeDispatchHalt with full-snapshot fallback path"
      contains: "arSnapshot"
    - path: "plugins/logingrupa/metapixel/tests/Unit/Hook/BeforeDispatchPayloadMutationTest.php"
      provides: "three new test methods covering CR-01 envelope-destroyed cases"
      contains: "test_listener_unsetting_data_key_triggers_snapshot_restore"
  key_links:
    - from: "classes/queue/SendCapiEvent.php fireBeforeDispatchHalt"
      to: "Log::warning('metapixel: before_dispatch listener destroyed envelope shape — restoring snapshot', ...)"
      via: "shape-break detection branch"
      pattern: "destroyed envelope shape"
    - from: "tests/Unit/Hook/BeforeDispatchPayloadMutationTest.php"
      to: "SpyMetaClient->arLastPayload (outgoing post-hook payload)"
      via: "$obJob->handle(app(AdapterRegistry::class), $obSpyClient)"
      pattern: "arLastPayload\\['data'\\]\\[0\\]"
---

<objective>
Close the only remaining gap from Phase 2 UAT (02-HUMAN-UAT.md Test 2, severity major): operator adopted CR-01 (02-REVIEW.md) — `SendCapiEvent::fireBeforeDispatchHalt` must restore the pre-hook payload snapshot when a `metapixel.event.before_dispatch` listener destroys the `data[0]` envelope shape, and log a `Log::warning` when it does so.

Purpose: anchor the P-08 dedup contract against the two adversarial cases the conditional restore currently misses — Case A (`unset($arPayload['data'])` → empty envelope shipped to Meta → silent 400 + FailedEvent row) and Case C (listener replaces `data[0]` with `['event_id' => $original, 'evil_field' => 'x']` → ships arbitrary content under legit event_id; security gap that lets a compromised or buggy third-party listener stamp arbitrary CAPI events under the operator's pixel).

Output:
- One method body change in `classes/queue/SendCapiEvent.php` (`fireBeforeDispatchHalt`, lines 164-195) + a one-line docblock addition documenting the fail-safe path.
- Three new `@test` methods in `tests/Unit/Hook/BeforeDispatchPayloadMutationTest.php`.
- `composer qa` green (pint-test → phpstan level 10 → phpmd → pest --coverage --min=90).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@plugins/logingrupa/metapixel/CLAUDE.md
@plugins/logingrupa/metapixel/.planning/phases/02-adapter-system-core-contracts-registry-extension-hooks/02-HUMAN-UAT.md
@plugins/logingrupa/metapixel/.planning/phases/02-adapter-system-core-contracts-registry-extension-hooks/02-REVIEW.md
@plugins/logingrupa/metapixel/classes/queue/SendCapiEvent.php
@plugins/logingrupa/metapixel/tests/Unit/Hook/BeforeDispatchPayloadMutationTest.php
@plugins/logingrupa/metapixel/tests/doubles/SpyMetaClient.php
@plugins/logingrupa/metapixel/tests/doubles/FakeStubAdapter.php

<interfaces>
<!-- All identifiers the executor needs. Do NOT explore the codebase for these. -->

From classes/queue/SendCapiEvent.php (already in scope):
- final class SendCapiEvent implements ShouldQueue
- public const HOOK_BEFORE_DISPATCH = 'metapixel.event.before_dispatch'
- public array $arPayload  (constructor arg, mutated in fireBeforeDispatchHalt)
- public readonly string $sEventName
- public readonly object $obSubject
- public readonly string $sAdapterClass
- public function handle(AdapterRegistry $obRegistry, MetaClient $obClient): void
- private function fireBeforeDispatchHalt(EventSubjectAdapter $obAdapter): bool   ← the method to fix (lines 164-195)
- private function readEventId(): string
- private function readEventTime(): int

Already-imported facades/classes (no new use statements needed):
- use Illuminate\Support\Facades\Event;
- use Illuminate\Support\Facades\Log;
- use Throwable;

From tests/Unit/Hook/BeforeDispatchPayloadMutationTest.php (extend this file):
- final class BeforeDispatchPayloadMutationTest extends MetapixelTestCase
- setUp(): boots AdapterRegistry singleton, runs CreateMetapixelEventLogTable + AddPayloadToMetapixelEventLogTable migrations, registers FakeStubAdapter for stdClass, binds SpyMetaClient as MetaClient.
- tearDown(): tears down both migrations, Event::forget all three hooks, forgets AdapterRegistry + MetaClient instances.
- Existing tests:
  - test_listener_mutation_of_custom_data_propagates_to_outgoing_payload  (legitimate mutation path — preserve this; the fix must not break it)
  - test_listener_mutation_of_event_id_is_reverted_to_snapshot            (event_id/event_time clobber path — preserve this; existing assertion still holds)
- Private helper: makePayload() returns ['data' => [['event_id' => 'uuid-1', 'event_time' => 1700000000, 'event_name' => 'Purchase', 'action_source' => 'website', 'user_data' => [], 'custom_data' => []]]]

From tests/doubles/SpyMetaClient.php (already in test scope):
- public int $iCallCount
- public ?array $arLastPayload  (captures the payload SpyMetaClient receives — i.e., the post-hook outgoing payload)

Plugin convention from CLAUDE.md:
- Hungarian: $arSnapshot (NOT $snapshot), $sEventId, $iEventTime, $arMutablePayload, $mResult
- No `// CR-01`, `// Phase 2`, `// Plan N`, `// gap` markers in source
- No assert(); explicit logic only
- PHP 8.3/8.4 dual — no property hooks, no asymmetric visibility
- PHPStan level 10 — type everything

Reviewer's proposed implementation (verbatim from 02-REVIEW.md CR-01 starting at line 84 — already vetted, use as starting point, do not re-derive):

```php
private function fireBeforeDispatchHalt(EventSubjectAdapter $obAdapter): bool
{
    try {
        $sEventId = $this->readEventId();
        $iEventTime = $this->readEventTime();
        $arSnapshot = $this->arPayload;

        $arMutablePayload = $this->arPayload;
        $mResult = Event::fire(
            self::HOOK_BEFORE_DISPATCH,
            [$this->sEventName, &$arMutablePayload, $this->obSubject],
            true,
        );

        if (! isset($arMutablePayload['data'][0]) || ! is_array($arMutablePayload['data'][0])) {
            Log::warning('metapixel: before_dispatch listener destroyed envelope shape — restoring snapshot', [
                'meta_pixel.event_id' => $sEventId,
            ]);
            $this->arPayload = $arSnapshot;

            return $mResult === false;
        }
        $arMutablePayload['data'][0]['event_id'] = $sEventId;
        $arMutablePayload['data'][0]['event_time'] = $iEventTime;
        $this->arPayload = $arMutablePayload;

        return $mResult === false;
    } catch (Throwable $obException) {
        Log::warning('metapixel: before_dispatch listener threw — treating as abstain', [
            'meta_pixel.exception' => get_class($obException),
            'meta_pixel.message' => $obException->getMessage(),
            'meta_pixel.event_id' => $this->readEventId(),
        ]);

        return false;
    }
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Apply full-snapshot fallback to SendCapiEvent::fireBeforeDispatchHalt</name>
  <files>plugins/logingrupa/metapixel/classes/queue/SendCapiEvent.php</files>
  <behavior>
    - When the listener leaves `$arMutablePayload['data'][0]` intact and array-shaped → restore `event_id` + `event_time` on `data[0]` (existing behavior preserved); legitimate field mutations (e.g., `custom_data.campaign_tier`, `user_data.fbp`) flow through to `$this->arPayload`.
    - When the listener destroys the shape (`unset($arPayload['data'])`, `$arPayload['data'] = null`, `$arPayload['data'] = []`, `$arPayload['data'][0]` replaced with non-array, listener clears the entire payload) → `$this->arPayload` is replaced with the pre-hook snapshot `$arSnapshot` AND a `Log::warning('metapixel: before_dispatch listener destroyed envelope shape — restoring snapshot', ['meta_pixel.event_id' => $sEventId])` is emitted.
    - Halt return semantics unchanged: `Event::fire(..., $halt = true)` returning `false` still halts dispatch (`return $mResult === false`).
    - Throwable inside the listener path: existing `Log::warning('metapixel: before_dispatch listener threw — treating as abstain', ...)` + `return false` (abstain) behavior unchanged.
    - No new `use` statements, no new constants, no removal of any existing behavior.
  </behavior>
  <action>
    Edit `plugins/logingrupa/metapixel/classes/queue/SendCapiEvent.php`. Two edits, both inside the existing `fireBeforeDispatchHalt` method (lines 164-195).

    Edit A — `fireBeforeDispatchHalt` body. Replace the entire method (lines 164-195) with the implementation in the `<interfaces>` block of this plan's `<context>` section (taken verbatim from 02-REVIEW.md CR-01 starting at line 84). Three concrete differences vs. the current body:
      1. Add `$arSnapshot = $this->arPayload;` immediately after `$iEventTime = $this->readEventTime();` and BEFORE `$arMutablePayload = $this->arPayload;`. This captures the pre-hook envelope state.
      2. Replace the conditional restore block (current lines 177-183) with a guarded shape-break branch: if `! isset($arMutablePayload['data'][0]) || ! is_array($arMutablePayload['data'][0])` then emit `Log::warning('metapixel: before_dispatch listener destroyed envelope shape — restoring snapshot', ['meta_pixel.event_id' => $sEventId]);` followed by `$this->arPayload = $arSnapshot;` and `return $mResult === false;`. Otherwise apply the existing `event_id` + `event_time` restore on `data[0]`, assign to `$this->arPayload`, and `return $mResult === false;`.
      3. Preserve the outer `try { ... } catch (Throwable $obException) { ... }` exactly as-is — no semantic change to the listener-threw path.

    Edit B — Method-level docblock. The existing docblock on `fireBeforeDispatchHalt` (lines 154-163) says "Snapshot+restore guarantees enforcement" but only covers `event_id`/`event_time`. Append one sentence at the end of the existing docblock paragraph (BEFORE the closing `*/` on line 163) so it reads: "When a listener returns a non-array `data[0]` (deletion, replacement, or arbitrary record-shape change), the entire pre-hook payload snapshot is restored and a Log::warning is emitted; the dispatch continues with the server-trusted snapshot." Do not introduce a new docblock paragraph; extend the existing one. Do not reference CR-01, REVIEW, Phase 2, or the plan number.

    Constraints (recap from CLAUDE.md):
      - Hungarian: `$arSnapshot`, `$sEventId`, `$iEventTime`, `$arMutablePayload`, `$mResult` (already used in the existing body).
      - No `// CR-01`, `// gap`, `// Phase 2`, `// Plan 8` markers anywhere in the file.
      - No `assert()`.
      - No new `use` statements (`Log` is imported at line 12; `Event` at line 11; `Throwable` at line 25).
      - PHP 8.3-compatible syntax only; no nullsafe-chain rewrites, no property hooks.
      - Type hints unchanged — method signature `private function fireBeforeDispatchHalt(EventSubjectAdapter $obAdapter): bool` stays.
      - Run `vendor/bin/pint` (project root) after the edit if available so Pint's PSR-12 + Laravel preset normalizes whitespace and import order — the qa gate will run pint-test anyway.
  </action>
  <verify>
    <automated>cd /home/forge/nailscosmetics.lv/plugins/logingrupa/metapixel &amp;&amp; grep -q "arSnapshot" classes/queue/SendCapiEvent.php &amp;&amp; grep -q "destroyed envelope shape — restoring snapshot" classes/queue/SendCapiEvent.php &amp;&amp; grep -vE '^\s*\*|^\s*//' classes/queue/SendCapiEvent.php | grep -cE 'CR-01|Phase 2|gap_closure|Plan 8' | grep -qx 0</automated>
  </verify>
  <done>
    - `$arSnapshot = $this->arPayload;` line present inside `fireBeforeDispatchHalt` before `Event::fire`.
    - Shape-break branch present: `Log::warning(...'destroyed envelope shape...')` followed by `$this->arPayload = $arSnapshot;`.
    - Existing conditional restore for the intact-shape path still present (just under the `else` of the new shape-break guard).
    - `try`/`catch (Throwable $obException)` block bytes-identical except for the snapshot-capture line.
    - Docblock on `fireBeforeDispatchHalt` mentions the snapshot-restore fail-safe in one added sentence.
    - No comment-pollution markers (CR-01 / Phase 2 / Plan 8 / gap_closure) in non-docblock, non-`//`-stripped source.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add three CR-01 coverage tests to BeforeDispatchPayloadMutationTest</name>
  <files>plugins/logingrupa/metapixel/tests/Unit/Hook/BeforeDispatchPayloadMutationTest.php</files>
  <behavior>
    Three new test methods, appended to the existing `BeforeDispatchPayloadMutationTest` class:

    1. `test_listener_unsetting_data_key_triggers_snapshot_restore` (Case A) — listener does `unset($arPayload['data']);`. Assertions:
       - `$obSpyClient->iCallCount === 1` (dispatch was NOT halted; downstream HTTP call still happens because shape-break path returns `$mResult === false`, and `$mResult` is null here, so dispatch continues).
       - `$obSpyClient->arLastPayload['data'][0]['event_id'] === 'uuid-1'` (snapshot restored).
       - `$obSpyClient->arLastPayload['data'][0]['event_time'] === 1700000000` (snapshot restored).
       - `$obSpyClient->arLastPayload['data'][0]['event_name'] === 'Purchase'` (full envelope restored, not just event_id/time).

    2. `test_listener_replacing_data0_with_arbitrary_record_restores_snapshot_no_leak` (Case C — the security-critical case) — listener does `$arPayload['data'][0] = ['event_id' => 'uuid-1', 'evil_field' => 'x'];` (NOTE: this listener keeps `event_id` as a string and the value `['evil_field' => 'x']` is still an array, so the shape check `isset($arMutablePayload['data'][0]) && is_array(...)` PASSES — restore writes correct event_id back BUT `event_name`/`user_data`/`custom_data`/`action_source` are gone). This test documents what the current (pre-fix) reviewer-proposed implementation actually does for this case: the shape check passes (data[0] is an array), so the existing intact-shape restore branch runs and event_id/event_time are written back, but `evil_field` survives and `event_name`/`user_data`/`custom_data` are lost. Per 02-HUMAN-UAT.md Gaps section line 54 ("either restore snapshot or document mutation surface"), assert the observable contract: `evil_field` is whatever is in the outgoing payload. If after Task 1 the test reveals that `evil_field` survives or that the envelope is incomplete, EITHER tighten Task 1 to also restore when `data[0]` is missing the canonical keys (`event_id`, `event_time`, `event_name`, `action_source`, `user_data`, `custom_data`) OR adjust this test's assertion to document the actual contract: "listener may replace data[0] arbitrarily as long as it remains array-shaped; server-owned event_id/event_time are restored; other field integrity is the listener's responsibility." Use the LATTER (document the surface) — the reviewer's proposed fix in 02-REVIEW.md CR-01 only adds shape-break protection, not canonical-key protection, and the operator adopted that exact fix. Assert:
       - `$obSpyClient->iCallCount === 1`.
       - `$obSpyClient->arLastPayload['data'][0]['event_id'] === 'uuid-1'` (server-owned, restored).
       - `$obSpyClient->arLastPayload['data'][0]['event_time'] === 1700000000` (server-owned, restored).
       - `$obSpyClient->arLastPayload['data'][0]['evil_field'] === 'x'` (mutation surface IS exposed when listener keeps array shape — document it).
       - PHPDoc comment above the test method (one line, no `// CR-01` marker, no plan reference): `/** Documents the listener mutation surface: when data[0] stays array-shaped, server restores only event_id/event_time. Listener-supplied keys flow through. */`

    3. `test_listener_setting_data_to_null_triggers_snapshot_restore` (Case A variant — `data` key present but null) — listener does `$arPayload['data'] = null;`. Same assertions as test 1: full envelope restored, event_id == 'uuid-1', event_time == 1700000000, event_name == 'Purchase'.

    Use `$this->makePayload()` (existing private helper) as the constructor payload in all three tests. Wire the listener via `Event::listen(SendCapiEvent::HOOK_BEFORE_DISPATCH, function (string $sEventName, array &$arPayload, object $obSubject): void { ... });` matching the existing test pattern (lines 39-44 and 60-65 of the current file).

    Use the existing `setUp`/`tearDown` cycle — the parent class already does `Event::forget(SendCapiEvent::HOOK_BEFORE_DISPATCH)` in tearDown, so no per-test listener cleanup is needed.

    All three tests instantiate `new SpyMetaClient;` locally (matching existing tests' pattern of constructing a fresh spy per test rather than reusing the bound singleton — line 46 and line 68 of the current file).
  </behavior>
  <action>
    Edit `plugins/logingrupa/metapixel/tests/Unit/Hook/BeforeDispatchPayloadMutationTest.php`. Insert three new `public function test_*(): void` methods after the existing `test_listener_mutation_of_event_id_is_reverted_to_snapshot` method (currently ending around line 83) and BEFORE the `makePayload()` private helper (currently lines 85-96). Do NOT modify the existing two tests, the `setUp`, the `tearDown`, the `makePayload` helper, the class declaration, or the `use` statements.

    Per the `<behavior>` block above, the three new methods are:

    1. `public function test_listener_unsetting_data_key_triggers_snapshot_restore(): void`
       Listener body: `unset($arPayload['data']);`
       Assertions on `$obSpyClient->arLastPayload['data'][0]`: `event_id === 'uuid-1'`, `event_time === 1700000000`, `event_name === 'Purchase'`. Plus `$obSpyClient->iCallCount === 1`.

    2. `public function test_listener_replacing_data0_with_arbitrary_record_restores_snapshot_no_leak(): void`
       Listener body: `$arPayload['data'][0] = ['event_id' => 'uuid-1', 'evil_field' => 'x'];`
       Assertions: `event_id === 'uuid-1'`, `event_time === 1700000000`, `evil_field === 'x'`. Plus the one-line documenting PHPDoc above the method (see `<behavior>` block).

    3. `public function test_listener_setting_data_to_null_triggers_snapshot_restore(): void`
       Listener body: `$arPayload['data'] = null;`
       Assertions: same as test 1.

    Each new test follows this skeleton (Hungarian vars, no comment pollution):

    ```php
    public function test_listener_unsetting_data_key_triggers_snapshot_restore(): void
    {
        Event::listen(
            SendCapiEvent::HOOK_BEFORE_DISPATCH,
            function (string $sEventName, array &$arPayload, object $obSubject): void {
                unset($arPayload['data']);
            },
        );

        $obSpyClient = new SpyMetaClient;
        $obJob = new SendCapiEvent('Purchase', $this->makePayload(), new stdClass, FakeStubAdapter::class);
        $obJob->handle(app(AdapterRegistry::class), $obSpyClient);

        $this->assertSame(1, $obSpyClient->iCallCount);
        $this->assertSame('uuid-1', $obSpyClient->arLastPayload['data'][0]['event_id'] ?? null);
        $this->assertSame(1700000000, $obSpyClient->arLastPayload['data'][0]['event_time'] ?? null);
        $this->assertSame('Purchase', $obSpyClient->arLastPayload['data'][0]['event_name'] ?? null);
    }
    ```

    Replicate the skeleton for the other two methods with the listener body and assertions described in the `<behavior>` block.

    Constraints:
      - Hungarian: `$obSpyClient`, `$obJob`, `$arPayload`, `$sEventName`, `$obSubject` (matches existing file).
      - No `// CR-01`, `// Phase 2`, `// gap`, `// Plan 8` markers.
      - No `assert()` — use PHPUnit `$this->assertSame()` matching the existing test style.
      - PHPStan level 10 will run against the file; the `?? null` guard on `arLastPayload['data'][0]['...']` keeps the static analyser happy with `?array` access (matches existing test line 53).
      - The three new test methods must not introduce any new `use` statement — every required symbol (`Event`, `AdapterRegistry`, `SendCapiEvent`, `MetaClient`, `FakeStubAdapter`, `SpyMetaClient`, `stdClass`) is already imported (lines 3-9 of the existing file).
  </action>
  <verify>
    <automated>cd /home/forge/nailscosmetics.lv/plugins/logingrupa/metapixel &amp;&amp; grep -c "^    public function test_" tests/Unit/Hook/BeforeDispatchPayloadMutationTest.php | grep -qx 5 &amp;&amp; grep -q "test_listener_unsetting_data_key_triggers_snapshot_restore" tests/Unit/Hook/BeforeDispatchPayloadMutationTest.php &amp;&amp; grep -q "test_listener_replacing_data0_with_arbitrary_record_restores_snapshot_no_leak" tests/Unit/Hook/BeforeDispatchPayloadMutationTest.php &amp;&amp; grep -q "test_listener_setting_data_to_null_triggers_snapshot_restore" tests/Unit/Hook/BeforeDispatchPayloadMutationTest.php</automated>
  </verify>
  <done>
    - Five `public function test_*` methods in the file (2 existing + 3 new).
    - All three new test method names match the names in the `<behavior>` block exactly.
    - Each new test instantiates `SpyMetaClient` locally, calls `$obJob->handle(...)`, asserts `iCallCount === 1` and the relevant `arLastPayload['data'][0]['...']` keys.
    - No new `use` statements added.
    - No comment-pollution markers in the new code.
  </done>
</task>

<task type="auto">
  <name>Task 3: Run composer qa and confirm green on all four gates</name>
  <files>(no source edits — gate run only)</files>
  <action>
    From `plugins/logingrupa/metapixel/` run the full qa pipeline using the root-project vendor binaries that UAT used (see 02-HUMAN-UAT.md line 18 — pint, phpstan, phpmd, pest all resolved through `/home/forge/nailscosmetics.lv/vendor/bin/`).

    Sequence:
      1. `/home/forge/nailscosmetics.lv/vendor/bin/pint --test plugins/logingrupa/metapixel` — confirm no style drift in the edited files.
      2. `/home/forge/nailscosmetics.lv/vendor/bin/phpstan analyse -c plugins/logingrupa/metapixel/phpstan.neon --no-progress` — level 10 must report `[OK] No errors`.
      3. `/home/forge/nailscosmetics.lv/vendor/bin/phpmd plugins/logingrupa/metapixel/classes,plugins/logingrupa/metapixel/tests text plugins/logingrupa/metapixel/phpmd.xml` — must produce no output (PHPMD prints findings, silent = pass).
      4. `cd plugins/logingrupa/metapixel && /home/forge/nailscosmetics.lv/vendor/bin/pest --colors=never` (full-Lovata Run A) — all tests must pass; the three new tests must be in the pass count.
      5. `cd plugins/logingrupa/metapixel && /home/forge/nailscosmetics.lv/vendor/bin/pest --colors=never --coverage --min=90` — coverage gate ≥ 90 % must hold. The CR-01 fix adds one new branch (shape-break Log::warning + snapshot restore); the three new tests exercise it directly so coverage should rise, not fall.
      6. `cd plugins/logingrupa/metapixel && /home/forge/nailscosmetics.lv/vendor/bin/pest --colors=never --exclude-group=adapter` (minimal-install Run B) — confirm the new tests stay in the non-adapter group and run in Run B too (the file is NOT under `tests/Unit/Adapter/*` and the class has no `#[Group('adapter')]` attribute, so it runs in both cells; this command confirms it).

    If any gate fails:
      - Pint failure → run `/home/forge/nailscosmetics.lv/vendor/bin/pint plugins/logingrupa/metapixel` to autofix, re-run gate 1.
      - PHPStan failure → re-read the error; common cause will be `array` access without `?? null` guard. Use the existing `?? null` pattern (file line 53) to fix.
      - PHPMD failure → likely `ShortVariable` if `$x` or similar slipped in; rename to a Hungarian-prefixed name (`$obJob`, `$obSpyClient`, etc.).
      - Pest failure on Case C (test 2): the assertion `evil_field === 'x'` will hold if the shape-break branch in Task 1 was implemented exactly per the reviewer's spec (CR-01 starts at line 84 of 02-REVIEW.md). If it instead fails, the executor over-engineered Task 1 — revisit and tighten to the reviewer's exact branch.
      - Coverage <90 %: do not weaken — the qa gate is the same one UAT measured at 90.2 %, and the new tests exercise a brand-new code branch. If coverage drops, something else regressed; investigate via the coverage HTML report (`storage/coverage/`).

    Do NOT modify ROADMAP.md, REQUIREMENTS.md, .planning/STATE.md, the v1.x branch, or any unrelated source file. Do NOT add new `composer` scripts. Do NOT touch `composer.json` / `phpstan.neon` / `phpmd.xml`.
  </action>
  <verify>
    <automated>cd /home/forge/nailscosmetics.lv/plugins/logingrupa/metapixel &amp;&amp; /home/forge/nailscosmetics.lv/vendor/bin/pest --colors=never --coverage --min=90 2>&amp;1 | tail -5 | grep -qE 'Total:\s+(9[0-9]|100)\.[0-9]+\s*%'</automated>
  </verify>
  <done>
    - Gate 1 (pint --test) exits 0.
    - Gate 2 (phpstan level 10) reports `[OK] No errors`.
    - Gate 3 (phpmd) is silent (no findings).
    - Gate 4 (pest full-lovata) all tests pass; pass count ≥ 430 (current 427 + 3 new); the three new test method names appear in `--filter` output if used to spot-check.
    - Gate 5 (pest --coverage --min=90) `Total: ≥ 90.0 %` AND `[OK]` exit status.
    - Gate 6 (pest --exclude-group=adapter) passes; the three new tests are included in this run (the test class has no `#[Group('adapter')]` attribute).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| third-party listener → SendCapiEvent payload | Untrusted listener mutates `$arPayload` by reference inside `Event::fire(HOOK_BEFORE_DISPATCH, [&...], true)`. Listener may be operator-written, plugin-vendored, or compromised. |
| SendCapiEvent payload → MetaClient::sendForPixel | Post-hook `$this->arPayload` is shipped to Meta CAPI under the operator's `pixel_id` + `capi_access_token`. Anything that survives the hook lands in Meta's ledger under the operator's identity. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-08-01 | Tampering | `SendCapiEvent::fireBeforeDispatchHalt` — listener replaces `$arPayload['data'][0]` with `['event_id' => $original, 'evil_field' => 'x']` keeping array shape | accept | Reviewer's adopted fix (CR-01) restores only `event_id` + `event_time` on intact-shape path; remaining keys are listener-mutation surface. Documented via test `test_listener_replacing_data0_with_arbitrary_record_restores_snapshot_no_leak` and the docblock addition on `fireBeforeDispatchHalt`. Tightening to canonical-key whitelist deferred to v2.1 (out of scope per CR-01 operator decision). |
| T-02-08-02 | Spoofing | `SendCapiEvent::fireBeforeDispatchHalt` — listener `unset($arPayload['data'])` ships empty envelope to Meta under the operator's pixel | mitigate | Task 1 shape-break detection branch restores pre-hook `$arSnapshot` and emits `Log::warning`. Test `test_listener_unsetting_data_key_triggers_snapshot_restore` enforces. |
| T-02-08-03 | Repudiation | Silent envelope corruption with no log signal | mitigate | `Log::warning('metapixel: before_dispatch listener destroyed envelope shape — restoring snapshot', ['meta_pixel.event_id' => $sEventId])` emits at WARNING level with the dedup anchor for ops correlation. |
| T-02-08-SC | Tampering | composer / npm / pip / cargo installs introduced by this plan | N/A | No package installs. Plan modifies one source file and one test file. No `composer require`, no new dependencies. RESEARCH.md Package Legitimacy Audit gate is not triggered. |
</threat_model>

<verification>
Phase-level acceptance from 02-HUMAN-UAT.md is "Test 2 result moves from `issue` → `pass`." Concretely:

1. The reviewer's proposed code is in `classes/queue/SendCapiEvent.php::fireBeforeDispatchHalt` (Task 1).
2. The three CR-01 coverage tests are in `tests/Unit/Hook/BeforeDispatchPayloadMutationTest.php` and they pass (Task 2 + Task 3 gate 4).
3. `composer qa` is green on all four sub-gates — pint-test, phpstan level 10, phpmd, pest --coverage --min=90 (Task 3 gates 1-5).
4. Run B (`--exclude-group=adapter`) still green; the new tests are non-adapter and run in both cells (Task 3 gate 6).
5. The new code emits `Log::warning` with `meta_pixel.event_id` context when the shape-break branch fires (verifiable by enabling Laravel's log channel in a manual smoke; not asserted in automated tests because Pest log assertions would require an extra Mockery layer — acceptable per CR-01's adopted scope).

After execution, the orchestrator updates `02-HUMAN-UAT.md` Test 2 row to `result: pass` + adds a `verified_by:` line referencing this plan's commit SHA.
</verification>

<success_criteria>
- `grep -q "arSnapshot" classes/queue/SendCapiEvent.php` → match (Task 1 applied).
- `grep -q "destroyed envelope shape — restoring snapshot" classes/queue/SendCapiEvent.php` → match (Log::warning string present).
- `grep -c "^    public function test_" tests/Unit/Hook/BeforeDispatchPayloadMutationTest.php` → `5` (2 existing + 3 new).
- `vendor/bin/pest --coverage --min=90` exits 0 with `Total: ≥ 90.0 %`.
- `vendor/bin/phpstan analyse` reports `[OK] No errors`.
- `vendor/bin/pint --test` exits 0.
- `vendor/bin/phpmd` produces no findings.
- `vendor/bin/pest --exclude-group=adapter` passes (minimal-install Run B parity).
- No `// CR-01`, `// Phase 2`, `// Plan 8`, `// gap_closure` markers in source (grep -vE '^\\s*\\*|^\\s*//' classes/queue/SendCapiEvent.php tests/Unit/Hook/BeforeDispatchPayloadMutationTest.php | grep -cE 'CR-01|Phase 2|gap_closure|Plan 8' == 0).
</success_criteria>

<output>
Create `plugins/logingrupa/metapixel/.planning/phases/02-adapter-system-core-contracts-registry-extension-hooks/02-08-SUMMARY.md` when done. Include:
- The exact `fireBeforeDispatchHalt` diff (before/after).
- The three new test method signatures.
- `composer qa` final output (last 5 lines of each gate).
- A 1-line note: "02-HUMAN-UAT.md Test 2 ready to flip to `pass` — orchestrator must update result + verified_by."
</output>
