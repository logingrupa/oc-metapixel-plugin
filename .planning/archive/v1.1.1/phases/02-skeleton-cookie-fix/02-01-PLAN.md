---
phase: 02-skeleton-cookie-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Plugin.php
  - plugin.yaml
  - models/Settings.php
  - models/settings/fields.yaml
  - lang/en/lang.php
  - lang/lv/lang.php
  - lang/ru/lang.php
  - composer.json
  - tests/Feature/SettingsRegistrationTest.php
requirements:
  - SKEL-01
  - SKEL-02
  - SKEL-06
autonomous: true
estimated_minutes: 90
tags:
  - octobercms
  - settings
  - lovata-toolbox
  - i18n
user_setup: []
notes: |
  Deliberate 8-task layout (above the standard 2-3 budget). Splitting would cascade
  depends_on rewrites across 02-02/03/04 with no execution-quality gain — the chain
  is strictly serial (Plugin.php boot → Settings model → fields.yaml → lang scaffolding
  → composer.json → feature test → qa gate). Tasks are individually small (≤ 15% context
  each) and group naturally by file family. Plan-checker W3 acknowledged.

must_haves:
  truths:
    - "Activating the plugin on a clean OctoberCMS 4.x + Shopaholic install boots without exceptions."
    - "Backend → Settings → Meta Pixel renders all 10 SKEL-02 fields."
    - "`paid_status_code` dropdown is populated from `\\Lovata\\OrdersShopaholic\\Models\\Status::lists('name','code')` and `new-payment-received` is selectable."
    - "All Settings field labels resolve via RainLab.Translate keys in `logingrupa.metapixelshopaholic::lang.field.*` (lang files present in en/lv/ru)."
    - "composer.json no longer hard-requires lovata/buddies-plugin (moved to `suggest`); phpmd script scans the widened directory set."
  artifacts:
    - path: "Plugin.php"
      provides: "Plugin metadata (lang-keyed) and `registerSettings()` mapping to Settings::class."
      contains: "registerSettings"
    - path: "models/Settings.php"
      provides: "Settings extending Lovata\\Toolbox\\Models\\CommonSettings with SETTINGS_CODE + `getPaidStatusCodeOptions()` + `getQueueConnectionOptions()`."
      contains: "class Settings extends CommonSettings"
    - path: "models/settings/fields.yaml"
      provides: "Form definition for the 10 SKEL-02 fields."
      contains: "pixel_id"
    - path: "lang/en/lang.php"
      provides: "RainLab.Translate keys for plugin metadata + settings labels + field labels."
      contains: "'plugin'"
    - path: "lang/lv/lang.php"
      provides: "Latvian lang scaffolding mirroring en key tree."
      contains: "'plugin'"
    - path: "lang/ru/lang.php"
      provides: "Russian lang scaffolding mirroring en key tree."
      contains: "'plugin'"
    - path: "composer.json"
      provides: "lovata/buddies-plugin moved to suggest; phpmd script widened to scan classes,middleware,models,components,controllers,updates."
      contains: "\"phpmd\""
    - path: "tests/Feature/SettingsRegistrationTest.php"
      provides: "Round-trip test for Settings::get/set + dropdown options + registerSettings array key + per-key fields.yaml lang assertion."
      contains: "logingrupa-metapixelshopaholic-settings"
  key_links:
    - from: "Plugin::registerSettings()"
      to: "Logingrupa\\Metapixelshopaholic\\Models\\Settings::class"
      via: "settings array entry"
      pattern: "'class'\\s*=>\\s*Settings::class"
    - from: "models/settings/fields.yaml"
      to: "Settings::getPaidStatusCodeOptions()"
      via: "`options: getPaidStatusCodeOptions` field key"
      pattern: "options:\\s*getPaidStatusCodeOptions"
    - from: "models/Settings.php::getPaidStatusCodeOptions()"
      to: "Lovata\\OrdersShopaholic\\Models\\Status::lists('name','code')"
      via: "static method call"
      pattern: "Status::lists\\('name',\\s*'code'\\)"
---

<objective>
Ship the plugin's identity layer: a rewritten `Plugin.php` (lang-keyed metadata, narrowed `$require`, `registerSettings()`), a `Settings` model extending `Lovata\Toolbox\Models\CommonSettings` with the 10 SKEL-02 fields and their dropdown option providers, a backend `fields.yaml`, an optional `plugin.yaml`, RainLab.Translate-compatible `lang/{en,lv,ru}/lang.php` scaffolding, and `composer.json` cleanup (drop hard Buddies require → suggest; widen phpmd scope). The plan also lands the first Pest feature test (`SettingsRegistrationTest`) covering round-trip Settings I/O + dropdown population + per-key fields.yaml lang-binding assertion.

Purpose: Without a working Settings model the rest of Phase 2 has nothing to read. Without lang scaffolding RainLab.Translate's `|_` filter prints raw key strings in the backend. composer.json hygiene closes Phase 1 leftovers LR-01-adjacent and MR-02 before they reopen at every later phase.

Output: Boot-loadable plugin scaffold whose Settings page renders 10 fields with `paid_status_code` populated from live Status codes, lang keys resolving, `composer qa` green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-skeleton-cookie-fix/02-CONTEXT.md
@.planning/phases/02-skeleton-cookie-fix/02-PATTERNS.md
@CLAUDE.md
@../../../CLAUDE.md
@Plugin.php
@composer.json
@tests/MetapixelTestCase.php
@tests/Unit/SanityTest.php

<interfaces>
<!-- Pre-existing contracts that Plan 02-01 consumes. Executor does not need to grep — these
     are the canonical signatures. -->

From plugins/lovata/toolbox/models/CommonSettings.php:
```php
namespace Lovata\Toolbox\Models;

class CommonSettings extends \System\Models\SettingModel
{
    // implements RainLab.Translate.Behaviors.TranslatableModel
    // uses October\Rain\Database\Traits\Multisite
    public $settingsFields = 'fields.yaml';
    public $propagatable = [];

    public static function get($sCode, $default = null);   // inherited from SettingModel
    public static function set($sCode, $sValue);
    public static function clearInternalCache(): void;
}
```

From plugins/lovata/ordersshopaholic/models/Status.php:
```php
namespace Lovata\OrdersShopaholic\Models;

class Status extends \October\Rain\Database\Model
{
    // CodeField scope provides "lists('name', 'code')" => array<string code, string name>
    public static function lists(string $sValue, string $sKey): array;
}
```

From plugins/logingrupa/goodsreceivedshopaholic/Plugin.php (canonical sibling):
```php
public function pluginDetails(): array;
public function registerSettings(): array;      // returns ['code' => ['label' => ..., 'class' => ::class, ...]]
```

From plugins/logingrupa/goodsreceivedshopaholic/models/Settings.php (pattern reference, NOT to be copied verbatim — must extend CommonSettings per SKEL-02, not bare SettingModel):
```php
public const SETTINGS_CODE = 'logingrupa_goodsreceivedshopaholic_settings';
public $settingsCode = self::SETTINGS_CODE;
public $settingsFields = 'fields.yaml';
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewrite Plugin.php with lang-keyed metadata, narrowed require, registerSettings()</name>
  <files>Plugin.php</files>
  <read_first>
    - Plugin.php (current — minimal shell from Phase 1)
    - plugins/logingrupa/goodsreceivedshopaholic/Plugin.php (canonical analog — lines 1-43, 117-145, 164-177)
    - plugins/logingrupa/campaignpricingshopaholic/Plugin.php (lang-keyed pluginDetails reference — lines 33-41, 67)
    - .planning/phases/02-skeleton-cookie-fix/02-PATTERNS.md — sections P3 (#[\Override]), P4 ($require list), P5 (lang-key strings)
    - .planning/phases/02-skeleton-cookie-fix/02-CONTEXT.md — Area 1 Q1, Q4 (boot strategy + $require value)
  </read_first>
  <behavior>
    - Test 1: `(new Plugin())->pluginDetails()` returns array with `name` value `'logingrupa.metapixelshopaholic::lang.plugin.name'` and `description` value `'logingrupa.metapixelshopaholic::lang.plugin.description'`.
    - Test 2: `(new Plugin())->registerSettings()` returns an array whose sole key is `'logingrupa-metapixelshopaholic-settings'` with `class` => `Logingrupa\Metapixelshopaholic\Models\Settings::class`.
    - Test 3: `Plugin::$require` is a list equal to `['Lovata.Toolbox', 'Lovata.Shopaholic', 'Lovata.OrdersShopaholic']` (Buddies absent — CONTEXT Area 1 Q4 locks this).
    - Test 4: `boot()` is callable without throwing on an empty Settings table (no Event::subscribe calls yet — only this plan; middleware push + PluginGuard prime land in Plans 03/04).
  </behavior>
  <action>
    Replace `Plugin.php` so the class declares:

    1. Namespace `Logingrupa\Metapixelshopaholic`. Imports: `Backend`, `Illuminate\Support\Facades\App`, `Logingrupa\Metapixelshopaholic\Models\Settings`, `System\Classes\PluginBase`. Do NOT add `declare(strict_types=1)` (sibling `campaignpricingshopaholic/Plugin.php` omits it; Phase 2 CONTEXT does not lock strict_types — match the closer sibling).

    2. `public $require` typed via PHPDoc `@var list<string>` with value `['Lovata.Toolbox', 'Lovata.Shopaholic', 'Lovata.OrdersShopaholic']`. NO `'Lovata.Buddies'`.

    3. `#[\Override] public function pluginDetails(): array` returning:
       - `name`        => `'logingrupa.metapixelshopaholic::lang.plugin.name'`
       - `description` => `'logingrupa.metapixelshopaholic::lang.plugin.description'`
       - `author`      => `'Logingrupa'`
       - `icon`        => `'icon-shopping-cart'`
       - `homepage`    => `'https://logingrupa.lv'`

    4. `#[\Override] public function registerSettings(): array` returning a single entry keyed `'logingrupa-metapixelshopaholic-settings'`:
       - `label`       => `'logingrupa.metapixelshopaholic::lang.settings.label'`
       - `description` => `'logingrupa.metapixelshopaholic::lang.settings.description'`
       - `category`    => `'lovata.shopaholic::lang.tab.settings'`
       - `icon`        => `'icon-shopping-cart'`
       - `class`       => `Settings::class`
       - `order`       => 500

    5. `#[\Override] public function boot(): void` body is intentionally empty in this plan. Add a single-line comment block: `// Boot wiring lands in Plan 02-02 (PluginGuard prime) and 02-03 (kernel pushMiddleware). Phase 2 keeps boot minimal per CONTEXT Area 1 Q1.`

    6. PHPDoc on `pluginDetails()`: `@return array{name: string, description: string, author: string, icon: string, homepage: string}`. PHPDoc on `registerSettings()`: `@return array<string, array<string, mixed>>`.

    Per repo CLAUDE.md Tiger-Style: short function, explicit return types declared. Hungarian notation applies (no local vars introduced here so no `$ob*` rename needed).
  </action>
  <verify>
    <automated>cd /home/forge/nailscosmetics.lv/plugins/logingrupa/metapixelshopaholic && ../../../vendor/bin/phpstan analyse Plugin.php --configuration=phpstan.neon --no-progress</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "lang\.plugin\.(name|description)" Plugin.php` returns at least 2 matches.
    - `grep -nE "Lovata\.Buddies" Plugin.php` returns 0 lines.
    - `grep -nE "Lovata\.OrdersShopaholic" Plugin.php` returns ≥ 1 line.
    - `grep -nE "registerSettings|pluginDetails" Plugin.php` returns ≥ 2 matches each guarded by `#[\\Override]`.
    - `php -l Plugin.php` reports "No syntax errors detected".
  </acceptance_criteria>
  <done>
    Plugin.php boots minimally, declares the locked `$require` list, exposes lang-keyed `pluginDetails()` + `registerSettings()` returning the Settings class. phpstan level 10 green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create plugin.yaml metadata (mirrors pluginDetails())</name>
  <files>plugin.yaml</files>
  <read_first>
    - plugins/logingrupa/campaignpricingshopaholic/plugin.yaml (lines 1-6 — canonical sibling)
    - Plugin.php (just-written pluginDetails() for value parity)
    - .planning/phases/02-skeleton-cookie-fix/02-CONTEXT.md — Claude's Discretion bullet 6 (include yaml — recommendation: yes, for explicit metadata declaration)
  </read_first>
  <behavior>
    - Test 1: YAML parses without error (`yaml_parse_file('plugin.yaml')` returns array).
    - Test 2: Top-level key is `plugin`; child keys are `name`, `description`, `author`, `icon`, `homepage`.
    - Test 3: `plugin.name` value equals the lang key `logingrupa.metapixelshopaholic::lang.plugin.name`.
  </behavior>
  <action>
    Create `plugin.yaml` with single top-level `plugin` mapping containing exactly the same five fields as Plugin.php `pluginDetails()`:

    - `name`: `'logingrupa.metapixelshopaholic::lang.plugin.name'`
    - `description`: `'logingrupa.metapixelshopaholic::lang.plugin.description'`
    - `author`: `Logingrupa`
    - `icon`: `icon-shopping-cart`
    - `homepage`: `'https://logingrupa.lv'`

    Use 4-space indentation matching `campaignpricingshopaholic/plugin.yaml`. Single-quote the lang-key strings (YAML interprets `::` safely unquoted, but quoting keeps it readable and avoids future parser drift).
  </action>
  <verify>
    <automated>cd /home/forge/nailscosmetics.lv/plugins/logingrupa/metapixelshopaholic && php -r "var_dump(yaml_parse_file('plugin.yaml'));" 2>&1 | grep -E "logingrupa.metapixelshopaholic::lang.plugin.name"</automated>
  </verify>
  <acceptance_criteria>
    - `yaml_parse_file('plugin.yaml')` returns an associative array with `plugin.name` containing the lang-key string.
    - `diff <(grep -oE "logingrupa\\.metapixelshopaholic::lang\\.plugin\\.[a-z]+" plugin.yaml | sort -u) <(grep -oE "logingrupa\\.metapixelshopaholic::lang\\.plugin\\.[a-z]+" Plugin.php | sort -u)` produces empty output (yaml and PHP reference the same lang keys).
  </acceptance_criteria>
  <done>
    `plugin.yaml` parses cleanly and its `plugin.*` keys match `Plugin::pluginDetails()` lang-key references one-to-one.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create models/Settings.php extending CommonSettings with dropdown providers</name>
  <files>models/Settings.php</files>
  <read_first>
    - plugins/lovata/toolbox/models/CommonSettings.php (lines 11-41 — parent contract)
    - plugins/logingrupa/goodsreceivedshopaholic/models/Settings.php (lines 1-47 — analog; note: it extends bare SettingModel, we must extend CommonSettings per SKEL-02)
    - plugins/lovata/shopaholic/models/Settings.php (lines 25-61 — `getXxxOptions()` convention)
    - plugins/lovata/ordersshopaholic/models/Status.php (line 39 — `CodeField` scope used by `lists('name','code')`)
    - .planning/phases/02-skeleton-cookie-fix/02-CONTEXT.md — Area 1 specifics + Claude's Discretion bullet 5 (tab grouping)
  </read_first>
  <behavior>
    - Test 1: `new Settings()` instance exposes `settingsCode === 'logingrupa_metapixelshopaholic_settings'`.
    - Test 2: `(new Settings())->getPaidStatusCodeOptions()` returns an array. When the Shopaholic status seeder ran, the array contains key `'new-payment-received'`.
    - Test 3: `(new Settings())->getQueueConnectionOptions()` returns exactly `['database' => 'database', 'redis' => 'redis', 'sync' => 'sync']`.
    - Test 4: `$translatable` array contains the string `'pixel_id'` (RainLab.Translate per-site override path).
    - Test 5: Class extends `Lovata\Toolbox\Models\CommonSettings`, not bare `SettingModel`.
  </behavior>
  <action>
    Create `models/Settings.php` declaring `namespace Logingrupa\Metapixelshopaholic\Models` with imports `Lovata\OrdersShopaholic\Models\Status` and `Lovata\Toolbox\Models\CommonSettings`.

    Class signature: `class Settings extends CommonSettings`.

    Required class members (Hungarian notation applies to local variables; class properties stay PSR-2 / sibling convention since CommonSettings exposes `$settingsCode`, `$settingsFields`, `$translatable` literally):

    1. `public const SETTINGS_CODE = 'logingrupa_metapixelshopaholic_settings';`
    2. `public $settingsCode = self::SETTINGS_CODE;`
    3. `public $settingsFields = 'fields.yaml';`
    4. `public $translatable = ['pixel_id'];` — declared as `@var array<int, string>`.

    Two public methods (auto-invoked by October form builder via `options: getXxxOptions` in fields.yaml):

    - `public function getPaidStatusCodeOptions(): array` — body: `$arList = (array) Status::lists('name', 'code'); return $arList;`. PHPDoc `@return array<string, string>`. Use `Status::lists('name','code')` exactly — NOT `pluck` (CONTEXT specifics line 110).
    - `public function getQueueConnectionOptions(): array` — returns the literal array `['database' => 'database', 'redis' => 'redis', 'sync' => 'sync']`. PHPDoc `@return array<string, string>`. Default value `'database'` is enforced in `fields.yaml` (next task), not here.

    No constructor. No `boot()` override. No event listeners. `CommonSettings` already implements `RainLab.Translate.Behaviors.TranslatableModel` and uses the `Multisite` trait — do not redeclare them.

    Add file-level PHPDoc block summarising: "Logingrupa.Metapixelshopaholic Settings model. Backed by Lovata\\Toolbox\\Models\\CommonSettings (Multisite + RainLab.Translate). Field schema in fields.yaml. Field-options methods are auto-invoked by October's form builder when `options: getXxxOptions` is declared in fields.yaml."
  </action>
  <verify>
    <automated>cd /home/forge/nailscosmetics.lv/plugins/logingrupa/metapixelshopaholic && ../../../vendor/bin/phpstan analyse models/Settings.php --configuration=phpstan.neon --no-progress</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "extends CommonSettings" models/Settings.php` ≥ 1.
    - `grep -c "Status::lists('name', 'code')" models/Settings.php` ≥ 1.
    - `grep -cE "function (getPaidStatusCodeOptions|getQueueConnectionOptions)\\(\\): array" models/Settings.php` == 2.
    - `grep -c "logingrupa_metapixelshopaholic_settings" models/Settings.php` ≥ 2 (constant + property).
    - phpstan level 10 produces 0 errors on this file.
  </acceptance_criteria>
  <done>
    Settings model loads via October's SettingsManager, exposes the locked `settingsCode`, and serves the two dropdown option providers consumed by `fields.yaml` Task 4.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Create models/settings/fields.yaml with the 10 SKEL-02 field definitions</name>
  <files>models/settings/fields.yaml</files>
  <read_first>
    - plugins/logingrupa/goodsreceivedshopaholic/models/settings/fields.yaml (lines 6-29 — switch + text + lang-keyed labels)
    - plugins/lovata/shopaholic/models/settings/fields.yaml (lines 29-96 — tab grouping + dropdown with `options: methodName`)
    - models/Settings.php (just-written — option provider method names)
    - .planning/phases/02-skeleton-cookie-fix/02-CONTEXT.md — Area 1 specifics (10 fields locked); Claude's Discretion bullet 5 (tab grouping decision)
  </read_first>
  <behavior>
    - Test 1: YAML parses; top-level key is `fields` (or `tabs.fields` for tabbed forms).
    - Test 2: Every one of the 10 SKEL-02 field keys exists exactly once.
    - Test 3: `pixel_id` declares `type: text` and `translatable: true`.
    - Test 4: `capi_access_token` declares `type: password` (DOM-masked per threat model).
    - Test 5: `paid_status_code` declares `type: dropdown`, `default: new-payment-received`, `options: getPaidStatusCodeOptions`.
    - Test 6: `queue_connection` declares `type: dropdown`, `default: database`, `options: getQueueConnectionOptions`.
    - Test 7: For each of the 10 field keys, `label` value equals `logingrupa.metapixelshopaholic::lang.field.<key>` AND `commentAbove` value equals `logingrupa.metapixelshopaholic::lang.field.<key>_comment`. (Per-key assertion — replaces W8 brittle count-only grep.)
  </behavior>
  <action>
    Create `models/settings/fields.yaml` using October's tabbed form structure (decision: split into three tabs — `Tracking`, `Compliance`, `Advanced` — per CONTEXT discretion bullet 5; recommended by pattern map S1). YAML root:

    ```
    tabs:
        fields:
            <field_key>:
                <field_attrs>
    ```

    Tab assignments via `tab:` key on each field (lang-keyed):

    **Tab 1 — `'logingrupa.metapixelshopaholic::lang.tab.tracking'`:**
    - `pixel_id` — type: `text`, translatable: `true`, span: `left`, required: `true`, `label`/`commentAbove` keys.
    - `capi_access_token` — type: `password`, span: `right`, required: `true`. Password type ensures DOM masking (threat model T-02-04).
    - `test_event_code` — type: `text`, span: `left`, default omitted (empty).
    - `currency_code` — type: `text`, span: `right`, default: `EUR`.

    **Tab 2 — `'logingrupa.metapixelshopaholic::lang.tab.compliance'`:**
    - `phone_country_code` — type: `text`, span: `left`, default: `'371'` (quoted to preserve leading char safety in YAML scalar parsing).
    - `send_hashed_pii` — type: `switch`, span: `right`, default: `true`.
    - `ensure_fbp_fbc_server_side` — type: `switch`, span: `left`, default: `true`.

    **Tab 3 — `'logingrupa.metapixelshopaholic::lang.tab.advanced'`:**
    - `queue_connection` — type: `dropdown`, span: `left`, default: `database`, `options: getQueueConnectionOptions`.
    - `paid_status_code` — type: `dropdown`, span: `right`, default: `new-payment-received`, `options: getPaidStatusCodeOptions`, `emptyOption: 'lovata.toolbox::lang.field.empty'`.
    - `refire_purchase_on_status_flip` — type: `switch`, span: `left`, default: `false`.

    Each field MUST declare:
    - `label`: `'logingrupa.metapixelshopaholic::lang.field.<key>'`
    - `commentAbove`: `'logingrupa.metapixelshopaholic::lang.field.<key>_comment'`
    - `tab`: lang-key for the parent tab
    - `type`: as listed above
    - `default`: as listed above (omit for `pixel_id`, `capi_access_token`, `test_event_code`)

    Indent with 4 spaces (October's YAML form convention; matches `goodsreceivedshopaholic/models/settings/fields.yaml`).

    Per-key label/commentAbove assertion (W8 fix): the SettingsRegistrationTest in Task 7 carries the per-key check by parsing the YAML via `Symfony\Component\Yaml\Yaml::parseFile()` (already available in Laravel 12 via `symfony/yaml`) and asserting each of the 10 field keys has the exact lang-key pair declared above. Grep-only count gates are removed from this task's acceptance.
  </action>
  <verify>
    <automated>cd /home/forge/nailscosmetics.lv/plugins/logingrupa/metapixelshopaholic && php -r '$a=yaml_parse_file("models/settings/fields.yaml"); $keys=array_keys($a["tabs"]["fields"]); $required=["pixel_id","capi_access_token","test_event_code","currency_code","phone_country_code","send_hashed_pii","queue_connection","paid_status_code","refire_purchase_on_status_flip","ensure_fbp_fbc_server_side"]; sort($keys); sort($required); echo (json_encode($keys)===json_encode($required) ? "OK 10 fields" : "FAIL ".json_encode(array_diff($required,$keys))).PHP_EOL;' | grep -q "OK 10 fields"</automated>
  </verify>
  <acceptance_criteria>
    - YAML parses (`yaml_parse_file` returns array).
    - All 10 SKEL-02 field keys present exactly once (verify script above prints `OK 10 fields`).
    - `grep -c "translatable: true" models/settings/fields.yaml` ≥ 1 (at least pixel_id).
    - `grep -c "type: password" models/settings/fields.yaml` == 1 (capi_access_token).
    - `grep -c "options: getPaidStatusCodeOptions" models/settings/fields.yaml` == 1.
    - `grep -c "options: getQueueConnectionOptions" models/settings/fields.yaml` == 1.
    - Per-key lang-binding assertion enforced by `SettingsRegistrationTest::test_fields_yaml_binds_lang_keys_per_field` (Task 7) — passes for all 10 keys.
  </acceptance_criteria>
  <done>
    Backend Settings form definition exists with all 10 fields organised across 3 tabs, dropdowns wired to model option providers, every key's `label` + `commentAbove` proven to point at `logingrupa.metapixelshopaholic::lang.field.<key>` / `<key>_comment` via the Pest assertion in Task 7.
  </done>
</task>

<task type="auto">
  <name>Task 5: Scaffold lang/{en,lv,ru}/lang.php with full key tree (en authoritative; lv/ru repeat en)</name>
  <files>lang/en/lang.php, lang/lv/lang.php, lang/ru/lang.php</files>
  <read_first>
    - plugins/logingrupa/goodsreceivedshopaholic/lang/en/lang.php (lines 1-23 — structure)
    - plugins/logingrupa/campaignpricingshopaholic/lang/lv/lang.php (lines 1-44 — lv pattern)
    - plugins/logingrupa/campaignpricingshopaholic/lang/ru/lang.php (ru pattern)
    - models/settings/fields.yaml (Task 4 output — exact field keys + tab keys)
    - .planning/phases/02-skeleton-cookie-fix/02-CONTEXT.md — Area 4 Q4 (stub English values; lv/ru repeat English; full translation in Phase 5 HARD-04)
  </read_first>
  <action>
    Create three lang files with the identical key tree. English values stubbed in plain English; `lv` and `ru` files repeat the same English values verbatim per CONTEXT Area 4 Q4 (RainLab.Translate `|_` filter falls back gracefully but the keys MUST exist or the filter prints the raw key string).

    Top-level key tree (PHP array returned by each file):

    ```
    plugin => [name, description]
    settings => [label, description]
    component => [name, description]
    tab => [tracking, compliance, advanced]
    field => [
        pixel_id, pixel_id_comment,
        capi_access_token, capi_access_token_comment,
        test_event_code, test_event_code_comment,
        currency_code, currency_code_comment,
        phone_country_code, phone_country_code_comment,
        send_hashed_pii, send_hashed_pii_comment,
        queue_connection, queue_connection_comment,
        paid_status_code, paid_status_code_comment,
        refire_purchase_on_status_flip, refire_purchase_on_status_flip_comment,
        ensure_fbp_fbc_server_side, ensure_fbp_fbc_server_side_comment,
    ]
    ```

    Suggested English values (concise; full copy lands in Phase 5):
    - `plugin.name` = `'Meta Pixel + CAPI'`
    - `plugin.description` = `'Server-deduplicated Meta Pixel and Conversions API tracking for Lovata Shopaholic.'`
    - `settings.label` = `'Meta Pixel'`
    - `settings.description` = `'Configure Pixel ID, CAPI token, paid-status trigger, queue connection, and cookie behavior.'`
    - `component.name` = `'Pixel Head'`
    - `component.description` = `'Renders fbq() init + PageView with server-generated event_id. Coexists with the theme'\''s facebook_pixel.htm partial.'`
    - `tab.tracking` = `'Tracking'`
    - `tab.compliance` = `'Compliance'`
    - `tab.advanced` = `'Advanced'`
    - `field.pixel_id` = `'Pixel ID'`
    - `field.pixel_id_comment` = `'Numeric Meta Pixel ID from Events Manager. Required.'`
    - `field.capi_access_token` = `'CAPI Access Token'`
    - `field.capi_access_token_comment` = `'System-user access token with ads_management scope. Stored encrypted in system_settings.'`
    - `field.test_event_code` = `'Test Event Code'`
    - `field.test_event_code_comment` = `'Optional. When set, server events appear in Events Manager → Test Events.'`
    - `field.currency_code` = `'Currency Code'`
    - `field.currency_code_comment` = `'ISO 4217 default currency for events lacking an explicit value (e.g. EUR, NOK).'`
    - `field.phone_country_code` = `'Phone Country Code'`
    - `field.phone_country_code_comment` = `'Default E.164 country code applied to unprefixed phone numbers before hashing.'`
    - `field.send_hashed_pii` = `'Send Hashed PII to CAPI'`
    - `field.send_hashed_pii_comment` = `'When ON, sha256-hashed email / phone / name fields are sent server-side. Required for EMQ ≥ 8.'`
    - `field.queue_connection` = `'Queue Connection'`
    - `field.queue_connection_comment` = `'Laravel queue driver used by SendCapiEvent. Default: database.'`
    - `field.paid_status_code` = `'Paid Status Code'`
    - `field.paid_status_code_comment` = `'OrdersShopaholic Status whose entry triggers Purchase CAPI dispatch. Default: new-payment-received.'`
    - `field.refire_purchase_on_status_flip` = `'Re-fire Purchase on Status Flip'`
    - `field.refire_purchase_on_status_flip_comment` = `'Off by default. When OFF, an order with a populated meta_purchase_event_id never re-fires Purchase.'`
    - `field.ensure_fbp_fbc_server_side` = `'Set _fbp / _fbc Server-side'`
    - `field.ensure_fbp_fbc_server_side_comment` = `'When ON, the EnsureFbpFbcCookies middleware sets _fbp / _fbc when missing. Fixes the empty-cookie bug.'`

    `lang/lv/lang.php` and `lang/ru/lang.php`: copy the en file verbatim (same English values). Full localisation deferred to Phase 5 HARD-04.

    Each file: standard PHP open tag `<?php` followed by `return [...]`. No `declare(strict_types=1)` (October convention: lang files are pure data).
  </action>
  <verify>
    <automated>cd /home/forge/nailscosmetics.lv/plugins/logingrupa/metapixelshopaholic && php -r '$en=require "lang/en/lang.php"; $lv=require "lang/lv/lang.php"; $ru=require "lang/ru/lang.php"; $flatten=function($a,$p="") use (&$flatten){$r=[]; foreach($a as $k=>$v){$nk=$p===""?$k:"$p.$k"; if(is_array($v)){$r=array_merge($r,$flatten($v,$nk));} else {$r[]=$nk;}} return $r;}; $kEn=$flatten($en); $kLv=$flatten($lv); $kRu=$flatten($ru); sort($kEn); sort($kLv); sort($kRu); echo (($kEn===$kLv && $kLv===$kRu && count($kEn) >= 25) ? "OK ".count($kEn)." keys" : "FAIL").PHP_EOL;' | grep -qE "OK [2-9][0-9]+ keys"</automated>
  </verify>
  <acceptance_criteria>
    - All three lang files return PHP arrays without parse errors.
    - en, lv, ru all expose the **same** flattened key set (≥ 25 keys including 10 field + 10 field_comment + plugin.* + settings.* + component.* + tab.*).
    - `grep -cE "'field' =>" lang/en/lang.php` == 1.
    - Verification script above prints `OK <N> keys` where N ≥ 25.
  </acceptance_criteria>
  <done>
    All RainLab.Translate keys referenced by Plugin.php, plugin.yaml, models/settings/fields.yaml resolve to English strings under en/lv/ru. Backend Settings page renders human-readable labels.
  </done>
</task>

<task type="auto">
  <name>Task 6: Update composer.json — Buddies suggest, phpmd scope widening</name>
  <files>composer.json</files>
  <read_first>
    - composer.json (current)
    - .planning/phases/02-skeleton-cookie-fix/02-PATTERNS.md — section S3 (phpmd scope widening)
    - .planning/STATE.md — Pending Todos MR-02
    - .planning/phases/02-skeleton-cookie-fix/02-CONTEXT.md — Area 1 Q4 (Buddies → suggest), Area 4 Q2 (phpmd widen)
  </read_first>
  <action>
    Edit `composer.json`:

    1. **Remove `lovata/buddies-plugin: ^1.10` from the `require` block.**
    2. **Add a top-level `suggest` block** (or extend if present) containing: `"lovata/buddies-plugin": "Optional — only required if your site uses Lovata.Buddies for user registration. UserHelper auto-detects at runtime.",`.
    3. **Replace the `phpmd` script value** from `"../../../vendor/bin/phpmd Plugin.php text phpmd.xml"` to `"../../../vendor/bin/phpmd Plugin.php,classes,middleware,models,components,controllers,updates text phpmd.xml"`. Pattern verbatim per PATTERNS S3 — directory names are lowercase, matching the lowercase directory layout used throughout Plans 02-02 / 02-03 / 02-04 (sibling-plugin precedent: `plugins/logingrupa/goodsreceivedshopaholic/classes/`, `plugins/logingrupa/storeextender/components/`, etc.).

    Do NOT touch the `require-dev`, `autoload`, `extra`, or `scripts.qa` blocks. Leave `minimum-stability` / `prefer-stable` unchanged.

    Run `php -r 'json_decode(file_get_contents("composer.json"), true) ?: exit(1);'` after edit to confirm valid JSON.
  </action>
  <verify>
    <automated>cd /home/forge/nailscosmetics.lv/plugins/logingrupa/metapixelshopaholic && php -r 'json_decode(file_get_contents("composer.json"), JSON_OBJECT_AS_ARRAY) ?: exit(1);' && jq -e '.require | has("lovata/buddies-plugin") | not' composer.json && jq -e '.suggest | has("lovata/buddies-plugin")' composer.json && jq -re '.scripts.phpmd' composer.json | grep -q "Plugin.php,classes,middleware,models,components,controllers,updates"</automated>
  </verify>
  <acceptance_criteria>
    - `jq '.require | has("lovata/buddies-plugin")' composer.json` returns `false`.
    - `jq '.suggest | has("lovata/buddies-plugin")' composer.json` returns `true`.
    - `jq -r '.scripts.phpmd' composer.json` exactly equals `"../../../vendor/bin/phpmd Plugin.php,classes,middleware,models,components,controllers,updates text phpmd.xml"`.
    - `composer validate --no-check-publish` exits 0 (or warns only about author email, which is fine).
  </acceptance_criteria>
  <done>
    composer.json deps reflect the SKEL-01 / CONTEXT Area 1 Q4 decisions; phpmd will scan all Phase-2-introduced directories (lowercase paths match the actual directory layout).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 7: Write tests/Feature/SettingsRegistrationTest.php (round-trip + dropdown + registerSettings + per-key fields.yaml lang assertion)</name>
  <files>tests/Feature/SettingsRegistrationTest.php</files>
  <read_first>
    - plugins/logingrupa/goodsreceivedshopaholic/tests/unit/Support/SettingsAccessorTest.php (lines 1-140 — Pest pattern + system_settings bootstrap)
    - tests/MetapixelTestCase.php (Phase 1 — `$autoMigrate=true` already runs `migrateModules()`)
    - tests/Unit/SanityTest.php (PHPUnit `extends MetapixelTestCase` pattern — Pest binding still flaky per Pest.php comment)
    - models/Settings.php (Task 3 output)
    - models/settings/fields.yaml (Task 4 output — per-key assertion targets)
    - Plugin.php (Task 1 output — registerSettings() return shape)
    - vendor/symfony/yaml/Yaml.php (verify `Yaml::parseFile()` static API — Laravel 12 ships symfony/yaml)
    - .planning/phases/02-skeleton-cookie-fix/02-PATTERNS.md — section "tests/Feature/SettingsRegistrationTest.php" (target assertions)
  </read_first>
  <behavior>
    - Test 1 (`test_register_settings_returns_meta_pixel_entry`): `(new Plugin())->registerSettings()` returns an array with exactly one key `'logingrupa-metapixelshopaholic-settings'`; the entry's `class` value equals `Logingrupa\Metapixelshopaholic\Models\Settings::class`.
    - Test 2 (`test_pixel_id_round_trips_through_settings`): `Settings::set('pixel_id', '2291486191076331'); Settings::clearInternalCache(); expect(Settings::get('pixel_id'))->toBe('2291486191076331');`.
    - Test 3 (`test_paid_status_code_options_contains_new_payment_received`): after `migrateModules()` has run (which seeds the `lovata_ordersshopaholic_statuses` table), `(new Settings())->getPaidStatusCodeOptions()` returns a non-empty array containing the key `'new-payment-received'`.
    - Test 4 (`test_queue_connection_options_returns_static_three_drivers`): `(new Settings())->getQueueConnectionOptions()` returns exactly `['database' => 'database', 'redis' => 'redis', 'sync' => 'sync']`.
    - Test 5 (`test_fields_yaml_binds_lang_keys_per_field`) — W8 fix: parse `models/settings/fields.yaml` via `Symfony\Component\Yaml\Yaml::parseFile()`. For each of the 10 keys `[pixel_id, capi_access_token, test_event_code, currency_code, phone_country_code, send_hashed_pii, queue_connection, paid_status_code, refire_purchase_on_status_flip, ensure_fbp_fbc_server_side]`, assert that `$arData['tabs']['fields'][$sKey]['label'] === "logingrupa.metapixelshopaholic::lang.field.$sKey"` AND `$arData['tabs']['fields'][$sKey]['commentAbove'] === "logingrupa.metapixelshopaholic::lang.field.{$sKey}_comment"`. On mismatch, fail with the offending key + actual value via `assertSame(..., ..., "Field '$sKey' label mismatch")`.
  </behavior>
  <action>
    Create `tests/Feature/SettingsRegistrationTest.php` as a **PHPUnit-style `final class` extending `MetapixelTestCase` directly** — mirror the `tests/Unit/SanityTest.php` pattern (Pest's `uses(...)->in()` binding is currently flaky per the comment in `tests/Pest.php`; sticking to the proven `extends` model avoids that until Phase 5 lands a repo-level harness).

    File structure:
    1. `namespace Logingrupa\Metapixelshopaholic\Tests\Feature;`
    2. `require_once __DIR__.'/../MetapixelTestCase.php';` (matches Phase 1 sanity test pattern).
    3. `use` declarations for `Logingrupa\Metapixelshopaholic\Plugin`, `Logingrupa\Metapixelshopaholic\Models\Settings`, `Logingrupa\Metapixelshopaholic\Tests\MetapixelTestCase`, `Symfony\Component\Yaml\Yaml`.
    4. `final class SettingsRegistrationTest extends MetapixelTestCase { ... }` with public methods `test_register_settings_returns_meta_pixel_entry`, `test_pixel_id_round_trips_through_settings`, `test_paid_status_code_options_contains_new_payment_received`, `test_queue_connection_options_returns_static_three_drivers`, `test_fields_yaml_binds_lang_keys_per_field`.
    5. Each test uses native PHPUnit assertions (`$this->assertArrayHasKey`, `$this->assertSame`, `$this->assertNotEmpty`, `$this->assertEqualsCanonicalizing` for the queue array).
    6. Test 5 implementation: declare a class constant `private const FIELD_KEYS = ['pixel_id', 'capi_access_token', 'test_event_code', 'currency_code', 'phone_country_code', 'send_hashed_pii', 'queue_connection', 'paid_status_code', 'refire_purchase_on_status_flip', 'ensure_fbp_fbc_server_side'];`. Parse YAML via `$arData = Yaml::parseFile(__DIR__.'/../../models/settings/fields.yaml');`. Loop each key and run the two assertSame checks.
    7. Add `protected function tearDown(): void { Settings::clearInternalCache(); parent::tearDown(); }` to avoid cache bleed between tests.

    The test inherits `MetapixelTestCase::setUp()`'s `migrateModules()` (which provisions `lovata_ordersshopaholic_statuses` and `system_settings`) — no additional `Schema::create` is required, unlike the goodsreceivedshopaholic shim. If migrateModules() fails to provide the statuses table on SQLite (verify when running), fall back to the goodsreceivedshopaholic `Schema::create('system_settings', ...)` pattern in `setUp()` per `02-PATTERNS.md` lines 706-731.

    Tiger-Style: each test method ≤ 15 lines. Explicit `assert*` calls. No mocking — hits the real SQLite-in-memory DB.
  </action>
  <verify>
    <automated>cd /home/forge/nailscosmetics.lv/plugins/logingrupa/metapixelshopaholic && ../../../vendor/bin/pest --configuration phpunit.xml --filter SettingsRegistrationTest --stop-on-failure</automated>
  </verify>
  <acceptance_criteria>
    - All 5 test methods pass.
    - `grep -c "extends MetapixelTestCase" tests/Feature/SettingsRegistrationTest.php` == 1.
    - `grep -c "Settings::class" tests/Feature/SettingsRegistrationTest.php` ≥ 1.
    - `grep -c "Yaml::parseFile" tests/Feature/SettingsRegistrationTest.php` == 1.
    - `grep -c "test_fields_yaml_binds_lang_keys_per_field" tests/Feature/SettingsRegistrationTest.php` == 1.
    - Test file passes phpstan level 10 (`../../../vendor/bin/phpstan analyse tests/Feature/SettingsRegistrationTest.php --configuration=phpstan.neon`).
  </acceptance_criteria>
  <done>
    Plan 02-01 ships its first feature test covering registerSettings + Settings round-trip + dropdown options + per-key fields.yaml lang-binding. SKEL-02's dropdown + round-trip behaviour AND the W8 per-key invariant are locked behind an automated gate that future plans cannot regress silently.
  </done>
</task>

<task type="auto">
  <name>Task 8: Run composer qa — must be green before commit</name>
  <files>(none — verification only)</files>
  <read_first>
    - composer.json (Task 6 output — phpmd script)
    - All files written in Tasks 1-7
  </read_first>
  <action>
    Run `composer qa` from the plugin directory. The script chains `pint-test` → `analyse` → `phpmd` → `test-cov`. Investigate and fix any failure:
    - `pint-test` failure → run `composer pint` (auto-fix) then re-run `pint-test`.
    - `phpstan analyse` failure → fix the type/null issue at source. Do NOT add baselines.
    - `phpmd` warnings on the widened scope (classes/middleware/models/components/controllers/updates) → fix; phpmd.xml's `LongVariable max=40` and `ShortVariable min=4` accommodate Hungarian notation, so unprefixed locals would be the most common offender.
    - `test-cov` failure → fix the failing test or the SUT.

    Acceptance: command exits 0.
  </action>
  <verify>
    <automated>cd /home/forge/nailscosmetics.lv/plugins/logingrupa/metapixelshopaholic && composer qa</automated>
  </verify>
  <acceptance_criteria>
    - `composer qa` exit code == 0.
    - `composer pint-test` reports clean (no formatting diffs).
    - `composer analyse` reports 0 errors (phpstan level 10 + larastan + disallowed-calls + universalObjectCrates).
    - `composer phpmd` reports 0 warnings across the widened directory set.
    - `composer test-cov` reports all tests passing (including the new SettingsRegistrationTest).
  </acceptance_criteria>
  <done>
    Phase 1 quality bar maintained: composer qa green on a plugin that now boots, registers Settings, scaffolds lang, and tests round-trip Settings I/O + per-key fields.yaml lang binding.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Backend → Settings model | Authenticated October admin user submits the Settings form; data lands in `system_settings.value` (`mediumtext` blob). Threat surface = mass-assignment + DOM exposure of secret values. |
| Backend → Status dropdown population | `getPaidStatusCodeOptions()` queries `Status::lists('name','code')` on every backend Settings page render. DoS surface if invoked unbounded outside admin context. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | I (Information disclosure) | `models/settings/fields.yaml` — `capi_access_token` field | mitigate | Declare `type: password` so October renders an input[type=password]; secret is masked in DOM and not visible in plain backend HTML. Task 4 acceptance criterion enforces `grep -c "type: password" == 1`. |
| T-02-02 | T (Tampering) | `models/Settings.php` mass-assignment via `CommonSettings::set` | accept | `CommonSettings` (parent) limits writable keys to those declared in `$settingsFields = 'fields.yaml'`. Only the 10 SKEL-02 fields can be persisted. October's SettingsManager enforces the fields.yaml schema at write time. No additional `$fillable` declaration needed. |
| T-02-03 | D (Denial of service) | `Settings::getPaidStatusCodeOptions()` called on every backend Settings render | accept | `Status::lists('name','code')` hits a small static-data table (typically < 20 rows). October's form builder calls it lazily on form render only. CONTEXT specifics line 110 locks the call. Caching via Toolbox `CCache` is deferred to Phase 3 if profile flags it (not currently a hotspot). |
| T-02-04 | I (Information disclosure) | RainLab.Translate `pixel_id` translatable field | accept | `$translatable = ['pixel_id']` only — `capi_access_token` is explicitly NOT translatable, preventing accidental cross-locale leakage of the secret token into RainLab's `rainlab_translate_message_data` table. Verified by Task 3 behavior #4. |
| T-02-05 | E (Elevation of privilege) | `Plugin::$require` drops Lovata.Buddies hard dep | mitigate | Buddies optionality is enforced at runtime by `Lovata\Toolbox\Classes\Helper\UserHelper::init()` (`PluginManager::instance()->exists('Lovata.Buddies')`). When Buddies is absent, no User-related listeners bind. Phase 2 ships no User-event listeners, so the attack surface is zero in this plan. |
</threat_model>

<verification>
1. Manual: Spin up backend, navigate to Settings → Meta Pixel, confirm all 10 fields render with translated labels, `paid_status_code` dropdown lists statuses including `new-payment-received` selectable. (Deferred to Phase 5 README — not blocking this plan.)
2. Automated: `composer qa` exits 0 (Task 8).
3. Goal-backward: Run `php -r '$s=new \Logingrupa\Metapixelshopaholic\Models\Settings(); var_dump($s->getPaidStatusCodeOptions());'` — must dump a non-empty array. (Covered by Test 3 in Task 7.)
4. Per-key lang-binding invariant: covered by `SettingsRegistrationTest::test_fields_yaml_binds_lang_keys_per_field` (Task 7 Test 5).
</verification>

<success_criteria>
- `composer qa` green on the plugin after this plan's tasks.
- All 5 must_haves.truths verifiable through grep gates listed under acceptance_criteria.
- `Plugin.php`, `models/Settings.php`, `models/settings/fields.yaml`, `lang/{en,lv,ru}/lang.php`, `plugin.yaml`, `composer.json`, `tests/Feature/SettingsRegistrationTest.php` all exist and pass automated gates.
- SKEL-01 (Plugin.php + plugin.yaml metadata layer), SKEL-02 (Settings + fields.yaml + getPaidStatusCodeOptions), SKEL-06 (lang scaffolding) addressed.
</success_criteria>

<output>
After completion, create `.planning/phases/02-skeleton-cookie-fix/02-01-SUMMARY.md` documenting:
- Plugin.php structure decisions (icon, lang keys, $require list)
- Settings model parent choice (CommonSettings, not bare SettingModel)
- fields.yaml tab grouping decision (3 tabs: Tracking / Compliance / Advanced)
- composer.json deltas (Buddies → suggest, phpmd scope widened to lowercase paths)
- Test 1 of 3 Phase 2 feature tests shipped; remaining 2 in Plans 02-02, 02-03 (Plan 02-04 adds PixelHeadTest as the 4th)
- Per-key fields.yaml lang-binding assertion gate (Task 7 Test 5) — replaces brittle count-only grep
- Any deviations from CONTEXT.md and why
</output>
</content>
</invoke>