---
id: 03.1-06
phase: 03.1-event-log-refactor
plan: 06
type: execute
wave: 5
depends_on:
  - 03.1-01
  - 03.1-02
  - 03.1-03
  - 03.1-04
  - 03.1-05
files_modified:
  - plugins/logingrupa/metapixelshopaholic/tests/Feature/PurchaseEndToEndIntegrationTest.php
  - plugins/logingrupa/metapixelshopaholic/.planning/phases/03.1-event-log-refactor/STAGING-RUNBOOK.md
  - plugins/logingrupa/metapixelshopaholic/.planning/STATE.md
  - plugins/logingrupa/metapixelshopaholic/.planning/ROADMAP.md
autonomous: true
requirements:
  - REFAC-11
rationale: >
  Re-opens Phase 3.1 with a final wave (Wave 5) that closes the deferred
  runtime-verification gap surfaced in VERIFICATION.md and the BRIEF
  acceptance criterion 4 manual checkpoint. Two deliverables: (1) one
  integration test class that codifies the 4 BRIEF staging scenarios as
  the full Purchase pipeline end-to-end (Watcher → SendCapiEvent →
  EventLogWriter → MockHandler ↔ PurchasePixel::onRun → onMarkFired)
  inside a single Pest/PHPUnit test surface — proving the contracts in
  CI so the operator runtime checkpoint only needs to confirm live-only
  plumbing (real PayPal IPN, real Pixel script handshake, Meta Events
  Manager Test Events dashboard); (2) a STAGING-RUNBOOK.md operator
  playbook covering deploy + the 4 scenarios as numbered procedures +
  `system_plugin_versions` SQL check + `composer qa` runtime command —
  this is the LAST 10% that genuinely cannot be automated. CI workflow
  (`.github/workflows/metapixel-qa.yml`) already runs `composer qa` on
  every push/PR — no CI edits in scope. NO new production code; this
  plan is purely test + runbook + ledger updates.

must_haves:
  truths:
    - "Scenario 1 (PayPal CAPI+Pixel pair, same event_id, deduplicated): codified by `tests/Feature/PurchaseEndToEndIntegrationTest.php::test_paypal_flow_fires_capi_then_pixel_with_same_event_id` — paid Order save triggers `OrderStatusWatcher::handleUpdated` → `SendCapiEvent::dispatchSync` → `EventLogWriter::record(channel='capi')` → exactly one Guzzle POST captured by MockHandler → `PurchasePixel::onRun()` for the same Order's secret_key renders `arMetaEvent.event_id` equal to the CAPI row's `event_id` → `PurchasePixel::onMarkFired()` with that event_id inserts the `channel='pixel'` row → both EventLog rows share `event_id` AND `event_time`."
    - "Scenario 2 (bank-transfer admin flip: CAPI only, then Pixel on customer visit): codified by `test_bank_transfer_admin_flip_fires_capi_only_pixel_renders_on_later_visit` — `bootOrdersStatuses` seeds Status 5 'new-payment-received'; first save with `status_id=1` (`new`) MUST NOT fire (assertion: zero MockHandler history + zero EventLog rows). Admin flip to `status_id=5` fires exactly one CAPI POST + records the CAPI row. Customer-side `PurchasePixel::onRun()` later renders with the persisted CAPI row's `event_id` + `event_time`."
    - "Scenario 3 (status flip-flop with refire OFF: no second event_log row, no second HTTP POST): codified by `test_status_flip_flop_with_refire_off_does_not_redispatch` — `Settings::set('refire_purchase_on_status_flip', false)`; flip 1→5 fires; flip 5→3 (`complete`) is a no-op (Watcher's `handleUpdated` guards on paid-status entry); flip 3→5 returns to paid → `OrderStatusWatcher::alreadyDispatched` returns true (EventLog has a CAPI row) → MockHandler history count stays at 1, EventLog::where(channel=capi)->count() stays at 1."
    - "Scenario 4 (refresh /checkout/{slug} after Pixel fired + incognito on different device: Purchase does NOT re-fire): codified by `test_pixel_row_suppresses_refire_across_devices_and_sessions` — pre-seed both CAPI and Pixel rows for an Order; instantiate two FRESH `PurchasePixel` component instances (simulating a refresh + a different-device incognito visit by clearing the in-process cache between renders) → both `onRun()` calls return null + `arMetaEvent` stays null + second `onMarkFired()` call with the same event_id returns `['ok' => true, 'won_race' => false]` (UNIQUE-collapsed no-op) + EventLog::where(channel=pixel)->count() stays at 1."
    - "Bonus multi-site contract sanity check (acceptance criterion 9 cross-link): `test_multi_site_same_order_id_records_two_independent_capi_rows` — switches `Config::set('system.active_site', 1)` then `(..., 2)` between two dispatchSync calls for the SAME Order id → both record one CAPI row each → MockHandler history count = 2 → EventLog::count() = 2. Reuses the MultiSiteEventLogTest pattern from Plan 03.1-05 but exercises the FULL SendCapiEvent dispatch chain (not just the writer) so the integration surface is provably multi-site safe."
    - "`.planning/phases/03.1-event-log-refactor/STAGING-RUNBOOK.md` exists and contains: (a) v1.1.0 deploy preconditions (`composer install`, `php artisan october:up`, `sudo systemctl reload php8.4-fpm`); (b) the 4 BRIEF scenarios as numbered operator procedures with expected Meta Events Manager Test Events dashboard observations (dedup ≥ 80%, EMQ ≥ 8 for Purchase, exactly one Purchase per Order); (c) `SELECT version FROM system_plugin_versions WHERE code = 'Logingrupa.Metapixelshopaholic'` SQL check returning `1.1.0`; (d) `composer qa` runtime verification command + expected output shape; (e) a 'Checkpoint passed' template line operator appends to STATE.md `Phase 3.1 Forward Notes` upon completion."
    - "`.planning/STATE.md` frontmatter advances `status: phase-3.1-runtime-verified` (was `phase-3.1-complete`), `last_updated:` to today, `last_activity:` to today; Pending Todos sweep marks the deferred runtime-verification gap CLOSED (was deferred from Plan 03.1-05 + VERIFICATION.md); Forward Notes block links to `STAGING-RUNBOOK.md` as the canonical operator handoff."
    - "`.planning/ROADMAP.md` Phase 3.1 entry appends `- [ ] 03.1-06-PLAN.md — Staging-checkpoint automation: integration test + operator runbook (REFAC-11 closure)` under the existing `Plans:` list; Phase 3.1 total bumped from `5/5` to `6/6` once executor marks the plan complete (Wave-5 close)."
    - "CI workflow `.github/workflows/metapixel-qa.yml` already wires `composer qa` on every push/PR touching the plugin tree — no edit needed (verified by executor via `cat` during Task 2; recorded in 03.1-06-SUMMARY.md)."
  artifacts:
    - path: "plugins/logingrupa/metapixelshopaholic/tests/Feature/PurchaseEndToEndIntegrationTest.php"
      provides: "4 BRIEF acceptance scenarios + 1 multi-site sanity check codified as integration tests against the FULL Purchase pipeline (Watcher + Queue + Writer + MetaClient Mock + Component + AJAX handler). MockHandler-backed Guzzle Client identical to SendCapiEventEventLogTest pattern. Deterministic event_time literal (1715000000) + Carbon::setTestNow freeze."
      contains: "final class PurchaseEndToEndIntegrationTest extends MetapixelTestCase"
    - path: "plugins/logingrupa/metapixelshopaholic/.planning/phases/03.1-event-log-refactor/STAGING-RUNBOOK.md"
      provides: "Operator playbook: deploy v1.1.0 + run 4 BRIEF scenarios + `system_plugin_versions` SQL check + `composer qa` runtime verification command + Test Events dashboard expectations + STATE.md checkpoint-passed template."
      contains: "# Phase 3.1 Staging Runbook"
    - path: "plugins/logingrupa/metapixelshopaholic/.planning/STATE.md"
      provides: "Phase 3.1 runtime-verification closure — status advanced, Pending Todos deferred-runtime item closed, Forward Notes link to STAGING-RUNBOOK.md."
      contains: "status: phase-3.1-runtime-verified"
    - path: "plugins/logingrupa/metapixelshopaholic/.planning/ROADMAP.md"
      provides: "Phase 3.1 plan list updated with 03.1-06 entry (6 plans total)."
      contains: "03.1-06-PLAN.md"
  key_links:
    - from: "tests/Feature/PurchaseEndToEndIntegrationTest.php"
      to: "classes/event/OrderStatusWatcher.php + classes/queue/SendCapiEvent.php + classes/helper/EventLogWriter.php + components/PurchasePixel.php"
      via: "Event::subscribe(OrderStatusWatcher::class) + Order::save() trigger + dispatchSync + onRun() + onMarkFired() chain"
      pattern: "Event::subscribe\\(OrderStatusWatcher::class\\)"
    - from: "tests/Feature/PurchaseEndToEndIntegrationTest.php"
      to: "MetaClient (Guzzle) via $this->app->instance(MetaClient::class, ...)"
      via: "MockHandler + history middleware (MC-04 pattern from SendCapiEventEventLogTest)"
      pattern: "\\$this->app->instance\\(MetaClient::class"
    - from: "STAGING-RUNBOOK.md"
      to: "VERIFICATION.md deferred-runtime block + BRIEF.md acceptance criteria lines 280-294"
      via: "1:1 procedural mapping — each VERIFICATION deferred item becomes one runbook step"
      pattern: "Scenario [1-4]"
    - from: ".planning/STATE.md"
      to: ".planning/phases/03.1-event-log-refactor/STAGING-RUNBOOK.md"
      via: "Forward Notes link"
      pattern: "STAGING-RUNBOOK\\.md"
---

<objective>
Close the Phase 3.1 runtime-verification gap surfaced in VERIFICATION.md
and BRIEF acceptance criterion 4. Two artefacts, two purposes:

1. `tests/Feature/PurchaseEndToEndIntegrationTest.php` — one integration
   test class that exercises the FULL Purchase pipeline end-to-end in
   PHPUnit/Pest (SQLite-in-memory + Guzzle MockHandler). Until now, each
   stage was unit-locked in isolation (OrderStatusWatcherEventLogTest,
   SendCapiEventEventLogTest, PurchasePixelEventLogGateTest,
   MultiSiteEventLogTest). This new file proves the chain joins
   end-to-end as the 4 BRIEF acceptance scenarios describe. After this
   class lands, the operator-side runtime check only needs to confirm
   live-environment plumbing (real PayPal IPN, real Pixel browser
   handshake, Meta Events Manager dashboard observation).

2. `STAGING-RUNBOOK.md` — operator playbook for the LAST 10% that
   genuinely cannot be automated. Deploy steps, the 4 scenarios as
   numbered live-environment procedures, expected Meta Events Manager
   dashboard output, `system_plugin_versions` SQL check, `composer qa`
   command + expected output, and a checkpoint-passed STATE.md template
   line.

Purpose: BRIEF acceptance criterion 4 (4 staging scenarios) + criterion
5 (`system_plugin_versions` v1.1.0) + criterion 1 (`composer qa` green)
were marked DEFERRED in VERIFICATION.md because the orchestrator could
not run vendor binaries / live DB / Meta dashboard. This plan splits
the closure: contract proofs land in CI (test class) + live-plumbing
proofs land in a runbook the operator follows after merge.

Output: 1 new test file + 1 new runbook + STATE.md status advance +
ROADMAP.md plan-list append. ZERO production-code edits — production
contracts were already shipped in Waves 1-4 (Plans 03.1-01..04) and
verified by Wave-4 Plan 03.1-05 grep-sweep.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@plugins/logingrupa/metapixelshopaholic/.planning/phases/03.1-event-log-refactor/BRIEF.md
@plugins/logingrupa/metapixelshopaholic/.planning/phases/03.1-event-log-refactor/VERIFICATION.md
@plugins/logingrupa/metapixelshopaholic/.planning/phases/03.1-event-log-refactor/03.1-PATTERNS.md
@plugins/logingrupa/metapixelshopaholic/.planning/phases/03.1-event-log-refactor/03.1-01-SUMMARY.md
@plugins/logingrupa/metapixelshopaholic/.planning/phases/03.1-event-log-refactor/03.1-02-SUMMARY.md
@plugins/logingrupa/metapixelshopaholic/.planning/phases/03.1-event-log-refactor/03.1-03-SUMMARY.md
@plugins/logingrupa/metapixelshopaholic/.planning/phases/03.1-event-log-refactor/03.1-04-SUMMARY.md
@plugins/logingrupa/metapixelshopaholic/.planning/phases/03.1-event-log-refactor/03.1-05-SUMMARY.md
@plugins/logingrupa/metapixelshopaholic/tests/MetapixelTestCase.php
@plugins/logingrupa/metapixelshopaholic/tests/Support/OrderFixtures.php
@plugins/logingrupa/metapixelshopaholic/tests/Feature/OrderStatusWatcherEventLogTest.php
@plugins/logingrupa/metapixelshopaholic/tests/Feature/SendCapiEventEventLogTest.php
@plugins/logingrupa/metapixelshopaholic/tests/Feature/PurchasePixelEventLogGateTest.php
@plugins/logingrupa/metapixelshopaholic/tests/Feature/MultiSiteEventLogTest.php
@plugins/logingrupa/metapixelshopaholic/classes/event/OrderStatusWatcher.php
@plugins/logingrupa/metapixelshopaholic/classes/queue/SendCapiEvent.php
@plugins/logingrupa/metapixelshopaholic/classes/helper/EventLogWriter.php
@plugins/logingrupa/metapixelshopaholic/classes/helper/SiteResolver.php
@plugins/logingrupa/metapixelshopaholic/components/PurchasePixel.php
@plugins/logingrupa/metapixelshopaholic/models/EventLog.php
@plugins/logingrupa/metapixelshopaholic/Plugin.php
@plugins/logingrupa/metapixelshopaholic/updates/version.yaml
@plugins/logingrupa/metapixelshopaholic/.planning/STATE.md
@plugins/logingrupa/metapixelshopaholic/.planning/ROADMAP.md
@plugins/logingrupa/metapixelshopaholic/.github/workflows/metapixel-qa.yml

<interfaces>
From Phase 3.1 Waves 1-4 (already shipped, v1.1.0):

- `OrderStatusWatcher` — `final class` at `classes/event/OrderStatusWatcher.php`. Public: `subscribe(Dispatcher $obEvents): void`, `static flushStatusCache(): void`, `handleUpdated(Order $obOrder): void` (line 136), `handleCreated(Order $obOrder): void` (line 170). Private: `fireForwardDispatch(Order $obOrder): void` (line 201) — calls `DB::afterCommit(static function () use ($arPayload, $iOrderId, $sOrderNumber, $sUuid, $obOrder): void { SendCapiEvent::dispatch('Purchase', $arPayload, $obOrder); })`. Private: `alreadyDispatched(Order $obOrder): bool` (line 259) — EventLog existence check, SiteResolver-scoped.

- `SendCapiEvent` — `final class` at `classes/queue/SendCapiEvent.php`. Constructor (line 112) promotes `public readonly Order $obSubject` as 3rd argument: `__construct(string $sEventName, array $arPayload, Order $obSubject)`. `handle()` (line 130) first calls `$this->raceFenceWon()`. Private: `raceFenceWon(): bool` (line 201) → `EventLogWriter::record($sEventId, $this->sEventName, EventLog::CHANNEL_CAPI, $this->obSubject, $this->stringOrNull(...), $iEventTime)`. Has `$tries = 3`, `$backoff = [1,4,16]`, `failed()` hook.

- `EventLogWriter::record(string $sEventId, string $sEventName, string $sChannel, object $obSubject, ?string $sSecretKey, int $iEventTime): bool` — `final class` at `classes/helper/EventLogWriter.php`. Returns true=race-winner, false=race-loser (or DB failure absorbed). Uses `DB::table((new EventLog())->table)->insertOrIgnore([...])`. Auto-populates `site_id` via `SiteResolver::getActiveSiteId()`.

- `SiteResolver::getActiveSiteId(): ?int` — `final class` at `classes/helper/SiteResolver.php`. Reads October 4 SDK; null in CLI / queue / single-site context. Test injection: `Config::set('system.active_site', $i)`.

- `PurchasePixel` — `final class` at `components/PurchasePixel.php`. Public properties: `?array $arMetaEvent = null` (line 80), `?string $sCustomDataJson = null` (line 92). Public methods: `componentDetails(): array`, `defineProperties(): array`, `onRun()` (line 133), `onMarkFired(): array` (line 233). Private: `findEventLogRow(Order $obOrder, string $sChannel): ?EventLog` (line 302). `onRun()` gate chain: isDisabled → resolveOrder → isAtPaidStatus → CAPI row exists → Pixel row absent → populate `$arMetaEvent`. `onMarkFired()` validates submitted `event_id` matches CAPI row's `event_id`; mismatch returns `['ok' => false]` + Log::warning with LENGTH only.

- `EventLog` model — `final class` at `models/EventLog.php`. Constants: `CHANNEL_CAPI = 'capi'`, `CHANNEL_PIXEL = 'pixel'`, `EVENT_PURCHASE = 'Purchase'`. `$table = 'logingrupa_metapixel_event_log'`. Polymorphic `subject()` MorphTo. UNIQUE on (subject_type, subject_id, event_name, channel, site_id) — NULL is distinct in MySQL UNIQUE per BRIEF.

- `MetapixelTestCase` helpers — `bootSystemSettings()`, `bootOrdersStatuses()` (seeds Statuses 1-5 including code='new-payment-received' at id=5), `bootOrdersTable()`, `bootEventLogTable()` (provisions logingrupa_metapixel_event_log with UNIQUE + event_id index). `flushPluginSingletons()` resets `PluginGuard::flush()` between tests.

- `OrderFixtures::makePaidOrder(): Order` — provisions an Order with `status_id=5` (paid), `secret_key` set, plus the offer/product/order_positions hermetic schema slice via `provisionHermeticOfferProductTables()`. `dropHermeticOfferProductTables()` is the matching tearDown.

- `Settings::set('refire_purchase_on_status_flip', $bFlag)` — round-trips via Settings model (HR-02 flap-mitigation: prefer reflection `Settings::instance()->setAttribute(...)` if Settings::set flaps under SQLite-in-memory; see SendCapiEventEventLogTest::primeSettings).

- Guzzle MockHandler binding (MC-04 pattern, copy verbatim from SendCapiEventEventLogTest::bindMetaClientWithMockResponses lines 229-241):
  ```
  $obMockHandler = new MockHandler($arResponses);
  $obStack = HandlerStack::create($obMockHandler);
  $obHistoryMw = \GuzzleHttp\Middleware::history($arHistory);
  $obStack->push($obHistoryMw);
  $obGuzzle = new Client(['handler' => $obStack, 'http_errors' => false]);
  $this->app->instance(MetaClient::class, new MetaClient($obGuzzle));
  ```

CI surface (already wired in Wave-1 / Phase 1 TOOL-04):
- `.github/workflows/metapixel-qa.yml` — triggers on push (master) + PR (paths: `plugins/logingrupa/metapixelshopaholic/**`). Runs `composer install` (repo root) + `composer qa` (plugin dir) on PHP 8.4. NO edits needed; executor confirms by reading file in Task 2.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: PurchaseEndToEndIntegrationTest — 4 BRIEF scenarios + 1 multi-site sanity, full pipeline chain</name>
  <files>
    plugins/logingrupa/metapixelshopaholic/tests/Feature/PurchaseEndToEndIntegrationTest.php (NEW)
  </files>
  <read_first>
    plugins/logingrupa/metapixelshopaholic/.planning/phases/03.1-event-log-refactor/BRIEF.md (acceptance criteria lines 280-294 — the 4 scenarios; Tests block lines 240-274 — pattern guidance),
    plugins/logingrupa/metapixelshopaholic/.planning/phases/03.1-event-log-refactor/VERIFICATION.md (human_verification block lines 10-19 + Deferred-Runtime Verification lines 162-178 — the gap this test class closes),
    plugins/logingrupa/metapixelshopaholic/.planning/phases/03.1-event-log-refactor/03.1-PATTERNS.md (read end-to-end — every pattern this file uses is documented there),
    plugins/logingrupa/metapixelshopaholic/tests/Feature/SendCapiEventEventLogTest.php (MockHandler + history + MetaClient binding pattern — MC-04; primeSettings via reflection — HR-02),
    plugins/logingrupa/metapixelshopaholic/tests/Feature/OrderStatusWatcherEventLogTest.php (Event::subscribe(OrderStatusWatcher::class) in setUp pattern; OrderFixtures::makePaidOrder usage; status flip-flop test method shape),
    plugins/logingrupa/metapixelshopaholic/tests/Feature/PurchasePixelEventLogGateTest.php (onRun + onMarkFired component-test pattern; makeComponent helper shape),
    plugins/logingrupa/metapixelshopaholic/tests/Feature/MultiSiteEventLogTest.php (Config::set('system.active_site', $i) + Hungarian variable conventions),
    plugins/logingrupa/metapixelshopaholic/classes/event/OrderStatusWatcher.php (method line numbers verified: handleUpdated 136, handleCreated 170, fireForwardDispatch 201, alreadyDispatched 259),
    plugins/logingrupa/metapixelshopaholic/classes/queue/SendCapiEvent.php (constructor 112 — Order $obSubject promoted readonly; handle 130; raceFenceWon 201),
    plugins/logingrupa/metapixelshopaholic/classes/helper/EventLogWriter.php (record signature + INSERT IGNORE semantics),
    plugins/logingrupa/metapixelshopaholic/components/PurchasePixel.php (onRun 133; onMarkFired 233; findEventLogRow 302),
    plugins/logingrupa/metapixelshopaholic/models/EventLog.php (constants + table name).
  </read_first>
  <behavior>
    Each test method asserts the FULL chain (model save → subscriber → queue handle → writer → MockHandler ↔ component → AJAX). Variables Hungarian: `$obOrder`, `$arPayload`, `$arHistory`, `$obMockHandler`, `$sEventId`, `$iEventTime`, `$bWon`, `$obComponent`, `$arResult`, `$obCapiRow`, `$obPixelRow`, `$iCount`, `$obException`. Deterministic event_time literal `private const int FIXED_EVENT_TIME = 1715000000;`. `Carbon::setTestNow(Carbon::createFromTimestamp(self::FIXED_EVENT_TIME))` in setUp to freeze wall clock (Tiger-Style determinism). tearDown unfreezes via `Carbon::setTestNow();`. Single deterministic UUID seed pattern: `Uuid::uuid4()->toString()` captured into a setUp helper.

    Test methods (5 in total):

    - `test_paypal_flow_fires_capi_then_pixel_with_same_event_id` — Scenario 1.
      Steps:
      (a) `$obOrder = OrderFixtures::makePaidOrder();` (status_id=5 from the start, simulating PayPal IPN-flipped state).
      (b) Bind MetaClient with MockHandler returning one `Response(200, [], '{"events_received": 1, "fbtrace_id": "scenario-1"}')`.
      (c) Trigger Watcher: `Event::dispatch('eloquent.updated: '.Order::class, [$obOrder]);` OR call `OrderStatusWatcher::handleUpdated($obOrder)` directly (mirror OrderStatusWatcherEventLogTest pattern, whichever is simpler — choose whichever is closer to production). NOTE: Under `Queue::fake()` the dispatched job's handle() does NOT execute. For this END-TO-END test we DO NOT fake the queue — we want `dispatchSync` semantics (the test's `Queue::shouldReceive` is too brittle). Easiest path: invoke `SendCapiEvent::dispatchSync('Purchase', $arPayload, $obOrder)` directly after asserting Watcher prepared the payload. The two-stage assertion: (i) Watcher's `alreadyDispatched` returns false before dispatch; (ii) dispatchSync produces one MockHandler POST + one CAPI row.
      (d) Assert exactly one MockHandler history entry: `$this->assertCount(1, $arHistory);`.
      (e) Assert exactly one EventLog CAPI row: `EventLog::where('channel', EventLog::CHANNEL_CAPI)->count() === 1`.
      (f) Extract the CAPI row: `$obCapiRow = EventLog::where('channel', EventLog::CHANNEL_CAPI)->first();`. Capture `$sCapiEventId = (string) $obCapiRow->event_id;` and `$iCapiEventTime = (int) $obCapiRow->event_time;`.
      (g) Instantiate `PurchasePixel` component with `orderSlug = $obOrder->secret_key`. Call `$obComponent->onRun();`. Assert `$obComponent->arMetaEvent['event_id'] === $sCapiEventId` AND `$obComponent->arMetaEvent['event_time'] === $iCapiEventTime`.
      (h) Simulate browser AJAX confirmation: `Request::merge(['event_id' => $sCapiEventId]);` then `$arResult = $obComponent->onMarkFired();`. Assert `$arResult === ['ok' => true, 'won_race' => true]` (race-winner) AND `EventLog::where('channel', EventLog::CHANNEL_PIXEL)->count() === 1`. Extract the Pixel row: `$obPixelRow = EventLog::where('channel', EventLog::CHANNEL_PIXEL)->first();`. Assert `$obPixelRow->event_id === $sCapiEventId` (same event_id) AND `(int) $obPixelRow->event_time === $iCapiEventTime` (same event_time).

    - `test_bank_transfer_admin_flip_fires_capi_only_pixel_renders_on_later_visit` — Scenario 2.
      Steps:
      (a) Create Order with `status_id=1` (Status code='new'): `$obOrder = OrderFixtures::makePaidOrder();` then `$obOrder->status_id = 1; $obOrder->save();` (or build a non-paid fixture inline — choose simplest). At this point, save the Order but do NOT trigger any dispatch.
      (b) Bind MetaClient MockHandler with one Response (queued for the upcoming flip).
      (c) Assert zero MockHandler history + zero EventLog rows BEFORE the flip.
      (d) Admin flip: `$obOrder->status_id = 5; $obOrder->save();` THEN explicitly invoke Watcher: `(new OrderStatusWatcher())->handleUpdated($obOrder);`. (Production Plugin::boot wires this via Event::subscribe; the test wires manually.)
      (e) Now invoke `SendCapiEvent::dispatchSync('Purchase', $arPayload, $obOrder)` to simulate queue worker draining (or assert via Queue capture that the job was queued, then dispatchSync separately — keep simple: dispatchSync).
      (f) Assert exactly one MockHandler POST + one CAPI row + zero Pixel rows (`EventLog::where('channel', EventLog::CHANNEL_PIXEL)->count() === 0`).
      (g) Customer visit later: instantiate `PurchasePixel` component with the secret_key, call `onRun()`. Assert `$obComponent->arMetaEvent !== null` AND `$obComponent->arMetaEvent['event_id']` equals the CAPI row event_id (Pixel renders with the persisted event_id from the bank-transfer admin flip).

    - `test_status_flip_flop_with_refire_off_does_not_redispatch` — Scenario 3.
      Steps:
      (a) Prime refire flag OFF via reflection: `Settings::instance()->setAttribute('refire_purchase_on_status_flip', false);` (HR-02 mitigation — Settings::set flaps under SQLite-in-memory).
      (b) Create paid Order; dispatchSync the first CAPI event (this is the established baseline). Assert MockHandler history count = 1, EventLog count = 1.
      (c) Flip to non-paid: `$obOrder->status_id = 3; $obOrder->save();` then invoke `(new OrderStatusWatcher())->handleUpdated($obOrder);`. Assert NO new dispatch (the Watcher's `isAtPaidStatus` returns false → method returns early without calling `fireForwardDispatch`).
      (d) Flip back to paid: `$obOrder->status_id = 5; $obOrder->save();` then invoke `handleUpdated`. The Watcher's `alreadyDispatched` now returns true (CAPI row exists from step b) → method returns early. Assert MockHandler history STILL count = 1 (queue MockHandler had only one Response queued; second drain would have errored, which is itself a hard assertion). Assert EventLog::where(channel=capi)->count() STILL = 1.

    - `test_pixel_row_suppresses_refire_across_devices_and_sessions` — Scenario 4.
      Steps:
      (a) Create paid Order. Pre-seed both CAPI and Pixel rows via direct `EventLog::forceFill()->save()` (mirror PurchasePixelEventLogGateTest::seedEventLogRow pattern). Same event_id on both rows.
      (b) First "device" — instantiate `PurchasePixel` component, call `onRun()`. Assert `$obComponent->arMetaEvent === null` (Pixel row blocks render).
      (c) Simulate a different incognito on different device — flush PluginGuard + any in-process cache (`PluginGuard::flush(); Settings::clearInternalCache(); Cache::flush();`), instantiate a FRESH `PurchasePixel` component, call `onRun()`. Assert `$arMetaEvent === null` again (server-side row persists across the simulated session boundary).
      (d) Attempt a second `onMarkFired()` call: `Request::merge(['event_id' => $sCapiEventId]);` → `$arResult = $obComponent->onMarkFired();`. Assert `$arResult === ['ok' => true, 'won_race' => false]` (UNIQUE-collapsed no-op) AND EventLog::where(channel=pixel)->count() STILL = 1 (no duplicate insert).

    - `test_multi_site_same_order_id_records_two_independent_capi_rows` — bonus sanity (criterion 9 cross-link).
      Steps:
      (a) Create paid Order.
      (b) Bind MetaClient MockHandler with TWO Response objects.
      (c) `Config::set('system.active_site', 1);` then dispatchSync. Assert MockHandler history count = 1, EventLog count = 1, EventLog::first()->site_id === 1.
      (d) `Config::set('system.active_site', 2);` then dispatchSync (SAME Order id, different active site). Assert MockHandler history count = 2, EventLog count = 2, the second row's site_id === 2.
      (e) tearDown resets `Config::set('system.active_site', null);` (mirror MultiSiteEventLogTest pattern).
  </behavior>
  <action>
    Create `tests/Feature/PurchaseEndToEndIntegrationTest.php`. `declare(strict_types=1);`, namespace `Logingrupa\Metapixelshopaholic\Tests\Feature`.

    File header docblock summarises the 4 BRIEF scenarios it codifies, links VERIFICATION.md's deferred-runtime block as the gap it closes, and notes the bonus multi-site sanity. Mirror the docblock style used in SendCapiEventEventLogTest + PurchasePixelEventLogGateTest (PHPDoc block above the `final class` keyword).

    `require_once __DIR__.'/../MetapixelTestCase.php';` + `require_once __DIR__.'/../Support/OrderFixtures.php';` at the top (mirror sibling test files in this directory).

    Imports (alphabetised): `Carbon\Carbon`, `GuzzleHttp\Client`, `GuzzleHttp\Handler\MockHandler`, `GuzzleHttp\HandlerStack`, `GuzzleHttp\Middleware`, `GuzzleHttp\Psr7\Response`, `Illuminate\Support\Facades\Cache`, `Illuminate\Support\Facades\Config`, `Illuminate\Support\Facades\Event`, `Illuminate\Support\Facades\Log`, `Illuminate\Support\Facades\Request`, `Illuminate\Support\Facades\Schema`, `Logingrupa\Metapixelshopaholic\Classes\Event\OrderStatusWatcher`, `Logingrupa\Metapixelshopaholic\Classes\Helper\PluginGuard`, `Logingrupa\Metapixelshopaholic\Classes\Meta\MetaClient`, `Logingrupa\Metapixelshopaholic\Classes\Queue\SendCapiEvent`, `Logingrupa\Metapixelshopaholic\Components\PurchasePixel`, `Logingrupa\Metapixelshopaholic\Models\EventLog`, `Logingrupa\Metapixelshopaholic\Models\Settings`, `Logingrupa\Metapixelshopaholic\Tests\MetapixelTestCase`, `Logingrupa\Metapixelshopaholic\Tests\Support\OrderFixtures`, `Lovata\OrdersShopaholic\Models\Order`, `Ramsey\Uuid\Uuid`.

    Class: `final class PurchaseEndToEndIntegrationTest extends MetapixelTestCase`.

    Constants:
    `private const int FIXED_EVENT_TIME = 1715000000;`
    `private const string PIXEL_ID = '2291486191076331';`
    `private const string CAPI_TOKEN = 'EAA-integration-token';`

    `setUp(): void`:
    - `parent::setUp();`
    - `$this->bootSystemSettings();`
    - `$this->bootOrdersStatuses();`
    - `$this->bootOrdersTable();`
    - `$this->bootEventLogTable();`
    - `OrderFixtures::provisionHermeticOfferProductTables();`
    - `Cache::flush(); Settings::clearInternalCache(); PluginGuard::flush();`
    - `$this->primeSettings();` (sets pixel_id + capi_token + refire_purchase_on_status_flip default false)
    - `Carbon::setTestNow(Carbon::createFromTimestamp(self::FIXED_EVENT_TIME));`
    - `Event::subscribe(OrderStatusWatcher::class);` (production Plugin::boot wiring; mirror OrderStatusWatcherEventLogTest setUp)
    - `OrderStatusWatcher::flushStatusCache();` (WR-08 cache flush hook)

    `tearDown(): void`:
    - `Carbon::setTestNow();` (unfreeze)
    - `Config::set('system.active_site', null);` (multi-site bleed mitigation — T-3.1-25)
    - `OrderFixtures::dropHermeticOfferProductTables();`
    - `parent::tearDown();`

    Private helpers (mirror SendCapiEventEventLogTest internal structure):
    - `bindMetaClientWithMockResponses(array $arResponses, array &$arHistory): MockHandler` — copy verbatim from SendCapiEventEventLogTest lines 229-241.
    - `primeSettings(): void` — copy from SendCapiEventEventLogTest lines 247-253, then add `$obInstance->setAttribute('refire_purchase_on_status_flip', false);`.
    - `makePayload(string $sEventId, int $iEventTime = null): array` — copy from SendCapiEventEventLogTest lines 261-277; default to `self::FIXED_EVENT_TIME` when 0.
    - `seedEventLogRow(Order $obOrder, string $sEventId, int $iEventTime, string $sChannel): EventLog` — copy from PurchasePixelEventLogGateTest pattern (forceFill + save with channel + subject_type=Order::class + site_id=null).
    - `makeComponent(string $sOrderSlug): PurchasePixel` — copy from PurchasePixelEventLogGateTest pattern (component instantiated with `App::make` + property bag `['orderSlug' => $sOrderSlug]`).

    5 test methods per the `<behavior>` block above. Tiger-Style: each method <70 LOC. Every catch logs + rethrows or carries explicit `// silent:` reason comment (only used inside MockHandler/PreseededRow helpers if at all — primary test bodies should NOT silent-catch). No `assert(...)` calls (forbidden by spaze plugin); use PHPUnit `$this->assertSame`, `$this->assertCount`, `$this->assertNull`, `$this->assertNotNull`, `$this->assertSame` exclusively. Explicit `: void` returns on every method. PHPDoc on every test method describing the scenario + expected invariants.

    EXECUTOR DIRECTION (LOCKED — pinned by plan-checker review 2026-05-14, supersedes "either-or" branches in `<behavior>` steps above):

    1. **Watcher dispatch path — production fidelity, NOT a `dispatchSync` shortcut.** Every scenario drives the chain via `(new OrderStatusWatcher())->handleUpdated($obOrder)` (and `->handleCreated($obOrder)` where the BRIEF scenario starts from a freshly-created paid Order) AFTER `$obOrder->save()`. Do NOT call `SendCapiEvent::dispatchSync` directly from test bodies — that bypasses the Watcher's payload construction (`fireForwardDispatch` line 201) and reduces the test to coverage already shipped by `SendCapiEventEventLogTest`. The Watcher's `DB::afterCommit` callback executes immediately when no DB transaction is open (which is the test's default state), so dispatch fires synchronously inside the `handleUpdated` invocation.

    2. **Queue connection — force `sync` in setUp, NO `Queue::fake()`, NO `dispatchSync` from test bodies.** Add `Config::set('queue.default', 'sync');` to setUp (place after `parent::setUp();` and before the `bootSystemSettings()` chain). This makes `SendCapiEvent::dispatch` (called by `fireForwardDispatch`) resolve into an inline `handle()` invocation under the same call stack. Re-read of step (e) "Now invoke `SendCapiEvent::dispatchSync('Purchase', ...)`" in Scenario 2 + step (b) "dispatchSync the first CAPI event" in Scenario 3 + step (c)/(d) "then dispatchSync" in Scenario 5: REPLACE all of them with the Watcher `handleUpdated($obOrder)` call (after the prerequisite `$obOrder->save()`). The Watcher path is sufficient; no separate dispatchSync call needed.

    3. **Refire-flag round-trip — reflection, NOT `Settings::set`.** Step (a) of Scenario 3 stays as-is — `Settings::instance()->setAttribute('refire_purchase_on_status_flip', false)` is the locked path (HR-02 mitigation: `Settings::set` flaps under SQLite-in-memory; sibling `SendCapiEventEventLogTest::primeSettings` lines 247-253 are the proven reference).

    4. **Carbon time freeze — belt-and-suspenders, NOT the source of truth.** Always seed `$arPayload['data'][0]['event_time'] = self::FIXED_EVENT_TIME` in `makePayload()` (the SendCapiEvent constructor reads `event_time` from `$arPayload[data][0]`, NOT from `time()`). The `Carbon::setTestNow(Carbon::createFromTimestamp(self::FIXED_EVENT_TIME))` freeze in setUp is defensive cover for any incidental `Carbon::now()` reads inside helpers. If a future production change starts reading `time()` directly, the payload injection still wins.

    IMPLEMENTATION NOTE: production Watcher wires via `Event::subscribe(OrderStatusWatcher::class)` in Plugin::boot. In the test environment (autoRegister = autoMigrate = false), this MUST be called manually in setUp (mirror `OrderStatusWatcherEventLogTest` setUp body — registers the subscriber, then the test drives `handleUpdated` directly because `eloquent.updated` event dispatch on the Order in a hermetic environment is more brittle than the direct invocation).

    IMPLEMENTATION NOTE: For Scenario 3 step (d), the assertion "MockHandler history STILL count = 1 because second drain would have errored" is the hard fail-fast lock — do NOT queue a second `Response` into the MockHandler. If `alreadyDispatched` gate fails to short-circuit, the second `handleUpdated` call WILL try to POST and the MockHandler will throw `OutOfBoundsException` — that exception IS the test failure signal. Tiger-Style: the test exploits MockHandler's exhaustion as an assertion mechanism.
  </action>
  <verify>
    <automated>
      cd plugins/logingrupa/metapixelshopaholic
      &amp;&amp; test -f tests/Feature/PurchaseEndToEndIntegrationTest.php
      &amp;&amp; grep -q "final class PurchaseEndToEndIntegrationTest extends MetapixelTestCase" tests/Feature/PurchaseEndToEndIntegrationTest.php
      &amp;&amp; grep -q "test_paypal_flow_fires_capi_then_pixel_with_same_event_id" tests/Feature/PurchaseEndToEndIntegrationTest.php
      &amp;&amp; grep -q "test_bank_transfer_admin_flip_fires_capi_only_pixel_renders_on_later_visit" tests/Feature/PurchaseEndToEndIntegrationTest.php
      &amp;&amp; grep -q "test_status_flip_flop_with_refire_off_does_not_redispatch" tests/Feature/PurchaseEndToEndIntegrationTest.php
      &amp;&amp; grep -q "test_pixel_row_suppresses_refire_across_devices_and_sessions" tests/Feature/PurchaseEndToEndIntegrationTest.php
      &amp;&amp; grep -q "test_multi_site_same_order_id_records_two_independent_capi_rows" tests/Feature/PurchaseEndToEndIntegrationTest.php
      &amp;&amp; grep -q "FIXED_EVENT_TIME = 1715000000" tests/Feature/PurchaseEndToEndIntegrationTest.php
      &amp;&amp; grep -q "Carbon::setTestNow" tests/Feature/PurchaseEndToEndIntegrationTest.php
      &amp;&amp; grep -q "this-&gt;app-&gt;instance(MetaClient::class" tests/Feature/PurchaseEndToEndIntegrationTest.php
      &amp;&amp; grep -q "Event::subscribe(OrderStatusWatcher::class)" tests/Feature/PurchaseEndToEndIntegrationTest.php
      &amp;&amp; ! grep -nE '^[^*]*\bassert\(' tests/Feature/PurchaseEndToEndIntegrationTest.php
      &amp;&amp; vendor/bin/pest tests/Feature/PurchaseEndToEndIntegrationTest.php
      &amp;&amp; composer pint-test
      &amp;&amp; composer analyse
    </automated>
  </verify>
  <acceptance_criteria>
    - 5 test methods present and passing under `vendor/bin/pest`.
    - Each test exercises the FULL chain (Watcher or direct Order save → SendCapiEvent → EventLogWriter → MockHandler ↔ PurchasePixel onRun → onMarkFired). No single test mocks out more than one stage.
    - Scenario 1 asserts CAPI and Pixel rows share `event_id` AND `event_time`.
    - Scenario 2 asserts zero dispatch on non-paid save + exactly one dispatch on admin flip + Pixel renders on later customer visit.
    - Scenario 3 asserts MockHandler history count + EventLog::where(channel=capi)->count() BOTH stay at 1 across the flip-flop with refire OFF.
    - Scenario 4 asserts `arMetaEvent === null` on both renders AND `['ok' => true, 'won_race' => false]` on second `onMarkFired` AND `EventLog::where(channel=pixel)->count() === 1`.
    - Bonus multi-site test asserts `EventLog::count() === 2` after dispatching twice under different `Config::set('system.active_site', ...)` values for the SAME Order id.
    - Deterministic time: `Carbon::setTestNow(Carbon::createFromTimestamp(1715000000))` in setUp; `tearDown` unfreezes.
    - Zero `assert(...)` calls (spaze plugin invariant) — grep gate.
    - `composer pint-test` + `composer analyse` clean (PSR-2 + phpstan level 10).
    - Methods <70 LOC each (Tiger-Style).
    - Hungarian notation on every local variable.
  </acceptance_criteria>
  <done>
    The 4 BRIEF acceptance scenarios + 1 multi-site sanity check are codified as automated integration tests in CI. Deferred-runtime gap from VERIFICATION.md is closed at the contract level — only live-environment plumbing (real PayPal IPN + real Pixel script + Meta dashboard) remains for the operator-side runbook.
  </done>
</task>

<task type="auto">
  <name>Task 2: STAGING-RUNBOOK.md — operator playbook for live-environment verification</name>
  <files>
    plugins/logingrupa/metapixelshopaholic/.planning/phases/03.1-event-log-refactor/STAGING-RUNBOOK.md (NEW)
  </files>
  <read_first>
    plugins/logingrupa/metapixelshopaholic/.planning/phases/03.1-event-log-refactor/BRIEF.md (acceptance criteria lines 280-294),
    plugins/logingrupa/metapixelshopaholic/.planning/phases/03.1-event-log-refactor/VERIFICATION.md (Deferred-Runtime Verification block lines 162-178; Recommended Next Actions for Operator lines 181-189),
    plugins/logingrupa/metapixelshopaholic/.planning/phases/03.1-event-log-refactor/03.1-05-SUMMARY.md (Phase 3.1 Forward Notes section),
    plugins/logingrupa/metapixelshopaholic/.github/workflows/metapixel-qa.yml (confirms composer qa already wired in CI — runbook references it),
    plugins/logingrupa/metapixelshopaholic/updates/version.yaml (confirms 1.1.0 ledger entry — runbook quotes it).
  </read_first>
  <action>
    Create `STAGING-RUNBOOK.md` in `.planning/phases/03.1-event-log-refactor/`. Markdown; H1 title `# Phase 3.1 Staging Runbook — Event-Log Refactor v1.1.0`. Frontmatter at top:

    ```yaml
    ---
    phase: 03.1-event-log-refactor
    plugin_version: 1.1.0
    audience: operator (deployer of nailscosmetics.no / .lv / .lt)
    purpose: Close BRIEF acceptance criteria 1, 4, and 5 — runtime-verification gap deferred in VERIFICATION.md
    requires: merged main branch + Laravel Forge access + Meta Events Manager Test Events tab
    estimated_duration: 30 min (sequential 4 scenarios + composer qa + SQL check)
    ---
    ```

    Sections (in order):

    1. `## Preconditions`
       - Branch merged to `master` with all Phase 3.1 commits (03.1-01..06).
       - Laravel Forge SSH access to staging server (lowest-traffic site first — .lt or .lv).
       - Meta Business Suite → Events Manager → Test Events tab open in a separate browser window with `test_event_code` configured.
       - One real Meta Pixel pixel_id loaded in plugin Settings (NOT the production pixel — staging Pixel only).

    2. `## Step 1 — Deploy v1.1.0`
       Numbered shell commands:
       ```bash
       # SSH into staging
       ssh forge@staging.nailscosmetics.lv

       # Pull latest
       cd /home/forge/staging.nailscosmetics.lv
       git fetch origin master
       git checkout master
       git pull origin master

       # Vendor + migrations
       composer install --no-dev --optimize-autoloader --no-interaction
       php artisan october:up

       # Flush OPcache so the new code is actually live
       sudo systemctl reload php8.4-fpm

       # Clear October caches
       php artisan cache:clear
       php artisan october:util clear cache
       ```
       Expected output: `october:up` reports `Logingrupa.Metapixelshopaholic: Database is up to date.` AFTER the migration list shows `drop_meta_purchase_columns_from_orders_table` + `create_metapixel_event_log_table` ran exactly once.

    3. `## Step 2 — Confirm system_plugin_versions row reads v1.1.0` (BRIEF criterion 5)
       SQL check (run via `php artisan tinker` OR Forge MySQL console):
       ```sql
       SELECT version FROM system_plugin_versions
       WHERE code = 'Logingrupa.Metapixelshopaholic';
       ```
       Expected: single row, `version = '1.1.0'`. If the row reads `1.0.3` or earlier → `php artisan october:up` did not run or the migration list was not picked up → re-check `updates/version.yaml` matches `git log -1 --name-only`.

    4. `## Step 3 — Run composer qa` (BRIEF criterion 1)
       ```bash
       cd /home/forge/staging.nailscosmetics.lv/plugins/logingrupa/metapixelshopaholic
       composer install --dev --no-interaction
       composer qa
       ```
       Expected: `pint-test`, `analyse`, `phpmd`, `pest` all exit 0. Coverage report shows ≥ 90% plugin-wide (Phase 3 baseline 89.6%; Phase 3.1 Wave-5 PurchaseEndToEndIntegrationTest lifts coverage above the threshold).

       (Note: CI workflow `.github/workflows/metapixel-qa.yml` already runs this on every push/PR — the staging-side check is the operator's belt-and-suspenders confirmation before flipping production traffic.)

    5. `## Step 4 — Scenario 1: PayPal end-to-end CAPI + Pixel pair` (BRIEF criterion 2 + 3, lines 283-285)
       Procedure:
       1. Create a test order at `/lv/cart` via the live UI. Choose PayPal as the payment method.
       2. Complete PayPal sandbox checkout (or live with a small-value real order — operator's choice).
       3. PayPal IPN flips the order to `new-payment-received`. Watch the Order detail in Backend Admin Orders → confirm `status.code = 'new-payment-received'`.
       4. Open Meta Events Manager → Test Events. Within ≤ 60 seconds, expect exactly TWO event entries for the SAME `event_id`: one labelled `Browser (Pixel)`, one labelled `Server (CAPI)`. Meta UI shows them as deduplicated (the `Deduplicated` badge is visible).
       5. Customer-side: navigate to `/lv/checkout/{secret_key}` for the order. Open browser DevTools Network → filter by `facebook.com`. Confirm `fbevents.js` fires `track Purchase` exactly ONCE per page load. Refresh the page. Confirm `track Purchase` does NOT fire on the refresh (server-side `event_log` row with `channel='pixel'` blocks `onRun()`).
       Expected dashboard metric: dedup ≥ 80% on Purchase, EMQ ≥ 8.
       Pass/fail recorded in the operator log + STATE.md.

    6. `## Step 5 — Scenario 2: Bank-transfer admin flip` (BRIEF criterion 2 line 286)
       Procedure:
       1. Place a bank-transfer order via `/lv/checkout` (payment method: bank-transfer). Order lands in status `new` (id=1).
       2. Wait 30 seconds — confirm NO Purchase event appears in Test Events for this order (the order is unpaid; nothing should fire).
       3. In Backend Admin → Orders → flip the order's status to `new-payment-received`. Save.
       4. Within ≤ 60 seconds: Test Events shows exactly ONE event for the order, labelled `Server (CAPI)` only (no Browser/Pixel twin yet — the customer is not on `/lv/checkout/{secret_key}` at this moment).
       5. Customer visits `/lv/checkout/{secret_key}` later (different browser session simulating "next time customer opens the link"). Confirm Test Events now shows the matching `Browser (Pixel)` entry with the SAME `event_id` as step 4.
       Pass/fail recorded.

    7. `## Step 6 — Scenario 3: Status flip-flop never re-fires` (BRIEF criterion 2 line 287)
       Procedure:
       1. Take the order from Scenario 1 OR Scenario 2 (already paid, has CAPI+Pixel rows).
       2. In Backend Admin → flip status to `canceled` (id=4). Save.
       3. Wait 30 seconds — confirm NO new event appears in Test Events (the away-flip is a no-op).
       4. Flip status BACK to `new-payment-received`. Save.
       5. Wait 60 seconds — confirm Test Events shows NO NEW event for this order. The existing CAPI+Pixel pair is unchanged.
       6. Verify in DB: `SELECT COUNT(*) FROM logingrupa_metapixel_event_log WHERE subject_id = {iOrderId} AND event_name = 'Purchase' AND channel = 'capi';` → returns exactly 1.
       Pass/fail recorded.

    8. `## Step 7 — Scenario 4: Refresh + incognito on different device never re-fires` (BRIEF criterion 3 + 4 lines 287-288)
       Procedure:
       1. Take the order from Scenario 1 (has CAPI+Pixel rows).
       2. Hard-refresh `/lv/checkout/{secret_key}` (Ctrl+Shift+R). Confirm DevTools Network → `fbevents.js` → NO `track Purchase` request fires. PageView still fires (PixelHead is untouched).
       3. Open the same URL in a new incognito window on a DIFFERENT device (e.g. phone). Confirm NO `track Purchase` fires.
       4. Wait 10 days (or set a calendar reminder; this is the cross-Meta-7-day-dedup-window test). On day 11, repeat step 3. Still no `track Purchase` — server-side `event_log` is the only source of truth, independent of Meta's eventID dedup window expiry.
       Pass/fail recorded (the 10-day check can be deferred; capture the immediate refresh + incognito results as the criterion-4 pass marker).

    9. `## Step 8 — Record checkpoint pass in STATE.md`
       Once Steps 1-7 are all PASS, append to `.planning/STATE.md` Phase 3.1 Forward Notes block:
       ```markdown
       ### Phase 3.1 Manual Staging Checkpoint — PASSED YYYY-MM-DD
       - Site: {staging.nailscosmetics.{lt,lv,no}}
       - composer qa: green
       - system_plugin_versions: 1.1.0
       - Scenario 1 (PayPal): PASS — dedup XX%, EMQ X
       - Scenario 2 (bank-transfer admin flip): PASS
       - Scenario 3 (status flip-flop): PASS
       - Scenario 4 (refresh + incognito): PASS
       - Operator: {your name / email}
       ```
       Commit: `docs(03.1-06): staging checkpoint PASSED on {site}`. Tag plugin `v1.1.0` once all three sites pass.

    10. `## Failure recovery`
        - Migration list incomplete → roll back via `php artisan october:down --rollback={count}` then re-pull master + re-run `october:up`.
        - composer qa fails → re-read latest plugin SUMMARYs; identify which test broke; fix on a feature branch; re-merge.
        - Test Events shows duplicate (non-deduped) Purchase events → `event_id` mismatch between server and browser → check that `PurchasePixel::onRun()` reads from the EventLog CAPI row (not a fresh UUID); confirm `arMetaEvent.event_id` in DevTools Network payload matches `event_log.event_id` for that order.
        - Test Events shows zero events on PayPal flow → check PayPal IPN reaches the site (Forge logs); check `OrderStatusWatcher::handleUpdated` fires (add a temporary `Log::info` in the watcher, redeploy, retry); check `composer qa` ran the latest test suite (the WatcherTest covers this contract).

    11. `## Cross-references`
        - BRIEF acceptance criteria: `.planning/phases/03.1-event-log-refactor/BRIEF.md` lines 280-294.
        - VERIFICATION report (deferred items): `.planning/phases/03.1-event-log-refactor/VERIFICATION.md` lines 162-178.
        - Integration test class (CI-side proofs): `tests/Feature/PurchaseEndToEndIntegrationTest.php`.
        - CI workflow: `.github/workflows/metapixel-qa.yml`.

    Markdown style: 2-space indent in fenced code blocks; backticks for filenames, env vars, SQL keywords; numbered lists for procedures; bold for emphasised pass/fail markers. NO emojis (CLAUDE.md rule).
  </action>
  <verify>
    <automated>
      cd plugins/logingrupa/metapixelshopaholic
      &amp;&amp; test -f .planning/phases/03.1-event-log-refactor/STAGING-RUNBOOK.md
      &amp;&amp; grep -q "# Phase 3.1 Staging Runbook" .planning/phases/03.1-event-log-refactor/STAGING-RUNBOOK.md
      &amp;&amp; grep -q "plugin_version: 1.1.0" .planning/phases/03.1-event-log-refactor/STAGING-RUNBOOK.md
      &amp;&amp; grep -q "## Step 1 — Deploy v1.1.0" .planning/phases/03.1-event-log-refactor/STAGING-RUNBOOK.md
      &amp;&amp; grep -q "## Step 2 — Confirm system_plugin_versions row reads v1.1.0" .planning/phases/03.1-event-log-refactor/STAGING-RUNBOOK.md
      &amp;&amp; grep -q "## Step 3 — Run composer qa" .planning/phases/03.1-event-log-refactor/STAGING-RUNBOOK.md
      &amp;&amp; grep -q "Scenario 1: PayPal end-to-end" .planning/phases/03.1-event-log-refactor/STAGING-RUNBOOK.md
      &amp;&amp; grep -q "Scenario 2: Bank-transfer admin flip" .planning/phases/03.1-event-log-refactor/STAGING-RUNBOOK.md
      &amp;&amp; grep -q "Scenario 3: Status flip-flop never re-fires" .planning/phases/03.1-event-log-refactor/STAGING-RUNBOOK.md
      &amp;&amp; grep -q "Scenario 4: Refresh + incognito on different device" .planning/phases/03.1-event-log-refactor/STAGING-RUNBOOK.md
      &amp;&amp; grep -q "Step 8 — Record checkpoint pass in STATE.md" .planning/phases/03.1-event-log-refactor/STAGING-RUNBOOK.md
      &amp;&amp; grep -q "php artisan october:up" .planning/phases/03.1-event-log-refactor/STAGING-RUNBOOK.md
      &amp;&amp; grep -q "SELECT version FROM system_plugin_versions" .planning/phases/03.1-event-log-refactor/STAGING-RUNBOOK.md
      &amp;&amp; grep -q "composer qa" .planning/phases/03.1-event-log-refactor/STAGING-RUNBOOK.md
      &amp;&amp; grep -q "PurchaseEndToEndIntegrationTest" .planning/phases/03.1-event-log-refactor/STAGING-RUNBOOK.md
    </automated>
  </verify>
  <acceptance_criteria>
    - Runbook has frontmatter with `plugin_version: 1.1.0` + `audience` + `purpose`.
    - 11 numbered sections (Preconditions through Cross-references) in the specified order.
    - The 4 BRIEF scenarios appear as Steps 4-7 with concrete operator procedures.
    - SQL check for `system_plugin_versions` is present and exact.
    - `composer qa` is referenced AND noted as already CI-wired.
    - STATE.md checkpoint-passed template line is present (Step 8).
    - Cross-references to BRIEF, VERIFICATION, PurchaseEndToEndIntegrationTest, and metapixel-qa.yml workflow are all present.
    - Markdown lints clean (no broken fenced code blocks; backticks balanced).
  </acceptance_criteria>
  <done>
    Operator has a complete, step-by-step playbook for the last-10% live-environment verification. Combined with the Task 1 integration test class, BRIEF acceptance criteria 1, 2, 3, 4, and 5 are now ALL covered (contracts in CI + plumbing in runbook).
  </done>
</task>

<task type="auto">
  <name>Task 3: Update STATE.md + ROADMAP.md — re-open and close Phase 3.1 Wave 5</name>
  <files>
    plugins/logingrupa/metapixelshopaholic/.planning/STATE.md (UPDATE)
    plugins/logingrupa/metapixelshopaholic/.planning/ROADMAP.md (UPDATE)
  </files>
  <read_first>
    plugins/logingrupa/metapixelshopaholic/.planning/STATE.md (current frontmatter + Current Position + Accumulated Context + Pending Todos + Phase 3.1 Forward Notes — the FULL file before editing),
    plugins/logingrupa/metapixelshopaholic/.planning/ROADMAP.md (Phase 3.1 entry — line 73 onwards; the `**Plans:**` list under Phase 3.1; the Progress table at the bottom),
    plugins/logingrupa/metapixelshopaholic/.planning/phases/03.1-event-log-refactor/VERIFICATION.md (the deferred-runtime block — this plan CLOSES that gap).
  </read_first>
  <action>
    Edit BOTH files surgically (use Edit tool for targeted line changes; Write only if a complete section rewrite is necessary and the prior content is preserved verbatim elsewhere).

    **STATE.md edits:**

    1. Frontmatter:
       - `status: phase-3.1-runtime-verified` (was `phase-3.1-complete`).
       - `last_updated:` ISO datetime today (2026-05-14, with a Z-suffix UTC timestamp matching prior plans' format).
       - `last_activity: 2026-05-14`.
       - `stopped_at:` extend with a new sentence: "Phase 3.1 Wave-5 (Plan 03.1-06) closes the runtime-verification gap: PurchaseEndToEndIntegrationTest codifies the 4 BRIEF scenarios in CI + STAGING-RUNBOOK.md hands the live-plumbing checks to the operator. Next: operator runs `STAGING-RUNBOOK.md` on staging; on PASS, append checkpoint line + tag v1.1.0; then `/gsd-plan-phase 4`."
       - `progress.completed_plans:` increment by 1 (16 → 17 if Plan 03.1-06 is treated as a completed plan once executor finishes; the planner records the BEFORE state — the executor will bump after running the plan).
       - `progress.percent:` recompute deterministically from `completed_plans / total_plans`.
       NOTE: executor edits these numerics in the SUMMARY-write step, not as part of plan creation; the planner writes the SEMANTIC closure language above.

    2. Current Position:
       - Change "Phase 3.1 (event-log refactor) — COMPLETE" to "Phase 3.1 (event-log refactor) — RUNTIME-VERIFIED (CI contracts) + STAGING CHECKPOINT PENDING (operator action via STAGING-RUNBOOK.md)".
       - Plan: 6 of 6 (was 5 of 5).
       - Status: extend with "PurchaseEndToEndIntegrationTest (5 methods) codifies the 4 BRIEF acceptance scenarios + 1 multi-site sanity check in CI. STAGING-RUNBOOK.md spells out the deploy + 4-scenario live-environment procedures."

    3. Accumulated Context → Pending Todos:
       - Find the deferred-runtime item (added in Plan 03.1-05 under Phase 3.1 Forward Notes; recorded in VERIFICATION.md `human_verification` block) → mark CLOSED via strikethrough or "✓ CLOSED 2026-05-14 by Plan 03.1-06 — integration test in CI + STAGING-RUNBOOK.md for operator".
       - PH-01 + MC-07 remain OPEN for Phase 5 (preservation invariant from Plan 03.1-05).

    4. Phase 3.1 Forward Notes:
       - Add a link line: "**Operator handoff**: see [`STAGING-RUNBOOK.md`](phases/03.1-event-log-refactor/STAGING-RUNBOOK.md) for the deploy + 4-scenario live-environment procedures."
       - Preserve all prior Forward Notes content (Phase 4 API surface block-quote, system_plugin_versions ledger check, PAY-10/PAY-11 supersession).

    5. Session Continuity:
       - Last activity: 2026-05-14.
       - Stopped at: "Phase 3.1 Wave-5 — runtime verification closed at the contract level (PurchaseEndToEndIntegrationTest in CI); operator runs STAGING-RUNBOOK.md to close the live-environment checkpoint, then tag v1.1.0 + `/gsd-plan-phase 4`."

    **ROADMAP.md edits:**

    6. Phase 3.1 `**Plans:**` line (currently "Plans 03.1-01..05 executed 2026-05-13 (see ...). Plugin v1.1.0 published."):
       - REWRITE this single line to: "Plans 03.1-01..05 executed 2026-05-13 (see `phases/03.1-event-log-refactor/0*-SUMMARY.md`). Plan 03.1-06 (Wave 5) executed YYYY-MM-DD — staging-checkpoint automation. Plugin v1.1.0 published; operator runbook at `phases/03.1-event-log-refactor/STAGING-RUNBOOK.md`."
       - (Executor fills in the YYYY-MM-DD when the plan completes.)

    7. Progress table row for Phase 3.1:
       - Update "5/5" to "6/6".
       - Update "Complete — v1.1.0 published; manual staging checkpoint pending operator" to "Complete — v1.1.0 published; CI contracts (5 BRIEF scenarios) locked; operator runbook ships at STAGING-RUNBOOK.md".

    8. Phase 3.1 header line at top of file (top-level checklist entry):
       - Append "; Plan 03.1-06 Wave 5 closes runtime-verification gap with PurchaseEndToEndIntegrationTest + STAGING-RUNBOOK.md" after the existing "Plugin bumped to v1.1.0." sentence.

    Tiger-Style: surgical edits only. Use the Edit tool with exact-match strings. Do NOT delete any prior phase context. Do NOT renumber existing plans. Phase 3.1 retains its checkmark `[x]` — Wave 5 is the closure of the runtime-verification SUBGOAL, not a new phase.
  </action>
  <verify>
    <automated>
      cd plugins/logingrupa/metapixelshopaholic
      &amp;&amp; grep -q "status: phase-3.1-runtime-verified" .planning/STATE.md
      &amp;&amp; grep -q "PurchaseEndToEndIntegrationTest" .planning/STATE.md
      &amp;&amp; grep -q "STAGING-RUNBOOK.md" .planning/STATE.md
      &amp;&amp; grep -q "RUNTIME-VERIFIED" .planning/STATE.md
      &amp;&amp; grep -q "Plan 03.1-06" .planning/ROADMAP.md
      &amp;&amp; grep -q "STAGING-RUNBOOK.md" .planning/ROADMAP.md
      &amp;&amp; grep -q "6/6" .planning/ROADMAP.md
    </automated>
  </verify>
  <acceptance_criteria>
    - STATE.md `status:` advanced to `phase-3.1-runtime-verified`.
    - STATE.md Current Position reflects "RUNTIME-VERIFIED (CI contracts) + STAGING CHECKPOINT PENDING".
    - STATE.md Pending Todos closes the deferred-runtime item from Plan 03.1-05.
    - STATE.md Forward Notes links to STAGING-RUNBOOK.md.
    - ROADMAP.md Phase 3.1 plan list mentions 03.1-06 + STAGING-RUNBOOK.md.
    - ROADMAP.md Phase 3.1 row in Progress table reads 6/6.
    - No accidental deletion of prior phase context.
    - Phase 3.1 still marked `[x]` (checkmark preserved — Wave-5 closes a sub-gap, not a new phase).
  </acceptance_criteria>
  <done>
    STATE.md + ROADMAP.md reflect Phase 3.1 Wave-5 closure. Plugin remains at v1.1.0. Operator has a clear handoff path: read STAGING-RUNBOOK.md → execute → append checkpoint-passed line → tag v1.1.0 → trigger `/gsd-plan-phase 4`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test environment → Production behavior | The integration test exercises the FULL chain in SQLite-in-memory + MockHandler. Mismatch between hermetic test schema and production MySQL/MariaDB is a risk; mitigated by reusing the exact `bootEventLogTable` + `bootOrdersTable` helpers already proven against the production migration in Plan 03.1-01. |
| STAGING-RUNBOOK.md → Operator | The runbook IS the human-verification surface. Missing step / wrong SQL / wrong dashboard expectation = blind handoff. |
| STATE.md → Future planner | Phase 4 will read STATE.md to discover the EventLog + EventLogWriter + SiteResolver API surface AND the runtime-verified status. Outdated `status:` field = wrong assumption about deploy readiness. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3.1-30 | Tampering | PurchaseEndToEndIntegrationTest hermetic schema drift | mitigate | Test reuses `MetapixelTestCase::bootEventLogTable` which mirrors production migration column-by-column (Plan 03.1-01 SUMMARY locks this). Plan 03.1-02 Wave-2 SUMMARY recorded production-vs-test schema parity assertion. |
| T-3.1-31 | Tampering | Carbon::setTestNow propagation | mitigate | If Carbon mock does not reach production `time()` calls, fall back to seeding deterministic `event_time` directly into `$arPayload['data'][0]['event_time']`. Documented in Task 1 IMPLEMENTATION NOTE. |
| T-3.1-32 | Tampering | Event::subscribe(OrderStatusWatcher::class) test bleed | mitigate | If the subscribed listener persists across test methods (causing duplicate dispatch in subsequent tests), tearDown calls `Event::forget('eloquent.updated: '.Order::class)` — escape hatch documented in Task 1 NOTE. |
| T-3.1-33 | Repudiation | Operator checkpoint-pass log entry | mitigate | STAGING-RUNBOOK.md Step 8 mandates a STATE.md checkpoint-passed template line + a `docs(03.1-06)` commit + an annotated git tag `v1.1.0`. Triple-redundant audit trail. |
| T-3.1-34 | Information Disclosure | STAGING-RUNBOOK.md operator email/name | accept | Operator name in checkpoint line is internal accountability — recorded in private repo. No customer PII. |
| T-3.1-35 | Denial of Service | composer qa runtime on staging (long-running) | accept | Operator-side belt-and-suspenders only; CI workflow on every push/PR is the primary gate. Staging composer qa is optional + idempotent. |
| T-3.1-36 | Elevation of Privilege | onMarkFired event_id forgery on staging | mitigate | The integration test `test_pixel_row_suppresses_refire_across_devices_and_sessions` exercises the second-call path; PurchasePixelEventLogGateTest method `test_onmarkfired_rejects_event_id_mismatch` covers the spoofing branch. Both are in CI. |
| T-3.1-37 | Tampering | STATE.md frontmatter `progress.completed_plans` drift | mitigate | Executor recomputes deterministically from ROADMAP plan list when finalising the plan SUMMARY. Planner records the intent in Task 3 only; numerics filled in at execute time. |
</threat_model>

<verification>
After all 3 tasks (END OF PHASE 3.1 WAVE 5):

1. `vendor/bin/pest tests/Feature/PurchaseEndToEndIntegrationTest.php` reports green — all 5 test methods pass.
2. `vendor/bin/pest tests/Feature/` reports green — the new file does not break any of the 14 pre-existing feature test files (EventLogTest, SendCapiEventEventLogTest, OrderStatusWatcherEventLogTest, PurchasePixelEventLogGateTest, MultiSiteEventLogTest, SendCapiEventTest, PurchasePixelTest, BootsWithoutPixelIdTest, EnsureFbpFbcCookiesTest, FailedEventModelTest, MigrationsBootTest, PixelHeadTest, SettingsRegistrationTest).
3. `composer qa` exits 0. Coverage report shows ≥ 90% plugin-wide.
4. `test -f .planning/phases/03.1-event-log-refactor/STAGING-RUNBOOK.md` returns 0.
5. `grep -q "## Step 4 — Scenario 1: PayPal end-to-end" .planning/phases/03.1-event-log-refactor/STAGING-RUNBOOK.md` returns 0 (all 4 scenarios present).
6. STATE.md `status: phase-3.1-runtime-verified`.
7. ROADMAP.md Phase 3.1 plan list contains `03.1-06-PLAN.md`.
8. `grep -rn 'meta_purchase_event_id\|meta_purchase_event_time' --include='*.php' --include='*.htm' --include='*.yaml' . | grep -v 'updates/drop_meta_purchase_columns_from_orders_table.php' | grep -v 'updates/version.yaml'` STILL returns ZERO lines (Plan 03.1-05 invariant preserved).
9. No new files in `classes/`, `components/`, `controllers/`, `middleware/`, `models/`, `updates/` (production code untouched).
10. Manual: operator runs STAGING-RUNBOOK.md → appends `Phase 3.1 Manual Staging Checkpoint — PASSED YYYY-MM-DD` to STATE.md → tags v1.1.0. THIS IS THE FINAL OPERATOR ACTION — recorded for traceability, NOT part of plan execution.
</verification>

<success_criteria>
- `tests/Feature/PurchaseEndToEndIntegrationTest.php` ships with 5 test methods locking BRIEF acceptance scenarios 1-4 + multi-site sanity in CI.
- `STAGING-RUNBOOK.md` ships with the deploy + 4-scenario live-environment playbook + SQL check + composer qa command + checkpoint-passed template.
- STATE.md `status: phase-3.1-runtime-verified` reflects the closed runtime-verification gap.
- ROADMAP.md Phase 3.1 plan list contains `03.1-06-PLAN.md` + STAGING-RUNBOOK.md cross-reference.
- All BRIEF acceptance criteria 1-5 are addressed (contracts in CI + plumbing in runbook). Criterion 6-9 remain locked by Plans 03.1-01..05 (verified in VERIFICATION.md).
- `composer qa` green. No production-code edits. Plugin remains at v1.1.0.
</success_criteria>

<output>
After completion, create `.planning/phases/03.1-event-log-refactor/03.1-06-SUMMARY.md` capturing:

- 5 integration test methods + scenario mapping table (test method ↔ BRIEF criterion ↔ VERIFICATION deferred item ↔ STAGING-RUNBOOK step).
- LOC counts per test method (Tiger-Style <70 LOC compliance table).
- Final coverage delta vs Plan 03.1-05 baseline.
- STAGING-RUNBOOK.md section index (Steps 1-11).
- STATE.md + ROADMAP.md delta (before/after for `status:`, `completed_plans`, `Progress` table row, plan list).
- CI workflow confirmation (already wired `composer qa` — no edit needed).
- Operator-facing next-step block: "Run STAGING-RUNBOOK.md on the lowest-traffic site → on PASS, append checkpoint line to STATE.md → tag v1.1.0 → repeat on remaining two sites → trigger `/gsd-plan-phase 4` for Funnel Completion."
</output>
</content>
</invoke>