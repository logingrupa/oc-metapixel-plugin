---
id: 03.1-05
phase: 03.1-event-log-refactor
plan: 05
type: execute
wave: 4
depends_on:
  - 03.1-01
  - 03.1-02
  - 03.1-03
  - 03.1-04
files_modified:
  - plugins/logingrupa/metapixelshopaholic/tests/Feature/MultiSiteEventLogTest.php
  - plugins/logingrupa/metapixelshopaholic/.planning/STATE.md
autonomous: true
requirements:
  - REFAC-10
  - REFAC-11
rationale: >
  Wave-4 final cleanup + multi-site contract proof. REFAC-10 closes the
  loose ends: a sweep across the plugin tree to verify ZERO surviving
  references to `meta_purchase_event_id` / `meta_purchase_event_time` in
  any docblock, PHPDoc, helper comment, or fixture; and STATE.md Pending
  Todos updated to close the Phase 3 column-related items (PAY-10 manual
  staging checkpoint deferral, MC-07 PH-01 retro-fit note context). REFAC-11
  ships the final test file — `MultiSiteEventLogTest` — locking the
  multi-site row independence + single-site NULL-site_id scoping + active-site
  read-scoping invariants. Version.yaml is already at v1.1.0 from Wave-1
  Task 2 — this plan VERIFIES the system_plugin_versions row would record
  v1.1.0 after `php artisan october:up`; no further file edit needed unless
  drift surfaces during verification.
must_haves:
  truths:
    - "Zero references to `meta_purchase_event_id` or `meta_purchase_event_time` anywhere in `plugins/logingrupa/metapixelshopaholic/` EXCEPT in the `drop_meta_purchase_columns_from_orders_table.php` migration's `down()` (rollback re-add) and in the version.yaml 1.0.1 historical changelog line. Confirmed via `grep -rn` from the plugin root."
    - "`tests/Feature/MultiSiteEventLogTest.php` exists with 3 test methods locking: (a) two sites bind same Order id → 2 independent rows recorded, (b) single-site install (SiteResolver returns null) → site_id NULL on all rows, (c) active-site-scoped read excludes other sites' rows."
    - "STATE.md Pending Todos has a Phase 3.1 closure section documenting: PAY-10 + PAY-11 manual staging checkpoint is now the canonical staging verification for the new mechanism (NOT a column-based contract anymore); SCE-09 lock superseded; SCE-03 lock superseded (constructor breaking change); WR-12 superseded; column-related items closed."
    - "`composer qa` exits 0 with full plugin coverage ≥ 90 % (target: matches or exceeds Phase 3's 89.6 % since the rewrite preserved coverage AND the new tests add coverage for SiteResolver + EventLogWriter + EventLog model + multi-site scoping)."
    - "After `php artisan october:up`, `system_plugin_versions` records `code = 'Logingrupa.Metapixelshopaholic'` + `version = '1.1.0'` (verified by manual `php artisan tinker` or grep of the migrations ledger output — this is a runtime check; the SUMMARY records the operator-facing verification command)."
    - "All 5 acceptance criteria from BRIEF.md (lines 280-294) are now demonstrably met by the test suite + schema state. The 4th criterion (refresh `/lv/checkout/{slug}` → PageView fires, Purchase does NOT re-fire) and the 5th (new incognito on different device → still no Purchase re-fire) are testable behaviorally via the PurchasePixelEventLogGateTest CAPI+Pixel gate logic shipped in Plan 03.1-04 — the test file proves the contract; staging verification is the manual rollover."
  artifacts:
    - path: "plugins/logingrupa/metapixelshopaholic/tests/Feature/MultiSiteEventLogTest.php"
      provides: "3 multi-site invariants. Single test file, no production-code edits — by Wave-4 the production code already honors multi-site contract via Wave-2 SiteResolver + Wave-3 SiteResolver-scoped queries."
      contains: "final class MultiSiteEventLogTest extends MetapixelTestCase"
    - path: "plugins/logingrupa/metapixelshopaholic/.planning/STATE.md"
      provides: "Phase 3.1 closure entry — Pending Todos sweep + Phase cursor advancement + accumulated context update for the new EventLog API surface."
      contains: "Phase 3.1 complete"
  key_links:
    - from: "tests/Feature/MultiSiteEventLogTest.php"
      to: "classes/helper/EventLogWriter.php + classes/helper/SiteResolver.php"
      via: "Config::set('system.active_site', $i) + EventLogWriter::record assertion chain"
      pattern: "Config::set\\('system\\.active_site'"
---

<objective>
Final-wave cleanup + multi-site contract proof. Sweep the plugin tree for
any surviving references to the dropped columns. Ship the 3-method
MultiSiteEventLogTest. Update STATE.md to close Phase 3.1 and roll
forward the v1.1.0 milestone.

Purpose: nothing in the plugin should mention the legacy column mechanism
once Phase 3.1 ships. Multi-site contract is locked by an explicit test
that exercises the SiteResolver + UNIQUE NULL-distinct semantic together.

Output: 1 new test file + STATE.md update + grep-sweep verification.
This is the smallest plan in the phase — its job is to PROVE the prior
waves left no loose ends.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@plugins/logingrupa/metapixelshopaholic/.planning/phases/03.1-event-log-refactor/BRIEF.md
@plugins/logingrupa/metapixelshopaholic/.planning/phases/03.1-event-log-refactor/03.1-PATTERNS.md
@plugins/logingrupa/metapixelshopaholic/.planning/phases/03.1-event-log-refactor/03.1-01-SUMMARY.md
@plugins/logingrupa/metapixelshopaholic/.planning/phases/03.1-event-log-refactor/03.1-02-SUMMARY.md
@plugins/logingrupa/metapixelshopaholic/.planning/phases/03.1-event-log-refactor/03.1-03-SUMMARY.md
@plugins/logingrupa/metapixelshopaholic/.planning/phases/03.1-event-log-refactor/03.1-04-SUMMARY.md
@plugins/logingrupa/metapixelshopaholic/classes/helper/EventLogWriter.php
@plugins/logingrupa/metapixelshopaholic/classes/helper/SiteResolver.php
@plugins/logingrupa/metapixelshopaholic/models/EventLog.php
@plugins/logingrupa/metapixelshopaholic/tests/Feature/FailedEventModelTest.php
@plugins/logingrupa/metapixelshopaholic/tests/MetapixelTestCase.php
@plugins/logingrupa/metapixelshopaholic/tests/Support/OrderFixtures.php
@plugins/logingrupa/metapixelshopaholic/.planning/STATE.md
@plugins/logingrupa/metapixelshopaholic/updates/version.yaml
@modules/system/classes/sitemanager/HasActiveSite.php

<interfaces>
From Wave 2 (Plan 03.1-02):
- `EventLogWriter::record(string, string, string, object, ?string, int): bool`.
- `SiteResolver::getActiveSiteId(): ?int` — reads `Config::get('system.active_site')` per October 4 SDK.

SDK contract: `Config::set('system.active_site', $i)` is the deterministic test injection path. No `App::instance` swap needed — the SDK reads from Config, not from any singleton state (verified at `modules/system/classes/sitemanager/HasActiveSite.php` lines 84-87 in PATTERNS.md line 1259).

Test base setUp helpers available after Wave 1 + 2:
- `bootSystemSettings()`, `bootOrdersStatuses()`, `bootOrdersTable()`, `bootEventLogTable()`.
- `OrderFixtures::makePaidOrder()` / `OrderFixtures::provisionHermeticOfferProductTables()`.

STATE.md frontmatter shape (current — Phase 3.1 awaiting plan):
```yaml
gsd_state_version: 1.0
milestone: v1.0.0
status: phase-3.1-inserted-awaiting-plan
progress:
  total_phases: 6
  completed_phases: 2
  total_plans: 6
  completed_plans: 5.5
```
After Wave 4 completion, advance to `status: phase-3.1-complete`, `milestone: v1.1.0`, recompute completed_plans for the 5 Phase-3.1 plans.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: MultiSiteEventLogTest — 3 multi-site invariants</name>
  <files>
    plugins/logingrupa/metapixelshopaholic/tests/Feature/MultiSiteEventLogTest.php (NEW)
  </files>
  <read_first>
    plugins/logingrupa/metapixelshopaholic/.planning/phases/03.1-event-log-refactor/BRIEF.md (REFAC-11 lines 264-272 — MultiSiteEventLogTest scope; Multi-site contract lines 52-74),
    plugins/logingrupa/metapixelshopaholic/.planning/phases/03.1-event-log-refactor/03.1-PATTERNS.md (Pattern Assignment: MultiSiteEventLogTest, lines 1195-1259),
    plugins/logingrupa/metapixelshopaholic/classes/helper/SiteResolver.php (Wave-2 — verify how the SDK probe reads Config),
    plugins/logingrupa/metapixelshopaholic/classes/helper/EventLogWriter.php (Wave-2 — the helper exercised by the test),
    plugins/logingrupa/metapixelshopaholic/models/EventLog.php (Wave-2),
    plugins/logingrupa/metapixelshopaholic/tests/Feature/FailedEventModelTest.php (skeleton + MetapixelTestCase setUp pattern),
    plugins/logingrupa/metapixelshopaholic/tests/Support/OrderFixtures.php (`makePaidOrder` returns Order with secret_key).
  </read_first>
  <behavior>
    - `test_two_sites_bind_same_order_id_records_independently` — `Config::set('system.active_site', 1)`; `$bWonA = EventLogWriter::record('uuid-a', 'Purchase', 'capi', $obOrder, $obOrder->secret_key, $iTime);` → expect `$bWonA === true`. Then `Config::set('system.active_site', 2)`; `$bWonB = EventLogWriter::record('uuid-b', 'Purchase', 'capi', $obOrder, $obOrder->secret_key, $iTime);` → expect `$bWonB === true` (NOT blocked by UNIQUE — site_id differs). `EventLog::count() === 2`.
    - `test_single_site_install_writes_null_site_id` — `Config::set('system.active_site', null);` → SiteResolver returns null → `EventLogWriter::record(...)` writes a row with `site_id IS NULL`. `EventLog::first()->site_id === null`.
    - `test_active_site_scoped_read_excludes_other_sites_rows` — record under site 1 + record under site 2 → switch Config back to site 1 → `EventLog::where('site_id', SiteResolver::getActiveSiteId())->count() === 1`.
  </behavior>
  <action>
    Create `tests/Feature/MultiSiteEventLogTest.php`. `declare(strict_types=1);`, namespace `Logingrupa\Metapixelshopaholic\Tests\Feature`.

    Imports: `Cache`, `Illuminate\Support\Facades\Config`, `Logingrupa\Metapixelshopaholic\Classes\Helper\EventLogWriter`, `Logingrupa\Metapixelshopaholic\Classes\Helper\PluginGuard`, `Logingrupa\Metapixelshopaholic\Classes\Helper\SiteResolver`, `Logingrupa\Metapixelshopaholic\Models\EventLog`, `Logingrupa\Metapixelshopaholic\Models\Settings`, `Logingrupa\Metapixelshopaholic\Tests\MetapixelTestCase`, `Logingrupa\Metapixelshopaholic\Tests\Support\OrderFixtures`.

    Class: `final class MultiSiteEventLogTest extends MetapixelTestCase`.

    `setUp(): void`:
    - `parent::setUp();`
    - `$this->bootSystemSettings();`
    - `$this->bootOrdersStatuses();`
    - `$this->bootOrdersTable();`
    - `$this->bootEventLogTable();`
    - `OrderFixtures::provisionHermeticOfferProductTables();`
    - `Cache::flush(); Settings::clearInternalCache(); PluginGuard::flush();`

    `tearDown(): void`: `Config::set('system.active_site', null);` (avoid Config bleed between tests).

    3 test methods per PATTERNS.md lines 1214-1256 — exactly the behavior block above. Variables use Hungarian: `$obOrder`, `$bWonA`, `$bWonB`, `$iTime`, `$iCount`. Event_time uses a fixed deterministic literal (e.g. `1715000000`) so tests are time-independent (Tiger-Style determinism).

    Notice: SiteManager class is NOT bound in test environment (PATTERNS.md line 1259 — the SDK reads from Config directly via `HasActiveSite::getActiveSiteId(): Config::get('system.active_site')`). So `Config::set` is sufficient; no `App::instance('system.sites', ...)` swap needed.

    NOTE: if `Config::set('system.active_site', 1)` does NOT reach `SiteResolver::getActiveSiteId()` because `SiteManager::instance()` is gated by the `class_exists` check, the test will need an alternative scoping. Verify in execution. PATTERNS.md line 1259 indicates the SDK uses Config; if SiteManager::instance() throws in test environment due to missing service-provider, the SiteResolver's Throwable catch absorbs and returns null — in which case ALL three tests still pass under the "single-site" branch (and the row independence test reduces to insertOrIgnore with site_id=null → second insert returns false). Executor must:
    1. First attempt the Config::set approach.
    2. If site_id stays NULL despite Config setting, document the gap in SUMMARY and either: (a) bind a minimal SiteManager test double, OR (b) reflection-patch `SiteResolver` via a test-only override hook. Pick whichever is simpler — both are acceptable per Tiger-Style "fail-fast, document explicit reason comments" (the override hook would be reason-tagged with the SDK-resolution drift).
  </action>
  <verify>
    <automated>
      cd plugins/logingrupa/metapixelshopaholic
      &amp;&amp; test -f tests/Feature/MultiSiteEventLogTest.php
      &amp;&amp; grep -q "final class MultiSiteEventLogTest extends MetapixelTestCase" tests/Feature/MultiSiteEventLogTest.php
      &amp;&amp; grep -q "test_two_sites_bind_same_order_id_records_independently" tests/Feature/MultiSiteEventLogTest.php
      &amp;&amp; grep -q "test_single_site_install_writes_null_site_id" tests/Feature/MultiSiteEventLogTest.php
      &amp;&amp; grep -q "test_active_site_scoped_read_excludes_other_sites_rows" tests/Feature/MultiSiteEventLogTest.php
      &amp;&amp; grep -q "Config::set('system.active_site'" tests/Feature/MultiSiteEventLogTest.php
      &amp;&amp; vendor/bin/pest tests/Feature/MultiSiteEventLogTest.php
      &amp;&amp; composer pint-test
      &amp;&amp; composer analyse
    </automated>
  </verify>
  <acceptance_criteria>
    - 3 test methods present and passing.
    - `Config::set('system.active_site', $i)` used for site switching (deterministic per HasActiveSite SDK contract).
    - Test 1 asserts `EventLog::count() === 2` after 2 record() calls with different site_ids.
    - Test 2 asserts row's `site_id` is `null` (NOT 0) when Config is null — phpstan-narrow via `$this->assertNull($obRow->site_id);`.
    - Test 3 asserts `EventLog::where('site_id', SiteResolver::getActiveSiteId())->count() === 1`.
    - No time-dependent assertions (deterministic event_time literals only).
    - `composer pint-test` + `composer analyse` clean.
    - If SDK resolution requires the override hook from the executor note: documented in plan SUMMARY as a Phase-5 README runbook line.
  </acceptance_criteria>
  <done>
    REFAC-11 final test (MultiSiteEventLogTest) locked. BRIEF acceptance criterion 9 ("Multi-site test: same Order id on two `site_id` values → two independent CAPI fires") is now codified.
  </done>
</task>

<task type="auto">
  <name>Task 2: Grep-sweep cleanup — verify no surviving meta_purchase_event_* references + remove any drift</name>
  <files>
    plugins/logingrupa/metapixelshopaholic/ (any file flagged by grep-sweep — likely zero, this is the verification + insurance task)
  </files>
  <read_first>
    plugins/logingrupa/metapixelshopaholic/.planning/phases/03.1-event-log-refactor/BRIEF.md (REFAC-10 lines 234-238 — delete obsolete code),
    plugins/logingrupa/metapixelshopaholic/.planning/phases/03.1-event-log-refactor/03.1-01-SUMMARY.md (Wave 1 closure — what was deleted),
    plugins/logingrupa/metapixelshopaholic/.planning/phases/03.1-event-log-refactor/03.1-03-SUMMARY.md (Wave 3 closure — what was rewritten in queue + watcher),
    plugins/logingrupa/metapixelshopaholic/.planning/phases/03.1-event-log-refactor/03.1-04-SUMMARY.md (Wave 3 closure — what was rewritten in component + Twig).
  </read_first>
  <action>
    Sweep the entire plugin tree for stale references:

    1. From plugin root, run:
       `grep -rn 'meta_purchase_event_id\|meta_purchase_event_time' --include='*.php' --include='*.htm' --include='*.yaml' --include='*.md' .`

    2. EXPECTED matches (acceptable — DO NOT remove):
       - `updates/drop_meta_purchase_columns_from_orders_table.php` — `down()` re-adds the columns for rollback safety.
       - `updates/version.yaml` — 1.0.1 historical changelog line.
       - `.planning/phases/03-purchase-end-to-end/*.md` — historical Phase 3 docs (superseded but preserved per gsd convention).
       - `.planning/phases/03.1-event-log-refactor/*.md` — the BRIEF + PATTERNS + plan files describe the removal contract.
       - `.planning/STATE.md` — Phase 3 historical entries.
       - `.planning/PROJECT.md` — historical Key Decisions row (already strikethrough'd per STATE.md prior wave).

    3. UNEXPECTED matches (remove):
       - Any `.php` file under `classes/`, `components/`, `controllers/`, `middleware/`, `models/` containing the strings. Should be ZERO after Waves 1-3.
       - Any `tests/Feature/*.php` containing the strings (Phase-3 PurchasePixelTest + OrderStatusWatcherTest were already handled in Plan 03.1-03 + 03.1-04, but double-check).
       - Any PHPDoc `@property` line on Order model extension files (Phase 3 may have added one — check `plugins/logingrupa/metapixelshopaholic/Plugin.php` and the storeextender plugin).

    4. For each unexpected match: open the file and remove the line cleanly (delete PHPDoc property declaration, delete docblock reference, etc.). Run `composer pint-test` + `composer analyse` after each file edit.

    5. ALSO sweep for stale imports left over from the rewrite waves:
       - `grep -rn 'meta_purchase' --include='*.php' .` — should match only the drop migration's down() body + nothing else.

    6. Verify version.yaml v1.1.0 entry from Wave-1 Task 2 is still correct (no drift): the `1.1.0:` key lists `drop_meta_purchase_columns_from_orders_table.php` + `create_metapixel_event_log_table.php`. If correct → no edit needed. If drift → fix.

    7. Run final full-suite test: `vendor/bin/pest` + `composer qa`. All green.
  </action>
  <verify>
    <automated>
      cd plugins/logingrupa/metapixelshopaholic
      &amp;&amp; UNEXPECTED=$(grep -rn 'meta_purchase_event_id\|meta_purchase_event_time' --include='*.php' --include='*.htm' --include='*.yaml' . | grep -v 'updates/drop_meta_purchase_columns_from_orders_table.php' | grep -v 'updates/version.yaml')
      &amp;&amp; [ -z "$UNEXPECTED" ]
      &amp;&amp; composer qa
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep` shows ZERO unexpected matches under `--include='*.php' --include='*.htm' --include='*.yaml'` (the down() migration body + the version.yaml historical 1.0.1 line are explicitly excluded via grep -v).
    - The `--include='*.md'` set is NOT subjected to the strict filter — `.planning/**.md` files (BRIEF, PATTERNS, SUMMARYs, STATE, PROJECT, ROADMAP) legitimately reference the removed columns.
    - `composer qa` exits 0 with full plugin coverage report.
    - Full pest suite passes — including all 5 Phase 3.1 test files (EventLogTest, SendCapiEventEventLogTest, OrderStatusWatcherEventLogTest, PurchasePixelEventLogGateTest, MultiSiteEventLogTest) + the updated Phase 3 files (SendCapiEventTest, PurchasePixelTest, FailedEventModelTest, MigrationsBootTest, BootsWithoutPixelIdTest, EnsureFbpFbcCookiesTest, PixelHeadTest, SettingsRegistrationTest).
    - version.yaml 1.1.0 entry matches the Wave-1 schema.
  </acceptance_criteria>
  <done>
    REFAC-10 atomic commit (only if cleanup edits were needed — likely a zero-line commit). The plugin tree contains ZERO production-code references to the legacy columns. Plan SUMMARY records the grep output + the v1.1.0 ledger state.
  </done>
</task>

<task type="auto">
  <name>Task 3: Update STATE.md — close Phase 3.1, advance milestone to v1.1.0, sweep Pending Todos</name>
  <files>
    plugins/logingrupa/metapixelshopaholic/.planning/STATE.md (UPDATE)
  </files>
  <read_first>
    plugins/logingrupa/metapixelshopaholic/.planning/STATE.md (current — frontmatter + Accumulated Context + Pending Todos sections),
    plugins/logingrupa/metapixelshopaholic/.planning/phases/03.1-event-log-refactor/03.1-01-SUMMARY.md,
    plugins/logingrupa/metapixelshopaholic/.planning/phases/03.1-event-log-refactor/03.1-02-SUMMARY.md,
    plugins/logingrupa/metapixelshopaholic/.planning/phases/03.1-event-log-refactor/03.1-03-SUMMARY.md,
    plugins/logingrupa/metapixelshopaholic/.planning/phases/03.1-event-log-refactor/03.1-04-SUMMARY.md.
  </read_first>
  <action>
    Edit `.planning/STATE.md`:

    1. Frontmatter:
       - `milestone: v1.1.0` (was v1.0.0).
       - `status: phase-3.1-complete` (was `phase-3.1-inserted-awaiting-plan`).
       - `stopped_at:` updated with a Phase 3.1 completion line including the 5 plan IDs and a one-line summary of the v1.1.0 break.
       - `last_updated:` ISO date today.
       - `last_activity:` Phase 3.1 close.
       - `progress.completed_phases: 3` (Phase 1, Phase 2, Phase 3.1; Phase 3 stays superseded — record it as `5.5/6 — superseded by 3.1` per existing STATE convention).
       - `progress.total_plans: 11` (1 + 4 + 6 + 5 = 16 BUT some plans superseded; better to add 5 new for 3.1 to existing 10.5 = 15.5 — executor: recompute deterministically from actual ROADMAP and prior STATE).
       - `progress.completed_plans: 15.5` (or whatever the deterministic count is post-wave-4).
       - `progress.percent:` recompute.

    2. Current Position:
       - Phase: 3.1 → completed; next focus: Phase 4 (funnel completion).
       - Plan: 5/5.
       - Status: Phase 3.1 complete. v1.1.0 ledger entry in version.yaml. Schema bedrock + helpers + queue + watcher + component + multi-site test + cleanup all green.

    3. Accumulated Context → Decisions:
       - Re-emphasize the SUPERSEDED column-idempotency entry (already strikethrough — confirm preservation).
       - Add: "Phase 3.1 race-fence atomicity = `EventLog` UNIQUE constraint on `(subject_type, subject_id, event_name, channel, site_id)`. EventLogWriter::record returns bool. Multi-site contract via SiteResolver."

    4. Accumulated Context → Pending Todos:
       - Mark CLOSED any items tied to the removed columns (PAY-10 + PAY-11 column-based staging items if any).
       - Add new closed items for SCE-03 break (constructor signature change), WR-12 deletion (atomic CAS gone), MIG-02 carried forward.
       - Preserve PH-01 (regex validator) as still OPEN — that's Phase 5 / pixel_id stored-XSS surface.
       - Preserve MC-07 (regex validator HIGH-priority) as still OPEN.
       - Add the SDK resolution note from Task 1 if the executor used a test-only override hook.

    5. Add a "Phase 3.1 Forward Notes" subsection capturing:
       - New API surface for Phase 4: `EventLog` model + `EventLogWriter::record` + `SiteResolver::getActiveSiteId`. Phase 4 funnel events (AddToCart, ViewContent, etc.) reuse this writer with different `$sChannel` + `$sEventName` per BRIEF "Phase 4 funnel events designed for it" + PATTERNS.md "Race-fence INSERT IGNORE Pattern" forward-apply note.
       - PAY-10 + PAY-11 manual staging checkpoint deferred from Phase 3 is now Phase 3.1's manual checkpoint — operator deploys v1.1.0 to staging, runs the 4 acceptance scenarios from BRIEF (PayPal CAPI+Pixel pair, bank-transfer admin-flip CAPI-only, status flip-flop never re-fires, refresh/incognito Purchase NEVER re-fires).
       - `system_plugin_versions` row check command for the operator.

    6. Session Continuity:
       - Last activity: Phase 3.1 complete.
       - Last session: today.
       - Stopped at: Phase 3.1 (event-log refactor) closed. v1.1.0 published to plugin repo. Manual staging verification of BRIEF acceptance criteria pending operator action. Next: `/gsd-plan-phase 4` for Funnel Completion phase.
       - Resume file: `.planning/ROADMAP.md`.

    Edit precisely — STATE.md has structure-sensitive markdown (frontmatter YAML + section headers). Use Edit/Write tool. Do NOT rewrite from scratch — surgical edits to existing sections only. Preserve all entries from prior plans that are still relevant.
  </action>
  <verify>
    <automated>
      cd plugins/logingrupa/metapixelshopaholic
      &amp;&amp; grep -q "milestone: v1.1.0" .planning/STATE.md
      &amp;&amp; grep -q "status: phase-3.1-complete" .planning/STATE.md
      &amp;&amp; grep -q "Phase 3.1" .planning/STATE.md
      &amp;&amp; grep -q "EventLogWriter::record" .planning/STATE.md
      &amp;&amp; grep -q "SiteResolver" .planning/STATE.md
      &amp;&amp; grep -q "v1.1.0" .planning/STATE.md
    </automated>
  </verify>
  <acceptance_criteria>
    - STATE.md frontmatter advances `milestone` + `status` + `stopped_at` + `last_updated` + `last_activity` + progress counters.
    - "Current Position" reflects Phase 3.1 closure and Phase 4 as next focus.
    - "Accumulated Context → Decisions" preserves the strikethrough'd column decision AND adds the new EventLog + EventLogWriter + SiteResolver canonical decision.
    - "Pending Todos" sweep — column-related items closed; PH-01 + MC-07 preserved open for Phase 5.
    - "Phase 3.1 Forward Notes" subsection captures the manual staging checkpoint AND the Phase 4 API surface reuse.
    - "Session Continuity" advances to Phase 3.1 closure + next: `/gsd-plan-phase 4`.
    - No accidental deletion of prior phase context.
  </acceptance_criteria>
  <done>
    Phase 3.1 milestone closure recorded. Plugin advances to v1.1.0. Operator has a clear path: run staging verification per BRIEF acceptance criteria, then trigger `/gsd-plan-phase 4`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test environment → Production SDK | Multi-site Config::set in tests must not bleed across test methods. tearDown reset prevents bleed. |
| STATE.md → Operator | The STATE.md "Phase 3.1 Forward Notes" subsection IS the staging-verification handoff. Operator reads it; missing entries = blind handoff. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3.1-25 | Tampering | MultiSiteEventLogTest Config bleed | mitigate | tearDown resets `Config::set('system.active_site', null);` so subsequent test files don't inherit a site binding. |
| T-3.1-26 | Information Disclosure | STATE.md staging-verification handoff | accept | STATE.md is gsd-internal documentation. Contains no secrets; references operator-action items. |
| T-3.1-27 | Repudiation | Phase 3.1 closure record | mitigate | STATE.md `last_updated` timestamp + git commit message records the closure date. ROADMAP.md mirror entry (separate plan or quick edit) confirms phase status. |
| T-3.1-28 | Denial of Service | grep-sweep recursive scan | accept | Single one-shot grep against the plugin tree. No production impact. |
| T-3.1-29 | Elevation | Pending Todos sweep — closing too aggressively | mitigate | Task 3 explicitly preserves PH-01 + MC-07 (Phase 5 launch items). Executor reads existing Pending Todos before editing. |
</threat_model>

<verification>
After all 3 tasks (END OF PHASE 3.1):
1. `composer qa` exits 0.
2. `vendor/bin/pest tests/Feature/` reports all green:
   - Wave 2: EventLogTest (4 methods).
   - Wave 3a: SendCapiEventEventLogTest (3 methods), OrderStatusWatcherEventLogTest (10 methods), SendCapiEventTest (12 methods updated).
   - Wave 3b: PurchasePixelEventLogGateTest (6 methods), PurchasePixelTest (Phase-3 surviving methods).
   - Wave 4: MultiSiteEventLogTest (3 methods).
   - Pre-existing: BootsWithoutPixelIdTest, EnsureFbpFbcCookiesTest, FailedEventModelTest, MigrationsBootTest, PixelHeadTest, SettingsRegistrationTest.
3. Coverage on entire plugin ≥ 90 % (target — recover Phase 3's 89.6 % and improve via SiteResolver + EventLogWriter + EventLog at 100 % from EventLogTest + MultiSiteEventLogTest).
4. `grep -rn 'meta_purchase_event_id\|meta_purchase_event_time' --include='*.php' --include='*.htm' --include='*.yaml' plugins/logingrupa/metapixelshopaholic/ | grep -v drop_meta_purchase_columns | grep -v version.yaml` returns ZERO lines.
5. STATE.md `status: phase-3.1-complete` + `milestone: v1.1.0`.
6. Manual staging verification (NOT part of plan execution — recorded in STATE.md as operator handoff): operator deploys v1.1.0, runs the 4 BRIEF acceptance scenarios.
</verification>

<success_criteria>
- MultiSiteEventLogTest ships with 3 invariants locking the multi-site contract.
- No production-code reference to dropped columns survives the sweep.
- STATE.md advances to Phase 3.1 closure + v1.1.0 milestone.
- All 5 BRIEF acceptance criteria 1-3 + 5-9 demonstrably met by the suite (criteria 4 is the operator manual staging check).
- `composer qa` green.
</success_criteria>

<output>
After completion, create `.planning/phases/03.1-event-log-refactor/03.1-05-SUMMARY.md` AND a phase-level `.planning/phases/03.1-event-log-refactor/PHASE-SUMMARY.md` (or update an existing one) with:

03.1-05-SUMMARY.md:
- Multi-site invariants codified.
- Grep-sweep result block-quoted.
- STATE.md delta.
- Final coverage report (full plugin %) compared to Phase 3 baseline.

PHASE-SUMMARY.md (Phase 3.1 roll-up):
- 5 plans × atomic commit count = total commit count.
- Files added / deleted / rewritten with LOC delta.
- API surface for Phase 4 (EventLog + EventLogWriter + SiteResolver — block-quote the signatures).
- v1.1.0 breaking changes section (SCE-03 constructor signature; WR-12 atomic-CAS gone; column reads gone).
- Manual staging checkpoint handoff — exact scenarios, exact commands to verify.
- Recommended next step: `/gsd-plan-phase 4` for Funnel Completion.
</output>
