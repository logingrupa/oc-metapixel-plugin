---
phase: 03.1-08-dead-code-cleanup
plan: 04
subsystem: planning-docs + composer-manifest
tags:
  - cleanup
  - planning-docs
  - composer
  - phase-3.1
requirements:
  - CLEAN-04
dependency_graph:
  requires: []
  provides:
    - "SUPERSEDED annotation on .planning/PLAN.md + PLAN-v2-original.md (pre-3.1 design now flagged at read-time)"
    - "Schema-clean composer.json (composer validate --strict exits 0)"
    - "updates/ directory free of obsolete .gitkeep placeholder"
    - "FOLLOWUP.md deferred-item register for phase 03.1-08 (Constraint #1 compliance)"
  affects:
    - ".planning/PLAN.md (annotated header — body unchanged)"
    - ".planning/PLAN-v2-original.md (annotated header — body unchanged)"
    - "updates/.gitkeep (deleted)"
    - "composer.json (`_comments` key removed)"
    - ".planning/phases/03.1-08-dead-code-cleanup/FOLLOWUP.md (created)"
tech_stack:
  added: []
  patterns:
    - "SUPERSEDED blockquote header pattern (BRIEF T4.1 option B) for retiring stale design docs"
    - "Atomic-commit-per-concern (SRP) per BRIEF Constraint #3"
    - "Deferred-out-of-scope sink via FOLLOWUP.md per BRIEF Constraint #1 (no drive-by README edit)"
key_files:
  created:
    - ".planning/phases/03.1-08-dead-code-cleanup/FOLLOWUP.md"
  modified:
    - ".planning/PLAN.md"
    - ".planning/PLAN-v2-original.md"
    - "composer.json"
  deleted:
    - "updates/.gitkeep"
decisions:
  - "Picked BRIEF T4.1 option (B) annotate (not delete/archive) — preserves audit trail, signals staleness at read-time"
  - "Picked BRIEF T4.3 default option (DELETE `_comments` block) — PHP 8.4 constraint rationale already lives in plugin CLAUDE.md Tech Stack section; no COMPOSER.md companion docblock needed"
  - "README.md update deferred to FOLLOWUP.md per BRIEF Constraint #1 (no drive-by refactors). FOLLOWUP entry co-committed with PLAN.md/PLAN-v2-original.md annotation per BRIEF Constraint #3 (same logical concern)"
metrics:
  duration_seconds: ~600
  completed_date: "2026-05-14"
  tasks_completed: 3
  files_touched: 5
  followup_entries_created: 1
---

# Phase 03.1-08 Plan 04: Planning-Doc Cleanup + Composer Schema Clean — Summary

Annotated two stale top-level planning docs (`PLAN.md` + `PLAN-v2-original.md`) as SUPERSEDED, deleted redundant `updates/.gitkeep` placeholder, and stripped the non-standard `_comments` key from `composer.json`. Three atomic `03.1-cleanup`-scoped commits. Zero production-code, zero test-file, zero `.planning/README.md` touches. `composer validate --strict` now exits 0.

## Commits (in order)

| # | SHA       | Type   | Subject                                                                       |
|---|-----------|--------|-------------------------------------------------------------------------------|
| 1 | `4e62b45` | docs   | annotate PLAN.md + PLAN-v2-original.md as superseded by phase 03.1            |
| 2 | `47aca55` | chore  | remove updates/.gitkeep — directory non-empty                                 |
| 3 | `1cc53df` | chore  | composer.json remove non-standard _comments key                               |

## composer validate --strict — before / after Task 3

**Before (HEAD~1 = `47aca55`):**

```
./composer.json is valid for simple usage with Composer but has
strict errors that make it unable to be published as a package
# Publish errors
- The property _comments is not defined and the definition does not allow additional properties
EXIT: 2
```

**After (HEAD = `1cc53df`):**

```
./composer.json is valid
EXIT: 0
```

## Files touched

| File                                                       | Op       | Notes                                                                 |
|------------------------------------------------------------|----------|-----------------------------------------------------------------------|
| `.planning/PLAN.md`                                        | modified | +2 lines (SUPERSEDED blockquote + blank line) — body byte-identical   |
| `.planning/PLAN-v2-original.md`                            | modified | +2 lines (SUPERSEDED blockquote + blank line) — body byte-identical   |
| `.planning/phases/03.1-08-dead-code-cleanup/FOLLOWUP.md`   | created  | 5 lines — header + 1 README-defer entry                               |
| `updates/.gitkeep`                                         | deleted  | Placeholder no longer needed (dir has version.yaml + 2 migrations)    |
| `composer.json`                                            | modified | -3 lines (`_comments` block removed); zero other keys touched         |

**Total files touched: 5** (planned: 4 minimum + 1 if FOLLOWUP branch fires; README-references-PLAN audit triggered the branch — `.planning/README.md` has 5 PLAN.md/PLAN-v2-original.md references).

## FOLLOWUP.md created? Y/N + entry count

**Created: Y** (1 entry).

Entry: `- 2026-05-14 plan-04.task-1: .planning/README.md references now-SUPERSEDED PLAN.md — update README to point at .planning/phases/03.1-event-log-refactor/BRIEF.md`

## Deviations from Plan

None — plan executed exactly as written. Both planned conditional branches resolved deterministically against repo state:

- Task 1 README-references-PLAN.md branch: **fired** (`.planning/README.md` line 25 + table rows reference PLAN.md as primary source). FOLLOWUP.md created with the prescribed one-line entry; co-committed with the PLAN.md annotation per Constraint #3.
- Task 3 COMPOSER.md companion docblock branch: **did NOT fire**. The `_comments` content was a single `php-constraint` rationale already documented in plugin CLAUDE.md Tech Stack section (`PHP 8.4.18 (NTS) + Zend OPcache v8.4.18 on PHP-FPM`). No information loss from deletion.

## Acceptance Criteria

| Criterion | Status |
|---|---|
| `head -5 .planning/PLAN.md \| grep -q "SUPERSEDED 2026-05-13"` exit 0 | PASS |
| `head -5 .planning/PLAN-v2-original.md \| grep -q "SUPERSEDED 2026-05-13"` exit 0 | PASS |
| Both files link to `phases/03.1-event-log-refactor/BRIEF.md` | PASS |
| Body content of both PLAN docs unchanged below prepended block | PASS (git diff shows only `+` additions in headers) |
| `.planning/README.md` untouched | PASS (`git diff --name-only HEAD~3 HEAD -- .planning/README.md` = 0 lines) |
| `updates/.gitkeep` gone | PASS |
| `updates/version.yaml` + ≥ 2 migration `.php` files still present | PASS (`create_metapixel_event_log_table.php`, `create_table_failed_events.php`) |
| `! grep -q "_comments" composer.json` | PASS |
| `composer validate --strict` exits 0 | PASS |
| `composer validate --strict` zero WARNING/ERROR | PASS |
| `composer.json` JSON still valid (`python3 -c "import json; json.load(...)"`) | PASS |
| Only `_comments` key removed in `composer.json` diff (no other key altered) | PASS |
| 3 atomic commits with `03.1-cleanup` scope | PASS |
| Zero production-code, zero test-file touches | PASS (`grep -E '^(classes\|components\|controllers\|middleware\|models\|Plugin\.php\|plugin\.yaml\|tests/)/' HEAD~3..HEAD = 0 hits`) |

## Threat Flags

None — both threats in the plan's threat register were `accept` dispositions (T-03.1-08-08 schema drift hygiene + T-03.1-08-09 stale planning narrative). No new security-relevant surface introduced.

## Self-Check: PASSED

- `.planning/PLAN.md` — present, head-5 contains `SUPERSEDED 2026-05-13`: FOUND
- `.planning/PLAN-v2-original.md` — present, head-5 contains `SUPERSEDED 2026-05-13`: FOUND
- `.planning/phases/03.1-08-dead-code-cleanup/FOLLOWUP.md` — present, contains README-defer entry: FOUND
- `updates/.gitkeep` — gone (intentional): MISSING (expected)
- `composer.json` — present, no `_comments` key, JSON valid, validates strict: FOUND
- Commit `4e62b45` — `git log --oneline --all | grep 4e62b45`: FOUND
- Commit `47aca55` — FOUND
- Commit `1cc53df` — FOUND
