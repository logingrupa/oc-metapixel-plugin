---
phase: 03.1-08-dead-code-cleanup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - plugins/logingrupa/metapixelshopaholic/tests/Feature/MultiSiteCrossContextTest.php
  - plugins/logingrupa/metapixelshopaholic/tests/Feature/SendCapiEventEventLogTest.php
  - plugins/logingrupa/metapixelshopaholic/tests/Feature/PurchaseEndToEndIntegrationTest.php
  - plugins/logingrupa/metapixelshopaholic/tests/Feature/OrderStatusWatcherEventLogTest.php
  - plugins/logingrupa/metapixelshopaholic/tests/Feature/SendCapiEventTest.php
autonomous: true
requirements:
  - CLEAN-02
tags:
  - cleanup
  - tests
  - dry
  - lint
  - phase-3.1
must_haves:
  truths:
    - "tests/Feature/MultiSiteCrossContextTest.php imports `use Ramsey\\Uuid\\Uuid;` and has zero inline `\\Ramsey\\Uuid\\Uuid::` FQNs"
    - "tests/Feature/SendCapiEventEventLogTest.php line ~237 + tests/Feature/PurchaseEndToEndIntegrationTest.php line ~340 use `Uuid::uuid4()->toString()` (no hardcoded hex literals as event_id)"
    - "tests/Feature/PurchaseEndToEndIntegrationTest.php lines ~482-487 stale `$casts int` rationale comment removed (or replaced with one-line caveman reference to 03.1-07 REFAC-12)"
    - "tests/Feature/OrderStatusWatcherEventLogTest.php opens with `declare(strict_types=1);` after `<?php`"
    - "tests/Feature/SendCapiEventTest.php setUp() docstring no longer mentions WR-07 layered migration (reflects inlined unique idx post-08a0d12)"
  artifacts:
    - path: "plugins/logingrupa/metapixelshopaholic/tests/Feature/MultiSiteCrossContextTest.php"
      provides: "Uuid import in alphabetical use block; 3 FQN→short replacements"
      contains: "use Ramsey\\Uuid\\Uuid;"
    - path: "plugins/logingrupa/metapixelshopaholic/tests/Feature/SendCapiEventEventLogTest.php"
      provides: "Uuid::uuid4()->toString() event_id literal"
      contains: "Uuid::uuid4()"
    - path: "plugins/logingrupa/metapixelshopaholic/tests/Feature/PurchaseEndToEndIntegrationTest.php"
      provides: "Uuid::uuid4()->toString() event_id literal + stale comment removed"
      contains: "Uuid::uuid4()"
    - path: "plugins/logingrupa/metapixelshopaholic/tests/Feature/OrderStatusWatcherEventLogTest.php"
      provides: "declare(strict_types=1) header"
      contains: "declare(strict_types=1)"
    - path: "plugins/logingrupa/metapixelshopaholic/tests/Feature/SendCapiEventTest.php"
      provides: "Caveman-compressed setUp docstring synced to post-merge migration shape"
      contains: "setUp"
  key_links:
    - from: "tests/Feature/MultiSiteCrossContextTest.php Uuid import"
      to: "Ramsey UUID library (DRY pattern shared across plugin test suite)"
      via: "use statement in alphabetical imports block"
      pattern: "^use Ramsey\\\\Uuid\\\\Uuid;"
    - from: "tests/Feature/SendCapiEventEventLogTest.php :237 event_id"
      to: "PayloadBuilder UUIDv4 validation contract"
      via: "Uuid::uuid4()->toString() captured into $sEventId"
      pattern: "Uuid::uuid4\\(\\)->toString\\(\\)"
---

<objective>
Track T2 — test-suite DRY + lint cleanup. Six atomic commits (one per BRIEF track T2.1..T2.5, with T2.2 producing two commits — one per file). Resolve phase 03.1-07 REVIEW.md findings LOW-01 (inline FQN Uuid → `use` import), LOW-02 + LOW-03 (hardcoded non-UUIDv4 hex `event_id` literals replaced with `Uuid::uuid4()->toString()`), LOW-04 (stale `$casts int` rationale comment), LOW-05 (missing `declare(strict_types=1)`), and BRIEF T2.5 (SendCapiEventTest setUp docstring sync post-08a0d12 unique-idx merge).

Purpose: stale comments + non-UUIDv4 literals + inline FQNs trip future contributors. Tests work today because they bypass `PayloadBuilder::build()` validation, but Phase 4 funnel tests will share the same DRY UUID pattern — fix the precedent now. Each task = one atomic commit (Tiger-Style SRP, BRIEF Constraint #3).

Output: 6 commits with `03.1-cleanup` scope on 5 test files. **Zero production-code touches** (BRIEF Constraint #5 — Track 2 is test-only).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@plugins/logingrupa/metapixelshopaholic/CLAUDE.md
@plugins/logingrupa/metapixelshopaholic/.planning/STATE.md
@plugins/logingrupa/metapixelshopaholic/.planning/phases/03.1-08-dead-code-cleanup/BRIEF.md
@plugins/logingrupa/metapixelshopaholic/.planning/phases/03.1-07-multi-site-site-id-symmetry/03.1-07-REVIEW.md

<interfaces>
<!-- DRY reference pattern from MultiSiteCrossContextTest (already-fixed example): -->
```
use Ramsey\Uuid\Uuid;
// ...
$sEventId = Uuid::uuid4()->toString();
EventLog::create(['event_id' => $sEventId, /* ... */]);
$obResponse->assertJson(['event_id' => $sEventId]);
```
This is the pattern to mirror in T2.2 for SendCapiEventEventLogTest:237 + PurchaseEndToEndIntegrationTest:340.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1 (T2.1): LOW-01 — MultiSiteCrossContextTest replace 3 inline FQN Uuid with use import (DRY)</name>
  <files>plugins/logingrupa/metapixelshopaholic/tests/Feature/MultiSiteCrossContextTest.php</files>
  <read_first>
    - plugins/logingrupa/metapixelshopaholic/tests/Feature/MultiSiteCrossContextTest.php (read ENTIRE file once — locate use-block (likely after namespace) and every `\Ramsey\Uuid\Uuid::` call site in single pass)
    - plugins/logingrupa/metapixelshopaholic/.planning/phases/03.1-08-dead-code-cleanup/BRIEF.md (T2.1 spec lines 91-99)
    - plugins/logingrupa/metapixelshopaholic/.planning/phases/03.1-07-multi-site-site-id-symmetry/03.1-07-REVIEW.md (LOW-01 finding)
  </read_first>
  <action>
    Add `use Ramsey\Uuid\Uuid;` to the use-imports block in alphabetical order (Ramsey sorts after Lovata, Logingrupa, Mockery; before Tests). Replace all 3 inline `\Ramsey\Uuid\Uuid::uuid4()->toString()` occurrences with `Uuid::uuid4()->toString()`. Use `grep -n '\\\\Ramsey\\\\Uuid\\\\Uuid::' tests/Feature/MultiSiteCrossContextTest.php` to confirm 3 hits found, then replace each at the printed line numbers. Method bodies remain semantically identical.
  </action>
  <verify>
    <automated>cd plugins/logingrupa/metapixelshopaholic && grep -q "^use Ramsey\\\\Uuid\\\\Uuid;" tests/Feature/MultiSiteCrossContextTest.php && ! grep -q "\\\\Ramsey\\\\Uuid\\\\Uuid::" tests/Feature/MultiSiteCrossContextTest.php && composer test -- --filter MultiSiteCrossContextTest 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "^use Ramsey\\\\Uuid\\\\Uuid;" plugins/logingrupa/metapixelshopaholic/tests/Feature/MultiSiteCrossContextTest.php` exit 0 (import present, top-level)
    - `grep -c "\\\\Ramsey\\\\Uuid\\\\Uuid::" plugins/logingrupa/metapixelshopaholic/tests/Feature/MultiSiteCrossContextTest.php` returns 0 (no inline FQN remains)
    - `grep -c "Uuid::uuid4\\(\\)->toString\\(\\)" plugins/logingrupa/metapixelshopaholic/tests/Feature/MultiSiteCrossContextTest.php` returns ≥ 3 (the 3 replacements present)
    - `composer test -- --filter MultiSiteCrossContextTest` exits 0 (3 methods still green — REVIEW.md confirms this test is currently passing)
    - Use-block alphabetically sorted — `awk '/^use /{print}' tests/Feature/MultiSiteCrossContextTest.php` output is identical to `sort` of itself (verifiable manually)
  </acceptance_criteria>
  <commit_message>refactor(03.1-cleanup): MultiSiteCrossContextTest use Ramsey Uuid import (DRY)</commit_message>
  <done>
    Uuid import in alphabetical use-block, 3 FQN call sites replaced with short form, MultiSiteCrossContextTest exits 0 (3 methods green). Atomic commit landed.
  </done>
</task>

<task type="auto">
  <name>Task 2 (T2.2 part 1): LOW-02 — SendCapiEventEventLogTest replace hex literal event_id with Uuid::uuid4()</name>
  <files>plugins/logingrupa/metapixelshopaholic/tests/Feature/SendCapiEventEventLogTest.php</files>
  <read_first>
    - plugins/logingrupa/metapixelshopaholic/tests/Feature/SendCapiEventEventLogTest.php (read ENTIRE file once — extract: line ~237 hex literal context, use-block state, every event_id usage downstream of the literal in the same test method, all in one pass)
    - plugins/logingrupa/metapixelshopaholic/tests/Feature/MultiSiteCrossContextTest.php (DRY reference — read once to internalize the `$sEventId = Uuid::uuid4()->toString();` capture-then-use pattern)
    - plugins/logingrupa/metapixelshopaholic/.planning/phases/03.1-08-dead-code-cleanup/BRIEF.md (T2.2 spec lines 101-118 — capture into $sEventId local before first use)
    - plugins/logingrupa/metapixelshopaholic/.planning/phases/03.1-07-multi-site-site-id-symmetry/03.1-07-REVIEW.md (LOW-02 finding)
  </read_first>
  <action>
    Add `use Ramsey\Uuid\Uuid;` to use-block in alphabetical order if not present (audit first via `grep -q "^use Ramsey\\\\Uuid\\\\Uuid;" tests/Feature/SendCapiEventEventLogTest.php`). Locate the hex literal near line 237 — the form is `'22222222-2222-2222-2222-222222222222'` or a 32-char hex string used as event_id. Replace with a `$sEventId = Uuid::uuid4()->toString();` local capture inserted BEFORE first use in the test method, then replace the literal with `$sEventId` at the original call site AND any downstream assertions that compared against the literal (e.g., `$obResponse->assertJson(['event_id' => $sEventId])`). Mirror MultiSiteCrossContextTest pattern exactly.
  </action>
  <verify>
    <automated>cd plugins/logingrupa/metapixelshopaholic && grep -q "^use Ramsey\\\\Uuid\\\\Uuid;" tests/Feature/SendCapiEventEventLogTest.php && ! grep -qE "['\"]22222222-2222-2222-2222-222222222222['\"]" tests/Feature/SendCapiEventEventLogTest.php && grep -q "Uuid::uuid4()->toString()" tests/Feature/SendCapiEventEventLogTest.php && composer test -- --filter SendCapiEventEventLogTest 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "^use Ramsey\\\\Uuid\\\\Uuid;" plugins/logingrupa/metapixelshopaholic/tests/Feature/SendCapiEventEventLogTest.php` exit 0
    - `grep -cE "'[a-f0-9]{8}-[a-f0-9]{4}-[a-f0-9]{4}-[a-f0-9]{4}-[a-f0-9]{12}'" plugins/logingrupa/metapixelshopaholic/tests/Feature/SendCapiEventEventLogTest.php` returns 0 in the test method block at the prior literal location (no hardcoded UUID-shaped string used as event_id — comments referencing UUIDs OK only in PHPDoc tags which we did not edit)
    - `grep -q "Uuid::uuid4()->toString()" plugins/logingrupa/metapixelshopaholic/tests/Feature/SendCapiEventEventLogTest.php` exit 0
    - `composer test -- --filter "SendCapiEventEventLogTest"` failure count ≤ baseline (T3.6 race-fence failure remains a T3 concern; the test method this task edits must NOT be the one that newly fails because of this edit)
  </acceptance_criteria>
  <commit_message>test(03.1-cleanup): SendCapiEventEventLogTest replace hex literal with Uuid::uuid4()</commit_message>
  <done>
    Uuid import added (or confirmed present); hex literal replaced with `$sEventId = Uuid::uuid4()->toString();` local capture; downstream assertions reference `$sEventId`. Filter-test failure delta = 0. Atomic commit landed.
  </done>
</task>

<task type="auto">
  <name>Task 3 (T2.2 part 2): LOW-03 — PurchaseEndToEndIntegrationTest replace hex literal event_id with Uuid::uuid4()</name>
  <files>plugins/logingrupa/metapixelshopaholic/tests/Feature/PurchaseEndToEndIntegrationTest.php</files>
  <read_first>
    - plugins/logingrupa/metapixelshopaholic/tests/Feature/PurchaseEndToEndIntegrationTest.php (read ENTIRE file once — extract: line ~340 hex literal, use-block, every downstream event_id assertion in the same test method, also flag the line ~482-487 region for T2.3 next task, all in one pass)
    - plugins/logingrupa/metapixelshopaholic/.planning/phases/03.1-08-dead-code-cleanup/BRIEF.md (T2.2 spec lines 101-118)
    - plugins/logingrupa/metapixelshopaholic/.planning/phases/03.1-07-multi-site-site-id-symmetry/03.1-07-REVIEW.md (LOW-03 finding)
  </read_first>
  <action>
    Same pattern as Task 2 applied to `tests/Feature/PurchaseEndToEndIntegrationTest.php`. Add `use Ramsey\Uuid\Uuid;` if not present (alphabetical). Locate hex literal near line 340. Insert `$sEventId = Uuid::uuid4()->toString();` capture before first use. Replace literal at call site + every downstream assertion within the same test method (`assertJson`, `assertDatabaseHas`, etc.). Do NOT touch line 482-487 region in this task — that is T2.3.
  </action>
  <verify>
    <automated>cd plugins/logingrupa/metapixelshopaholic && grep -q "^use Ramsey\\\\Uuid\\\\Uuid;" tests/Feature/PurchaseEndToEndIntegrationTest.php && grep -q "Uuid::uuid4()->toString()" tests/Feature/PurchaseEndToEndIntegrationTest.php && composer test -- --filter PurchaseEndToEndIntegrationTest 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "^use Ramsey\\\\Uuid\\\\Uuid;" plugins/logingrupa/metapixelshopaholic/tests/Feature/PurchaseEndToEndIntegrationTest.php` exit 0
    - Hex UUID-shaped literal at line ~340 removed (verifiable by `git diff` showing replacement); `grep -cE "'[a-f0-9]{8}-[a-f0-9]{4}-[a-f0-9]{4}-[a-f0-9]{4}-[a-f0-9]{12}'" tests/Feature/PurchaseEndToEndIntegrationTest.php` returns lower count than pre-edit (or 0)
    - `grep -q "Uuid::uuid4()->toString()" plugins/logingrupa/metapixelshopaholic/tests/Feature/PurchaseEndToEndIntegrationTest.php` exit 0
    - `composer test -- --filter PurchaseEndToEndIntegrationTest` exits 0 (this test is currently green per 03.1-06 SUMMARY — 5 methods PASS)
  </acceptance_criteria>
  <commit_message>test(03.1-cleanup): PurchaseEndToEndIntegrationTest replace hex literal with Uuid::uuid4()</commit_message>
  <done>
    Uuid import added; hex literal replaced with `$sEventId` local capture; downstream assertions reference local. PurchaseEndToEndIntegrationTest exits 0. Atomic commit landed.
  </done>
</task>

<task type="auto">
  <name>Task 4 (T2.3): LOW-04 — PurchaseEndToEndIntegrationTest remove stale $casts int rationale comment</name>
  <files>plugins/logingrupa/metapixelshopaholic/tests/Feature/PurchaseEndToEndIntegrationTest.php</files>
  <read_first>
    - plugins/logingrupa/metapixelshopaholic/tests/Feature/PurchaseEndToEndIntegrationTest.php (already read in Task 3 — re-locate lines 482-487 stale comment region; if Task 3 shifted line numbers, find via `grep -nE 'casts.*int|coerces null' tests/Feature/PurchaseEndToEndIntegrationTest.php`)
    - plugins/logingrupa/metapixelshopaholic/.planning/phases/03.1-08-dead-code-cleanup/BRIEF.md (T2.3 spec lines 120-128 — prefer DELETE comment over annotation)
    - plugins/logingrupa/metapixelshopaholic/.planning/phases/03.1-07-multi-site-site-id-symmetry/03.1-07-REVIEW.md (LOW-04 — comment claims `$casts int` coerces null → 0 but REFAC-12 fixed that path)
  </read_first>
  <action>
    Locate the comment block at (now-shifted-by-Task-3) line ~482-487 that narrates `$casts int` coercion of null → 0. Per BRIEF preference: DELETE the comment entirely (the code is self-explanatory post-REFAC-12). If deleting leaves a confusing bare expression, replace with a one-line caveman comment: `// site_id null preserved through cast — REFAC-12 (03.1-07)`. No code-line changes — comment-only edit. Use `git diff tests/Feature/PurchaseEndToEndIntegrationTest.php` post-edit to confirm only comment lines changed.
  </action>
  <verify>
    <automated>cd plugins/logingrupa/metapixelshopaholic && ! grep -qE "casts.*int.*coerces null" tests/Feature/PurchaseEndToEndIntegrationTest.php && composer test -- --filter PurchaseEndToEndIntegrationTest 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `! grep -qE "casts.*int.*coerces null|coerces null.*0" plugins/logingrupa/metapixelshopaholic/tests/Feature/PurchaseEndToEndIntegrationTest.php` (stale wording gone)
    - `git diff HEAD~1 -- plugins/logingrupa/metapixelshopaholic/tests/Feature/PurchaseEndToEndIntegrationTest.php | grep -cE '^\\+[^+]' | head -1` shows ≤ 1 net-added line (delete or one-liner replacement)
    - `git diff HEAD~1 -- plugins/logingrupa/metapixelshopaholic/tests/Feature/PurchaseEndToEndIntegrationTest.php` shows only comment-line changes (no code expression edits — `awk` confirms changed lines all start with `//` or `*` or `/*` or are whitespace)
    - `composer test -- --filter PurchaseEndToEndIntegrationTest` exits 0
  </acceptance_criteria>
  <commit_message>test(03.1-cleanup): PurchaseEndToEndIntegrationTest remove stale $casts rationale comment</commit_message>
  <done>
    Stale comment removed (or replaced with one-line caveman REFAC-12 reference). Only comment lines diffed. Test green. Atomic commit landed.
  </done>
</task>

<task type="auto">
  <name>Task 5 (T2.4): LOW-05 — OrderStatusWatcherEventLogTest add declare(strict_types=1)</name>
  <files>plugins/logingrupa/metapixelshopaholic/tests/Feature/OrderStatusWatcherEventLogTest.php</files>
  <read_first>
    - plugins/logingrupa/metapixelshopaholic/tests/Feature/OrderStatusWatcherEventLogTest.php (read header — lines 1-15 — confirm `declare(strict_types=1)` is absent and identify exact insertion point after `<?php`)
    - plugins/logingrupa/metapixelshopaholic/.planning/phases/03.1-08-dead-code-cleanup/BRIEF.md (T2.4 spec lines 130-134)
    - plugins/logingrupa/metapixelshopaholic/.planning/phases/03.1-07-multi-site-site-id-symmetry/03.1-07-REVIEW.md (LOW-05 finding)
  </read_first>
  <action>
    In `tests/Feature/OrderStatusWatcherEventLogTest.php`, insert `declare(strict_types=1);` immediately after the opening `<?php` line, followed by ONE blank line, then the existing `namespace` (or existing first non-blank line). No other lines touched. Run `composer test -- --filter OrderStatusWatcherEventLogTest` and confirm exit 0 (no regression). Tiger-Style SRP: single-file, single-concern, single atomic commit.
  </action>
  <verify>
    <automated>cd plugins/logingrupa/metapixelshopaholic && head -3 tests/Feature/OrderStatusWatcherEventLogTest.php | grep -q "declare(strict_types=1);" && composer test -- --filter OrderStatusWatcherEventLogTest 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `head -3 plugins/logingrupa/metapixelshopaholic/tests/Feature/OrderStatusWatcherEventLogTest.php | grep -q "declare(strict_types=1);"` exit 0 (declare on line 2 or 3, immediately after `<?php`)
    - `composer test -- --filter OrderStatusWatcherEventLogTest` exits 0 (test was green per 03.1-07 SUMMARY — no regression)
    - `git diff HEAD~1 -- plugins/logingrupa/metapixelshopaholic/tests/Feature/OrderStatusWatcherEventLogTest.php | grep -cE '^\\+[^+]'` returns ≤ 2 (declare line + blank line)
    - `git diff HEAD~1 -- plugins/logingrupa/metapixelshopaholic/tests/Feature/OrderStatusWatcherEventLogTest.php | grep -cE '^-[^-]'` returns 0 (no deletions — pure prepend)
  </acceptance_criteria>
  <commit_message>test(03.1-cleanup): OrderStatusWatcherEventLogTest add declare(strict_types=1)</commit_message>
  <done>
    `declare(strict_types=1);` present at line 2 of OrderStatusWatcherEventLogTest.php. Filter-test green. Atomic commit landed.
  </done>
</task>

<task type="auto">
  <name>Task 6 (T2.5): SendCapiEventTest setUp comment — reflect unique idx inline post-08a0d12</name>
  <files>plugins/logingrupa/metapixelshopaholic/tests/Feature/SendCapiEventTest.php</files>
  <read_first>
    - plugins/logingrupa/metapixelshopaholic/tests/Feature/SendCapiEventTest.php (read setUp() docstring — find via `grep -n "WR-07\\|layered migration\\|AddUniqueIndexToFailedEvents" tests/Feature/SendCapiEventTest.php`; read the full setUp method block once)
    - plugins/logingrupa/metapixelshopaholic/.planning/phases/03.1-08-dead-code-cleanup/BRIEF.md (T2.5 spec lines 136-140)
    - commit 08a0d12 (`git show 08a0d12 --stat`) — confirm the unique idx was inlined into the create-table migration; the layered `AddUniqueIndexToFailedEvents` migration was removed
  </read_first>
  <action>
    In `tests/Feature/SendCapiEventTest.php`, locate the `setUp()` method docstring or inline comment that mentions `WR-07 layered migration` (or `AddUniqueIndexToFailedEvents` if that's the comment phrasing). Per migration-merge commit 08a0d12, the unique idx is now inline in the create-table migration. Rewrite the comment as a single caveman-compressed line such as `// unique idx inline in create_table_failed_events (commit 08a0d12 merged WR-07 layered)`. Remove the obsolete `(new AddUniqueIndexToFailedEvents)->up()` setUp narration from the comment region. Actual code-level setUp body remains unchanged unless it referenced a now-non-existent migration class. Run `composer test -- --filter SendCapiEventTest` and confirm exit 0 (no regression). Tiger-Style SRP: single-file, single-concern, single atomic commit.
  </action>
  <verify>
    <automated>cd plugins/logingrupa/metapixelshopaholic && ! grep -qE "WR-07 layered" tests/Feature/SendCapiEventTest.php && composer test -- --filter SendCapiEventTest 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `! grep -qE "WR-07 layered migration" plugins/logingrupa/metapixelshopaholic/tests/Feature/SendCapiEventTest.php` exit 0 (stale narration removed)
    - `! grep -qE "AddUniqueIndexToFailedEvents\\)->up\\(\\)" plugins/logingrupa/metapixelshopaholic/tests/Feature/SendCapiEventTest.php` exit 0 (obsolete setUp call narration removed from comment region — actual code-level setUp must remain unchanged unless it referenced a now-non-existent migration class)
    - `composer test -- --filter SendCapiEventTest` exits 0 (test was green per 03.1-07 SUMMARY — no regression)
    - `git diff HEAD~1 -- plugins/logingrupa/metapixelshopaholic/tests/Feature/SendCapiEventTest.php` shows only comment-line changes (no code expression edits — `awk` confirms changed lines all start with `//` or `*` or `/*` or are whitespace)
  </acceptance_criteria>
  <commit_message>test(03.1-cleanup): SendCapiEventTest setUp comment — reflect unique idx inline</commit_message>
  <done>
    SendCapiEventTest setUp comment reflects merged-unique-idx state (no WR-07 layered references, no obsolete `AddUniqueIndexToFailedEvents` call narration). Filter-test green. Atomic commit landed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test file → developer reading code | Stale comments + non-UUIDv4 literals mislead future contributors. Not a security boundary — documentation-quality only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03.1-08-03 | Information disclosure | Hex literal in test | accept | Hardcoded test fixture, not PII or secret. Replacement with `Uuid::uuid4()` is hygiene, not security. |
| T-03.1-08-04 | Tampering | Stale comment misleads contributor | accept | Doc-quality issue. Mitigation = remove the misleading line. |
</threat_model>

<verification>
After all 6 commits land:
- `cd plugins/logingrupa/metapixelshopaholic && composer test 2>&1 | tail -5` — failure count must NOT exceed baseline of 6.
- `git log --oneline | head -7` shows 6 commits with `03.1-cleanup` scope:
  1. `refactor(03.1-cleanup): MultiSiteCrossContextTest use Ramsey Uuid import (DRY)`
  2. `test(03.1-cleanup): SendCapiEventEventLogTest replace hex literal with Uuid::uuid4()`
  3. `test(03.1-cleanup): PurchaseEndToEndIntegrationTest replace hex literal with Uuid::uuid4()`
  4. `test(03.1-cleanup): PurchaseEndToEndIntegrationTest remove stale $casts rationale comment`
  5. `test(03.1-cleanup): OrderStatusWatcherEventLogTest add declare(strict_types=1)`
  6. `test(03.1-cleanup): SendCapiEventTest setUp comment — reflect unique idx inline`
- `git diff --stat HEAD~6 HEAD` confirms only 5 test files touched (NO production-code modification — BRIEF Constraint #5).
</verification>

<success_criteria>
- Track T2 (CLEAN-02) closed: 6 atomic commits with `03.1-cleanup` scope, messages match BRIEF prescriptions.
- Roadmap success criteria 7, 8, 9 satisfied (Uuid import + hex literals replaced; strict_types declared; setUp comment synced).
- Zero new test failures.
- Zero production-code touches.
- Each task = single-file edit + single atomic commit (Tiger-Style SRP + BRIEF Constraint #3).
</success_criteria>

<output>
After completion, create `.planning/phases/03.1-08-dead-code-cleanup/03.1-08-02-SUMMARY.md` documenting:
- 6 commit SHAs in order
- Diff statistics (5 files touched, 0 production files)
- Test failure count: baseline preserved (≤ 6)
</output>
