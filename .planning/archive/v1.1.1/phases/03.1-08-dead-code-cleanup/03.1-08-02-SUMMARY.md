---
phase: 03.1-08-dead-code-cleanup
plan: 02
subsystem: test-suite
tags: [cleanup, tests, dry, lint, phase-3.1]
requires: [03.1-07-multi-site-site-id-symmetry]
provides: ["DRY Uuid import pattern across 5 feature tests"]
affects: [tests/Feature]
tech-stack:
  added: []
  patterns: ["Ramsey\\Uuid\\Uuid use-import (DRY) replaces inline FQN and hex literals"]
key-files:
  created: []
  modified:
    - tests/Feature/MultiSiteCrossContextTest.php
    - tests/Feature/SendCapiEventEventLogTest.php
    - tests/Feature/PurchaseEndToEndIntegrationTest.php
    - tests/Feature/OrderStatusWatcherEventLogTest.php
    - tests/Feature/SendCapiEventTest.php
decisions:
  - "Caveman-compress over delete for seedEventLogRow PHPDoc: keep insertOrIgnore byte-match rationale, drop the false $casts int coercion claim, anchor to 03.1-07 REFAC-12 in one line."
  - "Replace hex literals with Uuid::uuid4()->toString() rather than reuse a named constant — mirrors MultiSiteCrossContextTest DRY pattern; PayloadBuilder UUIDv4 contract aligned for the day Phase 4 reuses these test paths."
metrics:
  duration: "~5 min"
  completed: "2026-05-14"
  commits: 6
  files_touched: 5
  production_files_touched: 0
  lines_added: 18
  lines_removed: 17
---

# Phase 3.1-08 Plan 02: test-suite DRY + lint cleanup Summary

Six atomic test-only commits closing the five Phase 3.1-07 REVIEW.md LOW findings (LOW-01 → LOW-05) plus BRIEF T2.5 (SendCapiEventTest setUp comment sync to post-08a0d12 inline unique-idx migration shape). Five feature-test files modified; zero production-code touches; pest baseline preserved at **171 passed / 6 failed**.

## Commits

| # | SHA       | Type     | Subject |
|---|-----------|----------|---------|
| 1 | 8ccf738   | refactor | `MultiSiteCrossContextTest use Ramsey Uuid import (DRY)` — LOW-01 |
| 2 | fd10dfa   | test     | `SendCapiEventEventLogTest replace hex literal with Uuid::uuid4()` — LOW-02 |
| 3 | 28599d4   | test     | `PurchaseEndToEndIntegrationTest replace hex literal with Uuid::uuid4()` — LOW-03 |
| 4 | 8fef82a   | test     | `PurchaseEndToEndIntegrationTest remove stale $casts rationale comment` — LOW-04 |
| 5 | 9935c82   | test     | `OrderStatusWatcherEventLogTest add declare(strict_types=1)` — LOW-05 |
| 6 | a310216   | test     | `SendCapiEventTest setUp comment — reflect unique idx inline` — T2.5 |

Each commit = one logical concern on one file (Tiger-Style SRP + BRIEF Constraint #3).

## Acceptance Criteria — Verified

All five must_haves in PLAN.md frontmatter confirmed:

1. MultiSiteCrossContextTest imports `use Ramsey\Uuid\Uuid;` and has zero inline `\Ramsey\Uuid\Uuid::` FQNs — verified by `grep -c "\\\\Ramsey\\\\Uuid\\\\Uuid::"` returns 0; 3 `Uuid::uuid4()->toString()` hits remain.
2. SendCapiEventEventLogTest and PurchaseEndToEndIntegrationTest no longer carry hardcoded hex literals as `event_id` — both now use `Uuid::uuid4()->toString()`.
3. PurchaseEndToEndIntegrationTest stale `$casts int` rationale comment removed; replaced with a one-line REFAC-12 (03.1-07) caveman reference. The insertOrIgnore byte-match rationale (still load-bearing) is preserved.
4. OrderStatusWatcherEventLogTest now opens with `declare(strict_types=1);` after `<?php`.
5. SendCapiEventTest setUp inline comment caveman-compressed to two lines reflecting the post-08a0d12 unique-idx-inline migration shape (no `WR-07 layered migration` narration, no `AddUniqueIndexToFailedEvents)->up()` call narration — neither was present pre-edit; the comment was already partially compressed by 08a0d12 and is now caveman-final).

## Test Suite Delta vs Baseline

| Bucket                                          | Before | After  | Delta |
|-------------------------------------------------|--------|--------|-------|
| Total passed (full `composer test`)             | 171    | 171    | 0     |
| Total failed (full `composer test`)             | 6      | 6      | 0     |
| MultiSiteCrossContextTest filter                | 3 / 3 ✓ | 3 / 3 ✓ | 0     |
| SendCapiEventEventLogTest filter                | 3 pass / 1 fail | 3 pass / 1 fail | 0  |
| PurchaseEndToEndIntegrationTest filter          | 5 / 5 ✓ | 5 / 5 ✓ | 0     |
| OrderStatusWatcherEventLogTest filter           | 13 / 13 ✓ | 13 / 13 ✓ | 0   |
| SendCapiEventTest filter                        | 14 / 14 ✓ | 14 / 14 ✓ | 0   |

The persistent SendCapiEventEventLogTest failure at line 156 (`test_second_concurrent_dispatch_returns_false_no_http_post`) is the **Track 3.6 race-fence baseline regression** documented in BRIEF.md lines 204-212 — explicitly out of scope for Track 2 (this plan) and will be tackled by a subsequent Track-3 fix-plan.

## Verification How

The worktree at `.claude/worktrees/agent-a6ecb8baecb0c4742` is a clone of just the plugin (no October app skeleton), so `composer test` cannot run there (the `vendor/bin/pest` path + bootstrap path resolve from `plugins/logingrupa/metapixelshopaholic/...` relative to the October repo root). For each task, the modified file was copied into the real plugin tree at `/home/forge/nailscosmetics.lv/plugins/logingrupa/metapixelshopaholic/`, `composer test -- --filter <TestClass>` ran from there, then `git checkout HEAD -- tests/Feature/<file>` restored the plugin tree to HEAD before moving to the next task. The full-suite verification at the end did the same with all 5 modified files at once, then restored. Plugin tree is clean (no uncommitted changes) after the run.

## Deviations from Plan

**None.** Plan executed exactly as written. The task-6 (SendCapiEventTest setUp) acceptance criteria's already-true state — no `WR-07 layered` or `AddUniqueIndexToFailedEvents)->up()` narration in the file pre-edit — was a consequence of commit 08a0d12 having partially updated the comment ahead of this plan. The caveman-compression edit was still applied (3 lines → 2 lines) to honor BRIEF T2.5's "compress" directive.

## Threat Flags

None. All edits are test-file comment/syntax cleanups; no new trust boundaries, no new schema, no new I/O surface.

## Self-Check: PASSED

Files created:
- `.planning/phases/03.1-08-dead-code-cleanup/03.1-08-02-SUMMARY.md` — FOUND.

Files modified across the 6 commits:
- `tests/Feature/MultiSiteCrossContextTest.php` — FOUND in commit 8ccf738.
- `tests/Feature/SendCapiEventEventLogTest.php` — FOUND in commit fd10dfa.
- `tests/Feature/PurchaseEndToEndIntegrationTest.php` — FOUND in commits 28599d4 + 8fef82a.
- `tests/Feature/OrderStatusWatcherEventLogTest.php` — FOUND in commit 9935c82.
- `tests/Feature/SendCapiEventTest.php` — FOUND in commit a310216.

Commits exist in `git log`:
- 8ccf738 — FOUND.
- fd10dfa — FOUND.
- 28599d4 — FOUND.
- 8fef82a — FOUND.
- 9935c82 — FOUND.
- a310216 — FOUND.

Full-suite verification (after stacking all 6 edits into the real plugin tree): `Tests: 6 failed, 171 passed (503 assertions)` — exact baseline preservation. Plugin tree restored to HEAD; no leakage out of the worktree.
