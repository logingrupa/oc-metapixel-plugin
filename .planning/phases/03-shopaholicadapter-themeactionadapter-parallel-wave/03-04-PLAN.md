---
phase: 03-shopaholicadapter-themeactionadapter-parallel-wave
plan: 04
type: execute
wave: 1
depends_on: [03-03]
files_modified:
  - tests/Feature/Adapter/Shopaholic/PurchaseFlowIntegrationTest.php
autonomous: true
requirements: [SHOP-05]
must_haves:
  truths:
    - "End-to-end Purchase flow on a generic Order fixture (host example.test, hermetic SQLite) exercises adapter resolution → race-fence INSERT IGNORE → MetaClient HTTP send → after_dispatch hook."
    - "Guzzle MockHandler captures the POST to https://graph.facebook.com/v23.0/{pixel_id}/events and asserts payload shape (event_name=Purchase, event_id is UUIDv4 shape, custom_data.content_ids[0] matches SKU-{product_id}[-{offer_id}])."
    - "Second admin-flip on the same Order does NOT re-fire — EventLog UNIQUE race-fence blocks; Middleware::history call count stays at 1."
    - "subject_type written to EventLog row is the literal string 'shopaholic.order' (NOT class FQN — P-05 anchor)."
  artifacts:
    - path: "tests/Feature/Adapter/Shopaholic/PurchaseFlowIntegrationTest.php"
      provides: "SHOP-05 end-to-end integration test"
      contains: "graph.facebook.com/v23.0"
  key_links:
    - from: "PurchaseFlowIntegrationTest"
      to: "classes/queue/SendCapiEvent.php"
      via: "OrderStatusWatcher::handle → SendCapiEvent::dispatchNow"
      pattern: "dispatchNow|handle\\("
    - from: "PurchaseFlowIntegrationTest"
      to: "Guzzle MockHandler + Middleware::history"
      via: "App::bind(MetaClient::class, ...)"
      pattern: "MockHandler"
---

<objective>
Ship the SHOP-05 end-to-end integration test that proves the full Purchase pipeline works from Order.status_id flip through Adapter resolution, race-fence INSERT IGNORE, Settings credential lookup, MetaClient HTTP POST to Graph API v23.0, after_dispatch hook fire, and the second-flip dedup contract. This is the gate proving the Phase 3 Shopaholic surface ships marketplace-ready.

Purpose: Closes SHOP-05. Closes ROADMAP Phase 3 SC1. Plan 03-04 has ZERO production code changes — only the integration test. If this test fails, an earlier plan's production code needs fixing; this plan stays test-only.
Output: One Pest feature test (~250 LOC) under `tests/Feature/Adapter/Shopaholic/` with two test cases — happy-path Purchase + dedup-on-second-flip — using Guzzle MockHandler + Middleware::history pattern from Phase 2 BackboneIntegrationTest.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-shopaholicadapter-themeactionadapter-parallel-wave/03-CONTEXT.md
@.planning/phases/03-shopaholicadapter-themeactionadapter-parallel-wave/03-RESEARCH.md
@tests/Feature/Adapter/BackboneIntegrationTest.php
@tests/ShopaholicAdapterTestCase.php
@classes/adapter/shopaholic/ShopaholicOrderAdapter.php
@classes/adapter/shopaholic/ShopaholicOrderValueResolver.php
@classes/event/adapter/shopaholic/OrderStatusWatcher.php
@classes/queue/SendCapiEvent.php
@classes/meta/MetaClient.php
@classes/meta/PayloadBuilder.php
@classes/helper/EventLogWriter.php
@models/EventLog.php
@Plugin.php
@CLAUDE.md
@../../CLAUDE.md

<interfaces>
<!-- Phase 2 BackboneIntegrationTest is the template — H-7 lock from STATE.md -->

H-7 Middleware::history > MockHandler queue count pattern (STATE.md anchored):
- Push Middleware::history($arHistory) onto HandlerStack BEFORE MockHandler.
- Assert `count($arHistory)` for accurate HTTP call count (NOT mock queue size).

MetaClient construction for test injection:
- `MetaClient::__construct(?ClientInterface $obClient = null)` — Phase 2 lock. Pass a Guzzle Client built around a HandlerStack with MockHandler + Middleware::history.

Settings stub for credentials:
- `Settings::set(['pixel_id' => 'test-pixel-123', 'capi_access_token' => 'test-token-xyz', 'paid_status_code' => 'new-payment-received', 'default_currency_code' => 'EUR']);` + `Settings::clearInternalCache();`

Order fixture (hermetic — uses ShopaholicAdapterTestCase):
- `bootOrdersTable() + bootOrdersStatuses()` from ShopaholicAdapterTestCase provisions everything needed for the Order side.
- For the SKU content_ids assertion to land, also boot the Offer + Product tables (from 03-03 Task 1's `bootOffersAndProductsTables()` helper).
- Then insert hermetic rows: Product id=1, Offer id=1 (one per product), OrderPosition table (provision hermetically here — schema: id, order_id, offer_id|item_id+item_type, quantity, price_value), Order id=1 with status_id=1 (initially 'new'), site_id=1, secret_key='order-secret-abc', email='a@b.test', total_price_value=99.99, currency_code='EUR'.

Expected POST URL (Phase 2 MetaClient lock):
`https://graph.facebook.com/v23.0/{pixel_id}/events`

Expected payload JSON keys (Phase 2 PayloadBuilder shape):
```
{
  "data": [{
    "event_id": "<uuid>",
    "event_time": <int>,
    "event_name": "Purchase",
    "action_source": "website",
    "user_data": { ... 13 keys, hashed where applicable ... },
    "custom_data": {
      "currency": "EUR",
      "value": 99.99,
      "num_items": <int>,
      "contents": [ ... ],
      "content_ids": ["SKU-1"],
      "content_type": "product"
    }
  }],
  "access_token": "<token>"  // merged INTO body by MetaClient
}
```

UUIDv4 shape: 8-4-4-4-12 hex chars. Regex: `/^[0-9a-f]{8}-[0-9a-f]{4}-4[0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$/i`.

Dedup contract:
- After first dispatch succeeds: EventLog has 1 row for (shopaholic.order, 1, Purchase, capi, 1).
- After SECOND OrderStatusWatcher::handle invocation on the same Order: EventLog count stays at 1; Middleware::history count stays at 1.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the SHOP-05 end-to-end integration test — happy-path Purchase</name>
  <files>tests/Feature/Adapter/Shopaholic/PurchaseFlowIntegrationTest.php</files>
  <read_first>
    - tests/Feature/Adapter/BackboneIntegrationTest.php (entire file — H-7 Middleware::history pattern + MockHandler binding + setUp shape)
    - tests/ShopaholicAdapterTestCase.php (post 03-03 — bootOrdersTable/Statuses/CartPosition/OffersAndProducts)
    - classes/adapter/shopaholic/ShopaholicOrderAdapter.php (post 03-02)
    - classes/event/adapter/shopaholic/OrderStatusWatcher.php (post 03-02)
    - classes/meta/MetaClient.php (Phase 2 — constructor accepts ?ClientInterface; URL + JSON-body shape)
    - classes/queue/SendCapiEvent.php (handle orchestration; readEventId / readEventTime helpers)
    - models/EventLog.php (post 03-01 — $jsonable=['payload'])
    - tests/MetapixelTestCase.php (verify whether queue.default = sync is pre-configured by the base; the W3 lock below assumes NOT and explicitly sets it)
  </read_first>
  <action>
Create `tests/Feature/Adapter/Shopaholic/PurchaseFlowIntegrationTest.php`. Namespace `Logingrupa\Metapixel\Tests\Feature\Adapter\Shopaholic`. PHP attributes: `use PHPUnit\Framework\Attributes\Group;` then `#[Group('adapter')]` on class. Class auto-extends `ShopaholicAdapterTestCase` via Pest.php binding. `final class PurchaseFlowIntegrationTest extends ShopaholicAdapterTestCase`. `protected $autoMigrate = true;`. `protected $autoRegister = true;` (boots `Plugin.php` register + boot via October's PerformsRegistrations trait so the AdapterRegistry binding + the Lovata-conditional `Plugin::boot` registers OrderAdapter).

setUp():
- `parent::setUp();`
- `$this->bootOrdersTable(); $this->bootOrdersStatuses(); $this->bootOffersAndProductsTables();`
- Provision hermetic `lovata_orders_shopaholic_order_positions` table: `Schema::create('lovata_orders_shopaholic_order_positions', function ($obTable) { $obTable->increments('id'); $obTable->integer('order_id')->nullable(); $obTable->integer('item_id')->nullable(); $obTable->string('item_type')->nullable(); $obTable->integer('offer_id')->nullable(); $obTable->integer('quantity')->nullable(); $obTable->decimal('price_value', 15, 2)->nullable(); $obTable->timestamps(); });`
- Provision hermetic `lovata_shopaholic_currencies` table: id, code (string), symbol, active, timestamps. Seed `['id' => 1, 'code' => 'EUR']`.
- Seed test data:
  - Product id=1, name='Test Product', slug='test-product'.
  - Offer id=1, product_id=1, price_value=99.99, name='Test Offer'.
  - Order id=1, status_id=1 (new, not paid), site_id=1, secret_key='order-secret-abc', email='customer@example.test', name='John', last_name='Doe', phone='+371...', total_price_value=99.99, currency_id=1.
  - OrderPosition id=1, order_id=1, item_id=1, item_type='Lovata\\Shopaholic\\Models\\Offer', offer_id=1, quantity=1, price_value=99.99.
- Set Settings: `Settings::clearInternalCache(); Settings::set(['pixel_id' => 'test-pixel-123', 'capi_access_token' => 'test-token-xyz', 'paid_status_code' => 'new-payment-received', 'default_currency_code' => 'EUR']);`
- Bind a Guzzle Mock HTTP client into the container so MetaClient receives it:
  ```
  $this->arHistory = [];
  $obMock = new \GuzzleHttp\Handler\MockHandler([
      new \GuzzleHttp\Psr7\Response(200, [], json_encode([
          'events_received' => 1,
          'fbtrace_id' => 'AbCd_xyz',
      ])),
      new \GuzzleHttp\Psr7\Response(200, [], json_encode([
          'events_received' => 1,
          'fbtrace_id' => 'second-AbCd',
      ])),
  ]);
  $obStack = \GuzzleHttp\HandlerStack::create($obMock);
  $obStack->push(\GuzzleHttp\Middleware::history($this->arHistory));
  $obGuzzle = new \GuzzleHttp\Client(['handler' => $obStack, 'http_errors' => false]);
  $this->app->bind(\Logingrupa\Metapixel\Classes\Meta\MetaClient::class, fn() => new \Logingrupa\Metapixel\Classes\Meta\MetaClient($obGuzzle));
  ```
- **Queue strategy (W3 lock — revision iteration 1):** explicitly set the queue driver to `sync` in setUp so `SendCapiEvent::dispatch(...)` invoked from inside `OrderStatusWatcher::handle` resolves synchronously through `SendCapiEvent::handle` in the same process — Guzzle's MockHandler then captures the HTTP POST inside the test scope. Implementation: `\Illuminate\Support\Facades\Config::set('queue.default', 'sync');` placed AFTER `parent::setUp()` and BEFORE the Watcher invocation. Do NOT use `Bus::fake()` — that would prevent `SendCapiEvent::handle` from running and Guzzle would never see the POST. Do NOT use `dispatchNow(...)` manually — the Watcher's call site uses the static `SendCapiEvent::dispatch(...)` API, and the sync driver routes through that API without a separate manual invocation. Rationale: `tests/MetapixelTestCase.php` does not set the queue driver explicitly, so depending on the host's `.env` / config defaults, the dispatch could be queued (silently swallowed in test scope) OR sync. The `Config::set` line removes that ambiguity.

`tearDown()` — `Schema::dropIfExists('lovata_orders_shopaholic_order_positions'); Schema::dropIfExists('lovata_shopaholic_currencies'); parent::tearDown();`. The parent already drops orders + statuses + cart_positions + offers + products via 03-03 Task 1.

Test method `test_purchase_dispatched_end_to_end_when_status_flipped_to_paid`:
1. Load Order id=1, flip its status_id from 1 → 5 (the paid_status_code 'new-payment-received'), call `$obOrder->save()`. (This fires eloquent.updated which would invoke the Watcher in production. For the test, invoke the Watcher's handle method manually to control timing: `(new OrderStatusWatcher)->handle($obOrder);`.)
2. With `queue.default = sync` set (per the W3 lock in setUp), the Watcher's `SendCapiEvent::dispatch(...)` resolves synchronously — `handle()` executes within the MockHandler scope, Guzzle records the POST in `$this->arHistory`. No `Bus::fake()` and no manual `dispatchNow` are needed.
3. (Step retained for numbering — handle() already ran synchronously in step 2 via the sync queue driver. Skip directly to step 4 assertions.)
4. Assertions:
   - `count($this->arHistory) === 1` (Guzzle made exactly one POST).
   - `(string) $this->arHistory[0]['request']->getUri() === 'https://graph.facebook.com/v23.0/test-pixel-123/events'`.
   - `$arBody = json_decode((string) $this->arHistory[0]['request']->getBody(), true); $arBody['access_token'] === 'test-token-xyz';`
   - `$arBody['data'][0]['event_name'] === 'Purchase';`
   - `preg_match('/^[0-9a-f]{8}-[0-9a-f]{4}-4[0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$/i', $arBody['data'][0]['event_id']);` (UUIDv4 shape).
   - `$arBody['data'][0]['custom_data']['content_ids'] === ['SKU-1'];` (single-offer-per-product SKU shape; Order had Product id=1 with one Offer).
   - `$arBody['data'][0]['custom_data']['currency'] === 'EUR';`
   - `$arBody['data'][0]['custom_data']['value']` is approximately 99.99 (use `assertEqualsWithDelta(99.99, ..., 0.01)`).
   - EventLog row exists: `DB::table('logingrupa_metapixel_event_log')->where('subject_type', 'shopaholic.order')->where('subject_id', 1)->where('event_name', 'Purchase')->where('channel', 'capi')->count() === 1;` — **assert subject_type literal === 'shopaholic.order'** (P-05 anchor).
   - EventLog row's payload column is non-NULL JSON (W8 narrowing per revision iteration 1 — read DB::table raw value into `$sPayloadRaw` typed as string before decode): `$sPayloadRaw = (string) (DB::table('logingrupa_metapixel_event_log')->value('payload') ?? ''); $arDecoded = json_decode($sPayloadRaw, true) ?: []; assertSame('Purchase', $arDecoded['data'][0]['event_name']);` (D-09 frozen-payload audit).
  </action>
  <verify>
    <automated>cd /home/forge/nailscosmetics.lv/plugins/logingrupa/metapixel && /home/forge/nailscosmetics.lv/vendor/bin/pest --compact tests/Feature/Adapter/Shopaholic/PurchaseFlowIntegrationTest.php --filter test_purchase_dispatched_end_to_end_when_status_flipped_to_paid 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/Feature/Adapter/Shopaholic/PurchaseFlowIntegrationTest.php` exists with `#[Group('adapter')]` class attribute.
    - Test contains literal substring `'https://graph.facebook.com/v23.0/test-pixel-123/events'`.
    - Test setUp explicitly sets `queue.default = sync` (W3 lock — revision iteration 1): `grep -q "Config::set('queue.default'" tests/Feature/Adapter/Shopaholic/PurchaseFlowIntegrationTest.php`.
    - Test does NOT use `Bus::fake()` for the happy-path dispatch (the sync queue driver routes through `SendCapiEvent::handle` directly): `! grep -q 'Bus::fake' tests/Feature/Adapter/Shopaholic/PurchaseFlowIntegrationTest.php` OR if Bus::fake IS present (e.g. for the Task 2 dedup-test scoped assertion), it MUST be inside a single test method scope, not setUp.
    - Test asserts `subject_type === 'shopaholic.order'` (P-05 byte-match).
    - Test asserts `content_ids === ['SKU-1']` (D-20 byte-match with FacebookCatalog exporter format).
    - Test asserts `event_name === 'Purchase'` AND event_id matches the UUIDv4 regex.
    - Test asserts EventLog row payload column non-NULL + decoded payload has `data[0].event_name === 'Purchase'`.
    - `pest --compact tests/Feature/Adapter/Shopaholic/PurchaseFlowIntegrationTest.php --filter test_purchase_dispatched_end_to_end_when_status_flipped_to_paid` exits 0.
  </acceptance_criteria>
  <done>End-to-end happy path Purchase test passes; SHOP-05 happy half closed.</done>
</task>

<task type="auto">
  <name>Task 2: Add the second-flip dedup test case + composer qa + commit</name>
  <files>tests/Feature/Adapter/Shopaholic/PurchaseFlowIntegrationTest.php (extend the same file with the dedup test method)</files>
  <read_first>
    - tests/Feature/Adapter/Shopaholic/PurchaseFlowIntegrationTest.php (post Task 1 — happy-path method shape)
    - models/EventLog.php (UNIQUE race-fence anchor on subject_type+subject_id+event_name+channel+site_id)
    - classes/helper/EventLogWriter.php (insertOrIgnore returns 0 on collision → false)
  </read_first>
  <action>
Add a second public test method to the existing `PurchaseFlowIntegrationTest` class:

`test_second_admin_flip_on_same_order_does_not_re_fire_eventlog_race_fence_blocks`:
1. Reuse the same setUp fixture state (constructed by Pest before each test method — Order id=1 created at status_id=1).
2. First flip + Watcher dispatch + handle (mirror Task 1 happy path): drive `(new OrderStatusWatcher)->handle($obOrder);` after `$obOrder->status_id = 5; $obOrder->save();`. Assert EventLog count is 1 + Guzzle history count is 1.
3. SECOND flip simulation: keep status_id at 5 (no real transition), simulate the eloquent.updated event by manually invoking `(new OrderStatusWatcher)->handle($obOrder);` again. This MAY:
   - Be blocked by the Watcher's `wasChanged('status_id')` guard (Pitfall 2) — `wasChanged` returns false on a no-op save, so the Watcher returns early. Watcher-side dedup → test asserts no SECOND dispatch happens AND Guzzle history stays at 1.
   - OR if a developer changes the Watcher logic and `wasChanged` returns true incorrectly, the dispatch proceeds → SendCapiEvent::handle → EventLogWriter::record returns false (insertOrIgnore collision on UNIQUE constraint) → handle short-circuits without sending HTTP. Test asserts Guzzle history stays at 1.
   - Both paths converge on the same observable: `count($this->arHistory) === 1` after the second handle call. The test asserts this regardless of which guard triggered.
4. To FORCE the second flow into the SendCapiEvent::handle path (proving the race-fence works, not just the Watcher guard), the test ALSO directly dispatches a synthetic SendCapiEvent with the SAME subject + payload:
   - Build payload via `PayloadBuilder::buildEventPayload('Purchase', $obAdapter, $obOrder, $obResolver, 'a-different-uuid-' . Uuid::uuid4()->toString(), time(), [])`.
   - `(new SendCapiEvent('Purchase', $arPayload, $obOrder, ShopaholicOrderAdapter::class))->handle(app(AdapterRegistry::class), app(MetaClient::class));`
   - Assert: `count($this->arHistory) === 1` (still 1 — second handle short-circuited because EventLogWriter::record returned false on UNIQUE collision).
   - Assert: `DB::table('logingrupa_metapixel_event_log')->count() === 1` (only the first row exists; UNIQUE constraint blocked the second insert).
5. Add explicit assertions on the EventLog row to PROVE the dedup anchor: the surviving row's event_id matches the FIRST dispatch's event_id (NOT the second's), since the first insert won the race-fence.

After both tests pass, run `vendor/bin/pint --dirty --format agent`, `composer qa`, and `composer deps`. Atomic commit `test(03-04): SHOP-05 end-to-end Purchase flow + dedup race-fence integration test`.
  </action>
  <verify>
    <automated>cd /home/forge/nailscosmetics.lv/plugins/logingrupa/metapixel && /home/forge/nailscosmetics.lv/vendor/bin/pest --compact tests/Feature/Adapter/Shopaholic/PurchaseFlowIntegrationTest.php 2>&1 | tail -10 && composer qa 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/Feature/Adapter/Shopaholic/PurchaseFlowIntegrationTest.php` declares TWO public test methods (the happy path from Task 1 + the dedup test from this task).
    - Dedup test asserts `count($this->arHistory) === 1` after invoking handle() twice on the same Order.
    - Dedup test asserts `EventLog::count() === 1` after both invocations.
    - Dedup test asserts the surviving EventLog row's event_id matches the first dispatch's event_id (race-fence first-wins anchor).
    - `pest --compact tests/Feature/Adapter/Shopaholic/PurchaseFlowIntegrationTest.php` exits 0 with both test methods green.
    - `composer qa` exits 0 (coverage ≥ 90% on Run A full-Lovata cell).
    - `composer deps` exits 0.
    - Atomic commit on master; git status clean.
  </acceptance_criteria>
  <done>SHOP-05 closed end-to-end with dedup proof; ROADMAP Phase 3 SC1 achieved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test harness → MockHandler | Test-controlled; no untrusted input. |
| MetaClient → graph.facebook.com URL | URL is built server-side from constants + Settings.pixel_id. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04-01 | Tampering | Tests bypass dedup contract | mitigate | Test ASSERTS the dedup contract; cannot tamper without making the test red. |
| T-03-04-02 | Information Disclosure | Test logs hashed PII | accept | Test fixture uses placeholder values; no real PII. |
| T-03-04-SC | Tampering | composer install | accept | Zero new packages. |
</threat_model>

<verification>
- `pest tests/Feature/Adapter/Shopaholic/PurchaseFlowIntegrationTest.php --compact` runs both test methods green.
- Guzzle `Middleware::history` records exactly ONE POST to `https://graph.facebook.com/v23.0/test-pixel-123/events` even when handle() is invoked twice on the same Order.
- EventLog UNIQUE race-fence blocks the second insert (`EventLog::count() === 1`).
- subject_type written as opaque alias `'shopaholic.order'` (P-05 byte-match).
- content_ids byte-match: `['SKU-1']` (single-offer-per-product fixture).
- event_id is UUIDv4 (regex match).
- payload column captured the frozen envelope (D-09 audit row).
- composer qa + composer deps exit 0.
</verification>

<success_criteria>
- SHOP-05 closed: end-to-end Purchase test exercises Watcher → adapter → race-fence → MetaClient mock → assertions on URL + JSON body + EventLog row.
- Dedup contract proven: second flip results in zero new HTTP calls + zero new EventLog rows.
- ROADMAP Phase 3 SC1 achieved.
- composer qa green; coverage ≥ 90%.
</success_criteria>

<output>
Create `.planning/phases/03-shopaholicadapter-themeactionadapter-parallel-wave/03-04-SUMMARY.md` when done.
</output>
