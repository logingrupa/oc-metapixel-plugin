---
phase: 03-shopaholicadapter-themeactionadapter-parallel-wave
plan: 09
type: execute
wave: 9
depends_on: [03-02, 03-03]
gap_closure: true
gap_refs:
  - "VERIFICATION.md::Gap 1 — ShopaholicOrderValueResolver::buildContentId orphan TypeError"
  - "VERIFICATION.md::Gap 2 — ShopaholicCartPositionAdapter::getSiteId always null"
requirements: [SHOP-02, SHOP-03]
files_modified:
  - classes/adapter/shopaholic/ShopaholicOrderValueResolver.php
  - classes/adapter/shopaholic/ShopaholicCartPositionAdapter.php
  - phpstan.neon
  - tests/Unit/Adapter/Shopaholic/ShopaholicOrderValueResolverTest.php
  - tests/Unit/Adapter/Shopaholic/ShopaholicCartPositionAdapterTest.php
autonomous: true
must_haves:
  truths:
    - "ShopaholicOrderValueResolver::buildContentId no longer raises TypeError when an OrderPosition's Offer has a null/missing product relation — orphaned-offer Purchase dispatch reaches EventLog without crashing."
    - "ShopaholicOrderValueResolver::resolveContentIds returns the literal string 'SKU-0' (or skips the row — implementation decides; see action) when offer.product is null, never throwing and never producing a malformed empty 'SKU-' string."
    - "ShopaholicCartPositionAdapter::getSiteId returns a non-null int when running inside an HTTP request, even though Lovata Cart has no site_id column — uses October's Site::getCurrent()?->getId() request-context fallback (D-15-style exception) so the EventLog UNIQUE race-fence dedup is effective on MySQL (NULL != NULL semantics)."
    - "phpstan.neon `disallowIn` block now excludes `classes/adapter/shopaholic/ShopaholicCartPositionAdapter.php` from the Site/SiteManager/Request ban (mirrors the existing classes/adapter/theme/* exclusion documented per D-16). The exclusion is documented inline in the file's class-level PHPDoc + in plugin CLAUDE.md 'Locked decisions' carry-forward."
    - "Two new negative-assertion test cases prove the bug is fixed: (a) orphan-product test runs resolveContentIds without raising TypeError + returns the documented fallback shape; (b) cart-position site_id test invokes getSiteId in an HTTP request context where Site::getCurrent() returns a non-null Site, asserts non-null int return."
  artifacts:
    - path: "classes/adapter/shopaholic/ShopaholicOrderValueResolver.php"
      provides: "Null-safe buildContentId via productOf() helper mirroring ShopaholicCartPositionValueResolver Pattern 4"
      contains: "private function productOf"
    - path: "classes/adapter/shopaholic/ShopaholicCartPositionAdapter.php"
      provides: "D-15-style request-context fallback for site_id with class-level PHPDoc documenting the exception"
      contains: "Site::getCurrent"
    - path: "phpstan.neon"
      provides: "ShopaholicCartPositionAdapter added to disallowIn exclusion alongside ThemeActionAdapter (documented exception)"
      contains: "ShopaholicCartPositionAdapter"
    - path: "tests/Unit/Adapter/Shopaholic/ShopaholicOrderValueResolverTest.php"
      provides: "Orphan-product regression test — resolveContentIds completes without TypeError when offer.product is null"
      contains: "test_resolve_content_ids_handles_orphaned_offer_without_typeerror"
    - path: "tests/Unit/Adapter/Shopaholic/ShopaholicCartPositionAdapterTest.php"
      provides: "site_id request-context fallback regression test — non-null int return inside an HTTP request"
      contains: "test_get_site_id_returns_non_null_via_site_current_fallback"
  key_links:
    - from: "classes/adapter/shopaholic/ShopaholicOrderValueResolver.php"
      to: "private function productOf(Offer $obOffer): ?Product"
      via: "$obOffer->getRelationValue('product') + instanceof Product narrowing"
      pattern: "private function productOf"
    - from: "classes/adapter/shopaholic/ShopaholicCartPositionAdapter.php"
      to: "October\\Rain\\Support\\Facades\\Site::getCurrent"
      via: "fallback when $obCart->site_id is null"
      pattern: "Site::getCurrent"
---

<objective>
Close VERIFICATION.md Gap 1 + Gap 2: patch `ShopaholicOrderValueResolver::buildContentId` to null-guard the `$obOffer->product` dereference (the exact bug that crashes Purchase CAPI dispatch on orphaned offers), and patch `ShopaholicCartPositionAdapter::getSiteId` to return a non-null `?int` via the D-15-style `Site::getCurrent()` request-context fallback so the EventLog UNIQUE race-fence works correctly on MySQL (NULL != NULL in UNIQUE index breaks dedup).

Purpose: Two production blockers prevent SHOP-02 + SHOP-03 from achieving their stated goal. Plan 03-09 narrows on the two bugs without scope creep — no replan of 03-01..03-08, no defensive abstractions, no new features. The fix mirrors patterns already shipped in this phase (`ShopaholicCartPositionValueResolver::productOf` for Pattern 4 null-guard; `classes/adapter/theme/*` exclusion for D-15-style request-context exception).
Output: One null-guard helper added to ShopaholicOrderValueResolver, one Site::getCurrent() fallback added to ShopaholicCartPositionAdapter, one phpstan.neon disallowIn line, two new negative-assertion test cases (one per gap), composer qa green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-shopaholicadapter-themeactionadapter-parallel-wave/03-CONTEXT.md
@.planning/phases/03-shopaholicadapter-themeactionadapter-parallel-wave/03-RESEARCH.md
@.planning/phases/03-shopaholicadapter-themeactionadapter-parallel-wave/03-VERIFICATION.md
@.planning/phases/03-shopaholicadapter-themeactionadapter-parallel-wave/03-02-SUMMARY.md
@.planning/phases/03-shopaholicadapter-themeactionadapter-parallel-wave/03-03-SUMMARY.md
@classes/adapter/shopaholic/ShopaholicOrderValueResolver.php
@classes/adapter/shopaholic/ShopaholicCartPositionAdapter.php
@classes/adapter/shopaholic/ShopaholicCartPositionValueResolver.php
@classes/adapter/EventSubjectAdapter.php
@classes/adapter/ValueResolver.php
@phpstan.neon
@tests/Unit/Adapter/Shopaholic/ShopaholicOrderValueResolverTest.php
@tests/ShopaholicAdapterTestCase.php
@CLAUDE.md
@../../CLAUDE.md

<interfaces>
<!-- The exact reference implementation the executor mirrors -->

ShopaholicCartPositionValueResolver::productOf (classes/adapter/shopaholic/ShopaholicCartPositionValueResolver.php:115-120) — the exact pattern the Order resolver MUST mirror:
```
private function productOf(Offer $obOffer): ?Product
{
    $mProduct = $obOffer->getRelationValue('product');

    return $mProduct instanceof Product ? $mProduct : null;
}
```

ShopaholicCartPositionValueResolver::buildContentId (classes/adapter/shopaholic/ShopaholicCartPositionValueResolver.php:97-108) — the null-guarded buildContentId pattern. Note the explicit `sprintf('SKU-%d', 0)` fallback when productOf returns null:
```
private function buildContentId(Offer $obOffer): string
{
    $obProduct = $this->productOf($obOffer);
    if ($obProduct === null) {
        return sprintf('SKU-%d', 0);
    }
    $iProductId = $this->intAttr($obProduct, 'id');

    return $obProduct->offer->count() > 1
        ? sprintf('SKU-%d-%d', $iProductId, $this->intAttr($obOffer, 'id'))
        : sprintf('SKU-%d', $iProductId);
}
```

Current (broken) ShopaholicOrderValueResolver::buildContentId (classes/adapter/shopaholic/ShopaholicOrderValueResolver.php:124-132):
```
private function buildContentId(Offer $obOffer): string
{
    $obProduct = $obOffer->product;        // <-- NO null guard; Lovata @property Product $product is phpdoc-as-fact (null at runtime when product_id orphan)
    $iProductId = $this->intAttr($obProduct, 'id');  // <-- intAttr expects non-nullable Model — TypeError when $obProduct is null

    return $obProduct->offer->count() > 1
        ? sprintf('SKU-%d-%d', $iProductId, $this->intAttr($obOffer, 'id'))
        : sprintf('SKU-%d', $iProductId);
}
```

intAttr signature in ShopaholicOrderValueResolver (line 134-139) — non-nullable Model param, hence the TypeError:
```
private function intAttr(Model $obModel, string $sAttr): int
```

ShopaholicCartPositionAdapter::getSiteId (current — broken):
```
public function getSiteId(object $obSubject): ?int
{
    $mCart = $this->positionOf($obSubject)?->getRelationValue('cart');
    $mSiteId = is_object($mCart) ? ($mCart->site_id ?? null) : null;

    return is_numeric($mSiteId) ? (int) $mSiteId : null;
}
```

D-15 documented exception (CONTEXT.md line 62) — exact pattern for ThemeActionAdapter that this plan extends to CartPositionAdapter:
- `arPayload['site_id']` first (N/A for cart subject — no payload-arg surface);
- if missing or non-int, falls back to `\October\Rain\Support\Facades\Site::getCurrent()?->getId()`.
- Documented in class-level PHPDoc + plugin CLAUDE.md "Locked decisions" section.
- Rationale: cart-add events fire in-request by definition (no queue rehydrate cross-context drift possible — CartPositionWatcher binds `eloquent.created` + `eloquent.updated` which always fire on the request boundary, never from queue worker).

D-16 disallowIn exclusion (CONTEXT.md line 67) — `classes/adapter/theme/` is excluded. This plan adds `classes/adapter/shopaholic/ShopaholicCartPositionAdapter.php` as a SECOND documented exception. Plugin CLAUDE.md "Locked decisions" section already documents that adapter dir is in `disallowIn`; the new entry is a per-file (NOT per-dir) exclusion.

October Site facade contract (vendor/october/rain/src/Support/Facades/Site.php):
- `Site::getCurrent()` returns `?\System\Models\SiteDefinition` — null in non-HTTP / CLI contexts, non-null in HTTP request.
- `$obSite->getId(): int` — returns the integer site id when SiteDefinition is bound.

Lovata Cart model (plugins/lovata/ordersshopaholic/models/Cart.php) — verified: NO site_id column in migrations:
```
grep site_id plugins/lovata/ordersshopaholic/updates/table_*cart*
# returns nothing
```

The hermetic test schema in `tests/ShopaholicAdapterTestCase::bootCartTable` adds a `site_id` column ONLY because the previous adapter contract documented it. After this plan, the hermetic schema's site_id column becomes an OPTIONAL pre-population path — the adapter prefers cart.site_id when non-null, falls back to Site::getCurrent() when null. Tests for both branches required.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add productOf() helper to ShopaholicOrderValueResolver + null-guard buildContentId</name>
  <files>classes/adapter/shopaholic/ShopaholicOrderValueResolver.php</files>
  <read_first>
    - classes/adapter/shopaholic/ShopaholicOrderValueResolver.php (current state — 160 LOC; buildContentId at lines 124-132 dereferences $obOffer->product unguarded)
    - classes/adapter/shopaholic/ShopaholicCartPositionValueResolver.php (lines 97-120 — reference pattern: productOf helper + null-guarded buildContentId mirror)
    - .planning/phases/03-shopaholicadapter-themeactionadapter-parallel-wave/03-VERIFICATION.md (Gap 1 — full description of the bug)
    - .planning/phases/03-shopaholicadapter-themeactionadapter-parallel-wave/03-02-SUMMARY.md (Plan 03-02 deviation #2 — Pattern 3 type-narrowing helpers context)
  </read_first>
  <action>
Modify `classes/adapter/shopaholic/ShopaholicOrderValueResolver.php`:

1. Add a NEW private helper method `productOf(Offer $obOffer): ?Product` placed RIGHT BEFORE the existing `buildContentId` method (line 124). Mirror `ShopaholicCartPositionValueResolver::productOf` byte-for-byte — three-line body, getRelationValue + instanceof narrowing:
   ```
   /**
    * Read the BelongsTo $product relation via getRelationValue() + runtime
    * narrowing to Product — Lovata's phpdoc declares the relation non-nullable
    * but resolves to null at the DB when product_id is unset or points at a
    * deleted product (Pitfall 1 / Gap 1).
    */
   private function productOf(Offer $obOffer): ?Product
   {
       $mProduct = $obOffer->getRelationValue('product');

       return $mProduct instanceof Product ? $mProduct : null;
   }
   ```

2. Add `use Lovata\Shopaholic\Models\Product;` to the existing `use` block (between `Lovata\OrdersShopaholic\Models\OrderPosition` and `Lovata\Shopaholic\Models\Offer` to maintain alphabetical order).

3. Modify the existing `buildContentId(Offer $obOffer): string` method body to mirror `ShopaholicCartPositionValueResolver::buildContentId` exactly — replace the unguarded `$obProduct = $obOffer->product;` line with a null-guard via the new productOf helper. Use `sprintf('SKU-%d', 0)` as the fallback string when product is null (NOT 'SKU-unknown' — matches the existing CartPosition resolver byte-for-byte to keep parity; also avoids introducing a third SKU shape that downstream code does not expect):
   ```
   private function buildContentId(Offer $obOffer): string
   {
       $obProduct = $this->productOf($obOffer);
       if ($obProduct === null) {
           return sprintf('SKU-%d', 0);
       }
       $iProductId = $this->intAttr($obProduct, 'id');

       return $obProduct->offer->count() > 1
           ? sprintf('SKU-%d-%d', $iProductId, $this->intAttr($obOffer, 'id'))
           : sprintf('SKU-%d', $iProductId);
   }
   ```

DO NOT change any other method body. DO NOT introduce new abstractions. DO NOT touch the resolveContentIds / resolveContents iteration — they already call buildContentId per OrderPosition, the fix is local to buildContentId.

LOC budget: file currently 160 LOC; expected new total ≤ 175 LOC (one new method ≈ 10 lines + 1 use line + null-guard branch ≈ 4 lines).
  </action>
  <verify>
    <automated>cd /home/forge/nailscosmetics.lv/plugins/logingrupa/metapixel && grep -q "private function productOf" classes/adapter/shopaholic/ShopaholicOrderValueResolver.php && grep -q "use Lovata.Shopaholic.Models.Product" classes/adapter/shopaholic/ShopaholicOrderValueResolver.php && grep -q "instanceof Product" classes/adapter/shopaholic/ShopaholicOrderValueResolver.php && grep -q "sprintf('SKU-%d', 0)" classes/adapter/shopaholic/ShopaholicOrderValueResolver.php && ! grep -q '$obOffer->product;' classes/adapter/shopaholic/ShopaholicOrderValueResolver.php && wc -l classes/adapter/shopaholic/ShopaholicOrderValueResolver.php | awk '{exit ($1<=180)?0:1}' && PATH=/home/forge/nailscosmetics.lv/vendor/bin:$PATH phpstan analyse --configuration=phpstan.neon --no-progress classes/adapter/shopaholic/ShopaholicOrderValueResolver.php 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - File `classes/adapter/shopaholic/ShopaholicOrderValueResolver.php` declares `private function productOf(Offer $obOffer): ?Product` (exact signature — `?Product` return type).
    - File imports `use Lovata\Shopaholic\Models\Product;`.
    - File `buildContentId` body contains the literal substring `$this->productOf($obOffer)` AND `sprintf('SKU-%d', 0)` (the orphan fallback).
    - File NO LONGER contains the bare unguarded line `$obProduct = $obOffer->product;` (`! grep -q '\\$obProduct = \\$obOffer->product;' …`).
    - File contains `$mProduct instanceof Product` (the narrowing line in productOf).
    - File total LOC ≤ 180 (current 160 + ≈10-15 new).
    - `phpstan analyse` exits 0 on the file (level 10, no new errors, no `@phpstan-ignore` introduced).
    - The file's existing `intAttr(Model $obModel, ...)` signature is UNCHANGED — fix is upstream at the buildContentId call site, not downstream at intAttr.
  </acceptance_criteria>
  <done>buildContentId returns the documented orphan fallback string when offer.product is null; no TypeError can propagate from this method into Purchase CAPI dispatch.</done>
</task>

<task type="auto">
  <name>Task 2: Add Site::getCurrent() request-context fallback to ShopaholicCartPositionAdapter::getSiteId + phpstan disallowIn per-file exclusion</name>
  <files>classes/adapter/shopaholic/ShopaholicCartPositionAdapter.php, phpstan.neon</files>
  <read_first>
    - classes/adapter/shopaholic/ShopaholicCartPositionAdapter.php (current state — 80 LOC; getSiteId at lines 33-39 always returns null because Lovata Cart has no site_id column)
    - classes/adapter/theme/ThemeActionAdapter.php (reference D-15 pattern — Site::getCurrent fallback inside an adapter that is excluded from the phpstan disallowIn ban)
    - phpstan.neon (the disallowedMethodCalls block — search for the existing `October\Rain\Support\Facades\Site` entry and its disallowIn list; the new exclusion line goes here)
    - .planning/phases/03-shopaholicadapter-themeactionadapter-parallel-wave/03-VERIFICATION.md (Gap 2 — full description of the bug + MySQL NULL UNIQUE semantics impact)
    - .planning/phases/03-shopaholicadapter-themeactionadapter-parallel-wave/03-CONTEXT.md (D-15 + D-16 — locked rationale for the documented exception)
    - .planning/phases/03-shopaholicadapter-themeactionadapter-parallel-wave/03-03-SUMMARY.md (key-decisions #2: "Lovata Cart has no site_id column natively" + "Real-world deployment options" documentation — this plan picks option (c): D-15-style fallback)
  </read_first>
  <action>
Modify `classes/adapter/shopaholic/ShopaholicCartPositionAdapter.php`:

1. Update the class-level PHPDoc (lines 9-13) to document the D-15-style exception. Replace the existing one-line docblock with a multi-line version capturing the locked rationale:
   ```
   /**
    * EventSubjectAdapter for Lovata\OrdersShopaholic\Models\CartPosition. Alias
    * 'shopaholic.cart_position'. Supports AddToCart on capi+pixel channels.
    *
    * site_id source: prefers $obPosition->cart->site_id (1-hop relation) when
    * non-null; falls back to October's Site::getCurrent()?->getId() as the
    * SECOND documented P-01 exception (alongside ThemeActionAdapter; CONTEXT.md
    * D-15). Cart events fire in-request by definition (eloquent.created /
    * eloquent.updated on CartPosition — never from queue worker rehydration),
    * so the request-context fallback is safe. phpstan disallowIn excludes this
    * file from the Site/SiteManager/Request ban (D-16; second documented
    * exception). Lovata Cart has no site_id column natively (verified via
    * grep on lovata_orders_shopaholic_carts migrations); without this fallback,
    * MySQL UNIQUE index dedup is broken (NULL != NULL semantics).
    */
   ```

2. Add `use October\Rain\Support\Facades\Site;` to the existing `use` block (after the other Lovata `use` lines — keep alphabetical-by-namespace ordering: Lovata namespaces first, then October, then Logingrupa).

3. Modify `getSiteId(object $obSubject): ?int` body to fall back to `Site::getCurrent()?->getId()` when the cart.site_id read returns null. Keep the existing cart-relation traversal as the primary source (so operators who DO populate cart.site_id via a custom migration continue to get the explicit value):
   ```
   public function getSiteId(object $obSubject): ?int
   {
       $mCart = $this->positionOf($obSubject)?->getRelationValue('cart');
       $mSiteId = is_object($mCart) ? ($mCart->site_id ?? null) : null;
       if (is_numeric($mSiteId)) {
           return (int) $mSiteId;
       }

       $obSite = Site::getCurrent();

       return $obSite !== null ? (int) $obSite->getId() : null;
   }
   ```

4. Modify `phpstan.neon` — locate the existing `disallowedMethodCalls` entry for `October\Rain\Support\Facades\Site::*` (and the parallel entry for `System\Classes\SiteManager::*`, and any `Illuminate\Http\Request::*` entry). For each of those three entries, append `'classes/adapter/shopaholic/ShopaholicCartPositionAdapter.php'` to the `disallowIn` list (per-file, NOT per-dir — the rest of the shopaholic adapter dir MUST continue to enforce the ban). The new entry goes RIGHT AFTER the existing `classes/adapter/theme/*` glob (so the two documented exceptions sit together for grep-discoverability).

5. ALSO modify `phpstan.neon` `disallowedFunctionCalls` for `request()` — append the same per-file path to its `disallowIn` list. (Per D-16: the three disallowed-method ranges + the one disallowed-function range all apply to the adapter dir; the exception per-file applies to all four.)

DO NOT touch any other phpstan.neon block. DO NOT touch the `classes/event/adapter/shopaholic/*` watcher entry — the CartPositionWatcher continues to be banned from Site/SiteManager/Request (the fallback lives in the adapter, not the watcher).

DO NOT add `@phpstan-ignore` anywhere.

LOC budget: ShopaholicCartPositionAdapter currently 80 LOC; expected new total ≤ 95 LOC (PHPDoc expansion ≈ 10 lines + 1 use line + 4 new body lines = ≈ 15 added; or ≤ 95).
  </action>
  <verify>
    <automated>cd /home/forge/nailscosmetics.lv/plugins/logingrupa/metapixel && grep -q "Site::getCurrent" classes/adapter/shopaholic/ShopaholicCartPositionAdapter.php && grep -q "use October.Rain.Support.Facades.Site" classes/adapter/shopaholic/ShopaholicCartPositionAdapter.php && grep -q "D-15" classes/adapter/shopaholic/ShopaholicCartPositionAdapter.php && grep -q "ShopaholicCartPositionAdapter.php" phpstan.neon && wc -l classes/adapter/shopaholic/ShopaholicCartPositionAdapter.php | awk '{exit ($1<=100)?0:1}' && PATH=/home/forge/nailscosmetics.lv/vendor/bin:$PATH phpstan analyse --configuration=phpstan.neon --no-progress classes/adapter/shopaholic/ShopaholicCartPositionAdapter.php 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - File `classes/adapter/shopaholic/ShopaholicCartPositionAdapter.php` `getSiteId` body contains `Site::getCurrent()` (the fallback call).
    - File imports `use October\Rain\Support\Facades\Site;`.
    - File class-level PHPDoc references `D-15` (the documented exception anchor).
    - File total LOC ≤ 100.
    - File `phpstan.neon` `disallowIn` list (for both `Site::*`, `SiteManager::*`, `Request::*`, and `request()`) contains the literal path `classes/adapter/shopaholic/ShopaholicCartPositionAdapter.php`. Verifiable: `grep -c "ShopaholicCartPositionAdapter.php" phpstan.neon` returns ≥ 4 (one per banned call category).
    - `phpstan analyse classes/adapter/shopaholic/ShopaholicCartPositionAdapter.php` exits 0 with no new errors — the per-file exclusion permits the Site::getCurrent() call.
    - `phpstan analyse classes/adapter/shopaholic/ShopaholicOrderAdapter.php` STILL exits 0 AND would FAIL if it tried to call Site::getCurrent (regression guard — the per-file exclusion does NOT widen to the whole dir).
    - No `@phpstan-ignore` introduced (`! grep -q "@phpstan-ignore" classes/adapter/shopaholic/ShopaholicCartPositionAdapter.php`).
  </acceptance_criteria>
  <done>ShopaholicCartPositionAdapter::getSiteId returns a non-null int in HTTP request context via Site::getCurrent fallback; phpstan.neon documents the per-file exception for this single adapter; UNIQUE race-fence dedup on MySQL becomes effective for AddToCart events.</done>
</task>

<task type="auto">
  <name>Task 3: Add regression tests for Gap 1 (orphaned offer) + Gap 2 (site_id fallback)</name>
  <files>tests/Unit/Adapter/Shopaholic/ShopaholicOrderValueResolverTest.php, tests/Unit/Adapter/Shopaholic/ShopaholicCartPositionAdapterTest.php</files>
  <read_first>
    - tests/Unit/Adapter/Shopaholic/ShopaholicOrderValueResolverTest.php (current state — extend with one new test case + supporting fixture helper if not present)
    - tests/Unit/Adapter/Shopaholic/ShopaholicCartPositionValueResolverTest.php (reference: hermetic Offer + Product + cart_positions setUp pattern)
    - tests/ShopaholicAdapterTestCase.php (bootCartTable + bootCartPositionTable + bootOffersAndProductsTables helpers — current state)
    - classes/adapter/shopaholic/ShopaholicOrderValueResolver.php (POST Task 1 — new productOf + null-guarded buildContentId)
    - classes/adapter/shopaholic/ShopaholicCartPositionAdapter.php (POST Task 2 — new Site::getCurrent fallback)
    - tests/Pest.php (verify ShopaholicAdapterTestCase is bound to tests/Unit/Adapter/Shopaholic/* directory — if not, the new ShopaholicCartPositionAdapterTest must `uses(ShopaholicAdapterTestCase::class)` or extend it explicitly)
    - .planning/phases/03-shopaholicadapter-themeactionadapter-parallel-wave/03-VERIFICATION.md (re-read both gaps to confirm the assertion shape — "raises TypeError on orphaned offer" + "always returns null — MySQL NULL != NULL")
  </read_first>
  <action>
**Step A — Extend `tests/Unit/Adapter/Shopaholic/ShopaholicOrderValueResolverTest.php` with ONE new regression test:**

Add a single test method (Pest closure-style if the file uses Pest closures, OR class method if class-based — match the existing file's convention):

```
test('resolve_content_ids handles orphaned offer without TypeError', function () {
    // Seed hermetic schema: an OrderPosition whose MorphTo $item points at an
    // Offer that exists but whose product_id references a NON-EXISTENT product
    // (orphan — product row was deleted, Offer.product relation resolves to null).
    // The pre-Task-1 buildContentId would TypeError here because $obOffer->product
    // is null and intAttr(Model $obModel) declares a non-nullable parameter.

    // Seed Order + Offer (no Product row inserted — product_id=999 has no matching row)
    $iOrderId = DB::table('lovata_orders_shopaholic_orders')->insertGetId([
        'site_id' => 1, 'status_id' => 5, 'secret_key' => 'orphan-test',
        'total_price_value' => 25.0, 'currency_id' => null,
        'email' => 'a@b.test', 'name' => 'A', 'last_name' => 'B', 'phone' => '1',
        'created_at' => now(), 'updated_at' => now(),
    ]);

    DB::table('lovata_shopaholic_offers')->insert([
        'id' => 1, 'product_id' => 999, // <- ORPHAN product_id
        'name' => 'Orphan offer', 'price_value' => 10.0,
        'active' => 1, 'sort_order' => 0,
        'created_at' => now(), 'updated_at' => now(),
    ]);

    DB::table('lovata_orders_shopaholic_order_positions')->insert([
        'order_id' => $iOrderId,
        'item_type' => Offer::class,
        'item_id' => 1,
        'quantity' => 2, 'price_value' => 10.0,
        'created_at' => now(), 'updated_at' => now(),
    ]);

    $obOrder = Order::find($iOrderId);
    $obResolver = new ShopaholicOrderValueResolver();

    // Negative assertion: pre-Task-1 this would throw TypeError. Post-fix it must
    // return WITHOUT throwing and the result must be the documented fallback shape.
    $arContentIds = $obResolver->resolveContentIds($obOrder);

    expect($arContentIds)->toBe(['SKU-0']); // <- orphan fallback per Task 1 action
});
```

(If the existing test file is class-based phpunit-style instead of Pest closures, write the test as a public method `public function test_resolve_content_ids_handles_orphaned_offer_without_typeerror(): void` with the same body — match the file's style; do NOT mix.)

The test ALSO asserts no orderpositions table exists yet → if the existing setUp does not provision `lovata_orders_shopaholic_order_positions` schema, add a minimal `bootOrderPositionsTable` helper to ShopaholicAdapterTestCase (or inline in the test setUp) with columns: id, order_id, item_type, item_id, quantity, price_value, timestamps. Check first via `grep -q bootOrderPositionsTable tests/ShopaholicAdapterTestCase.php`. If present, reuse; if absent, add it.

**Step B — Create NEW test file `tests/Unit/Adapter/Shopaholic/ShopaholicCartPositionAdapterTest.php`:**

If a ShopaholicCartPositionAdapterTest.php already exists in this directory, EXTEND it with the new test method instead of creating a duplicate file. (Check: `ls tests/Unit/Adapter/Shopaholic/ShopaholicCartPositionAdapterTest.php`.)

The file extends ShopaholicAdapterTestCase via Pest.php auto-binding (or explicit `uses(ShopaholicAdapterTestCase::class)` in the file head if Pest closures). Class-level attribute `#[\PHPUnit\Framework\Attributes\Group('adapter')]` (D-24 — for `pest --exclude-group=adapter` minimal-install regression cell).

Two test methods (BOTH branches: prefer-cart.site_id-when-non-null, fall-back-to-Site::getCurrent-when-null):

```
test('get_site_id returns cart.site_id when non-null (primary source)', function () {
    $iCartId = DB::table('lovata_orders_shopaholic_carts')->insertGetId([
        'user_id' => null,
        'site_id' => 7, // <- explicit site_id on the cart row
        'created_at' => now(), 'updated_at' => now(),
    ]);

    $iPositionId = DB::table('lovata_orders_shopaholic_cart_positions')->insertGetId([
        'cart_id' => $iCartId,
        'item_type' => Offer::class,
        'item_id' => 1, 'quantity' => 1, 'property' => null,
        'created_at' => now(), 'updated_at' => now(),
    ]);

    $obPosition = CartPosition::find($iPositionId);
    $obAdapter = new ShopaholicCartPositionAdapter();

    expect($obAdapter->getSiteId($obPosition))->toBe(7); // <- primary path, no Site::getCurrent involved
});

test('get_site_id returns non-null via Site::getCurrent fallback', function () {
    // Seed a cart with NULL site_id (the real-world Lovata Cart shape — no site_id col,
    // but the hermetic schema has the column nullable so we can test the null branch).
    $iCartId = DB::table('lovata_orders_shopaholic_carts')->insertGetId([
        'user_id' => null,
        'site_id' => null, // <- null forces fallback
        'created_at' => now(), 'updated_at' => now(),
    ]);

    $iPositionId = DB::table('lovata_orders_shopaholic_cart_positions')->insertGetId([
        'cart_id' => $iCartId,
        'item_type' => Offer::class,
        'item_id' => 1, 'quantity' => 1, 'property' => null,
        'created_at' => now(), 'updated_at' => now(),
    ]);

    // Bind a SiteDefinition double via the Site facade so Site::getCurrent() returns
    // a non-null Site with getId() === 3.
    $obFakeSite = Mockery::mock();
    $obFakeSite->shouldReceive('getId')->andReturn(3);
    Site::shouldReceive('getCurrent')->andReturn($obFakeSite);

    $obPosition = CartPosition::find($iPositionId);
    $obAdapter = new ShopaholicCartPositionAdapter();

    expect($obAdapter->getSiteId($obPosition))->toBe(3); // <- fallback path
});
```

Notes:
- Use `Mockery::mock()` (or `\Mockery::mock()`) for the SiteDefinition double — match the existing test base's Mockery usage.
- Use the `Site` facade explicitly: `use October\Rain\Support\Facades\Site;` at the top of the test file.
- Tests use `tearDown` (or Mockery::close() per test if Pest closure style) — match the existing `MetapixelTestCase` Mockery hygiene.
- The hermetic `bootCartTable` schema already includes a nullable site_id column per `03-03-SUMMARY.md` Decisions #2 — the test exercises BOTH the explicit-set branch + the null-fallback branch.

DO NOT mock the adapter itself. DO NOT mock business logic. The CartPosition + Cart Eloquent objects are real (hermetic), the only mock is the Site facade for the fallback path branch.
  </action>
  <verify>
    <automated>cd /home/forge/nailscosmetics.lv/plugins/logingrupa/metapixel && grep -q "orphaned offer" tests/Unit/Adapter/Shopaholic/ShopaholicOrderValueResolverTest.php && grep -q "ShopaholicCartPositionAdapter" tests/Unit/Adapter/Shopaholic/ShopaholicCartPositionAdapterTest.php && grep -q "Site::getCurrent" tests/Unit/Adapter/Shopaholic/ShopaholicCartPositionAdapterTest.php && grep -q "Group('adapter')" tests/Unit/Adapter/Shopaholic/ShopaholicCartPositionAdapterTest.php && PATH=/home/forge/nailscosmetics.lv/vendor/bin:$PATH pest --compact tests/Unit/Adapter/Shopaholic/ShopaholicOrderValueResolverTest.php tests/Unit/Adapter/Shopaholic/ShopaholicCartPositionAdapterTest.php 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/Unit/Adapter/Shopaholic/ShopaholicOrderValueResolverTest.php` contains a test referencing "orphaned offer" (case-insensitive name match) AND the test body inserts an Offer with a product_id that has no matching products row.
    - The orphan test asserts `resolveContentIds(...)` returns `['SKU-0']` (the Task 1 documented fallback) — NOT throws.
    - File `tests/Unit/Adapter/Shopaholic/ShopaholicCartPositionAdapterTest.php` exists AND contains a `#[Group('adapter')]` class attribute (or `pest()->group('adapter')` if closure-style).
    - The cart-position test file contains TWO test methods: one for primary path (cart.site_id non-null) and one for Site::getCurrent fallback (cart.site_id null).
    - The Site::getCurrent fallback test asserts `getSiteId(...)` returns the int returned by the Mockery-faked `getId()` (e.g. `toBe(3)`).
    - `pest --compact tests/Unit/Adapter/Shopaholic/ShopaholicOrderValueResolverTest.php tests/Unit/Adapter/Shopaholic/ShopaholicCartPositionAdapterTest.php` exits 0.
    - The new tests are tagged `#[Group('adapter')]` so `pest --exclude-group=adapter` continues to skip them (minimal-install regression guard preserved).
    - No business-logic mocking (the only Mockery double is the Site facade for the fallback branch; CartPosition + Cart + Offer + Order objects are all real hermetic rows).
  </acceptance_criteria>
  <done>Two negative-assertion tests prove (a) orphan-offer no longer raises TypeError and returns the documented fallback SKU; (b) cart-position site_id falls back to Site::getCurrent when cart.site_id is null. Both tests tagged adapter-group.</done>
</task>

<task type="auto">
  <name>Task 4: composer qa green + atomic commit</name>
  <files>(QA gate — verifies Tasks 1-3 land cleanly)</files>
  <read_first>
    - composer.json (qa script chain)
    - All files modified in Tasks 1-3
  </read_first>
  <action>
Run `PATH=/home/forge/nailscosmetics.lv/vendor/bin:$PATH vendor/bin/pint --dirty --format agent` from the plugin dir to auto-format the touched files. Then run the full `PATH=/home/forge/nailscosmetics.lv/vendor/bin:$PATH composer qa` chain (pint-test → phpstan level 10 → phpmd → pest --coverage --min=90). Fix any reported violations without `@phpstan-ignore` (D-28 lock) and without `assert()` (D-28 lock). On green, ONE atomic commit covering all four files + the phpstan.neon update:

`fix(03-09): null-guard ShopaholicOrderValueResolver buildContentId + ShopaholicCartPositionAdapter site_id fallback (Gap 1 + Gap 2)`

Verify minimal-install regression cell stays green:
`PATH=/home/forge/nailscosmetics.lv/vendor/bin:$PATH pest --exclude-group=adapter --compact 2>&1 | tail -5` — count must match the pre-plan count (no orphaned tagged tests leaking into the minimal cell).
  </action>
  <verify>
    <automated>cd /home/forge/nailscosmetics.lv/plugins/logingrupa/metapixel && PATH=/home/forge/nailscosmetics.lv/vendor/bin:$PATH composer qa 2>&1 | tail -20 && PATH=/home/forge/nailscosmetics.lv/vendor/bin:$PATH pest --exclude-group=adapter --compact 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `composer qa` exits 0 (pint-test + phpstan level 10 + phpmd + pest --coverage --min=90).
    - `! grep -rn "@phpstan-ignore" classes/ models/ Plugin.php tests/Unit/Adapter/Shopaholic/` returns no matches.
    - `! grep -rn "assert(" classes/ models/ Plugin.php tests/Unit/Adapter/Shopaholic/` returns no matches (production code; PHPUnit `$this->assertSame` etc in tests are OK — grep target is the bare `assert(` PHP builtin).
    - `pest --exclude-group=adapter` exit code 0 (minimal-install regression guard).
    - Single atomic commit with `git status` clean after.
  </acceptance_criteria>
  <done>SHOP-02 + SHOP-03 unblocked; phase verification re-runnable green; composer qa + minimal-install cell both green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OrderPosition.item → Offer.product | Orphaned product_id (admin deleted product but offer rows remain). Untrusted in the sense that data integrity is not guaranteed by Lovata. |
| Cart row → Site context | Lovata Cart has no site_id column natively; Site::getCurrent() supplies the active request site. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-09-01 | Denial of Service | ShopaholicOrderValueResolver::buildContentId | mitigate | productOf() helper + null-guard return early with the documented 'SKU-0' fallback; orphan offers no longer crash Purchase CAPI dispatch. |
| T-03-09-02 | Tampering | EventLog UNIQUE race-fence dedup on CartPosition rows | mitigate | Site::getCurrent() fallback in getSiteId returns a non-null int; UNIQUE index (subject_type, subject_id, event_name, channel, site_id) functions correctly on MySQL (no NULL != NULL trap). |
| T-03-09-03 | Spoofing | site_id source | mitigate | Per-file phpstan disallowIn exclusion documented per D-16; only this single adapter file gains the Site::getCurrent permission, the rest of the shopaholic adapter dir keeps the ban. Watcher (`classes/event/adapter/shopaholic/CartPositionWatcher.php`) continues to be banned. |
| T-03-09-SC | Tampering | composer install / vendor | accept | Plan ships ZERO new external packages. No supply chain surface. |
</threat_model>

<verification>
- `composer qa` exits 0 end-to-end (pint + phpstan + phpmd + pest --coverage --min=90).
- `pest tests/Unit/Adapter/Shopaholic/ShopaholicOrderValueResolverTest.php --compact` exits 0 — orphan test asserts no TypeError + documented fallback string.
- `pest tests/Unit/Adapter/Shopaholic/ShopaholicCartPositionAdapterTest.php --compact` exits 0 — primary path + Site::getCurrent fallback path both green.
- `pest --exclude-group=adapter --compact` exit code 0 — minimal-install regression count unchanged.
- `grep -c "ShopaholicCartPositionAdapter.php" phpstan.neon` returns ≥ 4 — per-file exclusion lands in all 3 disallowedMethodCalls entries + the 1 disallowedFunctionCalls entry.
- `grep -q "Site::getCurrent" classes/adapter/shopaholic/ShopaholicCartPositionAdapter.php` matches; `! grep -q "Site::getCurrent" classes/adapter/shopaholic/ShopaholicOrderAdapter.php` confirms the exception is narrow (only the cart-position adapter, not the order adapter).
- Static defense: `grep -rE 'Site::|SiteManager::|Request::|request\(' classes/event/adapter/shopaholic/` STILL returns no matches — the watcher dir keeps the ban.
</verification>

<success_criteria>
- VERIFICATION Gap 1 closed: ShopaholicOrderValueResolver::buildContentId null-guards $obOffer->product via the new productOf() helper; orphan offers return the documented 'SKU-0' fallback without TypeError.
- VERIFICATION Gap 2 closed: ShopaholicCartPositionAdapter::getSiteId returns a non-null int via Site::getCurrent() fallback in HTTP request context; MySQL UNIQUE race-fence dedup is effective for AddToCart events.
- phpstan.neon documents the per-file exception (4 entries: 3 disallowed-method + 1 disallowed-function) — the rest of `classes/adapter/shopaholic/*` keeps the Site/SiteManager/Request ban.
- Two regression tests anchor the bug-was-fixed evidence: orphan offer (Gap 1) + site_id fallback (Gap 2). Both tagged `#[Group('adapter')]`.
- composer qa exits 0; phpstan level 10 clean; no `@phpstan-ignore`; no `assert()`; minimal-install regression cell unchanged.
</success_criteria>

<output>
Create `.planning/phases/03-shopaholicadapter-themeactionadapter-parallel-wave/03-09-SUMMARY.md` when done.
</output>
