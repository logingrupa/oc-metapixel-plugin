---
phase: 03-shopaholicadapter-themeactionadapter-parallel-wave
plan: 02
type: execute
wave: 1
depends_on: [03-01]
files_modified:
  - classes/adapter/shopaholic/ShopaholicOrderAdapter.php
  - classes/adapter/shopaholic/ShopaholicOrderValueResolver.php
  - classes/exception/OrderHasNoCurrencyException.php
  - classes/event/adapter/shopaholic/OrderStatusWatcher.php
  - models/Settings.php
  - models/settings/fields.yaml
  - Plugin.php
  - composer-dependency-analyser.php
  - phpstan.neon
  - tests/Contract/Adapter/Shopaholic/ShopaholicOrderAdapterContractTest.php
  - tests/Unit/Adapter/Shopaholic/ShopaholicOrderValueResolverTest.php
  - tests/Unit/Event/Adapter/Shopaholic/OrderStatusWatcherTest.php
  - tests/Feature/Plugin/ShopaholicConditionalRegistrationTest.php
autonomous: true
requirements: [SHOP-01, SHOP-02, SHOP-03, SHOP-04]
must_haves:
  truths:
    - "ShopaholicOrderAdapter satisfies the EventSubjectAdapter contract — 10 invariant contract test passes."
    - "ShopaholicOrderAdapter::getSubjectType returns the opaque alias 'shopaholic.order' (not class FQN) — P-05 prevention."
    - "ShopaholicOrderAdapter::getSiteId reads ONLY Order.site_id — never SiteManager / Site / request — PHPStan disallowed-calls bans them inside classes/adapter/shopaholic/ + classes/event/adapter/shopaholic/."
    - "ShopaholicOrderValueResolver returns content_ids in the exact format 'SKU-{product_id}' or 'SKU-{product_id}-{offer_id}' matching the Facebook Catalog feed byte-for-byte."
    - "OrderStatusWatcher binds eloquent.updated + eloquent.created on Order, fires Purchase on transition to the operator's paid_status_code, ignores unrelated field changes, never rethrows (Tiger-Style fail-fast — log + return)."
    - "Plugin::boot conditionally registers the adapter + subscribes the watcher only when PluginManager::instance()->exists('Lovata.OrdersShopaholic') is true — minimal-install CI boots clean."
    - "composer-dependency-analyser config permits Lovata\\OrdersShopaholic\\* imports in classes/adapter/shopaholic/ AND classes/event/adapter/shopaholic/ — nowhere else (P-03)."
  artifacts:
    - path: "classes/adapter/shopaholic/ShopaholicOrderAdapter.php"
      provides: "EventSubjectAdapter implementation for Lovata\\OrdersShopaholic\\Models\\Order"
      contains: "'shopaholic.order'"
    - path: "classes/adapter/shopaholic/ShopaholicOrderValueResolver.php"
      provides: "ValueResolver — SKU content_ids + currency fallback chain"
      contains: "'SKU-'"
    - path: "classes/event/adapter/shopaholic/OrderStatusWatcher.php"
      provides: "Plain Event::subscribe class binding Order eloquent.updated|created"
      contains: "eloquent.updated"
    - path: "Plugin.php"
      provides: "Conditional ShopaholicOrderAdapter registration via PluginManager::exists gate"
      contains: "PluginManager::instance()->exists"
  key_links:
    - from: "Plugin.php"
      to: "classes/adapter/shopaholic/ShopaholicOrderAdapter.php"
      via: "AdapterRegistry::register(Order::class, ShopaholicOrderAdapter::class)"
      pattern: "ShopaholicOrderAdapter::class"
    - from: "classes/event/adapter/shopaholic/OrderStatusWatcher.php"
      to: "classes/queue/SendCapiEvent.php"
      via: "SendCapiEvent::dispatch('Purchase', $arPayload, $obOrder, ShopaholicOrderAdapter::class)"
      pattern: "SendCapiEvent::dispatch"
---

<objective>
Ship the first Shopaholic adapter: `ShopaholicOrderAdapter` + `ShopaholicOrderValueResolver` + `OrderStatusWatcher`, all gated behind `PluginManager::instance()->exists('Lovata.OrdersShopaholic')` in `Plugin::boot()`. Implements **SHOP-01..04** verbatim. FRESH code per CONTEXT.md D-02 + D-27 — NOT a port of the 367-LOC v1.x watcher.

Purpose: Closes the Phase 3 Shopaholic Purchase path end-to-end at the unit/contract level. Plan 03-04 then ships the end-to-end integration test (SHOP-05) on top of this surface. Phase 4 layers Multisite per-site credential routing onto the same dispatch flow.
Output: Three adapter classes, one watcher, one new exception, one Settings dropdown helper, a Settings YAML field for `paid_status_code`, conditional Plugin::boot registration, three new test files (contract + unit + feature), updated phpstan + composer-dependency-analyser configs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-shopaholicadapter-themeactionadapter-parallel-wave/03-CONTEXT.md
@.planning/phases/03-shopaholicadapter-themeactionadapter-parallel-wave/03-RESEARCH.md
@classes/adapter/EventSubjectAdapter.php
@classes/adapter/ValueResolver.php
@classes/adapter/AdapterRegistry.php
@classes/testing/EventSubjectAdapterContractTestCase.php
@classes/queue/SendCapiEvent.php
@classes/helper/EventLogWriter.php
@classes/meta/PayloadBuilder.php
@classes/meta/UserDataHasher.php
@models/EventLog.php
@models/Settings.php
@models/settings/fields.yaml
@Plugin.php
@phpstan.neon
@composer-dependency-analyser.php
@tests/MetapixelTestCase.php
@tests/ShopaholicAdapterTestCase.php
@tests/Contract/Adapter/FakeAdapterContractTest.php
@tests/Feature/Adapter/BackboneIntegrationTest.php
@CLAUDE.md
@../../CLAUDE.md

<interfaces>
<!-- The exact contracts the executor MUST implement against -->

EventSubjectAdapter contract (classes/adapter/EventSubjectAdapter.php — 7 methods):
- getSubjectType(object): string  — MUST return 'shopaholic.order' (NOT FQN); ≤ 64 chars; no backslashes.
- getSubjectId(object): int  — MUST be > 0.
- getSiteId(object): ?int  — MUST read $obOrder->getAttribute('site_id'); MUST NOT use SiteManager / Site / request.
- getSecretKey(object): ?string  — MUST return $obOrder->getAttribute('secret_key').
- getValueResolver(object): ValueResolver  — return new ShopaholicOrderValueResolver.
- getUserData(object): array<string, ?string>  — keys subset of {em, ph, fn, ln, ct, st, zp, country, external_id, fbp, fbc, client_ip_address, client_user_agent}; missing keys = null (not omitted).
- getSupportedEvents(): array<string, list<string>>  — return `['Purchase' => ['capi', 'pixel']]`.

ValueResolver contract (classes/adapter/ValueResolver.php — 5 methods):
- resolveContentIds(object): list<string>
- resolveValue(object): float
- resolveCurrency(object): string  — currency fallback chain: Order.currency relation->code → Order.currency_code field → Settings.default_currency_code → throw OrderHasNoCurrencyException.
- resolveContents(object): list<array{id: string, quantity: int, item_price: float}>
- resolveNumItems(object): int

EventLogWriter::record signature (post 03-01 — D-07 trailing arg):
EventLogWriter::record($sEventId, $sEventName, $sChannel, $obSubject, $sSecretKey, $iEventTime, $iSiteId, $arPayload): bool

SendCapiEvent::dispatch (Phase 2 — 4-arg constructor):
SendCapiEvent::dispatch(string $sEventName, array $arPayload, object $obSubject, string $sAdapterClass)

PayloadBuilder::buildEventPayload (Phase 2 D-22 event-agnostic):
PayloadBuilder::buildEventPayload(string $sEventName, EventSubjectAdapter, object, ValueResolver, string $sEventId, int $iEventTime, array $arEventExtras): array

Order model (plugins/lovata/ordersshopaholic/models/Order.php — verified columns):
- $obOrder->id (int)
- $obOrder->site_id (int, fillable @ line 145)
- $obOrder->secret_key (string)
- $obOrder->status_id (int)
- $obOrder->currency_id (int)
- $obOrder->currency_code (string accessor — PriceHelperTrait)
- $obOrder->currency relation (BelongsTo Currency; Currency::code = 'EUR'|'NOK'|'USD')
- $obOrder->status relation (BelongsTo Status)
- $obOrder->total_price_value (float)
- $obOrder->email, name, last_name, phone (PII fields for user_data)
- $obOrder->order_position relation (HasMany OrderPosition — has `offer_id`, `quantity`, `price_value`, `product_id` via Offer)
- $obOrder->getOriginal('status_id') — pre-save value (Pitfall 2)
- $obOrder->wasChanged('status_id') — post-save was-changed indicator

content_ids SKU format anchor (plugins/logingrupa/facebookcatalogshopaholic/classes/helper/ExportCatalogFacebookHelper.php:356-360):
- Multi-offer-per-product: 'SKU-' . $obOffer->product->id . '-' . $obOffer->id  →  e.g. "SKU-42-7"
- Single-offer-per-product: 'SKU-' . $obOffer->product->id  →  e.g. "SKU-42"
- Decision: $obOffer->product->offer()->count() > 1

Status model (plugins/lovata/ordersshopaholic/models/Status.php — verified):
- No `lists()` method exists. Use `Status::orderBy('sort_order')->pluck('name', 'code')->all()` to source the dropdown options.
- Status columns: id, name, code, sort_order, color, is_user_show.

Hermetic test fixtures (tests/ShopaholicAdapterTestCase.php):
- `bootOrdersTable()` creates `lovata_orders_shopaholic_orders` with: id, status_id, order_number, secret_key, total_price_value, currency_id, email, phone, name, last_name, site_id, timestamps.
- `bootOrdersStatuses()` seeds 5 rows including id=5 'new-payment-received' (default paid_status_code).

PluginManager::exists (modules/system/classes/PluginManager.php:380):
- `exists('Lovata.OrdersShopaholic'): bool` — true iff plugin is FOUND + ENABLED (not just autoloadable). P-11 anchor.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tighten phpstan + composer-dependency-analyser configs for the new adapter + event subdirs</name>
  <files>phpstan.neon, composer-dependency-analyser.php</files>
  <read_first>
    - phpstan.neon (current disallowedFunctionCalls + disallowedMethodCalls — see disallowIn lists)
    - composer-dependency-analyser.php (current ignoreErrorsOnPackageAndPath whitelist for lovata/* on classes/adapter/shopaholic only)
  </read_first>
  <action>
Modify `phpstan.neon` `disallowedFunctionCalls.request()` `disallowIn` list to append `'classes/event/adapter/shopaholic/*'`. (`classes/adapter/*` already covers `classes/adapter/shopaholic/*` via glob — keep that line; the new entry is for the watcher subdir.) Apply the same append to all three `disallowedMethodCalls` entries (`System\Classes\SiteManager::*`, `October\Rain\Support\Facades\Site::*`, `Illuminate\Http\Request::*`) — each gets `'classes/event/adapter/shopaholic/*'` added to its `disallowIn` list. Do NOT alter the existing `classes/adapter/*` glob — that wider glob remains the canonical ban for adapter dirs. The new event/adapter/shopaholic entries are belt-and-braces for the watcher subdir.

Modify `composer-dependency-analyser.php` to add an ignore for `lovata/ordersshopaholic-plugin` + `lovata/shopaholic-plugin` + `lovata/buddies-plugin` on the path `__DIR__.'/classes/event/adapter/shopaholic'` (mirror the existing three calls — copy lines 28-42 and add a second permitted path block for each package, OR refactor to a small loop over `[__DIR__.'/classes/adapter/shopaholic', __DIR__.'/classes/event/adapter/shopaholic']`).
  </action>
  <verify>
    <automated>cd /home/forge/nailscosmetics.lv/plugins/logingrupa/metapixel && grep -c "classes/event/adapter/shopaholic" phpstan.neon | awk '{exit ($1>=4)?0:1}' && grep -q "classes/event/adapter/shopaholic" composer-dependency-analyser.php && /home/forge/nailscosmetics.lv/vendor/bin/phpstan analyse --configuration=phpstan.neon --no-progress 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - File `phpstan.neon` contains `classes/event/adapter/shopaholic/*` at least 4 times (1 function-call entry + 3 method-call entries).
    - File `composer-dependency-analyser.php` contains the literal substring `classes/event/adapter/shopaholic` and the additions cover all three Lovata packages.
    - `phpstan analyse` exits 0 (no regression on existing tree before new adapter/watcher files are introduced).
    - The wider `classes/adapter/*` glob is preserved (`grep -c "classes/adapter/\*" phpstan.neon` returns ≥ 4 — one per banned call).
  </acceptance_criteria>
  <done>PHPStan disallowed-calls and composer-dependency-analyser whitelist permit Lovata\\OrdersShopaholic\\* imports + ban SiteManager/Site/Request inside both `classes/adapter/shopaholic/` and `classes/event/adapter/shopaholic/`.</done>
</task>

<task type="auto">
  <name>Task 2: Author ShopaholicOrderAdapter + ShopaholicOrderValueResolver + OrderHasNoCurrencyException</name>
  <files>classes/adapter/shopaholic/ShopaholicOrderAdapter.php, classes/adapter/shopaholic/ShopaholicOrderValueResolver.php, classes/exception/OrderHasNoCurrencyException.php</files>
  <read_first>
    - classes/adapter/EventSubjectAdapter.php (7-method contract — copy signatures verbatim)
    - classes/adapter/ValueResolver.php (5-method contract)
    - classes/exception/MissingPixelConfigException.php OR MissingCapiTokenException.php (pattern for new final exception class — see Phase 2 plan 02-03b exception hierarchy lock in STATE.md)
    - plugins/lovata/ordersshopaholic/models/Order.php (lines 28-160 — fillable cols, relations, accessors)
    - plugins/logingrupa/facebookcatalogshopaholic/classes/helper/ExportCatalogFacebookHelper.php (lines 350-360 — SKU format)
    - classes/meta/UserDataHasher.php (review HASHABLE_FIELDS const + PASSTHROUGH_FIELDS so adapter knows which keys to surface raw)
  </read_first>
  <action>
Create `classes/exception/OrderHasNoCurrencyException.php`. `final class OrderHasNoCurrencyException extends Logingrupa\Metapixel\Classes\Exception\MetaPixelException`. One-line PHPDoc: "Thrown when a Shopaholic Order has neither a currency relation nor a currency_code field and no Settings default exists." No additional public methods. Constructor inherited from parent.

Create `classes/adapter/shopaholic/ShopaholicOrderAdapter.php`. Namespace `Logingrupa\Metapixel\Classes\Adapter\Shopaholic`. `final class ShopaholicOrderAdapter implements EventSubjectAdapter`. Imports: `Logingrupa\Metapixel\Classes\Adapter\EventSubjectAdapter`, `Logingrupa\Metapixel\Classes\Adapter\ValueResolver`, `Lovata\OrdersShopaholic\Models\Order`. Class-level PHPDoc one-liner: "EventSubjectAdapter for Lovata\\OrdersShopaholic\\Models\\Order. Alias 'shopaholic.order'. Supports Purchase on capi+pixel channels."

Class constant `private const SUBJECT_TYPE = 'shopaholic.order';` (D-19). Class constant `private const SUPPORTED_EVENTS = ['Purchase' => ['capi', 'pixel']];` (D-02).

Implement the 7 contract methods:
- `getSubjectType(object $obSubject): string { return self::SUBJECT_TYPE; }` — does NOT touch the subject (alias is constant).
- `getSubjectId(object $obSubject): int { return (int) $obSubject->getAttribute('id'); }` (assumes Order subject; relies on registry hitting Order class only).
- `getSiteId(object $obSubject): ?int { $mSiteId = $obSubject->getAttribute('site_id'); return is_int($mSiteId) ? $mSiteId : (is_numeric($mSiteId) ? (int) $mSiteId : null); }` — MUST NOT use Site / SiteManager / request.
- `getSecretKey(object $obSubject): ?string { $mKey = $obSubject->getAttribute('secret_key'); return is_string($mKey) ? $mKey : null; }`
- `getValueResolver(object $obSubject): ValueResolver { return new ShopaholicOrderValueResolver; }`
- `getUserData(object $obSubject): array` — return array with all 13 Meta CAPI keys (em, ph, fn, ln, ct, st, zp, country, external_id, fbp, fbc, client_ip_address, client_user_agent). Phase 3 first-party scope populates ONLY the ones derivable from Order fields:
  - 'em' => `$obSubject->getAttribute('email')` ?: null (runtime guard for non-string).
  - 'ph' => `$obSubject->getAttribute('phone')` ?: null.
  - 'fn' => `$obSubject->getAttribute('name')` ?: null.
  - 'ln' => `$obSubject->getAttribute('last_name')` ?: null.
  - 'external_id' => `$obSubject->getAttribute('secret_key')` ?: null (the anonymous-token contract from STATE.md — UserDataHasher sha256's it before send).
  - ALL other keys explicitly => null (D-19 + contract invariant 07 forbids omitting).
  No request/Request fields read — fbp / fbc / client_ip_address / client_user_agent are theme-side responsibilities (set null here; Phase 4 cookie middleware populates them at Theme/EventPixel layer, NOT at Order adapter layer per D-15+D-16 separation).
- `getSupportedEvents(): array { return self::SUPPORTED_EVENTS; }`

Total file LOC ≤ 80 (D-27).

Create `classes/adapter/shopaholic/ShopaholicOrderValueResolver.php`. Namespace `Logingrupa\Metapixel\Classes\Adapter\Shopaholic`. `final class ShopaholicOrderValueResolver implements ValueResolver`. Imports: `Logingrupa\Metapixel\Classes\Adapter\ValueResolver`, `Logingrupa\Metapixel\Classes\Exception\OrderHasNoCurrencyException`, `Logingrupa\Metapixel\Models\Settings`. Class-level PHPDoc one-liner: "ValueResolver for Lovata\\OrdersShopaholic\\Models\\Order. content_ids in 'SKU-{product_id}[-{offer_id}]' format matching the Facebook Catalog feed (FacebookCatalogShopaholic ExportCatalogFacebookHelper line 356)."

Implement the 5 contract methods on Order:
- `resolveContentIds(object $obSubject): array` — iterate `$obSubject->order_position` (HasMany OrderPosition collection); for each OrderPosition `$obOrderPos`, derive the Offer via the OrderPosition's `item` MorphTo (matches CartPosition shape — order_position is similarly MorphTo per Lovata convention). Null-guard: if `$obOrderPos->item === null`, skip the row. Build content id via private helper `buildContentId(Offer $obOffer): string` returning `$obOffer->product->offer()->count() > 1 ? sprintf('SKU-%d-%d', $obOffer->product->id, $obOffer->id) : sprintf('SKU-%d', $obOffer->product->id);` (anchored byte-for-byte to FacebookCatalog exporter helper line 356). Return list<string>. If no positions or all null-items → return `[]`.
- `resolveValue(object $obSubject): float { $mTotal = $obSubject->getAttribute('total_price_value'); return is_numeric($mTotal) ? (float) $mTotal : 0.0; }`
- `resolveCurrency(object $obSubject): string` — Currency fallback chain:
  1. `$mRelation = $obSubject->currency; if ($mRelation !== null && is_string($mRelation->code) && $mRelation->code !== '') return $mRelation->code;`
  2. `$mField = $obSubject->getAttribute('currency_code'); if (is_string($mField) && $mField !== '') return $mField;`
  3. `$mDefault = Settings::get('default_currency_code', ''); if (is_string($mDefault) && $mDefault !== '') return $mDefault;`
  4. `throw new OrderHasNoCurrencyException('Order ' . $obSubject->getAttribute('id') . ' has no currency relation, currency_code, or Settings default');`
- `resolveContents(object $obSubject): array` — iterate order_position again, build list of `['id' => $sContentId, 'quantity' => (int) $obOrderPos->quantity, 'item_price' => (float) $obOrderPos->price_value]`. Null-guard MorphTo `item`. Return list<array{id,quantity,item_price}>.
- `resolveNumItems(object $obSubject): int { $iTotal = 0; foreach ($obSubject->order_position as $obPos) { $iTotal += (int) $obPos->quantity; } return $iTotal; }`. Null-safe via Eloquent Collection (HasMany returns empty Collection, not null).

Total file LOC ≤ 100 (D-27).

Final-class everything. No `@phpstan-ignore`. No `assert()`. PHPDoc one-line summaries only. Hungarian notation for locals (`$obSubject`, `$obOrderPos`, `$obOffer`, `$mTotal`, `$mField`, `$sContentId`, `$iTotal`).
  </action>
  <verify>
    <automated>cd /home/forge/nailscosmetics.lv/plugins/logingrupa/metapixel && grep -q "'shopaholic.order'" classes/adapter/shopaholic/ShopaholicOrderAdapter.php && grep -q "'SKU-'" classes/adapter/shopaholic/ShopaholicOrderValueResolver.php && grep -q "class OrderHasNoCurrencyException" classes/exception/OrderHasNoCurrencyException.php && wc -l classes/adapter/shopaholic/ShopaholicOrderAdapter.php | awk '{exit ($1<=80)?0:1}' && wc -l classes/adapter/shopaholic/ShopaholicOrderValueResolver.php | awk '{exit ($1<=100)?0:1}' && /home/forge/nailscosmetics.lv/vendor/bin/phpstan analyse --configuration=phpstan.neon --no-progress 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - File `classes/adapter/shopaholic/ShopaholicOrderAdapter.php` exists and contains literal `'shopaholic.order'` (the subject_type alias — P-05 anchor).
    - File `classes/adapter/shopaholic/ShopaholicOrderAdapter.php` does NOT contain `Site::` or `SiteManager::` or `Request::` or `request(` (`! grep -E '(Site|SiteManager|Request)::|request\\(' classes/adapter/shopaholic/ShopaholicOrderAdapter.php`).
    - File `classes/adapter/shopaholic/ShopaholicOrderValueResolver.php` contains literal `sprintf('SKU-%d-%d'` AND literal `sprintf('SKU-%d'` (both single-offer + multi-offer branches present).
    - File `classes/exception/OrderHasNoCurrencyException.php` declares `final class OrderHasNoCurrencyException extends MetaPixelException`.
    - LOC counts: ShopaholicOrderAdapter ≤ 80, ShopaholicOrderValueResolver ≤ 100.
    - PHPStan level 10 exits 0 on the three new files (no errors, no `@phpstan-ignore` introduced).
    - `getSupportedEvents` returns exactly `['Purchase' => ['capi', 'pixel']]` (grep `"'Purchase' => \\['capi', 'pixel'\\]"` matches).
  </acceptance_criteria>
  <done>Two adapter classes + one exception class shipped; phpstan green; LOC budgets met; SKU format matches the catalog exporter byte-for-byte.</done>
</task>

<task type="auto">
  <name>Task 3: Author OrderStatusWatcher (plain Event::subscribe class) + Settings::getPaidStatusCodeOptions dropdown helper + Settings YAML field</name>
  <files>classes/event/adapter/shopaholic/OrderStatusWatcher.php, models/Settings.php, models/settings/fields.yaml</files>
  <read_first>
    - classes/adapter/shopaholic/ShopaholicOrderAdapter.php (post Task 2 — Watcher dispatches using this class FQN)
    - classes/adapter/shopaholic/ShopaholicOrderValueResolver.php (post Task 2)
    - classes/meta/PayloadBuilder.php (Phase 2 — Watcher calls buildEventPayload to assemble dispatch payload)
    - classes/meta/UserDataHasher.php (Phase 2 — PayloadBuilder requires UserDataHasher in its constructor)
    - classes/queue/SendCapiEvent.php (Phase 2 — 4-arg constructor)
    - models/Settings.php (current shape — Phase 2 lookupForSite + @method static get/set)
    - plugins/lovata/ordersshopaholic/models/Status.php (NO lists() — use orderBy + pluck)
    - models/settings/fields.yaml (current 3 fields — add paid_status_code + default_currency_code)
    - .planning/phases/03-shopaholicadapter-themeactionadapter-parallel-wave/03-RESEARCH.md §"Pattern 1" + §"Pitfall 2"
    - **W1 note (revision iteration 1):** CONTEXT.md §"Claude's Discretion" line 97 mentions `Status::lists()` as the dropdown source. RESEARCH.md correction (a) supersedes — use `Status::orderBy('sort_order')->pluck('name', 'code')->all()` (Lovata.OrdersShopaholic Status model has no `lists()` method). The PLAN body below is authoritative; this note is informational only.
  </read_first>
  <action>
Create `classes/event/adapter/shopaholic/OrderStatusWatcher.php`. Namespace `Logingrupa\Metapixel\Classes\Event\Adapter\Shopaholic`. `final class OrderStatusWatcher`. Imports:
- `Illuminate\Events\Dispatcher`
- `Illuminate\Support\Facades\Log`
- `Logingrupa\Metapixel\Classes\Adapter\Shopaholic\ShopaholicOrderAdapter`
- `Logingrupa\Metapixel\Classes\Adapter\Shopaholic\ShopaholicOrderValueResolver`
- `Logingrupa\Metapixel\Classes\Meta\PayloadBuilder`
- `Logingrupa\Metapixel\Classes\Meta\UserDataHasher`
- `Logingrupa\Metapixel\Classes\Queue\SendCapiEvent`
- `Logingrupa\Metapixel\Models\Settings`
- `Lovata\OrdersShopaholic\Models\Order`
- `Ramsey\Uuid\Uuid`
- `Throwable`

Class-level PHPDoc one-liner: "Watches Order eloquent.updated|created; fires Purchase on transition to paid_status_code. EventLog UNIQUE race-fence is the dedup anchor."

Method `subscribe(Dispatcher $obDispatcher): void`:
- `$obDispatcher->listen('eloquent.updated: ' . Order::class, [$this, 'handle']);`
- `$obDispatcher->listen('eloquent.created: ' . Order::class, [$this, 'handle']);`

Method `handle(Order $obOrder): void` (≤50 LOC, Tiger-Style):
1. Wrap entire body in try/catch(Throwable). Catch logs `Log::warning('metapixel: OrderStatusWatcher payload-build failed', ['meta_pixel.order_id' => $obOrder->id, 'meta_pixel.exception' => get_class($obException), 'meta_pixel.message' => $obException->getMessage()]);` and RETURNS — does NOT rethrow (D-27 + Tiger-Style — would cascade-break Order::save via Lovata OrderProcessor/Campaign/PromoMechanism).
2. Resolve paid status code: `$mPaidCode = Settings::get('paid_status_code', 'new-payment-received'); $sPaidCode = is_string($mPaidCode) ? $mPaidCode : 'new-payment-received';`
3. Get Order.status relation. If `$obStatus === null || $obStatus->code !== $sPaidCode` → return (not paid).
4. Transition guard (Pitfall 2): if this is an `eloquent.updated` (detect via `$obOrder->wasChanged('status_id')` — only fire when status_id was actually changed in this save). On `eloquent.created`, `wasChanged` returns true for all fields, so creating at the paid status legitimately dispatches. Implementation: `if ($obOrder->exists && ! $obOrder->wasChanged('status_id')) { return; }` — guards the updated path; created path passes because exists is true post-create but wasChanged also true.
5. Build payload: instantiate `$obAdapter = new ShopaholicOrderAdapter; $obResolver = new ShopaholicOrderValueResolver; $obBuilder = new PayloadBuilder(new UserDataHasher);`. `$sEventId = Uuid::uuid4()->toString(); $iEventTime = time(); $arPayload = $obBuilder->buildEventPayload('Purchase', $obAdapter, $obOrder, $obResolver, $sEventId, $iEventTime, []);`
6. Dispatch: `SendCapiEvent::dispatch('Purchase', $arPayload, $obOrder, ShopaholicOrderAdapter::class);`

Total LOC ≤ 70 (D-27 + reviewer smell test).

Modify `models/Settings.php`:
- Add `use Lovata\OrdersShopaholic\Models\Status;` import.
- Add NEW public static method `getPaidStatusCodeOptions(): array` with body `return Status::orderBy('sort_order')->pluck('name', 'code')->all();` PHPDoc one-liner: "Dropdown options for paid_status_code Settings field. Sources current Status rows via Lovata.OrdersShopaholic; no lists() helper exists on the Status model." Wrap in `class_exists(Status::class)` guard — return `[]` when Lovata.OrdersShopaholic not installed (Run B minimal cell). PHPDoc `@return array<string, string>` (code → name).
- Add NEW public static method `getDefaultCurrencyCodeOptions(): array` returning a small static map `['EUR' => 'EUR', 'NOK' => 'NOK', 'USD' => 'USD', 'GBP' => 'GBP']`. (`default_currency_code` is the SettingsResolver fallback for `resolveCurrency`.) PHPDoc one-liner.

The `use Lovata\OrdersShopaholic\Models\Status;` import inside `models/Settings.php` would be flagged by composer-dependency-analyser (Settings is in `models/`, not adapter dir). Therefore extract the dropdown helpers to a NEW dedicated helper file inside the adapter dir: create `classes/adapter/shopaholic/ShopaholicSettingsOptions.php` with `final class ShopaholicSettingsOptions` and two public static methods `getPaidStatusCodeOptions(): array` + `getDefaultCurrencyCodeOptions(): array`. Wire the YAML option callbacks to that class instead of Settings. Update `models/Settings.php` to add only the `default_currency_code` fillable / lookup if not present — DO NOT import Lovata models into Settings.

Modify `models/settings/fields.yaml`:
- Add field `paid_status_code` after `test_event_code`. Type `dropdown`, span `auto`, options `Logingrupa\Metapixel\Classes\Adapter\Shopaholic\ShopaholicSettingsOptions::getPaidStatusCodeOptions`, default `new-payment-received`, label key `logingrupa.metapixel::lang.settings.fields.paid_status_code_label`, commentAbove key `logingrupa.metapixel::lang.settings.fields.paid_status_code_comment`.
- Add field `default_currency_code` after `paid_status_code`. Type `dropdown`, span `auto`, options `Logingrupa\Metapixel\Classes\Adapter\Shopaholic\ShopaholicSettingsOptions::getDefaultCurrencyCodeOptions`, default `EUR`, label + commentAbove keys reserved (Phase 4 LANG-01 populates).
  </action>
  <verify>
    <automated>cd /home/forge/nailscosmetics.lv/plugins/logingrupa/metapixel && grep -q "eloquent.updated: ' . Order::class" classes/event/adapter/shopaholic/OrderStatusWatcher.php && grep -q "wasChanged('status_id')" classes/event/adapter/shopaholic/OrderStatusWatcher.php && grep -q "SendCapiEvent::dispatch" classes/event/adapter/shopaholic/OrderStatusWatcher.php && wc -l classes/event/adapter/shopaholic/OrderStatusWatcher.php | awk '{exit ($1<=70)?0:1}' && grep -q "paid_status_code" models/settings/fields.yaml && grep -q "getPaidStatusCodeOptions" classes/adapter/shopaholic/ShopaholicSettingsOptions.php && /home/forge/nailscosmetics.lv/vendor/bin/phpstan analyse --configuration=phpstan.neon --no-progress 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - File `classes/event/adapter/shopaholic/OrderStatusWatcher.php` exists; LOC ≤ 70.
    - File contains the literal substring `'eloquent.updated: ' . Order::class` AND `'eloquent.created: ' . Order::class`.
    - File contains `SendCapiEvent::dispatch('Purchase', $arPayload, $obOrder, ShopaholicOrderAdapter::class);` verbatim.
    - File contains `wasChanged('status_id')` (Pitfall 2 transition guard).
    - File's try/catch handler does NOT rethrow: `! grep -E 'throw \$obException' classes/event/adapter/shopaholic/OrderStatusWatcher.php`.
    - File `classes/adapter/shopaholic/ShopaholicSettingsOptions.php` exists with two public static dropdown methods.
    - File `models/Settings.php` does NOT import `Lovata\OrdersShopaholic\Models\Status` (`! grep "OrdersShopaholic" models/Settings.php`).
    - File `models/settings/fields.yaml` contains `paid_status_code` + `default_currency_code` field entries.
    - `phpstan analyse` exits 0.
  </acceptance_criteria>
  <done>Plain Event::subscribe Watcher binds Order eloquent events; dispatches Purchase only on status_id transition to paid_status_code; Tiger-Style log + return on payload-build failure; Settings dropdown helpers live inside the adapter dir (Lovata-isolation respected).</done>
</task>

<task type="auto">
  <name>Task 4: Wire conditional Plugin::boot registration via PluginManager::exists gate</name>
  <files>Plugin.php</files>
  <read_first>
    - Plugin.php (current state — post 03-01 register() with AdapterRegistry singleton + registerSchedule + registerConsoleCommand)
    - classes/adapter/shopaholic/ShopaholicOrderAdapter.php (post Task 2)
    - classes/event/adapter/shopaholic/OrderStatusWatcher.php (post Task 3)
    - .planning/phases/03-shopaholicadapter-themeactionadapter-parallel-wave/03-RESEARCH.md §"Example 2" (PluginManager::exists gate shape)
  </read_first>
  <action>
Modify `Plugin.php` `boot(): void` body:
- Add imports: `use System\Classes\PluginManager;`, `use Illuminate\Support\Facades\App;`, `use Logingrupa\Metapixel\Classes\Adapter\Shopaholic\ShopaholicOrderAdapter;`, `use Logingrupa\Metapixel\Classes\Event\Adapter\Shopaholic\OrderStatusWatcher;`, `use Lovata\OrdersShopaholic\Models\Order;`, `use Event;` (or `use Illuminate\Support\Facades\Event;`).
- Add a NEW protected helper method on `Plugin` (W2 lock — revision iteration 1 — makes the conditional gate testable by replacing the static `PluginManager::instance()` call site with a container-resolved alternative the test can fake):
  ```
  /**
   * Whether Lovata.OrdersShopaholic is installed + enabled. Resolves via the
   * container-bound PluginManager so tests can swap a Mockery fake via
   * `$this->app->instance(PluginManager::class, $obFakePM)`.
   */
  protected function isShopaholicEnabled(): bool
  {
      /** @var PluginManager $obPluginManager */
      $obPluginManager = App::make(PluginManager::class);
      return $obPluginManager->exists('Lovata.OrdersShopaholic');
  }
  ```
- Replace empty `public function boot(): void {}` with body:
  ```
  if ($this->isShopaholicEnabled()) {
      App::make(AdapterRegistry::class)->register(
          Order::class,
          ShopaholicOrderAdapter::class,
      );
      Event::subscribe(OrderStatusWatcher::class);
  }
  ```
- Note: per 03-RESEARCH.md §"Example 2" note, do NOT add `AdapterRegistry::instance()` static helper — use `App::make(AdapterRegistry::class)` directly (CLAUDE.md "no over-engineering" lock).
- Note: the `isShopaholicEnabled()` helper routes through `App::make(PluginManager::class)`, NOT `PluginManager::instance()`. Laravel's container resolves `PluginManager::class` via the framework's service-provider bindings (October binds it during boot). When a test calls `$this->app->instance(PluginManager::class, $obFakePM)`, subsequent `App::make(PluginManager::class)` resolutions return the fake. This is what makes W2 Task 5's test strategy work without `Mockery::mock('overload:...')`.

DO NOT register the ThemeActionAdapter / ThemeAjaxHandler here — those land Plans 03-05 + 03-07. Plan 03-02 ships ONLY the Shopaholic Order side.

DO NOT register the CartPosition adapter here — that lands Plan 03-03.
  </action>
  <verify>
    <automated>cd /home/forge/nailscosmetics.lv/plugins/logingrupa/metapixel && grep -q "PluginManager::instance()->exists('Lovata.OrdersShopaholic')" Plugin.php && grep -q "AdapterRegistry::class)->register" Plugin.php && grep -q "Event::subscribe(OrderStatusWatcher::class)" Plugin.php && /home/forge/nailscosmetics.lv/vendor/bin/phpstan analyse --configuration=phpstan.neon --no-progress 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - File `Plugin.php` declares `protected function isShopaholicEnabled(): bool` with a one-line PHPDoc summary (W2 lock).
    - File `Plugin.php` `isShopaholicEnabled()` body resolves PluginManager via `App::make(PluginManager::class)` (NOT `PluginManager::instance()`) so the container can swap a test fake.
    - File `Plugin.php` `boot()` body invokes `$this->isShopaholicEnabled()` inside the if-condition (not the static singleton).
    - File contains `Event::subscribe(OrderStatusWatcher::class);` inside the if-block.
    - File does NOT contain a static `AdapterRegistry::instance()` call (`! grep "AdapterRegistry::instance(" Plugin.php`).
    - PHPStan level 10 exits 0.
  </acceptance_criteria>
  <done>Plugin::boot conditionally registers ShopaholicOrderAdapter + OrderStatusWatcher only when Lovata.OrdersShopaholic is installed + enabled; minimal-install boot path is no-op.</done>
</task>

<task type="auto">
  <name>Task 5: Author 4 test files — ContractTest + ValueResolverTest + WatcherTest + ConditionalRegistrationTest</name>
  <files>tests/Contract/Adapter/Shopaholic/ShopaholicOrderAdapterContractTest.php, tests/Unit/Adapter/Shopaholic/ShopaholicOrderValueResolverTest.php, tests/Unit/Event/Adapter/Shopaholic/OrderStatusWatcherTest.php, tests/Feature/Plugin/ShopaholicConditionalRegistrationTest.php</files>
  <read_first>
    - classes/testing/EventSubjectAdapterContractTestCase.php (10 invariants — Phase 3 contract tests extend this)
    - tests/Contract/Adapter/FakeAdapterContractTest.php (pattern reference for Pest contract test subclass — makeAdapter + makeSubject factory methods)
    - tests/ShopaholicAdapterTestCase.php (hermetic Orders + Statuses tables; tests under tests/{Unit,Feature}/Adapter/Shopaholic auto-use this base via Pest.php)
    - classes/adapter/shopaholic/ShopaholicOrderAdapter.php (post Task 2)
    - classes/adapter/shopaholic/ShopaholicOrderValueResolver.php (post Task 2)
    - classes/event/adapter/shopaholic/OrderStatusWatcher.php (post Task 3)
    - tests/Feature/Adapter/BackboneIntegrationTest.php (pattern for test-DB AdapterRegistry binding + queue Bus::fake)
    - Plugin.php (post Task 4 — boot conditional registration)
  </read_first>
  <action>
Create `tests/Contract/Adapter/Shopaholic/ShopaholicOrderAdapterContractTest.php`. Namespace `Logingrupa\Metapixel\Tests\Contract\Adapter\Shopaholic`. Class-level attribute `#[\PHPUnit\Framework\Attributes\Group('adapter')]` (D-24 — minimal-install excludes via `pest --exclude-group=adapter`). `final class ShopaholicOrderAdapterContractTest extends EventSubjectAdapterContractTestCase`. Because contract tests live under `tests/Contract/Adapter/Shopaholic/` AND `tests/Pest.php` binds `ShopaholicAdapterTestCase` to `tests/Unit/Adapter/Shopaholic` + `tests/Feature/Adapter/Shopaholic` only, also bind `ShopaholicAdapterTestCase` to `tests/Contract/Adapter/Shopaholic` by extending the parent `EventSubjectAdapterContractTestCase` here. **However**, EventSubjectAdapterContractTestCase already extends MetapixelTestCase (not ShopaholicAdapterTestCase). To get the hermetic Orders+Statuses tables in the contract subclass, override setUp to provision via the same helpers — do NOT alter the abstract base; instead create a minimal Order fixture directly in `makeSubject` using `DB::table('lovata_orders_shopaholic_orders')->insert(...)` + `Order::find($iId)`.

`protected $autoMigrate = true;` so Phase 2 + 03-01 migrations run. setUp:
- `parent::setUp();`
- Create `lovata_orders_shopaholic_orders` + `lovata_orders_shopaholic_statuses` hermetic tables via Schema::create directly (mirror ShopaholicAdapterTestCase::bootOrdersTable + bootOrdersStatuses — copy the helper bodies inline). OR extend ShopaholicAdapterTestCase instead of EventSubjectAdapterContractTestCase — but the contract base is what supplies the 10 invariants. **Resolution:** Subclass uses trait composition: keep `extends EventSubjectAdapterContractTestCase`, paste the bootOrdersTable + bootOrdersStatuses bodies into a private setUp helper. Lower cognitive surface; ContextTestCase test isolation guaranteed.

Implement two abstract factories:
- `protected function makeAdapter(): EventSubjectAdapter { return new ShopaholicOrderAdapter; }`
- `protected function makeSubject(): object` — insert a row into the hermetic Orders table with site_id=1, status_id=5, secret_key='test-secret-abc', total_price_value=12.50, currency_id=null, email='a@b.test', name='A', last_name='B', phone='123'. Use `Order::query()->create([...])` OR direct DB insert + `Order::find($id)`. Return the loaded Order instance.

10 invariants are inherited from the base — Pest auto-discovers + runs them.

Create `tests/Unit/Adapter/Shopaholic/ShopaholicOrderValueResolverTest.php`. Namespace `Logingrupa\Metapixel\Tests\Unit\Adapter\Shopaholic`. `#[Group('adapter')]`. Class extends ShopaholicAdapterTestCase via Pest.php auto-binding. `protected $autoMigrate = true;` + setUp boots Orders+Statuses tables. Test methods:
- `test_resolve_value_returns_total_price_value_as_float` — Create Order with total_price_value=99.50; assert resolveValue returns float 99.50.
- `test_resolve_currency_uses_relation_first_when_present` — Mock Currency model row + Order with currency_id wired; assert returns 'EUR'.
- `test_resolve_currency_falls_back_to_currency_code_attribute` — Order with no currency relation but currency_code='NOK' attribute set; assert returns 'NOK'.
- `test_resolve_currency_falls_back_to_settings_default` — Order with neither relation nor field; set `Settings::set(['default_currency_code' => 'USD'])`; assert returns 'USD'.
- `test_resolve_currency_throws_when_no_fallback_available` — Order with no relation, no field, no Settings default; assert OrderHasNoCurrencyException is thrown.
- `test_resolve_content_ids_returns_empty_when_no_positions` — Order with no order_position; assert resolveContentIds returns `[]`.
- `test_resolve_num_items_sums_quantity_across_positions` — Create OrderPositions with quantities 2 + 3; assert resolveNumItems returns 5. (May require hermetic order_position table — provision via Schema::create in setUp.)

NOTE: Some of these tests need the full Lovata Currency / Offer / OrderPosition / Product hermetic schema. Scope rule: implement the tests that can run hermetically with the existing 2-table fixture set; for tests that require Currency / Offer / Product (SKU format), provision those tables hermetically in test setUp via Schema::create with the minimal column set the resolver reads. Each test isolates its provisioning. Keep total LOC reasonable (< 250).

Create `tests/Unit/Event/Adapter/Shopaholic/OrderStatusWatcherTest.php`. Namespace `Logingrupa\Metapixel\Tests\Unit\Event\Adapter\Shopaholic`. `#[Group('adapter')]`. Extends MetapixelTestCase (the Watcher uses Order::class as a string for event keys; the test exercises the handler directly without needing real Eloquent eloquent.updated firing). `protected $autoMigrate = true;` + Schema::create the hermetic Orders+Statuses tables in setUp. Bind AdapterRegistry + register ShopaholicOrderAdapter for Order::class. Use `Illuminate\Support\Facades\Bus::fake()` so SendCapiEvent::dispatch is asserted. Test methods:
- `test_dispatches_purchase_on_paid_status_match_with_status_changed` — Create Order with status_id=5 (paid), invoke `(new OrderStatusWatcher)->handle($obOrder)` after marking `$obOrder->syncOriginalAttribute('status_id') === 1` so wasChanged returns true. Assert `Bus::assertDispatched(SendCapiEvent::class)` with the dispatched job's sEventName === 'Purchase' AND sAdapterClass === ShopaholicOrderAdapter::class.
- `test_does_not_dispatch_when_status_code_does_not_match_paid_setting` — Order with status_id=3 (complete, not paid); handle returns silently; `Bus::assertNotDispatched(SendCapiEvent::class)`.
- `test_does_not_dispatch_when_status_unchanged` — Order already persisted at status_id=5, handle invoked on update where status_id wasn't touched (`wasChanged('status_id') === false`); assert no dispatch.
- `test_catches_payload_build_exception_logs_warning_does_not_rethrow` — Force ShopaholicOrderValueResolver::resolveCurrency to throw (e.g. Order with no currency anywhere); assert handle() returns without throwing; `Log::shouldReceive('warning')->once();` (Mockery).

Create `tests/Feature/Plugin/ShopaholicConditionalRegistrationTest.php`. Namespace `Logingrupa\Metapixel\Tests\Feature\Plugin`. Extends MetapixelTestCase. Two test methods:
**Testing strategy (W2 lock — revision iteration 1): fake PluginManager via container instance binding.**

The test MUST bind a Mockery double of `System\Classes\PluginManager` into the Laravel container via `$this->app->instance(PluginManager::class, $obFakePluginManager);`. The double stubs `exists($sPluginCode)` to return the test-driven boolean. The test then calls `(new Plugin($this->app))->boot();` and asserts the AdapterRegistry state matches the expected branch.

`Mockery::mock('overload:System\\Classes\\PluginManager')` is EXPLICITLY DROPPED — it requires `runInSeparateProcess` PHPUnit attribute, polluting test isolation across the suite, and is not how the rest of the plugin's test base wires fakes.

The conditional registration in `Plugin.php` (Task 4 — see action update below) MUST route through a protected `isShopaholicEnabled(): bool` helper that the test fakes via the container instance binding, so the bool path is driven by the faked `PluginManager::exists()` return value.

- `test_adapter_registered_when_plugin_manager_reports_exists_true` — Bind a Mockery PluginManager double via `$this->app->instance(PluginManager::class, $obFakePM)` where `$obFakePM->shouldReceive('exists')->with('Lovata.OrdersShopaholic')->andReturn(true)`. Construct a fresh AdapterRegistry instance + bind it. Call `(new Plugin($this->app))->boot();`. Assert `App::make(AdapterRegistry::class)->resolveByClass(Lovata\OrdersShopaholic\Models\Order::class) === ShopaholicOrderAdapter::class` (or equivalent registry inspection method exposed in Phase 2).
- `test_adapter_not_registered_when_plugin_manager_reports_exists_false` — Same setup but `andReturn(false)`. After Plugin::boot, assert `App::make(AdapterRegistry::class)->resolveByClass(Lovata\OrdersShopaholic\Models\Order::class)` returns null OR the registry is empty. **Note**: this is the minimal-install regression guard for SHOP-04.

The `isShopaholicEnabled()` helper introduced in Task 4 is what makes this binding testable — Task 5 ONLY writes the test asserting the helper's behavior under the faked PluginManager. The test file does NOT redefine the helper or alter Plugin.php.

All four test files declare `#[Group('adapter')]` at the class level via PHP attribute. Use `use PHPUnit\Framework\Attributes\Group;`.
  </action>
  <verify>
    <automated>cd /home/forge/nailscosmetics.lv/plugins/logingrupa/metapixel && /home/forge/nailscosmetics.lv/vendor/bin/pest --compact tests/Contract/Adapter/Shopaholic/ShopaholicOrderAdapterContractTest.php tests/Unit/Adapter/Shopaholic/ShopaholicOrderValueResolverTest.php tests/Unit/Event/Adapter/Shopaholic/OrderStatusWatcherTest.php tests/Feature/Plugin/ShopaholicConditionalRegistrationTest.php 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - All 4 test files exist with `#[Group('adapter')]` class attribute (`grep -l "Group('adapter')" tests/Contract/Adapter/Shopaholic/ShopaholicOrderAdapterContractTest.php tests/Unit/Adapter/Shopaholic/ShopaholicOrderValueResolverTest.php tests/Unit/Event/Adapter/Shopaholic/OrderStatusWatcherTest.php tests/Feature/Plugin/ShopaholicConditionalRegistrationTest.php` lists all 4).
    - Contract test runs all 10 invariants from the base + passes for ShopaholicOrderAdapter.
    - ValueResolver test covers SKU format on multi-offer vs single-offer paths + currency fallback chain + OrderHasNoCurrencyException throw.
    - Watcher test covers (a) dispatch on paid match, (b) no dispatch when status_code mismatch, (c) no dispatch when status_id unchanged (Pitfall 2), (d) log + return on payload-build exception (no rethrow).
    - Conditional registration test asserts BOTH (a) plugin-present path registers and (b) plugin-absent path does NOT register.
    - All 4 test files pass: `pest --compact` exit code 0.
    - Minimal-install simulation: `pest --exclude-group=adapter --compact` runs without picking up these 4 files (verifiable via test counts staying constant when excluded).
  </acceptance_criteria>
  <done>Contract test (10 invariants), ValueResolver unit test (SKU + currency chain), Watcher unit test (4 dispatch scenarios), conditional registration feature test — all 4 green; SHOP-01..04 closed.</done>
</task>

<task type="auto">
  <name>Task 6: composer qa green + atomic commit</name>
  <files>(QA gate)</files>
  <read_first>
    - composer.json (qa script chain)
    - All files modified in Tasks 1-5
  </read_first>
  <action>
Run `vendor/bin/pint --dirty --format agent`. Then run `composer qa` (pint-test → phpstan level 10 → phpmd → pest --coverage --min=90). Run `composer deps` to verify composer-dependency-analyser passes with the new whitelist. Fix any violations without `@phpstan-ignore`. On green, single atomic commit `feat(03-02): ShopaholicOrderAdapter + ValueResolver + OrderStatusWatcher (SHOP-01..04)`.
  </action>
  <verify>
    <automated>cd /home/forge/nailscosmetics.lv/plugins/logingrupa/metapixel && composer qa 2>&1 | tail -20 && composer deps 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `composer qa` exits 0 (full chain — pint-test + phpstan level 10 + phpmd + pest --coverage --min=90).
    - `composer deps` (composer-dependency-analyser) exits 0; reports zero violations.
    - `! grep -rn "@phpstan-ignore" classes/ models/ Plugin.php` returns no matches.
    - Single atomic commit on master with `git status` clean.
  </acceptance_criteria>
  <done>SHOP-01..04 closed; composer qa + composer deps green; Plan 03-03 can proceed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin → Order.status_id | Backend admin user trusted; flips status_id through Lovata.OrdersShopaholic admin UI. |
| Order → Adapter → Queue | Order row attributes flow through ShopaholicOrderAdapter into SendCapiEvent payload; no untrusted input in this path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02-01 | Tampering | subject_type written to EventLog | mitigate | Adapter returns opaque alias `'shopaholic.order'` via `getSubjectType`; EventLogWriter writes alias (P-05 anchor). |
| T-03-02-02 | Tampering | site_id resolved at dispatch | mitigate | `getSiteId` reads ONLY `Order.site_id`; phpstan disallowed-calls bans Site / SiteManager / Request inside `classes/adapter/shopaholic/*` + `classes/event/adapter/shopaholic/*` (P-01 anchor). |
| T-03-02-03 | Information Disclosure | PII fields (email, name, phone) in Watcher payload | accept | PII is sha256-hashed by UserDataHasher before Meta send; in-flight via Phase 2 lock. |
| T-03-02-04 | Spoofing | Class FQN as subject_type | mitigate | `getSubjectType` returns const string, NOT `get_class($obSubject)`; contract test invariant 01 asserts no backslashes (P-05). |
| T-03-02-05 | DoS | Order::save cascade-failure on Watcher exception | mitigate | Tiger-Style log + return; no rethrow (would cascade-break Lovata OrderProcessor / Campaign / PromoMechanism). Test in Task 5 asserts. |
| T-03-02-06 | Tampering | Hidden Lovata import outside adapter dir | mitigate | composer-dependency-analyser config restricts `Lovata\OrdersShopaholic\*` + `Lovata\Shopaholic\*` + `Lovata\Buddies\*` imports to `classes/adapter/shopaholic/` + `classes/event/adapter/shopaholic/` only (P-03 anchor, Task 1 widens whitelist for the watcher subdir). |
| T-03-02-07 | DoS | Boot-time autoload race on Lovata-absent install | mitigate | `PluginManager::instance()->exists('Lovata.OrdersShopaholic')` gate is true only when plugin is FOUND + ENABLED (P-11 anchor). Tested in Task 5. |
| T-03-02-SC | Tampering | npm/composer install supply chain | accept | Phase 3 ships ZERO new external packages (RESEARCH §"Package Legitimacy Audit"). |
</threat_model>

<verification>
- `composer qa` + `composer deps` both exit 0.
- `pest tests/Contract/Adapter/Shopaholic/ShopaholicOrderAdapterContractTest.php --compact` runs all 10 invariants green for ShopaholicOrderAdapter.
- `pest tests/Unit/Adapter/Shopaholic/ShopaholicOrderValueResolverTest.php --compact` covers SKU format (single + multi offer) + currency 4-step fallback chain + exception path.
- `pest tests/Unit/Event/Adapter/Shopaholic/OrderStatusWatcherTest.php --compact` covers paid-match dispatch + miss + no-status-change + catch-log-return-no-rethrow.
- `pest tests/Feature/Plugin/ShopaholicConditionalRegistrationTest.php --compact` covers both PluginManager::exists paths (true/false).
- Minimal-install simulation: `pest --exclude-group=adapter --compact` excludes the 4 new test files (Run B regression guard).
- Static-source defence: `grep -rE '(Site|SiteManager|Request)::|request\\(' classes/adapter/shopaholic/ classes/event/adapter/shopaholic/` returns NO matches.
</verification>

<success_criteria>
- SHOP-01 closed: ShopaholicOrderAdapter satisfies EventSubjectAdapter; returns 'shopaholic.order'; reads Order.site_id only.
- SHOP-02 closed: ShopaholicOrderValueResolver returns SKU-{product_id}[-{offer_id}] matching FacebookCatalog exporter byte-for-byte; currency 4-step fallback chain ends with OrderHasNoCurrencyException throw.
- SHOP-03 closed: OrderStatusWatcher binds eloquent.updated|created on Order, dispatches Purchase on paid match + status_id change, Tiger-Style log + return on payload failure, ≤ 70 LOC.
- SHOP-04 closed: Plugin::boot conditional registration via PluginManager::instance()->exists gate; composer-dependency-analyser enforces zero Lovata\OrdersShopaholic\* imports outside two adapter/event subdirs.
- All 4 new test files green (contract + 2 unit + 1 feature); each tagged `#[Group('adapter')]`.
- composer qa exits 0; phpstan level 10 zero errors; no `@phpstan-ignore`; coverage stays ≥ 90%.
</success_criteria>

<output>
Create `.planning/phases/03-shopaholicadapter-themeactionadapter-parallel-wave/03-02-SUMMARY.md` when done.
</output>
