---
phase: 5
plan: 06
plan_id: 05-06
type: execute
wave: 5
depends_on: [05-04]
files_modified:
  - themes/logingrupa-naisstore/pages/order-complete.htm
  - themes/logingrupa-naisstore/pages/order-complete-proforma.htm
  - .planning/phases/05-documentation-marketplace-launch/05-06-UAT-GATE-3.md
autonomous: false
threat_refs: []
requirements: [DOCS-01]

must_haves:
  truths:
    - "order-complete.htm and order-complete-proforma.htm each declare the `eventPixel` component in INI section and render it via `{% component 'eventPixel' %}` in the page body — `eventPixel` is the ONLY alias registered for Logingrupa\\Metapixel\\Components\\EventPixel in Plugin.php line 111; the dead v1.x `purchasePixel` alias was removed in plan 05-02 Task 2"
    - "Component properties pin to the Shopaholic Order subject: `subject_class = 'Lovata\\OrdersShopaholic\\Models\\Order'`, `subject_slug_field = 'secret_key'` (matches existing component property surface in EventPixel.php)"
    - "EventPixel rendering looks up the CAPI EventLog row server-side and emits an inline fbq('track','Purchase',...,{eventID:<uuid>}) browser invocation with the matching event_id when a row exists and the channel=pixel row is still absent — preserving the dedup contract (D-04)"
    - "No PixelHead duplication added in these pages — pixelHead already runs at layout level (05-04); EventPixel is the per-event pixel on top of base PageView"
    - "Operator places a real test order on your-staging-host.example (real Lovata stack, test_event_code set) and the EventLog table contains EXACTLY 2 rows for the order's Purchase event: channel='capi' AND channel='pixel' with the SAME event_id (UUID v4)"
    - "Three-source convergence: Pixel Helper shows Purchase with the same eventID; Test Events live view shows Purchase 'Deduplicated'; EventLog returns the 2 rows"
    - "Without operator PASS on UAT Gate 3, plan 05-08 (live smoke) MUST NOT execute"
    - "Cutover gate is operator-confirmed per D-03 — autonomous: false, resume signal required."
    - "UAT verification combines D-05 three sources: Meta Pixel Helper, Meta Test Events live view, `logingrupa_metapixel_event_log` DB tail."
  artifacts:
    - path: "themes/logingrupa-naisstore/pages/order-complete.htm"
      provides: "Purchase event server-confirmed browser pixel via EventPixel"
      contains: "[eventPixel]"
    - path: "themes/logingrupa-naisstore/pages/order-complete-proforma.htm"
      provides: "same"
      contains: "[eventPixel]"
    - path: ".planning/phases/05-documentation-marketplace-launch/05-06-UAT-GATE-3.md"
      provides: "Operator-signed event_id round-trip log with EventLog query results, Pixel Helper / Test Events evidence"
  key_links:
    - from: "themes/logingrupa-naisstore/pages/order-complete*.htm"
      to: "plugins/logingrupa/metapixel/components/EventPixel.php"
      via: "{% component 'eventPixel' %}"
      pattern: "component\\s+'eventPixel'"
    - from: "EventPixel.onRun"
      to: "logingrupa_metapixel_event_log (subject_type='shopaholic.order', channel='capi')"
      via: "Direct DB read per Phase 3 D-09"
      pattern: "EventLog::where|DB::table.*logingrupa_metapixel_event_log"
---

<objective>
Two-task plan: Task 1 wires the `EventPixel` plugin component onto the two order-complete pages (after plan 05-02 removed the dead v1.x `[purchasePixel]` block). Task 2 is the operator-confirmed UAT Gate 3 — operator places a real test order on `your-staging-host.example` and verifies the Purchase event_id round-trips across all three sources. After Gate 3 PASS, the cutover wave is complete and plan 05-08 (live smoke for screenshots + README anchor) can run.

Purpose: Close the dedup loop on Purchase — the commercially most important Phase 3 event. AddToCart + ViewContent are deferred to post-launch per D-07; live smoke covers ViewContent via Theme Twig API (already shipped Phase 3). Three-source convergence on the SAME event_id is the dedup contract anchor.

Output: 2 page edits + 1 UAT log markdown. Single auto commit for page wiring; UAT log committed after operator signal.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-documentation-marketplace-launch/05-CONTEXT.md
@.planning/phases/05-documentation-marketplace-launch/05-RESEARCH.md
@.planning/phases/05-documentation-marketplace-launch/05-PATTERNS.md
@.planning/phases/05-documentation-marketplace-launch/05-04-SUMMARY.md
@plugins/logingrupa/metapixel/components/EventPixel.php
@plugins/logingrupa/metapixel/Plugin.php
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL slug → EventPixel component → DB | secret_key is the only operator-supplied input on the page; component already validates per Phase 3 THEM-06 |
| operator → UAT log | Operator-authored markdown; no executable surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-06-01 | Tampering | secret_key URL slug | accept | Phase 3 THEM-06 already validates event_id server-side; un-injectable per D-09 lock |
| T-05-06-02 | Information Disclosure | inline event_id in HTML script | accept | event_id is UUID v4 random — no PII; Meta-side dedup anchor only |
| T-05-06-03 | Tampering | event_id mismatch between channels | mitigate | If event_id differs across Pixel Helper / Test Events / EventLog, Meta will NOT dedup → Conversions metric inflates → FAIL gate, open /gsd-debug |
| T-05-06-04 | Repudiation | undocumented test order on staging | accept | Operator notes order ID, secret_key, place + cancel timestamps in UAT log |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Wire [eventPixel] onto order-complete.htm + order-complete-proforma.htm</name>
  <read_first>
    - themes/logingrupa-naisstore/pages/order-complete.htm (full file — verify the existing INI declares whatever Shopaholic components remain after plan 05-02's strip; the dead `[purchasePixel]` block was deleted in 05-02 Task 2 and MUST be replaced with `[eventPixel]`)
    - themes/logingrupa-naisstore/pages/order-complete-proforma.htm (same shape — verify INI + body; insert `[eventPixel]` block matching the order-complete.htm pattern)
    - plugins/logingrupa/metapixel/Plugin.php (registerComponents — lines 108-114 confirm `eventPixel` is the ONLY alias mapped to `Logingrupa\\Metapixel\\Components\\EventPixel`; `purchasePixel` is NOT registered)
    - plugins/logingrupa/metapixel/components/EventPixel.php (defineProperties — confirm required properties `subject_class` + `subject_slug_field` + their defaults; confirm onRun reads `secret_key` from page route params)
    - .planning/phases/05-documentation-marketplace-launch/05-PATTERNS.md lines 600-610 (Pass 3 EventPixel insertion table)
    - .planning/phases/05-documentation-marketplace-launch/05-02-SUMMARY.md (confirms `purchasePixel` strip completed)
  </read_first>
  <action>
    The component alias is `eventPixel` (verified in Plugin.php line 111). Do NOT use `purchasePixel` — that alias was the v1.x dead-name and was removed from the theme in plan 05-02 Task 2; it is NOT registered by Plugin.php so its use would throw "Unknown component" at render.

    For `themes/logingrupa-naisstore/pages/order-complete.htm`:
    - INI section: insert a new `[eventPixel]` declaration block (the prior `[purchasePixel]` block was deleted by plan 05-02 Task 2; insert `[eventPixel]` in the same general position alongside `[OrderPage]` and `[RetryPayment]`).
    - Set properties: `subject_class = "Lovata\\OrdersShopaholic\\Models\\Order"` (match the backslash-escaping syntax used by other Shopaholic component declarations in the theme) and `subject_slug_field = "secret_key"`.
    - Body: insert `{% component 'eventPixel' %}` at the position where the prior `{% component 'purchasePixel' %}` render used to be (line 18 before plan 05-02 strip), placing it after the order summary partial `{% partial 'order/order-complete/order-complete' obOrder=obOrder %}`.

    For `themes/logingrupa-naisstore/pages/order-complete-proforma.htm`: same shape — `[eventPixel]` block with the same two properties, `{% component 'eventPixel' %}` render at the equivalent body location (after `{% partial 'order/order-complete/order-complete-for-printing' %}`).

    Do NOT touch any other pages. AddToCart + ViewContent placements are deferred (D-07); ViewContent live-fires via the Theme Twig API on PDP renders (Phase 3 THEM-04) and does not need an explicit EventPixel placement.

    Commit message draft: `feat(05-06): wire EventPixel on order-complete + order-complete-proforma (Purchase event browser-side dedup)`.
  </action>
  <verify>
    <automated>cd /home/forge/nailscosmetics.lv && bash -c 'for f in order-complete order-complete-proforma; do grep -qE "^\[eventPixel\]" themes/logingrupa-naisstore/pages/$f.htm && grep -qE "component\s+'"'"'eventPixel'"'"'" themes/logingrupa-naisstore/pages/$f.htm || { echo fail-$f; exit 1; }; done; [ "$(grep -c purchasePixel themes/logingrupa-naisstore/pages/order-complete.htm themes/logingrupa-naisstore/pages/order-complete-proforma.htm 2>/dev/null | awk -F: "{s+=\$2} END {print s+0}")" = "0" ] && echo ok'</automated>
  </verify>
  <done>
    Both order-complete pages contain a `[eventPixel]` INI block (EXACT alias — regex `^\[eventPixel\]`) with `subject_class` + `subject_slug_field` properties pinned to the Shopaholic Order shape, AND a `{% component 'eventPixel' %}` (EXACT alias — single-quoted `eventPixel`) render call in the body. Both files contain ZERO occurrences of the dead `purchasePixel` alias (verified — alias was stripped in plan 05-02 Task 2). Page renders without 500 on a test order's order-complete URL (functional verification at UAT Gate 3 in Task 2).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: UAT Gate 3 — Deploy + place test order + verify event_id round-trip + author UAT-GATE-3 log</name>
  <read_first>
    - Task 1 commit SHA from this plan
    - .planning/phases/05-documentation-marketplace-launch/05-PATTERNS.md lines 605-610 (UAT Gate 3 pass criterion template)
    - .planning/phases/05-documentation-marketplace-launch/05-RESEARCH.md Pitfall 4 (three-source convergence) + Pattern 1 (gated UAT cutover)
    - .planning/phases/05-documentation-marketplace-launch/05-CONTEXT.md D-03 + D-04 + D-05 (locked decisions)
  </read_first>
  <what-built>
    Task 1 wired the EventPixel component onto order-complete.htm + order-complete-proforma.htm using the registered `eventPixel` alias (Plugin.php line 111). EventPixel reads the order via secret_key, looks up the CAPI EventLog row, and emits the matching browser-side fbq('track','Purchase',..,{eventID:<server-supplied-uuid>}) so Meta dedupes the browser event against the server-side CAPI dispatch (±10s window).
  </what-built>
  <how-to-verify>
    Operator steps:

    Step A — Deploy Task 1 commit to `your-staging-host.example`.

    Step B — Confirm Settings has a non-empty `pixel_id` AND `capi_access_token` AND `test_event_code` set (Backend → Settings → Marketing → Meta Pixel + CAPI). Use the staging test_event_code so events route to Meta Test Events live view, NOT production analytics.

    Step C — Place a real test order:
      1. Visit `https://your-staging-host.example/lv/catalog` in a fresh browser session with Meta Pixel Helper open
      2. Add any in-stock product to cart
      3. Proceed through checkout — use a clearly-flagged test email
      4. Complete payment (whatever staging payment method `your-staging-host.example` is configured with)
      5. Land on `/order-complete?secret_key=...`

    Step D — Verify three sources:
      1. Pixel Helper popup — Purchase event present with `eventID` field populated; copy the UUID
      2. Meta Test Events live view (Events Manager → Test Events tab → Filter by Pixel ID) — Purchase event present with "Deduplicated" label; click row to expand → event_id matches the UUID from Pixel Helper
      3. EventLog DB query: `SELECT id, event_id, event_name, channel, subject_type, subject_id, site_id, created_at FROM logingrupa_metapixel_event_log WHERE subject_type='shopaholic.order' AND event_name='Purchase' ORDER BY id DESC LIMIT 2` — MUST return exactly 2 rows for the newly placed order, both with the SAME event_id UUID matching Pixel Helper / Test Events value, channels capi + pixel respectively

    Step E — Author `.planning/phases/05-documentation-marketplace-launch/05-06-UAT-GATE-3.md`:

    ```
    # UAT Gate 3 — Purchase event_id round-trip

    **Date:** YYYY-MM-DD HH:MM
    **Env:** https://your-staging-host.example
    **Deploy SHA:** <commit-sha-from-task-1>
    **Operator:** <name>
    **Test order:** order ID = <N>, secret_key = <hash>, test email = <…>

    ## Three-source convergence

    | Source | event_id | event_name | Notes |
    |--------|----------|-----------|-------|
    | Pixel Helper | <uuid> | Purchase | eventID field populated |
    | Test Events live view | <uuid> | Purchase | Deduplicated label present |
    | EventLog channel=capi | <uuid> | Purchase | subject_type='shopaholic.order' |
    | EventLog channel=pixel | <uuid> | Purchase | same event_id, channel=pixel |

    All four <uuid> values identical: YES / NO

    ## Anomalies (if any)

    (free-form, route to /gsd-debug per D-09 if any non-PASS)

    ## Overall verdict

    GATE 3 PASS  (or  GATE 3 FAIL)
    ```

    Step F — Refund/cancel the test order after UAT to keep the test_event_code panel clean.
  </how-to-verify>
  <resume-signal>
    Type `GATE 3 PASS` to advance to plan 05-08 (live smoke). If FAIL, describe failing source + open `/gsd-debug` per D-09.
  </resume-signal>
</task>

</tasks>

<verification>
- 2 page files edited, each contains `[eventPixel]` INI block (exact alias) + `{% component 'eventPixel' %}` render
- Both pages contain zero `purchasePixel` references (alias removed in plan 05-02)
- Properties pin to Lovata\OrdersShopaholic\Models\Order subject and secret_key slug field
- `05-06-UAT-GATE-3.md` exists, signed, with 4 source rows showing the SAME event_id UUID
- Operator types `GATE 3 PASS`
</verification>

<success_criteria>
- Same event_id observable across 4 sources (Pixel Helper, Test Events, EventLog capi, EventLog pixel)
- Cutover wave (plans 05-02..05-06) complete; plugin + theme observably emit deduped Purchase events
- UAT log committed
</success_criteria>

<output>
Create `.planning/phases/05-documentation-marketplace-launch/05-06-SUMMARY.md` when done — note Task 1 commit SHA, properties set, UAT log path, test order ID, event_id sample, operator name + date, PASS status. Flag if any anomaly was routed to `/gsd-debug` (D-09) so plan 05-08 can wait on the fix.
</output>
</content>
