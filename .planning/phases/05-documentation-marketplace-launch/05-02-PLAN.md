---
phase: 5
plan: 02
plan_id: 05-02
type: execute
wave: 2
depends_on: [05-00]
files_modified:
  - .planning/phases/05-documentation-marketplace-launch/05-02-LEGACY-INVENTORY.md
  - themes/logingrupa-naisstore/partials/facebook_pixel.htm
  - themes/logingrupa-naisstore/layouts/main.htm
  - themes/logingrupa-naisstore/layouts/content.htm
  - themes/logingrupa-naisstore/layouts/light.htm
  - themes/logingrupa-naisstore/layouts/catalog_default.htm
  - themes/logingrupa-naisstore/pages/checkout.htm
  - themes/logingrupa-naisstore/pages/order-complete.htm
  - themes/logingrupa-naisstore/pages/order-complete-proforma.htm
  - themes/logingrupa-naisstore/partials/form/checkout-form/tracking/facebook-purchase-tracking.js
  - themes/logingrupa-naisstore/partials/shared/tracking/facebook-add-to-cart.js
  - themes/logingrupa-naisstore/partials/shared/tracking/facebook-view-content.js
  - themes/logingrupa-naisstore/partials/shared/controls/add-to-cart-control.js
  - themes/logingrupa-naisstore/partials/shared/controls/product-detail-control.js
  - themes/logingrupa-naisstore/partials/product/search-result/search.js
  - themes/logingrupa-naisstore/configs/fields.yaml
  - themes/logingrupa-naisstore/assets/js/common.js
autonomous: true
threat_refs: []
requirements: [DOCS-01]

must_haves:
  truths:
    - "Inventory file `05-02-LEGACY-INVENTORY.md` enumerates every legacy fbq emission site in themes/logingrupa-naisstore/ (3 patterns + bundle reach + theme settings yaml)"
    - "After strip, `grep -rnE 'fbq\\(|fbevents\\.js|connect\\.facebook\\.net' themes/logingrupa-naisstore/ --include='*.htm' --include='*.js' --exclude-dir='node_modules'` returns ZERO hits in source files"
    - "After `pnpm run prod` rebuild, `grep -aoE 'fbq\\(' themes/logingrupa-naisstore/assets/js/common.js` returns ZERO hits in the bundled artifact"
    - "ALL `[purchasePixel]` INI blocks AND `{% component 'purchasePixel' %}` renders are deleted from order-complete.htm AND order-complete-proforma.htm — `purchasePixel` is dead v1.x markup (Plugin.php registers only `eventPixel` + `pixelHead`); leaving it in place would 500-error the pages between Wave 3 and Wave 5 and block UAT Gate 1"
    - "Theme renders without 500 on /, /catalog, /product/<slug>, /checkout, /order-complete after strip (functional UAT in plan 05-03)"
    - "No reference to PixelHead or EventPixel introduced yet — those land in plans 05-04 and 05-06"
  artifacts:
    - path: ".planning/phases/05-documentation-marketplace-launch/05-02-LEGACY-INVENTORY.md"
      provides: "Markdown table inventory consumed by the strip steps in this same plan"
    - path: "themes/logingrupa-naisstore/partials/facebook_pixel.htm"
      provides: "DELETED"
    - path: "themes/logingrupa-naisstore/partials/form/checkout-form/tracking/facebook-purchase-tracking.js"
      provides: "DELETED"
    - path: "themes/logingrupa-naisstore/partials/shared/tracking/facebook-add-to-cart.js"
      provides: "DELETED"
    - path: "themes/logingrupa-naisstore/partials/shared/tracking/facebook-view-content.js"
      provides: "DELETED"
    - path: "themes/logingrupa-naisstore/pages/order-complete.htm"
      provides: "Stripped of legacy fbq + dead-v1.x [purchasePixel] block and render"
    - path: "themes/logingrupa-naisstore/pages/order-complete-proforma.htm"
      provides: "Stripped of legacy fbq + any dead-v1.x [purchasePixel] block and render"
    - path: "themes/logingrupa-naisstore/assets/js/common.js"
      provides: "Rebuilt bundle — no fbq strings"
  key_links:
    - from: "themes/logingrupa-naisstore/partials/shared/controls/add-to-cart-control.js"
      to: "(removed import)"
      via: "import { trackFacebookAddToCart } from '../tracking/facebook-add-to-cart' → DELETED"
      pattern: "import.*trackFacebookAddToCart"
    - from: "themes/logingrupa-naisstore/partials/shared/controls/product-detail-control.js"
      to: "(removed import)"
      via: "import { trackFacebookViewContent } from '../tracking/facebook-view-content' → DELETED"
      pattern: "import.*trackFacebookViewContent"
---

<objective>
Inventory every legacy Facebook Pixel emission site in `themes/logingrupa-naisstore/` (Task 0), then execute the strip (Tasks 1-3). After this plan, the theme emits ZERO `fbq(...)` calls from both source files and the compiled webpack bundle, AND zero references to the dead v1.x `purchasePixel` component alias remain. NO replacement component is added — that lands in plans 05-04 (PixelHead) and 05-06 (EventPixel), gated by UAT in 05-03 / 05-04 task 2 / 05-06 task 2.

Purpose: Clean baseline for UAT Gate 1. Pixel Helper Chrome extension must show 0 events across all page-load surfaces before any new emission lands. The legacy `[purchasePixel]` INI block in `order-complete.htm` (line 10) and the `{% component 'purchasePixel' %}` render (line 18) reference an unregistered alias — leaving them in place would 500-error the page after deploy, blocking UAT measurement. Strip is irreversible-by-design — gated UAT cutover pattern (D-03).

Output: 1 inventory markdown file + 4 file deletes + 11 file edits + 1 bundle rebuild. Two feature commits: (a) inventory-only commit; (b) strip + bundle commit. Theme still renders normally; legacy fbq emissions + dead-v1.x purchasePixel refs gone.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-documentation-marketplace-launch/05-CONTEXT.md
@.planning/phases/05-documentation-marketplace-launch/05-RESEARCH.md
@.planning/phases/05-documentation-marketplace-launch/05-PATTERNS.md
@themes/logingrupa-naisstore/partials/facebook_pixel.htm
@themes/logingrupa-naisstore/webpack.mix.js
@plugins/logingrupa/metapixel/Plugin.php
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Strip removes existing emission surface; no new input/output handler |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02-01 | Repudiation | git commit log | accept | Strip change is large but mechanical; commit message + inventory file provide audit trail |
| T-05-02-02 | Denial of Service | theme render after strip (dead `purchasePixel` alias would 500) | mitigate | Task 2 explicitly deletes `[purchasePixel]` INI + render from both order-complete pages — Plugin.php registers only `eventPixel` + `pixelHead`, so leaving `purchasePixel` references in place would throw "Unknown component"; UAT Gate 1 (plan 05-03) cannot measure zero events on a 500-erroring page |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 0: Inventory legacy fbq sites + write 05-02-LEGACY-INVENTORY.md</name>
  <read_first>
    - themes/logingrupa-naisstore/partials/facebook_pixel.htm (7 LOC partial, current state)
    - themes/logingrupa-naisstore/layouts/main.htm (line ~149 partial include)
    - themes/logingrupa-naisstore/layouts/content.htm (line ~104 include)
    - themes/logingrupa-naisstore/layouts/light.htm (line ~76 include)
    - themes/logingrupa-naisstore/layouts/catalog_default.htm (line ~100 include)
    - themes/logingrupa-naisstore/pages/checkout.htm (lines 91-99 inline fbq InitiatedCheckout)
    - themes/logingrupa-naisstore/pages/order-complete.htm (lines 10, 18, 20-35 — `[purchasePixel]` INI block + `{% component 'purchasePixel' %}` render + disabled fbq trackCustom block)
    - themes/logingrupa-naisstore/pages/order-complete-proforma.htm (lines 13-26 fbq trackCustom ViewdOrderProformaPage; verify whether a `[purchasePixel]` block also exists in this file)
    - themes/logingrupa-naisstore/partials/product/search-result/search.js (lines 33-40 `_fbq('track','Search')` block)
    - themes/logingrupa-naisstore/partials/form/checkout-form/tracking/facebook-purchase-tracking.js
    - themes/logingrupa-naisstore/partials/shared/tracking/facebook-add-to-cart.js
    - themes/logingrupa-naisstore/partials/shared/tracking/facebook-view-content.js
    - themes/logingrupa-naisstore/partials/shared/controls/add-to-cart-control.js (line 13 import)
    - themes/logingrupa-naisstore/partials/shared/controls/product-detail-control.js (line 13 import)
    - themes/logingrupa-naisstore/configs/fields.yaml (facebook_pixel_id + facebook_domain_verification_id fields)
    - plugins/logingrupa/metapixel/Plugin.php registerComponents (lines 108-114) — confirms only `eventPixel` + `pixelHead` are registered; `purchasePixel` is dead alias
    - .planning/phases/05-documentation-marketplace-launch/05-RESEARCH.md lines 419-437 (Runtime State Inventory baseline)
  </read_first>
  <action>
    Run four independent greps from project root. Pattern 1 (Twig/HTML inline fbq): `grep -rnE 'fbq\(|fbevents\.js|connect\.facebook\.net' themes/logingrupa-naisstore/ --include='*.htm' --include='*.html'`. Pattern 2 (JS source emission): `grep -rnE 'fbq\(|_fbq\(|trackFacebook|facebook-purchase-tracking|facebook-add-to-cart|facebook-view-content' themes/logingrupa-naisstore/ --include='*.js' --exclude-dir='node_modules' --exclude-dir='assets/js'`. Pattern 3 (Twig dataLayer dispatcher): `grep -rnE '\{% set fbq|dataLayer\s*=|gtag\(|fbq_payload' themes/logingrupa-naisstore/ --include='*.htm'`. Pattern 4 (dead v1.x `purchasePixel` component refs): `grep -rnE '\[purchasePixel\]|purchasePixel' themes/logingrupa-naisstore/pages/ themes/logingrupa-naisstore/layouts/ themes/logingrupa-naisstore/partials/`. Then verify bundle reach: `grep -aoE 'fbq\("track","[A-Za-z]+"' themes/logingrupa-naisstore/assets/js/common.js | sort -u`.

    Write `.planning/phases/05-documentation-marketplace-launch/05-02-LEGACY-INVENTORY.md` with six sections:

    Section 1 "Pattern 1 — Inline fbq in Twig (.htm)" — markdown table columns `file | line | snippet (first ~60 chars) | strip action` (values: `DELETE-file`, `delete-line(s) N..M`, `delete-block N..M`).

    Section 2 "Pattern 2 — JS source emission" — same column shape. Tracking helper modules = `DELETE-file`; their callers = `edit-line N (drop import + call site)`.

    Section 3 "Pattern 3 — Twig dataLayer dispatcher" — table OR explicit `(no hits)`.

    Section 4 "Dead v1.x `purchasePixel` component references" — table of every `[purchasePixel]` INI block and every `{% component 'purchasePixel' %}` render found. Each entry strip action = `delete-block N..M`. Cite Plugin.php registerComponents (lines 108-114) showing only `eventPixel` + `pixelHead` are registered, so `purchasePixel` would throw "Unknown component" if left in place.

    Section 5 "Webpack bundle reach" — record unique `fbq("track","X")` strings in `themes/logingrupa-naisstore/assets/js/common.js`; tie each back to a source file in Section 2; document rebuild command `cd themes/logingrupa-naisstore && pnpm run prod`.

    Section 6 "Theme settings cleanup (configs/fields.yaml)" — list the `facebook_pixel_id` and `facebook_domain_verification_id` form fields with line numbers and strip action.

    End with "Strip order for Tasks 1-3" ordered list: (1) delete tracking helper modules (leaves first); (2) edit callers to drop imports; (3) delete partial-include lines from layouts; (4) delete inline fbq blocks in pages; (5) delete dead v1.x `[purchasePixel]` INI blocks + render calls from order-complete*.htm; (6) delete configs/fields.yaml entries; (7) `pnpm run prod` rebuild; (8) re-grep verification (zero hits) before UAT gate 1.

    Commit the inventory file before starting Task 1 strip. Commit message draft: `docs(05-02): legacy Facebook Pixel inventory across themes/logingrupa-naisstore (strip prerequisite)`.
  </action>
  <verify>
    <automated>cd /home/forge/nailscosmetics.lv && test -f .planning/phases/05-documentation-marketplace-launch/05-02-LEGACY-INVENTORY.md && grep -v '^#' .planning/phases/05-documentation-marketplace-launch/05-02-LEGACY-INVENTORY.md | grep -cE '^\|' | awk '$1 >= 15 {print "ok"; exit 0} {print "fail: "$1" rows"; exit 1}'</automated>
  </verify>
  <done>
    Inventory file exists with ≥15 table rows total. Section 4 (dead v1.x purchasePixel refs) has ≥2 rows (the INI block on order-complete.htm line 10 and the render on line 18; symmetric on order-complete-proforma.htm if found). Strip-order ordered list ends with "re-grep zero hits". Inventory file committed before Task 1.
  </done>
</task>

<task type="auto">
  <name>Task 1: Delete tracking helper JS modules + drop their callers' imports</name>
  <read_first>
    - .planning/phases/05-documentation-marketplace-launch/05-02-LEGACY-INVENTORY.md (authoritative strip order from Task 0)
    - themes/logingrupa-naisstore/partials/form/checkout-form/checkout-form-validation.js (caller of facebook-purchase-tracking — already comments out the import per RESEARCH line 432)
    - themes/logingrupa-naisstore/partials/shared/controls/add-to-cart-control.js (line 13 import to drop)
    - themes/logingrupa-naisstore/partials/shared/controls/product-detail-control.js (line 13 import to drop)
    - themes/logingrupa-naisstore/partials/product/search-result/search.js (lines 33-40 block to delete; preserve surrounding ShopaholicSearch flow)
    - themes/logingrupa-naisstore/partials/shared/tracking/facebook-add-to-cart.js (target file)
    - themes/logingrupa-naisstore/partials/shared/tracking/facebook-view-content.js (target file)
    - themes/logingrupa-naisstore/partials/form/checkout-form/tracking/facebook-purchase-tracking.js (target file)
  </read_first>
  <action>
    DELETE 3 tracking helper modules via `git rm`: (a) `themes/logingrupa-naisstore/partials/form/checkout-form/tracking/facebook-purchase-tracking.js`; (b) `themes/logingrupa-naisstore/partials/shared/tracking/facebook-add-to-cart.js`; (c) `themes/logingrupa-naisstore/partials/shared/tracking/facebook-view-content.js`.

    EDIT 3 caller files: in `partials/shared/controls/add-to-cart-control.js` delete the import line for `trackFacebookAddToCart` and any call sites referencing that identifier. In `partials/shared/controls/product-detail-control.js` apply the symmetric edit for `trackFacebookViewContent`. In `partials/product/search-result/search.js` delete only the lines emitting `_fbq('track','Search',…)` (~lines 33-40); preserve the ShopaholicSearch flow that surrounds the block. After each edit re-grep the file to confirm zero `fbq|trackFacebook` substrings remain.

    DO NOT rebuild the webpack bundle yet — Task 3 does that after the .htm strips land so the bundle rebuild is a single atomic step.
  </action>
  <verify>
    <automated>cd /home/forge/nailscosmetics.lv && ! test -f themes/logingrupa-naisstore/partials/shared/tracking/facebook-add-to-cart.js && ! test -f themes/logingrupa-naisstore/partials/shared/tracking/facebook-view-content.js && ! test -f themes/logingrupa-naisstore/partials/form/checkout-form/tracking/facebook-purchase-tracking.js && grep -cE 'trackFacebookAddToCart|facebook-add-to-cart' themes/logingrupa-naisstore/partials/shared/controls/add-to-cart-control.js | awk '$1 == 0 {print "ok"; exit 0} {print "fail: "$1" stray refs"; exit 1}' || true</automated>
  </verify>
  <done>
    Three tracking module files are deleted. The three caller files contain no remaining `trackFacebook*` identifier OR `facebook-*-tracking` import path. `search.js` no longer contains `_fbq(` substring. Surrounding non-Facebook logic remains intact.
  </done>
</task>

<task type="auto">
  <name>Task 2: Strip Twig inline fbq + dead v1.x purchasePixel refs + delete facebook_pixel.htm partial + theme settings yaml</name>
  <read_first>
    - .planning/phases/05-documentation-marketplace-launch/05-02-LEGACY-INVENTORY.md (strip order from Task 0; Section 4 enumerates the dead `purchasePixel` refs)
    - themes/logingrupa-naisstore/partials/facebook_pixel.htm (7 LOC — full delete)
    - themes/logingrupa-naisstore/layouts/main.htm (line ~149 `{% partial 'facebook_pixel' obUser=obUser %}` to delete)
    - themes/logingrupa-naisstore/layouts/content.htm (line ~104 partial include to delete)
    - themes/logingrupa-naisstore/layouts/light.htm (line ~76 partial include to delete)
    - themes/logingrupa-naisstore/layouts/catalog_default.htm (line ~100 partial include to delete)
    - themes/logingrupa-naisstore/pages/checkout.htm (lines ~91-99 inline fbq InitiatedCheckout block — delete the `{% scripts %} … {% endscripts %}` wrapper containing fbq)
    - themes/logingrupa-naisstore/pages/order-complete.htm (full file — lines 10-12 `[purchasePixel]` INI block, line 18 `{% component 'purchasePixel' %}` render, lines 20-35 disabled fbq trackCustom block — ALL THREE must go; Plugin.php registers only `eventPixel` + `pixelHead`, so `purchasePixel` is a dead v1.x alias that would throw "Unknown component" on render)
    - themes/logingrupa-naisstore/pages/order-complete-proforma.htm (lines 13-26 inline fbq block; ALSO verify whether a `[purchasePixel]` INI block + render exist in this file per Task 0 Section 4 — delete if found, leave file alone if not)
    - themes/logingrupa-naisstore/configs/fields.yaml (lines ~447-456 `facebook_pixel_id` + `facebook_domain_verification_id` form fields delete)
    - plugins/logingrupa/metapixel/Plugin.php lines 108-114 (registerComponents — confirms only `eventPixel` + `pixelHead` are registered)
  </read_first>
  <action>
    DELETE `themes/logingrupa-naisstore/partials/facebook_pixel.htm` entirely via `git rm`.

    EDIT 4 layouts to remove the `{% partial 'facebook_pixel' obUser=obUser %}` (or equivalent) line: `main.htm`, `content.htm`, `light.htm`, `catalog_default.htm`.

    EDIT `pages/checkout.htm` — delete the full `{% scripts %} … fbq('track','InitiatedCheckout',{content_ids:[…]}) … {% endscripts %}` block per inventory.

    EDIT `pages/order-complete.htm` — make THREE deletions in this single file:
    (i) the entire `[purchasePixel]` INI declaration block (currently lines 10-12: `[purchasePixel]\norderSlug = "{{ :slug }}"\n`) — leave the surrounding `[OrderPage]` and `[RetryPayment]` blocks intact;
    (ii) the `{% component 'purchasePixel' %}` render line (currently line 18) — leave the `{% partial 'order/order-complete/order-complete' obOrder=obOrder %}` line intact;
    (iii) the disabled-but-still-present fbq trackCustom block including the `{# Disabled: legacy fbq replaced by Logingrupa.Metapixelshopaholic PurchasePixel ... #}` comment (currently lines 20-35) — strip both the dead code AND the v1.x-naming comment per D-23.

    EDIT `pages/order-complete-proforma.htm` — delete the inline fbq trackCustom block (current lines 13-26). If Task 0 Section 4 also identified a `[purchasePixel]` INI block + render in this file, delete those too (same pattern as order-complete.htm).

    EDIT `themes/logingrupa-naisstore/configs/fields.yaml` — delete the two form fields `facebook_pixel_id` and `facebook_domain_verification_id` (entire block for each field). Verify YAML still parses (`python3 -c 'import yaml; yaml.safe_load(open("themes/logingrupa-naisstore/configs/fields.yaml"))'`).
  </action>
  <verify>
    <automated>cd /home/forge/nailscosmetics.lv && bash -c '! test -f themes/logingrupa-naisstore/partials/facebook_pixel.htm && [ "$(grep -rl "partial '"'"'facebook_pixel'"'"'" themes/logingrupa-naisstore/layouts/ themes/logingrupa-naisstore/pages/ 2>/dev/null | wc -l)" = "0" ] && [ "$(grep -rcE "fbq\(" themes/logingrupa-naisstore/ --include="*.htm" 2>/dev/null | grep -v ":0$" | wc -l)" = "0" ] && [ "$(grep -c "purchasePixel" themes/logingrupa-naisstore/pages/order-complete.htm themes/logingrupa-naisstore/pages/order-complete-proforma.htm 2>/dev/null | awk -F: "{s+=\$2} END {print s+0}")" = "0" ] && ! grep -q "facebook_pixel_id" themes/logingrupa-naisstore/configs/fields.yaml && echo ok'</automated>
  </verify>
  <done>
    `partials/facebook_pixel.htm` deleted. 4 layouts contain no `partial 'facebook_pixel'` reference. `pages/checkout.htm` contains no `fbq(` call. `pages/order-complete.htm` AND `pages/order-complete-proforma.htm` contain ZERO occurrences of the literal string `purchasePixel` (verified by `grep -c purchasePixel`). `configs/fields.yaml` parses as valid YAML and contains no `facebook_pixel_id` / `facebook_domain_verification_id` keys.
  </done>
</task>

<task type="auto">
  <name>Task 3: Rebuild webpack bundle + verify zero fbq in compiled output + final grep gate</name>
  <read_first>
    - themes/logingrupa-naisstore/webpack.mix.js (entry config — verify `common.js` is the entry and rebuild produces `assets/js/common.js`)
    - themes/logingrupa-naisstore/package.json (verify `prod` script and pnpm version)
    - themes/logingrupa-naisstore/pnpm-lock.yaml (lockfile present)
    - .planning/phases/05-documentation-marketplace-launch/05-02-LEGACY-INVENTORY.md "Webpack bundle reach" section (baseline fbq strings the bundle currently contains)
  </read_first>
  <action>
    Execute `cd themes/logingrupa-naisstore && pnpm run prod` from inside the theme directory. This rebuilds `assets/js/common.js` from the now-stripped source tree. Record byte delta in plan summary.

    Verify the rebuilt bundle is fbq-free: `grep -aoE 'fbq\(|_fbq\(|fbevents\.js' themes/logingrupa-naisstore/assets/js/common.js` MUST return zero hits.

    Verify the dead v1.x `purchasePixel` alias is fully gone from theme pages: `grep -c 'purchasePixel' themes/logingrupa-naisstore/pages/order-complete*.htm` MUST return 0.

    Stage the rebuilt `assets/js/common.js` in git. The strip + bundle change ship as a single commit (separate from the Task 0 inventory commit) with message `feat(05-02): strip legacy Facebook Pixel + dead v1.x purchasePixel refs from themes/logingrupa-naisstore — 4 files deleted, 11 files edited, bundle rebuilt`. This is the last commit before UAT Gate 1 (plan 05-03).
  </action>
  <verify>
    <automated>cd /home/forge/nailscosmetics.lv && test -f themes/logingrupa-naisstore/assets/js/common.js && bash -c '[ "$(grep -aoE "fbq\(|_fbq\(|fbevents\.js" themes/logingrupa-naisstore/assets/js/common.js 2>/dev/null | wc -l)" = "0" ] && [ "$(grep -c "purchasePixel" themes/logingrupa-naisstore/pages/order-complete.htm themes/logingrupa-naisstore/pages/order-complete-proforma.htm 2>/dev/null | awk -F: "{s+=\$2} END {print s+0}")" = "0" ] && echo ok'</automated>
  </verify>
  <done>
    `themes/logingrupa-naisstore/assets/js/common.js` regenerated by `pnpm run prod` (mtime newer than git HEAD before run). Bundle contains zero `fbq(`, `_fbq(`, or `fbevents.js` strings. `grep -c purchasePixel themes/logingrupa-naisstore/pages/order-complete*.htm` returns 0 (dead v1.x alias fully removed — page no longer 500-errors at render). Source-tree grep `grep -rnE 'fbq\(|fbevents\.js|connect\.facebook\.net' themes/logingrupa-naisstore/ --include='*.htm' --include='*.js' --exclude-dir='node_modules' --exclude-dir='assets/js'` returns 0. Single commit staged. Theme functional verification deferred to UAT Gate 1 (plan 05-03).
  </done>
</task>

</tasks>

<verification>
- Inventory file `05-02-LEGACY-INVENTORY.md` exists, committed first
- 4 files deleted (3 JS tracking helpers + 1 facebook_pixel.htm partial)
- 11 files edited (4 layouts, 3 pages, 3 control JS callers, 1 configs/fields.yaml)
- `assets/js/common.js` rebuilt with zero fbq strings
- `grep -rnE 'fbq\(|fbevents\.js' themes/logingrupa-naisstore/ --exclude-dir=node_modules` returns 0 hits across both source and bundle
- `grep -c 'purchasePixel' themes/logingrupa-naisstore/pages/order-complete*.htm` returns 0 (dead v1.x alias removed)
</verification>

<success_criteria>
- Source-tree fbq grep: 0 hits
- Bundle-artifact fbq grep: 0 hits
- Dead `purchasePixel` grep on order-complete pages: 0 hits
- Two feature commits (inventory + strip+bundle) per Tiger-Style "small commits, one concern"
- Theme renders on /, /catalog, /product/<slug>, /checkout, /order-complete (functional check is UAT Gate 1's job)
</success_criteria>

<output>
Create `.planning/phases/05-documentation-marketplace-launch/05-02-SUMMARY.md` when done — note bundle size before/after, commit SHAs, file delete/edit counts, dead-`purchasePixel` removal confirmation, and explicit "READY FOR UAT GATE 1 (plan 05-03)" handoff line.
</output>
</content>
</invoke>