---
phase: 05-documentation-marketplace-launch
plan: 17
type: execute
wave: 13
depends_on: ['05-15']
files_modified:
  - classes/event/adapter/shopaholic/ProductPageWatcher.php
  - classes/meta/PayloadBuilder.php
  - tests/Feature/Adapter/Shopaholic/ProductPageWatcherTest.php
autonomous: true
gap_closure: true
requirements: [D-07]

must_haves:
  truths:
    - "Every PDP view dispatches a server ViewContent CAPI event whose event_id matches the browser fbq eventID rendered for that same view (repeat views of the same product included)"
    - "The EventLog dedup fence still blocks same-view retry duplicates but never blocks a NEW view of a previously-viewed product"
    - "Offer-switch ViewContent dispatch is per-switch unique under the same rule"
    - "PageView (and any zero-value subject) CAPI payload carries NO junk custom_data (no value:0 / num_items:0 / empty contents)"
  artifacts:
    - classes/event/adapter/shopaholic/ProductPageWatcher.php
    - classes/meta/PayloadBuilder.php
  key_links:
    - "ProductPageWatcher dispatch → per-view-unique subject (theme.action pattern, action_key 'viewcontent:{pid}:{eid}') instead of shopaholic.product/{id}"
    - "PixelHead.php:182 PageView dispatch = the proven per-view ThemeActionAdapter idiom to mirror"
---

<objective>
UAT-discovered gaps (05-16 checkpoint, 2026-07-02): server ViewContent fires only
ONCE PER PRODUCT EVER because SendCapiEvent is dispatched with subject
shopaholic.product/{product_id} and the EventLog UNIQUE race-fence
(subject_type, subject_id, event_name, channel, site_id) has no time dimension.
Live evidence: product 450 capi row dated 2026-06-17; PDP view 2026-07-02
15:44:20 UTC rendered browser fbq ViewContent event_id
9eecc318-71d7-4dd6-8821-9834ed697e6c with NO EventLog row and NO CAPI dispatch —
orphan browser event, no dedup pair. Only 16 ViewContent capi rows exist
all-time (one per product). PageView proves the correct idiom: it dispatches via
ThemeActionAdapter with a per-view unique action_key subject and fires on every
view.

Secondary gap: PayloadBuilder unconditionally emits custom_data
currency/value/num_items/contents, so PageView CAPI arrives at Meta with junk
value:0, num_items:0 params (operator-observed in Test Events).

Fix both. Do NOT touch the AddToCart wire (05-15) or Purchase
(OrderStatusWatcher) dispatch subjects — Purchase per-order subject is CORRECT
(one Purchase per order is the desired dedup).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@classes/event/adapter/shopaholic/ProductPageWatcher.php
@components/PixelHead.php
@classes/adapter/theme/ThemeAjaxHandler.php
@classes/adapter/theme/ThemeActionAdapter.php
@classes/meta/PayloadBuilder.php
@classes/queue/SendCapiEvent.php
@classes/event/EventLogWriter.php
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ViewContent per-view dispatch — mirror the PageView theme.action idiom</name>
  <files>classes/event/adapter/shopaholic/ProductPageWatcher.php, tests/Feature/Adapter/Shopaholic/ProductPageWatcherTest.php</files>
  <behavior>
    - Two consecutive handle() calls for the SAME product each dispatch SendCapiEvent with a subject the EventLog fence treats as distinct (assert two capi rows / two dispatches; today the second is silently fenced).
    - The dispatched subject/event carries an action_key of shape 'viewcontent:{product_id}:{event_id}' (per-view unique) and resolves the SAME site_id the product subject would have resolved.
    - The browser collector push and the CAPI dispatch share the same event_id per view (dedup pair intact).
    - dispatchForOfferSwitch gets the same treatment: per-switch unique subject, action_key 'viewcontent:{pid}:{oid}:{eid}'.
    - The ViewContent custom_data (content_ids, content_name via extras, value, currency) is unchanged — payload is still built by ShopaholicProductAdapter + ShopaholicProductValueResolver BEFORE dispatch; only the SendCapiEvent subject/adapter routing changes.
  </behavior>
  <action>
    Study how PixelHead.php:182 dispatches PageView: it wraps the request in the
    theme-action event object (see how $obEvent is constructed there and in
    ThemeAjaxHandler:118) and dispatches with ThemeActionAdapter::class — the
    adapter derives subject_id by hashing the per-view action_key, so the
    EventLog fence keys on the unique view, not the product. Reuse that exact
    event-object type + adapter for ProductPageWatcher::handle and
    dispatchForOfferSwitch, constructing the event object from the
    already-generated action_key + event_id + site (site MUST come from the
    subject/event object, never SiteManager/request — phpstan disallowed-calls
    bans those in classes/event/). Keep the prebuilt $arPayload untouched.
    If the theme-action event object cannot carry a prebuilt payload cleanly,
    prefer the smallest change that makes the fence per-view (e.g. a tiny
    first-party value object implementing the same contract ThemeActionAdapter
    expects) — do NOT widen EventSubjectAdapter, do NOT alter the fence columns,
    do NOT drop the fence (same-key retry dedup must survive).
    Hungarian locals, short docblocks, no workflow-marker comments.
  </action>
  <verify>
    <automated>cd /home/forge/nailscosmetics.lv/plugins/logingrupa/metapixel && ../../../vendor/bin/pest --configuration phpunit.xml tests/Feature/Adapter/Shopaholic/ --no-coverage</automated>
  </verify>
  <done>Repeat views of the same product each dispatch CAPI with per-view unique fence subjects; browser/server event_id pairing preserved; offer-switch same; all Shopaholic adapter tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: PayloadBuilder — drop empty/zero custom_data entries</name>
  <files>classes/meta/PayloadBuilder.php, tests/Unit/Meta/PayloadBuilderTest.php</files>
  <behavior>
    - A subject whose resolver returns value 0.0, num_items 0, contents [] produces custom_data WITHOUT value, num_items, contents keys; currency is dropped too when value is dropped.
    - A subject with real values (AddToCart/Purchase/ViewContent) produces custom_data identical to today (regression: existing PayloadBuilder + adapter tests unchanged and green).
    - $arEventExtras still merge on top unconditionally.
  </behavior>
  <action>
    In buildEventPayload, filter the base custom_data before the content_ids /
    extras merges: drop 'contents' when [], drop 'num_items' when 0, drop
    'value' when 0.0, and drop 'currency' when 'value' was dropped (currency
    without value is meaningless to Meta). Explicit conditionals, not a clever
    array_filter one-liner (Simple > clever). Update/extend the PayloadBuilder
    unit tests for both shapes.
  </action>
  <verify>
    <automated>cd /home/forge/nailscosmetics.lv/plugins/logingrupa/metapixel && ../../../vendor/bin/pest --configuration phpunit.xml --no-coverage && ../../../vendor/bin/pint --test && ../../../vendor/bin/phpstan analyse --no-progress</automated>
  </verify>
  <done>PageView payload carries no zero-value junk; value-bearing events byte-identical to before; full suite + pint + phpstan L10 green.</done>
</task>

</tasks>

<verification>
- Full pest suite green; pint green; phpstan L10 + disallowed-calls green (no SiteManager/request in classes/event/).
- Two sequential handle() calls for one product → two distinct fence subjects (test-asserted).
- PageView payload junk gone (test-asserted).
</verification>

<success_criteria>
- Server ViewContent fires on EVERY PDP view with browser/server event_id pair intact.
- Fence still dedups same-view retries.
- PageView CAPI clean of value:0/num_items:0.
- AddToCart (05-15) and Purchase paths untouched.
</success_criteria>

<output>
Create `.planning/phases/05-documentation-marketplace-launch/05-17-SUMMARY.md` when done.
</output>
