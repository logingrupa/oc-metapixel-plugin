---
phase: 5
plan: 10
plan_id: 05-10
type: execute
wave: 7
depends_on: [05-00]
files_modified:
  - docs/CUSTOM-ADAPTERS.md
autonomous: true
threat_refs: []
requirements: [DOCS-03]

must_haves:
  truths:
    - "docs/CUSTOM-ADAPTERS.md ships at plugins/logingrupa/metapixel/docs/CUSTOM-ADAPTERS.md (creates docs/ directory if not present)"
    - "Doc ships BOTH names per the AcmeCart-vs-OFFLINE\\Mall conflict resolution (W-12): the minimal AdapterRegistry::register snippet uses `AcmeCartAdapter` (the literal name in REQUIREMENTS.md DOCS-03 + ROADMAP.md Architecture-at-a-glance lines 119-135), AND the full ~50-LOC EventSubjectAdapter implementation + ~30-LOC value resolver track `OFFLINE\\Mall\\Models\\Order` per CONTEXT.md D-14. The first satisfies the literal requirement text; the second satisfies the D-14 lock."
    - "All three Event::fire hook constants are documented with copy-paste-able examples: before_dispatch (inject test_event_code), after_dispatch (mirror to analytics dashboard), dead_letter (Slack webhook). Hook constant strings match plugins/logingrupa/metapixel/classes/queue/SendCapiEvent.php lines 27-47 verbatim."
    - "## Testing your adapter section shows how to extend Logingrupa\\Metapixel\\Classes\\Testing\\EventSubjectAdapterContractTestCase and implement makeAdapter() + makeSubject() (D-16)"
    - "Section order matches RESEARCH § Pattern 4: Overview → Contract → Register snippet (AcmeCart minimal) → Full inline example (OFFLINE\\Mall) → Trigger dispatch → 3 hooks → Testing → Anti-patterns"
    - "Wave 0 test tests/Feature/Docs/CustomAdaptersStructureTest.php flips RED → GREEN after this plan; the test asserts BOTH `grep -c 'AcmeCartAdapter\\|class AcmeCart' docs/CUSTOM-ADAPTERS.md >= 1` AND `grep -c 'OFFLINE\\\\Mall\\|class MallOrderAdapter' docs/CUSTOM-ADAPTERS.md >= 1`"
    - "Doc has zero `v1.1.1`, `legacy/v1`, or `Phase [0-9]` references (D-22 + D-23)"
  artifacts:
    - path: "docs/CUSTOM-ADAPTERS.md"
      provides: "Third-party adapter authoring guide per DOCS-03 — ships AcmeCart minimal register snippet + OFFLINE\\Mall full inline example"
      contains: "metapixel.event.before_dispatch, metapixel.event.after_dispatch, metapixel.event.dead_letter, AcmeCartAdapter, OFFLINE\\Mall, EventSubjectAdapterContractTestCase, makeAdapter, makeSubject, AdapterRegistry::instance()->register"
      min_lines: 180
  key_links:
    - from: "docs/CUSTOM-ADAPTERS.md"
      to: "plugins/logingrupa/metapixel/classes/adapter/EventSubjectAdapter.php"
      via: "Interface signatures copied verbatim into Contract section"
      pattern: "interface EventSubjectAdapter"
    - from: "docs/CUSTOM-ADAPTERS.md"
      to: "plugins/logingrupa/metapixel/classes/queue/SendCapiEvent.php"
      via: "Hook constants HOOK_BEFORE_DISPATCH/HOOK_AFTER_DISPATCH/HOOK_DEAD_LETTER copied"
      pattern: "metapixel\\.event\\.(before_dispatch|after_dispatch|dead_letter)"
    - from: "docs/CUSTOM-ADAPTERS.md"
      to: ".planning/ROADMAP.md (Architecture-at-a-glance lines 119-135)"
      via: "AcmeCartAdapter snippet copies the canonical registration pattern verbatim"
      pattern: "AdapterRegistry::instance\\(\\)->register"
    - from: "docs/CUSTOM-ADAPTERS.md"
      to: "plugins/logingrupa/metapixel/classes/testing/EventSubjectAdapterContractTestCase.php"
      via: "Testing section references the class FQN + makeAdapter/makeSubject abstract methods"
      pattern: "EventSubjectAdapterContractTestCase"
---

<objective>
Author `docs/CUSTOM-ADAPTERS.md` — the third-party adapter authoring guide. **AcmeCart vs OFFLINE\Mall conflict resolution (W-12):** REQUIREMENTS.md DOCS-03 literally specifies `AcmeCartAdapter` (matching ROADMAP.md Architecture-at-a-glance lines 119-135 canonical snippet); CONTEXT.md D-14 substitutes `OFFLINE\Mall\Models\Order` for the inline implementation. The doc satisfies BOTH by shipping AcmeCart as the minimal REGISTRATION-PATTERN example (the snippet from ROADMAP.md) AND OFFLINE\Mall as the CONTRACT example (the full ~50-LOC EventSubjectAdapter + ~30-LOC ValueResolver implementation per D-14). Inline-only — NO `classes/adapter/mall/` directory. Three Event::fire hooks documented with concrete copy-paste use cases per D-15. Dedicated `## Testing your adapter` section anchored to `EventSubjectAdapterContractTestCase` (D-16). Flips Wave 0 `CustomAdaptersStructureTest` from RED to GREEN.

Purpose: DOCS-03 deliverable. The marketplace contract for third-party adapters is the 10 invariants in `EventSubjectAdapterContractTestCase`; this doc surfaces that contract for buyers while satisfying both the literal REQUIREMENTS.md AcmeCart wording AND the D-14 OFFLINE\Mall lock.

**Wave 0 test extension (W-12):** The Wave 0 `tests/Feature/Docs/CustomAdaptersStructureTest.php` adds two assertions: (a) `grep -c 'AcmeCartAdapter\|class AcmeCart' docs/CUSTOM-ADAPTERS.md` returns ≥1; (b) `grep -c 'OFFLINE\\\\Mall\|class MallOrderAdapter' docs/CUSTOM-ADAPTERS.md` returns ≥1. Plan 05-00 Task 1 must extend its CustomAdaptersStructureTest to include both assertions; if 05-00 already shipped without the AcmeCart assertion, this plan also updates the test.

Output: `docs/CUSTOM-ADAPTERS.md`. Wave 0 test green. Possible small extension to `tests/Feature/Docs/CustomAdaptersStructureTest.php` if the AcmeCart-name assertion is not already present.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-documentation-marketplace-launch/05-CONTEXT.md
@.planning/phases/05-documentation-marketplace-launch/05-RESEARCH.md
@.planning/phases/05-documentation-marketplace-launch/05-PATTERNS.md
@plugins/logingrupa/metapixel/classes/adapter/EventSubjectAdapter.php
@plugins/logingrupa/metapixel/classes/adapter/ValueResolver.php
@plugins/logingrupa/metapixel/classes/adapter/AdapterRegistry.php
@plugins/logingrupa/metapixel/classes/queue/SendCapiEvent.php
@plugins/logingrupa/metapixel/classes/testing/EventSubjectAdapterContractTestCase.php
@plugins/logingrupa/metapixel/CLAUDE.md
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| doc → third-party developer | Static markdown; instructs developer on adapter authoring |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-10-01 | Tampering | doc example invites third-party to mutate event_id in before_dispatch | mitigate | Doc explicitly forbids mutating event_id / event_time in before_dispatch payload (dedup contract anchor — Phase 2 P-08 snapshot+restore) |
| T-05-10-02 | Information Disclosure | doc example shows real EAA-prefix token | accept | Hook example uses literal `'config('mall.metapixel.test_event_code')'` placeholder — no real token; Slack webhook example uses `'#alerts-metapixel'` channel placeholder |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Extend tests/Feature/Docs/CustomAdaptersStructureTest.php with the AcmeCart-name assertion if absent</name>
  <read_first>
    - tests/Feature/Docs/CustomAdaptersStructureTest.php (verify whether `test_doc_contains_acme_cart_adapter_name_reference` already exists per W-12)
    - .planning/ROADMAP.md lines 117-136 (AcmeCart canonical snippet)
    - .planning/REQUIREMENTS.md MKT-related rows mentioning AcmeCartAdapter
  </read_first>
  <action>
    Open `tests/Feature/Docs/CustomAdaptersStructureTest.php`. Verify the file contains an assertion equivalent to: load `docs/CUSTOM-ADAPTERS.md`, assertTrue that the string contains BOTH `AcmeCartAdapter` (or `class AcmeCart`) AND `OFFLINE\Mall` (or `class MallOrderAdapter`). If the assertion is already present, skip the rest of this task.

    If absent, add a new public test method `test_doc_contains_both_acme_cart_minimal_and_mall_full_examples` to the existing CustomAdaptersStructureTest class. The method asserts:
      - `assertStringContainsString('AcmeCartAdapter', $sDoc, 'DOCS-03 + ROADMAP.md Architecture-at-a-glance — minimal register snippet uses canonical AcmeCart name')` OR `assertStringContainsString('class AcmeCart', $sDoc, ...)`
      - `assertStringContainsString('OFFLINE\\Mall', $sDoc, 'CONTEXT.md D-14 — full inline example tracks OFFLINE\\Mall\\Models\\Order')` OR `assertStringContainsString('class MallOrderAdapter', $sDoc, ...)`

    Run `vendor/bin/pest --filter='CustomAdaptersStructureTest'` after the edit; expect the NEW test to be RED (CUSTOM-ADAPTERS.md not yet authored), other tests in the class still RED with their original missing-artifact messages.
  </action>
  <verify>
    <automated>cd /home/forge/nailscosmetics.lv/plugins/logingrupa/metapixel && grep -E "AcmeCart|class AcmeCart" tests/Feature/Docs/CustomAdaptersStructureTest.php | head -3 | wc -l | awk '$1 >= 1 {print "ok"; exit 0} {print "fail: AcmeCart assertion not present"; exit 1}'</automated>
  </verify>
  <done>
    CustomAdaptersStructureTest contains an assertion for both `AcmeCart` and `OFFLINE\Mall`/`MallOrder` references in CUSTOM-ADAPTERS.md. Test stays RED until Task 2 ships the doc.
  </done>
</task>

<task type="auto">
  <name>Task 2: Author docs/CUSTOM-ADAPTERS.md with AcmeCart minimal + OFFLINE\Mall full + 3 hooks + Testing section</name>
  <read_first>
    - plugins/logingrupa/metapixel/classes/adapter/EventSubjectAdapter.php (interface — 7 methods)
    - plugins/logingrupa/metapixel/classes/adapter/ValueResolver.php (interface — 5 methods)
    - plugins/logingrupa/metapixel/classes/adapter/AdapterRegistry.php (register/resolveFor/resolveByClass + the instance() singleton)
    - plugins/logingrupa/metapixel/classes/queue/SendCapiEvent.php lines 27-60 (3 hook constants + signatures)
    - plugins/logingrupa/metapixel/classes/testing/EventSubjectAdapterContractTestCase.php lines 18-50 (abstract makeAdapter + makeSubject + the 10 invariant test method names)
    - plugins/logingrupa/metapixel/CLAUDE.md "Extensibility contract" section (six-rank list — rank 6 last-resort warning on Component::extend)
    - .planning/phases/05-documentation-marketplace-launch/05-RESEARCH.md lines 597-738 (Code Examples — verbatim OFFLINE\Mall adapter + 3 hook examples + Testing section)
    - .planning/phases/05-documentation-marketplace-launch/05-PATTERNS.md lines 186-249 (CUSTOM-ADAPTERS doc anchoring strategy + section ordering)
    - .planning/ROADMAP.md lines 117-136 (AcmeCart canonical register snippet — copied verbatim into the Register section per W-12)
    - .planning/REQUIREMENTS.md DOCS-03 row (AcmeCartAdapter literal name source)
    - tests/Feature/Docs/CustomAdaptersStructureTest.php (Wave 0 assertions)
  </read_first>
  <action>
    Create `plugins/logingrupa/metapixel/docs/CUSTOM-ADAPTERS.md`. Create the `docs/` directory if absent.

    Section order (W-12 resolution: AcmeCart minimal example FIRST as the register pattern; OFFLINE\Mall full implementation SECOND as the contract example):

    1. `# Authoring a custom adapter` — one-paragraph overview (what an adapter is, when third party needs one).

    2. `## The contract: EventSubjectAdapter + ValueResolver` — copy the 7-method EventSubjectAdapter interface signature verbatim and the 5-method ValueResolver signature. Each method gets a one-paragraph English explanation. End section by noting subject_type is opaque alias (no backslashes, ≤64 chars per invariant 01) and getSiteId reads from subject only (P-01 anchor).

    3. `## Minimal example: register your adapter (AcmeCart)` — copy verbatim from ROADMAP.md lines 117-136. This is the canonical 18-LOC AcmeCart `Plugin::boot()` snippet showing `AdapterRegistry::instance()->register(AcmeCart::class, AcmeCartAdapter::class)`, the `$require = ['Logingrupa.Metapixel']` declaration, and the `AcmeCart::extend` model hook firing `SendCapiEvent::dispatch('Purchase', ...)`. This satisfies the literal REQUIREMENTS.md DOCS-03 + ROADMAP.md Architecture-at-a-glance reference to `AcmeCartAdapter`. Add a one-paragraph intro: "This minimal snippet shows the registration shape. Implementations of `AcmeCartAdapter` itself follow the contract above — see the next section for a fully-worked example."

    4. `## Full inline example: OFFLINE Mall` (D-14 anchor) — fenced PHP block with the ~50-LOC `MallOrderAdapter` per RESEARCH lines 601-636. Add a fenced PHP block for `MallOrderValueResolver` (~30 LOC) implementing the 5 ValueResolver methods. State explicitly under the example: "This code lives ONLY in this documentation. The plugin itself does NOT ship a `classes/adapter/mall/` directory. Copy these blocks into your own plugin and adapt." (D-14 lock).

    5. `## Trigger dispatch` — one-paragraph explaining `SendCapiEvent::dispatch` is the only public entry; queue worker rehydrates the adapter via `AdapterRegistry::resolveByClass`; on permanent failure FailedEvent row + dead_letter event fire.

    6. `## Hook patterns` — three subsections:
       - `### before_dispatch — inject test_event_code for staging` — fenced PHP block per RESEARCH lines 664-672. Add a one-line warning: "MUST NOT mutate event_id or event_time — Meta dedup contract anchor."
       - `### after_dispatch — mirror to analytics dashboard` — fenced PHP block per RESEARCH lines 677-686.
       - `### dead_letter — Slack alert on permanent failure` — fenced PHP block per RESEARCH lines 691-700.
       Each hook constant string appears verbatim in the fenced blocks: `metapixel.event.before_dispatch`, `metapixel.event.after_dispatch`, `metapixel.event.dead_letter`.

    7. `## Testing your adapter` — copy from RESEARCH lines 705-737. Fenced PHP block extending `EventSubjectAdapterContractTestCase` with `makeAdapter()` + `makeSubject()` factory methods. Below the block enumerate all 10 invariants from the test method names verbatim. Closing line: "(Source: `classes/testing/EventSubjectAdapterContractTestCase.php`)".

    8. `## Anti-patterns` — copy from CLAUDE.md "Extensibility contract" rank 6 warning verbatim: `Component::extend(PixelHead::class, ...)` is LAST RESORT; use Event::fire hooks instead.

    Hard constraints:
    - Zero `v1.1.1`, `legacy/v1`, `Phase [0-9]` references
    - All three hook constant strings appear ≥1 time each
    - `AcmeCart` (or `AcmeCartAdapter`) appears ≥1 time (W-12 — Wave 0 enforces)
    - `OFFLINE\Mall` (or `MallOrderAdapter`) appears ≥1 time (W-12 — Wave 0 enforces; D-14 anchor)
    - `EventSubjectAdapterContractTestCase` appears ≥1 time
    - `AdapterRegistry::instance()->register` appears ≥1 time
    - `makeAdapter` AND `makeSubject` appear

    Commit message draft: `docs(05-10): add docs/CUSTOM-ADAPTERS.md (DOCS-03 — AcmeCart register snippet + OFFLINE Mall full example + 3 hook examples + testing contract)`.
  </action>
  <verify>
    <automated>cd /home/forge/nailscosmetics.lv/plugins/logingrupa/metapixel && vendor/bin/pest --filter='CustomAdaptersStructureTest' 2>&1 | tail -20</automated>
  </verify>
  <done>
    `docs/CUSTOM-ADAPTERS.md` exists. `vendor/bin/pest --filter='CustomAdaptersStructureTest'` reports ALL tests GREEN (including the W-12 dual-name assertion). `grep -c 'metapixel\.event\.\(before_dispatch\|after_dispatch\|dead_letter\)' docs/CUSTOM-ADAPTERS.md` returns ≥3. `grep -c 'AcmeCart' docs/CUSTOM-ADAPTERS.md` returns ≥1. `grep -c 'OFFLINE\\\\Mall' docs/CUSTOM-ADAPTERS.md` returns ≥1. `grep -c 'EventSubjectAdapterContractTestCase' docs/CUSTOM-ADAPTERS.md` returns ≥1.
  </done>
</task>

</tasks>

<verification>
- `docs/CUSTOM-ADAPTERS.md` exists with all 8 named sections from RESEARCH § Pattern 4 (reordered per W-12 to put AcmeCart minimal before OFFLINE\Mall full)
- Wave 0 CustomAdaptersStructureTest GREEN — both AcmeCart + OFFLINE\Mall assertions satisfied
- All 3 hook constants + AcmeCart + OFFLINE\Mall + ContractTestCase ref verifiable by grep
- No production `classes/adapter/mall/` directory created
</verification>

<success_criteria>
- DOCS-03 met (literal AcmeCart name + D-14 OFFLINE\Mall lock both honored)
- Wave 0 test GREEN
- Third-party developer can copy the OFFLINE\Mall example, swap model names per the AcmeCart minimal-register pattern, and ship an adapter without touching plugin core
</success_criteria>

<output>
Create `.planning/phases/05-documentation-marketplace-launch/05-10-SUMMARY.md` when done — note doc line count, section count, AcmeCart-snippet line count vs OFFLINE\Mall-full line count, Wave 0 test status (GREEN), commit SHA.
</output>
</content>
</invoke>