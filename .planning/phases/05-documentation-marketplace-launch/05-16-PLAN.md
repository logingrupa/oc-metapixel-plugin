---
phase: 05-documentation-marketplace-launch
plan: 16
type: execute
wave: 12
depends_on: ['05-15']
files_modified: []
autonomous: false
gap_closure: true
requirements: [D-07]

must_haves:
  truths:
    - "Operator confirms in Meta Test Events that the browser + server AddToCart pair is deduplicated BY EVENT ID (not fbp fallback)"
    - "Operator confirms the browser AddToCart custom_data carries content_ids, contents, num_items, value, currency"
    - "Operator confirms the stray no-event_id / cs_est browser AddToCart is gone (or its emitter is identified)"
  artifacts: []
  key_links:
    - "theme asset rebuild (pnpm run prod) + FPM reload → the new $.request follow-up reaches production"
---

<objective>
Operator UAT re-test of the D-07 browser AddToCart wire shipped in 05-15. Closes
the single outstanding gap (test 9) in 05-UAT-CUTOVER.md. Verifies true event_id
deduplication and full custom_data on the browser AddToCart, and confirms the
stray no-event_id browser AddToCart observed 2026-07-02 is gone.

Purpose: operator sign-off that the fbp-fallback dedup is replaced by event_id
dedup before v2.0.0 launch.
Output: pass/fail record appended to 05-UAT-CUTOVER.md test 9.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-documentation-marketplace-launch/05-UAT-CUTOVER.md
@.planning/phases/05-documentation-marketplace-launch/05-15-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Deploy the wire — rebuild theme assets and flush OPcache</name>
  <files>../../../themes/logingrupa-naisstore/assets/js/</files>
  <action>
    Rebuild the theme bundle so the add-to-cart-control.js change from 05-15 reaches production, then flush OPcache so the new PHP handler is live:
    - `cd /home/forge/nailscosmetics.lv/themes/logingrupa-naisstore && pnpm run prod`
    - `sudo systemctl reload php8.4-fpm`
    Confirm the compiled bundle in assets/js/ contains the string `Metapixel::onMarkAddToCart` (grep the built file). If the theme build tooling is unavailable in this environment, STOP and surface it to the operator — do not fake the build.
  </action>
  <verify>
    <automated>grep -rql "Metapixel::onMarkAddToCart" /home/forge/nailscosmetics.lv/themes/logingrupa-naisstore/assets/js/ && echo BUILT</automated>
  </verify>
  <done>Compiled theme JS in assets/js/ contains the onMarkAddToCart follow-up call; php8.4-fpm reloaded.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Operator verifies event_id-deduplicated AddToCart in Meta Test Events</name>
  <action>Halt and wait for the operator to run the verification steps below in Meta Test Events, then resume on their signal. Do not self-approve.</action>
  <what-built>
    A browser fbq AddToCart emitter (05-15) that reuses the server CAPI event_id
    and carries full custom_data. Server path (CartPositionWatcher CAPI) is
    unchanged; the browser wire is PIXEL-ONLY and writes a channel='pixel'
    EventLog twin row. No second CAPI dispatch.
  </what-built>
  <how-to-verify>
    On https://new.nailscosmetics.lv with Meta Pixel Helper + Meta Test Events
    live view open (ensure Settings.test_event_code is set so events surface in
    the Test Events tab):
    1. Add a product to cart. Watch Meta Test Events: an AddToCart Browser entry
       AND a Server entry should appear, labelled "Deduplicated" and matched BY
       EVENT ID (not by fbp). Confirm the eventID on the Browser event equals the
       Server event's event_id.
    2. Inspect the browser AddToCart params: content_ids, contents, num_items,
       value, currency must all be present and match the server event's
       custom_data (no more value/currency/cs_est-only event).
    3. Confirm the previously-observed STRAY browser AddToCart with NO event_id /
       cs_est=true is GONE. If it still appears, capture: is it a second fbq call
       (check for a duplicate base pixel / autoConfig), or Meta client-side
       estimation? Note the emitter for a fast-follow.
    4. Tail `logingrupa_metapixel_event_log`: exactly one channel='capi' AND one
       channel='pixel' AddToCart row per cart-add for the cart_position; adding
       the same offer again (qty bump) does NOT create duplicate rows.
    5. Regression: repeat UAT-CUTOVER tests 1 (AddToCart existing) and 2
       (Purchase) briefly to confirm no regression on the shipped dedup paths.
  </how-to-verify>
  <resume-signal>Type "approved" if browser+server AddToCart deduplicate by event_id with full custom_data and the stray event is gone; otherwise describe what you observed (event_id present? which params? stray still there + its source?).</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Record the UAT outcome</name>
  <files>.planning/phases/05-documentation-marketplace-launch/05-UAT-CUTOVER.md</files>
  <action>
    After operator sign-off, update `05-UAT-CUTOVER.md`: flip item 9 `result: issue`
    → `result: pass` with a dated operator note summarizing the observed event_id
    dedup + custom_data + stray-event outcome; move the Gaps entry to resolved (or
    strike it) and update the Summary counts (issues 1 → 0, passed 7 → 8). If the
    operator reported the stray event persists or any param is missing, keep item 9
    as `issue`, record the finding verbatim, and STOP with a note that a fast-follow
    gap plan is needed — do NOT mark pass.
  </action>
  <verify>
    <automated>grep -q "result: pass" /home/forge/nailscosmetics.lv/plugins/logingrupa/metapixel/.planning/phases/05-documentation-marketplace-launch/05-UAT-CUTOVER.md && echo RECORDED</automated>
  </verify>
  <done>05-UAT-CUTOVER.md item 9 reflects the operator's actual verdict (pass on success; issue + fast-follow note on failure).</done>
</task>

</tasks>

<verification>
- Compiled theme bundle carries the onMarkAddToCart call; FPM reloaded.
- Operator confirms event_id dedup + full custom_data + stray-event resolution.
- 05-UAT-CUTOVER.md item 9 updated to the true verdict.
</verification>

<success_criteria>
- Browser + server AddToCart deduplicate BY EVENT ID in Meta Test Events with full custom_data.
- Stray no-event_id browser AddToCart gone (or emitter identified for fast-follow).
- UAT test 9 signed off by operator.
</success_criteria>

<output>
Create `.planning/phases/05-documentation-marketplace-launch/05-16-SUMMARY.md` when done.
</output>
