---
phase: 05-documentation-marketplace-launch
plan: 21
type: execute
wave: 2
depends_on: ['05-19', '05-20']
files_modified:
  - .github/workflows/metapixel-qa.yml
autonomous: false
gap_closure: true
requirements: [MKT-04]
must_haves:
  truths:
    - "`.github/workflows/metapixel-qa.yml` targets the standalone public repo layout where the plugin IS the repo root — it no longer assumes a `plugins/logingrupa/metapixel/` working directory or a `../../../vendor/` monorepo nesting that does not exist on the public `origin`"
    - "The workflow reconstructs a full OctoberCMS app around the checked-out plugin and authenticates to gateway.octobercms.com via a repository secret, because `october/system` + `lovata/toolbox-plugin` resolve only through the authenticated gateway (no public Packagist path exists)"
    - "The rewritten workflow is valid YAML and its root-cause diagnosis (monorepo-path workflow copied into a standalone repo) is recorded in the commit message"
    - "master is pushed to origin ONLY after explicit operator approval at the human-verify checkpoint (outward-facing; republishes tracked `.planning/` docs to the already-public repo)"
    - "After the push, the CI matrix run on HEAD is watched to green — or, if it stays red, the workflow is iterated until the 4 cells (php 8.3/8.4 × full-lovata/minimal) pass"
  artifacts:
    - ".github/workflows/metapixel-qa.yml (rewritten for standalone-plugin-at-root + gateway auth from secret)"
  key_links:
    - "workflow gateway-auth step → `secrets.OCTOBER_AUTH_JSON`: without the operator-supplied October license secret, no CI cell can install october/system or lovata/* and every cell fails at composer install"
    - "git push origin master → GitHub Actions push trigger → `gh run watch`: the push is what starts the CI run the executor then observes"
---

<objective>
Close the actionable half of Gap 2 (UAT test 9, MKT-04): CI is red and stale (only 3 runs ever, all failures, last 2026-05-21) and 147 commits are unpushed. Root cause (confirmed this session): the public `origin` repo (https://github.com/logingrupa/oc-metapixel-plugin) has the plugin AT ITS ROOT (composer.json, phpstan.neon, phpunit.xml, .github/ all at top level), but `metapixel-qa.yml` was copied from the monorepo and still uses `working-directory: plugins/logingrupa/metapixel` and `../../../vendor/bin/pest` — paths that do not exist in the standalone checkout, so every cell fails within ~21s.

A second, harder reality (confirmed via the monorepo `auth.json` carrying a `gateway.octobercms.com` credential): the plugin requires `october/system` + `lovata/toolbox-plugin`, which are distributed ONLY through the authenticated October gateway. A bare GitHub runner has no such credential, so even with corrected paths the workflow cannot install dependencies without an operator-supplied license secret. This makes true green CI operator-gated.

Purpose: Give the plugin a truthful, working CI gate on its own public repo and get the 147 local commits published — the prerequisite for any future `v2.0.0` tag (tag creation itself stays gated on the operator's LAUNCH SCHEDULED signal and is NOT in this plan).
Output: A rewritten workflow for the standalone layout, an operator-supplied gateway secret, an approved push, and the CI matrix watched to green.

Scope note (do NOT expand): Do NOT create the `v2.0.0` tag, do NOT run security-sweep Step B, do NOT flip repo visibility (already public). Those are operator decisions gated on LAUNCH SCHEDULED. This plan fixes CI and publishes the current branch — nothing more.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-documentation-marketplace-launch/05-UAT.md

# The workflow being repaired + the qa script it must run
@.github/workflows/metapixel-qa.yml
@composer.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite metapixel-qa.yml for the standalone-plugin-at-root repo</name>
  <files>.github/workflows/metapixel-qa.yml</files>
  <action>Rewrite the workflow so it runs correctly when the checkout IS the plugin (the public `origin` layout), not a monorepo. Keep the 2x2 matrix (php 8.3/8.4 × install full-lovata/minimal) and the least-privilege `permissions: contents: read`. Drop the `pull_request` `paths:` filter that references `plugins/logingrupa/metapixel/**` (that path never matches in the standalone repo) — trigger on push to master and on pull_request generally. Because the plugin's pest suite bootstraps through a host October app (MetapixelTestCase extends October's test harness) and cannot `composer install` standalone, reconstruct a host app in the runner: create a fresh OctoberCMS 4.x app (via `composer create-project october/october` pinned to `^4.0`), then place the checked-out plugin under that app's `plugins/logingrupa/metapixel/` (symlink or copy the workspace), then require the Lovata cart packages into the app for the full-lovata cell (and omit them for the minimal cell, mirroring the existing `--exclude-group=adapter` split). Run the qa chain (pint-test → analyse → phpmd → deps → pest) from the plugin directory inside that app, matching the app-relative `vendor/bin/pest` location. Add a gateway-authentication step BEFORE any composer install that writes the operator-supplied October gateway credential from `secrets.OCTOBER_AUTH_JSON` into a composer `auth.json` (fail the job early with a clear message if the secret is absent), because october/system and lovata/* resolve only via authenticated gateway.octobercms.com. Preserve the coverage gate (`--coverage --min=90`) on the full-lovata cell only. Validate the file is well-formed YAML locally before committing (parse it with a YAML loader or `gh workflow` lint if available). Commit with a message that records the root cause: the monorepo-path workflow was copied into a standalone repo.</action>
  <verify>The workflow file parses as valid YAML (e.g. `python3 -c 'import yaml,sys; yaml.safe_load(open(".github/workflows/metapixel-qa.yml"))'` exits 0); grep confirms no remaining `working-directory: plugins/logingrupa/metapixel` and no `../../../vendor` reference; the gateway-auth step references `secrets.OCTOBER_AUTH_JSON`</verify>
  <done>metapixel-qa.yml is rewritten for the standalone layout, reconstructs a host October app, authenticates the gateway from a secret, keeps the 2x2 matrix + coverage gate, and is valid YAML with the root cause documented in the commit.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Checkpoint: Operator adds the OCTOBER_AUTH_JSON gateway secret</name>
  <action>Halt and wait for the operator to add the `OCTOBER_AUTH_JSON` repository secret per the steps below, then resume on their signal. Do not self-approve — the executor has no access to the operator's paid October license.</action>
  <what-built>The workflow now expects a repository secret `OCTOBER_AUTH_JSON` containing the October gateway credential. CI physically cannot install `october/system` or the `lovata/*` backbone without it — there is no public-registry or CLI/API path for the plugin to supply the operator's paid October license.</what-built>
  <how-to-verify>
    1. In GitHub → the `logingrupa/oc-metapixel-plugin` repo → Settings → Secrets and variables → Actions → New repository secret.
    2. Name it exactly `OCTOBER_AUTH_JSON`.
    3. Value: the JSON contents of your October `auth.json` for `gateway.octobercms.com` (the same `http-basic.gateway.octobercms.com` username/password block your project uses). Do NOT paste it anywhere else and do NOT commit it to the repo.
    4. Save. Confirm the secret appears in the Actions secrets list.
  </how-to-verify>
  <resume-signal>Type "secret added" once `OCTOBER_AUTH_JSON` exists in the repo's Actions secrets, or "no license" if you cannot provide it (the executor will then propose a reduced-scope standalone gate — e.g. pint-only — as a documented fallback).</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Operator approves the outward push and CI is watched to green</name>
  <action>Halt and wait for the operator's explicit push decision below, then resume on their signal. Do not push without approval — the push is outward-facing and republishes tracked `.planning/` docs to the already-public repo.</action>
  <what-built>The corrected workflow, the README install fixes (05-19), and the ROADMAP revert (05-20) are committed locally on master. Pushing publishes 147+ commits to the ALREADY-PUBLIC repo and starts the CI matrix.</what-built>
  <how-to-verify>
    Before approving, weigh the outward-facing consequence: `.planning/` is tracked in this repo, so the push republishes internal planning docs — including staging hostnames such as `your-staging-host.example` — to the public repo. Security-sweep Step B (the `.planning/` redaction) is still deferred (`launch-01-SECURITY-SWEEP.md` = PARTIAL). Decide one of:
    1. Approve the push as-is (accept that internal `.planning/` content becomes public), OR
    2. Ask to run the deferred security-sweep Step B redaction first, THEN push, OR
    3. Hold the push (the CI fix stays local until you are ready).
    On approval, the executor runs `git push origin master`, then `gh run watch` (or `gh run list`) on the resulting run and reports whether all four matrix cells (php 8.3/8.4 × full-lovata/minimal) go green. If any cell is red, the executor iterates on `metapixel-qa.yml` and re-pushes until green or reports the blocking failure.
  </how-to-verify>
  <resume-signal>Type "push approved" to push and watch CI to green, "redact first" to run Step B before pushing, or "hold" to leave the CI fix local.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local git → public origin | Pushing publishes every tracked file, including `.planning/` internal docs, to the world-readable repo |
| operator license → GitHub Actions secret | The October gateway credential is stored in GitHub and consumed by CI composer installs |
| CI runner → gateway.octobercms.com | Authenticated dependency download of october/system + lovata/* |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-21-01 | Information disclosure | tracked `.planning/` docs on push | high | mitigate | Human-verify checkpoint surfaces the `.planning/` staging-hostname exposure and offers to run security-sweep Step B before pushing |
| T-05-21-02 | Information disclosure | `OCTOBER_AUTH_JSON` secret | high | mitigate | Stored only as a GitHub Actions secret; written to a runner-local auth.json, never echoed or committed; workflow keeps `permissions: contents: read` |
| T-05-21-03 | Elevation of privilege | workflow token scope | medium | mitigate | Preserve least-privilege `permissions: contents: read` — the QA gate cannot push refs, create releases, or tags |
| T-05-21-04 | Tampering | invalid/stale workflow | medium | mitigate | Local YAML validation in Task 1 + watch-to-green iteration at the push checkpoint |
</threat_model>

<verification>
- Task 1: workflow is valid YAML, standalone-layout paths only, gateway-auth step present — verified locally without network.
- Checkpoints gate the two operator-only actions: providing the gateway license secret and approving the outward push.
- Green CI is confirmed via `gh run watch` after the approved push; a red cell drives another workflow iteration.
- Explicitly OUT of scope (operator-gated, NOT in this plan): `v2.0.0` tag creation (awaits LAUNCH SCHEDULED), security-sweep Step B decision, repo-visibility flip (already public).
</verification>

<success_criteria>
metapixel-qa.yml runs on the standalone public repo, the operator supplies the gateway secret, master is pushed with explicit operator approval (security tradeoff surfaced), and the CI matrix is watched to green on HEAD — clearing the CI-red + unpushed-commits half of UAT test 9. Tag creation remains correctly gated on the operator's LAUNCH SCHEDULED signal.
</success_criteria>

<output>
Create `.planning/phases/05-documentation-marketplace-launch/05-21-SUMMARY.md` when done.
</output>
