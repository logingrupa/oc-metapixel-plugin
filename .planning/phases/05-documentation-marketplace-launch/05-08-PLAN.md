---
phase: 5
plan: 08
plan_id: 05-08
type: execute
wave: 6
depends_on: [05-06]
files_modified:
  - .planning/phases/05-documentation-marketplace-launch/05-SMOKE-LOG.md
  - plugins/logingrupa/metapixel/docs/screenshots/01-settings.png
  - plugins/logingrupa/metapixel/docs/screenshots/02-failed-events-list.png
  - plugins/logingrupa/metapixel/docs/screenshots/03-replay-flow.png
  - plugins/logingrupa/metapixel/docs/screenshots/04-check-dedup.png
  - plugins/logingrupa/metapixel/docs/screenshots/05-twig-api.png
autonomous: false
threat_refs: [T-05-03]
requirements: [DOCS-01, MKT-03]

must_haves:
  truths:
    - "On new.nailscosmetics.lv, operator places a Purchase, browses a product PDP (firing ViewContent via theme Twig API), and reloads any layout page (firing PageView via PixelHead) — three event classes captured"
    - "EventLog table contains 6+ rows (3 events × 2 channels minimum) for the smoke window; query: SELECT count(*), event_name, channel FROM logingrupa_metapixel_event_log GROUP BY event_name, channel"
    - "5 PNG screenshots saved at plugins/logingrupa/metapixel/docs/screenshots/0[1-5]-*.png — Settings UI, FailedEvents list, Replay flow success, CheckDedup with Meta API response, theme Twig API on a real product page (paths are plugin-relative; Wave 0 AssetsExistTest uses `dirname(__DIR__, 3).'/docs/screenshots/...'` from tests/Feature/Docs/ which resolves to the PLUGIN ROOT — operator MUST save PNGs under the plugin's `docs/screenshots/` directory, NOT a repo-root `docs/screenshots/`)"
    - "Before any screenshot capture, operator sets Settings to D-18 dummy values via OctoberCMS Backend UI (PRIMARY path): Backend → Settings → Marketing → Meta Pixel + CAPI; pixel_id='000000000000000', capi_access_token='REDACTED_FOR_DEMO_DO_NOT_USE'; save. SQL UPDATE is a fallback if Backend UI is unavailable. After capture, operator restores real values. Screenshots show ONLY dummy values."
    - "05-SMOKE-LOG.md captures: timestamp, env, exact button clicks, EventLog row counts, Test Events screenshot count, fbp/fbc cookie values (redact-friendly format), event_id sample, fail/pass per step"
    - "README walkthrough (plan 05-09) copies the validated step sequence verbatim from 05-SMOKE-LOG.md"
  artifacts:
    - path: ".planning/phases/05-documentation-marketplace-launch/05-SMOKE-LOG.md"
      provides: "Operator-authored smoke audit trail per D-08"
    - path: "plugins/logingrupa/metapixel/docs/screenshots/01-settings.png"
      provides: "MKT-03 asset #1 — Backend Settings → Marketing → Meta Pixel + CAPI showing dummy row values"
    - path: "plugins/logingrupa/metapixel/docs/screenshots/02-failed-events-list.png"
      provides: "MKT-03 asset #2 — FailedEvents list view after an intentional bad-token call produced a row"
    - path: "plugins/logingrupa/metapixel/docs/screenshots/03-replay-flow.png"
      provides: "MKT-03 asset #3 — Replay button → success flash on a fixed FailedEvent row"
    - path: "plugins/logingrupa/metapixel/docs/screenshots/04-check-dedup.png"
      provides: "MKT-03 asset #4 — CheckDedup popup showing Meta Test Events status JSON"
    - path: "plugins/logingrupa/metapixel/docs/screenshots/05-twig-api.png"
      provides: "MKT-03 asset #5 — theme product page showing the `{% do this.metapixel.pushEvent(...) %}` Twig snippet OR the resulting fbq script in browser devtools view"
  key_links:
    - from: "05-SMOKE-LOG.md"
      to: "05-09 README walkthrough"
      via: "README copies the validated step sequence verbatim (D-08)"
      pattern: "(workflow hop)"
    - from: "plugins/logingrupa/metapixel/docs/screenshots/0[1-5]-*.png"
      to: "README.md image references"
      via: "Markdown image links by plugin-relative path `docs/screenshots/0[1-5]-*.png`"
      pattern: "!\\[.*\\]\\(docs/screenshots/0[1-5]-.*\\.png\\)"
---

<objective>
Operator runs the full live smoke on `new.nailscosmetics.lv`: places a real Purchase, browses a product (fires ViewContent via Theme Twig API), reloads a layout page (fires PageView via PixelHead). The smoke validates the entire Phase 2 + 3 + 4 stack end-to-end and produces the 5 PNG screenshots for MKT-03. The smoke log records the validated step sequence that the README walkthrough (plan 05-09) copies verbatim.

Purpose: DOCS-01 launch acceptance gate input — the smoke step sequence IS the README walkthrough. Five screenshots become marketplace assets. AddToCart + Theme Twig API custom event are deferred per D-07.

**Screenshot path anchoring (B-4 fix):** All PNG assets MUST live under the plugin's `docs/screenshots/` directory at `plugins/logingrupa/metapixel/docs/screenshots/0[1-5]-*.png`. The Wave 0 `tests/Feature/Docs/AssetsExistTest.php` resolves paths via `dirname(__DIR__, 3).'/docs/screenshots/...'` from `tests/Feature/Docs/`, which resolves to the plugin root — NOT the repository root. Saving PNGs to a repo-root `/docs/screenshots/` would silently break the Wave 0 test.

Output: 5 PNG screenshots committed under `plugins/logingrupa/metapixel/docs/screenshots/` + `05-SMOKE-LOG.md` audit trail. Both consumed by plan 05-09.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-documentation-marketplace-launch/05-CONTEXT.md
@.planning/phases/05-documentation-marketplace-launch/05-RESEARCH.md
@.planning/phases/05-documentation-marketplace-launch/05-PATTERNS.md
@.planning/phases/05-documentation-marketplace-launch/05-06-SUMMARY.md
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| screenshot capture → public repo | PNG files committed to repo; will ship public per D-25/D-27 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03 | Information Disclosure | screenshot accidentally captures real Pixel ID / token | mitigate | D-18 dummy-row strategy via Backend UI BEFORE capture; visual review of every PNG before commit; restore real values AFTER capture |
| T-05-08-01 | Information Disclosure | smoke log captures real customer PII or operator internal hostnames beyond new.nailscosmetics.lv | mitigate | Plan 05-13 sweep redacts new.nailscosmetics.lv refs from .planning/ before public flip; D-08 already prescribes redact-friendly cookie value format |
| T-05-08-02 | Tampering | dummy Settings row left in place after capture | accept | Operator restores real values at end of smoke per Step M; UAT closure includes verifying real Settings values restored |
</threat_model>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Run live smoke, capture 5 screenshots to plugin-relative path, author 05-SMOKE-LOG.md</name>
  <read_first>
    - .planning/phases/05-documentation-marketplace-launch/05-CONTEXT.md D-06 + D-07 + D-08 + D-17 + D-18 (smoke env + event set + log shape + screenshot subjects + dummy-row strategy)
    - .planning/phases/05-documentation-marketplace-launch/05-RESEARCH.md lines 174-247 (smoke flow diagram) + lines 478-490 (Pitfall 3 — screenshot real Pixel ID leak)
    - .planning/phases/05-documentation-marketplace-launch/05-PATTERNS.md lines 649-664 (smoke-log required fields per D-08)
    - plugins/logingrupa/metapixel/models/Settings.php (Multisite trait — Settings row update happens via Backend UI top-bar site picker; SQL UPDATE is fallback)
    - plugins/logingrupa/metapixel/controllers/FailedEvents.php (Replay + CheckDedup buttons — exact button labels for screenshots)
  </read_first>
  <what-built>
    Phases 2 + 3 + 4 shipped the full stack: ShopaholicOrderAdapter dispatches Purchase via SendCapiEvent + MetaClient; ThemeActionAdapter handles theme-Twig pushEvent; PixelHead component emits base PageView in layout `<head>`; EventPixel emits deduped Purchase browser-pixel on order-complete; FailedEvents backend UI exposes Replay + CheckDedup. Gate 3 (plan 05-06 Task 2) confirmed event_id round-trip works.
  </what-built>
  <how-to-verify>
    **Path callout (B-4):** All screenshot capture steps below save PNGs to the plugin's `docs/screenshots/` directory. From the project root that is `plugins/logingrupa/metapixel/docs/screenshots/`. From the plugin root (after `cd plugins/logingrupa/metapixel`) the relative path is `docs/screenshots/`. The Wave 0 test expects the plugin-root-relative layout — saving to a repo-root `/docs/screenshots/` will break the AssetsExistTest. Operator runs `cd /home/forge/nailscosmetics.lv/plugins/logingrupa/metapixel` BEFORE any screenshot file write so relative-path tooling resolves correctly.

    Operator steps:

    Step PRE — Set Settings to D-18 dummy values BEFORE any screenshot is taken. **PRIMARY path — OctoberCMS Backend UI (W-15 fix):**
      1. Sign into Backend at `https://new.nailscosmetics.lv/<backend-uri>` with operator credentials
      2. Navigate Settings → Marketing → Meta Pixel + CAPI
      3. In the Pixel ID field, clear current value and enter `000000000000000`
      4. In the CAPI Access Token field, clear current value and enter `REDACTED_FOR_DEMO_DO_NOT_USE`
      5. Click Save (look for the green flash confirmation)
      6. Refresh the Settings page once — confirm both fields persist with the dummy values
      7. Note the SQL row state for reproducibility in the smoke log (the actual UPDATE statement October emitted appears in `storage/logs/laravel.log` if query logging is on, OR can be reconstructed from the form state)

    **FALLBACK path — direct SQL** (advanced operators only, e.g. if Backend UI is broken):
    ```sql
    -- Verify column shape first
    DESCRIBE system_settings;
    -- Then issue an UPDATE matching the actual JSON path discovered above. October stores
    -- Lovata.Toolbox CommonSettings under `item = 'logingrupa_metapixel_settings'`. Example:
    UPDATE system_settings
       SET value = JSON_SET(value, '$.pixel_id', '000000000000000',
                                   '$.capi_access_token', 'REDACTED_FOR_DEMO_DO_NOT_USE')
     WHERE item = 'logingrupa_metapixel_settings';
    ```
    Document whichever path was used in the smoke log.

    Step A — `01-settings.png`. Login to Backend → Settings → Marketing → Meta Pixel + CAPI. Verify the Pixel ID field reads `000000000000000` and the access_token field reads `REDACTED_FOR_DEMO_DO_NOT_USE` (or masked). Capture screenshot. Save to `plugins/logingrupa/metapixel/docs/screenshots/01-settings.png` (PNG, lossless).

    Step B — Restore real Settings values via the same Backend UI path (so the rest of the smoke produces real events). Place a real test order (Purchase event). On the order-complete page, verify EventPixel emits the Purchase fbq with matching event_id. Record: order ID, secret_key, event_id (UUID).

    Step C — Trigger ViewContent. Visit any product PDP — the theme's `{% do this.metapixel.pushEvent({name:'ViewContent', action_key:'product-view:'~product.id, content_ids:[...], value:..., currency:'EUR'}) %}` (Phase 3 THEM-04) fires the event. Verify in Pixel Helper + Test Events.

    Step D — Trigger PageView. Reload `/` or `/catalog` — PixelHead emits the base PageView. Verify in Pixel Helper + Test Events.

    Step E — `05-twig-api.png`. Capture either the Twig source snippet from theme `pages/product.htm` shown in an editor/GitHub render OR the resulting browser DevTools showing the inline `<script>` fbq emission. Save to `plugins/logingrupa/metapixel/docs/screenshots/05-twig-api.png`.

    Step F — Set Settings to D-18 dummy values AGAIN via Backend UI. Verify a Backend Settings view still shows dummy values.

    Step G — Force a FailedEvent row. Update Settings via Backend UI to an obviously-bad capi_access_token like `EAA-DUMMY-FAIL`. Place another small test order — CAPI dispatch will fail with Meta 4xx → SendCapiEvent::failed() writes a FailedEvent row + fires dead_letter. Backend → Settings → Marketing → Failed events now has one row.

    Step H — `02-failed-events-list.png`. Capture the FailedEvents list view showing at least one row with columns event_id / event_name / adapter_type / http_status / attempts / created_at / graph_error snippet. Save to `plugins/logingrupa/metapixel/docs/screenshots/02-failed-events-list.png`.

    Step I — Restore real Settings values temporarily so Replay works.

    Step J — `03-replay-flow.png`. Click the Replay button on the row. The MetaClient re-dispatches, attempts increments, flash-success appears. Capture both the row state after replay AND the success flash banner. Save to `plugins/logingrupa/metapixel/docs/screenshots/03-replay-flow.png`.

    Step K — `04-check-dedup.png`. Click CheckDedup. The endpoint queries Meta Test Events status via MetaClient::fetchTestEventsStatus and returns JSON with dedup % + EMQ. Capture the resulting popup/panel. Save to `plugins/logingrupa/metapixel/docs/screenshots/04-check-dedup.png`.

    Step L — Visual review: open every PNG. Confirm Pixel ID field never shows >0 non-dummy digits. Confirm access_token field never shows EAA-prefix real tokens. Confirm no other UI element accidentally captured real customer PII or sensitive emails.

    Step M — Restore real Settings values for ongoing `new.nailscosmetics.lv` operation via Backend UI.

    Step Z — Author `.planning/phases/05-documentation-marketplace-launch/05-SMOKE-LOG.md` with: full timestamp per step, env=new.nailscosmetics.lv, exact button-click sequence per step (PRE..M, noting whether Backend UI or SQL fallback was used for Step PRE), EventLog row counts via `SELECT count(*), event_name, channel FROM logingrupa_metapixel_event_log WHERE created_at >= '<smoke-start-time>' GROUP BY event_name, channel`, Test Events screenshot count (5), fbp/fbc cookie values (redact-friendly: first 6 chars + "..." + last 4 chars), event_id samples per event, dummy/real Settings transitions per step, pass/fail per step, and "READY FOR README (plan 05-09)" handoff line.

    Commit the 5 PNG files (paths plugin-relative: `cd plugins/logingrupa/metapixel && git add docs/screenshots/0[1-5]-*.png` OR from repo root `git add plugins/logingrupa/metapixel/docs/screenshots/0[1-5]-*.png`) + the smoke log. Commit message draft: `docs(05-08): live smoke on new.nailscosmetics.lv + 5 marketplace screenshots`.
  </how-to-verify>
  <resume-signal>
    Type `SMOKE LOG COMPLETE` to advance to plan 05-09. If any anomaly was found during smoke that needs `/gsd-debug`, type `SMOKE FAIL — <bug> routed to /gsd-debug` and resume only after the fix-plan lands.
  </resume-signal>
</task>

</tasks>

<verification>
- 5 PNG files exist at `plugins/logingrupa/metapixel/docs/screenshots/01-settings.png` through `05-twig-api.png`
- `05-SMOKE-LOG.md` exists with timestamps, EventLog counts, event_ids, pass/fail per step, Backend-UI-vs-SQL choice documented for Step PRE
- `SELECT count(*), event_name, channel FROM logingrupa_metapixel_event_log WHERE created_at BETWEEN '<smoke-start>' AND '<smoke-end>' GROUP BY event_name, channel` returns ≥6 rows (3 event_names × 2 channels) — visible in smoke log
- All screenshots show dummy Settings values (operator-confirmed visual review)
- Settings restored to real values after smoke
</verification>

<success_criteria>
- 6 file artifacts committed (5 PNGs at plugin-relative path + smoke log)
- Smoke produces 3 event classes × 2 channels = 6+ EventLog rows (D-07)
- Screenshot visual review confirms no Pixel ID / token leak (D-18)
- README walkthrough (plan 05-09) has its step sequence input ready
</success_criteria>

<output>
Create `.planning/phases/05-documentation-marketplace-launch/05-08-SUMMARY.md` when done — note smoke start + end timestamps, event_id samples per event, screenshot byte sizes, the Settings dummy-row mechanism used (Backend UI primary OR SQL fallback) for plan 05-12 reproducibility, and "READY FOR README (plan 05-09)" handoff.
</output>
</content>
</invoke>