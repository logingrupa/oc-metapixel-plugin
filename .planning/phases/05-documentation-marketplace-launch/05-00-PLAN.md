---
phase: 5
plan: 00
plan_id: 05-00
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/Feature/Docs/ReadmeStructureTest.php
  - tests/Feature/Docs/CustomAdaptersStructureTest.php
  - tests/Feature/Docs/AssetsExistTest.php
  - tests/Feature/Plugin/PluginYamlSanityTest.php
autonomous: true
threat_refs: []
requirements: [DOCS-01, DOCS-02, DOCS-03, MKT-02, MKT-03]

must_haves:
  truths:
    - "Four Pest test files exist under tests/Feature/{Docs,Plugin}/"
    - "Each test extends Logingrupa\\Metapixel\\Tests\\MetapixelTestCase via final class"
    - "Each test currently FAILS RED because target artifacts (README.md, docs/CUSTOM-ADAPTERS.md, docs/screenshots/, CHANGELOG.md) do not yet exist — RED-GREEN cycle anchored for downstream plans"
    - "Tests load files hermetically via dirname(__DIR__, 3) — no Translator/Filesystem facade"
  artifacts:
    - path: "tests/Feature/Docs/ReadmeStructureTest.php"
      provides: "DOCS-01 + DOCS-02 grep gates on README.md (≥7 named sections, no v1.x refs, lang-key label coverage)"
    - path: "tests/Feature/Docs/CustomAdaptersStructureTest.php"
      provides: "DOCS-03 grep gates on docs/CUSTOM-ADAPTERS.md (3 hook constants, OFFLINE\\Mall inline, ContractTestCase ref, AdapterRegistry::instance()->register snippet)"
    - path: "tests/Feature/Docs/AssetsExistTest.php"
      provides: "MKT-03 file-existence gates (5 PNG screenshots matching 0[1-5]-*.png glob; CHANGELOG.md with ## [2.0.0] - YYYY-MM-DD header + ### Added subsection + no v1.x diff)"
    - path: "tests/Feature/Plugin/PluginYamlSanityTest.php"
      provides: "MKT-02 yaml-parse assertions (name+description lang-key, author 'Logingrupa', icon 'icon-bullseye', homepage GitHub URL)"
  key_links:
    - from: "tests/Feature/Docs/*"
      to: "README.md, docs/CUSTOM-ADAPTERS.md, docs/screenshots/, CHANGELOG.md"
      via: "file_get_contents(dirname(__DIR__, 3).'/<path>')"
      pattern: "dirname\\(__DIR__,\\s*3\\)\\.'/"
    - from: "tests/Feature/Plugin/PluginYamlSanityTest.php"
      to: "plugin.yaml"
      via: "Symfony\\Component\\Yaml\\Yaml::parseFile"
      pattern: "Yaml::parseFile"
---

<objective>
Ship four Pest 4 test files that anchor the RED side of the Wave 0 RED→GREEN cycle for Phase 5 doc/manifest/assets deliverables. Tests currently fail because the artifacts they assert against (README.md, docs/CUSTOM-ADAPTERS.md, docs/screenshots/0[1-5]-*.png, CHANGELOG.md) do not yet exist. Plans 05-09, 05-10, 05-12 turn them GREEN.

Purpose: Anchor goal-backward verification before content authoring. Tests are the executable contract for what each downstream doc/manifest plan must produce. No new framework install — Pest 4 + Larastan already shipped Phase 1.

Output: 4 test files extending MetapixelTestCase, all in RED state. composer qa exits non-zero on the new tests (expected); existing 430+ test suite stays green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-documentation-marketplace-launch/05-CONTEXT.md
@.planning/phases/05-documentation-marketplace-launch/05-RESEARCH.md
@.planning/phases/05-documentation-marketplace-launch/05-PATTERNS.md
@.planning/phases/05-documentation-marketplace-launch/05-VALIDATION.md
@plugins/logingrupa/metapixel/tests/Feature/Lang/LangKeyCoverageTest.php
@plugins/logingrupa/metapixel/tests/Unit/PluginSanityTest.php
@plugins/logingrupa/metapixel/tests/MetapixelTestCase.php
@plugins/logingrupa/metapixel/plugin.yaml
@plugins/logingrupa/metapixel/lang/en/lang.php

<interfaces>
- MetapixelTestCase under namespace `Logingrupa\Metapixel\Tests` — autoload via `tests/Pest.php` + classmap (Phase 1 TOOL-08)
- Hermetic file-load helper: `dirname(__DIR__, 3).'/<relpath>'` resolves plugin root from `tests/Feature/<Group>/`
- Symfony Yaml component (`Symfony\Component\Yaml\Yaml::parseFile`) — already required transitively by October (no new composer require)
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none in Wave 0) | Wave 0 produces hermetic file-load tests only — no user input, no network, no DB writes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-00-01 | Tampering | tests/Feature/Docs/*.php | accept | Tests are read-only filesystem assertions; no input surface. ASVS L1 V5 NA. |

(Phase 5 threat model anchors live in plans 05-08, 05-12, 05-13 — see those files for T-05-01 secret-history, T-05-03 screenshot-leak, T-05-04 webhook-url-leak.)
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Ship ReadmeStructureTest.php + CustomAdaptersStructureTest.php</name>
  <read_first>
    - plugins/logingrupa/metapixel/tests/Feature/Lang/LangKeyCoverageTest.php (canonical hermetic-file-load analog, lines 1-105)
    - plugins/logingrupa/metapixel/tests/MetapixelTestCase.php (base class — must be extended by `final class … extends MetapixelTestCase`)
    - plugins/logingrupa/metapixel/lang/en/lang.php (DOCS-02 walkthrough fidelity — every `field.*_label` value also appears in README)
    - plugins/logingrupa/metapixel/classes/queue/SendCapiEvent.php (lines 27-47 — 3 hook constant strings DOCS-03 doc must contain)
    - plugins/logingrupa/metapixel/.planning/phases/05-documentation-marketplace-launch/05-PATTERNS.md (lines 252-350 "Wave 0 Tests" section — assertions enumeration)
    - plugins/logingrupa/metapixel/.planning/phases/05-documentation-marketplace-launch/05-VALIDATION.md (Per-Task Verification Map row for DOCS-01/02/03)
  </read_first>
  <action>
    Create file `tests/Feature/Docs/ReadmeStructureTest.php`. Open with `<?php` then `use Logingrupa\Metapixel\Tests\MetapixelTestCase;`. Declare `final class ReadmeStructureTest extends MetapixelTestCase`. Add private helper `loadReadme(): string` returning `(string) file_get_contents(dirname(__DIR__, 3).'/README.md')`. Add public test methods (PHPUnit attribute style, snake_case names per PATTERNS file): `test_readme_file_exists` (assertFileExists at dirname(__DIR__,3).'/README.md' with explicit message "README.md must ship at plugin root for DOCS-01."); `test_readme_contains_seven_named_sections` (preg_match_all '/^## (Install|Configure|Acquire|Shopaholic|Theme|FailedEvents|Troubleshoot)/m' must return ≥7 — assertGreaterThanOrEqual); `test_readme_has_no_v1x_references` (assertStringNotContainsString 'v1.1.1' and 'legacy/v1' — D-22 + D-23 lock); `test_readme_install_block_shows_october_up` (assertStringContainsString 'php artisan october:up' — Pitfall 5 mitigation); `test_readme_install_block_shows_vcs_repositories_pattern` (assertStringContainsString '"type":"vcs"' OR '"type": "vcs"' — D-25 VCS install path); `test_readme_anchors_field_labels_from_lang_en` (load lang/en/lang.php via require dirname(__DIR__,3).'/lang/en/lang.php', flatten to leaf scalar values, for any non-empty string value under the `field` sub-array assert it appears verbatim in the README content — DOCS-02 walkthrough fidelity). The last test is the tightest gate; document its rationale in a one-line method docblock. NO subjective language anywhere — only literal-string + regex assertions.

    Create file `tests/Feature/Docs/CustomAdaptersStructureTest.php` (same header/class style). Helper `loadCustomAdaptersDoc(): string` returning `(string) file_get_contents(dirname(__DIR__, 3).'/docs/CUSTOM-ADAPTERS.md')`. Tests: `test_custom_adapters_doc_file_exists`; `test_doc_contains_all_three_hook_constants` (substr_count for each of 'metapixel.event.before_dispatch', 'metapixel.event.after_dispatch', 'metapixel.event.dead_letter' returns ≥1 per constant — three separate asserts to mirror D-15); `test_doc_contains_offline_mall_inline_example` (assertStringContainsString 'OFFLINE\\Mall' AND assertStringContainsString 'mall.order' — opaque alias from D-14); `test_doc_contains_contract_testcase_reference` (assertStringContainsString 'EventSubjectAdapterContractTestCase' AND assertStringContainsString 'makeAdapter' AND assertStringContainsString 'makeSubject' — D-16); `test_doc_shows_register_pattern` (assertStringContainsString 'AdapterRegistry::instance()->register' OR assertStringContainsString 'AdapterRegistry::register'). All assertions use literal strings — no fuzzy matching.
  </action>
  <verify>
    <automated>cd /home/forge/nailscosmetics.lv/plugins/logingrupa/metapixel && vendor/bin/pest --filter='ReadmeStructure|CustomAdapters' 2>&1 | tail -20</automated>
  </verify>
  <done>
    Both files exist at `tests/Feature/Docs/`. `vendor/bin/pest --filter='ReadmeStructure'` runs and reports failing tests with the message "README.md must ship at plugin root for DOCS-01." (RED — README.md does not yet exist). Same for CustomAdaptersStructureTest reporting docs/CUSTOM-ADAPTERS.md missing. No PHP parse errors. PHPStan exit 0 on both files: `vendor/bin/phpstan analyse tests/Feature/Docs/ReadmeStructureTest.php tests/Feature/Docs/CustomAdaptersStructureTest.php --no-progress --memory-limit=512M`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Ship AssetsExistTest.php + PluginYamlSanityTest.php</name>
  <read_first>
    - plugins/logingrupa/metapixel/tests/Feature/Lang/LangKeyCoverageTest.php (file-exists assertion pattern, lines 76-103)
    - plugins/logingrupa/metapixel/tests/Unit/PluginSanityTest.php (pluginDetails + author + lang-key assertion shape, lines 22-30)
    - plugins/logingrupa/metapixel/plugin.yaml (current shape — verify keys: name=`logingrupa.metapixel::lang.plugin.name`, description=`logingrupa.metapixel::lang.plugin.description`, author=`Logingrupa`, icon=`icon-bullseye`, homepage=`https://github.com/logingrupa/oc-metapixel-plugin`)
    - plugins/logingrupa/metapixel/.planning/phases/05-documentation-marketplace-launch/05-PATTERNS.md (lines 360-400 PluginYamlSanityTest derivation)
    - plugins/logingrupa/metapixel/.planning/phases/05-documentation-marketplace-launch/05-VALIDATION.md (MKT-02 + MKT-03 rows)
  </read_first>
  <action>
    Create file `tests/Feature/Docs/AssetsExistTest.php`. Header `<?php` + `use Logingrupa\Metapixel\Tests\MetapixelTestCase;`. `final class AssetsExistTest extends MetapixelTestCase`. Tests: `test_five_screenshots_present_with_padded_prefix` (use `glob(dirname(__DIR__,3).'/docs/screenshots/0[1-5]-*.png')` — assertCount 5 with message "MKT-03 demands 5 PNG screenshots at docs/screenshots/0[1-5]-*.png"); `test_changelog_file_exists` (assertFileExists CHANGELOG.md); `test_changelog_has_v2_section_header_with_iso_date` (assertMatchesRegularExpression '/^## \\[2\\.0\\.0\\] - \\d{4}-\\d{2}-\\d{2}$/m' against CHANGELOG content — Keep-a-Changelog 1.1.0 format, planner Discretion locked YYYY-MM-DD); `test_changelog_has_added_subsection` (assertStringContainsString '### Added'); `test_changelog_has_no_v1x_diff_text` (assertStringNotContainsString 'v1.1.1' AND assertStringNotContainsString 'legacy/v1' — D-22 lock).

    Create file `tests/Feature/Plugin/PluginYamlSanityTest.php` (parallel directory `tests/Feature/Plugin/` — already used by Phase 3 ShopaholicConditionalRegistrationTest per PATTERNS line 23). Header + `use Logingrupa\Metapixel\Tests\MetapixelTestCase;` + `use Symfony\Component\Yaml\Yaml;`. `final class PluginYamlSanityTest extends MetapixelTestCase`. Tests: `test_plugin_yaml_parses` (Yaml::parseFile returns array with 'plugin' key); `test_plugin_name_is_lang_key` (assertSame 'logingrupa.metapixel::lang.plugin.name'); `test_plugin_description_is_lang_key` (assertSame 'logingrupa.metapixel::lang.plugin.description'); `test_plugin_author_is_logingrupa` (assertSame 'Logingrupa'); `test_plugin_icon_is_icon_bullseye` (assertSame 'icon-bullseye' — D-20 lock); `test_plugin_homepage_matches_github_vcs_url` (assertMatchesRegularExpression '#^https://github\\.com/logingrupa/oc-metapixel-plugin$#'). NOTE plan 05-12 verifies plugin.yaml is already MKT-02-ready per PATTERNS — these tests should run GREEN immediately for MKT-02 (plugin.yaml already in shape); the other 3 tests stay RED until plans 05-09/05-10/05-12 land their artifacts.
  </action>
  <verify>
    <automated>cd /home/forge/nailscosmetics.lv/plugins/logingrupa/metapixel && vendor/bin/pest --filter='AssetsExist|PluginYamlSanity' 2>&1 | tail -30</automated>
  </verify>
  <done>
    Both files exist at correct paths. `vendor/bin/pest --filter='PluginYamlSanity'` reports all 6 PluginYamlSanityTest tests GREEN (plugin.yaml already meets MKT-02 — verified RESEARCH § Pattern Map). `vendor/bin/pest --filter='AssetsExist'` reports all 5 AssetsExistTest tests RED (no screenshots, no CHANGELOG.md yet). PHPStan exit 0: `vendor/bin/phpstan analyse tests/Feature/Docs/AssetsExistTest.php tests/Feature/Plugin/PluginYamlSanityTest.php --no-progress --memory-limit=512M`. Full `composer qa` is NOT expected to be green at end of plan (Wave 0 is RED-anchor).
  </done>
</task>

</tasks>

<verification>
- Four test files exist at `tests/Feature/Docs/{ReadmeStructure,CustomAdapters,AssetsExist}Test.php` + `tests/Feature/Plugin/PluginYamlSanityTest.php`
- Each file: declares `final class … extends Logingrupa\\Metapixel\\Tests\\MetapixelTestCase`; uses hermetic file-load via `dirname(__DIR__, 3)`
- PluginYamlSanityTest 6/6 GREEN (existing plugin.yaml meets MKT-02)
- Other 3 test files RED — each test method reports the missing-artifact message used by downstream plans
- PHPStan level 10 passes on all 4 files
- No production code modified (no `classes/`, `models/`, `Plugin.php` touched)
</verification>

<success_criteria>
- Pest can discover and run all 4 new test files (no autoload/syntax errors)
- PluginYamlSanityTest GREEN; the other three RED with specific missing-file/missing-section messages
- `composer qa` exit code may be non-zero (RED expected on 3 files); existing 430+ test suite remains GREEN
- The Per-Task Verification Map in 05-VALIDATION.md now has automated commands populated for DOCS-01/02/03 and MKT-02/03
</success_criteria>

<output>
Create `.planning/phases/05-documentation-marketplace-launch/05-00-SUMMARY.md` when done. Note which tests are RED vs GREEN at plan close (anchor for plans 05-09/05-10/05-12 to flip RED→GREEN).
</output>
