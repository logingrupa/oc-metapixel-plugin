---
phase: 5
plan: 03
plan_id: 05-03
type: execute
wave: 3
depends_on: [05-02]
files_modified:
  - .planning/phases/05-documentation-marketplace-launch/05-03-UAT-GATE-1.md
autonomous: false
threat_refs: []
requirements: [DOCS-01]

must_haves:
  truths:
    - "Operator verifies ZERO Pixel events fire on /, /catalog, /product/<slug>, /checkout, /order-complete after the strip in plan 05-02"
    - "Three independent verification sources converge on zero: Meta Pixel Helper Chrome extension, Meta Events Manager Test Events live view, logingrupa_metapixel_event_log DB tail"
    - "Operator confirmation is recorded in 05-03-UAT-GATE-1.md with named PASS/FAIL per source, page-by-page"
    - "Without operator PASS this gate, plan 05-04 (PixelHead) MUST NOT execute"
  artifacts:
    - path: ".planning/phases/05-documentation-marketplace-launch/05-03-UAT-GATE-1.md"
      provides: "Operator-signed UAT log — timestamp, env, three-source verdict per page, overall PASS/FAIL"
  key_links:
    - from: "05-02 strip commit"
      to: "05-03 UAT gate"
      via: "operator deploys the strip commit to new.nailscosmetics.lv, then manually verifies"
      pattern: "(workflow hop)"
    - from: "05-03 PASS"
      to: "05-04 PixelHead wire"
      via: "operator approval unblocks the next wave"
      pattern: "(gate hop)"
---

<objective>
Operator-confirmed UAT gate. After plan 05-02 strip ships to `new.nailscosmetics.lv`, operator verifies via three independent sources that ZERO Facebook Pixel events fire across the 5 critical pages. Without this PASS, plan 05-04 (PixelHead wire) does not execute. Pitfall 4 mitigation: three-source convergence required (operator MUST tick all three, not one combined PASS).

Purpose: Anchor the gated UAT cutover pattern (D-03, D-05). Strip is observably clean before any new emission lands; double-counting between legacy + new is impossible because legacy has been demonstrably removed (and the dead v1.x `purchasePixel` component refs were also stripped in plan 05-02 Task 2 so the page doesn't 500 here).

Output: `05-03-UAT-GATE-1.md` — operator-authored markdown log with PASS/FAIL per page × per source. Resume signal: operator types "GATE 1 PASS" or describes failure.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-documentation-marketplace-launch/05-CONTEXT.md
@.planning/phases/05-documentation-marketplace-launch/05-RESEARCH.md
@.planning/phases/05-documentation-marketplace-launch/05-PATTERNS.md
@.planning/phases/05-documentation-marketplace-launch/05-02-SUMMARY.md
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → UAT log | Operator-authored markdown; no executable surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03-01 | Spoofing | UAT PASS without verification | mitigate | Three-source convergence required (Pitfall 4) — each source has its own measurable threshold |
| T-05-03-02 | Information Disclosure | screenshots accidentally captured during UAT | accept | Operator is informed by the plan to NOT commit screenshots from UAT gates; only plan 05-08 produces formal screenshots from the smoke |
</threat_model>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Deploy strip to new.nailscosmetics.lv + author UAT-GATE-1 log</name>
  <read_first>
    - .planning/phases/05-documentation-marketplace-launch/05-02-SUMMARY.md (strip commit SHA + file list, including dead-purchasePixel removal confirmation)
    - .planning/phases/05-documentation-marketplace-launch/05-PATTERNS.md lines 582-587 (UAT Gate 1 pass criterion template)
    - .planning/phases/05-documentation-marketplace-launch/05-RESEARCH.md lines 295-310 (gated UAT cutover pattern + three-source convergence)
    - .planning/phases/05-documentation-marketplace-launch/05-CONTEXT.md D-03 + D-05 (locked UAT decisions)
  </read_first>
  <what-built>
    Plan 05-02 stripped legacy Facebook Pixel emission from themes/logingrupa-naisstore/: 4 files deleted, 11 files edited, bundle rebuilt. Source + bundle grep returns zero `fbq` strings AND zero references to the dead v1.x `purchasePixel` component alias on order-complete pages — so the page does not 500-error at render (Plugin.php registers only `eventPixel` + `pixelHead`).
  </what-built>
  <how-to-verify>
    Operator steps (Claude does NOT auto-deploy — operator owns Forge deploy + UI verification):

    Step A — Deploy the strip commit. Operator runs the deploy on Forge for `new.nailscosmetics.lv` (Forge UI Sites → new.nailscosmetics.lv → Deploy Now, or git push to the deploy branch). Confirm release timestamp post-strip-commit.

    Step B — Verify on 5 page URLs. Open Meta Pixel Helper Chrome extension. Visit each URL in turn on `https://new.nailscosmetics.lv/`:
      1. `/` (homepage)
      2. `/catalog` (or `/lv/catalog`)
      3. `/product/<any product slug>` (a PDP route)
      4. `/checkout` (Shopaholic checkout)
      5. `/order-complete` (post-purchase route — may require placing a test order first; if so, place + cancel the order or use the proforma route)

    For EACH URL record three sources independently:
      - Pixel Helper popup: shows N events for the current page-load — PASS condition N = 0
      - Meta Events Manager → Test Events live view (left rail filter on the configured Pixel ID and `test_event_code` set in Settings): shows N new event rows in the last 60 seconds — PASS condition N = 0
      - DB tail: SSH to Forge box → `mysql -u <user> -p <db> -e "SELECT count(*) FROM logingrupa_metapixel_event_log WHERE created_at >= NOW() - INTERVAL 1 MINUTE"` — PASS condition count = 0

    Also confirm `/order-complete` returns HTTP 200 (not 500). A 500 here would indicate plan 05-02 missed the dead-`purchasePixel` strip.

    Step C — Author the UAT log. Create `.planning/phases/05-documentation-marketplace-launch/05-03-UAT-GATE-1.md` with this structure:

    ```
    # UAT Gate 1 — Zero Events After Strip

    **Date:** YYYY-MM-DD HH:MM (operator timezone)
    **Env:** https://new.nailscosmetics.lv
    **Deploy SHA:** <commit-sha-from-05-02>
    **Operator:** <name>

    ## Three-source verdict per page

    | Page | HTTP status | Pixel Helper count | Test Events count | EventLog count | Verdict |
    |------|-------------|---------------------|-------------------|----------------|---------|
    | / | 200 | 0 | 0 | 0 | PASS |
    | /catalog | … | … | … | … | … |
    | /product/<slug> | … | … | … | … | … |
    | /checkout | … | … | … | … | … |
    | /order-complete | … | … | … | … | … |

    ## Anomalies (if any)

    (free-form notes per page — anomaly description, expected vs actual, screenshot path if helpful)

    ## Overall verdict

    GATE 1 PASS  (or  GATE 1 FAIL — see Anomalies)
    ```

    Step D — If any cell is non-zero OR any HTTP status is not 200: do NOT type the resume signal. Open a `/gsd-debug` session per D-09.
  </how-to-verify>
  <resume-signal>
    Type `GATE 1 PASS` exactly to advance to plan 05-04. If FAIL, describe the failing source + page count + suspected cause (open `/gsd-debug` per D-09).
  </resume-signal>
</task>

</tasks>

<verification>
- `.planning/phases/05-documentation-marketplace-launch/05-03-UAT-GATE-1.md` exists and is signed by operator
- The 5-row table has all 20 cells filled (5 pages × {HTTP status, Pixel Helper, Test Events, EventLog}) — all zeros OR documented anomaly + remediation
- Operator resume signal received exactly as `GATE 1 PASS`
</verification>

<success_criteria>
- Three-source convergence on zero for all 5 pages
- /order-complete returns HTTP 200 (dead `purchasePixel` alias confirmed stripped)
- UAT log committed to `.planning/`
- No new code committed in this plan (UAT is read-only)
</success_criteria>

<output>
Create `.planning/phases/05-documentation-marketplace-launch/05-03-SUMMARY.md` when done — note the UAT-GATE-1.md path, the operator name + date, and pass status.
</output>
</content>
</invoke>
