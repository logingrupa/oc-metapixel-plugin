---
phase: 05-documentation-marketplace-launch
plan: 19
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - tests/Feature/Docs/ReadmeStructureTest.php
autonomous: true
gap_closure: true
requirements: [DOCS-01]
must_haves:
  truths:
    - "The README Install section documents `php artisan project:set <license>` as a prerequisite BEFORE the require command (registers the October gateway so `october/system` + `lovata/*` resolve on a fresh install)"
    - "The require command reads `composer require logingrupa/oc-metapixel-plugin -W` with a one-line why for the `-W` flag (fresh October lockfile pins composer/installers that toolbox needs to move)"
    - "The README no longer says 'Meta Business Manager' where the Meta UI and lang/en pixel_id_comment say 'Events Manager' (README ~line 81, D1)"
    - "A single ordered quick-start reaches the first Meta Test Events hit without jumping into the full Shopaholic/Theme walkthroughs (D2)"
    - "`../../../vendor/bin/pest --configuration phpunit.xml --filter=ReadmeStructure` is GREEN — every prior assertion plus the two new install-fidelity assertions pass"
  artifacts:
    - "README.md (Install section corrected + ordered quick-start box added)"
    - "tests/Feature/Docs/ReadmeStructureTest.php (extended with install-fidelity assertions locking the -W flag + project:set prerequisite + quick-start ordered steps)"
  key_links:
    - "ReadmeStructureTest install-fidelity assertions → README Install/Quick-start prose: the gate fails if either fix regresses"
    - "README H2 section set stays intact — the existing `^## (Install|Configure|Acquire|Shopaholic|Theme|FailedEvents|Troubleshoot)` regex still matches ≥ 7 sections"
---

<objective>
Close Gap 1 (UAT test 7, SC1/DOCS-01): a buyer following ONLY the README dead-ends at install step 1 on a genuinely fresh OctoberCMS 4.x install. Two undocumented prerequisites block them — the require needs `-W` (fresh October lock pins composer/installers ~1.0 that `lovata/toolbox-plugin` must move), and `lovata/*` + `october/system` are unresolvable until `php artisan project:set <license>` registers the October gateway repo. Both are proven by the 2026-07-03 clean-install run (UAT test 8). Everything downstream is verified working; this is a pure documentation-accuracy fix.

Purpose: Make the README install path executable end-to-end on a fresh install so the timed dry-run (launch acceptance gate) can actually start.
Output: Corrected README Install section + one ordered quick-start box to first Test Event + extended ReadmeStructureTest locking both fixes.

Scope note (do NOT expand): This plan is documentation-only. Do NOT touch composer.json, Plugin.php, or any runtime code — the install mechanics are correct; only the README under-documents the fresh-install prerequisites. Keep the strip of v1.x narrative intact (README must still contain zero `v1.1.1` / `legacy/v1` strings — the existing gate enforces this).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-documentation-marketplace-launch/05-UAT.md

# The file being fixed and the gate that guards it
@README.md
@tests/Feature/Docs/ReadmeStructureTest.php
</context>

<tasks>

<task type="auto">
  <name>Task 1: Correct the README Install section for fresh-install reality</name>
  <files>README.md</files>
  <action>Rewrite the Install section so it works on a truly fresh `composer create-project october/october` app. Keep the existing VCS `repositories` JSON block (the ReadmeStructure gate asserts the `"type": "vcs"` pattern — do not remove it). BEFORE the require command, add a prerequisite step instructing the operator to run `php artisan project:set <license>` (substituting their own October project license key) with a one-line why: it registers the authenticated gateway.octobercms.com repository so `october/system` and the `lovata/*` backbone packages resolve. Change the require command to `composer require logingrupa/oc-metapixel-plugin -W` and add a one-line why for `-W`: a fresh October lockfile pins composer/installers at the ~1.0 line that `lovata/toolbox-plugin ^2.2` must move, so Composer needs the with-all-dependencies flag to update the shared constraint. Keep `php artisan october:up` exactly as-is (the migration step is asserted by the gate). Separately, in the "Acquire Meta credentials" body prose (around line 81), change every user-facing reference from "Meta Business Manager" to "Meta Events Manager" to match the live Meta UI and the lang/en `pixel_id_comment` wording (D1) — do NOT rename the `## Acquire Meta credentials` H2 heading itself.</action>
  <verify>../../../vendor/bin/pest --configuration phpunit.xml --filter=ReadmeStructure passes (all existing assertions stay green); grep confirms `php artisan project:set` and `oc-metapixel-plugin -W` both present in README.md and zero occurrences of `Meta Business Manager`</verify>
  <done>README Install section documents the gateway prerequisite + `-W` flag with one-line justifications; no "Meta Business Manager" wording remains; existing ReadmeStructure assertions unbroken.</done>
</task>

<task type="auto">
  <name>Task 2: Add the ordered quick-start box and lock both fixes in the gate</name>
  <files>README.md, tests/Feature/Docs/ReadmeStructureTest.php</files>
  <action>Add a single ordered "Quick start — first event in 10 minutes" box to the README (place it directly after the Install section, as an H3 under Install or a short standalone block — do NOT add a new H2 that could shadow the seven asserted section names). The box lists, in order, the shortest path from zero to a verified Meta Test Events hit: (1) add the VCS repository entry; (2) `php artisan project:set <license>`; (3) `composer require logingrupa/oc-metapixel-plugin -W`; (4) `php artisan october:up`; (5) enter the four required Settings fields (Pixel ID, CAPI Access Token, Test Events Code, Default currency code) under Settings → Marketing → Meta Pixel + CAPI; (6) mount the head Pixel by adding the pixelHead component to the layout; (7) load any front-end page and confirm the event appears in Meta Test Events. Reference `php artisan october:up` (not `october:migrate`) so the wording stays consistent with the rest of the README and the gate. Then extend ReadmeStructureTest with a new test method (e.g. `test_readme_install_documents_fresh_install_prerequisites`) asserting the README contains the substrings `php artisan project:set` and `oc-metapixel-plugin -W`, and a second method (e.g. `test_readme_ships_ordered_quick_start`) asserting the quick-start ordered steps are present (assert on distinctive, stable substrings such as `Quick start`, `project:set`, and `Test Events`). Do NOT assert on any string the existing no-v1x gate forbids.</action>
  <verify>../../../vendor/bin/pest --configuration phpunit.xml --filter=ReadmeStructure passes including the two new assertions</verify>
  <done>README carries one ordered quick-start reaching first Test Events; ReadmeStructureTest gains install-fidelity + quick-start assertions and the whole ReadmeStructure filter is GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| README → operator shell | Operator copy-pastes documented commands; a wrong command wastes trust but runs on the operator's own machine |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-19-01 | Information disclosure | README license example | low | mitigate | Document `project:set <license>` as a placeholder token; never embed a real license key in README |
| T-05-19-02 | Tampering | Doc gate drift | low | mitigate | ReadmeStructureTest install-fidelity assertions fail the build if the install prose regresses |
</threat_model>

<verification>
- `../../../vendor/bin/pest --configuration phpunit.xml --filter=ReadmeStructure` GREEN (all prior + 2 new methods).
- No runtime code touched — `composer qa` chain behaviour is unchanged; only README.md + ReadmeStructureTest.php in the diff.
- Manual carry-forward (not automatable here — no fresh October sandbox in this environment): the stopwatched < 10-minute clean-room dry-run remains an operator/Launch-Milestone acceptance step. This plan removes the documented dead-end that blocked it.
</verification>

<success_criteria>
README install path is executable on a fresh OctoberCMS 4.x app (gateway prerequisite + `-W` documented), the Business→Events Manager wording is corrected, one ordered quick-start reaches first Test Events, and the ReadmeStructure gate locks all of it GREEN.
</success_criteria>

<output>
Create `.planning/phases/05-documentation-marketplace-launch/05-19-SUMMARY.md` when done.
</output>
