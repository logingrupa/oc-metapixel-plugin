---
phase: 5
plan: 09
plan_id: 05-09
type: execute
wave: 7
depends_on: [05-08, 05-00]
files_modified:
  - README.md
autonomous: true
threat_refs: []
requirements: [DOCS-01, DOCS-02]

must_haves:
  truths:
    - "README.md ships at plugin root (plugins/logingrupa/metapixel/README.md)"
    - "Contains ≥7 named H2 sections per ReadmeStructureTest grep: Install, Configure, Acquire Meta (credentials), Shopaholic (walkthrough), Theme (walkthrough), FailedEvents (UI), Troubleshoot"
    - "Install section shows BOTH `composer require logingrupa/oc-metapixel-plugin` AND `php artisan october:up` (Pitfall 5 — migration step explicit) AND the VCS repositories JSON block (D-25). The VCS repositories block MUST appear BEFORE the `composer require` command in the rendered README so buyers configure their composer.json correctly before invoking install (W-13)."
    - "Settings walkthrough enumerates every lang/en/lang.php field.* label verbatim (DOCS-02 walkthrough fidelity — Wave 0 ReadmeStructureTest enforces)"
    - "Meta credential acquisition documented as numbered plain-text steps — NO screenshots of Meta Business Manager (D-12)"
    - "Troubleshooting section is a markdown table mapping symptom → Log::* signature → fix (D-13) with at least 5 rows; each Log::* signature is a real string found by `grep -rn 'Log::' plugins/logingrupa/metapixel/classes/`"
    - "Twin walkthroughs: Shopaholic Purchase path + Theme Twig API path — DOCS-02 mandate; matches MKT-05 CI matrix Run A + Run B audiences"
    - "Walkthrough copies the validated step sequence verbatim from 05-SMOKE-LOG.md (D-08)"
    - "References plugin-relative `docs/screenshots/0[1-5]-*.png` via markdown image-link syntax `![alt](docs/screenshots/0X-name.png)` — the Wave 0 ReadmeStructureTest enforces presence of at least one such image link (W-11)"
    - "Contains zero `v1.1.1` or `legacy/v1` references (D-22 + D-23)"
    - "Wave 0 test `tests/Feature/Docs/ReadmeStructureTest.php` flips RED → GREEN after this plan"
  artifacts:
    - path: "README.md"
      provides: "Single-page marketplace README per DOCS-01 + DOCS-02"
      contains: "## Install, ## Configure, ## Acquire Meta credentials, ## Shopaholic walkthrough, ## Theme walkthrough, ## FailedEvents UI, ## Troubleshoot, ## Multi-site routing, ## CHANGELOG, ## License"
      min_lines: 200
  key_links:
    - from: "README.md"
      to: "tests/Feature/Docs/ReadmeStructureTest.php"
      via: "All Wave 0 assertions on this file pass — including the markdown-image-link assertion (W-11) and the VCS-before-require ordering assertion (W-13)"
      pattern: "(test target)"
    - from: "README.md"
      to: "plugins/logingrupa/metapixel/docs/screenshots/0[1-5]-*.png"
      via: "Plugin-relative markdown image references — `docs/screenshots/0X-*.png` resolves relative to the README at plugin root"
      pattern: "!\\[.*\\]\\(docs/screenshots/0[1-5]-"
    - from: "README.md Troubleshoot section"
      to: "plugins/logingrupa/metapixel/classes/**/* Log::warning/Log::critical sites"
      via: "Each table row's signature column is a real Log::* string"
      pattern: "Log::(warning|critical|error)"
---

<objective>
Author the single-page README that gets a buyer from `composer require` to a verified CAPI event in under 10 minutes (DOCS-01 launch acceptance gate). Section order per RESEARCH § Pattern 2 + PATTERNS analogs from sibling Lovata plugins. Walkthrough step sequence is copied verbatim from `05-SMOKE-LOG.md`. Flips Wave 0 `ReadmeStructureTest` from RED to GREEN.

Purpose: DOCS-01 + DOCS-02 deliverable. Marketplace renders the README directly. No multi-page docs site (D-10 lock).

**Install-section ordering (W-13 fix):** The VCS `repositories` JSON block MUST appear BEFORE the `composer require` command in the rendered README. Composer reads `repositories` from the project's `composer.json` BEFORE resolving `require` entries, so a buyer following the README sequentially must configure their composer.json's `repositories` first, THEN run `composer require`. Wave 0 ReadmeStructureTest asserts the line offset of `"type":"vcs"` precedes the line offset of `composer require logingrupa/oc-metapixel-plugin`.

**Image-link assertion (W-11 fix):** The README MUST include at least one markdown-style image link to a screenshot, in the form `![alt text](docs/screenshots/0X-name.png)` where `X` is one of 1..5. The Wave 0 `tests/Feature/Docs/ReadmeStructureTest.php` adds an explicit assertion `assertMatchesRegularExpression('#!\[.*?\]\(docs/screenshots/0[1-5]-#', $sReadme)` enforcing this. Path `docs/screenshots/...` is plugin-relative (the README lives at plugin root; the screenshots in plan 05-08 are saved to `plugins/logingrupa/metapixel/docs/screenshots/`).

Output: `README.md` at plugin root. Wave 0 test green. No other files modified.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-documentation-marketplace-launch/05-CONTEXT.md
@.planning/phases/05-documentation-marketplace-launch/05-RESEARCH.md
@.planning/phases/05-documentation-marketplace-launch/05-PATTERNS.md
@.planning/phases/05-documentation-marketplace-launch/05-SMOKE-LOG.md
@plugins/logingrupa/metapixel/lang/en/lang.php
@plugins/logingrupa/metapixel/plugin.yaml
@plugins/logingrupa/metapixel/composer.json
@plugins/lovata/subscriptionsshopaholic/README.md
@plugins/lovata/paypalshopaholic/README.md
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| README → buyer | Static markdown; instructs buyer on Meta credential acquisition + .env handling |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-09-01 | Information Disclosure | buyer follows README and commits .env with secrets | mitigate | README explicitly tells buyer to NOT commit .env and to use OctoberCMS Backend Settings UI (not env vars) for pixel_id/capi_access_token |
| T-05-09-02 | Tampering | typosquat install link | mitigate | README install block pins exact package name `logingrupa/oc-metapixel-plugin` and VCS URL `https://github.com/logingrupa/oc-metapixel-plugin` |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Enumerate Log::* signatures + lang/en field labels (inputs to Troubleshoot + Configure sections)</name>
  <read_first>
    - plugins/logingrupa/metapixel/lang/en/lang.php (every `field.*_label` and `field.*_commentAbove` value — these enumerate the Settings UI field labels)
    - plugins/logingrupa/metapixel/lang/en/lang.php (`plugin.name`, `plugin.description`, `menu.failed_events`, `failed_events.column.*`, `failed_events.action.*` — README references each by human-readable string, not lang key)
    - .planning/phases/05-documentation-marketplace-launch/05-SMOKE-LOG.md (validated step sequence; copy verbatim into walkthroughs)
  </read_first>
  <action>
    Run `grep -rn 'Log::warning\|Log::critical\|Log::error' plugins/logingrupa/metapixel/classes/` and capture the message string from each call site. Group by likely-buyer-symptom. For each unique signature decide whether it warrants a troubleshooting table row. Target: ≥5 buyer-actionable rows.

    Build a list of Settings field labels from `lang/en/lang.php`. Note the human-readable English strings (not the lang keys).

    No file modified in this task — input gathering only.
  </action>
  <verify>
    <automated>cd /home/forge/nailscosmetics.lv/plugins/logingrupa/metapixel && grep -rn 'Log::\(warning\|critical\|error\)' classes/ 2>/dev/null | wc -l | awk '$1 >= 5 {print "ok-"$1; exit 0} {print "fail: only "$1" Log sites"; exit 1}'</automated>
  </verify>
  <done>
    Operator has the list of buyer-actionable Log::* signatures + Settings field labels staged for Task 2. No file modified.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write README.md per DOCS-01 + DOCS-02 specification (VCS-block-before-require + image-links)</name>
  <read_first>
    - .planning/phases/05-documentation-marketplace-launch/05-PATTERNS.md lines 58-141 (README pattern + heading skeleton + install block + troubleshoot table template)
    - .planning/phases/05-documentation-marketplace-launch/05-RESEARCH.md lines 312-336 (Pattern 2 — README single-page convention, ordered sections) + lines 544-581 (Example 1 README skeleton)
    - plugins/lovata/subscriptionsshopaholic/README.md lines 1-30 (heading + install + configure skeleton — analog)
    - tests/Feature/Docs/ReadmeStructureTest.php (Wave 0 assertions — every assertion must pass after this plan, including the new image-link and VCS-ordering assertions per W-11 + W-13)
  </read_first>
  <action>
    Create `plugins/logingrupa/metapixel/README.md`. Structure (each `##` becomes a section grepped by the Wave 0 test):

    1. Top heading `# Meta Pixel + Conversions API for OctoberCMS` + one-line description.

    2. `## Install` — Order matters (W-13):
       (i) First, an introductory line: "Add this VCS repository to your project's `composer.json` so Composer can locate the package:" followed by a fenced json block showing the `repositories` array with URL `https://github.com/logingrupa/oc-metapixel-plugin` (D-25).
       (ii) Then a fenced bash block with TWO commands: `composer require logingrupa/oc-metapixel-plugin` and `php artisan october:up`.
       (iii) Below the install block, an explicit note: "If Settings → Marketing → Meta Pixel + CAPI is not visible after install, run `php artisan october:up` to apply migrations." (Pitfall 5 mitigation).

       The Wave 0 ReadmeStructureTest will fail if the line containing `"type":"vcs"` appears AFTER the line containing `composer require logingrupa/oc-metapixel-plugin`. The VCS block must be the FIRST install-related fenced block.

    3. `## Configure` — numbered list walking through Backend → Settings → Marketing → Meta Pixel + CAPI. Each Settings field listed by its English label (from lang/en/lang.php field.*_label values). Include a markdown image-link `![Settings](docs/screenshots/01-settings.png)` (W-11 — this is the load-bearing image-link assertion gate; path is plugin-relative). Add a "Per-site setup" subsection noting the top-bar site picker scopes Settings reads/writes (Phase 4 MULT-* lock).

    4. `## Acquire Meta credentials` — numbered plain-text steps (D-12 — NO screenshots of Meta UI):
       1. Visit Meta Business Manager → Data sources → Pixel → Settings → copy Pixel ID (digits-only)
       2. Same screen → Generate Access Token → copy
       3. (Optional) Same screen → Test Events → copy Test Event Code (routes events to Test Events live view for verification)
       End with note: never commit access_token to git; OctoberCMS Backend Settings UI stores it server-side.

    5. `## Shopaholic walkthrough` (DOCS-02 twin walkthrough — Run A audience) — copy the validated Purchase step sequence verbatim from `05-SMOKE-LOG.md`. Confirm ShopaholicOrderAdapter registers automatically when Lovata.OrdersShopaholic is enabled. Document EventPixel placement on `order-complete.htm` for browser-side dedup.

    6. `## Theme walkthrough` (DOCS-02 twin walkthrough — Run B audience) — Lovata-free OctoberCMS install. Show PixelHead in `<head>` of layout (`{% component 'pixelHead' %}`). Show the Theme Twig API call `{% do this.metapixel.pushEvent({name: 'ViewContent', action_key: 'product-view:' ~ product.id, content_ids: ['SKU-' ~ product.id], value: product.price, currency: 'EUR'}) %}`. Include image-link `![Twig API](docs/screenshots/05-twig-api.png)`.

    7. `## FailedEvents UI` — Backend → Settings → Marketing → Failed events. Document Replay button + CheckDedup button. Include image-links: `![FailedEvents](docs/screenshots/02-failed-events-list.png)`, `![Replay flow](docs/screenshots/03-replay-flow.png)`, `![CheckDedup](docs/screenshots/04-check-dedup.png)`.

    8. `## Troubleshoot` — markdown table with columns `Symptom | Log::* signature (grep storage/logs/laravel.log) | Fix`. ≥5 rows, each with a real Log::* signature from Task 1's enumeration.

    9. `## Multi-site routing` — operator picks site from backend top-bar site picker; per-site `pixel_id` + `capi_access_token` stored as separate `system_settings` rows; `$propagatable = []` lock prevents token leak across sites.

    10. `## CHANGELOG` — link to `[CHANGELOG.md](CHANGELOG.md)`.

    11. `## License` — refer to LICENSE file or composer.json license field.

    Hard constraints:
    - ZERO occurrences of `v1.1.1`, `legacy/v1`, or `Phase [0-9]` strings (Wave 0 ReadmeStructureTest enforces)
    - Every English field label from `lang/en/lang.php` `field.*_label` MUST appear in the Configure section verbatim
    - VCS repositories pattern present AND positioned BEFORE the `composer require` line (W-13 — Wave 0 enforces)
    - At least one markdown image-link matching `!\[.*?\]\(docs/screenshots/0[1-5]-` present (W-11 — Wave 0 enforces); per the section breakdown above, the README will have ≥5 such image-links (one per screenshot)
    - `php artisan october:up` present
    - ≥7 named H2 sections
    - Use marketplace-friendly buyer tone (no GSD/internal-process language)

    Commit message draft: `docs(05-09): add README.md single-page marketplace walkthrough (DOCS-01 + DOCS-02)`.
  </action>
  <verify>
    <automated>cd /home/forge/nailscosmetics.lv/plugins/logingrupa/metapixel && vendor/bin/pest --filter='ReadmeStructureTest' 2>&1 | tail -20</automated>
  </verify>
  <done>
    `README.md` exists at plugin root. `vendor/bin/pest --filter='ReadmeStructureTest'` reports ALL tests GREEN. `grep -c '^## ' README.md` returns ≥7. `grep -E 'v1\.1\.1|legacy/v1|Phase [0-9]' README.md` returns no hits. All 5 screenshots referenced via `![...](docs/screenshots/0[1-5]-*.png)` (image-link gate). The line number of `"type":"vcs"` precedes the line number of `composer require logingrupa/oc-metapixel-plugin` (VCS-ordering gate).
  </done>
</task>

</tasks>

<verification>
- README.md exists, ≥7 H2 sections, ≥5 Troubleshoot table rows, all 5 screenshots referenced via markdown image-link syntax
- VCS `"type":"vcs"` line precedes `composer require` line (W-13)
- ≥1 markdown image-link matching `!\[.*?\]\(docs/screenshots/0[1-5]-` (W-11)
- `tests/Feature/Docs/ReadmeStructureTest.php` GREEN
- `grep -E 'v1\.|legacy/v1|Phase [0-9]' README.md` returns zero hits
</verification>

<success_criteria>
- Wave 0 ReadmeStructureTest GREEN (all new assertions including W-11 + W-13)
- README readable as a marketplace landing surface
- Walkthrough step sequence matches 05-SMOKE-LOG.md verbatim
</success_criteria>

<output>
Create `.planning/phases/05-documentation-marketplace-launch/05-09-SUMMARY.md` when done — note total README line count, H2 section count, troubleshoot table row count, image-link count, VCS-block-before-require confirmation, Wave 0 ReadmeStructureTest status (GREEN), commit SHA.
</output>
</content>
</invoke>