---
phase: 5
plan: 04
plan_id: 05-04
type: execute
wave: 4
depends_on: [05-03]
files_modified:
  - themes/logingrupa-naisstore/layouts/main.htm
  - themes/logingrupa-naisstore/layouts/content.htm
  - themes/logingrupa-naisstore/layouts/light.htm
  - themes/logingrupa-naisstore/layouts/catalog_default.htm
  - .planning/phases/05-documentation-marketplace-launch/05-04-UAT-GATE-2.md
autonomous: false
threat_refs: []
requirements: [DOCS-01]

must_haves:
  truths:
    - "Each of 4 theme layouts (main, content, light, catalog_default) declares `[pixelHead]` in INI section and renders it via `{% component 'pixelHead' %}` inside <head>"
    - "PixelHead component is the registered alias of Logingrupa\\Metapixel\\Components\\PixelHead (Plugin.php line 112 registerComponents)"
    - "Operator verifies exactly 1 PageView event per page-load across the same 5 page URLs verified in Gate 1"
    - "Three-source convergence on PageView: Pixel Helper sees 1 PageView; Test Events live view shows 'Browser' AND 'Server' sources for same event_id with 'Deduplicated' label; EventLog has 1 channel=capi row per page-load"
    - "PageView event_id is identical across all three sources (server-direction contract — Meta dedupes on event_id match ±10s)"
    - "Without operator PASS on UAT Gate 2, plan 05-06 (EventPixel wire) MUST NOT execute"
    - "Cutover gate is operator-confirmed per D-03 — autonomous: false, resume signal required."
    - "UAT verification combines D-05 three sources: Meta Pixel Helper, Meta Test Events live view, `logingrupa_metapixel_event_log` DB tail."
  artifacts:
    - path: "themes/logingrupa-naisstore/layouts/main.htm"
      provides: "head-tag base pixel via `{% component 'pixelHead' %}`"
      contains: "[pixelHead]"
    - path: "themes/logingrupa-naisstore/layouts/content.htm"
      provides: "same"
      contains: "[pixelHead]"
    - path: "themes/logingrupa-naisstore/layouts/light.htm"
      provides: "same"
      contains: "[pixelHead]"
    - path: "themes/logingrupa-naisstore/layouts/catalog_default.htm"
      provides: "same"
      contains: "[pixelHead]"
    - path: ".planning/phases/05-documentation-marketplace-launch/05-04-UAT-GATE-2.md"
      provides: "Operator-signed PageView-only UAT log with three-source verdict per page + event_id sample"
  key_links:
    - from: "themes/logingrupa-naisstore/layouts/*.htm"
      to: "plugins/logingrupa/metapixel/components/PixelHead.php"
      via: "{% component 'pixelHead' %}"
      pattern: "component\\s+'pixelHead'"
---

<objective>
Two-task plan: Task 1 wires the `PixelHead` plugin component into the 4 theme layouts inside `<head>`. Task 2 is the operator-confirmed UAT Gate 2 — after Task 1 ships to `your-staging-host.example`, operator verifies via three independent sources that exactly 1 PageView fires per page-load AND that browser-side fbq + server-side CAPI dispatch reach Meta with the same event_id (dedup contract anchor). Without Gate 2 PASS, plan 05-06 (EventPixel per-event wire) does not execute.

Purpose: Restore base PageView coverage that the legacy `partials/facebook_pixel.htm` provided, using the plugin's new component (D-04 lock). PixelHead is registered at `Plugin.php` line 112. Three-source convergence enforces dedup contract.

Output: 4 layout edits + 1 UAT log markdown. Single auto commit for layouts; UAT log committed after operator signal.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-documentation-marketplace-launch/05-CONTEXT.md
@.planning/phases/05-documentation-marketplace-launch/05-RESEARCH.md
@.planning/phases/05-documentation-marketplace-launch/05-PATTERNS.md
@.planning/phases/05-documentation-marketplace-launch/05-03-UAT-GATE-1.md
@plugins/logingrupa/metapixel/Plugin.php
@plugins/logingrupa/metapixel/components/PixelHead.php
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| layout → plugin component | Twig include of pixelHead — operator-trusted plugin code emits the pixel |
| operator → UAT log | Operator-authored markdown; no executable surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04-01 | Information Disclosure | PixelHead-rendered script | accept | PixelHead reads pixel_id from per-site Settings (Phase 4 MULT-02); empty pixel_id → PluginGuard disabled, no render (cascade-safe) |
| T-05-04-02 | Cross-Site Scripting | inline script in head | accept | pixel_id passes through Phase 4 Settings beforeSave validation; PixelHead component output escaped per Phase 3 THEM-05 hardening |
| T-05-04-03 | Spoofing | UAT PASS without three-source verification | mitigate | Three independent sources required; each has its own measurable threshold + event_id sample |
| T-05-04-04 | Tampering | CAPI server-side fails silently while Pixel Helper sees event | mitigate | EventLog DB tail check confirms server dispatch landed; if Pixel Helper count > EventLog count → FAIL + open /gsd-debug per D-09 |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add [pixelHead] INI + component render to 4 layouts</name>
  <read_first>
    - themes/logingrupa-naisstore/layouts/main.htm (full file — identify INI section + head block — placement target was the same location where `partial 'facebook_pixel'` used to live near line 149)
    - themes/logingrupa-naisstore/layouts/content.htm (same — old include was at ~line 104)
    - themes/logingrupa-naisstore/layouts/light.htm (same — old include was at ~line 76)
    - themes/logingrupa-naisstore/layouts/catalog_default.htm (same — old include was at ~line 100)
    - plugins/logingrupa/metapixel/Plugin.php (registerComponents method — verify alias is exactly `pixelHead` mapped to `Logingrupa\\Metapixel\\Components\\PixelHead`)
    - plugins/logingrupa/metapixel/components/PixelHead.php (verify component class + componentDetails + onRun signatures + accepts no required properties)
    - .planning/phases/05-documentation-marketplace-launch/05-PATTERNS.md lines 588-597 (Pass 2 PixelHead insertion table)
  </read_first>
  <action>
    For each of the 4 layout files (`main.htm`, `content.htm`, `light.htm`, `catalog_default.htm`) apply two edits.

    Edit A — INI section (top-of-file, between the two `==` separators): add `[pixelHead]` component declaration block. No properties.

    Edit B — Twig body: place `{% component 'pixelHead' %}` (single-quoted alias) inside the head element AFTER existing meta charset / title / link tags and BEFORE any analytics or other tracking scripts. Natural location is the line that previously held the deleted `{% partial 'facebook_pixel' obUser=obUser %}` line. If the strip removed that line with no replacement marker, insert immediately before the closing head tag.

    Do NOT add the component to any other layout. Do NOT touch pages. Do NOT add properties to `[pixelHead]`.

    Commit message draft: `feat(05-04): wire PixelHead component in 4 theme layouts (head-tag base pixel)`.
  </action>
  <verify>
    <automated>cd /home/forge/nailscosmetics.lv && bash -c 'for f in main content light catalog_default; do grep -q "^\[pixelHead\]" themes/logingrupa-naisstore/layouts/$f.htm && grep -q "pixelHead" themes/logingrupa-naisstore/layouts/$f.htm || { echo fail-$f; exit 1; }; done; echo ok'</automated>
  </verify>
  <done>
    All 4 layouts contain `[pixelHead]` INI block AND a `pixelHead` component render call. Twig syntax well-formed; theme still renders without HTTP 500 (deeper verification at UAT Gate 2 in Task 2). No other layouts/pages touched.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: UAT Gate 2 — Deploy + verify PageView-only + author UAT-GATE-2 log</name>
  <read_first>
    - Task 1 commit SHA from this plan
    - .planning/phases/05-documentation-marketplace-launch/05-PATTERNS.md lines 594-598 (UAT Gate 2 pass criterion template)
    - .planning/phases/05-documentation-marketplace-launch/05-RESEARCH.md Pitfall 4 (three-source convergence enforcement)
    - .planning/phases/05-documentation-marketplace-launch/05-CONTEXT.md D-03 + D-05 (locked UAT decisions)
  </read_first>
  <what-built>
    Task 1 added `[pixelHead]` INI + `{% component 'pixelHead' %}` render to 4 theme layouts. PixelHead now emits the head-tag base pixel on every page-load that uses one of these layouts. Server-side CAPI PageView dispatched in parallel; both share an event_id for Meta dedup.
  </what-built>
  <how-to-verify>
    Operator steps:

    Step A — Deploy Task 1 commit to `your-staging-host.example` (Forge UI → Deploy Now, or git push to deploy branch). Confirm release timestamp post-PixelHead commit.

    Step B — Verify on the same 5 URLs used in Gate 1: `/`, `/catalog`, `/product/<slug>`, `/checkout`, `/order-complete`. For each URL record three sources independently:
      - Pixel Helper count: 1 PageView event per page-load (PASS condition exactly 1)
      - Meta Test Events live view: 1 PageView event with both "Browser" and "Server" source labels, "Deduplicated" badge visible (PASS condition both sources present + Deduplicated)
      - EventLog count: `SELECT count(*) FROM logingrupa_metapixel_event_log WHERE event_name='PageView' AND channel='capi' AND created_at >= NOW() - INTERVAL 1 MINUTE` returns 1 (PASS condition 1)
      - Sample the event_id for at least one page: `SELECT event_id FROM logingrupa_metapixel_event_log WHERE event_name='PageView' ORDER BY id DESC LIMIT 1` — record the UUID; verify SAME UUID appears in Pixel Helper popup details (Event ID field) and in the Test Events row details

    Step C — Author `.planning/phases/05-documentation-marketplace-launch/05-04-UAT-GATE-2.md`:

    ```
    # UAT Gate 2 — PageView-only after PixelHead wire

    **Date:** YYYY-MM-DD HH:MM
    **Env:** https://your-staging-host.example
    **Deploy SHA:** <commit-sha-from-task-1>
    **Operator:** <name>

    ## Three-source verdict per page

    | Page | Pixel Helper | Test Events (sources) | EventLog (channel=capi) | event_id sample | Verdict |
    |------|--------------|-----------------------|-------------------------|------------------|---------|
    | / | 1 | Browser+Server Deduplicated | 1 | <uuid> | PASS |
    | /catalog | … | … | … | … | … |
    | /product/<slug> | … | … | … | … | … |
    | /checkout | … | … | … | … | … |
    | /order-complete | … | … | … | … | … |

    ## event_id round-trip proof (sample one page)

    Pixel Helper Event ID: <uuid>
    Test Events row event_id: <uuid>
    EventLog row event_id: <uuid>
    Three values identical: YES / NO

    ## Anomalies (if any)

    (free-form notes — describe any source under-counting; route to /gsd-debug per D-09)

    ## Overall verdict

    GATE 2 PASS  (or  GATE 2 FAIL)
    ```

    Step D — Do NOT type resume signal if any cell fails or event_id round-trip is NO. Route to `/gsd-debug` per D-09 and re-run.
  </how-to-verify>
  <resume-signal>
    Type `GATE 2 PASS` exactly to advance to plan 05-06. If FAIL describe failing source + suspected cause + the `/gsd-debug` session opened.
  </resume-signal>
</task>

</tasks>

<verification>
- 4 layout files edited, each contains `[pixelHead]` INI block AND `pixelHead` component render call
- `05-04-UAT-GATE-2.md` exists, signed by operator
- All 5 rows × 4 measurement columns filled
- event_id round-trip proof shows three matching UUIDs
- Resume signal exactly `GATE 2 PASS`
</verification>

<success_criteria>
- Three-source convergence on `1` for all 5 pages
- event_id matches across browser, Test Events, EventLog (server-direction contract holds)
- Single feature commit for Task 1; UAT log committed after operator signal
</success_criteria>

<output>
Create `.planning/phases/05-documentation-marketplace-launch/05-04-SUMMARY.md` when done — note line numbers where pixelHead was inserted in each layout, Task 1 commit SHA, UAT log path, operator name + date, PASS status, and "READY FOR EVENT PIXEL WIRE (plan 05-06)" handoff.
</output>
</content>
