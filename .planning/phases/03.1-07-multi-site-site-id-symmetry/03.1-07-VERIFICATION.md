---
status: passed
verified_at: 2026-05-14
phase: 03.1-07
requirements_verified: [REFAC-12, REFAC-13, REFAC-14]
must_haves_passed: 24
must_haves_failed: 0
gaps: 0
---

# Phase 3.1-07 Verification — Cross-context site_id symmetry

**Phase goal (BRIEF.md):** Close 2026-05-14 cross-context `site_id` divergence bug
(orders 29802 + 29803 on `new.nailscosmetics.lv`) at contract level. Writer (admin
`/back` queue) and reader (frontend `/lv/checkout`) MUST resolve the SAME `site_id`
for the same Order via `lovata_orders_shopaholic_orders.site_id` (Lovata v1.33).

**Verifier stance:** adversarial — every must_have falsified against codebase grep /
file-read / test-run / git-log evidence; SUMMARY.md claims trusted only when
corroborated by the artefact itself.

**Verdict:** PASSED — 24/24 must_haves verified; 0 new test failures; 0 new
phpstan errors; RED-before-GREEN TDD discipline preserved; caveman comment
compression honored in new/edited docblocks across all 6 touched production
files + BACKFILL.sql.

---

## Audit Table

### REFAC-12 — `SiteResolver::forOrder`

| # | Must-have | Evidence | Status |
|---|-----------|----------|--------|
| 1 | `SiteResolver::forOrder(Order $obOrder): ?int` exists with `is_numeric` narrow, no Throwable catch | `classes/helper/SiteResolver.php:60-65` — signature exact; body 2 lines (`$mId = $obOrder->getAttribute('site_id'); return is_numeric($mId) ? (int) $mId : null;`); zero `try` / `catch` | ✓ VERIFIED |
| 2 | `getActiveSiteId(): ?int` preserved unchanged | `classes/helper/SiteResolver.php:72-97` — body intact (`class_exists` guard + SDK call + `Throwable` boundary catch + `Log::warning` + return null) | ✓ VERIFIED |
| 3 | `Order` use import present in SiteResolver | `classes/helper/SiteResolver.php:8` — `use Lovata\OrdersShopaholic\Models\Order;` | ✓ VERIFIED |

### REFAC-13 — Writer/reader rewires + EventLogWriter signature DRY

| # | Must-have | Evidence | Status |
|---|-----------|----------|--------|
| 4 | `EventLogWriter::record` signature gains 7th optional `?int $iSiteId = null` | `classes/helper/EventLogWriter.php:70-78` — `public static function record(string $sEventId, string $sEventName, string $sChannel, object $obSubject, ?string $sSecretKey, int $iEventTime, ?int $iSiteId = null,): bool` | ✓ VERIFIED |
| 5 | Zero `SiteResolver::` references inside `EventLogWriter.php` (writer pure I/O) | `grep -E 'SiteResolver::' classes/helper/EventLogWriter.php` → zero matches; also no `use ... SiteResolver` import | ✓ VERIFIED |
| 6 | Caller-supplied `$iSiteId` persisted verbatim on insert | `classes/helper/EventLogWriter.php:106` — `'site_id' => $iSiteId,` inside `insertOrIgnore` array; no transformation | ✓ VERIFIED |
| 7 | `SendCapiEvent::raceFenceWon` passes `SiteResolver::forOrder($this->obSubject)` as 7th arg | `classes/queue/SendCapiEvent.php:221` — `SiteResolver::forOrder($this->obSubject),` as final arg of `EventLogWriter::record(...)` | ✓ VERIFIED |
| 8 | `OrderStatusWatcher::alreadyDispatched(Order $obOrder)` uses `SiteResolver::forOrder($obOrder)` | `classes/event/OrderStatusWatcher.php:272` — `$iSiteId = SiteResolver::forOrder($obOrder);` | ✓ VERIFIED |
| 9 | `PurchasePixel::findEventLogRow(Order, string)` uses `SiteResolver::forOrder($obOrder)` | `components/PurchasePixel.php:315` — `$iSiteId = SiteResolver::forOrder($obOrder);` (read site); `components/PurchasePixel.php:281` also passes `SiteResolver::forOrder($obOrder)` as 7th arg in `EventLogWriter::record(...)` inside `onMarkFired` | ✓ VERIFIED |
| 10 | Grep gate: zero `SiteResolver::getActiveSiteId()` in `classes/` + `components/` outside `helper/SiteResolver.php` | `grep -rE 'SiteResolver::getActiveSiteId\(\)' classes/ components/ \| grep -v helper/SiteResolver.php` → zero matches | ✓ VERIFIED |

### REFAC-14 — Operator artefacts

| # | Must-have | Evidence | Status |
|---|-----------|----------|--------|
| 11 | `BACKFILL.sql` exists at `.planning/phases/03.1-07-multi-site-site-id-symmetry/BACKFILL.sql` | File present, 27 LOC; UPDATE..JOIN..SET..WHERE shape matches BRIEF lines 113-121 | ✓ VERIFIED |
| 12 | BACKFILL.sql carries header docblock preconditions | Lines 1-20 — 4-point preconditions block (run per affected site pre-deploy; idempotent; NULL-only repair; safe pre-deploy because column-fence sole consumer); production incident context + REFAC-14 `@see` reference | ✓ VERIFIED |
| 13 | `STAGING-RUNBOOK.md` Scenario 5 documented alongside Scenarios 1-4 | `.planning/phases/03.1-event-log-refactor/STAGING-RUNBOOK.md:229` — `## Scenario 5 — Cross-context admin flip + customer frontend revisit`; full 8-step playbook (SQL probe, Meta Events Manager expectations, STATE.md append template, failure-mode regression detector) | ✓ VERIFIED |
| 14 | `updates/version.yaml` 1.1.1 block: single-line description, no migration filenames | `updates/version.yaml:16-17` — `1.1.1:` block, single bullet line, NO migration filenames (pure code refactor per plan) | ✓ VERIFIED |

### Tests

| # | Must-have | Evidence | Status |
|---|-----------|----------|--------|
| 15 | `tests/Unit/SiteResolverTest.php` exists with 3 test methods | File present; `test_for_order_returns_int_when_order_has_site_id`, `test_for_order_returns_null_when_order_site_id_null`, `test_for_order_returns_null_when_attribute_non_numeric` — all GREEN under pest run | ✓ VERIFIED |
| 16 | `tests/Feature/MultiSiteCrossContextTest.php` exists with 3 test methods | File present (307 LOC); `test_admin_flip_with_null_active_site_writes_capi_using_order_site_id`, `test_frontend_pixel_reads_capi_row_via_order_site_id_when_active_site_diverges`, `test_cross_context_pair_round_trips_for_single_site_install` — all GREEN | ✓ VERIFIED |
| 17 | `SendCapiEventEventLogTest` gained `test_writer_called_with_resolved_site_id_from_caller` | Lines 227-258 — seeds Order.site_id=7 + `Config::set('system.active_site', null)`; asserts row.site_id === 7 after dispatch — GREEN | ✓ VERIFIED |
| 18 | `OrderStatusWatcherEventLogTest` gained `test_already_dispatched_query_uses_order_site_id` | Lines 418-454 — seeds Order.site_id=7 + EventLog row site_id=7 + Config null; asserts `Queue::assertNotPushed(SendCapiEvent::class)` (alreadyDispatched suppresses) — GREEN | ✓ VERIFIED |
| 19 | `PurchasePixelEventLogGateTest` gained `test_findEventLogRow_uses_order_site_id_not_active_site` | Lines 260-299 — seeds Order.site_id=2 + row site_id=2 + Config active_site=1; asserts arMetaEvent populated + event_id matches — GREEN | ✓ VERIFIED |
| 20 | `MultiSiteEventLogTest` existing methods adjusted to `$obOrder->site_id = $i` pattern | Lines 114-146 (`test_two_sites_bind_same_order_id_records_independently`): writer now passes `1` + `2` explicitly as 7th arg; Lines 197-234 (`test_active_site_scoped_read_excludes_other_sites_rows`): seeds via explicit site_id arg + reader uses `SiteResolver::forOrder($obOrder)` | ✓ VERIFIED |
| 21 | `MultiSiteEventLogTest::test_single_site_install_writes_null_site_id` preserved unchanged | Lines 163-184 — same shape as Phase 3.1 baseline (Config null + record() without explicit site_id → row.site_id IS NULL) | ✓ VERIFIED |
| 22 | `MetapixelTestCase::bootOrdersTable` adds `$obTable->integer('site_id')->nullable()` | `tests/MetapixelTestCase.php:253-255` — `// Phase 3.1-07 REFAC-12: Lovata v1.33 site_id column. Nullable —` then `$obTable->integer('site_id')->nullable();` between `last_name` and `timestamps()` | ✓ VERIFIED |

### Ledger

| # | Must-have | Evidence | Status |
|---|-----------|----------|--------|
| 23 | `.planning/STATE.md` frontmatter `status: phase-3.1-cross-context-verified` | `.planning/STATE.md:5` — `status: phase-3.1-cross-context-verified` (advanced from `phase-3.1-runtime-verified`) | ✓ VERIFIED |
| 24 | `.planning/ROADMAP.md` Phase 3.1 plan list includes `03.1-07-PLAN.md` entry + 7/7 progress | `.planning/ROADMAP.md:102` (entry under Phase 3.1 Plans block) + `.planning/ROADMAP.md:161` (progress table: `3.1. Event-log refactor (INSERTED) \| 7/7 \| Complete`); plus dedicated `Phase 3.1-07: Multi-site site_id symmetry` section (lines 104-121) | ✓ VERIFIED |

---

## Grep Gates (Success Criteria)

```bash
$ grep -rE 'SiteResolver::getActiveSiteId\(\)' classes/ components/ | grep -v helper/SiteResolver.php
# (zero matches) ✓ PASS

$ grep -E 'SiteResolver::' classes/helper/EventLogWriter.php
# (zero matches) ✓ PASS
```

## TDD Discipline — RED before GREEN

| RED commit | Timestamp | GREEN counterpart | Timestamp |
|------------|-----------|-------------------|-----------|
| `736b3e3` test(03.1-07): RED — SiteResolverTest forOrder contract | 2026-05-14 12:44:47 | `2787bd8` fix(03.1-07): SiteResolver::forOrder (REFAC-12) | 2026-05-14 12:46:39 |
| `4d18a24` test(03.1-07): RED — MultiSiteCrossContextTest admin-flip cross-context | 2026-05-14 12:47:44 | `ac162e5` + `9e91044` refactor(03.1-07) | 2026-05-14 12:50:36 / 13:02:36 |
| `cb15b05` test(03.1-07): RED — SendCapiEventEventLogTest writer receives forOrder value | 2026-05-14 12:48:20 | `ac162e5` refactor(03.1-07): EventLogWriter::record accepts ?int site_id | 2026-05-14 12:50:36 |
| `cc6ee63` test(03.1-07): RED — OrderStatusWatcherEventLogTest alreadyDispatched via forOrder | 2026-05-14 12:48:51 | `9e91044` refactor(03.1-07): rewire Watcher + SendCapiEvent + PurchasePixel | 2026-05-14 13:02:36 |
| `97d5df2` test(03.1-07): RED — PurchasePixelEventLogGateTest findEventLogRow via forOrder | 2026-05-14 12:49:21 | `9e91044` (same GREEN refactor) | 2026-05-14 13:02:36 |

5 RED commits ≥ 4 required. Every RED SHA timestamps strictly BEFORE its
matching GREEN SHA. `git log --oneline --grep="^test(03.1-07): RED"` returns 5
entries. RED-before-GREEN ordering preserved across full sequence.

## Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Plugin pest suite runs | `composer test` | 172 passed, 6 failed (all 6 documented baseline) | ✓ PASS (baseline parity) |
| phpstan level 10 | `composer analyse` | 2 errors (extractFirstEvent return type widening + EventLog::subject MorphTo) — both pre-existing baseline | ✓ PASS (baseline parity) |
| Grep gate — getActiveSiteId outside helper | `grep -rE 'SiteResolver::getActiveSiteId\(\)' classes/ components/ \| grep -v helper/SiteResolver.php` | zero matches | ✓ PASS |
| Grep gate — SiteResolver inside writer | `grep -E 'SiteResolver::' classes/helper/EventLogWriter.php` | zero matches | ✓ PASS |
| RED commit count | `git log --oneline --grep="^test(03.1-07): RED" \| wc -l` | 5 | ✓ PASS (≥4) |

## Test Failure Classification

`composer test` total: 172 passed / 6 failed. Each failure cross-referenced against
`03.1-06-SUMMARY.md` deferred-issue list:

| # | Failure | Root cause | Phase 3.1-07 origin? |
|---|---------|-----------|----------------------|
| 1 | `BootsWithoutPixelIdTest::test_settings_with_pixel_id_disables_flag` | Settings::set HR-02 flap | NO — pre-existing |
| 2 | `EnsureFbpFbcCookiesTest::test_does_not_set_fbp_when_toggle_off` | Settings::set HR-02 flap | NO — pre-existing |
| 3 | `EventLogTest::test_event_id_36_chars_validation` | event_id length validation mismatch | NO — pre-existing |
| 4 | `ExceptionHierarchyTest::every_lang_key_resolves_to_a_string` | RainLab.Translate lang file population gap (Phase 5 HARD-04 deferred) | NO — pre-existing |
| 5 | `PurchasePixelEventLogGateTest::test_onmarkfired_second_call_returns_ok_true_no_duplicate` | SQLite UNIQUE NULL-distinct semantic (NOT-NULL site_id required for collision; single-site test path leaves NULL) | NO — pre-existing |
| 6 | `SendCapiEventEventLogTest::test_second_concurrent_dispatch_returns_false_no_http_post` | Same SQLite UNIQUE NULL-distinct semantic | NO — pre-existing |

Independent verification: running `vendor/bin/pest tests/Unit/ExceptionHierarchyTest.php`
against the current working tree confirms `every_lang_key_resolves_to_a_string` was
already failing at the head of this phase (lang keys still resolve to raw `::lang.`
strings because the Lang scaffolding ships empty per SKEL-06 — full population is
Phase 5 HARD-04). All 6 are scope-boundary issues outside Phase 3.1-07 fix shape.

Phase 3.1-07 introduced **0 new regressions** + improved baseline by re-stabilising
`PurchaseEndToEndIntegrationTest` Scenarios 4 + multi-site (Rule 3 auto-fix
documented in SUMMARY.md Deviations section).

## phpstan Error Classification

| # | Error | Phase 3.1-07 origin? |
|---|-------|----------------------|
| 1 | `classes/queue/SendCapiEvent.php:241 extractFirstEvent() return type widening` | NO — pre-existing baseline (03.1-06-SUMMARY.md Deferred Issues row 2) |
| 2 | `models/EventLog.php:144 subject() returns BelongsTo not MorphTo (October Rain quirk)` | NO — pre-existing baseline (03.1-06-SUMMARY.md Deferred Issues row 2) |

0 new phpstan errors introduced.

## Caveman Comment Compression (Sampled Files)

Plan scope: only the 6 production files + BACKFILL.sql touched by Phase 3.1-07
were caveman-compressed; pre-existing PHPDoc paragraphs from prior phases
preserved verbatim (Tiger-Style "no drive-by refactors").

Sample 1 — `classes/helper/SiteResolver.php` (full file rewritten in caveman style):

- Class-level docblock (lines 12-46): every paragraph is fragmentary, technical-term
  exact. Examples: "forOrder(Order): canonical Order-scoped resolver"; "getActiveSiteId
  null cases: 1. SiteManager class absent..."; "UNIQUE on `logingrupa_metapixel_event_log`
  treats NULL as distinct under MySQL — single-site (null) + multi-site (int) coexist
  on same table." Articles dropped where natural; technical terms (SDK, SRP, MySQL,
  Tiger-Style) preserved.
- `forOrder` method docblock (lines 49-58): caveman fragments — "Read Order.site_id
  stamped by Lovata MakeOrder at create. Null on single-site install or pre-v1.33
  Order. Deterministic across request contexts..." PHPDoc functional tags (`@see`)
  preserved verbatim.

Sample 2 — `classes/helper/EventLogWriter.php` (new docblock paragraphs):

- Class-level docblock (lines 13-55): pre-existing REFAC-05 paragraph preserved
  verbatim per plan rule; new Phase 3.1-07 paragraph (lines 40-43) is caveman-
  compressed — "Site scoping: Phase 3.1-07 REFAC-13 DRY — writer is pure I/O.
  Caller passes ?int $iSiteId (resolution policy at call sites). NULL on
  single-site / CLI / queue context — UNIQUE NULL-distinct keeps rows separate
  from multi-site rows on same table."
- `record()` PHPDoc (lines 58-69): new `@param ?int $iSiteId` line caveman:
  "Caller-resolved site_id (Phase 3.1-07 REFAC-13 DRY)." Pre-existing `@param`
  bullets for `$sEventId..$iEventTime` preserved.

Both samples honor the rule. No verbose-prose holdouts flagged in the new/edited
content. Legacy article-word counts in pre-existing docblocks (PurchasePixel.php
79 hits, OrderStatusWatcher.php 36 hits, etc.) reflect prior-phase paragraphs
deliberately preserved per "no drive-by refactors" Tiger-Style rule.

## Requirements Coverage

| Requirement | Source | Description | Status | Evidence |
|-------------|--------|-------------|--------|----------|
| REFAC-12 | ROADMAP.md line 109 / BRIEF.md lines 69-90 | `SiteResolver::forOrder(Order $obOrder): ?int` canonical Order-scoped resolver | ✓ SATISFIED | Must-haves 1-3, 22 |
| REFAC-13 | ROADMAP.md line 109 / BRIEF.md lines 92-108 | Rewire every Order-scoped SiteResolver call site + EventLogWriter::record DRY signature | ✓ SATISFIED | Must-haves 4-10 |
| REFAC-14 | ROADMAP.md line 109 / BRIEF.md lines 110-124 | BACKFILL.sql + STAGING-RUNBOOK Scenario 5 + v1.1.1 version bump | ✓ SATISFIED | Must-haves 11-14 |

ROADMAP.md Phase 3.1-07 Success Criteria (lines 110-118): all 8 SC verified
above (composer qa green = baseline parity confirmed; grep gates pass;
signature change + signature gate; new tests + extensions present and GREEN;
BACKFILL.sql + Scenario 5 + version.yaml; STATE.md status advanced; ROADMAP
table updated). SC-7 (`system_plugin_versions` row = v1.1.1) requires operator
deploy + `php artisan october:up` — flagged below as human-verify.

## Anti-Patterns Scan

| File | Pattern | Severity | Impact |
|------|---------|----------|--------|
| `classes/helper/SiteResolver.php` | None | — | clean |
| `classes/helper/EventLogWriter.php` | `// silent: DB-write failure must NOT cascade — Tiger-Style boundary.` (line 115) | ℹ Info | Documented silent catch with reason — Tiger-Style compliant; pre-existing |
| `classes/queue/SendCapiEvent.php` | `// silent: dead-letter persistence failure — log critical only, do not rethrow.` (line 270) | ℹ Info | Documented silent catch with reason — Tiger-Style compliant; pre-existing |
| `components/PurchasePixel.php` | None new | — | clean |
| `classes/event/OrderStatusWatcher.php` | None new | — | clean |
| `tests/MetapixelTestCase.php` | None new | — | clean |
| `BACKFILL.sql` | None | — | clean |

Zero TODO/FIXME/XXX/TBD markers introduced in any touched file. All catch
blocks logged with reason comments. No empty-return stubs. No debt markers.

## Human Verification Required

The phase goal-backward narrative is fully satisfied at the CI / contract
level. The following items remain operator-dependent and are explicitly part of
the v1.1.1 deploy ritual — they do NOT block the `passed` verdict because they
are intentionally external to the CI surface and documented in the SUMMARY's
"Forward Notes" + STAGING-RUNBOOK Scenario 5.

1. **STAGING-RUNBOOK Scenario 5 live run** — operator runs the 8-step cross-context
   playbook on each affected site (.lv, .lt, .no) after running BACKFILL.sql + deploying
   v1.1.1. Expected: Meta Events Manager Test Events shows ONE Purchase event with
   Browser + Server pair (same `event_id`). Cannot be CI-verified — requires real
   Meta Test Events dashboard + live Pixel HTTP traffic.
2. **`system_plugin_versions` row reflects v1.1.1** — verified post-deploy via
   `php artisan october:up` + `SELECT version FROM system_plugin_versions WHERE
   code = 'Logingrupa.Metapixelshopaholic';`. ROADMAP.md SC-7. Cannot be CI-verified
   pre-deploy.
3. **BACKFILL.sql on production** — operator runs the one-shot UPDATE..JOIN on each
   site BEFORE v1.1.1 deploys; verifies stranded rows (29802 + 29803 on
   `new.nailscosmetics.lv`) inherit `site_id` from `lovata_orders_shopaholic_orders.site_id`.
   Idempotent — safe to re-run; NULL-only repair.

## Gaps Summary

None. All 24 must_haves verified by grep / file-read / test-run / git-log evidence.
RED-before-GREEN TDD discipline preserved; caveman comment compression honored in
new/edited sections; 0 new test failures vs. baseline; 0 new phpstan errors. The
2026-05-14 production-blocking bug (orders 29802 + 29803 on `new.nailscosmetics.lv`
— writer admin context stamped `site_id=NULL`; reader frontend context queried
`where site_id=1`; gate failed; Pixel never rendered) is now CLOSED at contract
level. Operator runs BACKFILL.sql + STAGING-RUNBOOK Scenario 5 to close the live
network plumbing loop and tag v1.1.1.

---

*Verified: 2026-05-14*
*Verifier: Claude (gsd-verifier, goal-backward methodology)*
