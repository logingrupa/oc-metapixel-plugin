---
phase: 04-settings-rework-multisite-trustedhosts-cookie-failedevents-t
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - plugins/logingrupa/metapixel/models/Settings.php
  - plugins/logingrupa/metapixel/updates/AddMultisitePixelIdAndToken.php
  - plugins/logingrupa/metapixel/updates/version.yaml
  - plugins/logingrupa/metapixel/phpstan.neon
  - plugins/logingrupa/metapixel/tests/Unit/Models/SettingsMultisiteTraitTest.php
  - plugins/logingrupa/metapixel/tests/Feature/Settings/SettingsLookupForSiteTest.php
  - plugins/logingrupa/metapixel/tests/Feature/MultisiteEventLogRoutingTest.php
  - plugins/logingrupa/metapixel/tests/Feature/Migrations/AddMultisitePixelIdAndTokenTest.php
  - plugins/logingrupa/metapixel/tests/fixtures/sites.php
autonomous: true
requirements: [MULT-01, MULT-02, MULT-03, MULT-04, MULT-05, MULT-06]
must_haves:
  truths:
    - "MULT-01/D-20: Settings.php declares explicit protected $propagatable = [] descendant lock"
    - "MULT-02/D-02: pixel_id and capi_access_token are NEVER read by Settings::get('pixel_id') outside Settings::lookupForSite (PHPStan disallowed-calls enforces)"
    - "MULT-03/D-01: Settings::lookupForSite(?int) returns per-site row when set; empty per-site pixel_id OR capi_access_token falls back silently to default-row value"
    - "MULT-04: SendCapiEvent::handle continues to call Settings::lookupForSite($iSiteId) (Phase 2 callsite — no change, only re-implementation of body)"
    - "MULT-05/D-04: 8-path matrix (2 sites x 2 adapters x 2 channels) inserts EventLog rows under UNIQUE NULL-distinct semantics on (subject_type, subject_id, event_name, channel, site_id)"
    - "MULT-06/D-03: AddMultisitePixelIdAndToken migration is schema-additive only; idempotent Schema::hasTable guard + no-op body"
  artifacts:
    - path: "plugins/logingrupa/metapixel/models/Settings.php"
      provides: "Per-site credential lookup with default-row fallback; explicit $propagatable = [] lock"
      contains: "public static function lookupForSite"
    - path: "plugins/logingrupa/metapixel/updates/AddMultisitePixelIdAndToken.php"
      provides: "Schema-additive idempotent migration (no-op body for traceability)"
      contains: "class AddMultisitePixelIdAndToken extends Migration"
    - path: "plugins/logingrupa/metapixel/updates/version.yaml"
      provides: "Registers AddMultisitePixelIdAndToken migration"
      contains: "AddMultisitePixelIdAndToken.php"
    - path: "plugins/logingrupa/metapixel/phpstan.neon"
      provides: "Disallowed-calls rule banning direct Settings::get('pixel_id'|'capi_access_token') outside Settings::lookupForSite"
      contains: "lookupForSite"
  key_links:
    - from: "plugins/logingrupa/metapixel/classes/queue/SendCapiEvent.php"
      to: "Settings::lookupForSite"
      via: "Phase 2 callsite preserved"
      pattern: "Settings::lookupForSite\\("
    - from: "plugins/logingrupa/metapixel/models/Settings.php::lookupForSite"
      to: "October\\Rain\\Support\\Facades\\Site::withContext"
      via: "Per-site row read inside closure with Settings::clearInternalCache()"
      pattern: "Site::withContext"
---

<objective>
Implement MULT-01..06 — Multisite-aware credential lookup. Re-implement `Settings::lookupForSite(?int)` body to honor per-site rows via `Site::withContext` with silent default-row fallback (D-01). Add explicit `$propagatable = []` descendant lock (MULT-01). Land a schema-additive no-op migration `AddMultisitePixelIdAndToken` (MULT-06/D-03). Add PHPStan disallowed-calls rule banning direct `Settings::get('pixel_id'|'capi_access_token')` outside `Settings::lookupForSite` (D-02). Ship four Wave 0 test files including the 8-path matrix (D-04/MULT-05).

Purpose: Closes P-10 (Multisite token leak). Unlocks per-site `pixel_id` + `capi_access_token` routing for marketplace operators running multiple OctoberCMS sites from one plugin install.

Output: `Settings.php` with per-site `lookupForSite` body, `AddMultisitePixelIdAndToken.php` migration, updated `version.yaml`, extended `phpstan.neon` disallowed-calls block, 4 Pest test files + 1 fixture file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@plugins/logingrupa/metapixel/.planning/PROJECT.md
@plugins/logingrupa/metapixel/.planning/ROADMAP.md
@plugins/logingrupa/metapixel/.planning/STATE.md
@plugins/logingrupa/metapixel/.planning/phases/04-settings-rework-multisite-trustedhosts-cookie-failedevents-t/04-CONTEXT.md
@plugins/logingrupa/metapixel/.planning/phases/04-settings-rework-multisite-trustedhosts-cookie-failedevents-t/04-RESEARCH.md
@plugins/logingrupa/metapixel/.planning/phases/04-settings-rework-multisite-trustedhosts-cookie-failedevents-t/04-PATTERNS.md
@plugins/logingrupa/metapixel/CLAUDE.md
@/home/forge/nailscosmetics.lv/CLAUDE.md
@plugins/logingrupa/metapixel/models/Settings.php
@plugins/logingrupa/metapixel/updates/AddPayloadToMetapixelEventLogTable.php
@plugins/logingrupa/metapixel/updates/version.yaml
@plugins/logingrupa/metapixel/phpstan.neon
@plugins/logingrupa/metapixel/tests/Feature/Settings/SettingsLookupForSiteTest.php

<interfaces>
<!-- Key contracts the executor needs without exploring the codebase. -->

Settings (descends from Lovata\Toolbox\Models\CommonSettings which uses October\Rain\Database\Traits\Multisite):
```
public static function lookupForSite(?int $iSiteId): array{pixel_id: string, capi_access_token: string}
protected $propagatable = []   // empty whitelist — D-02 lock
public $settingsCode = 'logingrupa_metapixel_settings'
public $settingsFields = 'fields.yaml'
```

October\Rain\Support\Facades\Site (verified at vendor/october/rain/src/Database/Traits/Multisite.php:102/150/344/358):
```
Site::withGlobalContext(callable $fn): mixed
Site::withContext(int $iSiteId, callable $fn): mixed
```

System\Models\SettingModel (modules/system/models/SettingModel.php):
```
public static function clearInternalCache(): void   // line 241-244
```

Migration base (October\Rain\Database\Updates\Migration):
```
public function up()    // no return type — match in-plugin idiom
public function down()
```

Phase 2 callsite (do NOT modify) — classes/queue/SendCapiEvent.php already calls:
```
$arCreds = Settings::lookupForSite($iSiteId);
$obClient->sendForPixel($arCreds['pixel_id'], $arCreds['capi_access_token'], $arPayload);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 0: Wave 0 — author failing test scaffolds + 2-site fixture</name>
  <files>
    plugins/logingrupa/metapixel/tests/Unit/Models/SettingsMultisiteTraitTest.php,
    plugins/logingrupa/metapixel/tests/Feature/Settings/SettingsLookupForSiteTest.php,
    plugins/logingrupa/metapixel/tests/Feature/MultisiteEventLogRoutingTest.php,
    plugins/logingrupa/metapixel/tests/Feature/Migrations/AddMultisitePixelIdAndTokenTest.php,
    plugins/logingrupa/metapixel/tests/fixtures/sites.php
  </files>
  <read_first>
    plugins/logingrupa/metapixel/tests/Feature/Settings/SettingsLookupForSiteTest.php (Phase 2 stub — extend or replace, do not duplicate),
    plugins/logingrupa/metapixel/tests/Feature/Console/PurgeEventLogTest.php (verbatim setUp/tearDown pattern per 04-PATTERNS.md lines 932-961),
    plugins/logingrupa/metapixel/tests/Unit/Helper/PluginGuardTest.php (in-plugin unit setUp pattern),
    plugins/logingrupa/metapixel/tests/MetapixelTestCase.php (in-memory SQLite + system_settings recreation referenced by 04-RESEARCH §"Migration shape" — line 510),
    plugins/logingrupa/metapixel/.planning/phases/04-settings-rework-multisite-trustedhosts-cookie-failedevents-t/04-RESEARCH.md (§"Validation Architecture" + §"Wave 0 Gaps" lines 1818-1834),
    plugins/logingrupa/metapixel/.planning/phases/04-settings-rework-multisite-trustedhosts-cookie-failedevents-t/04-PATTERNS.md (Tests section lines 912-981 — analog table + setUp pattern)
  </read_first>
  <action>
    Author 4 Pest test files extending `Logingrupa\Metapixel\Tests\MetapixelTestCase` (lowercase `tests/` paths per Phase 2 lowercase folder convention; PascalCase class basenames matching FQN).

    Each test class is `final class` and calls `parent::setUp()`, then `$this->app->singleton(AdapterRegistry::class)`, then `Settings::clearInternalCache()` in setUp() (Pitfall 1 anchor — RESEARCH §"Pitfall 1"). All tests MUST currently FAIL (red bar) because the production code is not yet implemented.

    `tests/Unit/Models/SettingsMultisiteTraitTest.php` (MULT-01 + MULT-02):
    - `test_settings_declares_explicit_empty_propagatable_at_descendant_level`: use `Reflection` to assert `Settings` class itself declares `protected $propagatable = []` (NOT inherited only). Asserts `(new ReflectionClass(Settings::class))->hasProperty('propagatable')` returns true and `$obProp->getDeclaringClass()->getName() === Settings::class`.
    - `test_propagatable_is_empty_list`: asserts `(new Settings)->getPropagatable()` returns `[]` (or read via reflection if no public getter — D-02 lock).
    - `test_pixel_id_is_not_in_propagatable`: asserts `! in_array('pixel_id', $propagatable, true)`.
    - `test_capi_access_token_is_not_in_propagatable`: asserts `! in_array('capi_access_token', $propagatable, true)`.

    `tests/Feature/Settings/SettingsLookupForSiteTest.php` (MULT-03 — REPLACE existing Phase 2 stub file content; preserve filename for git blame continuity):
    - Recreate `system_settings` table in setUp (or rely on MetapixelTestCase fixture) + seed 2 site rows via the new `tests/fixtures/sites.php` fixture loader.
    - `test_lookup_for_site_null_returns_default_row`: `Settings::set([...])` (default row) → `Settings::lookupForSite(null)` returns the default values.
    - `test_lookup_for_site_with_id_returns_per_site_row`: seed per-site row inside `Site::withContext(1, fn)`; assert `Settings::lookupForSite(1)` returns per-site pixel_id.
    - `test_lookup_for_site_empty_per_site_pixel_falls_back_to_default`: seed per-site row with empty `pixel_id`; assert `Settings::lookupForSite(1)['pixel_id']` equals default value (D-01).
    - `test_lookup_for_site_null_per_site_token_falls_back_to_default`: same, but `capi_access_token` null → default.
    - `test_lookup_for_site_return_shape_is_array_with_two_keys`: keys exactly `pixel_id`, `capi_access_token`, both strings.

    `tests/Feature/MultisiteEventLogRoutingTest.php` (MULT-05 D-04 8-path matrix):
    - setUp recreates EventLog table + seeds 2 sites via fixture + registers a `FakeAdapter` returning subject_type `'fake.adapter'` and `getSiteId()` returning the seeded id from a per-test-case map.
    - 8 test cases iterate the matrix (subject_id ∈ {101, 202} × site_id ∈ {1, 2} × channel ∈ {capi, pixel}) calling `EventLogWriter::record` and asserting (a) every insert succeeds, (b) `DB::table('logingrupa_metapixel_event_log')->count()` equals 8, (c) `(subject_type, subject_id, event_name, channel, site_id)` UNIQUE allows different site_id rows for same subject_id under NULL-distinct semantics.
    - One additional `test_null_site_id_rows_are_distinct_under_unique`: 2 inserts with NULL site_id for same subject — both succeed.

    `tests/Feature/Migrations/AddMultisitePixelIdAndTokenTest.php` (MULT-06):
    - `test_up_is_idempotent_when_system_settings_table_present`: invoke `up()` once, then again — no exception.
    - `test_up_is_idempotent_when_system_settings_table_absent`: drop `system_settings` table beforehand, invoke `up()` — no exception, no side effect.
    - `test_down_is_noop`: invoke `down()` — no exception; `system_settings` schema unchanged (column count before == after).

    `tests/fixtures/sites.php` (shared fixture — per 04-RESEARCH §"Wave 0 Gaps" line 1832):
    - Returns a callable `function (\Illuminate\Database\Schema\Builder $obSchema, \Illuminate\Database\ConnectionInterface $obConn): void { … }` that creates a minimal `cms_themes`-equivalent `system_site_definitions` table (or whichever October core table backs `Site::listSites()` in the test bootstrap — verify against MetapixelTestCase setUp lines 83-90 referenced in RESEARCH §"Migration shape") and seeds 2 rows id=1 + id=2 with distinct names so `Site::withContext(1, fn)` and `Site::withContext(2, fn)` both resolve.

    Use Hungarian notation for ALL locals: `$obSettings`, `$arCreds`, `$iSiteId`, `$sPixelId`, `$bMatched`. No `assert()` anywhere — use `expect()` (Pest) or `$this->assert*` (PHPUnit) only.

    Every test file's docblock declares: "Wave 0 RED — fails until plan 04-01 ships."

    Do NOT use fenced code blocks inside action prose itself — emit source via Write tool.
  </action>
  <acceptance_criteria>
    - File `tests/Unit/Models/SettingsMultisiteTraitTest.php` exists; `final class SettingsMultisiteTraitTest extends MetapixelTestCase` present; contains 4 `test_*` methods.
    - File `tests/Feature/Settings/SettingsLookupForSiteTest.php` exists; contains 5 `test_*` methods including `test_lookup_for_site_empty_per_site_pixel_falls_back_to_default`.
    - File `tests/Feature/MultisiteEventLogRoutingTest.php` exists; contains 9 `test_*` methods (8 matrix + 1 NULL-distinct).
    - File `tests/Feature/Migrations/AddMultisitePixelIdAndTokenTest.php` exists; contains 3 `test_*` methods.
    - File `tests/fixtures/sites.php` exists; returns a callable that seeds 2 site rows.
    - `grep -c 'function test_' tests/Unit/Models/SettingsMultisiteTraitTest.php tests/Feature/Settings/SettingsLookupForSiteTest.php tests/Feature/MultisiteEventLogRoutingTest.php tests/Feature/Migrations/AddMultisitePixelIdAndTokenTest.php` reports sum ≥ 21.
    - Every test file imports `Logingrupa\Metapixel\Tests\MetapixelTestCase`.
    - No file contains `assert(`, `@phpstan-ignore`, or fenced code in the docblock body.
    - `composer test -- --filter=SettingsMultisiteTraitTest|SettingsLookupForSiteTest|MultisiteEventLogRoutingTest|AddMultisitePixelIdAndTokenTest` exits NON-ZERO (red — expected for Wave 0).
  </acceptance_criteria>
  <verify>
    <automated>cd plugins/logingrupa/metapixel &amp;&amp; grep -rn 'function test_' tests/Unit/Models/SettingsMultisiteTraitTest.php tests/Feature/Settings/SettingsLookupForSiteTest.php tests/Feature/MultisiteEventLogRoutingTest.php tests/Feature/Migrations/AddMultisitePixelIdAndTokenTest.php | wc -l | awk '{exit ($1 &gt;= 21 ? 0 : 1)}'</automated>
  </verify>
  <done>5 files exist with ≥ 21 test methods; tests currently fail (red), satisfying Wave 0 contract per VALIDATION.md.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1: Settings.php — explicit $propagatable lock + per-site lookupForSite body</name>
  <files>
    plugins/logingrupa/metapixel/models/Settings.php
  </files>
  <behavior>
    - With no per-site row seeded, `Settings::lookupForSite(null)` returns the default-row pixel_id + capi_access_token.
    - With a per-site row seeded (`Site::withContext(1, fn() => Settings::set(['pixel_id' => 'P1', 'capi_access_token' => 'T1']))`), `Settings::lookupForSite(1)` returns `['pixel_id' => 'P1', 'capi_access_token' => 'T1']`.
    - With a per-site row whose `pixel_id` is empty string, `Settings::lookupForSite(1)['pixel_id']` falls back to default-row value (D-01).
    - With a per-site row whose `capi_access_token` is null, the result's `capi_access_token` falls back to default-row value (D-01).
    - `(new Settings)::class`'s `protected $propagatable` is declared at the descendant level and equals `[]`.
    - Existing `beforeSave` + `splitEventNameInput` + `partitionEventNames` + `getThemeCustomEventNames` methods are PRESERVED verbatim.
  </behavior>
  <read_first>
    plugins/logingrupa/metapixel/models/Settings.php (full current state — preserve theme_custom_event_names sanitization),
    plugins/logingrupa/metapixel/.planning/phases/04-settings-rework-multisite-trustedhosts-cookie-failedevents-t/04-RESEARCH.md (Pattern 2 lines 406-470 — lookupForSite body sketch with two private helpers; Example 2 lines 1591-1619 — verbatim implementation; Pitfall 1 lines 1447-1455 — clearInternalCache requirement),
    plugins/logingrupa/metapixel/.planning/phases/04-settings-rework-multisite-trustedhosts-cookie-failedevents-t/04-PATTERNS.md (Settings.php section lines 36-116 — analog inheritance + change list)
  </read_first>
  <action>
    Per D-01 / D-02 / D-20 — edit `plugins/logingrupa/metapixel/models/Settings.php`:

    1) Import `October\Rain\Support\Facades\Site;` at top.
    2) Re-implement `public static function lookupForSite(?int $iSiteId): array` body per RESEARCH Example 2 verbatim (lines 1591-1619):
       - Read default-row credentials via a new private static `readCredentialsInGlobalContext(): array` that wraps `Settings::clearInternalCache()` + two `self::get(...)` calls inside `Site::withGlobalContext(callable)`.
       - When `$iSiteId === null`, return defaults.
       - Otherwise call new private static `readCredentialsForSiteContext(int $iSiteId): array` that wraps the same `clearInternalCache` + two `get` pattern inside `Site::withContext($iSiteId, callable)` (RESEARCH Pitfall 9 — site id arg FIRST, then callable).
       - Per D-01: each returned key fall back to the default value when the per-site value is empty string. The helpers MUST runtime-guard `is_string($mValue) ? $mValue : ''` per the level-10 mixed-cast lock from Phase 2 STATE.md (no `@phpstan-ignore`).
    3) Both private helpers MUST call `self::clearInternalCache()` INSIDE the closure (RESEARCH Pitfall 1 — without this, Eloquent `remember()` cache returns stale credentials across `Site::withContext` switches).
    4) Keep the existing `protected $propagatable = []` (line 29) — D-20 says explicit at the descendant level for marketplace audit. Update the docblock to call out D-02 lock.
    5) Update the existing class-level docblock to remove "Phase 2 stub" wording — the body is now live. Replace with one-line summary per plugin CLAUDE.md "Laravel short docblocks" rule (no multi-paragraph narrative).
    6) DO NOT add `theme_custom_event_names`-style `Flash::warning` to `lookupForSite` — it is a server-side helper, never a user-input boundary.

    Per `plugin CLAUDE.md` Hungarian notation rule: locals are `$mPixel`, `$mToken`, `$sDefaultPixel`, `$sDefaultToken`, `$sSitePixel`, `$sSiteToken`. Methods return type `array` is preserved.

    Apply Pint after the edit: `vendor/bin/pint --dirty --format agent` (from `/home/forge/nailscosmetics.lv`).
  </action>
  <acceptance_criteria>
    - `Settings.php` contains literal substring `Site::withContext($iSiteId,` (per-site closure invocation).
    - `Settings.php` contains literal substring `Site::withGlobalContext(` (default-row closure invocation).
    - `Settings.php` contains literal substring `self::clearInternalCache()` appearing TWICE (one per closure helper).
    - `Settings.php` contains literal substring `use October\Rain\Support\Facades\Site;` import.
    - `Settings.php` contains `protected $propagatable = []` declaration AT the class body (not inherited only).
    - `grep -c 'function ' plugins/logingrupa/metapixel/models/Settings.php` returns ≥ 6 (lookupForSite + 2 readCredentials helpers + beforeSave + splitEventNameInput + partitionEventNames + getThemeCustomEventNames).
    - `composer test -- --filter=SettingsLookupForSiteTest` exits 0 (5 cases pass).
    - `composer test -- --filter=SettingsMultisiteTraitTest` exits 0 (4 cases pass).
    - `vendor/bin/pint --test --format agent plugins/logingrupa/metapixel/models/Settings.php` exits 0 (no style violations).
  </acceptance_criteria>
  <verify>
    <automated>cd plugins/logingrupa/metapixel &amp;&amp; composer test -- --filter='SettingsLookupForSiteTest|SettingsMultisiteTraitTest'</automated>
  </verify>
  <done>Settings.php hosts a per-site-aware lookupForSite body with default fallback (D-01); $propagatable empty whitelist explicit (D-02); 9 Wave 0 tests in scope all green.</done>
</task>

<task type="auto">
  <name>Task 2: AddMultisitePixelIdAndToken migration + version.yaml registration</name>
  <files>
    plugins/logingrupa/metapixel/updates/AddMultisitePixelIdAndToken.php,
    plugins/logingrupa/metapixel/updates/version.yaml
  </files>
  <read_first>
    plugins/logingrupa/metapixel/updates/AddPayloadToMetapixelEventLogTable.php (verbatim idempotent migration shape — per 04-PATTERNS.md lines 254-287),
    plugins/logingrupa/metapixel/updates/version.yaml (current sequence — append after 1.0.1),
    plugins/logingrupa/metapixel/.planning/phases/04-settings-rework-multisite-trustedhosts-cookie-failedevents-t/04-RESEARCH.md (Pattern 3 lines 472-510 — migration body sketch; Open Question 6 lines 1745-1748 — pure no-op vs column-check),
    plugins/logingrupa/metapixel/.planning/phases/04-settings-rework-multisite-trustedhosts-cookie-failedevents-t/04-CONTEXT.md (D-03 — schema-additive only)
  </read_first>
  <action>
    Per D-03 / MULT-06 — author `plugins/logingrupa/metapixel/updates/AddMultisitePixelIdAndToken.php`.

    File MUST follow Phase 2 migration shape lock (STATE.md "Migration file naming convention: PascalCase basenames matching class FQN"):
    - Namespace `Logingrupa\Metapixel\Updates`.
    - PascalCase basename matching class FQN.
    - `class AddMultisitePixelIdAndToken extends October\Rain\Database\Updates\Migration`.
    - Public const `TABLE = 'system_settings'` for traceability grep.
    - `public function up()` body per Pattern 3 / Open Question 6 recommendation — pure no-op guarded by `Schema::hasTable('system_settings')` early-return. Docblock summarises D-03 in one line ("Schema-additive only; Multisite routes per-site via row layer, not column layer.").
    - `public function down()` body is a single `return;` line (down does nothing; the trait-level routing has no schema to drop).
    - Import `Schema` from `October\Rain\Support\Facades\Schema` (or top-level `use Schema;` — match the in-plugin sibling `AddPayloadToMetapixelEventLogTable.php` line 1-12 exactly).
    - No `assert()`, no `@phpstan-ignore`.

    Append to `plugins/logingrupa/metapixel/updates/version.yaml` per October convention (refer to current content):
    ```
    1.0.2:
        - Multisite per-site credential routing (MULT-01..06).
        - AddMultisitePixelIdAndToken.php
    ```
    (Use literal `1.0.2` bump — version.yaml currently ends at `1.0.1` per file read. Future plans 04-04 will bump again for AddDedupColumnsToFailedEvents.)

    Apply Pint: `vendor/bin/pint --dirty --format agent`.
  </action>
  <acceptance_criteria>
    - File `updates/AddMultisitePixelIdAndToken.php` exists.
    - File contains literal `class AddMultisitePixelIdAndToken extends Migration` (or fully-qualified `\October\Rain\Database\Updates\Migration`).
    - File `up()` body contains literal `Schema::hasTable('system_settings')`.
    - File contains exactly ONE `public function up` and ONE `public function down`.
    - File `down()` body has zero `Schema::` calls (verified via grep: `grep -c 'Schema::' .` in down section is 0).
    - `updates/version.yaml` contains literal `AddMultisitePixelIdAndToken.php` under a new version line (`1.0.2:`).
    - `composer test -- --filter=AddMultisitePixelIdAndTokenTest` exits 0 (3 cases green).
    - `vendor/bin/pint --test --format agent plugins/logingrupa/metapixel/updates/AddMultisitePixelIdAndToken.php` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd plugins/logingrupa/metapixel &amp;&amp; composer test -- --filter=AddMultisitePixelIdAndTokenTest</automated>
  </verify>
  <done>Idempotent no-op migration class exists; version.yaml registers it; MULT-06 test green.</done>
</task>

<task type="auto">
  <name>Task 3: phpstan.neon disallowed-calls — ban direct Settings::get('pixel_id'|'capi_access_token') outside lookupForSite + run 8-path matrix</name>
  <files>
    plugins/logingrupa/metapixel/phpstan.neon
  </files>
  <read_first>
    plugins/logingrupa/metapixel/phpstan.neon (current disallowed-calls block — D-02 extends this pattern),
    plugins/logingrupa/metapixel/.planning/phases/04-settings-rework-multisite-trustedhosts-cookie-failedevents-t/04-RESEARCH.md (§"Anti-Patterns" line 1410 — D-02 ban statement; §"Architectural Responsibility Map" line 144),
    plugins/logingrupa/metapixel/.planning/phases/04-settings-rework-multisite-trustedhosts-cookie-failedevents-t/04-CONTEXT.md (D-02 + "Claude's Discretion" line 71 — exact rule wording is planner's)
  </read_first>
  <action>
    Per D-02 — extend `phpstan.neon` disallowed-calls block.

    Add a new entry under `parameters.spaze.disallowedMethodCalls` (or `disallowedStaticCalls` — whichever the existing Phase 2 `SiteManager::*` / `Site::*` ban uses; mirror the structure of the closest existing rule).

    The rule MUST:
    - Target `Logingrupa\Metapixel\Models\Settings::get` (static call) when the FIRST argument string literal equals `'pixel_id'` OR `'capi_access_token'`. Use `paramsInAllowed` or `paramFromAllowed` constraint per spaze/phpstan-disallowed-calls docs. If a parameter-value match constraint is unavailable, fall back to: ban `Settings::get` in all files EXCEPT `models/Settings.php` (the file that hosts `lookupForSite`) via `allowIn` (or `disallowIn` deny-list NOT including that single file). Pick whichever matches the existing Phase 2 deny-list shape.
    - `message`: "Direct Settings::get('pixel_id'|'capi_access_token') is banned outside Settings::lookupForSite per D-02 — route credentials through Settings::lookupForSite(\$iSiteId)."
    - The rule does NOT need to ban `Settings::get` of OTHER keys (`trusted_hosts`, `ensure_fbp_fbc_server_side`, `theme_custom_event_names`, etc.) — those callsites are legitimate.

    After adding the rule, run the 8-path matrix test from Task 0 to confirm it passes once Tasks 1+2 land:
    - `composer test -- --filter=MultisiteEventLogRoutingTest`

    Then run the full `composer qa` chain to confirm:
    - `pint-test`: zero style violations on touched files.
    - `phpstan analyse`: zero errors at level 10 (the new disallowed-call rule itself must not flag `Settings::lookupForSite`'s own body since the body lives IN the allowed file).
    - `phpmd`: zero violations.
    - `pest --coverage --min=90`: coverage gate ≥ 90%.

    Apply Pint if `phpstan.neon` formatting requires it (neon syntax — typically untouched by Pint, but the chain runs the full suite).
  </action>
  <acceptance_criteria>
    - `phpstan.neon` contains a new disallowed-call rule referencing `Logingrupa\Metapixel\Models\Settings::get` (grep: `grep -c 'Settings::get' plugins/logingrupa/metapixel/phpstan.neon` ≥ 1).
    - `phpstan.neon` contains the literal substring `lookupForSite` (in the rule's `message` or `allowIn` clause).
    - `composer test -- --filter=MultisiteEventLogRoutingTest` exits 0 (9 cases including NULL-distinct).
    - `composer qa` exits 0 (full chain green — coverage ≥ 90%).
    - Inserting a temporary `Settings::get('pixel_id', '')` call in any file under `classes/` produces a PHPStan error referencing the new rule when running `vendor/bin/phpstan analyse --no-progress` (smoke test — manual verification; not required for CI).
  </acceptance_criteria>
  <verify>
    <automated>cd plugins/logingrupa/metapixel &amp;&amp; composer qa</automated>
  </verify>
  <done>D-02 statically enforced by phpstan disallowed-calls; full composer qa green (pint + phpstan level 10 + phpmd + pest 90% coverage); 8-path matrix green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator backend → Settings model | Admin saves per-site pixel_id / capi_access_token via top-bar site picker; Multisite trait persists per-site row keyed by `site_id`. |
| `SendCapiEvent::handle` → Meta CAPI | Job rehydrates Settings via `lookupForSite($iSiteId)` and POSTs to `graph.facebook.com/v23.0/{pixel}/events`. Wrong-site credential leak = cross-site Meta-pixel mismatch. |
| Cross-site row read | `Site::withContext($iSiteId, ...)` MUST scope per-site read; failure to clear `SettingModel::$instances` cache returns stale data across the context switch. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-MULT-01 | Information Disclosure | `Settings::lookupForSite` | mitigate | D-01 silent fallback path is intentional; D-02 `phpstan` disallowed-calls rule blocks direct `Settings::get('pixel_id'\|'capi_access_token')` outside `lookupForSite`. Test `test_lookup_for_site_with_id_returns_per_site_row` proves per-site routing. |
| T-04-MULT-02 | Information Disclosure (P-10) | `$propagatable` whitelist drift | mitigate | Explicit `protected $propagatable = []` at descendant level (MULT-01); unit test `test_pixel_id_is_not_in_propagatable` + `test_capi_access_token_is_not_in_propagatable` block accidental include of credential fields. |
| T-04-MULT-03 | Tampering | `SettingModel::$instances` cache leak | mitigate | `self::clearInternalCache()` inside each `Site::withContext` closure (RESEARCH Pitfall 1); 8-path matrix test fails fast if any call returns stale credentials. |
| T-04-MULT-04 | Denial of Service | Migration runs before `system_settings` table exists on fresh install | mitigate | `Schema::hasTable('system_settings')` guard at top of `up()` (Pattern 3); test `test_up_is_idempotent_when_system_settings_table_absent` proves no exception. |
</threat_model>

<verification>
- Wave 0 tests author phase (Task 0) — verify 5 files exist with ≥ 21 test cases via grep.
- After Task 1 — `composer test -- --filter='SettingsLookupForSiteTest|SettingsMultisiteTraitTest'` exits 0.
- After Task 2 — `composer test -- --filter=AddMultisitePixelIdAndTokenTest` exits 0.
- After Task 3 — `composer qa` exits 0 (full pint + phpstan + phpmd + pest 90% chain green); `composer test -- --filter=MultisiteEventLogRoutingTest` exits 0.
- Manual smoke (deferred to Phase 4 verification): inject a temporary direct `Settings::get('pixel_id')` call in any file under `classes/` and confirm `vendor/bin/phpstan analyse` emits the new D-02 disallowed-call error.
</verification>

<success_criteria>
- MULT-01: explicit `protected $propagatable = []` declared at `Settings::class` level — verified by reflection unit test.
- MULT-02: `pixel_id` + `capi_access_token` never appear in `$propagatable` — verified by 2 reflection unit tests.
- MULT-03: `lookupForSite(?int)` returns per-site row when present; falls back to default row when per-site value is empty string OR null — verified by 5 feature tests including D-01 fallback path.
- MULT-04: `SendCapiEvent::handle` callsite unchanged (Phase 2 BackboneIntegrationTest already proves this path); no edit to `classes/queue/SendCapiEvent.php` in this plan.
- MULT-05: 8-path matrix (2 sites × 2 adapters × 2 channels) + NULL-distinct UNIQUE test green; 9 cases verified by `MultisiteEventLogRoutingTest`.
- MULT-06: `AddMultisitePixelIdAndToken` migration idempotent + no-op safe; 3 cases verified by `AddMultisitePixelIdAndTokenTest`.
- `composer qa` exits 0 end-to-end; coverage ≥ 90%.
</success_criteria>

<output>
Create `.planning/phases/04-settings-rework-multisite-trustedhosts-cookie-failedevents-t/04-01-SUMMARY.md` when done — capture: artifacts (files created/modified counts), commits (atomic per task), test totals (Pest cases passing + coverage delta), Hungarian/Tiger-Style/Pint compliance confirmation, any deviations from this PLAN.md with reasons.
</output>
