---
phase: 03.1-08-dead-code-cleanup
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - plugins/logingrupa/metapixelshopaholic/.planning/PLAN.md
  - plugins/logingrupa/metapixelshopaholic/.planning/PLAN-v2-original.md
  - plugins/logingrupa/metapixelshopaholic/updates/.gitkeep
  - plugins/logingrupa/metapixelshopaholic/composer.json
  - plugins/logingrupa/metapixelshopaholic/.planning/phases/03.1-08-dead-code-cleanup/FOLLOWUP.md
autonomous: true
requirements:
  - CLEAN-04
tags:
  - cleanup
  - planning-docs
  - composer
  - phase-3.1
must_haves:
  truths:
    - ".planning/PLAN.md begins with `> **SUPERSEDED 2026-05-13**` annotation block pointing to .planning/phases/03.1-event-log-refactor/BRIEF.md"
    - ".planning/PLAN-v2-original.md begins with the same SUPERSEDED annotation block"
    - "updates/.gitkeep file deleted (directory non-empty post-08a0d12 migration cleanup)"
    - "composer.json has no `_comments` key AND `composer validate --strict` exits 0"
  artifacts:
    - path: "plugins/logingrupa/metapixelshopaholic/.planning/PLAN.md"
      provides: "Annotated pre-3.1 design doc with SUPERSEDED header"
      contains: "SUPERSEDED 2026-05-13"
    - path: "plugins/logingrupa/metapixelshopaholic/.planning/PLAN-v2-original.md"
      provides: "Annotated v2-era design doc with SUPERSEDED header"
      contains: "SUPERSEDED 2026-05-13"
    - path: "plugins/logingrupa/metapixelshopaholic/composer.json"
      provides: "Schema-clean composer manifest (no non-standard keys)"
      contains: "\"name\""
  key_links:
    - from: ".planning/PLAN.md SUPERSEDED block"
      to: ".planning/phases/03.1-event-log-refactor/BRIEF.md"
      via: "markdown link reference"
      pattern: "phases/03.1-event-log-refactor/BRIEF.md"
    - from: ".planning/PLAN-v2-original.md SUPERSEDED block"
      to: ".planning/phases/03.1-event-log-refactor/BRIEF.md"
      via: "markdown link reference"
      pattern: "phases/03.1-event-log-refactor/BRIEF.md"
---

<objective>
Track T4 — planning-doc + composer.json cleanup. Three atomic commits. Annotate the two stale top-level planning docs that still narrate the pre-3.1 `meta_purchase_event_id` column-based design as SUPERSEDED; delete `updates/.gitkeep` (directory is non-empty post-08a0d12); strip the non-standard `_comments` key from `composer.json`.

Purpose: planning-doc staleness misleads onboarding contributors; the `_comments` composer key is non-schema (composer validate flags it as a warning); the .gitkeep placeholder no longer serves a purpose. Zero production-code OR test-file touches (BRIEF Constraint #5).

Output: 3 commits with `03.1-cleanup` scope. Files touched are .planning markdown + updates/.gitkeep + composer.json only.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@plugins/logingrupa/metapixelshopaholic/CLAUDE.md
@plugins/logingrupa/metapixelshopaholic/.planning/STATE.md
@plugins/logingrupa/metapixelshopaholic/.planning/phases/03.1-08-dead-code-cleanup/BRIEF.md

<interfaces>
SUPERSEDED annotation block template per BRIEF T4.1 option (B). Prepended verbatim to both `.planning/PLAN.md` and `.planning/PLAN-v2-original.md` as the first content block (after any existing YAML frontmatter, if present):

    > **SUPERSEDED 2026-05-13**: this document describes the pre-3.1 design (idempotency via `meta_purchase_event_id` column on `lovata_orders_shopaholic_orders`). The column-based contract was replaced by the plugin-owned `logingrupa_metapixel_event_log` table in Phase 3.1. See [`.planning/phases/03.1-event-log-refactor/BRIEF.md`](phases/03.1-event-log-refactor/BRIEF.md) for the current architecture.

FOLLOWUP.md template (for any deferred-out-of-scope items per BRIEF Constraint #1):

    # FOLLOWUP — Phase 03.1-08 deferred items

    One-line entries. Created on first deferral; appended to thereafter. Cleared when items are addressed in a later phase.

    - <YYYY-MM-DD> <plan>.<task>: <one-line description of the deferred item + where it was spotted>
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1 (T4.1): Annotate .planning/PLAN.md + PLAN-v2-original.md as SUPERSEDED</name>
  <files>
    plugins/logingrupa/metapixelshopaholic/.planning/PLAN.md,
    plugins/logingrupa/metapixelshopaholic/.planning/PLAN-v2-original.md,
    plugins/logingrupa/metapixelshopaholic/.planning/phases/03.1-08-dead-code-cleanup/FOLLOWUP.md (only if README-references-PLAN.md branch fires)
  </files>
  <read_first>
    - plugins/logingrupa/metapixelshopaholic/.planning/PLAN.md (read first 30 lines once — check for existing frontmatter / top-of-file blocks to determine insertion point)
    - plugins/logingrupa/metapixelshopaholic/.planning/PLAN-v2-original.md (read first 30 lines once — same audit)
    - plugins/logingrupa/metapixelshopaholic/.planning/phases/03.1-08-dead-code-cleanup/BRIEF.md (T4.1 spec lines 222-236 — pick option B annotate, not delete or archive; AND Constraint #1 lines 301-303 — no drive-by edits outside listed scope; defer to FOLLOWUP.md instead)
  </read_first>
  <action>
    Prepend the SUPERSEDED annotation block (see interfaces section above) to BOTH files. Insertion point: immediately after any YAML frontmatter (a `---` opening line through the matching `---` closing line) if present, otherwise as the very first content of the file. The annotation block ends with one blank line before the existing body content begins.

    The exact prepended line is the single-line blockquote starting `> **SUPERSEDED 2026-05-13**:` shown in the interfaces section. Copy verbatim — do NOT paraphrase. Do NOT translate. Do NOT add additional metadata.

    Single commit covers both PLAN.md + PLAN-v2-original.md edits (same logical concern: "annotate two superseded docs").

    **README.md handling (BRIEF Constraint #1 — no drive-by):** Do NOT edit `.planning/README.md` inline as part of this task. If, while doing the audit, you observe that `.planning/README.md` exists AND it references PLAN.md or PLAN-v2-original.md as the canonical design doc, then APPEND a single one-line entry to `.planning/phases/03.1-08-dead-code-cleanup/FOLLOWUP.md` (create the file with the FOLLOWUP.md template header if absent). Entry shape: `- 2026-05-14 plan-04.task-1: .planning/README.md references now-SUPERSEDED PLAN.md — update README to point at .planning/phases/03.1-event-log-refactor/BRIEF.md`. The FOLLOWUP entry is co-committed with the PLAN.md/PLAN-v2-original.md annotation (single atomic commit per BRIEF Constraint #3 — the deferral note is the same logical concern as the annotation it defers from).
  </action>
  <verify>
    <automated>cd plugins/logingrupa/metapixelshopaholic && head -5 .planning/PLAN.md | grep -q "SUPERSEDED 2026-05-13" && head -5 .planning/PLAN-v2-original.md | grep -q "SUPERSEDED 2026-05-13" && grep -q "phases/03.1-event-log-refactor/BRIEF.md" .planning/PLAN.md && grep -q "phases/03.1-event-log-refactor/BRIEF.md" .planning/PLAN-v2-original.md</automated>
  </verify>
  <acceptance_criteria>
    - `head -5 plugins/logingrupa/metapixelshopaholic/.planning/PLAN.md | grep -q "SUPERSEDED 2026-05-13"` exit 0 (annotation in first 5 lines)
    - `head -5 plugins/logingrupa/metapixelshopaholic/.planning/PLAN-v2-original.md | grep -q "SUPERSEDED 2026-05-13"` exit 0
    - `grep -q "phases/03.1-event-log-refactor/BRIEF.md" plugins/logingrupa/metapixelshopaholic/.planning/PLAN.md` exit 0
    - `grep -q "phases/03.1-event-log-refactor/BRIEF.md" plugins/logingrupa/metapixelshopaholic/.planning/PLAN-v2-original.md` exit 0
    - Both files' body content unchanged below the prepended block (verify via `git diff` — only the prepended annotation lines should appear as `+` additions, no `-` deletions of existing body)
    - Annotation block uses BRIEF option (B) verbatim wording (markdown blockquote + bold SUPERSEDED token + date 2026-05-13 + link to BRIEF.md)
    - `git diff` shows NO changes to `.planning/README.md` (drive-by forbidden per BRIEF Constraint #1)
    - IF the audit found README.md references to PLAN.md, then `test -f plugins/logingrupa/metapixelshopaholic/.planning/phases/03.1-08-dead-code-cleanup/FOLLOWUP.md` exit 0 AND `grep -q "README.md references now-SUPERSEDED PLAN.md" plugins/logingrupa/metapixelshopaholic/.planning/phases/03.1-08-dead-code-cleanup/FOLLOWUP.md` exit 0
  </acceptance_criteria>
  <commit_message>docs(03.1-cleanup): annotate PLAN.md + PLAN-v2-original.md as superseded by phase 03.1</commit_message>
  <done>
    Both .planning/PLAN.md and .planning/PLAN-v2-original.md begin with SUPERSEDED annotation block pointing to phase 03.1 BRIEF.md. Body content untouched. .planning/README.md untouched (any required README update deferred to FOLLOWUP.md per BRIEF Constraint #1). Atomic commit landed.
  </done>
</task>

<task type="auto">
  <name>Task 2 (T4.2): Delete updates/.gitkeep</name>
  <files>plugins/logingrupa/metapixelshopaholic/updates/.gitkeep</files>
  <read_first>
    - plugins/logingrupa/metapixelshopaholic/.planning/phases/03.1-08-dead-code-cleanup/BRIEF.md (T4.2 spec lines 238-242)
    - Run `ls -la plugins/logingrupa/metapixelshopaholic/updates/` BEFORE the delete to confirm the directory has version.yaml + at least 2 migration .php files (post-08a0d12 inventory — .gitkeep is redundant)
  </read_first>
  <action>
    Run `git rm plugins/logingrupa/metapixelshopaholic/updates/.gitkeep` from repo root. If git reports the file is not tracked (untracked or already removed in working tree), fall back to plain `rm plugins/logingrupa/metapixelshopaholic/updates/.gitkeep`. After deletion, confirm `ls plugins/logingrupa/metapixelshopaholic/updates/` lists version.yaml + ≥ 2 migration files. No other file in updates/ is touched.
  </action>
  <verify>
    <automated>cd /home/forge/nailscosmetics.lv && ! test -f plugins/logingrupa/metapixelshopaholic/updates/.gitkeep && ls plugins/logingrupa/metapixelshopaholic/updates/ | grep -qE "version.yaml" && [[ $(ls plugins/logingrupa/metapixelshopaholic/updates/*.php 2>/dev/null | wc -l) -ge 2 ]]</automated>
  </verify>
  <acceptance_criteria>
    - `! test -f /home/forge/nailscosmetics.lv/plugins/logingrupa/metapixelshopaholic/updates/.gitkeep` (file gone)
    - `ls /home/forge/nailscosmetics.lv/plugins/logingrupa/metapixelshopaholic/updates/ | grep -qE "version.yaml"` (version.yaml still present)
    - `ls /home/forge/nailscosmetics.lv/plugins/logingrupa/metapixelshopaholic/updates/*.php 2>/dev/null | wc -l` returns ≥ 2 (migration files still present)
    - `git status plugins/logingrupa/metapixelshopaholic/updates/` shows only `.gitkeep` deletion (no other change)
  </acceptance_criteria>
  <commit_message>chore(03.1-cleanup): remove updates/.gitkeep — directory non-empty</commit_message>
  <done>
    updates/.gitkeep deleted; updates/ still contains version.yaml + migration files. Atomic commit landed.
  </done>
</task>

<task type="auto">
  <name>Task 3 (T4.3): Remove non-standard `_comments` key from composer.json</name>
  <files>plugins/logingrupa/metapixelshopaholic/composer.json</files>
  <read_first>
    - plugins/logingrupa/metapixelshopaholic/composer.json (read ENTIRE file once — locate the `_comments` key, identify what content lives under it, audit whether any content needs preserving elsewhere)
    - plugins/logingrupa/metapixelshopaholic/CLAUDE.md (Tech Stack section — confirms PHP constraint rationale already lives there per BRIEF T4.3 line 249)
    - plugins/logingrupa/metapixelshopaholic/.planning/phases/03.1-08-dead-code-cleanup/BRIEF.md (T4.3 spec lines 244-252)
  </read_first>
  <action>
    Per BRIEF T4.3: prefer DELETE the `_comments` block entirely (PHP-constraint rationale already lives in CLAUDE.md tech-stack section per the read_first audit). Edit composer.json removing the key + its value object (and any trailing comma cleanup so the JSON remains valid). Do NOT touch any other key — `require`, `require-dev`, `autoload`, `scripts`, `extra`, `config` all remain byte-identical.

    After edit, run `cd plugins/logingrupa/metapixelshopaholic && composer validate --strict` and confirm exit 0 with zero warnings. If composer validate complains about anything else (unrelated to `_comments`), that's a separate concern — surface to FOLLOWUP.md (BRIEF Constraint #1 — no drive-by fixes) and proceed with commit.

    Optional COMPOSER.md companion docblock (per BRIEF alternative): if any `_comments` content is genuinely operator-facing (not already in CLAUDE.md), create `plugins/logingrupa/metapixelshopaholic/COMPOSER.md` with the surviving rationale. Skip if CLAUDE.md already covers it (default).
  </action>
  <verify>
    <automated>cd plugins/logingrupa/metapixelshopaholic && ! grep -q "_comments" composer.json && composer validate --strict 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `! grep -q "_comments" plugins/logingrupa/metapixelshopaholic/composer.json` exit 0 (key gone)
    - `cd plugins/logingrupa/metapixelshopaholic && composer validate --strict` exits 0
    - `cd plugins/logingrupa/metapixelshopaholic && composer validate --strict 2>&1 | grep -c "WARNING\\|ERROR"` returns 0 OR every WARNING/ERROR present is logged in FOLLOWUP.md as out-of-scope (BRIEF Constraint #1)
    - `python3 -c "import json; json.load(open('plugins/logingrupa/metapixelshopaholic/composer.json'))"` exits 0 (JSON still valid)
    - `git diff plugins/logingrupa/metapixelshopaholic/composer.json` shows ONLY `_comments` key removal — no other key altered
  </acceptance_criteria>
  <commit_message>chore(03.1-cleanup): composer.json remove non-standard _comments key</commit_message>
  <done>
    composer.json no longer has `_comments` key; composer validate --strict exits 0; JSON valid; no other key touched. Atomic commit landed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| .planning/ docs → onboarding developer | Stale narrative misleads — not a security boundary, documentation-quality only. |
| composer.json → composer / packagist tooling | Non-standard key is schema-warning, not vulnerability. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03.1-08-08 | Repudiation | composer.json schema drift | accept | `_comments` key is non-standard but harmless. Removal is hygiene per `composer validate --strict`. |
| T-03.1-08-09 | Information disclosure | Stale PLAN.md narrative | accept | Public planning doc, no PII / secret. Annotation header serves as the mitigation. |
</threat_model>

<verification>
After all 3 tasks land:
- `git log --oneline | head -3` shows 3 commits with `03.1-cleanup` scope:
  1. `docs(03.1-cleanup): annotate PLAN.md + PLAN-v2-original.md as superseded by phase 03.1`
  2. `chore(03.1-cleanup): remove updates/.gitkeep — directory non-empty`
  3. `chore(03.1-cleanup): composer.json remove non-standard _comments key`
- `git diff --stat HEAD~3 HEAD` confirms 4-5 files touched (2 .planning markdown + 1 .gitkeep deletion + 1 composer.json + optional FOLLOWUP.md if README-defer branch fired).
- Zero production-code touches; zero test-file touches; zero .planning/README.md touches.
- `composer validate --strict` exits 0.
</verification>

<success_criteria>
- Track T4 (CLEAN-04) closed: 3 atomic commits.
- Roadmap success criteria 10, 11 satisfied (SUPERSEDED annotations on both PLAN.md + PLAN-v2-original.md; updates/.gitkeep removed; composer.json `_comments` stripped; composer validate --strict exits 0).
- Composer schema clean.
- Production code + test files + .planning/README.md untouched (any README update deferred to FOLLOWUP.md per BRIEF Constraint #1).
</success_criteria>

<output>
After completion, create `.planning/phases/03.1-08-dead-code-cleanup/03.1-08-04-SUMMARY.md` documenting:
- 3 commit SHAs in order
- composer validate exit code before/after Task 3
- Files touched count: 4 or 5 (no production-code, no tests; +1 if FOLLOWUP.md was created)
- FOLLOWUP.md created? Y/N + entry count
</output>
