---
phase: 03.1-08-dead-code-cleanup
plan: 05
type: execute
wave: 3
depends_on:
  - 01
  - 02
  - 03
  - 04
files_modified:
  - plugins/logingrupa/metapixelshopaholic/phpstan-baseline.neon
  - plugins/logingrupa/metapixelshopaholic/classes/queue/SendCapiEvent.php
  - plugins/logingrupa/metapixelshopaholic/models/EventLog.php
  - plugins/logingrupa/metapixelshopaholic/.planning/STATE.md
autonomous: true
requirements:
  - CLEAN-05
tags:
  - cleanup
  - milestone-close
  - composer-qa
  - git-tag
  - phase-3.1
must_haves:
  truths:
    - "`composer qa` from plugin dir exits 0 (pint-test + analyse + phpmd + test all green)"
    - "`composer test` reports 0 failed (or N failed where N matches every SKIP-BASELINE.md entry exactly)"
    - "`git tag -l v1.1.1` outputs `v1.1.1` (annotated, local-only — not pushed)"
    - "`.planning/STATE.md` `status:` field is `phase-3.1-milestone-ready`; `stopped_at:` updated per BRIEF T5.3 narrative"
  artifacts:
    - path: "plugins/logingrupa/metapixelshopaholic/phpstan-baseline.neon"
      provides: "Regenerated phpstan baseline IF the 2 pre-existing errors require >5 LOC PHPDoc fix (per BRIEF T5.1 fallback)"
      contains: "ignoreErrors"
    - path: "plugins/logingrupa/metapixelshopaholic/.planning/STATE.md"
      provides: "Updated frontmatter advancing status to `phase-3.1-milestone-ready`"
      contains: "phase-3.1-milestone-ready"
  key_links:
    - from: "composer qa script"
      to: "pint-test + analyse + phpmd + test composer-script chain"
      via: "composer.json scripts.qa array"
      pattern: "scripts.*qa"
    - from: "git tag v1.1.1"
      to: "version.yaml v1.1.1 ledger entry"
      via: "annotated tag matching version.yaml release line"
      pattern: "v1.1.1"
    - from: ".planning/STATE.md status field"
      to: "ROADMAP phase 03.1-08 closure"
      via: "frontmatter status: phase-3.1-milestone-ready"
      pattern: "phase-3.1-milestone-ready"
---

<objective>
Track T5 — milestone close for v1.1.1. Three atomic commits (plus the git tag, which is not a commit). Run `composer qa` end-to-end and ensure exit 0; create annotated local git tag `v1.1.1`; advance `.planning/STATE.md` to `phase-3.1-milestone-ready`. Depends on PLAN-01..04 having landed first (Wave 1: PLAN-01/02/04, Wave 2: PLAN-03, Wave 3: this).

Purpose: phase 3.1-08 cannot claim closure until composer qa goes green end-to-end (BRIEF T5.1). The git tag is the immutable ledger marker (BRIEF T5.2 — local-only, NOT pushed, operator decides push timing). STATE.md advance is the final cursor flip so future `/gsd-plan-phase 4` reads the correct starting state (BRIEF T5.3).

Output: 1 chore commit (composer qa green — may include phpstan-baseline.neon if narrow-fix infeasible); 1 git tag (NOT a commit); 1 docs commit on STATE.md. Total Phase 3.1-08 commit count = (T1: 3) + (T2: 5) + (T3: 6) + (T4: 3) + (T5: 2) = 19 atomic commits (BRIEF's 20-commit estimate gives one slot for T5.1 to include phpstan-baseline regeneration if needed; if a narrow PHPDoc fix lands instead, that's the 20th commit).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@plugins/logingrupa/metapixelshopaholic/CLAUDE.md
@plugins/logingrupa/metapixelshopaholic/.planning/STATE.md
@plugins/logingrupa/metapixelshopaholic/.planning/ROADMAP.md
@plugins/logingrupa/metapixelshopaholic/.planning/phases/03.1-08-dead-code-cleanup/BRIEF.md
@plugins/logingrupa/metapixelshopaholic/.planning/phases/03.1-08-dead-code-cleanup/03.1-08-01-PLAN.md
@plugins/logingrupa/metapixelshopaholic/.planning/phases/03.1-08-dead-code-cleanup/03.1-08-02-PLAN.md
@plugins/logingrupa/metapixelshopaholic/.planning/phases/03.1-08-dead-code-cleanup/03.1-08-03-PLAN.md
@plugins/logingrupa/metapixelshopaholic/.planning/phases/03.1-08-dead-code-cleanup/03.1-08-04-PLAN.md

<interfaces>
composer qa command chain (per BRIEF T5.1 lines 258-269):

    cd plugins/logingrupa/metapixelshopaholic
    composer pint-test     # PSR-2 + project pint.json
    composer analyse       # phpstan level 10
    composer phpmd         # plugin-scoped rules
    composer test          # all green
    composer qa            # composite — ALL exit 0

Known pre-existing phpstan errors (BRIEF T5.1 lines 271-274):
- `SendCapiEvent::extractFirstEvent` return-type widening
- `EventLog::subject` MorphTo

Fix path (BRIEF):
- Try narrow PHPDoc fix (≤ 5 LOC) first — touches `classes/queue/SendCapiEvent.php::extractFirstEvent` + `models/EventLog.php::subject()`
- Fallback: `composer baseline` + commit phpstan-baseline.neon (the two narrow-fix files remain untouched if this path is taken)

git tag (BRIEF T5.2 lines 277-286):

    cd plugins/logingrupa/metapixelshopaholic
    git tag -a v1.1.1 -m "Phase 3.1-07 cross-context site_id symmetry + 03.1-cleanup dead code removal"

Do NOT push to remote. Operator decision.

STATE.md frontmatter update (BRIEF T5.3 lines 289-297):

    status: phase-3.1-milestone-ready
    stopped_at: "Phase 3.1 closed end-to-end. v1.1.1 + 03.1-cleanup landed. qa green. Awaiting operator: v1.1.1 deploy on .lv/.lt/.no + STAGING Scenario 5 per site + manual repair of 2 stranded rows on new.nailscosmetics.lv (or accept Pixel-miss). Next: /gsd-plan-phase 4 (Funnel Completion)."
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1 (T5.1): composer qa end-to-end green pass</name>
  <files>
    plugins/logingrupa/metapixelshopaholic/phpstan-baseline.neon,
    plugins/logingrupa/metapixelshopaholic/classes/queue/SendCapiEvent.php,
    plugins/logingrupa/metapixelshopaholic/models/EventLog.php
    (narrow-fix path only — if baseline regen path taken, SendCapiEvent.php + EventLog.php untouched)
  </files>
  <read_first>
    - plugins/logingrupa/metapixelshopaholic/composer.json (read once — confirm scripts.qa chain exists and identify the exact sub-commands: pint-test, analyse, phpmd, test)
    - plugins/logingrupa/metapixelshopaholic/.planning/phases/03.1-08-dead-code-cleanup/BRIEF.md (T5.1 spec lines 258-275)
    - plugins/logingrupa/metapixelshopaholic/classes/queue/SendCapiEvent.php (find `extractFirstEvent` method — required to assess narrow PHPDoc fix candidate)
    - plugins/logingrupa/metapixelshopaholic/models/EventLog.php (find `subject()` MorphTo method — required to assess narrow PHPDoc fix)
    - plugins/logingrupa/metapixelshopaholic/phpstan.neon (read base config; confirm whether a baseline file is already configured / loaded)
  </read_first>
  <action>
    Execute the composer qa chain step-by-step from `plugins/logingrupa/metapixelshopaholic/`, capturing exit codes after each:

    Step 1: `composer pint-test`. If exit ≠ 0, run `composer pint` to auto-fix, then re-run `composer pint-test`. If still ≠ 0 after auto-fix, fail the task — pint diff means something is structurally wrong with formatting (likely PHP syntax error from earlier track). Investigate before proceeding.

    Step 2: `composer analyse` (phpstan level 10). Expected output: the 2 pre-existing errors (`SendCapiEvent::extractFirstEvent` + `EventLog::subject` MorphTo). For EACH error:
    - Attempt narrow PHPDoc fix first (BRIEF-preferred path): e.g., on `classes/queue/SendCapiEvent.php::extractFirstEvent` add `/** @return array<string,mixed>|null */` or whatever the precise widened type is; on `models/EventLog.php::subject()` add `/** @return MorphTo<\October\Rain\Database\Model, EventLog> */` (or the actual generic type per October Rain's universalObjectCrate config).
    - Re-run `composer analyse`. If both errors gone AND total LOC of PHPDoc additions ≤ 5 lines → keep the fix. Commit narrowly. The two files `classes/queue/SendCapiEvent.php` + `models/EventLog.php` are touched; phpstan-baseline.neon is NOT regenerated.
    - If narrow fix exceeds 5 LOC OR introduces type-juggling complexity OR breaks other phpstan checks → fall back: run `composer baseline` (regenerates phpstan-baseline.neon). Commit the regenerated baseline. In this fallback path, `classes/queue/SendCapiEvent.php` and `models/EventLog.php` remain untouched.

    Step 3: `composer phpmd`. Expected exit 0 (plugin-scoped rules historically clean per 03.1-07 SUMMARY). If new warnings appear, investigate root cause — likely from edits in PLAN-01/03 that exceeded cyclomatic-complexity or LongVariable thresholds; if so, surface as a regression on that PLAN, do not paper over.

    Step 4: `composer test`. Target: 0 failures. If any failures remain, they MUST appear in `tests/SKIP-BASELINE.md` per PLAN-03 fallback path. If a failure exists that is NOT in SKIP-BASELINE.md, return to PLAN-03 — do not proceed.

    Step 5: `composer qa` (composite). Final assertion — exit 0.

    Single commit covers any baseline regeneration OR narrow PHPDoc fix. Commit message PRIMARY (narrow fix): `fix(03.1-cleanup): narrow PHPDoc fix for phpstan SendCapiEvent + EventLog MorphTo`. Commit message FALLBACK (baseline regen): `chore(03.1-cleanup): composer qa green — regenerate phpstan-baseline.neon`. If NEITHER is needed (qa already green with prior commits — possible if Tracks 1-4 happened to resolve the phpstan errors as a side effect) → SKIP this commit; record the result in the SUMMARY but do not create empty commit.
  </action>
  <verify>
    <automated>cd plugins/logingrupa/metapixelshopaholic && composer pint-test 2>&1 | tail -3 && composer analyse 2>&1 | tail -5 && composer phpmd 2>&1 | tail -3 && composer test 2>&1 | tail -5 && composer qa; echo "qa exit: $?"</automated>
  </verify>
  <acceptance_criteria>
    - `cd plugins/logingrupa/metapixelshopaholic && composer pint-test; echo $?` outputs 0 on the final line
    - `cd plugins/logingrupa/metapixelshopaholic && composer analyse; echo $?` outputs 0 on the final line (0 errors after PHPDoc narrow fix OR baseline regen)
    - `cd plugins/logingrupa/metapixelshopaholic && composer phpmd; echo $?` outputs 0
    - `cd plugins/logingrupa/metapixelshopaholic && composer test; echo $?` outputs 0 (any baselined skip from PLAN-03 is honored; failure count = 0)
    - `cd plugins/logingrupa/metapixelshopaholic && composer qa; echo $?` outputs 0 (composite gate)
    - If phpstan-baseline.neon was regenerated (FALLBACK), the file diff shows only `ignoreErrors` entries for `SendCapiEvent::extractFirstEvent` and `EventLog::subject` (or those errors fully suppressed); no unrelated baseline entries added; and `classes/queue/SendCapiEvent.php` + `models/EventLog.php` show zero diff
    - If narrow PHPDoc fix path was taken (PRIMARY), `git diff` shows ≤ 5 net-added LOC across `classes/queue/SendCapiEvent.php` + `models/EventLog.php` combined (BRIEF threshold) AND phpstan-baseline.neon shows zero diff
  </acceptance_criteria>
  <commit_message>chore(03.1-cleanup): composer qa green — phpstan + pint + phpmd + pest</commit_message>
  <done>
    composer qa exits 0 end-to-end. phpstan path documented (narrow fix vs baseline regen vs no-op). Per-step exit codes recorded for SUMMARY. Atomic commit landed IF baseline or narrow fix touched files; otherwise no commit and the task records "no fix needed — qa already green from prior tracks".
  </done>
</task>

<task type="auto">
  <name>Task 2 (T5.2): git tag v1.1.1 annotated local-only</name>
  <files>(no files — git tag operation)</files>
  <read_first>
    - plugins/logingrupa/metapixelshopaholic/updates/version.yaml (confirm v1.1.1 ledger entry exists — per STATE.md it shipped in phase 03.1-07)
    - plugins/logingrupa/metapixelshopaholic/.planning/phases/03.1-08-dead-code-cleanup/BRIEF.md (T5.2 spec lines 277-286)
  </read_first>
  <action>
    Run from plugin dir (the plugin is its own nested git repo per STATE.md / plugin's CLAUDE.md):

        cd /home/forge/nailscosmetics.lv/plugins/logingrupa/metapixelshopaholic
        git tag -a v1.1.1 -m "Phase 3.1-07 cross-context site_id symmetry + 03.1-cleanup dead code removal"

    Verbatim tag name `v1.1.1` (lower-v + three integers). Verbatim message body per BRIEF T5.2 line 284.

    **CRITICAL — do NOT push.** No `git push origin v1.1.1` invocation. The operator decides push timing per STATE.md ("Awaiting operator: v1.1.1 deploy on .lv/.lt/.no + STAGING Scenario 5 per site"). Pushing without operator confirmation is forbidden.

    Tiger-Style + GSD safety note: git tag is locally irreversible and near-irreversible after push. After running tag command, executor MUST capture verification output from `git tag -l v1.1.1` + `git for-each-ref refs/tags/v1.1.1` before proceeding to Task 3. Push remains operator-gated (not part of this task).
  </action>
  <verify>
    <automated>cd /home/forge/nailscosmetics.lv/plugins/logingrupa/metapixelshopaholic && [[ $(git tag -l v1.1.1) == "v1.1.1" ]] && git for-each-ref refs/tags/v1.1.1 --format='%(objecttype)' | grep -q "^tag$"</automated>
  </verify>
  <acceptance_criteria>
    - `cd /home/forge/nailscosmetics.lv/plugins/logingrupa/metapixelshopaholic && git tag -l v1.1.1` outputs `v1.1.1` exactly
    - `cd /home/forge/nailscosmetics.lv/plugins/logingrupa/metapixelshopaholic && git for-each-ref refs/tags/v1.1.1 --format='%(objecttype)'` returns `tag` (annotated, not lightweight)
    - `cd /home/forge/nailscosmetics.lv/plugins/logingrupa/metapixelshopaholic && git cat-file -p v1.1.1 | grep -q "Phase 3.1-07 cross-context site_id symmetry + 03.1-cleanup dead code removal"` — tag message matches BRIEF verbatim
    - `cd /home/forge/nailscosmetics.lv/plugins/logingrupa/metapixelshopaholic && git ls-remote --tags origin v1.1.1 2>&1 | grep -c "refs/tags/v1.1.1"` returns 0 (not pushed to remote)
  </acceptance_criteria>
  <commit_message>(no commit — git tag is not a commit)</commit_message>
  <done>
    Local annotated tag v1.1.1 exists in plugin repo with BRIEF-verbatim message. NOT pushed. Operator verification confirmed.
  </done>
</task>

<task type="auto">
  <name>Task 3 (T5.3): STATE.md advance to phase-3.1-milestone-ready</name>
  <files>plugins/logingrupa/metapixelshopaholic/.planning/STATE.md</files>
  <read_first>
    - plugins/logingrupa/metapixelshopaholic/.planning/STATE.md (read frontmatter — lines 1-15 — confirm current status: `phase-3.1-cross-context-verified` and the existing `stopped_at:` narrative shape)
    - plugins/logingrupa/metapixelshopaholic/.planning/phases/03.1-08-dead-code-cleanup/BRIEF.md (T5.3 spec lines 289-297 — verbatim `stopped_at:` text)
  </read_first>
  <action>
    Edit the YAML frontmatter at top of `.planning/STATE.md`. Update two fields ONLY:

    1. `status:` → change from `phase-3.1-cross-context-verified` to `phase-3.1-milestone-ready`
    2. `stopped_at:` → replace existing value with the verbatim BRIEF T5.3 text: `"Phase 3.1 closed end-to-end. v1.1.1 + 03.1-cleanup landed. qa green. Awaiting operator: v1.1.1 deploy on .lv/.lt/.no + STAGING Scenario 5 per site + manual repair of 2 stranded rows on new.nailscosmetics.lv (or accept Pixel-miss). Next: /gsd-plan-phase 4 (Funnel Completion)."`

    Also update `last_updated:` to today's ISO-8601 timestamp (2026-05-14T...Z) and `last_activity:` to `2026-05-14`. Do NOT change other frontmatter fields (`gsd_state_version`, `milestone`, `milestone_name`, `progress.*`).

    Below the frontmatter, optionally append a new entry to the `## Session Continuity` section narrating Phase 3.1-08 closure — keep concise, mirror the existing 3.1-07 entry style. Body content other than frontmatter + (optional) session-continuity append remains byte-identical (verify via `git diff`).
  </action>
  <verify>
    <automated>cd plugins/logingrupa/metapixelshopaholic && grep -q "^status: phase-3.1-milestone-ready" .planning/STATE.md && grep -q "v1.1.1 + 03.1-cleanup landed. qa green." .planning/STATE.md && grep -q "/gsd-plan-phase 4 (Funnel Completion)" .planning/STATE.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "^status: phase-3.1-milestone-ready" plugins/logingrupa/metapixelshopaholic/.planning/STATE.md` exit 0 (frontmatter advanced)
    - `! grep -q "^status: phase-3.1-cross-context-verified" plugins/logingrupa/metapixelshopaholic/.planning/STATE.md` (old status replaced)
    - `grep -q "v1.1.1 + 03.1-cleanup landed. qa green." plugins/logingrupa/metapixelshopaholic/.planning/STATE.md` (BRIEF-verbatim narrative present)
    - `grep -q "/gsd-plan-phase 4 (Funnel Completion)" plugins/logingrupa/metapixelshopaholic/.planning/STATE.md` (next-step pointer)
    - `python3 -c "import yaml; import sys; doc = open('plugins/logingrupa/metapixelshopaholic/.planning/STATE.md').read(); fm = doc.split('---')[1]; yaml.safe_load(fm)"` exits 0 (frontmatter still valid YAML)
    - `git diff plugins/logingrupa/metapixelshopaholic/.planning/STATE.md | grep -cE '^\\+[^+]'` shows a small net-added-line count (frontmatter changes + optional session-continuity entry only — body untouched)
  </acceptance_criteria>
  <commit_message>docs(03.1-cleanup): STATE.md milestone-ready closure</commit_message>
  <done>
    STATE.md frontmatter advanced to `phase-3.1-milestone-ready`. stopped_at + last_updated + last_activity refreshed per BRIEF T5.3. Atomic commit landed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local git tag → remote git repository | Push is operator-gated (Task 2 explicitly does NOT push). Premature push exposes "claimed v1.1.1 ready" before operator-side staging verification. |
| composer qa exit code → CI / release readiness | Green qa is the only contract gate between "feature complete" and "tag-able". A green qa with hidden test skips (SKIP-BASELINE.md without rationale) is the failure mode. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03.1-08-10 | Elevation of privilege | git tag push timing | mitigate | Task 2 is type:checkpoint:human-action — executor MUST stop after `git tag -a` and confirm before Task 3. Operator decision-gate on push. |
| T-03.1-08-11 | Repudiation | phpstan baseline regeneration | mitigate | If baseline-regen path is taken in Task 1, commit body MUST document which 2 errors were baselined and why narrow fix exceeded 5 LOC. Auditable trail. |
| T-03.1-08-12 | Information disclosure | STATE.md stopped_at | accept | Public planning state; no PII / secret. BRIEF-verbatim narrative is canonical. |
</threat_model>

<verification>
After all 3 tasks land:
- `cd plugins/logingrupa/metapixelshopaholic && composer qa; echo $?` outputs 0
- `git tag -l v1.1.1` outputs `v1.1.1`
- `grep -q "^status: phase-3.1-milestone-ready" plugins/logingrupa/metapixelshopaholic/.planning/STATE.md` exit 0
- `git log --oneline | head -2` shows latest commits ending with `docs(03.1-cleanup): STATE.md milestone-ready closure` and (possibly) `chore(03.1-cleanup): composer qa green — phpstan + pint + phpmd + pest` (or `fix(03.1-cleanup): narrow PHPDoc fix...`) preceded by it; OR if Task 1 had no diff, just the STATE.md commit follows the Wave-1/2 commits.
- Final-report shape per BRIEF lines 314-333 captured in SUMMARY.md (tests delta, qa exit codes, FOLLOWUP.md inventory).
</verification>

<success_criteria>
- Track T5 (CLEAN-05) closed.
- Roadmap success criteria 1, 12, 13 satisfied (composer qa exit 0; git tag v1.1.1 present locally; STATE.md status = `phase-3.1-milestone-ready`).
- Operator-handoff narrative documented in STATE.md `stopped_at:` per BRIEF verbatim.
- v1.1.1 tag NOT pushed to remote.
</success_criteria>

<output>
After completion, create `.planning/phases/03.1-08-dead-code-cleanup/03.1-08-05-SUMMARY.md` AND the canonical phase-level final report (BRIEF lines 314-333 shape) at `.planning/phases/03.1-08-dead-code-cleanup/CLEANUP-COMPLETE.md`. The CLEANUP-COMPLETE.md aggregates results from PLANs 01..05:

```
## Cleanup Complete

| Track | Items | Commits | Status |
|---|---|---|---|
| T1 prod | 3 | <list 3 SHAs> | ✓ |
| T2 tests | 5 | <list 5 SHAs> | ✓ |
| T3 fixes | 6 | <list 6 SHAs> | ✓ |
| T4 docs | 3 | <list 3 SHAs> | ✓ |
| T5 close | 2-3 | <list SHAs> | ✓ qa green, tagged |

### Tests delta
- Before: 171 passed / 6 failed
- After: 177 passed / 0 failed (OR N passed / 0 failed with SKIP-BASELINE.md documenting baseline cases)

### qa exit codes
- pint-test: 0 | analyse: 0 | phpmd: 0 | test: 0 | qa (composite): 0

### Followup deferred
<items added to FOLLOWUP.md, if any>
```
</output>
