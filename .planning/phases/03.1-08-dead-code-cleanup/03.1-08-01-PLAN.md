---
phase: 03.1-08-dead-code-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - plugins/logingrupa/metapixelshopaholic/models/EventLog.php
  - plugins/logingrupa/metapixelshopaholic/classes/helper/EventLogWriter.php
  - plugins/logingrupa/metapixelshopaholic/classes/helper/SiteResolver.php
autonomous: true
requirements:
  - CLEAN-01
tags:
  - cleanup
  - dead-code
  - tiger-style
  - hungarian
  - shopaholic
  - phase-3.1
must_haves:
  truths:
    - "models/EventLog.php class-level docblock + property-level docblock reflects REFAC-13 caller-supplied site_id contract (no stale SiteResolver::getActiveSiteId() narrative)"
    - "classes/helper/EventLogWriter::record 7th parameter is required ?int $iSiteId (no `= null` default) OR carries @deprecated null default — required in Phase 4 PHPDoc when audit shows >3 implicit-null callers"
    - "classes/helper/SiteResolver.php class-level PHPDoc compressed to ≤ 10 lines preserving @see REFAC-04 + REFAC-12 anchors"
    - "composer test from plugin dir does not regress (still 171 passed / 6 failed baseline OR better — never worse)"
  artifacts:
    - path: "plugins/logingrupa/metapixelshopaholic/models/EventLog.php"
      provides: "EventLog Eloquent model with REFAC-13 docblock"
      contains: "caller-supplied"
    - path: "plugins/logingrupa/metapixelshopaholic/classes/helper/EventLogWriter.php"
      provides: "Race-fence writer with required ?int $iSiteId 7th param (preferred) OR @deprecated annotation"
      contains: "?int $iSiteId"
    - path: "plugins/logingrupa/metapixelshopaholic/classes/helper/SiteResolver.php"
      provides: "Order-scoped + active-site resolver with compact class PHPDoc"
      contains: "@see"
  key_links:
    - from: "classes/helper/EventLogWriter.php"
      to: "OrderStatusWatcher + SendCapiEvent + PurchasePixel callers"
      via: "?int $iSiteId 7th parameter contract (DRY — writer pure I/O)"
      pattern: "EventLogWriter::record"
    - from: "models/EventLog.php docblock"
      to: "REFAC-13 contract documented in STATE.md"
      via: "PHPDoc narrative"
      pattern: "REFAC-13"
---

<objective>
Track T1 — production-code dead/stale cleanup. Resolve phase 03.1-07 REVIEW.md findings MED-01 (EventLog stale docblock), MED-02 (EventLogWriter 7th param null default — fail-fast hardening), LOW-06 (SiteResolver PHPDoc bloat). Three atomic commits, each addressing one BRIEF item.

Purpose: phase 03.1-07 REFAC-12/13 reshaped the writer/reader contract (SiteResolver::forOrder canonical; writer takes explicit `?int $iSiteId`). Production-code docblocks + signatures must reflect that contract before v1.1.1 is tagged. Otherwise Phase 4 contributors get misled by the stale narrative.

Output: 3 commits with `03.1-cleanup` scope on `models/EventLog.php`, `classes/helper/EventLogWriter.php`, `classes/helper/SiteResolver.php`. No test files touched (BRIEF Constraint #5 — Track 1 owns all production-code edits; Tracks 2-4 do not touch production).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@plugins/logingrupa/metapixelshopaholic/CLAUDE.md
@plugins/logingrupa/metapixelshopaholic/.planning/STATE.md
@plugins/logingrupa/metapixelshopaholic/.planning/ROADMAP.md
@plugins/logingrupa/metapixelshopaholic/.planning/REQUIREMENTS.md
@plugins/logingrupa/metapixelshopaholic/.planning/phases/03.1-08-dead-code-cleanup/BRIEF.md
@plugins/logingrupa/metapixelshopaholic/.planning/phases/03.1-07-multi-site-site-id-symmetry/03.1-07-REVIEW.md
@plugins/logingrupa/metapixelshopaholic/.planning/phases/03.1-07-multi-site-site-id-symmetry/03.1-07-SUMMARY.md

<interfaces>
<!-- Contracts the executor needs in scope before touching files -->

EventLogWriter::record signature (current, post-REFAC-13):
```
public static function record(
    string $sEventId,
    string $sEventName,
    string $sChannel,
    object $obSubject,
    ?string $sSecretKey,
    int $iEventTime,
    ?int $iSiteId = null,   // ← MED-02: this default must be dropped (option A) or @deprecated (option B)
): bool
```

SiteResolver public surface (preserve verbatim):
```
public static function forOrder(\Lovata\OrdersShopaholic\Models\Order $obOrder): ?int;
public static function getActiveSiteId(): ?int;
```

EventLog model — relevant property-level/class-level docblock targets near line 45 reference site_id semantics.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1 (T1.1): Fix MED-01 — EventLog.php stale docblock</name>
  <files>plugins/logingrupa/metapixelshopaholic/models/EventLog.php</files>
  <read_first>
    - plugins/logingrupa/metapixelshopaholic/models/EventLog.php (current docblock state around line 45 and class-level header — read ENTIRE file once, extract docblock + @property/@var tags + site_id property location in single pass)
    - plugins/logingrupa/metapixelshopaholic/.planning/phases/03.1-08-dead-code-cleanup/BRIEF.md (T1.1 spec lines 38-48)
    - plugins/logingrupa/metapixelshopaholic/.planning/phases/03.1-07-multi-site-site-id-symmetry/03.1-07-REVIEW.md (MED-01 finding context — what exactly is stale)
    - plugins/logingrupa/metapixelshopaholic/.planning/phases/03.1-07-multi-site-site-id-symmetry/03.1-07-SUMMARY.md (REFAC-13 contract change record)
    - plugins/logingrupa/metapixelshopaholic/classes/helper/SiteResolver.php (verify forOrder + getActiveSiteId are both still public — narrative must reference both)
  </read_first>
  <action>
    Rewrite class-level docblock + property-level docblock around line 45 in `models/EventLog.php`. Replace the stale `SiteResolver::getActiveSiteId()` narrative with the REFAC-13 contract: (1) writer accepts explicit `?int $iSiteId` from caller (DRY — resolution at call sites), (2) Order-scoped subjects (Watcher, SendCapiEvent, PurchasePixel) use `SiteResolver::forOrder($obOrder)`, (3) future non-Order subjects (Lead, AddToCart, ViewContent — Phase 4) can use `SiteResolver::getActiveSiteId()` if request-scoped. Use caveman compression (drop articles, fragments OK) on prose; preserve every `@property`, `@var`, `@see`, `@param`, `@return` tag verbatim. Use the literal phrase `caller-supplied` in the new prose so the acceptance grep matches.
  </action>
  <verify>
    <automated>cd plugins/logingrupa/metapixelshopaholic && grep -q "caller-supplied" models/EventLog.php && ! grep -qE "SiteResolver::getActiveSiteId\(\)" models/EventLog.php && composer test -- --filter EventLogTest 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "caller-supplied" plugins/logingrupa/metapixelshopaholic/models/EventLog.php` exit 0
    - `grep -q "REFAC-13" plugins/logingrupa/metapixelshopaholic/models/EventLog.php` exit 0
    - `grep -qE "SiteResolver::getActiveSiteId\(\)" plugins/logingrupa/metapixelshopaholic/models/EventLog.php` exit NON-zero (stale narrative removed; method ref OK only inside future-Phase-4 sentence)
    - `grep -cE "^\s*\*\s*@(property|var|see)" plugins/logingrupa/metapixelshopaholic/models/EventLog.php` >= prior count (functional PHPDoc tags preserved)
    - `cd plugins/logingrupa/metapixelshopaholic && composer test -- --filter EventLogTest` exit code is identical to baseline (no regression — EventLogTest baseline carries 1 failure per T3.1; that single failure remains, all other methods green)
  </acceptance_criteria>
  <commit_message>docs(03.1-cleanup): EventLog model docblock — reflect REFAC-13 caller-supplied site_id</commit_message>
  <done>
    EventLog class-level + property-level docblocks narrate REFAC-13 caller-supplied `?int $iSiteId` contract; `@property`/`@var`/`@see` tags preserved verbatim; literal phrase "caller-supplied" + "REFAC-13" present; no stale `SiteResolver::getActiveSiteId()` narrative outside the future-Phase-4 reference; EventLogTest delta = 0 (no new failures introduced). Atomic commit landed with stated message.
  </done>
</task>

<task type="auto">
  <name>Task 2 (T1.2): Decision on MED-02 — EventLogWriter::record 7th param fail-fast vs @deprecated</name>
  <files>plugins/logingrupa/metapixelshopaholic/classes/helper/EventLogWriter.php</files>
  <read_first>
    - plugins/logingrupa/metapixelshopaholic/classes/helper/EventLogWriter.php (read ENTIRE file — signature on line ~60, method body, current @param @return tags, all in one pass)
    - plugins/logingrupa/metapixelshopaholic/.planning/phases/03.1-08-dead-code-cleanup/BRIEF.md (T1.2 spec lines 50-77 — decision tree A vs B + caller audit grep command)
    - plugins/logingrupa/metapixelshopaholic/.planning/phases/03.1-07-multi-site-site-id-symmetry/03.1-07-REVIEW.md (MED-02 finding rationale — Phase 4 silent-null cross-context bug class)
  </read_first>
  <action>
    Step 1 (audit): run `grep -rnE 'EventLogWriter::record' plugins/logingrupa/metapixelshopaholic/ --include='*.php' | grep -v 'classes/helper/EventLogWriter.php' | tee /tmp/eventlogwriter-callers.txt` and count callers. The two production callers per STATE.md REFAC-API are `SendCapiEvent::raceFenceWon` (or equivalent in handle()) and `PurchasePixel::onMarkFired`. Test files may also surface — those are call sites too. Count rows where the call does NOT pass an explicit 7th arg.

    Step 2 (decision per BRIEF tree):
    - If ≤ 3 implicit-null callers (expected — both production callers pass explicit value via `SiteResolver::forOrder($obOrder)`): pick (A). Drop the `= null` from line ~60 (7th parameter). The method signature becomes `?int $iSiteId,` with no default. Update the `@param` PHPDoc for `$iSiteId` to caveman-compressed prose noting "caller-supplied; Order-scoped via SiteResolver::forOrder, request-scoped via SiteResolver::getActiveSiteId". Any tests that pass null implicitly must be fixed in T2.x — but if test callers omit the arg, declare them as part of this task's audit and fix HERE (production-code task owns the writer contract; tests calling the writer with implicit null break the build and must be fixed atomically with the contract change).
    - If > 3 implicit-null callers surface that intentionally rely on the default: fall back to (B). Keep `= null` default but add `@deprecated null default — required in Phase 4 (REFAC-13); see classes/helper/SiteResolver::forOrder` PHPDoc tag on the `@param $iSiteId` line. Use caveman style.

    Use commit message per BRIEF: option (A) → `refactor(03.1-cleanup): EventLogWriter::record requires explicit ?int site_id (fail-fast)`. Option (B) → `docs(03.1-cleanup): EventLogWriter::record deprecate null default for site_id`.

    Preserve every `@param`, `@return`, `@throws`, `@see` tag verbatim. Function body remains unchanged. Method must remain ≤ 70 LOC per CLAUDE.md Tiger-Style.
  </action>
  <verify>
    <automated>cd plugins/logingrupa/metapixelshopaholic && grep -rnE 'EventLogWriter::record' --include='*.php' | grep -v 'classes/helper/EventLogWriter.php' | wc -l && grep -qE "(\?int \\\$iSiteId,|\\?int \\\$iSiteId\\s*\\)|@deprecated null default — required in Phase 4)" classes/helper/EventLogWriter.php && composer test -- --filter "MultiSiteEventLogTest|SendCapiEventEventLogTest" 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - Option (A): `grep -qE "\\?int \\\$iSiteId(\\s*,|\\s*\\))" plugins/logingrupa/metapixelshopaholic/classes/helper/EventLogWriter.php` exit 0 AND `! grep -qE "\\?int \\\$iSiteId\\s*=\\s*null" plugins/logingrupa/metapixelshopaholic/classes/helper/EventLogWriter.php` (default dropped)
    - Option (B): `grep -q "@deprecated null default — required in Phase 4" plugins/logingrupa/metapixelshopaholic/classes/helper/EventLogWriter.php` exit 0 (default retained, soft-migration tag present)
    - Either option: every caller of `EventLogWriter::record` outside `helper/EventLogWriter.php` passes an explicit 7th arg (`grep -rnE 'EventLogWriter::record\(' plugins/logingrupa/metapixelshopaholic/ --include='*.php' | grep -v 'classes/helper/EventLogWriter.php'` — manual inspection confirms each call site supplies the 7th param)
    - Decision recorded in commit message body: which path (A or B) was taken and the caller-count audit number
    - `composer test -- --filter "MultiSiteEventLogTest|SendCapiEventEventLogTest"` exit code is identical to baseline (no new failures; T3.6 SendCapiEventEventLogTest race-fence failure remains a T3 concern, not this task's regression)
    - Method body unchanged AND method LOC count `awk '/public static function record/,/^    \}/{print}' classes/helper/EventLogWriter.php | wc -l` ≤ 70 (Tiger-Style)
  </acceptance_criteria>
  <commit_message>refactor(03.1-cleanup): EventLogWriter::record requires explicit ?int site_id (fail-fast)</commit_message>
  <done>
    EventLogWriter::record signature reflects BRIEF decision tree outcome. Caller audit recorded in commit body. Test failures count unchanged. Functional PHPDoc tags preserved. One atomic commit landed.
  </done>
</task>

<task type="auto">
  <name>Task 3 (T1.3): LOW-06 — SiteResolver class-level PHPDoc caveman-compress</name>
  <files>plugins/logingrupa/metapixelshopaholic/classes/helper/SiteResolver.php</files>
  <read_first>
    - plugins/logingrupa/metapixelshopaholic/classes/helper/SiteResolver.php (read ENTIRE file once — extract class-level docblock lines 11-43, all method-level docblocks, every `@see` reference in single pass)
    - plugins/logingrupa/metapixelshopaholic/.planning/phases/03.1-08-dead-code-cleanup/BRIEF.md (T1.3 spec lines 79-85)
    - plugins/logingrupa/metapixelshopaholic/.planning/phases/03.1-07-multi-site-site-id-symmetry/03.1-07-REVIEW.md (LOW-06 finding)
  </read_first>
  <action>
    Compress class-level PHPDoc (lines 11-43, ~33 lines) to ≤ 10 lines covering: (a) purpose (one-line summary — "Multi-site `site_id` resolver. Two public statics: forOrder for Order-scoped writes/reads, getActiveSiteId for request-scoped non-Order subjects."), (b) two-method contract reference, (c) `@see` REFAC-04 + `@see` REFAC-12 anchors verbatim. Method-level docblocks already carry detail — do NOT touch them. Use caveman style (drop articles, fragments OK). Method bodies + signatures unchanged. Preserve every existing `@see` tag verbatim.
  </action>
  <verify>
    <automated>cd plugins/logingrupa/metapixelshopaholic && awk '/^\/\*\*/{f=1; c=0} f{c++; print} /^\s*\*\/$/ && f{f=0; print "===END BLOCK lines="c"==="; exit}' classes/helper/SiteResolver.php | head -20 && grep -qE "@see\s+.*REFAC-04" classes/helper/SiteResolver.php && grep -qE "@see\s+.*REFAC-12" classes/helper/SiteResolver.php && composer test -- --filter SiteResolverTest 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Class-level PHPDoc block (first `/**` through matching `*/` immediately preceding `class SiteResolver`) ≤ 10 lines inclusive of opening/closing markers — verifiable via `awk '/^\/\*\*/,/\*\/$/{print; if (/\*\/$/) exit}' plugins/logingrupa/metapixelshopaholic/classes/helper/SiteResolver.php | wc -l` returns ≤ 10
    - `grep -qE "@see\s+.*REFAC-04" plugins/logingrupa/metapixelshopaholic/classes/helper/SiteResolver.php` exit 0
    - `grep -qE "@see\s+.*REFAC-12" plugins/logingrupa/metapixelshopaholic/classes/helper/SiteResolver.php` exit 0
    - Method-level docblocks for `forOrder` and `getActiveSiteId` byte-identical to pre-edit state (verify via `git diff classes/helper/SiteResolver.php` — only class-level block changed)
    - `composer test -- --filter SiteResolverTest` exit code identical to baseline (zero new failures — SiteResolverTest is currently green per 03.1-07 SUMMARY)
  </acceptance_criteria>
  <commit_message>docs(03.1-cleanup): SiteResolver class-level PHPDoc — caveman compress</commit_message>
  <done>
    SiteResolver class-level PHPDoc ≤ 10 lines preserving `@see` REFAC-04 + REFAC-12 anchors. Method-level docblocks untouched. SiteResolverTest green. Atomic commit landed with stated message.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| EventLogWriter::record caller → writer | All 3 production callers (Watcher, SendCapiEvent, PurchasePixel) cross this boundary supplying `?int $iSiteId`. After (A), implicit null is rejected at PHP signature level — fail-fast. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03.1-08-01 | Tampering | EventLogWriter caller | mitigate | Option (A) drops `= null` default — Phase 4 callers cannot accidentally write `site_id NULL` rows in multi-site context (resurrects cross-context bug class fixed in 03.1-07). Fail-fast at compile/dispatch. |
| T-03.1-08-02 | Information disclosure | EventLog docblock | accept | Public docblock content — no PII, no secret. Stale narrative is documentation-quality issue, not security. |
</threat_model>

<verification>
After all 3 tasks land:
- `cd plugins/logingrupa/metapixelshopaholic && composer test 2>&1 | tail -5` — failure count must NOT exceed baseline of 6 (T3 fixes happen in PLAN-03, not here).
- `git log --oneline | head -3` shows exactly 3 commits with `03.1-cleanup` scope addressing T1.1, T1.2, T1.3 respectively.
- No file outside `models/EventLog.php`, `classes/helper/EventLogWriter.php`, `classes/helper/SiteResolver.php` modified by this plan (`git diff --stat HEAD~3 HEAD` confirms 3 files).
</verification>

<success_criteria>
- Track T1 (CLEAN-01) closed: 3 atomic commits with `03.1-cleanup` scope; commit messages match BRIEF prescriptions.
- Roadmap success criteria 3, 4, 5, 6 satisfied (EventLog docblock REFAC-13 narrative; EventLogWriter 7th param required OR @deprecated; SiteResolver PHPDoc ≤ 10 lines; @see anchors preserved).
- Zero new test failures introduced (baseline ≤ 6 failed preserved).
- Production-code-only touch (BRIEF Constraint #5) — Tracks 2-4 never blocked.
</success_criteria>

<output>
After completion, create `.planning/phases/03.1-08-dead-code-cleanup/03.1-08-01-SUMMARY.md` documenting:
- Decision recorded in T1.2 (option A or B + caller audit count)
- Line-count delta on SiteResolver class-level PHPDoc (e.g., "33 → 9 lines")
- Test-failure count unchanged (baseline preserved)
- 3 commit SHAs
</output>
