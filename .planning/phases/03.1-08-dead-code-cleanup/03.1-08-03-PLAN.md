---
phase: 03.1-08-dead-code-cleanup
plan: 03
type: execute
wave: 2
depends_on:
  - 01
files_modified:
  - plugins/logingrupa/metapixelshopaholic/tests/Feature/EventLogTest.php
  - plugins/logingrupa/metapixelshopaholic/tests/Feature/ExceptionHierarchyTest.php
  - plugins/logingrupa/metapixelshopaholic/tests/MetapixelTestCase.php
  - plugins/logingrupa/metapixelshopaholic/tests/Feature/BootsWithoutPixelIdTest.php
  - plugins/logingrupa/metapixelshopaholic/tests/Feature/EnsureFbpFbcCookiesTest.php
  - plugins/logingrupa/metapixelshopaholic/tests/Feature/PurchasePixelEventLogGateTest.php
  - plugins/logingrupa/metapixelshopaholic/tests/Feature/SendCapiEventEventLogTest.php
  - plugins/logingrupa/metapixelshopaholic/models/Settings.php
  - plugins/logingrupa/metapixelshopaholic/middleware/EnsureFbpFbcCookies.php
  - plugins/logingrupa/metapixelshopaholic/components/PurchasePixel.php
  - plugins/logingrupa/metapixelshopaholic/classes/queue/SendCapiEvent.php
  - plugins/logingrupa/metapixelshopaholic/lang/en/lang.php  # only if key audit shows missing — conditional (T3.2 primary path)
  - plugins/logingrupa/metapixelshopaholic/tests/SKIP-BASELINE.md
autonomous: true
requirements:
  - CLEAN-03
tags:
  - cleanup
  - test-failures
  - baseline
  - tiger-style
  - phase-3.1
must_haves:
  truths:
    - "Each of the 6 pre-existing test failures has either landed a PRIMARY fix (test exits 0 under filter) OR a FALLBACK baseline (test method carries `->skip('baselined: <reason>')` AND tests/SKIP-BASELINE.md has matching entry)"
    - "After all 6 T3 tasks land: `composer test` failure count = 0 (full green) OR all remaining failures appear in tests/SKIP-BASELINE.md with documented rationale"
    - "tests/SKIP-BASELINE.md exists at the listed path with one section per baselined test (entries created only when fallback path taken)"
    - "Production-code edits (Settings::isDisabled, EnsureFbpFbcCookies middleware, PurchasePixel::onMarkFired, SendCapiEvent::handle) confined to single-concern surgical changes per T3.x — no drive-by refactors (BRIEF Constraint #1)"
  artifacts:
    - path: "plugins/logingrupa/metapixelshopaholic/tests/SKIP-BASELINE.md"
      provides: "Inventory of formally baselined tests with rationale per BRIEF T3 fallback path"
      contains: "baselined"
    - path: "plugins/logingrupa/metapixelshopaholic/tests/MetapixelTestCase.php"
      provides: "Optional bootTranslations() hook registering plugin lang namespace (T3.2 primary fix)"
      contains: "addNamespace"
  key_links:
    - from: "tests/MetapixelTestCase.php bootTranslations()"
      to: "lang/en/lang.php plugin namespace"
      via: "App['translator']->addNamespace('logingrupa.metapixelshopaholic', __DIR__.'/../lang')"
      pattern: "addNamespace"
    - from: "models/Settings.php isDisabled()"
      to: "pixel_id Settings field"
      via: "field-read returning false when populated, true when empty"
      pattern: "isDisabled"
---

<objective>
Track T3 — six pre-existing test failures from phases 03.1-01..06 lingering as "baseline" through 03.1-07. Each one gets a 30-minute-bounded primary fix attempt (per BRIEF tree) with a formal skip-baseline fallback. After all 6 land: `composer test` failure count = 0 (full green) OR every remaining failure has a SKIP-BASELINE.md entry. Total of 6 atomic commits.

Purpose: phase v1.1.1 cannot claim "qa green" until either fix or formally baseline. BRIEF Constraint #1 forbids drive-by refactors — each fix is surgical, scoped to the single failing assertion + the minimal production-code or test-code change needed. Fallback path exists to honor the time-bound (BRIEF Constraint: "no scope creep") — formally baselining beats silent red.

Output: 6 commits (some `fix(03.1-cleanup):` production, some `test(03.1-cleanup):` test-only, some `chore(03.1-cleanup):` baseline entries depending on which path each task takes). Some tasks may produce production-code commits (Settings, middleware, PurchasePixel, SendCapiEvent), which is the ONE narrow exception to "Track 1 owns all production-code edits" — T3 by BRIEF design may touch production-code where the failure is in production-code (BRIEF lines 144-216 acknowledge this).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@plugins/logingrupa/metapixelshopaholic/CLAUDE.md
@plugins/logingrupa/metapixelshopaholic/.planning/STATE.md
@plugins/logingrupa/metapixelshopaholic/.planning/phases/03.1-08-dead-code-cleanup/BRIEF.md
@plugins/logingrupa/metapixelshopaholic/.planning/phases/03.1-event-log-refactor/03.1-06-SUMMARY.md
@plugins/logingrupa/metapixelshopaholic/.planning/phases/03.1-07-multi-site-site-id-symmetry/03.1-07-SUMMARY.md
@plugins/logingrupa/metapixelshopaholic/.planning/phases/03.1-08-dead-code-cleanup/03.1-08-01-PLAN.md

<interfaces>
<!-- Wave-2 dependency: PLAN-01 may have made EventLogWriter::record 7th param required.
     T3.6 (SendCapiEvent race-fence) must observe whatever signature shape T1.2 chose.
     Re-read EventLogWriter.php BEFORE attempting T3.6. -->

T3 universal escape hatch (per BRIEF lines 214-216):
```
$this->skip('baselined: <reason — file:line in SKIP-BASELINE.md>');
```
Inserted at the top of the failing test method. Append matching entry to `tests/SKIP-BASELINE.md` (create file if absent — first task to baseline owns the create).

SKIP-BASELINE.md row template:
```
## TestClassName::test_method_name

- **Phase:** 03.1-08
- **Reason:** <one-paragraph caveman explanation — why the primary fix exceeded 30-min bound or introduced drive-by refactors>
- **Baselined:** 2026-05-14
- **Resume condition:** <what would justify revisiting — e.g., "Phase 4 funnel-event scaffolding rewrites the parent component">
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1 (T3.1): EventLogTest event_id >36-char validation rejection</name>
  <files>plugins/logingrupa/metapixelshopaholic/tests/Feature/EventLogTest.php</files>
  <read_first>
    - plugins/logingrupa/metapixelshopaholic/tests/Feature/EventLogTest.php (read ENTIRE file — locate `test_event_id_validation_rejects_longer_than_36`, identify the current assertion shape, the `Model::save()` or `Model::validate()` call site, and the imports block in one pass)
    - plugins/logingrupa/metapixelshopaholic/models/EventLog.php (read once — confirm October Rain Validation trait usage, `public $rules` array contents, `public $throwOnValidation` setting if any)
    - plugins/logingrupa/metapixelshopaholic/.planning/phases/03.1-08-dead-code-cleanup/BRIEF.md (T3.1 spec lines 150-160 — prefer ModelException expectation per October Rain contract)
    - plugins/logingrupa/metapixelshopaholic/.planning/phases/03.1-event-log-refactor/03.1-06-SUMMARY.md (baseline context)
  </read_first>
  <action>
    PRIMARY (BRIEF prescribed): update test assertion to use `$this->expectException(\October\Rain\Database\ModelException::class)` (or whatever the actual ModelException FQN is — confirm via `grep -r 'class ModelException' vendor/october modules/system 2>/dev/null | head -3`) and assert the exception message contains `"must not be greater than 36 characters"`. Add `use` import for ModelException at top. Remove any current `assertFalse(...)` / `assertNull(...)` shape that expects a return-value rather than throw.

    30-min timer: if ModelException FQN turns out to be ambiguous OR the model uses a non-standard validation path that the test cannot easily target, STOP and switch to FALLBACK: add `$this->markTestSkipped('baselined: see tests/SKIP-BASELINE.md#EventLogTesttestevent_id_validation_rejects_longer_than_36')` at top of method AND append a SKIP-BASELINE.md entry. (Create tests/SKIP-BASELINE.md if absent — header `# SKIP-BASELINE — formally baselined tests in Phase 03.1-08`.)

    Commit message PRIMARY: `test(03.1-cleanup): EventLogTest expectException ModelException for >36 char event_id`
    Commit message FALLBACK: `test(03.1-cleanup): EventLogTest baseline event_id >36 validation — see SKIP-BASELINE.md`
  </action>
  <verify>
    <automated>cd plugins/logingrupa/metapixelshopaholic && composer test -- --filter "EventLogTest::test_event_id_validation_rejects_longer_than_36" 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - EITHER `composer test -- --filter "EventLogTest::test_event_id_validation_rejects_longer_than_36"` exits 0 (PRIMARY) — and the test method now uses `expectException(ModelException::class)` per `grep -A 5 "test_event_id_validation_rejects_longer_than_36" tests/Feature/EventLogTest.php | grep -q "expectException.*ModelException"`
    - OR `grep -q "baselined" plugins/logingrupa/metapixelshopaholic/tests/SKIP-BASELINE.md` AND `grep -A 2 "test_event_id_validation_rejects_longer_than_36" plugins/logingrupa/metapixelshopaholic/tests/Feature/EventLogTest.php | grep -q "markTestSkipped.*baselined"` (FALLBACK)
    - Whichever path: `composer test -- --filter "EventLogTest"` failure count drops by 1 from baseline (the targeted method no longer counts as a failure — either passes or is formally skipped)
  </acceptance_criteria>
  <commit_message>test(03.1-cleanup): EventLogTest expectException ModelException for >36 char event_id</commit_message>
  <done>
    Either primary fix landed (ModelException expected) OR baselined entry recorded. Filter test no longer counts as failure. Atomic commit landed with prescribed message.
  </done>
</task>

<task type="auto">
  <name>Task 2 (T3.2): ExceptionHierarchyTest translation namespace boot</name>
  <files>
    plugins/logingrupa/metapixelshopaholic/tests/MetapixelTestCase.php,
    plugins/logingrupa/metapixelshopaholic/tests/Feature/ExceptionHierarchyTest.php,
    plugins/logingrupa/metapixelshopaholic/lang/en/lang.php (conditional — only if read_first audit shows key missing)
  </files>
  <read_first>
    - plugins/logingrupa/metapixelshopaholic/tests/MetapixelTestCase.php (read ENTIRE file — locate setUp/bootSystemSettings, identify whether a bootTranslations hook already exists, the test-case parent constructor flow, all in one pass)
    - plugins/logingrupa/metapixelshopaholic/tests/Feature/ExceptionHierarchyTest.php (read assertions referencing `logingrupa.metapixelshopaholic::lang.exception.missing_pixel_config` — confirm the literal translation key)
    - plugins/logingrupa/metapixelshopaholic/lang/en/lang.php (verify key `exception.missing_pixel_config` exists — `grep -A 3 "missing_pixel_config" lang/en/lang.php`)
    - plugins/logingrupa/metapixelshopaholic/.planning/phases/03.1-08-dead-code-cleanup/BRIEF.md (T3.2 spec lines 162-170)
  </read_first>
  <action>
    PRIMARY: add a `bootTranslations(): void` protected method to `tests/MetapixelTestCase.php` that calls `$this->app['translator']->addNamespace('logingrupa.metapixelshopaholic', __DIR__.'/../lang');`. Call it from `setUp()` immediately after the existing `bootSystemSettings()` (or equivalent) call. Verify `lang/en/lang.php` has the key `'exception' => ['missing_pixel_config' => '...']` per the read_first audit. If the key is missing → add it to `lang/en/lang.php` with caveman-compressed English text (e.g., `'missing_pixel_config' => 'Pixel ID not configured — plugin disabled'`) — this is the ONLY scenario in which `lang/en/lang.php` is touched (the file is listed conditionally in `files_modified`). If key exists in lang/lv + lang/ru also, leave those alone (Phase 5 HARD-04 owns full translation).

    30-min timer: if `App['translator']->addNamespace` raises a runtime error in the hermetic test harness (October's translator service may not be bound at test boot time), STOP and fall back to skip. Append SKIP-BASELINE.md entry with rationale "translator service binding incompatible with hermetic test harness — Phase 5 HARD-04 owns translation infra".

    Commit message PRIMARY: `fix(03.1-cleanup): MetapixelTestCase bootTranslations registers plugin lang namespace`
    Commit message FALLBACK: `test(03.1-cleanup): ExceptionHierarchyTest baseline translation namespace — see SKIP-BASELINE.md`
  </action>
  <verify>
    <automated>cd plugins/logingrupa/metapixelshopaholic && composer test -- --filter ExceptionHierarchyTest 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - EITHER `composer test -- --filter ExceptionHierarchyTest` exits 0 (PRIMARY) AND `grep -q "bootTranslations\\|addNamespace" plugins/logingrupa/metapixelshopaholic/tests/MetapixelTestCase.php` exit 0
    - OR `grep -q "ExceptionHierarchy" plugins/logingrupa/metapixelshopaholic/tests/SKIP-BASELINE.md` AND `grep -A 2 "ExceptionHierarchy" plugins/logingrupa/metapixelshopaholic/tests/Feature/ExceptionHierarchyTest.php | grep -q "markTestSkipped.*baselined"` (FALLBACK)
    - MetapixelTestCase.php method count unchanged or +1 (only `bootTranslations` added in primary path; nothing else touched — Tiger-Style SRP)
    - IF `lang/en/lang.php` was touched (audit showed missing key), `git diff plugins/logingrupa/metapixelshopaholic/lang/en/lang.php` shows ONLY a single key addition under `'exception'` — no other keys altered
  </acceptance_criteria>
  <commit_message>fix(03.1-cleanup): MetapixelTestCase bootTranslations registers plugin lang namespace</commit_message>
  <done>
    Either primary fix (bootTranslations hook) landed OR baselined. Filter test no longer counts as failure. Atomic commit landed.
  </done>
</task>

<task type="auto">
  <name>Task 3 (T3.3): BootsWithoutPixelIdTest::test_isdisabled_returns_false_when_pixel_id_populated</name>
  <files>
    plugins/logingrupa/metapixelshopaholic/models/Settings.php,
    plugins/logingrupa/metapixelshopaholic/tests/Feature/BootsWithoutPixelIdTest.php
  </files>
  <read_first>
    - plugins/logingrupa/metapixelshopaholic/models/Settings.php (read ENTIRE file — find `isDisabled()` impl, identify which field it reads, confirm `clearInternalCache()` is callable, in one pass)
    - plugins/logingrupa/metapixelshopaholic/tests/Feature/BootsWithoutPixelIdTest.php (read the failing test method + setUp — confirm what value `pixel_id` is being set to + the assertion shape)
    - plugins/logingrupa/metapixelshopaholic/.planning/phases/03.1-08-dead-code-cleanup/BRIEF.md (T3.3 spec lines 172-182)
  </read_first>
  <action>
    DIAGNOSIS: per BRIEF, the failure has two possible root causes — (a) `isDisabled()` impl checks the wrong field (e.g., reads `kill_switch` instead of `pixel_id`); (b) Settings cache stale (test sets pixel_id but the cached singleton returns null).

    Read `Settings::isDisabled()` in `models/Settings.php`. If it reads a field other than `pixel_id`, that's the bug — fix the impl: `public function isDisabled(): bool { return empty($this->pixel_id); }` (or use `Settings::get('pixel_id')` if static-context). Use caveman comment style on any new comments.

    If impl is correct (reads `pixel_id`), then the bug is cache-staleness — fix the TEST setUp to call `Settings::clearInternalCache()` after writing pixel_id and before asserting isDisabled.

    30-min timer: if neither root cause clearly resolves the failure, fall back to skip + SKIP-BASELINE.md entry with rationale.

    Commit message (production-fix): `fix(03.1-cleanup): Settings::isDisabled return false when pixel_id populated`
    Commit message (test-fix): `test(03.1-cleanup): BootsWithoutPixelIdTest clear Settings cache before isDisabled assertion`
    Commit message (FALLBACK): `test(03.1-cleanup): BootsWithoutPixelIdTest baseline isDisabled inversion — see SKIP-BASELINE.md`
  </action>
  <verify>
    <automated>cd plugins/logingrupa/metapixelshopaholic && composer test -- --filter "BootsWithoutPixelIdTest::test_isdisabled_returns_false_when_pixel_id_populated" 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - EITHER filter test exits 0 AND `grep -A 5 "function isDisabled" plugins/logingrupa/metapixelshopaholic/models/Settings.php | grep -qE "empty.*pixel_id|pixel_id.*empty"` (production fix) OR `grep -q "clearInternalCache" plugins/logingrupa/metapixelshopaholic/tests/Feature/BootsWithoutPixelIdTest.php` (test fix)
    - OR `grep -q "BootsWithoutPixelIdTest" plugins/logingrupa/metapixelshopaholic/tests/SKIP-BASELINE.md` AND test method carries `markTestSkipped('baselined: ...')` (FALLBACK)
    - Production-code edit (if production fix path taken): only `models/Settings.php::isDisabled()` body changed; method LOC ≤ 70; surrounding methods byte-identical via `git diff models/Settings.php`
  </acceptance_criteria>
  <commit_message>fix(03.1-cleanup): Settings::isDisabled return false when pixel_id populated</commit_message>
  <done>
    Either production-fix OR test-fix OR baseline landed per audit outcome. Filter test no longer counts as failure. Atomic commit landed with appropriate message variant.
  </done>
</task>

<task type="auto">
  <name>Task 4 (T3.4): EnsureFbpFbcCookiesTest toggle-OFF short-circuit guard</name>
  <files>
    plugins/logingrupa/metapixelshopaholic/middleware/EnsureFbpFbcCookies.php,
    plugins/logingrupa/metapixelshopaholic/tests/Feature/EnsureFbpFbcCookiesTest.php
  </files>
  <read_first>
    - plugins/logingrupa/metapixelshopaholic/middleware/EnsureFbpFbcCookies.php (read ENTIRE file — find `handle()` body, identify whether a feature-flag guard exists for `ensure_fbp_fbc_server_side` Settings field)
    - plugins/logingrupa/metapixelshopaholic/tests/Feature/EnsureFbpFbcCookiesTest.php (read the failing test method asserting cookie behavior with toggle OFF — confirm setUp/teardown of Settings flag)
    - plugins/logingrupa/metapixelshopaholic/models/Settings.php (confirm `ensure_fbp_fbc_server_side` field exists and is readable via `Settings::get('ensure_fbp_fbc_server_side')`)
    - plugins/logingrupa/metapixelshopaholic/.planning/phases/03.1-08-dead-code-cleanup/BRIEF.md (T3.4 spec lines 184-192)
  </read_first>
  <action>
    PRIMARY: if `middleware/EnsureFbpFbcCookies::handle()` does NOT have a feature-flag short-circuit at the top of the method, ADD it as the first line of the method body: `if (!Settings::get('ensure_fbp_fbc_server_side')) { return $next($request); }`. Use caveman comment style: `// flag OFF — middleware no-op` if a comment is needed (preferred: no comment, code self-documents). If the guard ALREADY exists, the bug is in the test (it's not actually setting the flag to OFF before the assertion) — fix the test setUp to write `Settings::set('ensure_fbp_fbc_server_side', false)` or equivalent.

    30-min timer: fallback to skip + SKIP-BASELINE.md if neither path resolves cleanly.

    Commit message (production-fix): `fix(03.1-cleanup): EnsureFbpFbcCookies middleware short-circuits when toggle OFF`
    Commit message (test-fix): `test(03.1-cleanup): EnsureFbpFbcCookiesTest set toggle OFF in setUp`
    Commit message (FALLBACK): `test(03.1-cleanup): EnsureFbpFbcCookiesTest baseline toggle-OFF guard — see SKIP-BASELINE.md`
  </action>
  <verify>
    <automated>cd plugins/logingrupa/metapixelshopaholic && composer test -- --filter EnsureFbpFbcCookiesTest 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - EITHER `composer test -- --filter EnsureFbpFbcCookiesTest` exits 0 AND `grep -qE "ensure_fbp_fbc_server_side.*return \\\$next|!.*Settings::get.*ensure_fbp_fbc" plugins/logingrupa/metapixelshopaholic/middleware/EnsureFbpFbcCookies.php` (production guard present)
    - OR test-fix path: `grep -q "ensure_fbp_fbc_server_side.*false\\|Settings::set.*ensure_fbp" plugins/logingrupa/metapixelshopaholic/tests/Feature/EnsureFbpFbcCookiesTest.php` (test correctly sets toggle OFF)
    - OR FALLBACK: `grep -q "EnsureFbpFbcCookies" plugins/logingrupa/metapixelshopaholic/tests/SKIP-BASELINE.md` AND test method skipped
    - Middleware `handle()` method LOC ≤ 70 (Tiger-Style)
  </acceptance_criteria>
  <commit_message>fix(03.1-cleanup): EnsureFbpFbcCookies middleware short-circuits when toggle OFF</commit_message>
  <done>
    Toggle-OFF path produces cookie-set no-op (or test correctly exercises that path, or baseline entry). Filter test no longer counts as failure. Atomic commit landed.
  </done>
</task>

<task type="auto">
  <name>Task 5 (T3.5): PurchasePixelEventLogGateTest race-loser return shape</name>
  <files>
    plugins/logingrupa/metapixelshopaholic/components/PurchasePixel.php,
    plugins/logingrupa/metapixelshopaholic/tests/Feature/PurchasePixelEventLogGateTest.php
  </files>
  <read_first>
    - plugins/logingrupa/metapixelshopaholic/components/PurchasePixel.php (read ENTIRE file — find `onMarkFired(): array` body, identify race-winner vs race-loser branches, confirm current return-array keys at each branch)
    - plugins/logingrupa/metapixelshopaholic/tests/Feature/PurchasePixelEventLogGateTest.php (read `test_onmarkfired_second_call_returns_ok_true_no_duplicate` — extract expected return shape `['ok' => true, 'won_race' => false]`)
    - plugins/logingrupa/metapixelshopaholic/classes/helper/EventLogWriter.php (the writer return values are the contract — true = race-winner, false = race-loser)
    - plugins/logingrupa/metapixelshopaholic/.planning/phases/03.1-08-dead-code-cleanup/BRIEF.md (T3.5 spec lines 194-202)
    - plugins/logingrupa/metapixelshopaholic/.planning/phases/03.1-08-dead-code-cleanup/03.1-08-01-PLAN.md (T1.2 may have changed EventLogWriter signature — confirm shape before editing PurchasePixel)
  </read_first>
  <action>
    PRIMARY: locate the branch in `PurchasePixel::onMarkFired` that handles `EventLogWriter::record(...) === false` (race-loser path — row already exists). Per BRIEF: this branch MUST return `['ok' => true, 'won_race' => false]` (success-for-caller idempotency — frontend treats this as a no-op, no re-fire). If the impl currently returns `['ok' => false, ...]` or any other shape, change it to the expected shape.

    Use caveman comment if helpful: `// race-loser: row exists — frontend treats as success (no re-fire)`. Method LOC ≤ 70.

    30-min timer: if the impl's branching is non-trivial to rework without drive-by refactor, fall back to skip + SKIP-BASELINE.md.

    Commit message (production-fix): `fix(03.1-cleanup): PurchasePixel onMarkFired race-loser returns ok=true won_race=false`
    Commit message (FALLBACK): `test(03.1-cleanup): PurchasePixelEventLogGateTest baseline race-loser shape — see SKIP-BASELINE.md`
  </action>
  <verify>
    <automated>cd plugins/logingrupa/metapixelshopaholic && composer test -- --filter "PurchasePixelEventLogGateTest::test_onmarkfired_second_call_returns_ok_true_no_duplicate" 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - EITHER filter test exits 0 AND `grep -A 20 "function onMarkFired" plugins/logingrupa/metapixelshopaholic/components/PurchasePixel.php | grep -qE "'ok'\\s*=>\\s*true.*'won_race'\\s*=>\\s*false|'won_race'\\s*=>\\s*false.*'ok'\\s*=>\\s*true"` (race-loser shape present in impl)
    - OR FALLBACK: SKIP-BASELINE.md entry + test method skipped
    - `components/PurchasePixel.php::onMarkFired()` method LOC ≤ 70 post-edit
  </acceptance_criteria>
  <commit_message>fix(03.1-cleanup): PurchasePixel onMarkFired race-loser returns ok=true won_race=false</commit_message>
  <done>
    Either race-loser branch returns `['ok' => true, 'won_race' => false]` OR baselined. Filter test no longer counts as failure. Atomic commit landed.
  </done>
</task>

<task type="auto">
  <name>Task 6 (T3.6): SendCapiEventEventLogTest race-fence ordering (skip HTTP POST when fence lost)</name>
  <files>
    plugins/logingrupa/metapixelshopaholic/classes/queue/SendCapiEvent.php,
    plugins/logingrupa/metapixelshopaholic/tests/Feature/SendCapiEventEventLogTest.php
  </files>
  <read_first>
    - plugins/logingrupa/metapixelshopaholic/classes/queue/SendCapiEvent.php (read ENTIRE file — locate `handle()` body, identify exact ordering of `EventLogWriter::record(...)` call vs the `MetaClient::send(...)` / HTTP POST call, confirm whether the writer-result is checked before the dispatch)
    - plugins/logingrupa/metapixelshopaholic/classes/helper/EventLogWriter.php (note: T1.2 in PLAN-01 may have made the 7th param required — confirm current signature shape post-Wave-1)
    - plugins/logingrupa/metapixelshopaholic/tests/Feature/SendCapiEventEventLogTest.php (read `test_second_concurrent_dispatch_returns_false_no_http_post` — extract the dispatch sequence + the assertion that NO HTTP POST happens on the second dispatch)
    - plugins/logingrupa/metapixelshopaholic/.planning/phases/03.1-08-dead-code-cleanup/BRIEF.md (T3.6 spec lines 204-216)
    - plugins/logingrupa/metapixelshopaholic/.planning/phases/03.1-08-dead-code-cleanup/03.1-08-01-PLAN.md (T1.2 EventLogWriter signature outcome)
  </read_first>
  <action>
    DIAGNOSIS: per BRIEF the race-fence must check `EventLogWriter::record(...)` return BEFORE `MetaClient::send(...)` / HTTP POST. If writer returns false (row exists / UNIQUE blocked), `handle()` must short-circuit without HTTP POST.

    Step 1: read `SendCapiEvent::handle()` body carefully. Find the line that calls `EventLogWriter::record(...)`. Confirm whether it's captured into a `$bWon` local. Find the line that calls `MetaClient::send($this->arPayload)` (or `$obClient->post(...)`).

    Step 2: if writer call is AFTER the HTTP POST, reorder — race-fence INSERT goes first, capture result, then `if ($bWon === false) { return; }` short-circuit, then HTTP POST. If writer call is BEFORE but result is ignored, add `if ($bWon === false) { Log::info('meta_pixel.race_fence_lost', ['event_id' => $this->arPayload[...]]); return; }`.

    Step 3: if ordering is already correct AND result is honored AND the test still fails, the bug is downstream (UNIQUE idx not applied in SQLite test fixture, or `INSERT IGNORE` semantics broken on SQLite). In that branch — fall back to SKIP-BASELINE.md with rationale "SQLite `insertOrIgnore` race-fence semantics diverge from MySQL — staging verification covers prod path; baselined pending Phase 5 test-infra parity work".

    Use caveman comment style. Method LOC ≤ 70 (Tiger-Style — if SendCapiEvent::handle is already at the limit, extract a private helper `private function raceFenceWon(): bool` rather than inflating handle()).

    Commit message PRIMARY: `fix(03.1-cleanup): SendCapiEvent::handle skip HTTP POST when race fence lost`
    Commit message FALLBACK: `test(03.1-cleanup): SendCapiEventEventLogTest baseline race-fence ordering — see SKIP-BASELINE.md`
  </action>
  <verify>
    <automated>cd plugins/logingrupa/metapixelshopaholic && composer test -- --filter "SendCapiEventEventLogTest::test_second_concurrent_dispatch_returns_false_no_http_post" 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - EITHER filter test exits 0 AND `grep -A 30 "function handle" plugins/logingrupa/metapixelshopaholic/classes/queue/SendCapiEvent.php | awk '/EventLogWriter::record/{wfound=NR} /MetaClient::send|->post\\(/{if(wfound && NR>wfound) {print "writer-first: OK"; exit 0}}'` confirms writer call precedes HTTP send
    - OR FALLBACK: `grep -q "SendCapiEventEventLogTest" plugins/logingrupa/metapixelshopaholic/tests/SKIP-BASELINE.md` AND test method carries `markTestSkipped('baselined: ...')`
    - `classes/queue/SendCapiEvent.php::handle()` method LOC ≤ 70 post-edit (extract helper if needed)
    - After all 6 T3 tasks land: `cd plugins/logingrupa/metapixelshopaholic && composer test 2>&1 | tail -3` reports failure count = 0 (full green) OR every remaining failure is documented in SKIP-BASELINE.md
  </acceptance_criteria>
  <commit_message>fix(03.1-cleanup): SendCapiEvent::handle skip HTTP POST when race fence lost</commit_message>
  <done>
    Either race-fence ordering fixed (writer-first, HTTP POST gated on `$bWon === true`) OR baselined. Filter test no longer counts as failure. After this task: full T3 closure — `composer test` failure count = 0 OR every remaining failure recorded in SKIP-BASELINE.md. Atomic commit landed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SendCapiEvent::handle() → MetaClient HTTP POST | Race-loser path must NOT cross this boundary (T-3.1-08-05). Writer-first contract is the security gate against duplicate Meta events. |
| Settings::isDisabled() → boot pipeline | False-negative from this method = plugin runs with no pixel_id, downstream NullPointer cascades (T-3.1-08-06). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03.1-08-05 | Spoofing / Duplication | SendCapiEvent::handle race-fence | mitigate | T3.6 enforces writer-first ordering — race-loser short-circuits before HTTP POST. Prevents duplicate CAPI events on Meta side. |
| T-03.1-08-06 | Denial of service | Settings::isDisabled inversion | mitigate | T3.3 corrects the predicate so disabled-flag flows correctly through PluginGuard short-circuit chain. Prevents cascading null-deref in event handlers. |
| T-03.1-08-07 | Information disclosure | EnsureFbpFbcCookies toggle-OFF | mitigate | T3.4 adds the feature-flag guard — operator turning toggle OFF must produce zero `_fbp`/`_fbc` writes. Prevents tracking-cookie leak when operator opts out. |
</threat_model>

<verification>
After all 6 tasks land:
- `cd plugins/logingrupa/metapixelshopaholic && composer test 2>&1 | tail -3` — failure count is 0 OR every remaining failure has a SKIP-BASELINE.md entry.
- `git log --oneline | head -7` shows 6 commits with `03.1-cleanup` scope (mix of `fix(...)`, `test(...)` prefixes per BRIEF).
- `tests/SKIP-BASELINE.md` exists if ANY task took the fallback path; absent if all 6 took primary fix.
- Roadmap success criterion 2 satisfied: "tests/SKIP-BASELINE.md documents the rationale" for any baselined tests.
- Pre-baseline failure count = 6; post-task failure count = 0; delta = -6 OR matches SKIP-BASELINE.md entries exactly.
</verification>

<success_criteria>
- Track T3 (CLEAN-03) closed: 6 atomic commits, each addressing one BRIEF T3.x item.
- Roadmap success criterion 2 satisfied (177 passed / 0 failed OR all remaining failures formally baselined).
- Production-code edits confined to surgical changes (Settings::isDisabled body; EnsureFbpFbcCookies handle guard; PurchasePixel::onMarkFired race-loser branch; SendCapiEvent::handle race-fence ordering) — no drive-by refactors.
- All methods edited remain ≤ 70 LOC (Tiger-Style).
</success_criteria>

<output>
After completion, create `.planning/phases/03.1-08-dead-code-cleanup/03.1-08-03-SUMMARY.md` documenting:
- 6 commit SHAs in order
- Per task: primary vs fallback path taken
- SKIP-BASELINE.md entries list (or "none — full green")
- Final test-failure count (target = 0)
</output>
