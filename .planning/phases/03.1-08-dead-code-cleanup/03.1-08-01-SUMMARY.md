---
phase: 03.1-08-dead-code-cleanup
plan: 01
subsystem: event-log
tags:
  - cleanup
  - dead-code
  - tiger-style
  - hungarian
  - shopaholic
  - phase-3.1
  - docblock
  - fail-fast
requirements:
  - CLEAN-01
dependency-graph:
  requires:
    - 03.1-07 (cross-context site_id symmetry — REFAC-12 + REFAC-13 contracts)
  provides:
    - Production-code docblocks aligned with REFAC-13 caller-supplied site_id
    - EventLogWriter::record signature fail-fast (7th param required ?int)
    - SiteResolver class-level PHPDoc ≤ 10 lines
  affects:
    - models/EventLog.php (docblock only)
    - classes/helper/EventLogWriter.php (signature + @param)
    - classes/helper/SiteResolver.php (class docblock only)
    - tests/Feature/MultiSiteEventLogTest.php (atomic test-caller fix — Rule 3)
tech-stack:
  added: []
  patterns:
    - Tiger-Style fail-fast: drop nullable-with-default in favor of nullable-required
    - Caveman PHPDoc compression: ≤ 10 lines class-level + method-level carries detail
key-files:
  created: []
  modified:
    - models/EventLog.php
    - classes/helper/EventLogWriter.php
    - classes/helper/SiteResolver.php
    - tests/Feature/MultiSiteEventLogTest.php
decisions:
  - "T1.2 picked option (A) — drop `= null` default on `EventLogWriter::record` 7th param. Caller audit: 7 total call sites, all 7 pass explicit value (2 production via SiteResolver::forOrder; 4 tests with explicit int; 1 test with explicit null after atomic fix). Implicit-null count = 0 post-commit; pre-commit = 1 (the single-site test caller, fixed atomically per task spec)."
  - "T1.3 compressed SiteResolver class-level PHPDoc 35 → 9 lines. Dropped 3 supplementary @see anchors (file-path pointers, not REFAC anchors); preserved both @see REFAC-04 + @see REFAC-12."
metrics:
  duration_min: 3
  completed: 2026-05-14
  tasks_completed: 3
  files_modified: 4
  commits: 3
commits:
  - hash: a64f3c3f568899ac50088853c38a539e3f468ec3
    short: a64f3c3
    message: "docs(03.1-cleanup): EventLog model docblock — reflect REFAC-13 caller-supplied site_id"
  - hash: 98ed70cfbad95175fac390bd7c55718de5175b2c
    short: 98ed70c
    message: "refactor(03.1-cleanup): EventLogWriter::record requires explicit ?int site_id (fail-fast)"
  - hash: f2f887c8125ea877f49c8dd50e6bd3ef8def194f
    short: f2f887c
    message: "docs(03.1-cleanup): SiteResolver class-level PHPDoc — caveman compress"
---

# Phase 03.1-08 Plan 01: Production-Code Dead/Stale Cleanup (Track 1) Summary

Track T1 of phase 03.1-08 closed — three atomic commits land production-code docblock + signature fixes addressing REVIEW.md MED-01, MED-02, LOW-06. Writer-contract narrative now reflects post-REFAC-13 caller-supplied `site_id` semantics; writer signature fail-fast (no implicit-null default); SiteResolver class-level PHPDoc caveman-compressed.

## Objective

Resolve phase 03.1-07 REVIEW.md findings before v1.1.1 tag:

- **MED-01** EventLog.php class-level docblock carried the pre-REFAC-13 narrative ("site_id is populated via SiteResolver::getActiveSiteId()") — misleading for Phase 4 contributors.
- **MED-02** EventLogWriter::record had `?int $iSiteId = null` 7th parameter — Phase 4 callers could accidentally write site_id NULL rows in multi-site context (cross-context bug class resurrection).
- **LOW-06** SiteResolver class-level PHPDoc was 35 lines for a class with 2 public statics — bloat versus method-level detail.

## Tasks Completed

### Task 1 (T1.1) — MED-01: EventLog.php stale docblock

**Commit:** `a64f3c3` — `docs(03.1-cleanup): EventLog model docblock — reflect REFAC-13 caller-supplied site_id`

**File:** `models/EventLog.php`

**Change:** Replaced the stale Multi-site paragraph (line 45-47) with the post-REFAC-13 narrative:
- writer pure I/O — accepts explicit `?int $iSiteId` 7th arg, no internal resolve
- Order-scoped subjects (Watcher, SendCapiEvent, PurchasePixel) pass `SiteResolver::forOrder($obOrder)` for writer+reader symmetry
- future Phase 4 non-Order subjects (Lead, AddToCart, ViewContent) may use `SiteResolver::getActiveSiteId()` for request-scoped writes

Literal phrase `caller-supplied` + `REFAC-13` present (acceptance grep matches). `@property` / `@var` / `@see` tag count preserved verbatim: 17 → 17.

### Task 2 (T1.2) — MED-02: EventLogWriter::record fail-fast

**Commit:** `98ed70c` — `refactor(03.1-cleanup): EventLogWriter::record requires explicit ?int site_id (fail-fast)`

**Files:** `classes/helper/EventLogWriter.php` + `tests/Feature/MultiSiteEventLogTest.php`

**Decision recorded:** option **(A) — make required** (drop `= null` default).

**Caller audit:** 7 total `EventLogWriter::record(` call sites outside the writer itself:

| Site | Path | 7th arg (pre-commit) | 7th arg (post-commit) |
|---|---|---|---|
| 1 | `classes/queue/SendCapiEvent.php:214` | `SiteResolver::forOrder($this->obSubject)` | unchanged |
| 2 | `components/PurchasePixel.php:274` | `SiteResolver::forOrder($obOrder)` | unchanged |
| 3 | `tests/Feature/MultiSiteEventLogTest.php:122` | `1` | unchanged |
| 4 | `tests/Feature/MultiSiteEventLogTest.php:133` | `2` | unchanged |
| 5 | `tests/Feature/MultiSiteEventLogTest.php:168` | (omitted) | `null` (atomic fix) |
| 6 | `tests/Feature/MultiSiteEventLogTest.php:203` | `1` | unchanged |
| 7 | `tests/Feature/MultiSiteEventLogTest.php:214` | `2` | unchanged |

**Implicit-null count (pre-commit): 1** — well within the BRIEF decision-tree ≤ 3 threshold. Option (A) selected. The single implicit-null test caller (site 5) is fixed atomically per task spec ("production-code task owns the writer contract; tests calling the writer with implicit null break the build and must be fixed atomically with the contract change") — passing explicit `null` preserves the test's "single-site install writes null site_id" semantic.

**Signature delta:**

```diff
-        ?int $iSiteId = null,
+        ?int $iSiteId,
```

**@param PHPDoc update:** caveman-narrates the caller-supplied contract — Order-scoped via `SiteResolver::forOrder`; request-scoped (Phase 4 non-Order) via `SiteResolver::getActiveSiteId`; explicit null = single-site / CLI / no scope.

**Method body unchanged.** Method LOC = 61 (≤ 70 Tiger-Style).

### Task 3 (T1.3) — LOW-06: SiteResolver class-level PHPDoc bloat

**Commit:** `f2f887c` — `docs(03.1-cleanup): SiteResolver class-level PHPDoc — caveman compress`

**File:** `classes/helper/SiteResolver.php`

**Class-level PHPDoc line count: 35 → 9 lines** (74% reduction).

**Preserved anchors (both REFAC `@see` tags verbatim):**

- `@see ... BRIEF.md REFAC-04` (canonical Phase 3.1 refactor anchor)
- `@see ... BRIEF.md REFAC-12` (canonical Phase 3.1-07 refactor anchor)

**Dropped supplementary @see** (file-path pointers, not REFAC anchors — detail recoverable from method-level docblocks):

- `modules/system/classes/SiteManager.php`
- `modules/system/classes/sitemanager/HasActiveSite.php`
- `classes/helper/PluginGuard.php` boundary-catch precedent

**Method bodies + signatures + method-level PHPDocs:** byte-identical pre/post (`git diff` confirms only class-level block changed).

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Atomic test-caller fix in MultiSiteEventLogTest.php**

- **Found during:** Task 2 caller audit.
- **Issue:** Pre-Task-2, the test `test_single_site_install_writes_null_site_id` at line 168 relied on the writer's default `= null` to omit the 7th arg. Dropping the default in Task 2 would have caused this test to fail at compile time (PHP `ArgumentCountError`) and broken the build.
- **Fix:** Pass explicit `null` as the 7th arg (semantically identical — both pre- and post-fix the writer receives `null` for `$iSiteId`; the test's "single-site install writes null site_id" invariant is preserved).
- **Files modified:** `tests/Feature/MultiSiteEventLogTest.php` (line 168 area, +3 lines including caveman explainer comment).
- **Commit:** `98ed70c` (bundled with Task 2 per the plan's explicit authorization — "tests calling the writer with implicit null break the build and must be fixed atomically with the contract change").

### Out-of-Scope Discoveries

None — no work outside the plan's prescribed three files + the authorized atomic test fix.

## Acceptance Criteria Verification

| Criterion | Status | Evidence |
|---|---|---|
| `grep -q "caller-supplied" models/EventLog.php` | PASS | `models/EventLog.php` Multi-site para uses literal phrase. |
| `grep -q "REFAC-13" models/EventLog.php` | PASS | Same para anchors REFAC-13. |
| Stale `SiteResolver::getActiveSiteId()` narrative gone | PASS | Only remaining ref is in future-Phase-4 sentence — explicitly allowed by plan. |
| `@property` / `@var` / `@see` tag count ≥ prior | PASS | 17 → 17 (no functional tag dropped). |
| `?int $iSiteId(,|))` present | PASS | Line 77 reads `?int $iSiteId,` (no default). |
| `?int $iSiteId = null` absent | PASS | Default dropped. |
| Every caller passes explicit 7th arg | PASS | 7/7 call sites supply 7th arg post-commit. |
| Decision (A/B) + audit count recorded in commit | PASS | Commit `98ed70c` body documents option (A) + caller-audit table. |
| Method LOC ≤ 70 | PASS | 61 lines. |
| Method body byte-unchanged | PASS | `git diff` confirms only signature + @param prose changed. |
| Class-level PHPDoc ≤ 10 lines | PASS | 9 lines. |
| `@see REFAC-04` preserved | PASS | line 18. |
| `@see REFAC-12` preserved | PASS | line 19. |
| Method-level docblocks byte-identical | PASS | `git diff classes/helper/SiteResolver.php` — only class-level block changed. |
| Exactly 3 commits with `03.1-cleanup` scope | PASS | `a64f3c3` + `98ed70c` + `f2f887c`. |
| 3 production files + 1 test file modified | PASS | 4 files total; test fix is Rule-3 atomic per plan spec. |

## Test Suite Status

**Baseline captured pre-edit (canonical plugin path):** `Tests: 6 failed, 171 passed (503 assertions). Duration: 6.37s.`

Source: `cd /home/forge/nailscosmetics.lv/plugins/logingrupa/metapixelshopaholic && composer test` (output truncated; failure count and pass count match the 03.1-06 SUMMARY baseline).

**Post-edit verification path:**

The plan executor runs in a git worktree at `<plugin>/.claude/worktrees/agent-ae1f4ab0ec35af11b`. October CMS's plugin autoloader scans the canonical `plugins/` tree from the main project root — the worktree copy is invisible to the test harness until the wave-merge step folds the worktree branch back onto the plugin's main branch. As a parallel executor agent I cannot directly run `composer test` against the worktree code; verification of the "baseline preserved" criterion is delegated to the orchestrator post-merge.

**Static reasoning preserves the baseline:**

- **Task 1** is docblock-only on `models/EventLog.php` — zero runtime impact (PHPDoc is not executed).
- **Task 3** is docblock-only on `classes/helper/SiteResolver.php` — zero runtime impact, method bodies + signatures byte-identical.
- **Task 2** changes the signature of `EventLogWriter::record` (drops default on 7th param). The only callers are 2 production sites + 5 test sites; ALL 7 now pass an explicit 7th arg (verified by `grep`). The 1 previously-implicit-null test caller is fixed atomically in the same commit. No call path remains where the default would have been silently relied upon. PHP syntax check passes for all 4 edited files (`php -l`). EventLogWriter::record method body is byte-identical pre/post — observable behavior unchanged.

**Expected post-merge test count:** unchanged at 171 passed / 6 failed. None of the 6 pre-existing failures touch EventLogWriter / EventLog / SiteResolver signature surface — they are diagnosed in Track 3 of phase 03.1-08 (separate plans). The MultiSiteEventLogTest suite (which is the only directly impacted test file) currently has all methods green per the 03.1-07 SUMMARY; the line-168 atomic fix preserves the test's invariant.

## Threat Model Notes

No new threat surface introduced. Tightening the writer signature (T-03.1-08-01 mitigation: drop nullable default) reduces the attack surface — Phase 4 callers cannot accidentally write `site_id NULL` rows in multi-site context, resurrecting the cross-context bug class fixed in 03.1-07. Fail-fast at PHP signature level.

## Authentication Gates

None encountered.

## Self-Check

**1. Files modified exist:**
- `models/EventLog.php` — FOUND
- `classes/helper/EventLogWriter.php` — FOUND
- `classes/helper/SiteResolver.php` — FOUND
- `tests/Feature/MultiSiteEventLogTest.php` — FOUND

**2. Commits exist:**
- `a64f3c3` — FOUND (`git log --grep="EventLog model docblock"`)
- `98ed70c` — FOUND (`git log --grep="EventLogWriter::record requires"`)
- `f2f887c` — FOUND (`git log --grep="SiteResolver class-level"`)

**3. Grep-based acceptance criteria:** all PASS (table above).

## Self-Check: PASSED

## Forward Notes

- **For T1.2 follow-up:** Phase 4 plans MUST pass an explicit 7th arg on every `EventLogWriter::record` call. New funnel jobs (AddToCart, ViewContent, Lead) should pass `SiteResolver::getActiveSiteId()` for request-scoped writes OR `SiteResolver::forOrder($obOrder)` if the subject is Order-bound. PHP signature now rejects omission at compile time.
- **For T1.3 follow-up:** The dropped `@see` to `classes/helper/PluginGuard.php boundary-catch precedent` was the only inline pointer to the Throwable-catch pattern. If Phase 4 introduces a third helper following the same pattern, consider centralizing the precedent in a `classes/helper/PATTERNS.md` rather than re-anchoring per-class.
- **Track 2-5 of phase 03.1-08 remain open:** test cleanup (5 items), 6 failing-test diagnosis (Track 3), planning-doc cleanup (Track 4), milestone close (Track 5). This plan owned Track 1 only.
