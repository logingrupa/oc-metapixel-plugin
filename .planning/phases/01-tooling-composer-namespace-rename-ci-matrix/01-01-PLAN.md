---
phase: 01-tooling-composer-namespace-rename-ci-matrix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - plugins/logingrupa/metapixelshopaholic/                            # DELETED (whole tree)
  - plugins/logingrupa/metapixel/                                      # CREATED (new tree root)
  - plugins/logingrupa/metapixel/Plugin.php                            # CREATED (minimal scaffold)
  - plugins/logingrupa/metapixel/plugin.yaml                           # CREATED
  - plugins/logingrupa/metapixel/composer.json                         # CREATED (per TOOL-01)
  - plugins/logingrupa/metapixel/lang/en/lang.php                      # CREATED (plugin.name/description only)
  - plugins/logingrupa/metapixel/lang/lv/lang.php                      # CREATED (plugin.name/description only)
  - plugins/logingrupa/metapixel/.gitignore                            # CREATED
  - plugins/logingrupa/metapixel/.planning/                            # MOVED from old dir (single rename commit)
autonomous: true
requirements:
  - TOOL-01
  - TOOL-02
  - TOOL-03
must_haves:
  truths:
    - "v1.x source preserved on `legacy/v1.1.1` git branch and reachable via `git show legacy/v1.1.1:<path>`."
    - "Master tree contains directory `plugins/logingrupa/metapixel/` with `Plugin.php` declaring namespace `Logingrupa\\Metapixel`."
    - "Master tree no longer contains directory `plugins/logingrupa/metapixelshopaholic/`."
    - "`.planning/` directory lives inside `plugins/logingrupa/metapixel/.planning/` (moved from old plugin dir)."
    - "`composer.json` advertises name `logingrupa/oc-metapixel-plugin`, php `^8.3 || ^8.4`, PSR-4 `Logingrupa\\Metapixel\\`, and lists Lovata cart plugins in `suggest:` (not `require:`)."
    - "`composer install` at repo root completes with exit 0 and no warnings about missing classes from the new plugin tree."
  artifacts:
    - path: "plugins/logingrupa/metapixel/Plugin.php"
      provides: "Plugin entry point with new namespace, empty boot/register (business logic ships in Phase 2)."
      contains: "namespace Logingrupa\\Metapixel"
    - path: "plugins/logingrupa/metapixel/plugin.yaml"
      provides: "October plugin manifest with lang-keyed name/description under `logingrupa.metapixel::` namespace."
      contains: "logingrupa.metapixel::lang.plugin.name"
    - path: "plugins/logingrupa/metapixel/composer.json"
      provides: "Composer package definition (TOOL-01 shape — no Lovata cart deps in require:)."
      contains: "logingrupa/oc-metapixel-plugin"
    - path: "plugins/logingrupa/metapixel/lang/en/lang.php"
      provides: "Plugin manifest lang keys under the renamed namespace."
      contains: "'plugin' =>"
    - path: "plugins/logingrupa/metapixel/.planning/ROADMAP.md"
      provides: ".planning/ directory carried into the new plugin dir."
      contains: "v2.0.0"
  key_links:
    - from: "plugins/logingrupa/metapixel/composer.json"
      to: "PSR-4 autoload"
      via: "autoload.psr-4 mapping"
      pattern: '"Logingrupa\\\\\\\\Metapixel\\\\\\\\": ""'
    - from: "plugins/logingrupa/metapixel/Plugin.php"
      to: "plugin.yaml"
      via: "October PluginManager identifier resolution"
      pattern: "Logingrupa\\\\Metapixel"
    - from: "plugins/logingrupa/metapixel/plugin.yaml"
      to: "lang/en/lang.php"
      via: "lang-key namespace `logingrupa.metapixel::lang.*`"
      pattern: "logingrupa.metapixel::lang.plugin"
---

<goal>
v1.x plugin source on master is replaced with a minimal v2.0 scaffold: directory renamed `metapixelshopaholic/` → `metapixel/`, root namespace `Logingrupa\Metapixelshopaholic` → `Logingrupa\Metapixel`, lang namespace `logingrupa.metapixelshopaholic::` → `logingrupa.metapixel::`, and `composer.json` redrawn to TOOL-01 shape (PHP ^8.3 || ^8.4, generic description, Lovata cart plugins in `suggest:` not `require:`). v1.x is preserved on `legacy/v1.1.1`; master ships zero business logic at the end of this plan, ready for tooling configs (Plan 01-02) and test scaffold (Plan 01-03).
</goal>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@plugins/logingrupa/metapixelshopaholic/.planning/PROJECT.md
@plugins/logingrupa/metapixelshopaholic/.planning/ROADMAP.md
@plugins/logingrupa/metapixelshopaholic/.planning/STATE.md
@plugins/logingrupa/metapixelshopaholic/.planning/REQUIREMENTS.md
@plugins/logingrupa/metapixelshopaholic/.planning/research/STACK.md
@plugins/logingrupa/metapixelshopaholic/.planning/research/PITFALLS.md
@CLAUDE.md

<interfaces>
Project-level locks the executor must respect:

- Plugin identifier (October PluginManager): `Logingrupa.Metapixel`
- PHP namespace root: `Logingrupa\Metapixel`
- Plugin directory: `plugins/logingrupa/metapixel/`
- Composer package name: `logingrupa/oc-metapixel-plugin`
- Lang namespace: `logingrupa.metapixel::lang.*`
- Settings code (future Phase 4): `logingrupa-metapixel-settings`
- DB table prefix (future Phase 2+): `logingrupa_metapixel_*`
- Hungarian notation: $ob (object), $ar (array), $i (int), $s (string), $b (bool), $f (float)
- No `declare(strict_types=1)` enforcement (optional per-file)
- No phase/CR/incident markers in code comments — workflow refs go in commits only
- Laravel short docblocks only: one-line summary + @param + @return (no multi-paragraph narrative)
</interfaces>

<reference_for_lift>
v1.x source files on master tree (DELETED at end of this plan, after lift):
- `plugins/logingrupa/metapixelshopaholic/composer.json` — lift overall shape, swap name/php/autoload/Lovata-deps per TOOL-01
- `plugins/logingrupa/metapixelshopaholic/plugin.yaml` — lift shape, swap lang keys
- `plugins/logingrupa/metapixelshopaholic/lang/en/lang.php` — lift only the `plugin` key (drop everything else; business lang keys ship in their owning phase)
- `plugins/logingrupa/metapixelshopaholic/Plugin.php` — DO NOT lift; write fresh minimal scaffold

After this plan completes, retrieve any v1.x file via:
    git show legacy/v1.1.1:plugins/logingrupa/metapixelshopaholic/<path>
</reference_for_lift>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Preserve v1.x on legacy/v1.1.1 branch (safety net)</name>
  <files>
    (no working-tree files — git ref operations only)
  </files>
  <action>
Verify (or create if missing) the `legacy/v1.1.1` git branch so v1.x source survives the master tree reset.

1. Run `git rev-parse --verify legacy/v1.1.1 2>/dev/null && echo EXISTS || echo MISSING` from repo root.
2. If MISSING: identify the SHA of v1.1.1 tag closure: `git rev-parse v1.1.1` (per STATE.md line 57, tag exists at SHA `3f32ca6`). Create branch: `git branch legacy/v1.1.1 v1.1.1`. Do NOT push.
3. If EXISTS: confirm the branch tip resolves to the v1.1.1 closure SHA (`git log -1 --format=%H legacy/v1.1.1` should match `git rev-parse v1.1.1`).
4. Sanity probe the branch can serve files: `git show legacy/v1.1.1:plugins/logingrupa/metapixelshopaholic/composer.json | head -5` returns the v1.x composer.json head.
5. NO COMMIT in this task — pure git-ref bookkeeping. Record the resolved SHA in the SUMMARY at phase close.
  </action>
  <verify>
    <automated>git rev-parse --verify legacy/v1.1.1 &amp;&amp; git show legacy/v1.1.1:plugins/logingrupa/metapixelshopaholic/composer.json | grep -q '"name": "logingrupa/oc-metapixel-plugin"'</automated>
  </verify>
  <done>`legacy/v1.1.1` branch exists locally and `git show legacy/v1.1.1:<path>` retrieves any v1.x file. v1.x source is now retrievable independently of the working tree.</done>
</task>

<task type="auto">
  <name>Task 2: Move directory metapixelshopaholic → metapixel (single git mv, preserves history)</name>
  <files>
    plugins/logingrupa/metapixelshopaholic/ → plugins/logingrupa/metapixel/
  </files>
  <action>
Perform a single `git mv` so git tracks the move as a rename for the whole subtree, preserving file history (including `.planning/`).

1. From repo root: `git mv plugins/logingrupa/metapixelshopaholic plugins/logingrupa/metapixel`.
2. Verify: `git status --short | head -20` shows R (renamed) entries, NOT D+A pairs. If git split it into delete+add, abort, run `git mv --force` or fall back to filesystem `mv` followed by `git add -A`.
3. Verify `.planning/` is INSIDE the new dir: `ls plugins/logingrupa/metapixel/.planning/ROADMAP.md` succeeds.
4. Commit ONLY this rename — no content edits yet:
       git commit -m "$(cat &lt;&lt;'EOF'
chore(metapixel): rename plugin directory metapixelshopaholic to metapixel

Per TOOL-02 (Phase 1 plan 01-01 task 2). Pure git mv — no content
changes in this commit. v1.x source preserved on legacy/v1.1.1.
EOF
)"
5. Do NOT edit any file content in this task — keep the rename diff atomic.
  </action>
  <verify>
    <automated>test -d plugins/logingrupa/metapixel &amp;&amp; test ! -d plugins/logingrupa/metapixelshopaholic &amp;&amp; test -f plugins/logingrupa/metapixel/.planning/ROADMAP.md &amp;&amp; git log -1 --name-status | grep -c '^R' | xargs test 1 -le</automated>
  </verify>
  <done>Directory rename committed as a single rename diff. v1.x file history follows the move. `.planning/` is now inside the new dir.</done>
</task>

<task type="auto">
  <name>Task 3: Rewrite namespace + lang-key references across the renamed tree</name>
  <files>
    plugins/logingrupa/metapixel/Plugin.php
    plugins/logingrupa/metapixel/classes/**/*.php
    plugins/logingrupa/metapixel/models/**/*.php
    plugins/logingrupa/metapixel/components/**/*.php
    plugins/logingrupa/metapixel/controllers/**/*.php
    plugins/logingrupa/metapixel/middleware/**/*.php
    plugins/logingrupa/metapixel/updates/**/*.php
    plugins/logingrupa/metapixel/tests/**/*.php
    plugins/logingrupa/metapixel/lang/**/*.php
    plugins/logingrupa/metapixel/plugin.yaml
    plugins/logingrupa/metapixel/phpstan.neon
    plugins/logingrupa/metapixel/phpmd.xml
    plugins/logingrupa/metapixel/rector.php
    plugins/logingrupa/metapixel/pint.json
    plugins/logingrupa/metapixel/phpunit.xml
    plugins/logingrupa/metapixel/composer.json
  </files>
  <action>
Rewrite every textual occurrence of the old namespace/lang-key/installer-name across the renamed tree. This is the TOOL-03 mechanical sweep.

Replacement table (case-sensitive, exact-string only — no regex broadening):

  Logingrupa\Metapixelshopaholic   → Logingrupa\Metapixel
  Logingrupa\\Metapixelshopaholic  → Logingrupa\\Metapixel        (escaped form in PHP strings / composer.json)
  logingrupa.metapixelshopaholic   → logingrupa.metapixel         (lang namespace)
  logingrupa_metapixelshopaholic   → logingrupa_metapixel         (snake-case identifiers, settings code prefix)
  Logingrupa.Metapixelshopaholic   → Logingrupa.Metapixel         (October PluginManager identifier)
  Metapixelshopaholic\Tests        → Metapixel\Tests              (test namespace tail)
  metapixelshopaholic              → metapixel                    (LOWERCASE BARE STRING — installer-name, paths, etc.)

Execute carefully:

1. From repo root, identify files needing edits:
       grep -rl -E '(Logingrupa\\\\?Metapixelshopaholic|logingrupa\.metapixelshopaholic|logingrupa_metapixelshopaholic|Logingrupa\.Metapixelshopaholic|metapixelshopaholic)' \
         plugins/logingrupa/metapixel/ \
         --exclude-dir=.planning --exclude-dir=vendor --exclude-dir=phpstan-baseline.neon \
         | tee /tmp/metapixel-rename-files.txt
2. For EACH file in the list, use the Edit tool (NOT sed) to apply the substitutions above. Edit tool requires reading file first — that is the project convention.
3. Special-case files where multiple variants co-exist on a single line (e.g. PSR-4 `"Logingrupa\\\\Metapixelshopaholic\\\\"`): apply the escaped form FIRST, then the unescaped form, to avoid partial substitution.
4. EXCLUDE `.planning/` from the rewrite — those are historical planning docs that reference the v1.x name correctly. Do NOT touch `.planning/**/*.md` or `phpstan-baseline.neon` (the baseline is regenerated in plan 01-02).
5. After edits, sanity probe — these must all return ZERO matches outside `.planning/`:
       grep -rE 'Logingrupa\\\\?Metapixelshopaholic' plugins/logingrupa/metapixel/ --exclude-dir=.planning | wc -l        # must be 0
       grep -rE 'logingrupa\.metapixelshopaholic'   plugins/logingrupa/metapixel/ --exclude-dir=.planning | wc -l         # must be 0
       grep -rE 'Logingrupa\.Metapixelshopaholic'   plugins/logingrupa/metapixel/ --exclude-dir=.planning | wc -l         # must be 0
       grep -rE 'metapixelshopaholic'               plugins/logingrupa/metapixel/ --exclude-dir=.planning | wc -l         # must be 0
6. Stage + commit ONLY the rename rewrite (do NOT touch composer.json's php/require/suggest sections yet — that ships in Task 4):
       git add -A plugins/logingrupa/metapixel/
       git commit -m "$(cat &lt;&lt;'EOF'
refactor(metapixel): rename namespace Logingrupa\Metapixelshopaholic to Logingrupa\Metapixel

Per TOOL-03 (Phase 1 plan 01-01 task 3). Mechanical sweep — namespace,
lang-key namespace, October plugin identifier, installer-name, snake_case
settings prefix. .planning/ untouched (historical record). composer.json
require/suggest blocks restructured in next commit.
EOF
)"
  </action>
  <verify>
    <automated>! grep -rE 'Logingrupa\\\\?Metapixelshopaholic|logingrupa\.metapixelshopaholic|Logingrupa\.Metapixelshopaholic|metapixelshopaholic' plugins/logingrupa/metapixel/ --exclude-dir=.planning</automated>
  </verify>
  <done>Zero references to the old name remain in the renamed tree (excluding `.planning/`). `git status` is clean. Plugin still has v1.x business code intact — only identifiers were rewritten.</done>
</task>

<task type="auto">
  <name>Task 4: Delete v1.x source tree (Plugin.php, classes, models, components, controllers, middleware, lang, updates, tests, configs) — keep .planning/ + plugin.yaml only</name>
  <files>
    DELETED: plugins/logingrupa/metapixel/Plugin.php
    DELETED: plugins/logingrupa/metapixel/classes/
    DELETED: plugins/logingrupa/metapixel/models/
    DELETED: plugins/logingrupa/metapixel/components/
    DELETED: plugins/logingrupa/metapixel/controllers/
    DELETED: plugins/logingrupa/metapixel/middleware/
    DELETED: plugins/logingrupa/metapixel/lang/
    DELETED: plugins/logingrupa/metapixel/updates/
    DELETED: plugins/logingrupa/metapixel/tests/
    DELETED: plugins/logingrupa/metapixel/composer.json
    DELETED: plugins/logingrupa/metapixel/phpstan.neon
    DELETED: plugins/logingrupa/metapixel/phpstan-baseline.neon
    DELETED: plugins/logingrupa/metapixel/phpmd.xml
    DELETED: plugins/logingrupa/metapixel/pint.json
    DELETED: plugins/logingrupa/metapixel/rector.php
    DELETED: plugins/logingrupa/metapixel/phpunit.xml
    KEPT:    plugins/logingrupa/metapixel/.planning/
    KEPT:    plugins/logingrupa/metapixel/plugin.yaml   (will be rewritten in task 5)
  </files>
  <action>
Delete all v1.x source artifacts. The renamed tree from task 3 still contains v1.x business code; this task removes it so the v2.0 scaffold replaces it cleanly. Configs (phpstan.neon, phpmd.xml, pint.json, rector.php, phpunit.xml, composer.json) are ALSO deleted — v2.0 configs land in plan 01-02 with TOOL-04..07 shapes.

1. From repo root, delete the v1.x trees:
       git rm -rf plugins/logingrupa/metapixel/Plugin.php \
                  plugins/logingrupa/metapixel/classes \
                  plugins/logingrupa/metapixel/models \
                  plugins/logingrupa/metapixel/components \
                  plugins/logingrupa/metapixel/controllers \
                  plugins/logingrupa/metapixel/middleware \
                  plugins/logingrupa/metapixel/lang \
                  plugins/logingrupa/metapixel/updates \
                  plugins/logingrupa/metapixel/tests \
                  plugins/logingrupa/metapixel/composer.json \
                  plugins/logingrupa/metapixel/phpstan.neon \
                  plugins/logingrupa/metapixel/phpstan-baseline.neon \
                  plugins/logingrupa/metapixel/phpmd.xml \
                  plugins/logingrupa/metapixel/pint.json \
                  plugins/logingrupa/metapixel/rector.php \
                  plugins/logingrupa/metapixel/phpunit.xml
2. Verify only `.planning/` and `plugin.yaml` remain:
       ls plugins/logingrupa/metapixel/
       # expected output: .planning  plugin.yaml
3. Commit the deletion:
       git commit -m "$(cat &lt;&lt;'EOF'
chore(metapixel): drop v1.x source tree — fresh scaffold for v2.0

Removes v1.x Plugin.php, classes/, models/, components/, controllers/,
middleware/, lang/, updates/, tests/, and all tooling configs. v1.x is
preserved on legacy/v1.1.1 — retrieve any file via
`git show legacy/v1.1.1:plugins/logingrupa/metapixelshopaholic/<path>`.

Only .planning/ and plugin.yaml remain. v2.0 scaffold follows.
EOF
)"
  </action>
  <verify>
    <automated>ls plugins/logingrupa/metapixel/ | sort | tr '\n' ' ' | grep -qE '^\.planning plugin\.yaml $'</automated>
  </verify>
  <done>Renamed tree contains only `.planning/` and `plugin.yaml`. v1.x business code is gone from master. `git status` clean.</done>
</task>

<task type="auto">
  <name>Task 5: Write v2.0 scaffold — composer.json (TOOL-01), Plugin.php, plugin.yaml, lang/{en,lv}/lang.php, .gitignore</name>
  <files>
    plugins/logingrupa/metapixel/composer.json
    plugins/logingrupa/metapixel/Plugin.php
    plugins/logingrupa/metapixel/plugin.yaml
    plugins/logingrupa/metapixel/lang/en/lang.php
    plugins/logingrupa/metapixel/lang/lv/lang.php
    plugins/logingrupa/metapixel/.gitignore
  </files>
  <action>
Write the minimal v2.0 scaffold. All artifacts are fresh — no v1.x carry-forward beyond the structural pattern.

5.1 — `plugins/logingrupa/metapixel/composer.json` (TOOL-01 shape):

  Required fields per TOOL-01 + STACK.md §"NEW pattern 1":
  - name: `logingrupa/oc-metapixel-plugin`
  - type: `october-plugin`
  - description: GENERIC (no "shopaholic", no vendor-specific terms). Use: "Meta Pixel and Conversions API event tracking for OctoberCMS."
  - license: `MIT`
  - authors: keep `Logingrupa` / `info@logingrupa.lv`
  - require:
        php: `^8.3 || ^8.4`
        october/system: `^4.0`
        october/rain: `^4.0`
        lovata/toolbox-plugin: `^2.2`
        guzzlehttp/guzzle: `^7.8`
        ramsey/uuid: `^4.7`
        jeremykendall/php-domain-parser: `^6.4`         (Phase 4 dep — install now per STACK.md to avoid later composer churn)
  - suggest:
        lovata/shopaholic-plugin: "Required by the bundled ShopaholicAdapter to track Lovata Shopaholic events. Install version ^1.32."
        lovata/ordersshopaholic-plugin: "Required by the bundled ShopaholicAdapter to track Order events. Install version ^1.33."
        lovata/buddies-plugin: "Optional — used by ShopaholicAdapter for hashed user_data enrichment. Install version ^1.10."
  - require-dev:
        pestphp/pest: `^4.1`
        pestphp/pest-plugin-drift: `^4.0`
        phpunit/phpunit: `^12`
        larastan/larastan: `^3.0`
        spaze/phpstan-disallowed-calls: `^4.0`
        phpmd/phpmd: `^2.15`
        laravel/pint: `^1.26`
        rector/rector: `^2.0`
        mockery/mockery: `^1.6`
        lovata/shopaholic-plugin: `^1.32`         (dev keeps Lovata cart deps so ShopaholicAdapter tests can run — per REQUIREMENTS.md TOOL-01 line 13)
        lovata/ordersshopaholic-plugin: `^1.33`
        lovata/buddies-plugin: `^1.10`
        (defer shipmonk/composer-dependency-analyser to plan 01-02 TOOL-11 — does not belong in this scaffold commit)
  - autoload.psr-4: `Logingrupa\\Metapixel\\` → `""` (root maps to plugin dir)
  - autoload-dev.psr-4: `Logingrupa\\Metapixel\\Tests\\` → `"tests/"` (lands now so test scaffold in plan 01-03 has its namespace pre-wired)
  - scripts: DEFER composer-qa-related script chain to plan 01-02 (lands with phpstan/phpmd/pint/rector configs). For now, include only `test` and `test-cov` so a fresh clone can at least invoke pest once test scaffold lands:
        test: `../../../vendor/bin/pest --configuration phpunit.xml`
        test-cov: `../../../vendor/bin/pest --configuration phpunit.xml --coverage`
  - extra.october:
        plugin: `Logingrupa.Metapixel`
        installer-name: `metapixel`
  - minimum-stability: `stable`
  - prefer-stable: `true`

5.2 — `plugins/logingrupa/metapixel/Plugin.php` (minimal scaffold — Phase 2 fills business logic):

  ```
  <?php

  namespace Logingrupa\Metapixel;

  use System\Classes\PluginBase;

  /**
   * Meta Pixel + Conversions API tracking plugin — v2.0 scaffold.
   *
   * Phase 1 ships an empty boot/register. Adapter registry, event hooks,
   * and Settings registration land in subsequent phases.
   */
  class Plugin extends PluginBase
  {
      /** @var list<string> */
      public $require = ['Lovata.Toolbox'];

      /**
       * @return array{name: string, description: string, author: string, icon: string, homepage: string}
       */
      public function pluginDetails(): array
      {
          return [
              'name'        => 'logingrupa.metapixel::lang.plugin.name',
              'description' => 'logingrupa.metapixel::lang.plugin.description',
              'author'      => 'Logingrupa',
              'icon'        => 'icon-bullseye',
              'homepage'    => 'https://github.com/logingrupa/oc-metapixel-plugin',
          ];
      }

      public function register(): void
      {
      }

      public function boot(): void
      {
      }
  }
  ```

  Notes for executor:
  - Single-class file, no `declare(strict_types=1)` (project lock: optional per-file).
  - DO NOT add `#[\Override]` on `pluginDetails()` — PHP 8.4-only attribute is fine on 8.3 too, but project lock disallows 8.4-only syntax broadly; keep the file 8.3-lowest-common-denominator simple.
  - `$require = ['Lovata.Toolbox']` ONLY. Lovata cart plugins are NOT required (TOOL-01 lock — they're in composer suggest:).
  - Empty `register()` / `boot()` is intentional — Phase 2 fills these.

5.3 — `plugins/logingrupa/metapixel/plugin.yaml`:

  ```
  plugin:
      name: 'logingrupa.metapixel::lang.plugin.name'
      description: 'logingrupa.metapixel::lang.plugin.description'
      author: Logingrupa
      icon: icon-bullseye
      homepage: 'https://github.com/logingrupa/oc-metapixel-plugin'
  ```

5.4 — `plugins/logingrupa/metapixel/lang/en/lang.php`:

  ```
  <?php

  return [
      'plugin' => [
          'name'        => 'Meta Pixel + Conversions API',
          'description' => 'Server-deduplicated Meta Pixel and Conversions API event tracking for OctoberCMS.',
      ],
  ];
  ```

5.5 — `plugins/logingrupa/metapixel/lang/lv/lang.php`:

  ```
  <?php

  return [
      'plugin' => [
          'name'        => 'Meta Pixel + Conversions API',
          'description' => 'Servera puses dublēšanas novēršanas Meta Pixel un Conversions API notikumu izsekošana OctoberCMS sistēmai.',
      ],
  ];
  ```

  (en+lv only — no ru per build philosophy lock. Operator adds own `lang/ru/lang.php` if needed.)

5.6 — `plugins/logingrupa/metapixel/.gitignore`:

  ```
  /vendor/
  /.phpunit.cache/
  /.phpunit.result.cache
  /coverage/
  /phpunit.coverage.xml
  /phpstan-baseline.neon.bak
  ```

5.7 — Commit:

       git add -A plugins/logingrupa/metapixel/
       git commit -m "$(cat &lt;&lt;'EOF'
feat(metapixel): scaffold v2.0 plugin — composer.json, Plugin.php, plugin.yaml, lang en+lv

Per TOOL-01 (composer logingrupa/oc-metapixel-plugin, PHP ^8.3 || ^8.4,
Lovata cart plugins in suggest:), TOOL-02 (dir + PluginManager id
Logingrupa.Metapixel), TOOL-03 (lang namespace logingrupa.metapixel::).

Plugin.php boot/register are empty — Phase 2 wires AdapterRegistry +
PluginGuard + Settings registration. Tooling configs ship in plan 01-02;
test scaffold in plan 01-03.
EOF
)"
  </action>
  <verify>
    <automated>php -r 'require "plugins/logingrupa/metapixel/Plugin.php"; $r = new ReflectionClass("Logingrupa\\Metapixel\\Plugin"); exit($r-&gt;getNamespaceName() === "Logingrupa\\Metapixel" ? 0 : 1);' &amp;&amp; grep -q '"logingrupa/oc-metapixel-plugin"' plugins/logingrupa/metapixel/composer.json &amp;&amp; grep -q '"php": "\^8.3 || \^8.4"' plugins/logingrupa/metapixel/composer.json &amp;&amp; ! grep -E '"lovata/(orders)?shopaholic-plugin"' plugins/logingrupa/metapixel/composer.json | grep -v suggest | grep -v require-dev</automated>
  </verify>
  <done>Plugin.php loads with namespace `Logingrupa\Metapixel`; composer.json has TOOL-01 shape with `php: ^8.3 || ^8.4`, Lovata cart plugins ONLY in `suggest:` and `require-dev:` (not `require:`); plugin.yaml + lang files reference the new lang namespace.</done>
</task>

<task type="auto">
  <name>Task 6: Run `composer install` at repo root — verify autoload registers Logingrupa\Metapixel and no class-resolution errors surface</name>
  <files>
    composer.lock                                                                    (regenerated)
    vendor/composer/installed.json                                                   (regenerated)
    vendor/composer/autoload_psr4.php                                                (regenerated)
  </files>
  <action>
The renamed plugin's PSR-4 mapping must register with the repo-root Composer autoloader so subsequent plans (and CI) can resolve `Logingrupa\Metapixel\Plugin`.

1. From repo root, run:
       composer install --no-progress --prefer-dist --no-interaction 2>&1 | tee /tmp/metapixel-composer-install.log
2. Confirm the autoload file maps the new namespace:
       grep "Logingrupa\\\\\\\\Metapixel" vendor/composer/autoload_psr4.php
3. Confirm the OLD namespace no longer maps:
       ! grep "Logingrupa\\\\\\\\Metapixelshopaholic" vendor/composer/autoload_psr4.php
4. Confirm `jeremykendall/php-domain-parser` resolved:
       test -d vendor/jeremykendall/php-domain-parser
5. Probe PHP can load the Plugin class via autoload:
       php -r 'require "vendor/autoload.php"; var_dump(class_exists("Logingrupa\\Metapixel\\Plugin"));' | grep -q "bool(true)"
6. Commit the composer.lock + autoload regen as a separate small chore commit:
       git add composer.lock vendor/composer/installed.json 2>/dev/null || true
       # vendor/ may be gitignored at repo root — only commit composer.lock.
       git status --short composer.lock | grep -q '^.M composer.lock' &amp;&amp; git commit -m "$(cat &lt;&lt;'EOF'
chore(deps): regenerate composer.lock after metapixel plugin rename

Picks up the new PSR-4 autoload prefix Logingrupa\Metapixel and the
php-domain-parser ^6.4 add. No other dependency drift expected.
EOF
)"
       # If composer.lock unchanged (already at the right state), skip the commit.
  </action>
  <verify>
    <automated>php -r 'require "vendor/autoload.php"; exit(class_exists("Logingrupa\\Metapixel\\Plugin") ? 0 : 1);'</automated>
  </verify>
  <done>`composer install` exits 0; PSR-4 autoload resolves `Logingrupa\Metapixel\Plugin`; old namespace is unmapped; `php-domain-parser` vendored.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries (Phase 1 scope)

| Boundary | Description |
|----------|-------------|
| composer.json → vendor install | Composer resolves dependencies from packagist + private GitHub repo. Hostile package on path = supply-chain. |
| git rename → reviewer | Reviewer must see the rename as a rename (not delete+add) to audit it efficiently. |
| Plugin namespace change → existing prod installs | Production .no/.lv/.lt currently run v1.1.1 source under the OLD namespace. This phase ships master that DIVERGES — deploy of master to prod would break it. Operator must NOT deploy master to prod until v2.0 reaches a release tag. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | git mv + namespace rewrite | mitigate | v1.x preserved on `legacy/v1.1.1` branch (task 1). Operator can always `git show legacy/v1.1.1:<path>` to retrieve any v1.x file. Each commit in this plan is atomic — a bad rewrite can be reverted without losing other commits. |
| T-01-02 | Denial of Service | production deploy of partial master | accept | Master tree from task 4 onward has ZERO business logic. Forge auto-deploy of master would un-install the plugin's behavior. **Operator responsibility (out of plan scope):** pin prod servers to `legacy/v1.1.1` until v2.0 release tag. Document in SUMMARY at phase close. |
| T-01-03 | Information Disclosure | composer.json publishes generic description | mitigate | TOOL-01 mandates generic description ("Meta Pixel and Conversions API event tracking for OctoberCMS") — no operator brand names baked in. Task 5 specifies the exact string. |
| T-01-04 | Tampering | namespace rewrite misses occurrences | mitigate | Task 3 verify gate runs four `grep` checks that MUST return zero matches outside `.planning/`. Automated. If any check fails, the commit is blocked at the verify step. |
| T-01-05 | Spoofing | Lovata cart plugins in `suggest:` but code path still imports them | accept (in this plan) — handed off to plan 01-02 | This plan ships ZERO source code that imports Lovata classes (Plugin.php is empty boot/register). The `shipmonk/composer-dependency-analyser` lint that catches hidden Lovata imports lands in plan 01-02 (TOOL-11). P-03 prevention is layered: this plan removes the require:; plan 01-02 adds the lint; phase 2+ keeps the discipline. |

</threat_model>

<verification>

## Goal-Backward Reachability Audit

Each must-have truth has an explicit reachability path:

1. "v1.x source preserved on `legacy/v1.1.1` git branch" — Task 1 creates/verifies the branch. Verified by `git show legacy/v1.1.1:plugins/logingrupa/metapixelshopaholic/composer.json` in the automated check.
2. "Master tree contains `plugins/logingrupa/metapixel/Plugin.php` with namespace `Logingrupa\Metapixel`" — Task 5 writes Plugin.php. Verified by `php -r ... class_exists()` in task 5 + 6.
3. "Master tree no longer contains `plugins/logingrupa/metapixelshopaholic/`" — Task 2 renames it. Verified by `test ! -d` in task 2.
4. "`.planning/` lives inside the new dir" — Task 2 renames the whole subtree including `.planning/`. Verified by `test -f .planning/ROADMAP.md` in task 2.
5. "composer.json advertises TOOL-01 shape" — Task 5 writes composer.json. Verified by `grep` checks in task 5 + `composer install` in task 6.
6. "`composer install` completes with exit 0" — Task 6 runs `composer install`. Verified by exit code + class_exists probe.

No must-have is UNREACHABLE.

## Multi-Source Coverage Audit

| Source item | Type | Plan(s) covering | Notes |
|-------------|------|------------------|-------|
| GOAL: "fresh clone of renamed plugin passes composer qa" | Goal | 01-01 (rename scaffold) + 01-02 (qa chain) + 01-03 (tests + CI) | Distributed across the three plans; this plan ships the rename + composer.json prerequisite |
| REQ TOOL-01 (composer.json shape) | Requirement | 01-01 (this plan) | Fully covered in task 5 |
| REQ TOOL-02 (dir rename) | Requirement | 01-01 (this plan) | Fully covered in tasks 2, 5 |
| REQ TOOL-03 (namespace rename + lang keys) | Requirement | 01-01 (this plan) | Fully covered in task 3 |
| REQ TOOL-04..11 | Requirement | 01-02 (TOOL-04..07, 10, 11), 01-03 (TOOL-08, 09) | Out of scope for this plan |
| RESEARCH STACK.md "ADD php-domain-parser ^6.4" | Research | 01-01 (this plan, composer.json task 5) | Installed now to amortize composer churn; consumed in Phase 4 |
| RESEARCH STACK.md "Composer suggest pattern" | Research | 01-01 (this plan, task 5) | Honored in TOOL-01 shape |
| RESEARCH STACK.md "PHP ^8.3 || ^8.4" | Research | 01-01 (this plan, task 5) | Honored in TOOL-01 shape |
| RESEARCH STACK.md "Graph API v23.0" | Research | Phase 2 ADAP-09 | Out of scope for Phase 1 |
| PITFALL P-03 (hidden Lovata imports) | Pitfall | 01-01 (no Lovata code shipped) + 01-02 (lint) + future phases (adapter dir isolation) | This plan keeps Plugin.php empty — zero imports to violate. Lint lands in 01-02. |
| PITFALL P-04 (orphan tables on legacy install) | Pitfall | OUT OF SCOPE for v2.0 per build philosophy ("no BC shims, operators stay on legacy/v1.1.1") | Documented exclusion |
| PITFALL P-06 (PHP 8.4-only syntax) | Pitfall | 01-02 (phpstan + rector + pint enforce) | This plan's Plugin.php is 8.3-compatible by inspection (no property hooks, no asymmetric visibility, no array_find, no #[\Deprecated]) |
| CONTEXT "Master tree resets, v1.x on legacy branch" | Context lock | 01-01 (tasks 1, 4) | Fully honored |
| CONTEXT "No business logic in Phase 1" | Context lock | 01-01 (task 5 ships empty Plugin.php) | Fully honored |
| CONTEXT "No phase/CR markers in code" | Context lock | 01-01 (task 5 Plugin.php docblock is two lines, no Phase/CR/REQ refs) | Fully honored |

Coverage is complete for this plan's scope. No source item is silently dropped.

</verification>

<success_criteria>

Plan 01-01 ships when ALL of the following hold:

1. `git rev-parse --verify legacy/v1.1.1` returns a SHA (branch exists). `git show legacy/v1.1.1:plugins/logingrupa/metapixelshopaholic/composer.json` returns the v1.x composer.json.
2. `plugins/logingrupa/metapixel/` directory exists on master and contains: `Plugin.php`, `plugin.yaml`, `composer.json`, `lang/en/lang.php`, `lang/lv/lang.php`, `.gitignore`, `.planning/`. Nothing else.
3. `plugins/logingrupa/metapixelshopaholic/` no longer exists on master.
4. `grep -rE 'Logingrupa\\?Metapixelshopaholic|logingrupa\.metapixelshopaholic|Logingrupa\.Metapixelshopaholic|metapixelshopaholic' plugins/logingrupa/metapixel/ --exclude-dir=.planning` returns ZERO matches.
5. `composer.json` declares `"name": "logingrupa/oc-metapixel-plugin"`, `"php": "^8.3 || ^8.4"`, PSR-4 `"Logingrupa\\Metapixel\\": ""`, and `lovata/shopaholic-plugin` + `lovata/ordersshopaholic-plugin` + `lovata/buddies-plugin` ONLY in `suggest:` (and in `require-dev:` for the test suite). NOT in `require:`.
6. `composer install` at repo root exits 0; `php -r 'require "vendor/autoload.php"; class_exists("Logingrupa\\Metapixel\\Plugin");'` returns `bool(true)`.
7. Each task above produced exactly one git commit. Phase 1 plan 01 has 5 commits total (task 2, 3, 4, 5, 6 — task 1 is git-ref bookkeeping only).

</success_criteria>

<commit_messages>

Pre-drafted (per task):

- Task 2: `chore(metapixel): rename plugin directory metapixelshopaholic to metapixel`
- Task 3: `refactor(metapixel): rename namespace Logingrupa\Metapixelshopaholic to Logingrupa\Metapixel`
- Task 4: `chore(metapixel): drop v1.x source tree — fresh scaffold for v2.0`
- Task 5: `feat(metapixel): scaffold v2.0 plugin — composer.json, Plugin.php, plugin.yaml, lang en+lv`
- Task 6: `chore(deps): regenerate composer.lock after metapixel plugin rename`

Per project lock ("no phase/CR/incident markers in code; workflow refs go in commits/PRs only"), commit subjects do NOT include `(01-01)` numeric suffixes. Body lines may reference TOOL-01/02/03 IDs for traceability — that is workflow context, not code.

</commit_messages>

<output>
After completion, create `plugins/logingrupa/metapixel/.planning/phases/01-tooling-composer-namespace-rename-ci-matrix/01-01-SUMMARY.md` documenting:
- The SHA of `legacy/v1.1.1` branch tip
- The 5 commit SHAs produced by this plan
- Confirmation that grep checks all returned zero
- The PSR-4 autoload entry as found in `vendor/composer/autoload_psr4.php` after composer install
- Any drift from the plan (file changes not in `files_modified` frontmatter)
- Handoff note for plan 01-02: "composer.json scripts section currently has only test/test-cov. Plan 01-02 expands to the full `qa` chain after phpstan/phpmd/pint/rector configs land."
</output>
