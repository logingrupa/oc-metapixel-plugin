---
id: 03.1-03
phase: 03.1-event-log-refactor
plan: 03
type: execute
wave: 3
depends_on:
  - 03.1-01
  - 03.1-02
files_modified:
  - plugins/logingrupa/metapixelshopaholic/classes/queue/SendCapiEvent.php
  - plugins/logingrupa/metapixelshopaholic/classes/event/OrderStatusWatcher.php
  - plugins/logingrupa/metapixelshopaholic/tests/Feature/SendCapiEventEventLogTest.php
  - plugins/logingrupa/metapixelshopaholic/tests/Feature/OrderStatusWatcherEventLogTest.php
  - plugins/logingrupa/metapixelshopaholic/tests/Feature/SendCapiEventTest.php
  - plugins/logingrupa/metapixelshopaholic/tests/Feature/OrderStatusWatcherTest.php
autonomous: true
requirements:
  - REFAC-06
  - REFAC-07
rationale: >
  Wave-3 queue + watcher rewrite. `SendCapiEvent::handle()` gains a
  race-fence pre-call to `EventLogWriter::record(channel='capi', ...)` BEFORE
  `MetaClient::send`. The race-fence atomicity moves from the legacy
  `meta_purchase_event_id IS NULL` column-CAS (gone) to the new event_log
  UNIQUE constraint. This is the v1.1.0 EXPLICIT BREAKING CHANGE: the
  SendCapiEvent constructor adds a required third parameter `Order $obSubject`
  (breaks Phase 3 SCE-03 lock signature `(string $sEventName, array $arPayload)`).
  Bumping the plugin to v1.1.0 in Wave 1 documents the break. `OrderStatusWatcher`
  loses the WR-12/WR-13 atomic-CAS-on-orders code (columns gone) and gains an
  `alreadyDispatched(Order): bool` existence-check helper reading event_log
  via SiteResolver-scoped query. `fireForwardDispatch` shrinks well under
  Tiger-Style 70-LOC ceiling. Two test files: NEW `SendCapiEventEventLogTest`
  locks race-winner / race-loser / DB-write-failure paths (REFAC-11 set 2);
  REWRITE `OrderStatusWatcherTest` → `OrderStatusWatcherEventLogTest` keeps
  all 10 existing method names and Queue::assertPushed patterns but swaps
  `$obOrder->fresh()->meta_purchase_event_id` assertions for EventLog row
  queries (REFAC-11 set 4). The legacy SendCapiEventTest stays — it locks
  the queue-job retry/dead-letter machinery (SCE-01..09) which Phase 3.1
  does NOT touch beyond the constructor signature; ALL 12 of its existing
  test calls must be updated to pass the new `$obOrder` constructor param.
must_haves:
  truths:
    - "`SendCapiEvent::__construct(string $sEventName, array $arPayload, Lovata\\OrdersShopaholic\\Models\\Order $obSubject)` — new mandatory third parameter. SCE-03 signature lock from Phase 3 is EXPLICITLY broken (documented in plan SUMMARY as the v1.1.0 minor-bump rationale)."
    - "`SendCapiEvent::handle(MetaClient)` calls `EventLogWriter::record($sEventId, 'Purchase', 'capi', $this->obSubject, $sSecretKey, $iEventTime)` BEFORE `$obClient->send($this->arPayload)`."
    - "When `EventLogWriter::record` returns false → `Log::info('Metapixel CAPI dispatch lost race — peer already POSTed', $this->buildLogContext())` + immediate return. NO HTTP POST."
    - "When `EventLogWriter::record` returns true → proceed to `$obClient->send(...)` with all SCE-01..09 retry / dead-letter machinery intact."
    - "All other SCE-* locks preserved: `final class`, `implements ShouldQueue`, 4 traits, `$tries = 3`, `$backoff = [1, 4, 16]`, `failed()` hook, multi-catch routing, `writeFailedEvent` silent-catch."
    - "`OrderStatusWatcher` no longer writes to `lovata_orders_shopaholic_orders.meta_purchase_event_id` / `meta_purchase_event_time` columns — they're gone. The WR-12 atomic-CAS update + setRawAttributes sync block is fully deleted."
    - "`OrderStatusWatcher::handleUpdated` and `handleCreated` are each <70 LOC; new helper `alreadyDispatched(Order): bool` performs the EventLog existence check with SiteResolver-scoped where-clause (NULL site_id branch + non-null branch)."
    - "Refire-flag semantics: `refire_purchase_on_status_flip = ON` → watcher always calls `fireForwardDispatch` (UNIQUE collision in EventLogWriter handles the race); `OFF` (default) → watcher pre-checks `alreadyDispatched` to skip."
    - "Status-flip-flop never re-fires Purchase: the EventLog UNIQUE row persists, so a second dispatch with refire-off returns immediately via `alreadyDispatched`."
    - "Concurrent test: pre-insert a CAPI event_log row, then `SendCapiEvent::dispatchSync(...)` for same Order → MockHandler records ZERO HTTP requests, EventLog row count remains 1."
    - "DB-write failure during record(): drop the event_log table mid-test, dispatchSync → no exception leaks (EventLogWriter Throwable catch absorbs)."
    - "`composer qa` exits 0; coverage on rewritten files ≥ 90 %."
  artifacts:
    - path: "plugins/logingrupa/metapixelshopaholic/classes/queue/SendCapiEvent.php"
      provides: "Queue job with v1.1.0 race-fence pre-call. Constructor signature has 3 params (new `Order $obSubject`). All retry/dead-letter machinery unchanged."
      contains: "EventLogWriter::record"
    - path: "plugins/logingrupa/metapixelshopaholic/classes/event/OrderStatusWatcher.php"
      provides: "Status watcher with EventLog existence check replacing column-CAS. New `alreadyDispatched(Order): bool` helper. `fireForwardDispatch` <70 LOC."
      contains: "private function alreadyDispatched(Order $obOrder): bool"
    - path: "plugins/logingrupa/metapixelshopaholic/tests/Feature/SendCapiEventEventLogTest.php"
      provides: "3 invariants: race-winner records row + POSTs / race-loser no-POST / DB-failure no-cascade."
      contains: "final class SendCapiEventEventLogTest extends MetapixelTestCase"
    - path: "plugins/logingrupa/metapixelshopaholic/tests/Feature/OrderStatusWatcherEventLogTest.php"
      provides: "10 invariants (renamed from OrderStatusWatcherTest) asserting via EventLog rows instead of columns."
      contains: "final class OrderStatusWatcherEventLogTest extends MetapixelTestCase"
  key_links:
    - from: "classes/queue/SendCapiEvent.php::handle"
      to: "classes/helper/EventLogWriter.php::record"
      via: "static method call BEFORE MetaClient::send"
      pattern: "EventLogWriter::record\\("
    - from: "classes/event/OrderStatusWatcher.php::alreadyDispatched"
      to: "models/EventLog.php (where query scoped by SiteResolver)"
      via: "EventLog::where(...) chain"
      pattern: "EventLog::where\\('subject_type', Order::class\\)"
    - from: "classes/event/OrderStatusWatcher.php::fireForwardDispatch"
      to: "classes/queue/SendCapiEvent::dispatch"
      via: "DB::afterCommit() + SendCapiEvent::dispatch with 3 args including $obOrder"
      pattern: "SendCapiEvent::dispatch\\('Purchase', \\$arPayload, \\$obOrder\\)"
---

<objective>
Rewrite `SendCapiEvent::handle()` and `OrderStatusWatcher::fireForwardDispatch`
+ `handleUpdated` + `handleCreated` to use the new event_log race-fence
helper. Delete the WR-12/WR-13 column-CAS code. Add a new
`alreadyDispatched(Order)` helper using SiteResolver-scoped EventLog query.
Update `SendCapiEvent` constructor to accept `Order $obSubject` as the
v1.1.0 breaking change. Update the existing SendCapiEventTest to pass the
new constructor arg. Create new SendCapiEventEventLogTest locking
race-winner / race-loser / DB-failure paths. Rewrite OrderStatusWatcherTest
to assert via EventLog rows.

Purpose: server-side race-fence atomicity moves from the foreign-schema
column to the plugin-owned event_log UNIQUE constraint. Status flip-flops
NEVER re-fire because the EventLog row persists.

Output: 2 production file rewrites + 4 test file updates/creations.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@plugins/logingrupa/metapixelshopaholic/.planning/phases/03.1-event-log-refactor/BRIEF.md
@plugins/logingrupa/metapixelshopaholic/.planning/phases/03.1-event-log-refactor/03.1-PATTERNS.md
@plugins/logingrupa/metapixelshopaholic/.planning/phases/03.1-event-log-refactor/03.1-01-SUMMARY.md
@plugins/logingrupa/metapixelshopaholic/.planning/phases/03.1-event-log-refactor/03.1-02-SUMMARY.md
@plugins/logingrupa/metapixelshopaholic/classes/queue/SendCapiEvent.php
@plugins/logingrupa/metapixelshopaholic/classes/event/OrderStatusWatcher.php
@plugins/logingrupa/metapixelshopaholic/classes/helper/EventLogWriter.php
@plugins/logingrupa/metapixelshopaholic/classes/helper/SiteResolver.php
@plugins/logingrupa/metapixelshopaholic/models/EventLog.php
@plugins/logingrupa/metapixelshopaholic/classes/meta/PayloadBuilder.php
@plugins/logingrupa/metapixelshopaholic/tests/Feature/SendCapiEventTest.php
@plugins/logingrupa/metapixelshopaholic/tests/Feature/OrderStatusWatcherTest.php
@plugins/logingrupa/metapixelshopaholic/tests/MetapixelTestCase.php
@plugins/logingrupa/metapixelshopaholic/tests/Support/OrderFixtures.php

<interfaces>
From Wave 2 (Plan 03.1-02 — just shipped):

`Logingrupa\Metapixelshopaholic\Classes\Helper\EventLogWriter`:
- `public static function record(string $sEventId, string $sEventName, string $sChannel, object $obSubject, ?string $sSecretKey, int $iEventTime): bool`
- Returns true = race winner (this caller's INSERT created the row), false = race loser OR DB error.

`Logingrupa\Metapixelshopaholic\Classes\Helper\SiteResolver`:
- `public static function getActiveSiteId(): ?int`

`Logingrupa\Metapixelshopaholic\Models\EventLog`:
- Public constants: `CHANNEL_CAPI = 'capi'`, `CHANNEL_PIXEL = 'pixel'`, `EVENT_PURCHASE = 'Purchase'`.
- `$table = 'logingrupa_metapixel_event_log'`.
- Polymorphic `$morphTo = ['subject' =&gt; []]` + `subject(): MorphTo`.
- Fillable: event_id, event_name, channel, subject_type, subject_id, secret_key, site_id, event_time, fired_at.

Existing `SendCapiEvent` (Phase 3 — SCE-01..09 locks):
- `final class SendCapiEvent implements ShouldQueue` + 4 traits.
- `public readonly string $sEventName` + `public readonly array $arPayload` (constructor promotion).
- `$tries = 3` + `$backoff = [1, 4, 16]`.
- `handle(MetaClient $obClient): void` with try/catch routing transient → rethrow, permanent → writeFailedEvent.
- `failed(Throwable $obException): void` wraps non-Meta in MetaApiPermanentException.
- Private helpers: `writeFailedEvent`, `buildLogContext`.

Existing `OrderStatusWatcher` (Phase 3 — WR-01..13 locks):
- `final class` + `subscribe(Dispatcher)` binding `eloquent.updated` + `eloquent.created` on Order.
- `handleUpdated(Order)`, `handleCreated(Order)`, `fireForwardDispatch(Order)`.
- Helpers `intOrZero`, `stringOrEmpty`, `lookupStatusCode`, `isPluginDisabled`, `readPaidStatusCode`, `readRefireFlag`, `isAtPaidStatus`, `isAwayFromPaid`.
- WR-12 CAS update at the OLD `fireForwardDispatch`: `DB::table('lovata_orders_shopaholic_orders')-&gt;whereNull('meta_purchase_event_id')-&gt;update([...])` — DELETED in Phase 3.1.
- `PayloadBuilder::buildPurchaseEventPayload(Order, string $sUuid, int $iEventTime): array`.

Existing SendCapiEventTest setUp:
- `bindMetaClientWithMockResponses(array $arResponses): void` test helper (SCE-06).
- `Order` fixture provisioned via OrderFixtures::makePaidOrder().
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewrite SendCapiEvent::handle + add Order $obSubject constructor param</name>
  <files>
    plugins/logingrupa/metapixelshopaholic/classes/queue/SendCapiEvent.php (REWRITE — surgical),
    plugins/logingrupa/metapixelshopaholic/tests/Feature/SendCapiEventTest.php (UPDATE all 12 dispatch calls)
  </files>
  <read_first>
    plugins/logingrupa/metapixelshopaholic/.planning/phases/03.1-event-log-refactor/BRIEF.md (REFAC-06 lines 156-167),
    plugins/logingrupa/metapixelshopaholic/.planning/phases/03.1-event-log-refactor/03.1-PATTERNS.md (Pattern Assignment: SendCapiEvent.php REWRITE, lines 547-641),
    plugins/logingrupa/metapixelshopaholic/classes/queue/SendCapiEvent.php (the file itself — every SCE-01..09 lock plus the `handle()` body and constructor),
    plugins/logingrupa/metapixelshopaholic/tests/Feature/SendCapiEventTest.php (existing 12 test methods — every `new SendCapiEvent(...)` or `SendCapiEvent::dispatch(...)` / `dispatchSync(...)` call site).
  </read_first>
  <behavior>
    - First call to `SendCapiEvent::dispatchSync('Purchase', $arPayload, $obOrder)` records an EventLog row with `channel='capi'`, returns from handle() AFTER MockHandler captures the HTTP POST.
    - Second concurrent call (or retry) with same Order → `EventLogWriter::record` returns false → Log::info "lost race" → handle() returns WITHOUT calling `$obClient->send`.
    - Constructor with 2 args (Phase 3 signature) throws a clear `TypeError` / `ArgumentCountError` — the third param is REQUIRED. Test asserts this lock to make the v1.1.0 break explicit.
    - All Phase 3 retry-then-dead-letter behavior preserved: 3× retry on MetaApiTransientException; dead-letter via FailedEvent on permanent.
  </behavior>
  <action>
    Surgical rewrite of `classes/queue/SendCapiEvent.php`:

    (a) ADD constructor parameter `public readonly Lovata\OrdersShopaholic\Models\Order $obSubject` — third parameter after `$sEventName` and `$arPayload`. Required (no default value). Mirror PATTERNS.md lines 630-636. Order is SerializesModels-compatible, so readonly survives queue rehydrate.

    (b) ADD imports:
      - `use Logingrupa\Metapixelshopaholic\Classes\Helper\EventLogWriter;`
      - `use Logingrupa\Metapixelshopaholic\Models\EventLog;`
      - `use Lovata\OrdersShopaholic\Models\Order;`

    (c) REWRITE `handle(MetaClient $obClient): void` head — insert race-fence pre-call BEFORE the existing try block:
      - First line of method body: `if (!$this->raceFenceWon()) { Log::info('Metapixel CAPI dispatch lost race — peer already POSTed', $this->buildLogContext()); return; }`.
      - Everything else (try { $obClient->send; Log::info success; } catch MetaApiTransientException { writeFailedEvent path doesn't run; rethrow; } catch MetaApiPermanentException | MissingPixelConfigException | MissingCapiTokenException { writeFailedEvent + no rethrow; }) STAYS unchanged.

    (d) ADD private helper `raceFenceWon(): bool` (mirror PATTERNS.md lines 596-616):
      - Read event_id + event_time from `$this->arPayload` via existing helper `extractFirstEvent()` (the file already has it — MOD-01 pattern from STATE.md).
      - Narrow `$sEventId = is_string($arFirst['event_id'] ?? null) ? $arFirst['event_id'] : '';`
      - Narrow `$iEventTime = is_int($arFirst['event_time'] ?? null) ? $arFirst['event_time'] : 0;`
      - Guard: if `$sEventId === ''` OR `$iEventTime === 0` → return false (malformed payload → refuse to dispatch).
      - Return `EventLogWriter::record($sEventId, $this->sEventName, EventLog::CHANNEL_CAPI, $this->obSubject, $this->stringOrNull($this->obSubject->getAttribute('secret_key')), $iEventTime);`
      - `stringOrNull` is an existing local narrowing helper OR add it inline as `is_string($mVal) && $mVal !== '' ? $mVal : null` per the PHPSTAN-01 narrowing convention. If a `stringOrNull` doesn't exist on the class, add it (mirror OrderStatusWatcher's intOrZero/stringOrEmpty narrowing pattern).

    (e) UPDATE `tests/Feature/SendCapiEventTest.php`:
      - Every `SendCapiEvent::dispatch(...)` or `dispatchSync(...)` or `new SendCapiEvent(...)` call must now pass 3 args. Use `OrderFixtures::makePaidOrder()` to obtain `$obOrder`, then `SendCapiEvent::dispatchSync('Purchase', $arPayload, $obOrder)`.
      - Every test setUp must call `$this->bootEventLogTable();` because handle() now writes to event_log.
      - Add a "constructor-arg-count" assertion test: `test_constructor_requires_order_subject_parameter` — assert `new SendCapiEvent('Purchase', [])` raises `TypeError` (PHP 8.4 strict mode catches missing required arg). This locks the v1.1.0 break.
      - Existing 12 tests continue to pass with the additional `$obOrder` arg.

    All SCE-01..09 locks preserved verbatim. The plan SUMMARY must document the SCE-03 break explicitly: "Phase 3.1 v1.1.0 breaking change — SendCapiEvent constructor takes 3 args, third is `Order $obSubject` required for `EventLogWriter::record` subject parameter."

    No fenced code in action body — read PATTERNS.md lines 555-617 for the exact head rewrite.
  </action>
  <verify>
    <automated>
      cd plugins/logingrupa/metapixelshopaholic
      &amp;&amp; grep -q "public readonly Order \$obSubject" classes/queue/SendCapiEvent.php
      &amp;&amp; grep -q "EventLogWriter::record" classes/queue/SendCapiEvent.php
      &amp;&amp; grep -q "EventLog::CHANNEL_CAPI" classes/queue/SendCapiEvent.php
      &amp;&amp; grep -q "private function raceFenceWon" classes/queue/SendCapiEvent.php
      &amp;&amp; grep -q "Metapixel CAPI dispatch lost race" classes/queue/SendCapiEvent.php
      &amp;&amp; grep -q "use Logingrupa\\\\Metapixelshopaholic\\\\Classes\\\\Helper\\\\EventLogWriter;" classes/queue/SendCapiEvent.php
      &amp;&amp; vendor/bin/pest tests/Feature/SendCapiEventTest.php
      &amp;&amp; composer pint-test
      &amp;&amp; composer analyse
    </automated>
  </verify>
  <acceptance_criteria>
    - SendCapiEvent constructor has 3 params; 3rd is `public readonly Lovata\OrdersShopaholic\Models\Order $obSubject`.
    - `handle(MetaClient)` first body statement is the `raceFenceWon()` guard.
    - When `raceFenceWon` returns false → Log::info "lost race" + return. NO `$obClient->send` call.
    - `raceFenceWon` private helper reads event_id + event_time from `$this->arPayload`, narrows to typed scalars, calls `EventLogWriter::record(...)` with all 6 args including `EventLog::CHANNEL_CAPI` constant.
    - All 12 existing tests in SendCapiEventTest pass with the updated 3-arg constructor.
    - New `test_constructor_requires_order_subject_parameter` asserts the v1.1.0 breaking change.
    - `composer pint-test` + `composer analyse` clean.
    - phpmd cyclomatic complexity stays ≤ 10 per method (raceFenceWon extraction handles this).
  </acceptance_criteria>
  <done>
    REFAC-06 atomic commit. SendCapiEvent gates HTTP POST behind EventLog UNIQUE-constraint race-fence. Constructor break is documented in commit message AS the v1.1.0 minor-bump rationale.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewrite OrderStatusWatcher — delete column-CAS, add alreadyDispatched + EventLog existence check</name>
  <files>
    plugins/logingrupa/metapixelshopaholic/classes/event/OrderStatusWatcher.php (REWRITE — surgical),
    plugins/logingrupa/metapixelshopaholic/tests/Feature/OrderStatusWatcherTest.php (DELETE),
    plugins/logingrupa/metapixelshopaholic/tests/Feature/OrderStatusWatcherEventLogTest.php (NEW — port the 10 existing methods)
  </files>
  <read_first>
    plugins/logingrupa/metapixelshopaholic/.planning/phases/03.1-event-log-refactor/BRIEF.md (REFAC-07 lines 169-182),
    plugins/logingrupa/metapixelshopaholic/.planning/phases/03.1-event-log-refactor/03.1-PATTERNS.md (Pattern Assignment: OrderStatusWatcher REWRITE, lines 644-786; OrderStatusWatcherEventLogTest UPDATE, lines 1165-1192),
    plugins/logingrupa/metapixelshopaholic/classes/event/OrderStatusWatcher.php (the file itself — `handleUpdated` lines 100-170 with refire-away-clear block lines 138-164; `fireForwardDispatch` lines 232-304 with WR-12 atomic-CAS update),
    plugins/logingrupa/metapixelshopaholic/tests/Feature/OrderStatusWatcherTest.php (existing 10 test methods — preserve every method name).
  </read_first>
  <behavior>
    - `handleUpdated(Order)` returns early when plugin disabled, when order not at paid status, OR when `alreadyDispatched(Order)` returns true with refire-off.
    - `handleCreated(Order)` mirrors the same gating; no longer reads `meta_purchase_event_id` column.
    - `fireForwardDispatch(Order)` generates UUIDv4 + event_time, builds payload, dispatches `SendCapiEvent::dispatch('Purchase', $arPayload, $obOrder)` via `DB::afterCommit(static function () { ... })`. NO column write. Under 70 LOC.
    - `alreadyDispatched(Order): bool` reads EventLog scoped by SiteResolver — when site_id is null uses `whereNull('site_id')`, otherwise `where('site_id', $iSiteId)`.
    - Refire-on: watcher always proceeds; UNIQUE-constraint collision in EventLogWriter handles same-order rebroadcast safely.
    - All Phase 3 WR-* locks preserved EXCEPT WR-12 (atomic-CAS update) + the refire-away-clear column-clearing branch — both deleted as obsolete with the columns.
  </behavior>
  <action>
    (A) Surgical rewrite of `classes/event/OrderStatusWatcher.php`:

    1. DELETE the refire-away-clear block (lines 138-164 of current file — the `DB::table('lovata_orders_shopaholic_orders')->where(...)->update([... null both columns ...])` chain). The columns are gone; clearing them is a no-op. Replace the whole code path with the new gating per PATTERNS.md lines 656-680.

    2. REWRITE `handleUpdated(Order)`:
       - First: `if ($this->isPluginDisabled()) return;`
       - `$sPaidCode = $this->readPaidStatusCode();`
       - `if (! $this->isAtPaidStatus($obOrder, $sPaidCode)) return;`
       - Idempotency gate: `if (! $this->readRefireFlag() && $this->alreadyDispatched($obOrder)) return;`
       - `$this->fireForwardDispatch($obOrder);`

    3. REWRITE `handleCreated(Order)`:
       - Drop the legacy `meta_purchase_event_id !== null` guard (line ~199-201).
       - Same shape as `handleUpdated` but always uses `readPaidStatusCode()` directly: per PATTERNS.md lines 686-699.

    4. REWRITE `fireForwardDispatch(Order)` per PATTERNS.md lines 714-746:
       - Delete WR-12 atomic CAS block + setRawAttributes sync.
       - Keep `$sUuid = Uuid::uuid4()->toString();`, `$iEventTime = time();`, `$iOrderId = $this->intOrZero($obOrder->getAttribute('id'));`, the `if ($iOrderId &lt;= 0) return;` guard.
       - Keep PayloadBuilder call inside try/catch on `MetaPixelException` — Log::warning + return on precondition failure.
       - Keep `DB::afterCommit(static function () use (...) { ... })` for WR-13 transaction safety.
       - The dispatch becomes `SendCapiEvent::dispatch('Purchase', $arPayload, $obOrder);` (3 args).
       - Total method ≤ 35 lines per PATTERNS.md line 701.

    5. NEW private method `alreadyDispatched(Order $obOrder): bool` per PATTERNS.md lines 753-773:
       - Narrow `$iSubjectId = $this->intOrZero($obOrder->getAttribute('id'));` + zero-guard.
       - `$iSiteId = SiteResolver::getActiveSiteId();`
       - Build query: `EventLog::where('subject_type', Order::class)->where('subject_id', $iSubjectId)->where('event_name', EventLog::EVENT_PURCHASE)->where('channel', EventLog::CHANNEL_CAPI);`
       - Apply site_id branch: `if ($iSiteId === null) { $obQuery->whereNull('site_id'); } else { $obQuery->where('site_id', $iSiteId); }`
       - Return `$obQuery->exists();`.

    6. UPDATE imports:
       - ADD: `use Logingrupa\Metapixelshopaholic\Classes\Helper\SiteResolver;`
       - ADD: `use Logingrupa\Metapixelshopaholic\Models\EventLog;`
       - KEEP: `use Illuminate\Support\Facades\DB;` (still needed for `DB::afterCommit`).
       - REMOVE: any column-name string literal `'meta_purchase_event_id'` / `'meta_purchase_event_time'` (grep first; should reduce to zero matches after rewrite).

    7. KEEP every Phase 3 helper: `intOrZero`, `stringOrEmpty`, `lookupStatusCode`, `isPluginDisabled`, `readPaidStatusCode`, `readRefireFlag`, `isAtPaidStatus`, `isAwayFromPaid`. `isAwayFromPaid` may become unused now that the refire-away-clear path is gone — if so, DELETE it (Tiger-Style "no dead code"). Grep usages first.

    (B) TEST FILES:

    8. DELETE the existing `tests/Feature/OrderStatusWatcherTest.php` (`git rm`). Its assertions read the dropped columns; the new test file supersedes it.

    9. CREATE `tests/Feature/OrderStatusWatcherEventLogTest.php` — port ALL 10 existing test method names verbatim (mirror PATTERNS.md lines 1167-1192). For each, replace `$obOrder->fresh()->meta_purchase_event_id` assertions with:
       ```
       $obRow = EventLog::where('subject_type', Order::class)
           ->where('subject_id', $obOrder->id)
           ->where('event_name', EventLog::EVENT_PURCHASE)
           ->where('channel', EventLog::CHANNEL_CAPI)
           ->first();
       $this->assertNotNull($obRow);
       $this->assertTrue(Uuid::isValid((string) $obRow->event_id));
       ```
       (See PATTERNS.md lines 1183-1191.)

       Setup: include `$this->bootEventLogTable()`. Use `Queue::fake()` + `Queue::assertPushed(SendCapiEvent::class, fn ($obJob): bool =&gt; $obJob->sEventName === 'Purchase' && $obJob->obSubject->id === $obOrder->id)` patterns where appropriate.

       For the refire-flip test, assert `EventLog::count()` (1 vs 2) instead of column-clear semantics.

       Class declared `final class OrderStatusWatcherEventLogTest extends MetapixelTestCase`.
  </action>
  <verify>
    <automated>
      cd plugins/logingrupa/metapixelshopaholic
      &amp;&amp; ! test -f tests/Feature/OrderStatusWatcherTest.php
      &amp;&amp; test -f tests/Feature/OrderStatusWatcherEventLogTest.php
      &amp;&amp; ! grep -n "meta_purchase_event_id" classes/event/OrderStatusWatcher.php
      &amp;&amp; ! grep -n "meta_purchase_event_time" classes/event/OrderStatusWatcher.php
      &amp;&amp; grep -q "private function alreadyDispatched(Order \$obOrder): bool" classes/event/OrderStatusWatcher.php
      &amp;&amp; grep -q "SendCapiEvent::dispatch('Purchase', \$arPayload, \$obOrder);" classes/event/OrderStatusWatcher.php
      &amp;&amp; grep -q "EventLog::EVENT_PURCHASE" classes/event/OrderStatusWatcher.php
      &amp;&amp; grep -q "SiteResolver::getActiveSiteId" classes/event/OrderStatusWatcher.php
      &amp;&amp; vendor/bin/pest tests/Feature/OrderStatusWatcherEventLogTest.php
      &amp;&amp; composer pint-test
      &amp;&amp; composer analyse
      &amp;&amp; composer phpmd
    </automated>
  </verify>
  <acceptance_criteria>
    - No `meta_purchase_event_id` or `meta_purchase_event_time` references in OrderStatusWatcher.php.
    - `handleUpdated`, `handleCreated`, `fireForwardDispatch`, `alreadyDispatched` ALL under 70 LOC each (Tiger-Style).
    - `alreadyDispatched` correctly branches on SiteResolver null vs int.
    - `SendCapiEvent::dispatch` called with 3 args.
    - All 10 ported test method names present and passing.
    - Old `OrderStatusWatcherTest.php` removed from disk (verified by `! test -f`).
    - `composer phpmd` clean — cyclomatic complexity ≤ 10 per method (alreadyDispatched extraction handles this).
    - `composer analyse` clean — phpstan level 10 + the Model|null narrowing pattern from MC-05.
  </acceptance_criteria>
  <done>
    REFAC-07 atomic commit. Watcher decides dispatch via EventLog existence check; race-fence atomicity lives entirely in SendCapiEvent → EventLogWriter. Status flip-flop never re-fires (the EventLog row persists). All 10 PAY-03 invariants re-locked via EventLog assertions.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: NEW SendCapiEventEventLogTest — race-winner / race-loser / DB-failure invariants</name>
  <files>
    plugins/logingrupa/metapixelshopaholic/tests/Feature/SendCapiEventEventLogTest.php (NEW)
  </files>
  <read_first>
    plugins/logingrupa/metapixelshopaholic/.planning/phases/03.1-event-log-refactor/BRIEF.md (REFAC-11 lines 249-254 — SendCapiEventEventLogTest scope),
    plugins/logingrupa/metapixelshopaholic/.planning/phases/03.1-event-log-refactor/03.1-PATTERNS.md (Pattern Assignment: SendCapiEventEventLogTest, lines 1134-1141),
    plugins/logingrupa/metapixelshopaholic/tests/Feature/SendCapiEventTest.php (existing — `bindMetaClientWithMockResponses` helper at SCE-06; `dispatchSync` patterns; `Mockery::on(closure)` capture-by-reference patterns at SCE-05),
    plugins/logingrupa/metapixelshopaholic/classes/queue/SendCapiEvent.php (just rewritten in Task 1 — now has the raceFenceWon guard),
    plugins/logingrupa/metapixelshopaholic/classes/helper/EventLogWriter.php (Wave 2 — the helper),
    plugins/logingrupa/metapixelshopaholic/models/EventLog.php (Wave 2).
  </read_first>
  <behavior>
    - `test_first_dispatch_records_capi_row_and_posts_to_meta`: `Queue::fake()` OR `dispatchSync`; after dispatch `EventLog::where(channel='capi')->count() === 1` AND the MockHandler captured exactly 1 POST.
    - `test_second_concurrent_dispatch_returns_false_no_http_post`: pre-insert a CAPI event_log row for the same Order; dispatchSync second SendCapiEvent → MockHandler captured ZERO POSTs + Log::shouldHaveReceived('info', /lost race/)-recordable via capture-buffer pattern (SCE-05). `EventLog::count() === 1`.
    - `test_db_write_failure_during_record_does_not_cascade`: drop the `logingrupa_metapixel_event_log` table mid-test (mirror SendCapiEventTest line ~196 pattern for failed_events); dispatchSync → no exception leaks (EventLogWriter Throwable catch absorbs); MockHandler captured ZERO POSTs.
  </behavior>
  <action>
    Create `tests/Feature/SendCapiEventEventLogTest.php`. `declare(strict_types=1);`, namespace `Logingrupa\Metapixelshopaholic\Tests\Feature`.

    Imports: `Cache`, `Carbon\Carbon`, `GuzzleHttp\Handler\MockHandler`, `GuzzleHttp\Psr7\Response`, `Illuminate\Support\Facades\Log`, `Illuminate\Support\Facades\Schema`, `Logingrupa\Metapixelshopaholic\Classes\Helper\PluginGuard`, `Logingrupa\Metapixelshopaholic\Classes\Meta\MetaClient`, `Logingrupa\Metapixelshopaholic\Classes\Queue\SendCapiEvent`, `Logingrupa\Metapixelshopaholic\Models\EventLog`, `Logingrupa\Metapixelshopaholic\Models\Settings`, `Logingrupa\Metapixelshopaholic\Tests\MetapixelTestCase`, `Logingrupa\Metapixelshopaholic\Tests\Support\OrderFixtures`, `Lovata\OrdersShopaholic\Models\Order`.

    Class: `final class SendCapiEventEventLogTest extends MetapixelTestCase`.

    `setUp(): void`:
    - `parent::setUp();`
    - `$this->bootSystemSettings();`
    - `$this->bootOrdersStatuses();`
    - `$this->bootOrdersTable();`
    - `$this->bootEventLogTable();` ← NEW
    - `OrderFixtures::provisionHermeticOfferProductTables();`
    - Reflection-priming Settings: `pixel_id`, `capi_access_token`, `paid_status_code = 'new-payment-received'` (PH-04 + MC-02 pattern).
    - `Cache::flush(); Settings::clearInternalCache(); PluginGuard::flush();`

    Provide private helper `dispatchAgainstMockHandler(array $arResponses): array` returning `[$obJob, $arHistory]` (MC-04 — explicit by-ref helper to avoid the array-destructuring by-ref pitfall).

    3 test methods per the behavior block. Each one:
    - Uses `OrderFixtures::makePaidOrder()` for `$obOrder`.
    - Builds payload via `PayloadBuilder` or fixture-array (whichever is shorter — match SendCapiEventTest's existing pattern).
    - For test 1: assert MockHandler has 1 request + `EventLog::where('channel', 'capi')->count() === 1` + the row's event_id matches the payload's event_id.
    - For test 2: pre-insert EventLog row directly with `EventLog::create([...])` to simulate race-winner; then dispatchSync → MockHandler has 0 requests + `EventLog::count() === 1`. Capture Log::info via a buffer-mockery closure per SCE-05.
    - For test 3: `Schema::drop('logingrupa_metapixel_event_log');` mid-setUp; dispatchSync → assert no `Throwable` leaks (the test passes iff dispatchSync returns normally) + MockHandler has 0 requests + Log::critical captured.

    Hungarian throughout.
  </action>
  <verify>
    <automated>
      cd plugins/logingrupa/metapixelshopaholic
      &amp;&amp; test -f tests/Feature/SendCapiEventEventLogTest.php
      &amp;&amp; grep -q "final class SendCapiEventEventLogTest extends MetapixelTestCase" tests/Feature/SendCapiEventEventLogTest.php
      &amp;&amp; grep -q "test_first_dispatch_records_capi_row_and_posts_to_meta" tests/Feature/SendCapiEventEventLogTest.php
      &amp;&amp; grep -q "test_second_concurrent_dispatch_returns_false_no_http_post" tests/Feature/SendCapiEventEventLogTest.php
      &amp;&amp; grep -q "test_db_write_failure_during_record_does_not_cascade" tests/Feature/SendCapiEventEventLogTest.php
      &amp;&amp; vendor/bin/pest tests/Feature/SendCapiEventEventLogTest.php
      &amp;&amp; composer pint-test
      &amp;&amp; composer analyse
    </automated>
  </verify>
  <acceptance_criteria>
    - 3 test methods present and pass via Pest.
    - Test 1 asserts both MockHandler request count (1) AND `EventLog::count()` (1).
    - Test 2 asserts both MockHandler request count (0) AND `EventLog::count()` (1) AND Log::info captured "lost race" (use SCE-05 capture-by-reference pattern, NOT `Log::shouldHaveReceived`).
    - Test 3 drops event_log table BEFORE dispatch; dispatchSync returns without throwing; MockHandler request count is 0; Log::critical captured.
    - `composer pint-test` + `composer analyse` clean.
  </acceptance_criteria>
  <done>
    REFAC-11 set 2 (SendCapiEventEventLogTest) locked. The three race-fence invariants — winner, loser, DB-failure — are codified. Plan SUMMARY records that the legacy SendCapiEventTest (Task 1's 12 updated tests) covers the queue-job retry/dead-letter machinery; SendCapiEventEventLogTest covers the new race-fence semantics. Both files coexist.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OrderStatusWatcher → DB transaction | `DB::afterCommit` defers `SendCapiEvent::dispatch` so a rolled-back order does NOT trigger a CAPI event (WR-13 lock preserved). |
| SendCapiEvent → EventLogWriter | The race-fence is the only thing standing between concurrent dispatches and double-billing the Meta CAPI quota. EventLogWriter's bool return is contract-load-bearing. |
| Queue worker → EventLog table | If the event_log table is unreachable mid-job, the worker must NOT cascade-fail or push the message to dead-letter prematurely. Tiger-Style boundary catch in EventLogWriter handles this. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3.1-12 | Tampering | SendCapiEvent constructor break | accept | The v1.1.0 minor bump documents the SCE-03 break. No code outside this plugin instantiates SendCapiEvent. |
| T-3.1-13 | Repudiation | OrderStatusWatcher dispatch decision | mitigate | EventLog row's `fired_at` + `created_at` + the `event_id` UUID are the audit trail. `Log::info('Metapixel: Purchase dispatched', [...])` line preserved from Phase 3 logs every dispatch with order_id + order_number + event_id. |
| T-3.1-14 | Denial of Service | Race-fence on hot path | mitigate | `insertOrIgnore` is a single round-trip; no SELECT-then-INSERT round. Latency is comparable to the legacy column-CAS UPDATE. |
| T-3.1-15 | Information Disclosure | EventLogWriter logs on DB failure | mitigate | Log::critical is operator-only telemetry. No PII (event_id is UUIDv4, order id is integer). |
| T-3.1-16 | Spoofing | OrderStatusWatcher::alreadyDispatched | mitigate | Site-scope via SiteResolver prevents one site's "already dispatched" decision from suppressing another site's legitimate dispatch (multi-site contract). |
| T-3.1-17 | Elevation | Order::secret_key read in SendCapiEvent | accept | secret_key is an existing Lovata column visible to any code with model access. Phase 3.1 only READS it, never writes. The secret_key is server-only and never exposed to clients beyond /checkout/{slug} URL routing. |
</threat_model>

<verification>
After all 3 tasks:
1. `composer qa` exits 0.
2. `vendor/bin/pest tests/Feature/SendCapiEventTest.php tests/Feature/SendCapiEventEventLogTest.php tests/Feature/OrderStatusWatcherEventLogTest.php` — all green (12 + 3 + 10 = 25 test methods).
3. Coverage on rewritten files: `SendCapiEvent.php` ≥ 90 % (was 100 % in Phase 3 — adding raceFenceWon + buildLogContext expansion may dip slightly; acceptable as long as ≥ 90 %), `OrderStatusWatcher.php` ≥ 90 % (was 90.3 % in Phase 3; new alreadyDispatched is fully covered by OrderStatusWatcherEventLogTest).
4. No surviving references to `meta_purchase_event_id` or `meta_purchase_event_time` in the entire `classes/` tree (`grep -rn 'meta_purchase_event_' classes/` returns 0).
</verification>

<success_criteria>
- SendCapiEvent constructor takes 3 args; race-fence pre-call lives in `raceFenceWon` private helper before `MetaClient::send`.
- OrderStatusWatcher's column-CAS code is gone; `alreadyDispatched` uses SiteResolver-scoped EventLog query.
- All 10 OrderStatusWatcher invariants ported to EventLog row assertions.
- New SendCapiEventEventLogTest locks the race-winner / race-loser / DB-failure paths.
- Phase-3 SendCapiEventTest still green with the 3-arg constructor.
- `composer qa` green.
</success_criteria>

<output>
After completion, create `.planning/phases/03.1-event-log-refactor/03.1-03-SUMMARY.md` with:
- File-by-file diff summary: SendCapiEvent (+ raceFenceWon, + Order $obSubject param), OrderStatusWatcher (- WR-12 CAS, + alreadyDispatched).
- Explicit v1.1.0 SCE-03 break section — quote the old + new constructor signatures side-by-side.
- LOC accounting: each method's new line count (must be &lt;70 per Tiger-Style).
- Coverage delta on the two production files.
- Forward note to Wave-3 Plan 03.1-04 — PurchasePixel rewrite consumes the same EventLog query pattern (findEventLogRow mirrors alreadyDispatched). Reference the helper if useful, but PurchasePixel needs its own EventLog row return (not just exists()), so the helpers don't reuse 1-for-1.
</output>
