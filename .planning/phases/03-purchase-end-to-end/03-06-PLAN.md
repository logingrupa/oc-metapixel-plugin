---
id: 03-06
phase: 03-purchase-end-to-end
plan: 06
type: execute
wave: 4
depends_on:
  - 03-01
  - 03-04
  - 03-05
files_modified:
  - plugins/logingrupa/metapixelshopaholic/classes/event/OrderStatusWatcher.php
  - plugins/logingrupa/metapixelshopaholic/Plugin.php
  - plugins/logingrupa/metapixelshopaholic/components/PurchasePixel.php
  - plugins/logingrupa/metapixelshopaholic/components/purchasepixel/default.htm
  - plugins/logingrupa/metapixelshopaholic/tests/Feature/OrderStatusWatcherTest.php
  - plugins/logingrupa/metapixelshopaholic/tests/Feature/PurchasePixelTest.php
  - plugins/logingrupa/metapixelshopaholic/tests/MetapixelTestCase.php
  - plugins/logingrupa/metapixelshopaholic/models/settings/fields.yaml
autonomous: false
requirements:
  - PAY-03
  - PAY-10
  - PAY-11
rationale: >
  Wave-4 wiring layer: OrderStatusWatcher (the actual dispatch site) +
  Plugin.php boot subscription + a manual staging verification checkpoint
  for PAY-10 + PAY-11 (Meta Test Events dedup % + EMQ + bank-transfer
  edge case — these acceptance criteria cannot be automated, only
  manually verified against real Meta Events Manager). Depends on 03-01
  (meta_purchase_event_id column + FailedEvent), 03-04 (PayloadBuilder
  for the dispatched payload), 03-05 (SendCapiEvent for the queue
  dispatch). Does NOT directly import MetaClient (03-03) — that's
  resolved via Laravel container inside SendCapiEvent::handle. Manual
  checkpoint task is autonomous=false (BLOCKING gate).
must_haves:
  truths:
    - "`OrderStatusWatcher::handleUpdated(Order)` fires `SendCapiEvent::dispatch('Purchase', $arPayload)` exactly once when an Order transitions to `status->code === Settings::get('paid_status_code', 'new-payment-received')` AND `meta_purchase_event_id IS NULL`."
    - "The UUIDv4 event_id is generated via `Ramsey\\Uuid\\Uuid::uuid4()->toString()` and persisted to `$obOrder->meta_purchase_event_id` via `$obOrder->saveQuietly()` BEFORE dispatch. `$iEventTime = time();` is also persisted to `$obOrder->meta_purchase_event_time` in the same `saveQuietly()` call so the browser-side Pixel twin (PurchasePixel component) reads the SAME event_time as the CAPI dispatch."
    - "PluginGuard short-circuit (`if (App::make('metapixel.disabled')) return;`) is the first statement in every handler entry."
    - "Status flip-flop with `refire_purchase_on_status_flip = false` (default): paid → other → paid fires Purchase exactly once (the second flip is a no-op because meta_purchase_event_id is non-null)."
    - "Status flip-flop with `refire_purchase_on_status_flip = true`: paid → other → paid fires twice (the away-transition clears BOTH the column and the event_time via saveQuietly)."
    - "`handleCreated(Order)` covers admin-created-already-paid orders (CONTEXT Area 2 Q2 — imports, seeds, manual entry)."
    - "`Plugin::boot()` registers `Event::subscribe(OrderStatusWatcher::class)` BEFORE the CLI-only gate so backend admin status-flip dispatches CAPI (PAY-11 bank-transfer path)."
    - "`PurchasePixel` component is registered in `Plugin::registerComponents()` with alias `purchasePixel`. When rendered on the thank-you page (`order-complete.htm`) with a paid order, it emits `<script>fbq('track', 'Purchase', custom_data, {eventID: $obOrder->meta_purchase_event_id})</script>` reading the persisted event_id + event_time so Pixel + CAPI dedup on Meta's side."
    - "Manual staging verification (PAY-10) confirms ≥ 80% dedup AND ≥ 8 EMQ in Meta Test Events for a real PayPal Purchase (Pixel + CAPI deduplicated end-to-end). PAY-11 confirms bank-transfer admin-flipped fires single-channel CAPI."
    - "`composer qa` exits 0 with all Phase-3 tests passing."
    - "Pixel ID regex validator (T-04-01 retro-fit from 02-04 PH-01) added to `models/settings/fields.yaml` — defense-in-depth against malicious admin breaking out of the inlined `<script>` string."
  artifacts:
    - path: "classes/event/OrderStatusWatcher.php"
      provides: "subscribe(Dispatcher) + handleUpdated(Order) + handleCreated(Order). PluginGuard short-circuit, status-fence + meta_purchase_event_id-fence guards, UUIDv4 generation, saveQuietly persistence, SendCapiEvent::dispatch."
      contains: "class OrderStatusWatcher"
    - path: "Plugin.php"
      provides: "Modified boot() to `Event::subscribe(OrderStatusWatcher::class)` before the CLI gate. Modified registerComponents() to register PurchasePixel as `purchasePixel`."
      contains: "Event::subscribe(OrderStatusWatcher::class)"
    - path: "components/PurchasePixel.php"
      provides: "Theme component for thank-you page. Reads Order by slug via existing OrderPage component data, checks `meta_purchase_event_id` is non-null, builds `$arMetaEvent = {event_id, event_time, event_name: 'Purchase', custom_data}` via PayloadBuilder::buildPurchaseEventPayload + extract custom_data slice. Twig partial emits `fbq('track', 'Purchase', custom_data, {eventID})` with the persisted UUID — Pixel side of the dedup contract."
      contains: "class PurchasePixel"
    - path: "components/purchasepixel/default.htm"
      provides: "Twig partial — emits the `fbq('track', 'Purchase', …, {eventID})` script with persisted event_id + event_time."
      contains: "fbq('track', 'Purchase'"
    - path: "tests/Feature/PurchasePixelTest.php"
      provides: "Locks: paid order with stored event_id renders fbq('track','Purchase',…,{eventID}); non-paid order renders nothing; missing event_id renders nothing; event_time matches persisted column."
      contains: "PurchasePixelTest"
    - path: "tests/Feature/OrderStatusWatcherTest.php"
      provides: "Locks: fresh-paid-dispatches-once, same-status-update-noop, refire=true flip-flop fires twice, refire=false flip-flop fires once, plugin-disabled-no-dispatch, admin-created-paid-fires, no-currency-no-dispatch-soft-skip."
      contains: "OrderStatusWatcherTest"
    - path: "tests/MetapixelTestCase.php (extend)"
      provides: "bootOrdersTable() (added 03-01) gets meta_purchase_event_id column appended to match Phase-3 schema; dropHermeticSchemas extended to clean fixture-side offer/product tables."
    - path: "models/settings/fields.yaml (modify)"
      provides: "Adds `regex: '/^\\d{6,20}$/'` validator to `pixel_id` field (PH-01 from 02-04 SUMMARY)."
  key_links:
    - from: "Plugin.php::boot"
      to: "classes/event/OrderStatusWatcher.php"
      via: "Event::subscribe(OrderStatusWatcher::class) before CLI gate"
      pattern: "Event::subscribe\\(OrderStatusWatcher"
    - from: "Plugin.php::registerComponents"
      to: "components/PurchasePixel.php"
      via: "PurchasePixel::class => 'purchasePixel'"
      pattern: "PurchasePixel::class"
    - from: "OrderStatusWatcher::handleUpdated"
      to: "lovata_orders_shopaholic_orders.meta_purchase_event_id"
      via: "$obOrder->meta_purchase_event_id = $sUuid; $obOrder->meta_purchase_event_time = $iEventTime; $obOrder->saveQuietly();"
      pattern: "saveQuietly"
    - from: "OrderStatusWatcher::handleUpdated"
      to: "classes/queue/SendCapiEvent.php"
      via: "SendCapiEvent::dispatch('Purchase', $arPayload)"
      pattern: "SendCapiEvent::dispatch"
    - from: "OrderStatusWatcher::handleUpdated"
      to: "classes/meta/PayloadBuilder.php"
      via: "(new PayloadBuilder())->buildPurchaseEventPayload($obOrder, $sUuid, $iEventTime)"
      pattern: "PayloadBuilder"
    - from: "components/PurchasePixel.php::onRun"
      to: "lovata_orders_shopaholic_orders.meta_purchase_event_id + meta_purchase_event_time"
      via: "$obOrder->meta_purchase_event_id (persisted UUID) + $obOrder->meta_purchase_event_time (persisted timestamp) used in fbq(...,{eventID}) and custom_data.event_time"
      pattern: "meta_purchase_event_id"
    - from: "components/purchasepixel/default.htm"
      to: "Meta Pixel browser side"
      via: "fbq('track', 'Purchase', custom_data, {eventID: arMetaEvent.event_id})"
      pattern: "fbq\\('track', 'Purchase'"
---

<objective>
Wire Phase 3 end-to-end: the OrderStatusWatcher event handler dispatches the Purchase CAPI event with DB-level idempotency, the Plugin.php boot subscribes the handler so it fires across storefront + backend + queue-worker contexts, the test suite locks every status-transition scenario, and a final manual staging checkpoint validates the dedup % / EMQ acceptance criteria that cannot be automated.

Purpose: Close the loop. Plans 03-01..03-05 ship the parts; this plan composes them into a working pipeline. The PAY-11 bank-transfer / admin-marked-paid edge case is the killer feature of Phase 3 — previously these orders never reached Meta because no browser session existed. OrderStatusWatcher fires the CAPI-only event from the backend request lifecycle.

Output: One handler class, one Plugin.php diff, one Feature-tier test suite, one MetapixelTestCase extension, one Settings YAML edit (PH-01 retro-fit), and one manual staging verification checkpoint blocking phase closure.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/forge/nailscosmetics.lv/plugins/logingrupa/metapixelshopaholic/CLAUDE.md
@/home/forge/nailscosmetics.lv/plugins/logingrupa/metapixelshopaholic/.planning/PROJECT.md
@/home/forge/nailscosmetics.lv/plugins/logingrupa/metapixelshopaholic/.planning/REQUIREMENTS.md
@/home/forge/nailscosmetics.lv/plugins/logingrupa/metapixelshopaholic/.planning/phases/03-purchase-end-to-end/03-CONTEXT.md
@/home/forge/nailscosmetics.lv/plugins/logingrupa/metapixelshopaholic/.planning/phases/03-purchase-end-to-end/03-PATTERNS.md

<interfaces>
<!-- From 03-04: -->
```php
class PayloadBuilder {
    public function buildPurchaseEventPayload(Order $obOrder, string $sEventId, int $iEventTime): array;
    // throws InvalidEventIdException | OrderHasNoCurrencyException | OrderHasNoItemsException
}
```

<!-- From 03-05: -->
```php
final class SendCapiEvent implements ShouldQueue {
    public static function dispatch(string $sEventName, array $arPayload): PendingDispatch;
    public static function dispatchSync(string $sEventName, array $arPayload): mixed;
}
```

<!-- From Phase 2 (already shipped): -->
```php
// PluginGuard short-circuit contract
if (App::make('metapixel.disabled')) { return; }

// Settings read pattern
$mValue = Settings::get('paid_status_code', 'new-payment-received');
$sValue = is_scalar($mValue) ? (string) $mValue : 'new-payment-received';
```

<!-- Existing Plugin.php boot() order (Phase 2): -->
// 1. PluginGuard::instance();
// 2. if (App::runningInConsole()) return; // CLI gate
// 3. $obKernel->pushMiddleware(EnsureFbpFbcCookies::class);
//
// Phase 3 modifies the order: insert Event::subscribe(OrderStatusWatcher::class)
// BEFORE step 2 so queue worker (CLI) sees model events too.

<!-- Order model event names (from Lovata.OrdersShopaholic): -->
// eloquent.updated: Lovata\OrdersShopaholic\Models\Order
// eloquent.created: Lovata\OrdersShopaholic\Models\Order
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend MetapixelTestCase::bootOrdersTable to include meta_purchase_event_id + meta_purchase_event_time; clean fixture tables in dropHermeticSchemas</name>
  <read_first>
    - plugins/logingrupa/metapixelshopaholic/tests/MetapixelTestCase.php (existing — bootOrdersTable added by 03-01 Task 4)
    - plugins/logingrupa/metapixelshopaholic/updates/add_meta_purchase_event_id_to_orders_table.php (03-01 Task 1 — column shape)
    - plugins/logingrupa/metapixelshopaholic/tests/Support/OrderFixtures.php (03-04 Task 1 — fixture-side tables to drop)
  </read_first>
  <behavior>
    - `bootOrdersTable()` now provisions the hermetic `lovata_orders_shopaholic_orders` table WITH BOTH the `meta_purchase_event_id VARCHAR(36) NULL` column AND the `meta_purchase_event_time BIGINT UNSIGNED NULL` column (so Phase 3 tests can read/write them directly without running the migration).
    - `dropHermeticSchemas()` adds `Schema::dropIfExists('lovata_orders_shopaholic_orders')`, `Schema::dropIfExists('lovata_orders_shopaholic_order_positions')`, `Schema::dropIfExists('lovata_shopaholic_offers')`, `Schema::dropIfExists('lovata_shopaholic_products')`, `Schema::dropIfExists('logingrupa_metapixel_failed_events')`.
    - All prior Phase-2 tests (BootsWithoutPixelIdTest, EnsureFbpFbcCookiesTest, PixelHeadTest, SettingsRegistrationTest, SanityTest) continue passing — the hermetic-table additions are additive only.
  </behavior>
  <action>
    Open `tests/MetapixelTestCase.php`. Locate the existing `bootOrdersTable(): void` helper (added by 03-01 Task 4). Inside the `Schema::create('lovata_orders_shopaholic_orders', function (Blueprint $obTable) {...})` callback, add TWO column declarations:
    ```
    $obTable->string('meta_purchase_event_id', 36)->nullable()->index();
    $obTable->unsignedBigInteger('meta_purchase_event_time')->nullable();
    ```
    Place them adjacent to `string('secret_key')->nullable()` to mirror the migration's `->after('secret_key')` hint. Both columns are needed because the PurchasePixel browser twin (Task 4) reads event_time from the row.

    Locate `dropHermeticSchemas(): void`. Extend it to drop ALL Phase-3-introduced hermetic tables (in reverse-FK order to avoid SQLite foreign-key errors even though SQLite-in-memory has FK checks disabled per `'foreign_key_constraints' => false`):
    ```
    Schema::dropIfExists('lovata_orders_shopaholic_order_positions');
    Schema::dropIfExists('lovata_orders_shopaholic_orders');
    Schema::dropIfExists('lovata_shopaholic_offers');
    Schema::dropIfExists('lovata_shopaholic_products');
    Schema::dropIfExists('logingrupa_metapixel_failed_events');
    // existing drops below (statuses, system_settings) — keep order
    Schema::dropIfExists('lovata_orders_shopaholic_statuses');
    Schema::dropIfExists('system_settings');
    ```
    Each Schema::dropIfExists is idempotent (no-op if the table doesn't exist).

    Do NOT add a new top-level helper method — extend the existing two. NO change to the parent's `setUp/tearDown/createApplication/flushPluginSingletons` methods.
  </action>
  <verify>
    <automated>cd plugins/logingrupa/metapixelshopaholic && grep -c "string('meta_purchase_event_id'" tests/MetapixelTestCase.php | grep -q "^1$" && grep -c "unsignedBigInteger('meta_purchase_event_time')" tests/MetapixelTestCase.php | grep -q "^1$" && grep -c "Schema::dropIfExists('lovata_orders_shopaholic_orders')" tests/MetapixelTestCase.php | grep -q "^1$" && grep -c "Schema::dropIfExists('lovata_orders_shopaholic_order_positions')" tests/MetapixelTestCase.php | grep -q "^1$" && grep -c "Schema::dropIfExists('lovata_shopaholic_offers')" tests/MetapixelTestCase.php | grep -q "^1$" && grep -c "Schema::dropIfExists('logingrupa_metapixel_failed_events')" tests/MetapixelTestCase.php | grep -q "^1$" && composer test 2>&1 | grep -qE "OK|passed"</automated>
  </verify>
  <acceptance_criteria>
    - `tests/MetapixelTestCase.php::bootOrdersTable` provisions `meta_purchase_event_id` column AND `meta_purchase_event_time` column.
    - `tests/MetapixelTestCase.php::dropHermeticSchemas` drops all 5 new Phase-3 tables.
    - All Phase-2 tests still pass after the extension (`composer test` exits 0).
    - No accidental changes to `setUp`, `tearDown`, `createApplication`, or `flushPluginSingletons`.
  </acceptance_criteria>
  <done>MetapixelTestCase extended additively to support OrderStatusWatcher's full schema needs (both event_id + event_time columns).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create classes/event/OrderStatusWatcher.php (subscribe + handleUpdated + handleCreated)</name>
  <read_first>
    - plugins/logingrupa/metapixelshopaholic/classes/helper/PluginGuard.php (App::make('metapixel.disabled') contract)
    - plugins/logingrupa/metapixelshopaholic/models/Settings.php (paid_status_code + refire_purchase_on_status_flip Settings)
    - plugins/logingrupa/metapixelshopaholic/classes/meta/PayloadBuilder.php (03-04)
    - plugins/logingrupa/metapixelshopaholic/classes/queue/SendCapiEvent.php (03-05)
    - plugins/logingrupa/metapixelshopaholic/classes/exception/MetaPixelException.php (03-02 base)
    - plugins/lovata/shopaholic/classes/event/product/ProductModelHandler.php (analog A — subscribe pattern)
    - plugins/lovata/toolbox/classes/event/ModelHandler.php (parent of analog A — bindEvent lines 27-62)
    - plugins/logingrupa/vippsshopaholic/classes/event/PaymentMethodModelHandler.php (analog B — simpler direct listen)
    - plugins/logingrupa/goodsreceivedshopaholic/classes/orchestrator/ApplyOrchestrator.php (saveQuietly precedent lines 252-268)
    - plugins/logingrupa/metapixelshopaholic/components/PixelHead.php (Uuid::uuid4()->toString() precedent line 93)
    - plugins/logingrupa/metapixelshopaholic/.planning/phases/03-purchase-end-to-end/03-CONTEXT.md (Area 2 Q1/Q2/Q3/Q4 + Specifics lines 149-156)
    - plugins/logingrupa/metapixelshopaholic/.planning/phases/03-purchase-end-to-end/03-PATTERNS.md (section: "classes/event/OrderStatusWatcher.php" lines 346-495)
  </read_first>
  <behavior>
    - `subscribe(Dispatcher $obEvents): void` — listens to both `eloquent.created: Order::class` and `eloquent.updated: Order::class`.
    - `handleUpdated(Order $obOrder): void` — full guard chain:
      1. `if (App::make('metapixel.disabled')) return;`
      2. Resolve `$sPaidCode = Settings::get('paid_status_code', 'new-payment-received');` with is_scalar guard.
      3. Read current status code via `$sCurrentCode = $obOrder->status?->code;` (null if relation not loaded — refresh if needed).
      4. Handle `refire_purchase_on_status_flip` ON branch: if `$bRefire === true && $obOrder->isDirty('status_id') === true && $obOrder->getOriginal('status_id') === [paid_status_id]` AND new status_id is NOT the paid one → clear `meta_purchase_event_id` via saveQuietly. This is the status-AWAY transition that enables the next paid flip to re-fire.
      5. Check `if ($sCurrentCode !== $sPaidCode) return;` — only paid-status orders proceed.
      6. Check `if ($obOrder->meta_purchase_event_id !== null) return;` — already fired (idempotency fence).
      7. Generate `$sUuid = Uuid::uuid4()->toString(); $iEventTime = time();`.
      8. `$obOrder->meta_purchase_event_id = $sUuid; $obOrder->meta_purchase_event_time = $iEventTime; $obOrder->saveQuietly();` — persist both columns BEFORE dispatch in a single saveQuietly call. The event_time companion is required so the browser-side PurchasePixel twin (Task 4) can read the SAME timestamp and Meta can dedup within ±10s. If dispatch fails the columns are still set; next attempt is via FailedEvent replay, not re-fire.
      9. Build payload via `(new PayloadBuilder())->buildPurchaseEventPayload($obOrder, $sUuid, $iEventTime)`. Wrap in try/catch: PayloadBuilder may throw OrderHasNoCurrencyException / OrderHasNoItemsException — catch MetaPixelException, log warning with context, return early (DO NOT rethrow; do NOT undo the saveQuietly because re-firing on a re-save would be just as broken).
      10. `SendCapiEvent::dispatch('Purchase', $arPayload);`. Log info-level breadcrumb with `meta_pixel.event_id, meta_pixel.order_id, meta_pixel.order_number`.
    - `handleCreated(Order $obOrder): void` — same guard chain MINUS the refire-flip logic (created path has no original status to flip away from).
    - All log messages namespaced `meta_pixel.*` per the convention.
    - Class is `final` (PHP 8.4 + project style).
    - `declare(strict_types=1);` per other Phase-3 classes.
  </behavior>
  <action>
    Create `classes/event/OrderStatusWatcher.php` with `declare(strict_types=1);`.

    Namespace `Logingrupa\Metapixelshopaholic\Classes\Event`. Imports:
    - `use Illuminate\Events\Dispatcher;`
    - `use Illuminate\Support\Facades\App;`
    - `use Illuminate\Support\Facades\Log;`
    - `use Logingrupa\Metapixelshopaholic\Classes\Exception\MetaPixelException;`
    - `use Logingrupa\Metapixelshopaholic\Classes\Meta\PayloadBuilder;`
    - `use Logingrupa\Metapixelshopaholic\Classes\Queue\SendCapiEvent;`
    - `use Logingrupa\Metapixelshopaholic\Models\Settings;`
    - `use Lovata\OrdersShopaholic\Models\Order;`
    - `use Lovata\OrdersShopaholic\Models\Status;`
    - `use Ramsey\Uuid\Uuid;`
    - `use Throwable;`

    Class signature: `final class OrderStatusWatcher`.

    Class-level docblock describing PAY-03/10/11 contract + the PluginGuard short-circuit + the saveQuietly idempotency fence.

    Public method:
    ```
    public function subscribe(Dispatcher $obEvents): void
    {
        $obEvents->listen('eloquent.updated: ' . Order::class, [$this, 'handleUpdated']);
        $obEvents->listen('eloquent.created: ' . Order::class, [$this, 'handleCreated']);
    }
    ```

    Public method `handleUpdated(Order $obOrder): void`:
    1. PluginGuard short-circuit: `if ($this->isPluginDisabled()) return;` (helper for testability — see below).
    2. Read settings via helpers (see below): `$sPaidCode`, `$bRefire`.
    3. Refire-OFF away-transition clear (the `refire_purchase_on_status_flip = true` branch). Pseudo-code:
       ```
       if ($bRefire && $this->isAwayFromPaid($obOrder, $sPaidCode)) {
           $obOrder->meta_purchase_event_id = null;
           $obOrder->meta_purchase_event_time = null;
           $obOrder->saveQuietly();
           Log::info('Metapixel: cleared meta_purchase_event_id + event_time on away-transition', [
               'meta_pixel.order_id' => (int) $obOrder->id,
               'meta_pixel.order_number' => (string) $obOrder->order_number,
           ]);
           // Continue — the same handleUpdated call may also be the back-to-paid transition.
       }
       ```
    4. Status fence: `if (! $this->isAtPaidStatus($obOrder, $sPaidCode)) return;`.
    5. Idempotency fence: `if ($obOrder->meta_purchase_event_id !== null) return;`.
    6. Generate UUID + event_time: `$sUuid = Uuid::uuid4()->toString(); $iEventTime = time();`.
    7. Persist UUID + event_time atomically: `$obOrder->meta_purchase_event_id = $sUuid; $obOrder->meta_purchase_event_time = $iEventTime; $obOrder->saveQuietly();`. Both columns are part of the same save so the browser-side PurchasePixel twin (Task 4) reads the SAME event_time as the CAPI dispatch (±10s Meta dedup window).
    8. Build payload + dispatch — wrap in try/catch (MetaPixelException = soft-skip):
       ```
       try {
           $arPayload = (new PayloadBuilder())->buildPurchaseEventPayload($obOrder, $sUuid, $iEventTime);
       } catch (MetaPixelException $obException) {
           Log::warning('Metapixel: PayloadBuilder precondition failed — Purchase NOT dispatched', [
               'meta_pixel.order_id' => (int) $obOrder->id,
               'meta_pixel.event_id' => $sUuid,
               'meta_pixel.exception' => get_class($obException),
               'meta_pixel.message' => $obException->getMessage(),
           ]);
           return;  // saveQuietly already wrote the UUID; not rolling back (manual replay path in Phase 5).
       }

       SendCapiEvent::dispatch('Purchase', $arPayload);
       Log::info('Metapixel: Purchase dispatched', [
           'meta_pixel.order_id' => (int) $obOrder->id,
           'meta_pixel.order_number' => (string) $obOrder->order_number,
           'meta_pixel.event_id' => $sUuid,
       ]);
       ```

    Public method `handleCreated(Order $obOrder): void`:
    Same as handleUpdated but skip step 3 (no away-transition logic on create). Document with: "CONTEXT Area 2 Q2 — covers admin-created-already-paid orders (imports, seeds, manual entry)."

    Private helpers (separate methods to satisfy PHPMD CyclomaticComplexity ≤ 10 on the public handlers):

    - `private function isPluginDisabled(): bool` — `return (bool) App::make('metapixel.disabled');`. Wrap in try/catch only if container resolution can fail at this boundary — Phase 2 02-04 PixelHeadTest confirms the binding is reliable post-boot. NO silent catch needed.

    - `private function readPaidStatusCode(): string` — Settings::get + is_scalar guard, default `'new-payment-received'`.

    - `private function readRefireFlag(): bool` — Settings::get + is_scalar guard, default `false`.

    - `private function isAtPaidStatus(Order $obOrder, string $sPaidCode): bool` — resolve `$sCurrentCode` via `$obOrder->status?->code` (Eloquent relation — refresh if null). If `status` relation is null (lazy-load failure), fall back to `Status::where('id', $obOrder->status_id)->value('code')`. Returns true iff `$sCurrentCode === $sPaidCode`.

    - `private function isAwayFromPaid(Order $obOrder, string $sPaidCode): bool` — `if (!$obOrder->isDirty('status_id')) return false;`. Resolve original status_id → original code via `Status::where('id', $obOrder->getOriginal('status_id'))->value('code')`. `return $sOriginalCode === $sPaidCode && !$this->isAtPaidStatus($obOrder, $sPaidCode);`.

    All methods typed per phpstan level 10. Hungarian notation. File ≤ 250 LOC.
  </action>
  <verify>
    <automated>cd plugins/logingrupa/metapixelshopaholic && grep -c "declare(strict_types=1);" classes/event/OrderStatusWatcher.php | grep -q "^1$" && grep -cE "final class OrderStatusWatcher" classes/event/OrderStatusWatcher.php | grep -q "^1$" && grep -cE "public function subscribe\(Dispatcher \\\$obEvents\): void" classes/event/OrderStatusWatcher.php | grep -q "^1$" && grep -cE "public function handleUpdated\(Order \\\$obOrder\): void" classes/event/OrderStatusWatcher.php | grep -q "^1$" && grep -cE "public function handleCreated\(Order \\\$obOrder\): void" classes/event/OrderStatusWatcher.php | grep -q "^1$" && grep -c "App::make('metapixel.disabled')" classes/event/OrderStatusWatcher.php | grep -qE "^[1-9]" && grep -c "Uuid::uuid4()->toString()" classes/event/OrderStatusWatcher.php | grep -qE "^[1-9]" && grep -c "->saveQuietly()" classes/event/OrderStatusWatcher.php | grep -qE "^[2-9]" && grep -c "SendCapiEvent::dispatch" classes/event/OrderStatusWatcher.php | grep -qE "^[1-9]" && grep -c "PayloadBuilder" classes/event/OrderStatusWatcher.php | grep -qE "^[1-9]" && grep -c "eloquent.updated:" classes/event/OrderStatusWatcher.php | grep -q "^1$" && grep -c "eloquent.created:" classes/event/OrderStatusWatcher.php | grep -q "^1$" && php -l classes/event/OrderStatusWatcher.php | grep -q "No syntax errors"</automated>
  </verify>
  <acceptance_criteria>
    - File `classes/event/OrderStatusWatcher.php` exists with `declare(strict_types=1);`.
    - `final class OrderStatusWatcher` (no inheritance).
    - `subscribe(Dispatcher): void`, `handleUpdated(Order): void`, `handleCreated(Order): void` signatures present.
    - `App::make('metapixel.disabled')` called at the entry of both handle methods (or via the `isPluginDisabled()` helper called first).
    - `Uuid::uuid4()->toString()` called at least once.
    - `saveQuietly()` called at least 2 times (once for the UUID + event_time write, once for the away-transition clear).
    - `meta_purchase_event_id` and `meta_purchase_event_time` BOTH assigned to `$obOrder` immediately before saveQuietly (grep both column names ≥ 2 each — one assign + one null-clear).
    - `SendCapiEvent::dispatch` called at least once.
    - `PayloadBuilder` constructor invoked at least once.
    - Event names exactly `'eloquent.updated: ' . Order::class` and `'eloquent.created: ' . Order::class`.
    - `php -l` exits 0.
    - File ≤ 250 LOC.
  </acceptance_criteria>
  <done>OrderStatusWatcher shipped with full guard chain, UUIDv4 generation, saveQuietly persistence, PayloadBuilder construction with MetaPixelException soft-skip, SendCapiEvent dispatch, refire-flip away-transition handling.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Modify Plugin.php to Event::subscribe(OrderStatusWatcher::class) before CLI gate</name>
  <read_first>
    - plugins/logingrupa/metapixelshopaholic/Plugin.php (current Phase-2 boot order)
    - plugins/logingrupa/metapixelshopaholic/classes/event/OrderStatusWatcher.php (Task 2)
    - plugins/lovata/discountsshopaholic/Plugin.php (analog: multiple Event::subscribe lines — lines 62-99)
    - plugins/logingrupa/metapixelshopaholic/.planning/phases/03-purchase-end-to-end/03-PATTERNS.md (section: "Plugin.php (modify — append Event::subscribe)" lines 814-884)
  </read_first>
  <behavior>
    - `Plugin::boot()` invokes `Event::subscribe(OrderStatusWatcher::class);` AFTER `PluginGuard::instance();` and BEFORE the `App::runningInConsole()` early-return.
    - The CLI gate (lines 99-101) and middleware push (lines 103-106) remain unchanged.
    - `Plugin::registerComponents()` adds a NEW entry `PurchasePixel::class => 'purchasePixel'` alongside the existing `PixelHead::class => 'pixelHead'` entry. Both components are registered together.
    - `use Illuminate\Support\Facades\Event;` added to the imports (alphabetised between `App` and `Backend` — actually `Event` sorts after `App` alphabetically; insert in correct position).
    - `use Logingrupa\Metapixelshopaholic\Classes\Event\OrderStatusWatcher;` added.
    - `use Logingrupa\Metapixelshopaholic\Components\PurchasePixel;` added (alphabetised with the existing `PixelHead` import).
  </behavior>
  <action>
    Open `Plugin.php`. Apply two additive edits — no deletions, no reordering of existing statements (only insert one new statement + two new imports).

    1. Add to the use block (alphabetised; pint will normalise — but write it sorted):
    ```
    use Illuminate\Support\Facades\Event;
    use Logingrupa\Metapixelshopaholic\Classes\Event\OrderStatusWatcher;
    use Logingrupa\Metapixelshopaholic\Components\PurchasePixel;
    ```
    Position: after `use Illuminate\Support\Facades\App;` for the Event facade; after `use Logingrupa\Metapixelshopaholic\Classes\Helper\PluginGuard;` for OrderStatusWatcher; after `use Logingrupa\Metapixelshopaholic\Components\PixelHead;` for PurchasePixel (or wherever pint orders by alphabet).

    2. Modify `Plugin::boot()`. The current shape is:
    ```
    public function boot(): void
    {
        // 1) Prime PluginGuard in every context (CONTEXT Area 1 Q2-Q3 + SKEL-05).
        PluginGuard::instance();

        // 2) CLI-only gate (WR-01): no HTTP response in CLI = nothing to
        // push to. Backend-vs-storefront discrimination happens inside the
        // middleware against the actual request URL.
        if (App::runningInConsole()) {
            return;
        }

        // 3) Push EnsureFbpFbcCookies onto the global HTTP middleware stack.
        /** @var HttpKernel $obKernel */
        $obKernel = $this->app->make(HttpKernel::class);
        $obKernel->pushMiddleware(EnsureFbpFbcCookies::class);
    }
    ```

    Insert between the `PluginGuard::instance();` line and the `// 2)` comment:
    ```

        // 1b) Phase 3 (PAY-03): subscribe OrderStatusWatcher globally —
        // storefront, backend, AND queue worker. Backend admin status-flip
        // (bank-transfer path PAY-11) MUST be observed; queue worker MUST
        // observe model updates when a saved-elsewhere Order is later
        // reloaded inside a job context. The CLI gate below is for the
        // middleware push only.
        Event::subscribe(OrderStatusWatcher::class);
    ```

    Renumber the existing comments if desired (purely cosmetic) — pick one: either keep the existing `// 2)` and `// 3)` labels (they remain accurate; the inserted block is `// 1b)`), OR renumber to `// 1` `// 2` `// 3` `// 4`. Default: keep `// 1b)` to minimize diff churn.

    3. Modify `Plugin::registerComponents()`. Current shape:
    ```
    public function registerComponents(): array
    {
        return [
            PixelHead::class => 'pixelHead',
        ];
    }
    ```

    Change to:
    ```
    public function registerComponents(): array
    {
        return [
            PixelHead::class => 'pixelHead',
            PurchasePixel::class => 'purchasePixel',
        ];
    }
    ```

    The `Plugin::boot()` body grows by ~3 lines + 1 comment block. The PluginDetails, requireMethods, registerSettings get +1 component registration line.
  </action>
  <verify>
    <automated>cd plugins/logingrupa/metapixelshopaholic && grep -c "Event::subscribe(OrderStatusWatcher::class);" Plugin.php | grep -q "^1$" && grep -c "use Logingrupa\\\\Metapixelshopaholic\\\\Classes\\\\Event\\\\OrderStatusWatcher;" Plugin.php | grep -q "^1$" && grep -c "use Illuminate\\\\Support\\\\Facades\\\\Event;" Plugin.php | grep -q "^1$" && grep -c "use Logingrupa\\\\Metapixelshopaholic\\\\Components\\\\PurchasePixel;" Plugin.php | grep -q "^1$" && grep -c "PurchasePixel::class => 'purchasePixel'" Plugin.php | grep -q "^1$" && php -l Plugin.php | grep -q "No syntax errors" && composer analyse 2>&1 | grep -qE "No errors|0 errors"</automated>
  </verify>
  <acceptance_criteria>
    - `Event::subscribe(OrderStatusWatcher::class);` present exactly once in `Plugin::boot()`.
    - All three new use imports present (Event facade, OrderStatusWatcher, PurchasePixel).
    - The Event::subscribe line comes BEFORE the `if (App::runningInConsole())` early-return.
    - `Plugin::registerComponents()` returns BOTH `PixelHead::class => 'pixelHead'` and `PurchasePixel::class => 'purchasePixel'`.
    - All Phase-2 boot order preserved (PluginGuard::instance() first, middleware push last).
    - `php -l Plugin.php` exits 0.
    - `composer analyse` exits 0.
  </acceptance_criteria>
  <done>Plugin.php boot wires OrderStatusWatcher into the global event subscription AND registerComponents adds PurchasePixel as `purchasePixel`, preserving the Phase-2 contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Create PurchasePixel component + Twig partial (browser-side Pixel twin for thank-you page — PAY-10 dedup contract)</name>
  <read_first>
    - plugins/logingrupa/metapixelshopaholic/components/PixelHead.php (Phase 2 — sibling component pattern; onRun() returning data; component property declaration)
    - plugins/logingrupa/metapixelshopaholic/components/pixelhead/default.htm (Phase 2 — fbq('track', name, custom_data, {eventID}) Twig precedent)
    - plugins/logingrupa/metapixelshopaholic/classes/helper/PluginGuard.php (App::make('metapixel.disabled') short-circuit contract)
    - plugins/logingrupa/metapixelshopaholic/classes/meta/PayloadBuilder.php (03-04 — buildPurchaseEventPayload returns full envelope; custom_data is at data[0].custom_data)
    - plugins/logingrupa/metapixelshopaholic/models/Settings.php (paid_status_code Setting — same fence used by OrderStatusWatcher)
    - plugins/lovata/ordersshopaholic/components/OrderPage.php (existing component that resolves Order by slug — read its property declaration + get() return shape so PurchasePixel can read the already-loaded Order from the same page)
    - themes/logingrupa-naisstore/pages/order-complete.htm (theme integration point; the thank-you page that uses {% set obOrder = OrderPage.get() %})
    - plugins/logingrupa/metapixelshopaholic/.planning/phases/03-purchase-end-to-end/03-CONTEXT.md (Specifics — Manual staging verification protocol PAY-10)
  </read_first>
  <behavior>
    - `components/PurchasePixel.php` is a `Cms\Classes\ComponentBase` subclass with `componentDetails()` returning a backend-facing description.
    - `defineProperties(): array` declares a single property `orderSlug` (string, required) — the order's `secret_key`/slug consumed by the existing `OrderPage` component pattern. Default value uses `{{ :slug }}` route binding so `themes/.../pages/order-complete.htm` (`url = "/checkout/:slug"`) can declare `[PurchasePixel] orderSlug = "{{ :slug }}"`.
    - `onRun(): void` resolves the Order by slug → secret_key, performs the guard chain, and sets `$this->arMetaEvent` for the Twig partial OR sets it to `null` (rendering nothing).
    - Twig partial `components/purchasepixel/default.htm` emits `<script>fbq('track', 'Purchase', custom_data, {eventID})</script>` ONLY when `arMetaEvent` is non-null. Custom_data carries the same `content_ids`, `value`, `currency`, `order_id`, `num_items`, `event_time` as the CAPI side — extracted from `PayloadBuilder::buildPurchaseEventPayload` via the `data[0].custom_data` slice.
    - Guard chain in `onRun()` — render nothing (`arMetaEvent = null`) on ANY of:
      1. PluginGuard disabled.
      2. Order not found by slug.
      3. Order status code !== Settings::get('paid_status_code', 'new-payment-received').
      4. `meta_purchase_event_id` is null (OrderStatusWatcher hasn't fired yet — possible if the user lands on order-complete before the CAPI status flip from PayPal IPN).
      5. `meta_purchase_event_time` is null (paired column; defensive — should always be set together with event_id).
    - When all guards pass: `arMetaEvent = ['event_id' => $obOrder->meta_purchase_event_id, 'event_time' => (int) $obOrder->meta_purchase_event_time, 'event_name' => 'Purchase', 'custom_data' => $ar['data'][0]['custom_data']]` where `$ar` is the full PayloadBuilder envelope. user_data is INTENTIONALLY OMITTED on the Pixel side (Meta CAPI spec — user_data is server-side hashes only; fbq() infers fbp/fbc/external_id from the browser session via the existing _fbp/_fbc cookies set by Phase 2 middleware).
  </behavior>
  <action>
    Create `components/PurchasePixel.php` with `declare(strict_types=1);`. Namespace `Logingrupa\Metapixelshopaholic\Components`.

    Imports:
    - `use Cms\Classes\ComponentBase;`
    - `use Illuminate\Support\Facades\App;`
    - `use Illuminate\Support\Facades\Log;`
    - `use Logingrupa\Metapixelshopaholic\Classes\Exception\MetaPixelException;`
    - `use Logingrupa\Metapixelshopaholic\Classes\Meta\PayloadBuilder;`
    - `use Logingrupa\Metapixelshopaholic\Models\Settings;`
    - `use Lovata\OrdersShopaholic\Models\Order;`
    - `use Throwable;`

    Class signature: `final class PurchasePixel extends ComponentBase`.

    Public methods:
    1. `componentDetails(): array` — returns `['name' => 'Purchase Pixel', 'description' => 'Browser-side Pixel twin for Purchase events. Reads persisted event_id + event_time so Meta can dedup Pixel + CAPI by event_id.']`.

    2. `defineProperties(): array`:
    ```
    return [
        'orderSlug' => [
            'title' => 'Order slug (secret_key route binding)',
            'description' => 'Secret_key of the paid order; usually bound to {{ :slug }} on the thank-you page.',
            'type' => 'string',
            'default' => '{{ :slug }}',
            'validationPattern' => '^[a-zA-Z0-9-]+$',
        ],
    ];
    ```

    3. `public ?array $arMetaEvent = null;` — property exposed to Twig; null = component renders nothing.

    4. `public function onRun(): void`:
    ```
    if ($this->isDisabled()) {
        return;
    }

    $obOrder = $this->resolveOrder();
    if ($obOrder === null) {
        return;
    }

    if (!$this->isAtPaidStatus($obOrder)) {
        return;
    }

    if ($obOrder->meta_purchase_event_id === null || $obOrder->meta_purchase_event_time === null) {
        return;
    }

    try {
        $arPayload = (new PayloadBuilder())->buildPurchaseEventPayload(
            $obOrder,
            (string) $obOrder->meta_purchase_event_id,
            (int) $obOrder->meta_purchase_event_time,
        );
    } catch (MetaPixelException $obException) {
        // silent: thank-you page Pixel must not 500 the page render. CAPI side already
        // dispatched (or didn't) — Pixel-side miss is a v1 acceptable degradation.
        Log::warning('Metapixel: PurchasePixel PayloadBuilder skipped', [
            'meta_pixel.order_id' => (int) $obOrder->id,
            'meta_pixel.exception' => get_class($obException),
        ]);
        return;
    }

    $arCustomData = isset($arPayload['data'][0]['custom_data']) && is_array($arPayload['data'][0]['custom_data'])
        ? $arPayload['data'][0]['custom_data']
        : [];

    $this->arMetaEvent = [
        'event_id' => (string) $obOrder->meta_purchase_event_id,
        'event_time' => (int) $obOrder->meta_purchase_event_time,
        'event_name' => 'Purchase',
        'custom_data' => $arCustomData,
    ];
    ```

    5. Private helpers:
    - `private function isDisabled(): bool` — `return (bool) App::make('metapixel.disabled');`.
    - `private function resolveOrder(): ?Order` — `$sSlug = (string) $this->property('orderSlug'); if ($sSlug === '') { return null; } return Order::where('secret_key', $sSlug)->first();`.
    - `private function isAtPaidStatus(Order $obOrder): bool` — read `paid_status_code` from Settings (is_scalar guard, default `'new-payment-received'`); compare to `$obOrder->status?->code`.

    Class file size: ≤ 200 LOC.

    Now create `components/purchasepixel/default.htm`. Mirror the existing `components/pixelhead/default.htm` pattern. The full file content:
    ```
    {% if __SELF__.arMetaEvent is not null %}
    <script>
        if (typeof fbq === 'function') {
            fbq('track', 'Purchase', Object.assign({event_time: {{ __SELF__.arMetaEvent.event_time }} }, {{ __SELF__.arMetaEvent.custom_data|json_encode|raw }}), { eventID: '{{ __SELF__.arMetaEvent.event_id|e('js') }}' });
        }
    </script>
    {% endif %}
    ```

    Use Twig's `e('js')` filter on `event_id` for JS-string-context escaping (T-04-02 mitigation — same defensive posture as PixelHead).

    After both files are written, manually document the theme integration step in `03-06-SUMMARY.md` (no code change to theme in this plan — Phase 5 HARD-05 README documents this):
    > Theme operator step (Phase 5): on `themes/logingrupa-naisstore/pages/order-complete.htm`, add `[purchasePixel] orderSlug = "{{ :slug }}"` to the component block and `{% component 'purchasePixel' %}` after the `{% partial 'order/order-complete/order-complete' %}` line. The existing `fbq('trackCustom', 'ViewdOrderCompleatedStatusPage', …)` block can remain — it carries no eventID and Meta treats it as a separate custom event (unrelated to Purchase dedup).
  </action>
  <verify>
    <automated>cd plugins/logingrupa/metapixelshopaholic && grep -c "declare(strict_types=1);" components/PurchasePixel.php | grep -q "^1$" && grep -cE "final class PurchasePixel extends ComponentBase" components/PurchasePixel.php | grep -q "^1$" && grep -cE "public function onRun\(\): void" components/PurchasePixel.php | grep -q "^1$" && grep -c "App::make('metapixel.disabled')" components/PurchasePixel.php | grep -qE "^[1-9]" && grep -c "meta_purchase_event_id" components/PurchasePixel.php | grep -qE "^[2-9]" && grep -c "meta_purchase_event_time" components/PurchasePixel.php | grep -qE "^[2-9]" && grep -c "PayloadBuilder" components/PurchasePixel.php | grep -qE "^[1-9]" && grep -c "fbq('track', 'Purchase'" components/purchasepixel/default.htm | grep -q "^1$" && grep -c "eventID:" components/purchasepixel/default.htm | grep -q "^1$" && php -l components/PurchasePixel.php | grep -q "No syntax errors" && composer analyse 2>&1 | grep -qE "No errors|0 errors"</automated>
  </verify>
  <acceptance_criteria>
    - File `components/PurchasePixel.php` exists with `declare(strict_types=1);`.
    - `final class PurchasePixel extends ComponentBase` present.
    - `onRun(): void` method present with full guard chain (PluginGuard, order-found, status, event_id non-null, event_time non-null).
    - `App::make('metapixel.disabled')` called.
    - `meta_purchase_event_id` referenced ≥ 2 times in PurchasePixel.php (guard + payload).
    - `meta_purchase_event_time` referenced ≥ 2 times in PurchasePixel.php (guard + payload).
    - `PayloadBuilder` invoked at least once.
    - File `components/purchasepixel/default.htm` exists containing `fbq('track', 'Purchase'` exactly once and `eventID:` exactly once.
    - Twig partial uses `e('js')` filter on event_id (T-04-02 mitigation).
    - `php -l` exits 0 on both files (Twig is parsed by October, php -l only checks PHP).
    - `composer analyse` exits 0.
    - File `components/PurchasePixel.php` ≤ 200 LOC.
  </acceptance_criteria>
  <done>PurchasePixel component + Twig partial shipped. Phase 3 self-validates dedup: the same event_id + event_time persisted by OrderStatusWatcher is consumed by PurchasePixel on the thank-you page so PayPal orders fire BOTH Pixel + CAPI with matching identifiers and Meta dedups.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: Write PurchasePixelTest feature suite locking the Pixel-twin dedup contract</name>
  <read_first>
    - plugins/logingrupa/metapixelshopaholic/components/PurchasePixel.php (Task 4)
    - plugins/logingrupa/metapixelshopaholic/components/purchasepixel/default.htm (Task 4)
    - plugins/logingrupa/metapixelshopaholic/tests/Feature/PixelHeadTest.php (Phase 2 — component test pattern; how to drive onRun() in test harness)
    - plugins/logingrupa/metapixelshopaholic/tests/Support/OrderFixtures.php (03-04 — Order with status + positions)
    - plugins/logingrupa/metapixelshopaholic/tests/MetapixelTestCase.php
  </read_first>
  <behavior>
    Test cases (all in `tests/Feature/PurchasePixelTest.php`):

    - `test_paid_order_with_persisted_event_id_populates_ar_meta_event`: paid Order with `meta_purchase_event_id` and `meta_purchase_event_time` set. Invoke `(new PurchasePixel())->onRun()` (after setting `$obComponent->setProperty('orderSlug', $obOrder->secret_key)`). Assert `$obComponent->arMetaEvent !== null`; assert `$obComponent->arMetaEvent['event_id'] === $obOrder->meta_purchase_event_id`; assert `$obComponent->arMetaEvent['event_time'] === (int) $obOrder->meta_purchase_event_time`; assert `$obComponent->arMetaEvent['event_name'] === 'Purchase'`; assert `is_array($obComponent->arMetaEvent['custom_data'])` and contains `content_ids`, `value`, `currency`, `order_id`, `num_items`.

    - `test_non_paid_order_does_not_populate_ar_meta_event`: Order at status_id=1 (pending). Even if `meta_purchase_event_id` is somehow set, the status fence rejects. Assert `$obComponent->arMetaEvent === null`.

    - `test_paid_order_without_persisted_event_id_does_not_populate_ar_meta_event`: paid Order with `meta_purchase_event_id = null`. Assert `arMetaEvent === null` (handles the IPN-race case: user lands on order-complete before PayPal IPN flips the status — Pixel correctly renders nothing rather than guess).

    - `test_paid_order_without_persisted_event_time_does_not_populate_ar_meta_event`: paid Order with `event_id` set but `event_time = null` (defensive — should never happen in production but test the column-pair contract).

    - `test_plugin_disabled_does_not_populate_ar_meta_event`: PluginGuard reflection-primed to disabled=true. Assert `arMetaEvent === null`.

    - `test_order_slug_not_found_does_not_populate_ar_meta_event`: Set property to a slug that does not match any Order row. Assert `arMetaEvent === null`.

    - `test_custom_data_matches_capi_envelope_byte_for_byte`: build the same payload via `(new PayloadBuilder())->buildPurchaseEventPayload($obOrder, $sUuid, $iTime)`. Assert `$obComponent->arMetaEvent['custom_data'] === $arEnvelope['data'][0]['custom_data']` (deep equality — the contract that Meta-dedup-on-event-id relies on means the custom_data shapes converge).
  </behavior>
  <action>
    Create `tests/Feature/PurchasePixelTest.php` with namespace `Logingrupa\Metapixelshopaholic\Tests\Feature`. Follow the existing PixelHeadTest pattern for the harness:
    - `require_once __DIR__.'/../MetapixelTestCase.php'; require_once __DIR__.'/../Support/OrderFixtures.php';`.
    - Class `final class PurchasePixelTest extends MetapixelTestCase`.
    - `setUp()`: parent::setUp(); Cache::flush(); Settings::clearInternalCache(); PluginGuard::flush(); bootSystemSettings(); bootOrdersStatuses(); bootOrdersTable(); OrderFixtures::provisionHermeticOfferProductTables(); Settings::set('pixel_id', '123456789012345'); Settings::set('capi_access_token', 'EAAFakeToken'); Settings::set('currency_code', 'EUR'); primePluginGuardEnabled() (reflection helper from PixelHeadTest).
    - Helper `makeComponent(string $sOrderSlug): PurchasePixel` — instantiates `new PurchasePixel(/* no controller for unit-mode */ null)` (October allows null controller for testing) and calls `$obComponent->setProperty('orderSlug', $sOrderSlug);`. Returns the instance.
    - Each test creates the Order via `OrderFixtures::makePaidOrder()` (or a status-specific variant), sets `meta_purchase_event_id` / `meta_purchase_event_time` manually via `$obOrder->forceFill([...])->save();` (NOT saveQuietly here — test setup, not handler context), runs `$obComponent->onRun()`, asserts `arMetaEvent`.
    - Use Pest 4 OR PHPUnit method style — match the project's PixelHeadTest style (PHPUnit `public function test_xxx(): void`).
    - All test names match those in <behavior>. Hungarian notation throughout.
  </action>
  <verify>
    <automated>cd plugins/logingrupa/metapixelshopaholic && grep -c "final class PurchasePixelTest extends MetapixelTestCase" tests/Feature/PurchasePixelTest.php | grep -q "^1$" && grep -cE "test_paid_order_with_persisted_event_id_populates_ar_meta_event|test_non_paid_order_does_not_populate_ar_meta_event|test_paid_order_without_persisted_event_id_does_not_populate_ar_meta_event|test_plugin_disabled_does_not_populate_ar_meta_event|test_custom_data_matches_capi_envelope_byte_for_byte" tests/Feature/PurchasePixelTest.php | grep -qE "^[5-9]$" && composer test 2>&1 | grep -qE "OK|passed"</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/Feature/PurchasePixelTest.php` exists with namespace `Logingrupa\Metapixelshopaholic\Tests\Feature` extending `MetapixelTestCase`.
    - All 7 named tests present and named exactly as in <behavior>.
    - `composer test` exits 0 with PurchasePixelTest tests passing.
    - `PurchasePixel.php` line coverage ≥ 80%.
  </acceptance_criteria>
  <done>PurchasePixel test suite locks the Pixel-twin dedup contract — payload bytes match the CAPI side, guards prevent Pixel-fires-without-CAPI race, persisted event_id + event_time round-trip from DB through the component back into fbq().</done>
</task>

<task type="auto" tdd="true">
  <name>Task 6: Add pixel_id regex validator to models/settings/fields.yaml (PH-01 retro-fit from 02-04 SUMMARY)</name>
  <read_first>
    - plugins/logingrupa/metapixelshopaholic/models/settings/fields.yaml (current — find the pixel_id field)
    - plugins/logingrupa/metapixelshopaholic/.planning/phases/02-skeleton-cookie-fix/02-04-SUMMARY.md (PH-01 — high priority Phase-5-launch OR Phase-3-pre-PAY-01)
    - plugins/logingrupa/metapixelshopaholic/components/PixelHead.php (the consumer that inlines pixel_id into <script>)
  </read_first>
  <behavior>
    - `models/settings/fields.yaml` has the `pixel_id` field with a `regex:` validator matching `/^\d{6,20}$/`.
    - Existing Settings round-trip tests still pass — the validator only kicks in on backend save, not on Settings::get reads.
  </behavior>
  <action>
    Open `models/settings/fields.yaml`. Locate the `pixel_id:` field block. October's Settings field shape supports a `validator:` or `rules:` key for inline validation. The exact key depends on the October CMS version + Lovata.Toolbox.CommonSettings convention. Check the existing fields for any precedent:
    - If `rules:` is the convention (CommonSettings + Validation trait integration), add `rules: 'regex:/^\d{6,20}$/'` under the pixel_id field.
    - If validation lives in a `validation:` block at the bottom of the YAML, OR in `Settings::$rules` on the model, prefer the model approach.
    - Phase 2 02-04 SUMMARY mentions "regex:/^\d{6,20}$/ validator to the pixel_id field in models/settings/fields.yaml per T-04-01" — confirm October's field-level rules syntax.

    Implementation alternative if YAML-level rules don't take: extend `Settings.php` to declare `public $rules = ['pixel_id' => 'nullable|regex:/^\d{6,20}$/'];` and `use October\Rain\Database\Traits\Validation;` if not already used. This is the CommonSettings precedent — Lovata.Toolbox CommonSettings is Validation-trait-aware via the underlying SettingModel base.

    Resolution: do BOTH (YAML hint for backend UI + model-level $rules for hard enforcement) — but in v1 keep it surgical. If `Settings::$rules` array doesn't exist yet, ADD it with just the pixel_id rule. Run the existing Phase-2 SettingsRegistrationTest to confirm no regression.

    Document the validator in the YAML with a comment: `# T-04-01 mitigation: pixel_id must be 6-20 digit numeric Meta-issued ID. Prevents <script> string break-out via stored XSS.`
  </action>
  <verify>
    <automated>cd plugins/logingrupa/metapixelshopaholic && grep -cE "regex.*\\\\\\\\d\{6,20\}|d\{6,20\}" models/settings/fields.yaml models/Settings.php 2>&1 | grep -qE "[1-9]" && composer test 2>&1 | grep -qE "OK|passed"</automated>
  </verify>
  <acceptance_criteria>
    - A regex constraint matching `/^\d{6,20}$/` (or equivalent) is present in either `models/settings/fields.yaml` OR `models/Settings.php::$rules`.
    - All existing Phase-2 Settings tests still pass (no regressions in SettingsRegistrationTest, BootsWithoutPixelIdTest).
    - `composer test` exits 0.
  </acceptance_criteria>
  <done>Pixel ID stored-XSS defense-in-depth shipped per PH-01.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 7: Write OrderStatusWatcherTest feature suite locking all PAY-03 idempotency invariants</name>
  <read_first>
    - plugins/logingrupa/metapixelshopaholic/classes/event/OrderStatusWatcher.php (Task 2)
    - plugins/logingrupa/metapixelshopaholic/Plugin.php (Task 3)
    - plugins/logingrupa/metapixelshopaholic/tests/MetapixelTestCase.php (Task 1)
    - plugins/logingrupa/metapixelshopaholic/tests/Support/OrderFixtures.php (03-04 Task 1)
    - plugins/logingrupa/metapixelshopaholic/tests/Feature/BootsWithoutPixelIdTest.php (Phase 2 — triple-reset pattern)
    - plugins/logingrupa/metapixelshopaholic/.planning/phases/03-purchase-end-to-end/03-CONTEXT.md (Specifics line 162 — 5 named scenarios)
    - plugins/logingrupa/metapixelshopaholic/.planning/phases/03-purchase-end-to-end/03-PATTERNS.md (section: "tests/Feature/OrderStatusWatcherTest.php" lines 937-995)
  </read_first>
  <behavior>
    Test cases (each Queue::fake before exercising; assert via Queue::assertPushed / assertPushedTimes / assertNotPushed):

    - `test_fresh_paid_order_dispatches_send_capi_event_once`: Settings::set pixel_id + capi_access_token. Create Order at non-paid status (status_id=1). Update status_id=5 + save (triggers eloquent.updated). Assert `Queue::assertPushed(SendCapiEvent::class, fn ($obJob) => $obJob->sEventName === 'Purchase')`. Assert `$obOrder->fresh()->meta_purchase_event_id !== null`.

    - `test_same_paid_status_save_does_not_redispatch`: as above, but after the first save, update some unrelated field (e.g. email) and save again. Assert `Queue::assertPushedTimes(SendCapiEvent::class, 1)` (only the first save fired).

    - `test_status_flip_away_then_back_with_refire_off_fires_only_once`: Settings::set refire_purchase_on_status_flip=false. Fresh paid order → fire 1. Update to status_id=4 (cancelled) → save. Update back to status_id=5 → save. Assert `Queue::assertPushedTimes(SendCapiEvent::class, 1)` (the second back-to-paid is no-op because meta_purchase_event_id is non-null).

    - `test_status_flip_away_then_back_with_refire_on_fires_twice`: Settings::set refire_purchase_on_status_flip=true. Fresh paid order → fire 1. Update to status_id=4 → save. Confirm `$obOrder->fresh()->meta_purchase_event_id === null` (the away-transition cleared it). Update back to status_id=5 → save → fire 2. Assert `Queue::assertPushedTimes(SendCapiEvent::class, 2)`.

    - `test_plugin_disabled_does_not_dispatch`: Settings::set pixel_id=''. Force PluginGuard re-prime (or reflection-prime to disabled=true). Fresh paid order. Assert `Queue::assertNotPushed(SendCapiEvent::class)`.

    - `test_admin_created_already_paid_order_dispatches`: Settings::set pixel_id + token. Create Order with status_id=5 directly on Order::create (eloquent.created event fires). Assert `Queue::assertPushed(SendCapiEvent::class)`.

    - `test_order_without_currency_does_not_dispatch_and_logs_warning`: Settings primed. Create Order with currency_code=null + currency_id=null. Update status_id=5. Assert `Queue::assertNotPushed(SendCapiEvent::class)`. Assert `Log::shouldHaveReceived('warning')->atLeast()->once()` (use Log::spy + the with-args matcher checking message contains 'PayloadBuilder precondition failed' or 'OrderHasNoCurrency').

    - `test_order_without_items_does_not_dispatch_and_logs_warning`: similar; Order has no OrderPositions.

    - `test_event_id_persisted_to_meta_purchase_event_id_column`: fresh paid order; after dispatch, assert `Uuid::isValid($obOrder->fresh()->meta_purchase_event_id) === true` AND that the dispatched job's `$arPayload['data'][0]['event_id']` equals the persisted UUID.

    - `test_event_time_is_within_two_seconds_of_now`: assert the dispatched job's `$arPayload['data'][0]['event_time']` differs from `time()` by ≤ 2 seconds.

    - `test_event_time_persisted_to_meta_purchase_event_time_column`: fresh paid order; after dispatch, assert `(int) $obOrder->fresh()->meta_purchase_event_time !== 0` AND that the persisted column value equals the dispatched job's `$arPayload['data'][0]['event_time']` (both reflect the same `time()` reading from inside the handler — proves the Pixel side will read the SAME event_time as CAPI).

    - `test_refire_on_clears_both_event_id_and_event_time_columns`: Settings::set refire_purchase_on_status_flip=true. Fresh paid order → fire 1. Update to status_id=4 → save. Assert `$obOrder->fresh()->meta_purchase_event_id === null` AND `$obOrder->fresh()->meta_purchase_event_time === null` (both columns cleared atomically by the away-transition saveQuietly).
  </behavior>
  <action>
    Create `tests/Feature/OrderStatusWatcherTest.php` with namespace `Logingrupa\Metapixelshopaholic\Tests\Feature`. `require_once __DIR__.'/../MetapixelTestCase.php';` + `require_once __DIR__.'/../Support/OrderFixtures.php';`.

    Class `final class OrderStatusWatcherTest extends MetapixelTestCase`.

    `setUp()`:
    ```
    parent::setUp();
    $this->bootSystemSettings();
    $this->bootOrdersStatuses();
    $this->bootOrdersTable();
    \Logingrupa\Metapixelshopaholic\Tests\Support\OrderFixtures::provisionHermeticOfferProductTables();
    Cache::flush();
    Settings::clearInternalCache();
    PluginGuard::flush();

    // Phase 3 NEW: register the watcher (Plugin::boot() doesn't run in test harness)
    Event::subscribe(OrderStatusWatcher::class);
    Queue::fake();

    // Prime PluginGuard enabled (reflection per 02-04 pattern to sidestep HR-02)
    $this->primePluginGuardEnabled('1234567890');
    $this->primeSettings('1234567890', 'TOKEN_X');
    ```

    Helpers (copy from 02-04 PixelHeadTest):
    - `primePluginGuardEnabled(string $sPixelId): void` — reflection set PluginGuard::$bIsDisabled = false + ::$sPixelId = $sPixelId + App::singleton('metapixel.disabled', fn () => false).
    - `primePluginGuardDisabled(): void` — same but disabled=true.
    - `primeSettings(string $sPixelId, string $sCapiToken, bool $bRefire = false): void` — Settings::set both + refire flag + clearInternalCache + Cache::flush.

    Implement all 10 test methods above.

    For the create-Order branch tests, use direct `Order::create([...all fillable...])`. For update-and-save, use `$obOrder = Order::create([...]); $obOrder->status_id = 5; $obOrder->save();` so the eloquent.updated event fires.

    For the Log spy test (currency/items), use Pest 4 `Log::spy(); /* exercise */ Log::shouldHaveReceived('warning')->atLeast()->once()->withArgs(fn ($sMsg, $arContext) => ...)`.

    Note on test isolation: `Event::subscribe` registers the watcher into the global Event dispatcher. The parent's `flushModelEventListeners` in tearDown should clean up; if not, add a `Model::flushEventListeners()` call in this test's tearDown.

    Hungarian notation throughout.
  </action>
  <verify>
    <automated>cd plugins/logingrupa/metapixelshopaholic && grep -cE "function test_fresh_paid_order_dispatches_send_capi_event_once|function test_same_paid_status_save_does_not_redispatch|function test_status_flip_away_then_back_with_refire_off_fires_only_once|function test_status_flip_away_then_back_with_refire_on_fires_twice|function test_plugin_disabled_does_not_dispatch|function test_admin_created_already_paid_order_dispatches|function test_order_without_currency_does_not_dispatch_and_logs_warning|function test_event_id_persisted_to_meta_purchase_event_id_column|function test_event_time_is_within_two_seconds_of_now" tests/Feature/OrderStatusWatcherTest.php | grep -qE "^[7-9]$|^1[0-9]$" && grep -c "Queue::fake" tests/Feature/OrderStatusWatcherTest.php | grep -qE "^[1-9]" && grep -c "Queue::assertPushed\|Queue::assertNotPushed\|Queue::assertPushedTimes" tests/Feature/OrderStatusWatcherTest.php | grep -qE "^[3-9]|^[1-9][0-9]" && composer test 2>&1 | grep -qE "OK|passed"</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/Feature/OrderStatusWatcherTest.php` exists with namespace `Logingrupa\Metapixelshopaholic\Tests\Feature`.
    - ≥ 8 of the 10 named test methods present.
    - `Queue::fake` used at least 1 time (setUp).
    - `Queue::assertPushed|assertNotPushed|assertPushedTimes` used at least 3 times.
    - `composer test` exits 0 with new tests passing.
    - All 5 CONTEXT-line-162 scenarios covered: fresh-paid, same-status-noop, refire=true flip-flop, refire=false flip-flop, plugin-disabled.
  </acceptance_criteria>
  <done>OrderStatusWatcherTest shipped with ≥ 8 test methods locking the full PAY-03 idempotency + status-flip-flop + plugin-disabled + soft-skip-on-precondition contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 8: composer qa green — Phase 3 closure</name>
  <read_first>
    - plugins/logingrupa/metapixelshopaholic/composer.json
    - plugins/logingrupa/metapixelshopaholic/phpstan.neon (paths += event subdirectory needed? No — `classes` covers all of classes/event; PurchasePixel is in `components/` which is already scanned)
  </read_first>
  <behavior>
    - `composer qa` exits 0.
    - Total Phase-3-cumulative test count rises by at least 15 (this plan's new tests: ≥ 8 OrderStatusWatcherTest + 7 PurchasePixelTest).
    - Coverage on `classes/event/OrderStatusWatcher.php` ≥ 80%.
    - Coverage on `components/PurchasePixel.php` ≥ 80%.
    - Coverage on `Plugin.php` rises (the new boot subscription branch + the new component-registration branch are exercised by every Phase-3 component / event test).
  </behavior>
  <action>
    Run `composer pint`. Run `composer analyse`. Run `composer phpmd`. Run `composer test-cov`. Iterate.

    Common edges this plan:
    - `Plugin.php` previously had Plugin::boot() coverage 52.0% (Phase 2 02-04 baseline). With the new Event::subscribe branch + new registerComponents entry exercised by Phase-3 tests, expected ≥ 75%.
    - `classes/event/OrderStatusWatcher.php` — every branch should be covered by the 10 test methods.
    - `components/PurchasePixel.php` — every guard-chain branch covered by the 7 PurchasePixelTest methods.
    - phpstan may complain about `Status::where('id', ...)->value('code')` returning `mixed` — coerce via `(string)` or use `is_string()` guard.
    - `$obOrder->status?->code` — null-safe operator on Eloquent relation; phpstan resolves via universalObjectCratesClasses for any Toolbox ElementItem (Order is NOT in that list — it's a plain Eloquent Model). Phpstan may require a PHPDoc hint on the local variable: `/** @var string|null $sCurrentCode */ $sCurrentCode = $obOrder->status?->code;`.

    Iterate `composer qa` until exit 0.
  </action>
  <verify>
    <automated>cd plugins/logingrupa/metapixelshopaholic && composer qa</automated>
  </verify>
  <acceptance_criteria>
    - `composer qa` exits 0.
    - phpstan level 10 zero errors.
    - phpmd zero warnings.
    - OrderStatusWatcher.php coverage ≥ 80%.
    - PurchasePixel.php coverage ≥ 80%.
    - Total Phase-3 test count ≥ 67 (Phase 2 baseline 26 + Phase 3 plans ≥ 41 new tests including the 7 PurchasePixelTest cases).
  </acceptance_criteria>
  <done>composer qa exits 0. Phase 3 production code committed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 9 [BLOCKING manual]: Staging verification of PAY-10 (dedup ≥ 80% + EMQ ≥ 8) and PAY-11 (bank-transfer single-channel CAPI)</name>
  <what-built>
    Phase 3 ships the full Purchase CAPI pipeline end-to-end: a paid order fires a single Pixel-deduplicated Purchase event via the Conversions API. This is the FIRST plan that requires real Meta Events Manager observation; the automated suite mocks Guzzle and cannot verify the real-Meta-API contract.

    Production deliverables in this plan:
    - `OrderStatusWatcher` subscribed in Plugin::boot()
    - The full dispatch chain — Order status change → UUID v4 generation → DB write (meta_purchase_event_id) → PayloadBuilder envelope → SendCapiEvent queue dispatch → MetaClient POST to Graph API
    - All 5 status-flip-flop scenarios automated-test-locked

    What this checkpoint verifies (cannot be automated):
    - PAY-10: Meta Events Manager → Test Events reports Pixel + CAPI dedup ≥ 80% and EMQ ≥ 8 for `Purchase`.
    - PAY-11: Bank-transfer / admin-marked-paid order fires Purchase via CAPI only (no Pixel twin), accepted by Meta.
  </what-built>
  <how-to-verify>
    Staging environment: nailscosmetics.lv staging (or a feature branch deployed via Forge).

    Step 1 — Configure staging Settings:
    1. Backend → Settings → Meta Pixel.
    2. Set `pixel_id` = staging Meta Pixel ID (production has `2291486191076331` per PROJECT.md; use the staging-distinct pixel).
    3. Set `capi_access_token` = staging CAPI long-lived access token from Meta Events Manager → Dataset → Settings → Conversions API → Generate access token.
    4. Set `test_event_code` = test code generated under Events Manager → Test Events → "Test browser events" tab. (e.g. `TEST12345`).
    5. Save Settings.
    6. Verify backend log shows no warnings about disabled plugin.

    Step 2 — Verify PAY-10 (PayPal Pixel + CAPI dedup):
    1. **Theme integration prerequisite:** confirm `themes/logingrupa-naisstore/pages/order-complete.htm` has the `[purchasePixel] orderSlug = "{{ :slug }}"` component block declared and `{% component 'purchasePixel' %}` rendered. If missing, add per the 03-06 SUMMARY theme-integration step (Task 4 documents the snippet). Without this, the Pixel-side twin never fires — Phase 3 ships the component but theme integration is the operator step that activates it.
    2. Open the staging storefront in a fresh-incognito browser session (no `_fbp`/`_fbc` cookies pre-existing — the middleware will set them on first request).
    3. Open browser DevTools → Network → filter for `fb.me/tr` and `facebook.com/tr` to capture Pixel requests.
    4. Open Meta Events Manager → Test Events for the staging Pixel in a separate tab. Filter to the configured `test_event_code`.
    5. Place an order through to PayPal payment completion — pick a non-zero-cost item. PayPal will set the order's status to `new-payment-received` (Status ID = 5) automatically per the live PaymentMethod.after_status_id=5 wiring.
    6. Within ~60 seconds, the Test Events view MUST show TWO entries for the same Purchase event_id:
       - One Pixel event (`source: Browser`) from the PurchasePixel component (Task 4) emitting `fbq('track', 'Purchase', custom_data, {eventID})` on the thank-you page render. The eventID is the persisted `meta_purchase_event_id` UUID.
       - One CAPI event (`source: Server`) from the SendCapiEvent → MetaClient → Graph API dispatch fired by OrderStatusWatcher when PayPal IPN flipped status to `new-payment-received`.
    7. Click on the Purchase event row in Test Events. Inspect the dedup card. Required values:
       - **EMQ (Event Match Quality):** ≥ 8 (10 is highest). Each user_data field hashed correctly contributes — em + ph + fn + ln + external_id + client_ip + client_user_agent + fbp + fbc all present and well-formed should give EMQ 9–10.
       - **Dedup status:** "Successfully matched" with dedup percentage ≥ 80%. This proves the Pixel + CAPI pair share the same (event_id, event_name, event_time ±10s) tuple. The shared event_time is the `meta_purchase_event_time` column populated atomically with `meta_purchase_event_id` by OrderStatusWatcher (Task 2) — the PurchasePixel component (Task 4) reads it back into the `event_time` slot of the `fbq()` custom_data parameter.
    8. Record: `pixel_id` used, EMQ score (number), dedup percentage (number), screenshot of the Test Events row showing both source: Browser + source: Server entries paired with the same event_id.

    Step 3 — Verify PAY-11 (bank-transfer admin-flipped):
    1. Switch staging payment method back to bank transfer (Settings → Shopaholic → Payment methods → Bank transfer → `after_status_id` field should be null, NOT 5).
    2. Place a bank-transfer order on staging through to checkout completion. Order status is whatever the bank-transfer pending state is (e.g. status_id=1 or 2 — NOT 5).
    3. Verify no CAPI event fired yet (Test Events view shows no Purchase event for this order).
    4. Backend → Orders → open the new order → change Status dropdown to "New payment received" → Save. This triggers the `eloquent.updated` on Order via OrderStatusWatcher.
    5. Within ~60 seconds, Test Events should show ONE new Purchase event (CAPI only, no Pixel twin — there is no browser session for the admin flip).
    6. Verify the event row in Test Events: source = "Server" (CAPI), event_name = "Purchase", and the EMQ score is still ≥ 8 (the same user_data hashing applies from UserDataHasher::forOrder).
    7. Record: order number, status flip timestamp, CAPI event row screenshot.

    Step 4 — Verify status flip-flop no-refire (PAY-03 success criterion 3):
    1. Take the same bank-transfer order from step 3. Backend → Orders → flip status back to "Canceled" (status_id=4). Save.
    2. Within 60s — no new CAPI event should appear in Test Events.
    3. Flip status back to "New payment received". Save.
    4. Within 60s — STILL no new CAPI event should appear (refire_purchase_on_status_flip is default OFF; meta_purchase_event_id column is non-null from step 3.5).
    5. Confirm via `\DB::table('lovata_orders_shopaholic_orders')->where('id', $iOrderId)->value('meta_purchase_event_id')` returns the UUID set in step 3.5.

    Step 5 — Record findings in the 03-06 SUMMARY:
    - Pixel + CAPI EMQ score per order (PAY-10).
    - Dedup percentage per order (PAY-10 — MUST be ≥ 80%).
    - Bank-transfer admin-flipped order CAPI dispatch confirmed (PAY-11).
    - Status flip-flop no-refire confirmed (PAY-03 criterion 3).
    - Screenshots attached or pasted as base64 / linked to a staging-only Slack thread.

    Step 6 — Decision gate (BOTH conditions required for Phase 3 close per REQUIREMENTS PAY-10):
    - If EMQ < 8: Phase 3 closure BLOCKED. Surface the gap (typically missing user_data field; check UserDataHasher::compute output for the order).
    - If dedup < 80%: Phase 3 closure BLOCKED. Diagnose: confirm PurchasePixel theme block is declared on `order-complete.htm`; confirm browser DevTools shows the Pixel call AND the eventID matches the persisted `meta_purchase_event_id`; confirm event_time delta between Pixel and CAPI is < 10 seconds (Pixel reads `meta_purchase_event_time` from the row written by OrderStatusWatcher — both sides MUST use the same value).
    - If EMQ ≥ 8 AND dedup ≥ 80% AND bank-transfer flip dispatched single-channel CAPI: APPROVED.
  </how-to-verify>
  <resume-signal>
    Type one of:
    - "approved" — manual verification complete, all four steps green (EMQ ≥ 8 AND dedup ≥ 80% AND bank-transfer single-channel CAPI confirmed AND status flip-flop no-refire confirmed). Phase 3 closes.
    - Or describe specific issues found (e.g. "EMQ=5, missing fbc cookie value in payload — investigate" / "dedup=40%, PurchasePixel event_time off by 18s — check meta_purchase_event_time column read path"). The plan re-enters revision mode.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Order DB row mutation → OrderStatusWatcher | Eloquent event fires on every Order update across web + console + queue contexts. Handler must not throw (would cascade-break Order save). |
| OrderStatusWatcher → SendCapiEvent | Plugin-internal trust; payload validated by PayloadBuilder; queue worker handles delivery. |
| Plugin::boot → Event::subscribe | One-time boot-path registration; runs in every context. |
| Backend admin form → pixel_id Settings | Admin-authed write. PH-01 retro-fit adds regex defense-in-depth against stored-XSS in components/pixelhead/default.htm AND components/purchasepixel/default.htm. |
| PurchasePixel component → DOM | Inlines server-generated UUIDv4 + server-built custom_data into a `<script>fbq(...)</script>` tag on the thank-you page. event_id is provenance-trusted (server-only writer). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-26 | Tampering | meta_purchase_event_id forced reset | mitigate | Column is only written by OrderStatusWatcher via saveQuietly. Backend admin form does not bind it. PHPMD enforces no other writer. Future Phase 5 manual replay (HARD-02) is the only sanctioned alternative writer. |
| T-03-27 | Denial of Service | Infinite recursion via Order save inside Order::eloquent.updated handler | mitigate | All writes inside handlers use `saveQuietly` (suppresses model observers — Phase 2 GoodsReceivedShopaholic ApplyOrchestrator precedent). Verified by `grep -c "->save()\|\$obOrder->save(\$" classes/event/OrderStatusWatcher.php` returns 0 (only saveQuietly is used). |
| T-03-28 | Spoofing | OrderStatusWatcher dispatches Purchase from a non-paid status | mitigate | Status-fence guard: `if ($sCurrentCode !== $sPaidCode) return;`. Idempotency-fence: `if ($obOrder->meta_purchase_event_id !== null) return;`. Both must pass for dispatch. |
| T-03-29 | Information Disclosure | Plugin::boot dumping Event subscribers to logs | accept | Event::subscribe is silent registration (no log line). Only OrderStatusWatcher's own breadcrumbs go to the log sink; those carry only hashed PII / order IDs. |
| T-03-30 | Elevation of Privilege | Stored XSS via pixel_id in <script> string | mitigate | PH-01 retro-fit: regex /^\d{6,20}$/ on pixel_id field. Admin Settings is auth-gated; this is defense-in-depth against a compromised admin account or SQL injection elsewhere. |
| T-03-31 | Repudiation | Bank-transfer admin-flipped Purchase has no Pixel twin | accept | PAY-11 explicit contract: Meta accepts single-channel CAPI when no browser session exists (admin flip happens entirely in the backend with no thank-you-page render). event_id is server-generated UUIDv4 — globally unique. Dedup is not broken because there is nothing to dedup against. The PurchasePixel browser twin only fires when a user lands on the thank-you page; admin flips bypass it cleanly. |
| T-03-32 | Tampering | Queue serializer leak of Order model | mitigate | `SendCapiEvent` carries `array $arPayload` (plain array, JSON-serializable) — NOT the Order model. SerializesModels trait is included for forward compatibility (Phase 4 may pass Models) but the Phase 3 contract uses plain arrays. No model state can drift between dispatch and handle. |
| T-03-33 | Elevation of Privilege | PurchasePixel renders attacker-controlled UUID via fbq() eventID | mitigate | `event_id` is server-generated UUIDv4 in `OrderStatusWatcher::handleUpdated` via `Ramsey\Uuid\Uuid::uuid4()->toString()` and written via `saveQuietly` (T-03-26 mitigation: column is only writeable by the handler — no fillable, no backend form binding). The PurchasePixel component reads it back from the DB; no user-controlled string ever reaches the column. Defense-in-depth: Twig partial uses `e('js')` filter on event_id even though UUIDv4 is provably alphanumeric+dash. |
| T-03-34 | Spoofing | PurchasePixel fires Pixel for a non-paid order | mitigate | Component guard chain: order resolved by `secret_key` slug binding → status fence (`status->code === paid_status_code`) → idempotency-column fence (`meta_purchase_event_id` non-null) → event_time-column fence (`meta_purchase_event_time` non-null). All 4 must pass. The status fence is the same one used by OrderStatusWatcher, so Pixel can only fire when CAPI has already fired or is firing. |
| T-03-35 | Information Disclosure | PurchasePixel custom_data leaks order details to browser network logs | accept | All custom_data fields (`content_ids`, `value`, `currency`, `order_id`, `num_items`) are values the user already knows from their own checkout flow — they placed the order. user_data (em/ph/fn/ln/external_id) is intentionally OMITTED from the Pixel side (Meta CAPI spec: server-side hashes only). PII never leaves the server boundary. |

</threat_model>

<verification>
After all nine tasks complete:

```bash
cd plugins/logingrupa/metapixelshopaholic

# Files exist
[ -f classes/event/OrderStatusWatcher.php ] && \
[ -f components/PurchasePixel.php ] && \
[ -f components/purchasepixel/default.htm ] && \
[ -f tests/Feature/OrderStatusWatcherTest.php ] && \
[ -f tests/Feature/PurchasePixelTest.php ] && \
echo "OK: new files exist"

# Plugin.php has the subscribe line + component registration
grep -c "Event::subscribe(OrderStatusWatcher::class);" Plugin.php
grep -c "PurchasePixel::class => 'purchasePixel'" Plugin.php

# pixel_id validator present
grep -c "d\{6,20\}" models/settings/fields.yaml models/Settings.php

# composer qa
composer qa
```

Manual staging verification (Task 9) recorded in 03-06-SUMMARY.md.
</verification>

<success_criteria>
- `classes/event/OrderStatusWatcher.php` shipped with subscribe + handleUpdated + handleCreated + PluginGuard short-circuit + status fence + idempotency fence + UUIDv4 generation + saveQuietly of BOTH event_id AND event_time + PayloadBuilder soft-skip + SendCapiEvent dispatch + refire-flip away-transition (clears both columns).
- `components/PurchasePixel.php` + `components/purchasepixel/default.htm` shipped — browser-side Pixel twin reading persisted event_id + event_time so Meta dedups Pixel + CAPI by event_id.
- `Plugin::boot()` registers `Event::subscribe(OrderStatusWatcher::class)` BEFORE the CLI gate. `Plugin::registerComponents()` registers PurchasePixel as `purchasePixel`.
- `models/settings/fields.yaml` OR `models/Settings.php::$rules` carries the `/^\d{6,20}$/` regex for pixel_id (PH-01 retro-fit).
- `tests/MetapixelTestCase.php::bootOrdersTable` extended with `meta_purchase_event_id` AND `meta_purchase_event_time` columns; `dropHermeticSchemas` cleans all Phase-3 hermetic tables.
- `tests/Feature/OrderStatusWatcherTest.php` shipped with ≥ 8 test methods (5 CONTEXT-line-162 scenarios + 3 additional edges including event_time persistence).
- `tests/Feature/PurchasePixelTest.php` shipped with 7 test methods covering paid-order-renders, non-paid-skips, missing-event-id-skips, missing-event-time-skips, plugin-disabled-skips, order-not-found-skips, custom_data-byte-for-byte-matches-CAPI.
- `composer qa` exits 0.
- OrderStatusWatcher.php coverage ≥ 80%. PurchasePixel.php coverage ≥ 80%.
- Total Phase-3 test count ≥ 67 (≥ 60 from baseline + 7 PurchasePixelTest).
- Task 9 manual checkpoint resolved as "approved" — REQUIRES BOTH dedup ≥ 80% AND EMQ ≥ 8 verified live on staging (no deferred-dedup escape hatch — that was the plan-checker BLOCKER 2 resolution path "Ship Purchase Pixel twin in Phase 3" selected 2026-05-12).
</success_criteria>

<output>
After completion, create `.planning/phases/03-purchase-end-to-end/03-06-SUMMARY.md`. Document:
- Final test count + coverage on OrderStatusWatcher.php, PurchasePixel.php, Plugin.php.
- Manual staging verification results: EMQ score (≥ 8), dedup percentage (≥ 80%), bank-transfer admin-flip single-channel CAPI confirmation, status flip-flop no-refire confirmation. Attach or paste screenshots / log excerpts.
- Decision on the manual checkpoint outcome (approved / issues-found).
- Theme integration instructions for `order-complete.htm`: the `[purchasePixel] orderSlug = "{{ :slug }}"` component block + `{% component 'purchasePixel' %}` render line. Operator step on staging deployment; Phase 5 HARD-05 README will document this for plugin consumers.
- Open TODOs forwarded to Phase 4:
  - FUN-01: PageView CAPI twin in PixelHead.
  - FUN-02..FUN-11: Funnel component family.
  - FUN-12: event_time mirror between browser fbq() and server CAPI is already wired for Purchase via PurchasePixel + the new event_time column; Phase 4 extends the same pattern (PixelHead-style components reading persisted event_time) to ViewContent / AddToCart / etc.
- Open TODOs forwarded to Phase 5:
  - HARD-01: backend FailedEvents controller; FailedEvent rows from Phase 3 are the v1 dead-letter audit log.
  - HARD-05: README documents the theme-integration step for the PurchasePixel component on `order-complete.htm`.
- Note any test-flake re-surfacing or reflection-priming workarounds applied.
- Confirmation that Phase 3 closes all 11 PAY-* requirements with the PAY-10 dedup contract fully validated end-to-end.
</output>
