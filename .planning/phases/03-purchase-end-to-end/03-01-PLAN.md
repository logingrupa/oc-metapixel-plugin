---
id: 03-01
phase: 03-purchase-end-to-end
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - plugins/logingrupa/metapixelshopaholic/updates/add_meta_purchase_event_id_to_orders_table.php
  - plugins/logingrupa/metapixelshopaholic/updates/create_table_failed_events.php
  - plugins/logingrupa/metapixelshopaholic/updates/version.yaml
  - plugins/logingrupa/metapixelshopaholic/models/FailedEvent.php
  - plugins/logingrupa/metapixelshopaholic/tests/Feature/MigrationsBootTest.php
  - plugins/logingrupa/metapixelshopaholic/tests/Feature/FailedEventModelTest.php
  - plugins/logingrupa/metapixelshopaholic/tests/MetapixelTestCase.php
autonomous: true
requirements:
  - PAY-04
  - PAY-05
rationale: >
  Wave-1 schema foundation. Two migrations (add column to orders + create
  failed_events table) and the FailedEvent Eloquent model. Has zero
  dependencies on other Phase-3 work. Splits cleanly from the exception
  hierarchy (03-02) because no Phase-3 production code references the model
  or migration class names. Plan 03-05 (SendCapiEvent) and 03-06
  (OrderStatusWatcher) consume the model and the new column respectively.
must_haves:
  truths:
    - "A fresh test run boots the `lovata_orders_shopaholic_orders` hermetic table with a `meta_purchase_event_id VARCHAR(36) NULL` indexed column."
    - "A fresh test run boots `logingrupa_metapixel_failed_events` with all six business columns (event_id, event_name, payload, graph_error, http_status, attempts) plus an autoincrement id and timestamps."
    - "`FailedEvent::create([...])` persists a row with required-field validation enforced (missing event_id/event_name/payload/attempts is rejected)."
    - "`FailedEvent::createFromPayloadAndException(array, MetaPixelException)` returns a persisted FailedEvent row whose `payload` column is a JSON-encoded string of the input array."
    - "`composer qa` exits 0 with the new files added (pint-test clean, phpstan level 10 zero errors, phpmd zero warnings, pest test suite green)."
  artifacts:
    - path: "updates/add_meta_purchase_event_id_to_orders_table.php"
      provides: "PAY-04 migration: adds VARCHAR(36) NULL indexed column to lovata_orders_shopaholic_orders, reversible down()."
      contains: "class AddMetaPurchaseEventIdToOrdersTable extends Migration"
    - path: "updates/create_table_failed_events.php"
      provides: "PAY-05 migration: creates logingrupa_metapixel_failed_events table with indexes on event_id, event_name, http_status."
      contains: "class CreateTableFailedEvents extends Migration"
    - path: "updates/version.yaml"
      provides: "October migration registry: registers both migrations under version 1.0.1 and 1.0.2."
      contains: "add_meta_purchase_event_id_to_orders_table.php"
    - path: "models/FailedEvent.php"
      provides: "Plain Model + Validation trait + createFromPayloadAndException factory."
      contains: "class FailedEvent extends Model"
    - path: "tests/Feature/MigrationsBootTest.php"
      provides: "Asserts both migrations apply cleanly on the SQLite hermetic harness and the schema fences exist."
      contains: "Schema::hasColumn"
    - path: "tests/Feature/FailedEventModelTest.php"
      provides: "Asserts validation rules + createFromPayloadAndException factory shape."
      contains: "FailedEvent::createFromPayloadAndException"
  key_links:
    - from: "updates/add_meta_purchase_event_id_to_orders_table.php"
      to: "lovata_orders_shopaholic_orders.meta_purchase_event_id"
      via: "Schema::table + Blueprint::string(...)->nullable()->after('secret_key')->index()"
      pattern: "->after\\('secret_key'\\)->index\\(\\)"
    - from: "models/FailedEvent.php"
      to: "logingrupa_metapixel_failed_events"
      via: "public $table = 'logingrupa_metapixel_failed_events';"
      pattern: "logingrupa_metapixel_failed_events"
    - from: "updates/version.yaml"
      to: "both migration class files"
      via: "October Updates Manager loads files listed under version keys."
      pattern: "create_table_failed_events.php"
---

<objective>
Ship the Wave-1 schema foundation for Phase 3: a column on the orders table to fence Purchase-event idempotency at the database level (PAY-04), and a dead-letter sink table + model for permanently-failed CAPI events (PAY-05). Plans 03-05 and 03-06 will consume both.

Purpose: Establish the persistent-state contract before any in-memory class (MetaClient, PayloadBuilder, SendCapiEvent, OrderStatusWatcher) is written. Idempotency lives in the DB column; the dead-letter row is the v1 alerting fallback per PROJECT.md decisions.

Output: Two reversible migrations, one Eloquent model with validation + a static factory, and a `version.yaml` registry update so October's Updates Manager picks them up.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/forge/nailscosmetics.lv/plugins/logingrupa/metapixelshopaholic/CLAUDE.md
@/home/forge/nailscosmetics.lv/plugins/logingrupa/metapixelshopaholic/.planning/PROJECT.md
@/home/forge/nailscosmetics.lv/plugins/logingrupa/metapixelshopaholic/.planning/REQUIREMENTS.md
@/home/forge/nailscosmetics.lv/plugins/logingrupa/metapixelshopaholic/.planning/phases/03-purchase-end-to-end/03-CONTEXT.md
@/home/forge/nailscosmetics.lv/plugins/logingrupa/metapixelshopaholic/.planning/phases/03-purchase-end-to-end/03-PATTERNS.md
@/home/forge/nailscosmetics.lv/plugins/logingrupa/metapixelshopaholic/tests/MetapixelTestCase.php

<interfaces>
<!-- Existing Lovata.OrdersShopaholic Order property shape, read from
     plugins/lovata/ordersshopaholic/models/Order.php. The new
     meta_purchase_event_id column is appended by Task 1's migration. -->

From plugins/lovata/ordersshopaholic/models/Order.php (selected $fillable + $jsonable):
```php
$fillable = [
    'currency_id', 'secret_key', 'order_number', 'status_id',
    'email', 'phone', 'name', 'last_name', 'total_price_value',
    // (...)
];
// HasMany 'order_position' => OrderPosition::class
// BelongsTo 'currency' => Currency::class (exposes $obOrder->currency->code)
```

From plugins/logingrupa/metapixelshopaholic/tests/MetapixelTestCase.php (existing helpers):
```php
protected $autoMigrate = false;           // hermetic mode — opt-in tables only
protected function bootSystemSettings(): void;   // already provides system_settings
protected function bootOrdersStatuses(): void;   // already provides _statuses
protected function dropHermeticSchemas(): void;  // tearDown sweep
```

From plugins/logingrupa/backinstockshopaholic/models/OfferSubscriber.php (analog FailedEvent shape):
```php
class OfferSubscriber extends Model {
    use Validation;
    public $table = '...';
    public $rules = [...];
    public $fillable = [...];
    public $jsonable = [];
    public $dates = ['created_at', 'updated_at'];
}
```

From plugins/logingrupa/backinstockshopaholic/updates/version.yaml (registry shape):
```yaml
1.0.0:
    - 'Init plugin'
1.0.1:
    - 'Create table x.'
    - create_table_x.php
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add meta_purchase_event_id column migration (PAY-04) + register in version.yaml</name>
  <read_first>
    - plugins/logingrupa/metapixelshopaholic/updates/.gitkeep (confirm directory currently empty)
    - plugins/logingrupa/extendshopaholic/updates/table_update_shipping_type_add_external_id_field.php (analog: Schema::table add-column with reversible down)
    - plugins/lovata/ordersshopaholic/models/Order.php (target table = lovata_orders_shopaholic_orders; confirm `secret_key` is a $fillable column)
    - plugins/logingrupa/backinstockshopaholic/updates/version.yaml (analog: version.yaml registry shape with version keys and file references)
    - plugins/logingrupa/metapixelshopaholic/.planning/phases/03-purchase-end-to-end/03-PATTERNS.md (section: "updates/{N}_add_meta_purchase_event_id_to_orders_table.php" — lines 640-728 contain the byte-for-byte concrete excerpt)
  </read_first>
  <behavior>
    - Test: `Schema::hasColumn('lovata_orders_shopaholic_orders', 'meta_purchase_event_id') === true` after migration runs.
    - Test: column type is `VARCHAR(36)` nullable (introspected via `DB::getSchemaBuilder()->getColumnType(...)` or equivalent SQLite PRAGMA).
    - Test: `up()` is idempotent — calling twice on a table that already has the column returns without error.
    - Test: `down()` removes the column (`Schema::hasColumn(...) === false` post-rollback), idempotent on missing column.
    - Test: `version.yaml` parses as valid YAML and lists `add_meta_purchase_event_id_to_orders_table.php` under a version key (use `Symfony\Component\Yaml\Yaml::parseFile` from a one-liner pest test).
  </behavior>
  <action>
    Create `updates/add_meta_purchase_event_id_to_orders_table.php` with namespace `Logingrupa\Metapixelshopaholic\Updates`. Class name `AddMetaPurchaseEventIdToOrdersTable extends October\Rain\Database\Updates\Migration`. Two class constants: `const TABLE_NAME = 'lovata_orders_shopaholic_orders';` and `const COLUMN_NAME = 'meta_purchase_event_id';`. Use `October\Rain\Database\Schema\Blueprint` (October-namespaced, matches ExtendShopaholic precedent). `up()` body: guard via `if (!Schema::hasTable(self::TABLE_NAME) || Schema::hasColumn(self::TABLE_NAME, self::COLUMN_NAME)) { return; }`, then `Schema::table(self::TABLE_NAME, function (Blueprint $obTable) { $obTable->string(self::COLUMN_NAME, 36)->nullable()->after('secret_key')->index(); });`. `down()` body: symmetric guard plus `Schema::table(...)->dropColumn([self::COLUMN_NAME]);`. Both methods declared `: void`. Both must include docblocks describing the PAY-04 contract and noting that `->after('secret_key')` is a MySQL-only Blueprint hint (silently ignored on SQLite per PATTERNS.md "Bug-watch").

    Then update `updates/version.yaml` (create if missing): file MUST start with version `1.0.0:` listing only the comment `- 'Initial plugin scaffolding (Phase 1 + Phase 2 — no migrations).'` (no file references), followed by `1.0.1:` listing the comment `- 'Add meta_purchase_event_id column to orders table for Purchase CAPI idempotency.'` then `- add_meta_purchase_event_id_to_orders_table.php`. Two-space indentation, single quotes per project pint preset. NO trailing whitespace, file ends with a single newline.

    Acceptance gate enforced via per-task verify automated commands below.
  </action>
  <verify>
    <automated>cd plugins/logingrupa/metapixelshopaholic && grep -c "class AddMetaPurchaseEventIdToOrdersTable extends Migration" updates/add_meta_purchase_event_id_to_orders_table.php | grep -q "^1$" && grep -cE "string\(self::COLUMN_NAME, 36\)->nullable\(\)->after\('secret_key'\)->index\(\)" updates/add_meta_purchase_event_id_to_orders_table.php | grep -q "^1$" && grep -cE "dropColumn\(\[self::COLUMN_NAME\]\)" updates/add_meta_purchase_event_id_to_orders_table.php | grep -q "^1$" && grep -c "add_meta_purchase_event_id_to_orders_table.php" updates/version.yaml | grep -q "^1$" && composer analyse 2>&1 | grep -qE "No errors|0 errors"</automated>
  </verify>
  <acceptance_criteria>
    - File `updates/add_meta_purchase_event_id_to_orders_table.php` exists with namespace `Logingrupa\Metapixelshopaholic\Updates` (NOT `LoginGrupa` — CLAUDE.md namespace casing rule).
    - `grep -c "class AddMetaPurchaseEventIdToOrdersTable extends Migration" updates/add_meta_purchase_event_id_to_orders_table.php` == 1.
    - `grep -c "const TABLE_NAME = 'lovata_orders_shopaholic_orders';" updates/add_meta_purchase_event_id_to_orders_table.php` == 1.
    - `grep -c "const COLUMN_NAME = 'meta_purchase_event_id';" updates/add_meta_purchase_event_id_to_orders_table.php` == 1.
    - `grep -cE "string\(self::COLUMN_NAME, 36\)->nullable\(\)->after\('secret_key'\)->index\(\)" updates/add_meta_purchase_event_id_to_orders_table.php` == 1.
    - `grep -cE "function up\(\)\s*:\s*void" updates/add_meta_purchase_event_id_to_orders_table.php` == 1.
    - `grep -cE "function down\(\)\s*:\s*void" updates/add_meta_purchase_event_id_to_orders_table.php` == 1.
    - `grep -cE "use October\\\\Rain\\\\Database\\\\Schema\\\\Blueprint" updates/add_meta_purchase_event_id_to_orders_table.php` == 1.
    - `updates/version.yaml` parses as valid YAML and contains the key `1.0.1` with `add_meta_purchase_event_id_to_orders_table.php` as one list entry.
    - `composer analyse` exits 0 (phpstan-level-10 ignores `updates/` per `excludePaths`, but the file MUST still be syntactically valid PHP; `php -l updates/add_meta_purchase_event_id_to_orders_table.php` exits 0).
  </acceptance_criteria>
  <done>Migration class file shipped with the exact constants, namespace, and reversible up/down bodies described above. version.yaml registers the file under key `1.0.1`. composer analyse green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create failed_events table migration (PAY-05) + register in version.yaml</name>
  <read_first>
    - plugins/logingrupa/metapixelshopaholic/updates/add_meta_purchase_event_id_to_orders_table.php (just shipped — same namespace pattern)
    - plugins/logingrupa/backinstockshopaholic/updates/create_table_offersubscribers.php (analog: Schema::create with InnoDB engine, indexed columns, reversible down)
    - plugins/logingrupa/metapixelshopaholic/.planning/phases/03-purchase-end-to-end/03-CONTEXT.md (Area 4 Q1 + Q2 — payload LONGTEXT, three indexed columns)
    - plugins/logingrupa/metapixelshopaholic/.planning/phases/03-purchase-end-to-end/03-PATTERNS.md (section: "updates/{N+1}_create_table_failed_events.php" — lines 731-811)
    - plugins/logingrupa/metapixelshopaholic/updates/version.yaml (registry — Task 1 created this)
  </read_first>
  <behavior>
    - Test: `Schema::hasTable('logingrupa_metapixel_failed_events') === true` after migration runs on SQLite.
    - Test: all six business columns present (event_id, event_name, payload, graph_error, http_status, attempts), plus id + timestamps.
    - Test: `Schema::hasColumn(...)` returns true for each of the six business columns.
    - Test: `up()` idempotent (early return when table exists).
    - Test: `down()` drops the table (`Schema::hasTable(...) === false` post-rollback).
  </behavior>
  <action>
    Create `updates/create_table_failed_events.php` with namespace `Logingrupa\Metapixelshopaholic\Updates`. Class name `CreateTableFailedEvents extends October\Rain\Database\Updates\Migration`. One class constant: `const TABLE = 'logingrupa_metapixel_failed_events';`. Use `October\Rain\Database\Schema\Blueprint` (October-namespaced, consistency with Task 1).

    `up()` body: guard `if (Schema::hasTable(self::TABLE)) { return; }`. Then `Schema::create(self::TABLE, function (Blueprint $obTable): void { ... })` with the following column definitions in this exact order:
    - `$obTable->engine = 'InnoDB';`
    - `$obTable->increments('id')->unsigned();`
    - `$obTable->string('event_id', 36)->index();`
    - `$obTable->string('event_name', 64)->index();`
    - `$obTable->longText('payload');`
    - `$obTable->text('graph_error')->nullable();`
    - `$obTable->smallInteger('http_status')->unsigned()->nullable()->index();`
    - `$obTable->unsignedInteger('attempts')->default(0);`
    - `$obTable->timestamps();`

    `down()` body: `Schema::dropIfExists(self::TABLE);`. Both methods declared `: void`. Add class-level docblock describing the PAY-05 contract — dead-letter sink for `MetaApiPermanentException` from `SendCapiEvent::handle()`.

    Then append to `updates/version.yaml` after the `1.0.1:` block:
    ```
    1.0.2:
        - 'Create failed_events dead-letter table for permanently-failed CAPI events.'
        - create_table_failed_events.php
    ```
    Maintain two-space indentation, single quotes, no trailing whitespace.
  </action>
  <verify>
    <automated>cd plugins/logingrupa/metapixelshopaholic && grep -c "class CreateTableFailedEvents extends Migration" updates/create_table_failed_events.php | grep -q "^1$" && grep -c "logingrupa_metapixel_failed_events" updates/create_table_failed_events.php | grep -q "^1$" && grep -cE "string\('event_id', 36\)->index\(\)" updates/create_table_failed_events.php | grep -q "^1$" && grep -cE "longText\('payload'\)" updates/create_table_failed_events.php | grep -q "^1$" && grep -cE "smallInteger\('http_status'\)->unsigned\(\)->nullable\(\)->index\(\)" updates/create_table_failed_events.php | grep -q "^1$" && grep -c "create_table_failed_events.php" updates/version.yaml | grep -q "^1$" && php -l updates/create_table_failed_events.php | grep -q "No syntax errors"</automated>
  </verify>
  <acceptance_criteria>
    - File `updates/create_table_failed_events.php` exists with namespace `Logingrupa\Metapixelshopaholic\Updates`.
    - `grep -c "class CreateTableFailedEvents extends Migration" updates/create_table_failed_events.php` == 1.
    - `grep -c "const TABLE = 'logingrupa_metapixel_failed_events';" updates/create_table_failed_events.php` == 1.
    - Each of the eight column definitions present exactly once: `engine = 'InnoDB'`, `increments('id')`, `string('event_id', 36)->index()`, `string('event_name', 64)->index()`, `longText('payload')`, `text('graph_error')->nullable()`, `smallInteger('http_status')->unsigned()->nullable()->index()`, `unsignedInteger('attempts')->default(0)`, `timestamps()`.
    - `grep -cE "function up\(\)\s*:\s*void" updates/create_table_failed_events.php` == 1.
    - `grep -cE "function down\(\)\s*:\s*void" updates/create_table_failed_events.php` == 1.
    - `Schema::dropIfExists(self::TABLE);` appears exactly once.
    - `updates/version.yaml` contains key `1.0.2` listing `create_table_failed_events.php`.
    - `php -l updates/create_table_failed_events.php` exits 0.
  </acceptance_criteria>
  <done>Migration class shipped with all column definitions in the order specified. version.yaml registers the file under key `1.0.2`. PHP lint green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create FailedEvent model with validation rules and createFromPayloadAndException factory (PAY-05)</name>
  <read_first>
    - plugins/logingrupa/metapixelshopaholic/updates/create_table_failed_events.php (just shipped — schema is the contract this model wraps)
    - plugins/logingrupa/backinstockshopaholic/models/OfferSubscriber.php (analog: plain Model + Validation, no Toolbox Item wrapper)
    - plugins/logingrupa/metapixelshopaholic/.planning/phases/03-purchase-end-to-end/03-PATTERNS.md (section: "models/FailedEvent.php" lines 566-637)
    - plugins/logingrupa/metapixelshopaholic/.planning/phases/03-purchase-end-to-end/03-CONTEXT.md (Area 4 + Discretion #6 — createFromPayloadAndException signature)
    - plugins/logingrupa/metapixelshopaholic/phpstan.neon (level 10 requires typed methods + return types)
  </read_first>
  <behavior>
    - Test: `FailedEvent::create([...all fillable...])` persists a row with the expected column values.
    - Test: `FailedEvent::create(['event_id' => '', ...])` fails Validation (empty event_id rejected by `required|string|max:36`).
    - Test: `FailedEvent::create(['event_id' => str_repeat('x', 37), ...])` fails Validation (>36 chars rejected by `max:36`).
    - Test: `FailedEvent::create(['event_name' => str_repeat('y', 65), ...])` fails Validation (>64 chars rejected by `max:64`).
    - Test: `FailedEvent::create([...] without 'attempts')` fails Validation (`required|integer`).
    - Test: `FailedEvent::createFromPayloadAndException(['data' => [['event_id' => 'abc', 'event_name' => 'Purchase']]], $obException)` returns a persisted `FailedEvent` whose `payload` is `json_encode(['data' => [['event_id' => 'abc', 'event_name' => 'Purchase']]], JSON_UNESCAPED_SLASHES | JSON_UNESCAPED_UNICODE)`, `event_id` is `'abc'`, `event_name` is `'Purchase'`, `attempts` defaults to `0` if the exception has no `arContext['attempts']`, and `http_status` is `$obException->arContext['http_status'] ?? null`.
    - Test: `FailedEvent::createFromPayloadAndException` writes `$obException->getMessage()` to `graph_error`.
  </behavior>
  <action>
    Create `models/FailedEvent.php` with namespace `Logingrupa\Metapixelshopaholic\Models`. NO `declare(strict_types=1)` (Lovata/Logingrupa house style does not enforce — see PROJECT.md "No declare(strict_types=1) enforcement"; phpstan level 10 still applies via per-method return types).

    Class shape:
    - `use Model;` (October's facade)
    - `use October\Rain\Database\Traits\Validation;`
    - `use Logingrupa\Metapixelshopaholic\Classes\Exception\MetaPixelException;` (forward reference — concrete class lands in plan 03-02; PSR-4 autoload resolves once that plan ships; until then phpstan sees the unresolved class but Phase 3 plan ordering guarantees 03-02 ships before 03-05 needs this model in practice).
    - `class FailedEvent extends Model { use Validation; ... }`

    Properties (PHPDoc tag each for phpstan level 10 + house style):
    - `public $table = 'logingrupa_metapixel_failed_events';`
    - `public $rules = ['event_id' => 'required|string|max:36', 'event_name' => 'required|string|max:64', 'payload' => 'required|string', 'http_status' => 'nullable|integer', 'attempts' => 'required|integer'];`
    - `public $fillable = ['event_id', 'event_name', 'payload', 'graph_error', 'http_status', 'attempts'];`
    - `public $jsonable = [];` (payload is stored as longtext, not JSON-cast per Area 4 Q1)
    - `public $dates = ['created_at', 'updated_at'];`
    - `public $casts = ['http_status' => 'int', 'attempts' => 'int'];`

    Static factory method:
    ```
    public static function createFromPayloadAndException(array $arPayload, MetaPixelException $obException): self
    ```
    Body: extract `$sEventId = $arPayload['data'][0]['event_id'] ?? ''; $sEventName = $arPayload['data'][0]['event_name'] ?? '';`. Encode payload: `$sJson = json_encode($arPayload, JSON_UNESCAPED_SLASHES | JSON_UNESCAPED_UNICODE); if ($sJson === false) { $sJson = '{}'; }`. Read context: `$iHttpStatus = $obException->arContext['http_status'] ?? null; $iAttempts = (int) ($obException->arContext['attempts'] ?? 0);`. Then `return self::create(['event_id' => (string) $sEventId, 'event_name' => (string) $sEventName, 'payload' => $sJson, 'graph_error' => $obException->getMessage(), 'http_status' => is_int($iHttpStatus) ? $iHttpStatus : null, 'attempts' => $iAttempts]);`.

    Add class-level docblock: "Plain Model audit log of permanently-failed Meta CAPI events. NOT a Toolbox Item-wrapped model — admin-only, never exposed to frontend Twig (PROJECT.md key decision). Phase 5 HARD-01 ships the backend list controller against this table."

    PHPMD note: `createFromPayloadAndException` is 24 chars — passes `LongVariable max=40` rule. Class file MUST stay under 1000 LOC (PHPMD ClassFileLength rule from TOOL-03).
  </action>
  <verify>
    <automated>cd plugins/logingrupa/metapixelshopaholic && grep -c "namespace Logingrupa\\\\Metapixelshopaholic\\\\Models" models/FailedEvent.php | grep -q "^1$" && grep -c "use October\\\\Rain\\\\Database\\\\Traits\\\\Validation" models/FailedEvent.php | grep -q "^1$" && grep -c "public \$table = 'logingrupa_metapixel_failed_events';" models/FailedEvent.php | grep -q "^1$" && grep -cE "public static function createFromPayloadAndException\(array \\\$arPayload, MetaPixelException \\\$obException\): self" models/FailedEvent.php | grep -q "^1$" && grep -c "JSON_UNESCAPED_SLASHES" models/FailedEvent.php | grep -q "^1$" && php -l models/FailedEvent.php | grep -q "No syntax errors"</automated>
  </verify>
  <acceptance_criteria>
    - File `models/FailedEvent.php` exists with namespace `Logingrupa\Metapixelshopaholic\Models`.
    - `grep -c "use October\\Rain\\Database\\Traits\\Validation" models/FailedEvent.php` == 1.
    - `grep -cE "use Logingrupa\\\\Metapixelshopaholic\\\\Classes\\\\Exception\\\\MetaPixelException" models/FailedEvent.php` == 1.
    - `grep -cE "class FailedEvent extends Model" models/FailedEvent.php` == 1.
    - `grep -cE "use Validation;" models/FailedEvent.php` >= 1.
    - All five `$rules` keys present: `event_id`, `event_name`, `payload`, `http_status`, `attempts` (one grep per key with the validation string).
    - All six `$fillable` keys present: `event_id`, `event_name`, `payload`, `graph_error`, `http_status`, `attempts`.
    - `grep -cE "public static function createFromPayloadAndException\(array \\\$arPayload, MetaPixelException \\\$obException\): self" models/FailedEvent.php` == 1.
    - `grep -c "JSON_UNESCAPED_SLASHES" models/FailedEvent.php` == 1 (matches GoodsReceivedException::jsonContext encoding pattern).
    - `php -l models/FailedEvent.php` exits 0.
  </acceptance_criteria>
  <done>FailedEvent model file shipped with the documented public surface and the createFromPayloadAndException static factory. PHP lint green. (phpstan green is verified by the suite-level qa gate in Task 5; the MetaPixelException forward reference is resolved by 03-02 — plans are wave-ordered.)</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Write MigrationsBootTest + FailedEventModelTest feature tests</name>
  <read_first>
    - plugins/logingrupa/metapixelshopaholic/tests/MetapixelTestCase.php (parent — bootSystemSettings + bootOrdersStatuses + dropHermeticSchemas)
    - plugins/logingrupa/metapixelshopaholic/tests/Feature/EnsureFbpFbcCookiesTest.php (analog: SQLite hermetic + Settings + Cache flush pattern)
    - plugins/logingrupa/metapixelshopaholic/tests/Feature/BootsWithoutPixelIdTest.php (analog: triple-reset setUp pattern)
    - plugins/logingrupa/metapixelshopaholic/updates/add_meta_purchase_event_id_to_orders_table.php (Task 1)
    - plugins/logingrupa/metapixelshopaholic/updates/create_table_failed_events.php (Task 2)
    - plugins/logingrupa/metapixelshopaholic/models/FailedEvent.php (Task 3)
    - plugins/logingrupa/retrypaymentshopaholic/tests/RetryPaymentTestCase.php (hermetic lovata_orders_shopaholic_orders pattern — lines 93-115)
    - plugins/logingrupa/metapixelshopaholic/.planning/phases/03-purchase-end-to-end/03-PATTERNS.md (sections: "tests/Support/OrderFixtures.php" lines 888-934, "tests/Feature/OrderStatusWatcherTest.php" lines 937-995)
  </read_first>
  <behavior>
    MigrationsBootTest (covers PAY-04 + PAY-05 schema-existence):
    - Test `test_orders_meta_purchase_event_id_column_exists_after_migration`: provision a minimal hermetic `lovata_orders_shopaholic_orders` table via the new `bootOrdersTable()` helper (added to MetapixelTestCase as part of this task), run the Phase 3 migration manually (`(new AddMetaPurchaseEventIdToOrdersTable())->up()`), assert `Schema::hasColumn('lovata_orders_shopaholic_orders', 'meta_purchase_event_id') === true`.
    - Test `test_orders_migration_down_drops_meta_column`: run up() then down(), assert `Schema::hasColumn(...) === false`.
    - Test `test_failed_events_table_created_with_all_business_columns`: run `(new CreateTableFailedEvents())->up()`, assert `Schema::hasTable(...) === true` and `Schema::hasColumn(..., 'event_id')`, `event_name`, `payload`, `graph_error`, `http_status`, `attempts` all true.
    - Test `test_failed_events_migration_down_drops_table`: assert `Schema::hasTable(...) === false` after down().
    - Test `test_version_yaml_lists_both_migrations`: parse `updates/version.yaml` via `Symfony\Component\Yaml\Yaml::parseFile(__DIR__ . '/../../updates/version.yaml')`; assert keys `1.0.1` and `1.0.2` both present and reference the two migration filenames.

    FailedEventModelTest (covers PAY-05 model contract):
    - Test `test_create_persists_a_row_with_all_fillable_columns`: provision the failed_events table (`(new CreateTableFailedEvents())->up()`), call `FailedEvent::create([...all six...])`, assert row exists in DB and all six values match.
    - Test `test_validation_rejects_empty_event_id`: `expect(fn () => FailedEvent::create(['event_id' => '', 'event_name' => 'Purchase', 'payload' => '{}', 'attempts' => 0]))->toThrow(\Illuminate\Validation\ValidationException::class)` OR equivalent (October's Validation trait throws `ModelException` — confirm via PHPDoc / sibling-plugin test).
    - Test `test_validation_rejects_event_id_over_36_chars`: same shape with `str_repeat('x', 37)`.
    - Test `test_validation_rejects_event_name_over_64_chars`: same shape with `str_repeat('y', 65)`.
    - Test `test_create_from_payload_and_exception_encodes_payload_as_json`: build a tiny `MetaPixelException` concrete (use the actual `MetaApiPermanentException` once 03-02 ships; until then, this test class uses a local anonymous-class extension of `MetaPixelException` declared inside the test file — explicitly noted with a `// TODO 03-02: replace with MetaApiPermanentException once 03-02 ships` comment). Build `$arPayload = ['data' => [['event_id' => 'abc-123', 'event_name' => 'Purchase']]]`. Call `FailedEvent::createFromPayloadAndException($arPayload, $obException)`. Assert: `$obFailed->event_id === 'abc-123'`, `$obFailed->event_name === 'Purchase'`, `$obFailed->payload === json_encode($arPayload, JSON_UNESCAPED_SLASHES | JSON_UNESCAPED_UNICODE)`, `$obFailed->graph_error === $obException->getMessage()`.
    - Test `test_create_from_payload_and_exception_reads_http_status_from_context`: construct exception with `arContext = ['http_status' => 503, 'attempts' => 4]`, assert `$obFailed->http_status === 503` and `$obFailed->attempts === 4`.
    - Test `test_create_from_payload_and_exception_defaults_attempts_to_zero`: construct exception with empty `arContext`, assert `$obFailed->attempts === 0`.

    Both tests use the established triple-reset setUp pattern (`parent::setUp(); Cache::flush(); $this->bootSystemSettings(); $this->bootOrdersTable(); $this->bootOrdersStatuses();`). New helper `bootOrdersTable()` lives in MetapixelTestCase (added by this task — see action) and provisions the hermetic `lovata_orders_shopaholic_orders` per the RetryPaymentTestCase precedent.
  </behavior>
  <action>
    Step 1 — Extend `tests/MetapixelTestCase.php` with a new protected helper `bootOrdersTable(): void` that calls `Schema::create('lovata_orders_shopaholic_orders', function ($obTable) {...})` provisioning the minimal columns this plugin's Phase 3 work cares about: `increments('id')`, `integer('status_id')->nullable()`, `string('order_number')->nullable()`, `string('secret_key')->nullable()`, `decimal('total_price_value', 15, 2)->nullable()`, `integer('currency_id')->nullable()`, `string('email')->nullable()`, `string('phone')->nullable()`, `string('name')->nullable()`, `string('last_name')->nullable()`, `timestamps()`. Guard with `if (Schema::hasTable(...)) { return; }`. Also extend `dropHermeticSchemas()` to drop the new table (`Schema::dropIfExists('lovata_orders_shopaholic_orders')`) and `Schema::dropIfExists('logingrupa_metapixel_failed_events')` for cleanliness.

    Step 2 — Create `tests/Feature/MigrationsBootTest.php` with namespace `Logingrupa\Metapixelshopaholic\Tests\Feature`. Use the existing `require_once __DIR__.'/../MetapixelTestCase.php';` autoload bridge per existing Feature tests. Class `final class MigrationsBootTest extends MetapixelTestCase` with `setUp()` calling `parent::setUp(); Cache::flush();`. Implement the five tests described in <behavior>. Use `Symfony\Component\Yaml\Yaml::parseFile` for the version.yaml assertion (Symfony YAML is shipped with Laravel 12).

    Step 3 — Create `tests/Feature/FailedEventModelTest.php` with the seven tests described in <behavior>. For the createFromPayloadAndException tests, declare an anonymous class extending `\Logingrupa\Metapixelshopaholic\Classes\Exception\MetaPixelException` INSIDE the test file (acceptable because 03-02 ships the abstract base before 03-05 needs the model — but for THIS plan's tests, an anonymous subclass of an abstract base must be deferred until 03-02 lands, OR we use a class_exists check in setUp + skip with `markTestSkipped` if not yet present). RESOLUTION: Use Pest 4 `beforeEach(fn () => class_exists(MetaPixelException::class) || test()->markTestSkipped('MetaPixelException ships in plan 03-02'))` at the top of the createFromPayloadAndException test block. The five non-exception tests run regardless. The two exception-dependent tests skip cleanly until 03-02 ships, after which they go green automatically on the next composer test-cov run (and 03-05 + 03-06 will re-exercise them anyway). Annotate the skip with `// Once 03-02 ships MetaPixelException, this auto-runs.` so the next developer sees the bridge contract.

    Step 4 — All tests follow Hungarian notation (`$obOrder`, `$obFailed`, `$obException`, `$arPayload`, `$sJson`). Use Pest 4 `it('...', function () { ... })` OR PHPUnit `public function test_xxx(): void { ... }` — match the style of existing Feature tests (BootsWithoutPixelIdTest uses `public function test_xxx(): void`).
  </action>
  <verify>
    <automated>cd plugins/logingrupa/metapixelshopaholic && grep -cE "function bootOrdersTable\(\): void" tests/MetapixelTestCase.php | grep -q "^1$" && grep -c "lovata_orders_shopaholic_orders" tests/MetapixelTestCase.php | grep -q -E "^[2-9]$|^[1-9][0-9]+$" && grep -cE "test_orders_meta_purchase_event_id_column_exists_after_migration|test_failed_events_table_created_with_all_business_columns|test_version_yaml_lists_both_migrations" tests/Feature/MigrationsBootTest.php | grep -q "^[3-5]$" && grep -cE "test_create_persists_a_row_with_all_fillable_columns|test_create_from_payload_and_exception_encodes_payload_as_json" tests/Feature/FailedEventModelTest.php | grep -q "^[1-9]$" && composer test 2>&1 | tail -5 | grep -qE "Tests:|passed|OK"</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/Feature/MigrationsBootTest.php` exists with namespace `Logingrupa\Metapixelshopaholic\Tests\Feature` extending `MetapixelTestCase`.
    - File `tests/Feature/FailedEventModelTest.php` exists with same namespace + parent.
    - `tests/MetapixelTestCase.php` adds `bootOrdersTable(): void` helper visible to `grep -cE "function bootOrdersTable\(\): void" tests/MetapixelTestCase.php` == 1.
    - `tests/MetapixelTestCase.php::dropHermeticSchemas()` adds `Schema::dropIfExists('lovata_orders_shopaholic_orders')` and `Schema::dropIfExists('logingrupa_metapixel_failed_events')` (grep both: each >= 1).
    - All 5 MigrationsBootTest cases named exactly as in <behavior> (test_orders_meta_purchase_event_id_column_exists_after_migration, test_orders_migration_down_drops_meta_column, test_failed_events_table_created_with_all_business_columns, test_failed_events_migration_down_drops_table, test_version_yaml_lists_both_migrations).
    - All 7 FailedEventModelTest cases named exactly as in <behavior>.
    - `composer test` exits 0 with the new tests reporting as passed or skipped (the two exception-dependent FailedEventModelTest cases skip until 03-02 ships).
    - Coverage delta: total test count rises by at least 10 (5 + 7 = 12 nominal; 2 may skip pre-03-02 but still register as test nodes).
  </acceptance_criteria>
  <done>Both test files shipped with the named test methods. MetapixelTestCase gained `bootOrdersTable()`. composer test passes (12 new test entries, of which 2 may skip if 03-02 hasn't shipped yet — they auto-run once it does).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: composer qa green — pint normalize + phpstan level 10 + phpmd + pest</name>
  <read_first>
    - plugins/logingrupa/metapixelshopaholic/composer.json (the qa script chain)
    - plugins/logingrupa/metapixelshopaholic/phpstan.neon (paths + level 10 + excludePaths)
    - plugins/logingrupa/metapixelshopaholic/phpmd.xml (LongVariable max=40, ShortVariable min=4)
    - plugins/logingrupa/metapixelshopaholic/pint.json (Laravel preset + ordered_imports)
    - plugins/logingrupa/metapixelshopaholic/.planning/phases/02-skeleton-cookie-fix/02-04-SUMMARY.md ("composer qa green" pattern + Deviation #3 pint auto-import)
  </read_first>
  <behavior>
    - Test: `composer pint-test` exits 0 (no formatting drift on any new file).
    - Test: `composer analyse` exits 0 (phpstan level 10 zero errors across paths Plugin.php, models, classes, middleware, components).
    - Test: `composer phpmd` exits 0 (zero warnings on Plugin.php, classes, middleware, models, components, controllers, updates).
    - Test: `composer test-cov` exits 0 with all prior-plan tests still passing AND the new MigrationsBootTest + FailedEventModelTest tests passing or skipping cleanly.
    - Test: full `composer qa` exits 0.
  </behavior>
  <action>
    Run `composer pint` (NOT pint-test — pint with no flag applies fixes) to normalize the new files to the Laravel preset + ordered_imports + single_quote. Common drift: pint may re-order use blocks alphabetically, normalize array-bracket spacing, prefer `?array` over `array|null` in PHPDoc, prefer fully_qualified_strict_types in PHPDoc (FQN replaced with use import + short reference — see 02-04-SUMMARY Deviation #3).

    If phpstan-level-10 reports errors on the new files:
    - Common issue: missing return type on a method — fix by adding `: void` / `: self` / `: array`.
    - Common issue: untyped property — fix by declaring `@var` in PHPDoc and using `public string $table = '...';` form. NOTE: October Eloquent models conventionally declare these as plain `public $table = '...';` without type declaration to support trait composition; if phpstan complains use `/** @var string */` PHPDoc above the property.
    - Common issue: `MetaPixelException` forward-reference unresolved — phpstan WILL complain because 03-02 hasn't shipped. Acceptable resolution paths:
      (a) Add a `phpstan-ignore-line` comment with explicit reason on the `use Logingrupa\Metapixelshopaholic\Classes\Exception\MetaPixelException;` line — message MUST start with `// phpstan-ignore-line // wave-1 forward reference; resolved by plan 03-02.`
      (b) OR: ship plan 03-02 first if executing waves in parallel — plan 03-01 and 03-02 are both Wave 1 with no inter-dependency, so an execution-order choice resolves this naturally.
    Choose path (b) if both Wave-1 plans run sequentially in a single agent context; choose path (a) if Wave-1 plans run in parallel. Document the chosen path in the SUMMARY.

    If phpmd reports `LongVariable` warnings: variable names must be ≤ 40 chars. `createFromPayloadAndException` is a method name (not variable) and is not subject to LongVariable. If a parameter or local exceeds 40 chars, shorten via Hungarian abbreviation (`$arResolvedFailedEventArray` → `$arRow` works).

    If phpmd reports `UnusedFormalParameter` on `down()` — both migration methods deliberately accept no parameters; this rule is a no-op for them.

    Run `composer qa`. Iterate until zero exit.
  </action>
  <verify>
    <automated>cd plugins/logingrupa/metapixelshopaholic && composer qa 2>&1 | tail -20 | grep -qE "OK|passed|Tests:|No errors" && composer qa 2>&1 | grep -qiE "FAIL|error" && exit 1 || true; composer qa</automated>
  </verify>
  <acceptance_criteria>
    - `composer pint-test` exits 0.
    - `composer analyse` exits 0 (phpstan level 10, zero errors).
    - `composer phpmd` exits 0.
    - `composer test-cov` exits 0 (all prior tests + new tests pass; skips are acceptable for the 2 exception-dependent FailedEventModelTest cases if 03-02 not yet shipped).
    - `composer qa` exits 0 end-to-end.
    - No files outside this plan's `files_modified` list have been modified (verify via `git diff --name-only HEAD` after commit).
    - SUMMARY documents whether path (a) phpstan-ignore-line or path (b) wave-sequential-execution was used for the MetaPixelException forward reference.
  </acceptance_criteria>
  <done>composer qa exits 0. All new files normalized to project style. SUMMARY notes the MetaPixelException forward-reference resolution choice.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| backend admin → DB | Admin writes Settings + admin-marks-paid an Order. The `meta_purchase_event_id` column is server-set only (never via Settings, never via admin form input); only `OrderStatusWatcher` and the migration touch it. |
| queue worker → DB | Queue worker (run via artisan queue:work) inserts FailedEvent rows on permanent failure. Worker context is trusted (it shares the application identity). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | `meta_purchase_event_id` column | mitigate | Column is server-written only by `OrderStatusWatcher::handleUpdated/handleCreated` (plan 03-06) via `saveQuietly`. No form binding, no admin-editable field in any backend YAML config. The migration creates the column with `nullable()` so NULL is the "not yet fired" state — there is no integrity rule that depends on the column being non-null. |
| T-03-02 | Information Disclosure | `logingrupa_metapixel_failed_events.payload` LONGTEXT | accept | The payload contains hashed PII (em/ph/fn/ln/external_id are sha256-hashed by UserDataHasher in plan 03-04) plus `client_ip_address`/`client_user_agent`/`fbp`/`fbc` in plaintext per Meta CAPI spec. Plaintext IP + UA + fbp + fbc are operational metadata, not PII triggering GDPR scope changes. Backend FailedEvents list (Phase 5) is admin-auth-gated. Document in HARD-05 README runbook. |
| T-03-03 | Denial of Service | `logingrupa_metapixel_failed_events` unbounded growth | accept | A pathological permanent-failure storm (e.g. revoked CAPI token) writes one row per failed event. Volume is bounded by the order rate × dead-letter probability. Phase 5 HARD-01 ships the backend list with retention controls; v1 acceptance is "log + admin sees the row + replays manually." Out-of-scope for plan 03-01. |
| T-03-04 | Repudiation | FailedEvent row lacks "who saved it" | accept | The dead-letter path runs inside a queue worker that has no human user attribution. The `$obException->arContext['order_id']` is the audit trail. No repudiation risk because there is no user action being repudiated. |
| T-03-05 | Elevation of Privilege | `FailedEvent::create([...])` mass-assignment | mitigate | `$fillable` whitelist contains exactly the six business columns (event_id, event_name, payload, graph_error, http_status, attempts) plus framework-managed id/timestamps. No FK columns, no admin/role flags. Validation rules enforce types + max-lengths on all five non-nullable fields. |

</threat_model>

<verification>
After all five tasks complete:

```bash
cd plugins/logingrupa/metapixelshopaholic

# Migration files exist and are valid PHP
[ -f updates/add_meta_purchase_event_id_to_orders_table.php ] && \
[ -f updates/create_table_failed_events.php ] && \
[ -f updates/version.yaml ] && \
[ -f models/FailedEvent.php ] && \
[ -f tests/Feature/MigrationsBootTest.php ] && \
[ -f tests/Feature/FailedEventModelTest.php ] && echo "OK: files exist"

# composer qa exits 0
composer qa

# Test count rises by at least 10
composer test 2>&1 | grep -oE "Tests: [0-9]+" | head -1

# No untracked changes outside this plan's files_modified
git status --porcelain | grep -vE "(add_meta_purchase|create_table_failed|version\.yaml|FailedEvent\.php|MigrationsBootTest|FailedEventModelTest|MetapixelTestCase\.php|03-01-SUMMARY)" | head -5
```
</verification>

<success_criteria>
- Both migration class files shipped (PAY-04, PAY-05) with reversible up/down and indexed columns per CONTEXT Area 4 Q2.
- `updates/version.yaml` registers both migrations under `1.0.1` and `1.0.2`.
- `models/FailedEvent.php` shipped as plain Model + Validation trait (no Toolbox Item wrapper per PROJECT.md decision).
- `FailedEvent::createFromPayloadAndException(array, MetaPixelException): self` factory present (Discretion #6).
- `tests/MetapixelTestCase.php` extends `bootOrdersTable()` for hermetic `lovata_orders_shopaholic_orders` provisioning (consumed by plan 03-06).
- 5 MigrationsBootTest cases + 7 FailedEventModelTest cases shipped (2 may auto-skip until 03-02 lands).
- `composer qa` exits 0.
- Total Phase-3-cumulative test count after this plan: 26 (Phase 2 baseline) + ≥ 10 (this plan's new tests) ≥ 36.
</success_criteria>

<output>
After completion, create `.planning/phases/03-purchase-end-to-end/03-01-SUMMARY.md` per `$HOME/.claude/get-shit-done/templates/summary.md`. Document:
- Final test count delta.
- Whether MetaPixelException forward-reference was resolved by path (a) phpstan-ignore-line or path (b) wave-sequential execution.
- Any deviations in column ordering (PATTERNS.md notes SQLite ignores `->after()` silently).
- Confirmation that 02-04's Phase 2 closure invariants (PluginGuard contract + container singleton) remain intact (no Plugin.php boot changes in this plan).
- Forward-pointing surface for plan 03-05 (FailedEvent::createFromPayloadAndException is the dead-letter write path) and plan 03-06 (`bootOrdersTable()` + `meta_purchase_event_id` is the idempotency fence).
</output>
