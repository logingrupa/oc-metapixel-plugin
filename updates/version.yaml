1.0.0:
    - 'Initial plugin scaffolding (no DB).'
1.1.0:
    - 'Create plugin-owned tables — failed_events dead-letter (PAY-05 + WR-07 unique idx inline) + event_log (REFAC-02 polymorphic subject, multi-site site_id, 5-column UNIQUE race-fence, 3 read-side indices).'
    - create_table_failed_events.php
    - create_metapixel_event_log_table.php
1.1.1:
    - 'Phase 3.1-07 cross-context site_id symmetry — SiteResolver::forOrder reads Order.site_id; EventLogWriter::record accepts explicit ?int site_id; Watcher + SendCapiEvent + PurchasePixel rewired (no schema change, no migration).'
